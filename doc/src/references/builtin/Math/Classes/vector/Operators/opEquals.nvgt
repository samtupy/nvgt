/**
	Overloads the == operator, checking if two vectors are equal.
	bool opEquals(const vector &in other);
	## Arguments:
		* const vector&in other: the vector to compare.
*/

// Example:
void main() {
	vector v1(2, 3, 5);
	vector v2(5, 4, 2);
	if(v1 != v2)
	alert("Compared", "Vectors are not equal.");
}
