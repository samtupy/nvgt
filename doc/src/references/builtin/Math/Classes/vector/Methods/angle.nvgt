/**
	Returns the angle in radians between this vector and the given vector.
	float angle(const vector&in compare);
	## Arguments:
		* compare: The vector to compare against.

	## Remarks
		This is useful for checking alignment, determining how far something has turned, or field of view/targeting logic.
*/

// Example:
void main() {
	vector v1(10, 0, 0); // 10 units to the right
	vector v2(0, 10, 0);
	alert("Angle", v2.angle(v1));
}
