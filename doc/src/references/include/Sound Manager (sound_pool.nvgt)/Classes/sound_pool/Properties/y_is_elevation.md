# y_is_elevation
Toggles whether the Y coordinate is the same as Z, useful for 2Demention. By default, is retrieves from the `sound_pool_default_y_elevation` global property.

`bool sound_pool::y_is_elevation = sound_pool_default_y_elevation;`
