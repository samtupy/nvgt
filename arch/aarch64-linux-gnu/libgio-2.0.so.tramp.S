/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

#define lr x30
#define ip0 x16

  .section .note.GNU-stack,"",@progbits

  .data

  .globl _libgio_2_0_so_tramp_table
  .hidden _libgio_2_0_so_tramp_table
  .align 8
_libgio_2_0_so_tramp_table:
  .zero 17096

  .text

  .globl _libgio_2_0_so_tramp_resolve
  .hidden _libgio_2_0_so_tramp_resolve

  .globl _libgio_2_0_so_save_regs_and_resolve
  .hidden _libgio_2_0_so_save_regs_and_resolve
  .type _libgio_2_0_so_save_regs_and_resolve, %function
_libgio_2_0_so_save_regs_and_resolve:
  .cfi_startproc

  // Slow path which calls dlsym, taken only on first call.
  // Registers are saved according to "Procedure Call Standard for the Arm® 64-bit Architecture".
  // For DWARF directives, read https://www.imperialviolet.org/2017/01/18/cfi.html.

  // Stack is aligned at 16 bytes

#define PUSH_PAIR(reg1, reg2) stp reg1, reg2, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset reg1, 0; .cfi_rel_offset reg2, 8
#define POP_PAIR(reg1, reg2) ldp reg1, reg2, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore reg2; .cfi_restore reg1

#define PUSH_WIDE_PAIR(reg1, reg2) stp reg1, reg2, [sp, #-32]!; .cfi_adjust_cfa_offset 32; .cfi_rel_offset reg1, 0; .cfi_rel_offset reg2, 16
#define POP_WIDE_PAIR(reg1, reg2) ldp reg1, reg2, [sp], #32; .cfi_adjust_cfa_offset -32; .cfi_restore reg2; .cfi_restore reg1

  // Save only arguments (and lr)
  PUSH_PAIR(x0, x1)
  PUSH_PAIR(x2, x3)
  PUSH_PAIR(x4, x5)
  PUSH_PAIR(x6, x7)
  PUSH_PAIR(x8, lr)

  ldr x0, [sp, #80]  // 16*5

  PUSH_WIDE_PAIR(q0, q1)
  PUSH_WIDE_PAIR(q2, q3)
  PUSH_WIDE_PAIR(q4, q5)
  PUSH_WIDE_PAIR(q6, q7)

  // Stack is aligned at 16 bytes

  bl _libgio_2_0_so_tramp_resolve
  mov ip0, x0

  // TODO: pop pc?

  POP_WIDE_PAIR(q6, q7)
  POP_WIDE_PAIR(q4, q5)
  POP_WIDE_PAIR(q2, q3)
  POP_WIDE_PAIR(q0, q1)

  POP_PAIR(x8, lr)
  POP_PAIR(x6, x7)
  POP_PAIR(x4, x5)
  POP_PAIR(x2, x3)
  POP_PAIR(x0, x1)

  br lr

  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_activate
  .p2align 4
  .type g_action_activate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_activate
#endif
g_action_activate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+0
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+0]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 0 & 0xffff
#if 0 > 0xffff
  movk ip0, 0 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_change_state
  .p2align 4
  .type g_action_change_state, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_change_state
#endif
g_action_change_state:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1 & 0xffff
#if 1 > 0xffff
  movk ip0, 1 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_get_enabled
  .p2align 4
  .type g_action_get_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_get_enabled
#endif
g_action_get_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2 & 0xffff
#if 2 > 0xffff
  movk ip0, 2 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_get_name
  .p2align 4
  .type g_action_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_get_name
#endif
g_action_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+24
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+24]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 3 & 0xffff
#if 3 > 0xffff
  movk ip0, 3 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_get_parameter_type
  .p2align 4
  .type g_action_get_parameter_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_get_parameter_type
#endif
g_action_get_parameter_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+32
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+32]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 4 & 0xffff
#if 4 > 0xffff
  movk ip0, 4 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_get_state
  .p2align 4
  .type g_action_get_state, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_get_state
#endif
g_action_get_state:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+40
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+40]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 5 & 0xffff
#if 5 > 0xffff
  movk ip0, 5 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_get_state_hint
  .p2align 4
  .type g_action_get_state_hint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_get_state_hint
#endif
g_action_get_state_hint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+48
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+48]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 6 & 0xffff
#if 6 > 0xffff
  movk ip0, 6 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_get_state_type
  .p2align 4
  .type g_action_get_state_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_get_state_type
#endif
g_action_get_state_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+56
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+56]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 7 & 0xffff
#if 7 > 0xffff
  movk ip0, 7 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_get_type
  .p2align 4
  .type g_action_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_get_type
#endif
g_action_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+64
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+64]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 8 & 0xffff
#if 8 > 0xffff
  movk ip0, 8 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_action_added
  .p2align 4
  .type g_action_group_action_added, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_action_added
#endif
g_action_group_action_added:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+72
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+72]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 9 & 0xffff
#if 9 > 0xffff
  movk ip0, 9 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_action_enabled_changed
  .p2align 4
  .type g_action_group_action_enabled_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_action_enabled_changed
#endif
g_action_group_action_enabled_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+80
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+80]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 10 & 0xffff
#if 10 > 0xffff
  movk ip0, 10 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_action_removed
  .p2align 4
  .type g_action_group_action_removed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_action_removed
#endif
g_action_group_action_removed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+88
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+88]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 11 & 0xffff
#if 11 > 0xffff
  movk ip0, 11 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_action_state_changed
  .p2align 4
  .type g_action_group_action_state_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_action_state_changed
#endif
g_action_group_action_state_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+96
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+96]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 12 & 0xffff
#if 12 > 0xffff
  movk ip0, 12 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_activate_action
  .p2align 4
  .type g_action_group_activate_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_activate_action
#endif
g_action_group_activate_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 13 & 0xffff
#if 13 > 0xffff
  movk ip0, 13 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_change_action_state
  .p2align 4
  .type g_action_group_change_action_state, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_change_action_state
#endif
g_action_group_change_action_state:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 14 & 0xffff
#if 14 > 0xffff
  movk ip0, 14 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_get_action_enabled
  .p2align 4
  .type g_action_group_get_action_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_get_action_enabled
#endif
g_action_group_get_action_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 15 & 0xffff
#if 15 > 0xffff
  movk ip0, 15 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_get_action_parameter_type
  .p2align 4
  .type g_action_group_get_action_parameter_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_get_action_parameter_type
#endif
g_action_group_get_action_parameter_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 16 & 0xffff
#if 16 > 0xffff
  movk ip0, 16 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_get_action_state
  .p2align 4
  .type g_action_group_get_action_state, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_get_action_state
#endif
g_action_group_get_action_state:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 17 & 0xffff
#if 17 > 0xffff
  movk ip0, 17 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_get_action_state_hint
  .p2align 4
  .type g_action_group_get_action_state_hint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_get_action_state_hint
#endif
g_action_group_get_action_state_hint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 18 & 0xffff
#if 18 > 0xffff
  movk ip0, 18 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_get_action_state_type
  .p2align 4
  .type g_action_group_get_action_state_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_get_action_state_type
#endif
g_action_group_get_action_state_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 19 & 0xffff
#if 19 > 0xffff
  movk ip0, 19 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_get_type
  .p2align 4
  .type g_action_group_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_get_type
#endif
g_action_group_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 20 & 0xffff
#if 20 > 0xffff
  movk ip0, 20 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_has_action
  .p2align 4
  .type g_action_group_has_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_has_action
#endif
g_action_group_has_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 21 & 0xffff
#if 21 > 0xffff
  movk ip0, 21 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_list_actions
  .p2align 4
  .type g_action_group_list_actions, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_list_actions
#endif
g_action_group_list_actions:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 22 & 0xffff
#if 22 > 0xffff
  movk ip0, 22 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_group_query_action
  .p2align 4
  .type g_action_group_query_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_group_query_action
#endif
g_action_group_query_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 23 & 0xffff
#if 23 > 0xffff
  movk ip0, 23 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_map_add_action
  .p2align 4
  .type g_action_map_add_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_map_add_action
#endif
g_action_map_add_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 24 & 0xffff
#if 24 > 0xffff
  movk ip0, 24 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_map_add_action_entries
  .p2align 4
  .type g_action_map_add_action_entries, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_map_add_action_entries
#endif
g_action_map_add_action_entries:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 25 & 0xffff
#if 25 > 0xffff
  movk ip0, 25 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_map_get_type
  .p2align 4
  .type g_action_map_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_map_get_type
#endif
g_action_map_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 26 & 0xffff
#if 26 > 0xffff
  movk ip0, 26 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_map_lookup_action
  .p2align 4
  .type g_action_map_lookup_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_map_lookup_action
#endif
g_action_map_lookup_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 27 & 0xffff
#if 27 > 0xffff
  movk ip0, 27 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_map_remove_action
  .p2align 4
  .type g_action_map_remove_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_map_remove_action
#endif
g_action_map_remove_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 28 & 0xffff
#if 28 > 0xffff
  movk ip0, 28 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_map_remove_action_entries
  .p2align 4
  .type g_action_map_remove_action_entries, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_map_remove_action_entries
#endif
g_action_map_remove_action_entries:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 29 & 0xffff
#if 29 > 0xffff
  movk ip0, 29 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_name_is_valid
  .p2align 4
  .type g_action_name_is_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_name_is_valid
#endif
g_action_name_is_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 30 & 0xffff
#if 30 > 0xffff
  movk ip0, 30 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_parse_detailed_name
  .p2align 4
  .type g_action_parse_detailed_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_parse_detailed_name
#endif
g_action_parse_detailed_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 31 & 0xffff
#if 31 > 0xffff
  movk ip0, 31 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_action_print_detailed_name
  .p2align 4
  .type g_action_print_detailed_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_action_print_detailed_name
#endif
g_action_print_detailed_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 32 & 0xffff
#if 32 > 0xffff
  movk ip0, 32 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_add_supports_type
  .p2align 4
  .type g_app_info_add_supports_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_add_supports_type
#endif
g_app_info_add_supports_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 33 & 0xffff
#if 33 > 0xffff
  movk ip0, 33 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_can_delete
  .p2align 4
  .type g_app_info_can_delete, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_can_delete
#endif
g_app_info_can_delete:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 34 & 0xffff
#if 34 > 0xffff
  movk ip0, 34 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_can_remove_supports_type
  .p2align 4
  .type g_app_info_can_remove_supports_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_can_remove_supports_type
#endif
g_app_info_can_remove_supports_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 35 & 0xffff
#if 35 > 0xffff
  movk ip0, 35 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_create_flags_get_type
  .p2align 4
  .type g_app_info_create_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_create_flags_get_type
#endif
g_app_info_create_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 36 & 0xffff
#if 36 > 0xffff
  movk ip0, 36 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_create_from_commandline
  .p2align 4
  .type g_app_info_create_from_commandline, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_create_from_commandline
#endif
g_app_info_create_from_commandline:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 37 & 0xffff
#if 37 > 0xffff
  movk ip0, 37 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_delete
  .p2align 4
  .type g_app_info_delete, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_delete
#endif
g_app_info_delete:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 38 & 0xffff
#if 38 > 0xffff
  movk ip0, 38 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_dup
  .p2align 4
  .type g_app_info_dup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_dup
#endif
g_app_info_dup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 39 & 0xffff
#if 39 > 0xffff
  movk ip0, 39 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_equal
  .p2align 4
  .type g_app_info_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_equal
#endif
g_app_info_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 40 & 0xffff
#if 40 > 0xffff
  movk ip0, 40 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_all
  .p2align 4
  .type g_app_info_get_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_all
#endif
g_app_info_get_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 41 & 0xffff
#if 41 > 0xffff
  movk ip0, 41 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_all_for_type
  .p2align 4
  .type g_app_info_get_all_for_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_all_for_type
#endif
g_app_info_get_all_for_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 42 & 0xffff
#if 42 > 0xffff
  movk ip0, 42 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_commandline
  .p2align 4
  .type g_app_info_get_commandline, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_commandline
#endif
g_app_info_get_commandline:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 43 & 0xffff
#if 43 > 0xffff
  movk ip0, 43 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_default_for_type
  .p2align 4
  .type g_app_info_get_default_for_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_default_for_type
#endif
g_app_info_get_default_for_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 44 & 0xffff
#if 44 > 0xffff
  movk ip0, 44 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_default_for_type_async
  .p2align 4
  .type g_app_info_get_default_for_type_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_default_for_type_async
#endif
g_app_info_get_default_for_type_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 45 & 0xffff
#if 45 > 0xffff
  movk ip0, 45 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_default_for_type_finish
  .p2align 4
  .type g_app_info_get_default_for_type_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_default_for_type_finish
#endif
g_app_info_get_default_for_type_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 46 & 0xffff
#if 46 > 0xffff
  movk ip0, 46 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_default_for_uri_scheme
  .p2align 4
  .type g_app_info_get_default_for_uri_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_default_for_uri_scheme
#endif
g_app_info_get_default_for_uri_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 47 & 0xffff
#if 47 > 0xffff
  movk ip0, 47 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_default_for_uri_scheme_async
  .p2align 4
  .type g_app_info_get_default_for_uri_scheme_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_default_for_uri_scheme_async
#endif
g_app_info_get_default_for_uri_scheme_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 48 & 0xffff
#if 48 > 0xffff
  movk ip0, 48 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_default_for_uri_scheme_finish
  .p2align 4
  .type g_app_info_get_default_for_uri_scheme_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_default_for_uri_scheme_finish
#endif
g_app_info_get_default_for_uri_scheme_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 49 & 0xffff
#if 49 > 0xffff
  movk ip0, 49 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_description
  .p2align 4
  .type g_app_info_get_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_description
#endif
g_app_info_get_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 50 & 0xffff
#if 50 > 0xffff
  movk ip0, 50 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_display_name
  .p2align 4
  .type g_app_info_get_display_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_display_name
#endif
g_app_info_get_display_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 51 & 0xffff
#if 51 > 0xffff
  movk ip0, 51 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_executable
  .p2align 4
  .type g_app_info_get_executable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_executable
#endif
g_app_info_get_executable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 52 & 0xffff
#if 52 > 0xffff
  movk ip0, 52 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_fallback_for_type
  .p2align 4
  .type g_app_info_get_fallback_for_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_fallback_for_type
#endif
g_app_info_get_fallback_for_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 53 & 0xffff
#if 53 > 0xffff
  movk ip0, 53 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_icon
  .p2align 4
  .type g_app_info_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_icon
#endif
g_app_info_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 54 & 0xffff
#if 54 > 0xffff
  movk ip0, 54 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_id
  .p2align 4
  .type g_app_info_get_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_id
#endif
g_app_info_get_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 55 & 0xffff
#if 55 > 0xffff
  movk ip0, 55 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_name
  .p2align 4
  .type g_app_info_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_name
#endif
g_app_info_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 56 & 0xffff
#if 56 > 0xffff
  movk ip0, 56 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_recommended_for_type
  .p2align 4
  .type g_app_info_get_recommended_for_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_recommended_for_type
#endif
g_app_info_get_recommended_for_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 57 & 0xffff
#if 57 > 0xffff
  movk ip0, 57 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_supported_types
  .p2align 4
  .type g_app_info_get_supported_types, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_supported_types
#endif
g_app_info_get_supported_types:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 58 & 0xffff
#if 58 > 0xffff
  movk ip0, 58 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_get_type
  .p2align 4
  .type g_app_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_get_type
#endif
g_app_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 59 & 0xffff
#if 59 > 0xffff
  movk ip0, 59 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_launch
  .p2align 4
  .type g_app_info_launch, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_launch
#endif
g_app_info_launch:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 60 & 0xffff
#if 60 > 0xffff
  movk ip0, 60 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_launch_default_for_uri
  .p2align 4
  .type g_app_info_launch_default_for_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_launch_default_for_uri
#endif
g_app_info_launch_default_for_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 61 & 0xffff
#if 61 > 0xffff
  movk ip0, 61 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_launch_default_for_uri_async
  .p2align 4
  .type g_app_info_launch_default_for_uri_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_launch_default_for_uri_async
#endif
g_app_info_launch_default_for_uri_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 62 & 0xffff
#if 62 > 0xffff
  movk ip0, 62 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_launch_default_for_uri_finish
  .p2align 4
  .type g_app_info_launch_default_for_uri_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_launch_default_for_uri_finish
#endif
g_app_info_launch_default_for_uri_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 63 & 0xffff
#if 63 > 0xffff
  movk ip0, 63 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_launch_uris
  .p2align 4
  .type g_app_info_launch_uris, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_launch_uris
#endif
g_app_info_launch_uris:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 64 & 0xffff
#if 64 > 0xffff
  movk ip0, 64 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_launch_uris_async
  .p2align 4
  .type g_app_info_launch_uris_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_launch_uris_async
#endif
g_app_info_launch_uris_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 65 & 0xffff
#if 65 > 0xffff
  movk ip0, 65 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_launch_uris_finish
  .p2align 4
  .type g_app_info_launch_uris_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_launch_uris_finish
#endif
g_app_info_launch_uris_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 66 & 0xffff
#if 66 > 0xffff
  movk ip0, 66 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_monitor_get
  .p2align 4
  .type g_app_info_monitor_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_monitor_get
#endif
g_app_info_monitor_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 67 & 0xffff
#if 67 > 0xffff
  movk ip0, 67 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_monitor_get_type
  .p2align 4
  .type g_app_info_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_monitor_get_type
#endif
g_app_info_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 68 & 0xffff
#if 68 > 0xffff
  movk ip0, 68 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_remove_supports_type
  .p2align 4
  .type g_app_info_remove_supports_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_remove_supports_type
#endif
g_app_info_remove_supports_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 69 & 0xffff
#if 69 > 0xffff
  movk ip0, 69 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_reset_type_associations
  .p2align 4
  .type g_app_info_reset_type_associations, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_reset_type_associations
#endif
g_app_info_reset_type_associations:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 70 & 0xffff
#if 70 > 0xffff
  movk ip0, 70 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_set_as_default_for_extension
  .p2align 4
  .type g_app_info_set_as_default_for_extension, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_set_as_default_for_extension
#endif
g_app_info_set_as_default_for_extension:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 71 & 0xffff
#if 71 > 0xffff
  movk ip0, 71 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_set_as_default_for_type
  .p2align 4
  .type g_app_info_set_as_default_for_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_set_as_default_for_type
#endif
g_app_info_set_as_default_for_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 72 & 0xffff
#if 72 > 0xffff
  movk ip0, 72 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_set_as_last_used_for_type
  .p2align 4
  .type g_app_info_set_as_last_used_for_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_set_as_last_used_for_type
#endif
g_app_info_set_as_last_used_for_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 73 & 0xffff
#if 73 > 0xffff
  movk ip0, 73 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_should_show
  .p2align 4
  .type g_app_info_should_show, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_should_show
#endif
g_app_info_should_show:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 74 & 0xffff
#if 74 > 0xffff
  movk ip0, 74 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_supports_files
  .p2align 4
  .type g_app_info_supports_files, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_supports_files
#endif
g_app_info_supports_files:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 75 & 0xffff
#if 75 > 0xffff
  movk ip0, 75 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_info_supports_uris
  .p2align 4
  .type g_app_info_supports_uris, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_info_supports_uris
#endif
g_app_info_supports_uris:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 76 & 0xffff
#if 76 > 0xffff
  movk ip0, 76 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_get_display
  .p2align 4
  .type g_app_launch_context_get_display, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_get_display
#endif
g_app_launch_context_get_display:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 77 & 0xffff
#if 77 > 0xffff
  movk ip0, 77 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_get_environment
  .p2align 4
  .type g_app_launch_context_get_environment, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_get_environment
#endif
g_app_launch_context_get_environment:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 78 & 0xffff
#if 78 > 0xffff
  movk ip0, 78 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_get_startup_notify_id
  .p2align 4
  .type g_app_launch_context_get_startup_notify_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_get_startup_notify_id
#endif
g_app_launch_context_get_startup_notify_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 79 & 0xffff
#if 79 > 0xffff
  movk ip0, 79 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_get_type
  .p2align 4
  .type g_app_launch_context_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_get_type
#endif
g_app_launch_context_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 80 & 0xffff
#if 80 > 0xffff
  movk ip0, 80 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_launch_failed
  .p2align 4
  .type g_app_launch_context_launch_failed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_launch_failed
#endif
g_app_launch_context_launch_failed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 81 & 0xffff
#if 81 > 0xffff
  movk ip0, 81 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_new
  .p2align 4
  .type g_app_launch_context_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_new
#endif
g_app_launch_context_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 82 & 0xffff
#if 82 > 0xffff
  movk ip0, 82 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_setenv
  .p2align 4
  .type g_app_launch_context_setenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_setenv
#endif
g_app_launch_context_setenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 83 & 0xffff
#if 83 > 0xffff
  movk ip0, 83 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_app_launch_context_unsetenv
  .p2align 4
  .type g_app_launch_context_unsetenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_app_launch_context_unsetenv
#endif
g_app_launch_context_unsetenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 84 & 0xffff
#if 84 > 0xffff
  movk ip0, 84 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_activate
  .p2align 4
  .type g_application_activate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_activate
#endif
g_application_activate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 85 & 0xffff
#if 85 > 0xffff
  movk ip0, 85 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_add_main_option
  .p2align 4
  .type g_application_add_main_option, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_add_main_option
#endif
g_application_add_main_option:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 86 & 0xffff
#if 86 > 0xffff
  movk ip0, 86 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_add_main_option_entries
  .p2align 4
  .type g_application_add_main_option_entries, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_add_main_option_entries
#endif
g_application_add_main_option_entries:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 87 & 0xffff
#if 87 > 0xffff
  movk ip0, 87 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_add_option_group
  .p2align 4
  .type g_application_add_option_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_add_option_group
#endif
g_application_add_option_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 88 & 0xffff
#if 88 > 0xffff
  movk ip0, 88 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_bind_busy_property
  .p2align 4
  .type g_application_bind_busy_property, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_bind_busy_property
#endif
g_application_bind_busy_property:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 89 & 0xffff
#if 89 > 0xffff
  movk ip0, 89 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_create_file_for_arg
  .p2align 4
  .type g_application_command_line_create_file_for_arg, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_create_file_for_arg
#endif
g_application_command_line_create_file_for_arg:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 90 & 0xffff
#if 90 > 0xffff
  movk ip0, 90 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_done
  .p2align 4
  .type g_application_command_line_done, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_done
#endif
g_application_command_line_done:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 91 & 0xffff
#if 91 > 0xffff
  movk ip0, 91 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_arguments
  .p2align 4
  .type g_application_command_line_get_arguments, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_arguments
#endif
g_application_command_line_get_arguments:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 92 & 0xffff
#if 92 > 0xffff
  movk ip0, 92 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_cwd
  .p2align 4
  .type g_application_command_line_get_cwd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_cwd
#endif
g_application_command_line_get_cwd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 93 & 0xffff
#if 93 > 0xffff
  movk ip0, 93 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_environ
  .p2align 4
  .type g_application_command_line_get_environ, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_environ
#endif
g_application_command_line_get_environ:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 94 & 0xffff
#if 94 > 0xffff
  movk ip0, 94 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_exit_status
  .p2align 4
  .type g_application_command_line_get_exit_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_exit_status
#endif
g_application_command_line_get_exit_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 95 & 0xffff
#if 95 > 0xffff
  movk ip0, 95 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_is_remote
  .p2align 4
  .type g_application_command_line_get_is_remote, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_is_remote
#endif
g_application_command_line_get_is_remote:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 96 & 0xffff
#if 96 > 0xffff
  movk ip0, 96 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_options_dict
  .p2align 4
  .type g_application_command_line_get_options_dict, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_options_dict
#endif
g_application_command_line_get_options_dict:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 97 & 0xffff
#if 97 > 0xffff
  movk ip0, 97 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_platform_data
  .p2align 4
  .type g_application_command_line_get_platform_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_platform_data
#endif
g_application_command_line_get_platform_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 98 & 0xffff
#if 98 > 0xffff
  movk ip0, 98 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_stdin
  .p2align 4
  .type g_application_command_line_get_stdin, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_stdin
#endif
g_application_command_line_get_stdin:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 99 & 0xffff
#if 99 > 0xffff
  movk ip0, 99 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_get_type
  .p2align 4
  .type g_application_command_line_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_get_type
#endif
g_application_command_line_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 100 & 0xffff
#if 100 > 0xffff
  movk ip0, 100 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_getenv
  .p2align 4
  .type g_application_command_line_getenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_getenv
#endif
g_application_command_line_getenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 101 & 0xffff
#if 101 > 0xffff
  movk ip0, 101 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_print
  .p2align 4
  .type g_application_command_line_print, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_print
#endif
g_application_command_line_print:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 102 & 0xffff
#if 102 > 0xffff
  movk ip0, 102 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_print_literal
  .p2align 4
  .type g_application_command_line_print_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_print_literal
#endif
g_application_command_line_print_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 103 & 0xffff
#if 103 > 0xffff
  movk ip0, 103 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_printerr
  .p2align 4
  .type g_application_command_line_printerr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_printerr
#endif
g_application_command_line_printerr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 104 & 0xffff
#if 104 > 0xffff
  movk ip0, 104 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_printerr_literal
  .p2align 4
  .type g_application_command_line_printerr_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_printerr_literal
#endif
g_application_command_line_printerr_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 105 & 0xffff
#if 105 > 0xffff
  movk ip0, 105 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_command_line_set_exit_status
  .p2align 4
  .type g_application_command_line_set_exit_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_command_line_set_exit_status
#endif
g_application_command_line_set_exit_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 106 & 0xffff
#if 106 > 0xffff
  movk ip0, 106 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_flags_get_type
  .p2align 4
  .type g_application_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_flags_get_type
#endif
g_application_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 107 & 0xffff
#if 107 > 0xffff
  movk ip0, 107 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_application_id
  .p2align 4
  .type g_application_get_application_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_application_id
#endif
g_application_get_application_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 108 & 0xffff
#if 108 > 0xffff
  movk ip0, 108 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_dbus_connection
  .p2align 4
  .type g_application_get_dbus_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_dbus_connection
#endif
g_application_get_dbus_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 109 & 0xffff
#if 109 > 0xffff
  movk ip0, 109 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_dbus_object_path
  .p2align 4
  .type g_application_get_dbus_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_dbus_object_path
#endif
g_application_get_dbus_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 110 & 0xffff
#if 110 > 0xffff
  movk ip0, 110 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_default
  .p2align 4
  .type g_application_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_default
#endif
g_application_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 111 & 0xffff
#if 111 > 0xffff
  movk ip0, 111 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_flags
  .p2align 4
  .type g_application_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_flags
#endif
g_application_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 112 & 0xffff
#if 112 > 0xffff
  movk ip0, 112 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_inactivity_timeout
  .p2align 4
  .type g_application_get_inactivity_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_inactivity_timeout
#endif
g_application_get_inactivity_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 113 & 0xffff
#if 113 > 0xffff
  movk ip0, 113 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_is_busy
  .p2align 4
  .type g_application_get_is_busy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_is_busy
#endif
g_application_get_is_busy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 114 & 0xffff
#if 114 > 0xffff
  movk ip0, 114 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_is_registered
  .p2align 4
  .type g_application_get_is_registered, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_is_registered
#endif
g_application_get_is_registered:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 115 & 0xffff
#if 115 > 0xffff
  movk ip0, 115 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_is_remote
  .p2align 4
  .type g_application_get_is_remote, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_is_remote
#endif
g_application_get_is_remote:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 116 & 0xffff
#if 116 > 0xffff
  movk ip0, 116 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_resource_base_path
  .p2align 4
  .type g_application_get_resource_base_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_resource_base_path
#endif
g_application_get_resource_base_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 117 & 0xffff
#if 117 > 0xffff
  movk ip0, 117 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_type
  .p2align 4
  .type g_application_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_type
#endif
g_application_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 118 & 0xffff
#if 118 > 0xffff
  movk ip0, 118 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_get_version
  .p2align 4
  .type g_application_get_version, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_get_version
#endif
g_application_get_version:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 119 & 0xffff
#if 119 > 0xffff
  movk ip0, 119 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_hold
  .p2align 4
  .type g_application_hold, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_hold
#endif
g_application_hold:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 120 & 0xffff
#if 120 > 0xffff
  movk ip0, 120 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_id_is_valid
  .p2align 4
  .type g_application_id_is_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_id_is_valid
#endif
g_application_id_is_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 121 & 0xffff
#if 121 > 0xffff
  movk ip0, 121 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_mark_busy
  .p2align 4
  .type g_application_mark_busy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_mark_busy
#endif
g_application_mark_busy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 122 & 0xffff
#if 122 > 0xffff
  movk ip0, 122 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_new
  .p2align 4
  .type g_application_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_new
#endif
g_application_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 123 & 0xffff
#if 123 > 0xffff
  movk ip0, 123 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_open
  .p2align 4
  .type g_application_open, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_open
#endif
g_application_open:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 124 & 0xffff
#if 124 > 0xffff
  movk ip0, 124 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_quit
  .p2align 4
  .type g_application_quit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_quit
#endif
g_application_quit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 125 & 0xffff
#if 125 > 0xffff
  movk ip0, 125 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_register
  .p2align 4
  .type g_application_register, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_register
#endif
g_application_register:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 126 & 0xffff
#if 126 > 0xffff
  movk ip0, 126 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_release
  .p2align 4
  .type g_application_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_release
#endif
g_application_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 127 & 0xffff
#if 127 > 0xffff
  movk ip0, 127 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_run
  .p2align 4
  .type g_application_run, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_run
#endif
g_application_run:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 128 & 0xffff
#if 128 > 0xffff
  movk ip0, 128 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_send_notification
  .p2align 4
  .type g_application_send_notification, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_send_notification
#endif
g_application_send_notification:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 129 & 0xffff
#if 129 > 0xffff
  movk ip0, 129 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_action_group
  .p2align 4
  .type g_application_set_action_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_action_group
#endif
g_application_set_action_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 130 & 0xffff
#if 130 > 0xffff
  movk ip0, 130 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_application_id
  .p2align 4
  .type g_application_set_application_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_application_id
#endif
g_application_set_application_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 131 & 0xffff
#if 131 > 0xffff
  movk ip0, 131 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_default
  .p2align 4
  .type g_application_set_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_default
#endif
g_application_set_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 132 & 0xffff
#if 132 > 0xffff
  movk ip0, 132 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_flags
  .p2align 4
  .type g_application_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_flags
#endif
g_application_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 133 & 0xffff
#if 133 > 0xffff
  movk ip0, 133 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_inactivity_timeout
  .p2align 4
  .type g_application_set_inactivity_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_inactivity_timeout
#endif
g_application_set_inactivity_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 134 & 0xffff
#if 134 > 0xffff
  movk ip0, 134 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_option_context_description
  .p2align 4
  .type g_application_set_option_context_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_option_context_description
#endif
g_application_set_option_context_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 135 & 0xffff
#if 135 > 0xffff
  movk ip0, 135 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_option_context_parameter_string
  .p2align 4
  .type g_application_set_option_context_parameter_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_option_context_parameter_string
#endif
g_application_set_option_context_parameter_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 136 & 0xffff
#if 136 > 0xffff
  movk ip0, 136 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_option_context_summary
  .p2align 4
  .type g_application_set_option_context_summary, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_option_context_summary
#endif
g_application_set_option_context_summary:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 137 & 0xffff
#if 137 > 0xffff
  movk ip0, 137 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_resource_base_path
  .p2align 4
  .type g_application_set_resource_base_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_resource_base_path
#endif
g_application_set_resource_base_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 138 & 0xffff
#if 138 > 0xffff
  movk ip0, 138 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_set_version
  .p2align 4
  .type g_application_set_version, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_set_version
#endif
g_application_set_version:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 139 & 0xffff
#if 139 > 0xffff
  movk ip0, 139 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_unbind_busy_property
  .p2align 4
  .type g_application_unbind_busy_property, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_unbind_busy_property
#endif
g_application_unbind_busy_property:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 140 & 0xffff
#if 140 > 0xffff
  movk ip0, 140 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_unmark_busy
  .p2align 4
  .type g_application_unmark_busy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_unmark_busy
#endif
g_application_unmark_busy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 141 & 0xffff
#if 141 > 0xffff
  movk ip0, 141 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_application_withdraw_notification
  .p2align 4
  .type g_application_withdraw_notification, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_application_withdraw_notification
#endif
g_application_withdraw_notification:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 142 & 0xffff
#if 142 > 0xffff
  movk ip0, 142 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ask_password_flags_get_type
  .p2align 4
  .type g_ask_password_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ask_password_flags_get_type
#endif
g_ask_password_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 143 & 0xffff
#if 143 > 0xffff
  movk ip0, 143 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_initable_get_type
  .p2align 4
  .type g_async_initable_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_initable_get_type
#endif
g_async_initable_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 144 & 0xffff
#if 144 > 0xffff
  movk ip0, 144 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_initable_init_async
  .p2align 4
  .type g_async_initable_init_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_initable_init_async
#endif
g_async_initable_init_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 145 & 0xffff
#if 145 > 0xffff
  movk ip0, 145 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_initable_init_finish
  .p2align 4
  .type g_async_initable_init_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_initable_init_finish
#endif
g_async_initable_init_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 146 & 0xffff
#if 146 > 0xffff
  movk ip0, 146 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_initable_new_async
  .p2align 4
  .type g_async_initable_new_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_initable_new_async
#endif
g_async_initable_new_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 147 & 0xffff
#if 147 > 0xffff
  movk ip0, 147 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_initable_new_finish
  .p2align 4
  .type g_async_initable_new_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_initable_new_finish
#endif
g_async_initable_new_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 148 & 0xffff
#if 148 > 0xffff
  movk ip0, 148 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_initable_new_valist_async
  .p2align 4
  .type g_async_initable_new_valist_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_initable_new_valist_async
#endif
g_async_initable_new_valist_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 149 & 0xffff
#if 149 > 0xffff
  movk ip0, 149 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_initable_newv_async
  .p2align 4
  .type g_async_initable_newv_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_initable_newv_async
#endif
g_async_initable_newv_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 150 & 0xffff
#if 150 > 0xffff
  movk ip0, 150 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_result_get_source_object
  .p2align 4
  .type g_async_result_get_source_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_result_get_source_object
#endif
g_async_result_get_source_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 151 & 0xffff
#if 151 > 0xffff
  movk ip0, 151 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_result_get_type
  .p2align 4
  .type g_async_result_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_result_get_type
#endif
g_async_result_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 152 & 0xffff
#if 152 > 0xffff
  movk ip0, 152 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_result_get_user_data
  .p2align 4
  .type g_async_result_get_user_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_result_get_user_data
#endif
g_async_result_get_user_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 153 & 0xffff
#if 153 > 0xffff
  movk ip0, 153 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_result_is_tagged
  .p2align 4
  .type g_async_result_is_tagged, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_result_is_tagged
#endif
g_async_result_is_tagged:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 154 & 0xffff
#if 154 > 0xffff
  movk ip0, 154 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_result_legacy_propagate_error
  .p2align 4
  .type g_async_result_legacy_propagate_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_result_legacy_propagate_error
#endif
g_async_result_legacy_propagate_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 155 & 0xffff
#if 155 > 0xffff
  movk ip0, 155 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_fill
  .p2align 4
  .type g_buffered_input_stream_fill, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_fill
#endif
g_buffered_input_stream_fill:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 156 & 0xffff
#if 156 > 0xffff
  movk ip0, 156 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_fill_async
  .p2align 4
  .type g_buffered_input_stream_fill_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_fill_async
#endif
g_buffered_input_stream_fill_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 157 & 0xffff
#if 157 > 0xffff
  movk ip0, 157 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_fill_finish
  .p2align 4
  .type g_buffered_input_stream_fill_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_fill_finish
#endif
g_buffered_input_stream_fill_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 158 & 0xffff
#if 158 > 0xffff
  movk ip0, 158 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_get_available
  .p2align 4
  .type g_buffered_input_stream_get_available, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_get_available
#endif
g_buffered_input_stream_get_available:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 159 & 0xffff
#if 159 > 0xffff
  movk ip0, 159 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_get_buffer_size
  .p2align 4
  .type g_buffered_input_stream_get_buffer_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_get_buffer_size
#endif
g_buffered_input_stream_get_buffer_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 160 & 0xffff
#if 160 > 0xffff
  movk ip0, 160 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_get_type
  .p2align 4
  .type g_buffered_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_get_type
#endif
g_buffered_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 161 & 0xffff
#if 161 > 0xffff
  movk ip0, 161 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_new
  .p2align 4
  .type g_buffered_input_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_new
#endif
g_buffered_input_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 162 & 0xffff
#if 162 > 0xffff
  movk ip0, 162 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_new_sized
  .p2align 4
  .type g_buffered_input_stream_new_sized, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_new_sized
#endif
g_buffered_input_stream_new_sized:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 163 & 0xffff
#if 163 > 0xffff
  movk ip0, 163 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_peek
  .p2align 4
  .type g_buffered_input_stream_peek, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_peek
#endif
g_buffered_input_stream_peek:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 164 & 0xffff
#if 164 > 0xffff
  movk ip0, 164 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_peek_buffer
  .p2align 4
  .type g_buffered_input_stream_peek_buffer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_peek_buffer
#endif
g_buffered_input_stream_peek_buffer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 165 & 0xffff
#if 165 > 0xffff
  movk ip0, 165 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_read_byte
  .p2align 4
  .type g_buffered_input_stream_read_byte, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_read_byte
#endif
g_buffered_input_stream_read_byte:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 166 & 0xffff
#if 166 > 0xffff
  movk ip0, 166 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_input_stream_set_buffer_size
  .p2align 4
  .type g_buffered_input_stream_set_buffer_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_input_stream_set_buffer_size
#endif
g_buffered_input_stream_set_buffer_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 167 & 0xffff
#if 167 > 0xffff
  movk ip0, 167 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_output_stream_get_auto_grow
  .p2align 4
  .type g_buffered_output_stream_get_auto_grow, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_output_stream_get_auto_grow
#endif
g_buffered_output_stream_get_auto_grow:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 168 & 0xffff
#if 168 > 0xffff
  movk ip0, 168 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_output_stream_get_buffer_size
  .p2align 4
  .type g_buffered_output_stream_get_buffer_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_output_stream_get_buffer_size
#endif
g_buffered_output_stream_get_buffer_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 169 & 0xffff
#if 169 > 0xffff
  movk ip0, 169 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_output_stream_get_type
  .p2align 4
  .type g_buffered_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_output_stream_get_type
#endif
g_buffered_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 170 & 0xffff
#if 170 > 0xffff
  movk ip0, 170 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_output_stream_new
  .p2align 4
  .type g_buffered_output_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_output_stream_new
#endif
g_buffered_output_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 171 & 0xffff
#if 171 > 0xffff
  movk ip0, 171 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_output_stream_new_sized
  .p2align 4
  .type g_buffered_output_stream_new_sized, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_output_stream_new_sized
#endif
g_buffered_output_stream_new_sized:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 172 & 0xffff
#if 172 > 0xffff
  movk ip0, 172 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_output_stream_set_auto_grow
  .p2align 4
  .type g_buffered_output_stream_set_auto_grow, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_output_stream_set_auto_grow
#endif
g_buffered_output_stream_set_auto_grow:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 173 & 0xffff
#if 173 > 0xffff
  movk ip0, 173 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_buffered_output_stream_set_buffer_size
  .p2align 4
  .type g_buffered_output_stream_set_buffer_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_buffered_output_stream_set_buffer_size
#endif
g_buffered_output_stream_set_buffer_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 174 & 0xffff
#if 174 > 0xffff
  movk ip0, 174 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_get
  .p2align 4
  .type g_bus_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_get
#endif
g_bus_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 175 & 0xffff
#if 175 > 0xffff
  movk ip0, 175 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_get_finish
  .p2align 4
  .type g_bus_get_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_get_finish
#endif
g_bus_get_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 176 & 0xffff
#if 176 > 0xffff
  movk ip0, 176 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_get_sync
  .p2align 4
  .type g_bus_get_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_get_sync
#endif
g_bus_get_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 177 & 0xffff
#if 177 > 0xffff
  movk ip0, 177 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_name_owner_flags_get_type
  .p2align 4
  .type g_bus_name_owner_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_name_owner_flags_get_type
#endif
g_bus_name_owner_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 178 & 0xffff
#if 178 > 0xffff
  movk ip0, 178 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_name_watcher_flags_get_type
  .p2align 4
  .type g_bus_name_watcher_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_name_watcher_flags_get_type
#endif
g_bus_name_watcher_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 179 & 0xffff
#if 179 > 0xffff
  movk ip0, 179 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_own_name
  .p2align 4
  .type g_bus_own_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_own_name
#endif
g_bus_own_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 180 & 0xffff
#if 180 > 0xffff
  movk ip0, 180 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_own_name_on_connection
  .p2align 4
  .type g_bus_own_name_on_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_own_name_on_connection
#endif
g_bus_own_name_on_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 181 & 0xffff
#if 181 > 0xffff
  movk ip0, 181 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_own_name_on_connection_with_closures
  .p2align 4
  .type g_bus_own_name_on_connection_with_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_own_name_on_connection_with_closures
#endif
g_bus_own_name_on_connection_with_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 182 & 0xffff
#if 182 > 0xffff
  movk ip0, 182 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_own_name_with_closures
  .p2align 4
  .type g_bus_own_name_with_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_own_name_with_closures
#endif
g_bus_own_name_with_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 183 & 0xffff
#if 183 > 0xffff
  movk ip0, 183 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_type_get_type
  .p2align 4
  .type g_bus_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_type_get_type
#endif
g_bus_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 184 & 0xffff
#if 184 > 0xffff
  movk ip0, 184 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_unown_name
  .p2align 4
  .type g_bus_unown_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_unown_name
#endif
g_bus_unown_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 185 & 0xffff
#if 185 > 0xffff
  movk ip0, 185 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_unwatch_name
  .p2align 4
  .type g_bus_unwatch_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_unwatch_name
#endif
g_bus_unwatch_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 186 & 0xffff
#if 186 > 0xffff
  movk ip0, 186 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_watch_name
  .p2align 4
  .type g_bus_watch_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_watch_name
#endif
g_bus_watch_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 187 & 0xffff
#if 187 > 0xffff
  movk ip0, 187 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_watch_name_on_connection
  .p2align 4
  .type g_bus_watch_name_on_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_watch_name_on_connection
#endif
g_bus_watch_name_on_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 188 & 0xffff
#if 188 > 0xffff
  movk ip0, 188 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_watch_name_on_connection_with_closures
  .p2align 4
  .type g_bus_watch_name_on_connection_with_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_watch_name_on_connection_with_closures
#endif
g_bus_watch_name_on_connection_with_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 189 & 0xffff
#if 189 > 0xffff
  movk ip0, 189 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bus_watch_name_with_closures
  .p2align 4
  .type g_bus_watch_name_with_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bus_watch_name_with_closures
#endif
g_bus_watch_name_with_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 190 & 0xffff
#if 190 > 0xffff
  movk ip0, 190 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_icon_get_bytes
  .p2align 4
  .type g_bytes_icon_get_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_icon_get_bytes
#endif
g_bytes_icon_get_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 191 & 0xffff
#if 191 > 0xffff
  movk ip0, 191 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_icon_get_type
  .p2align 4
  .type g_bytes_icon_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_icon_get_type
#endif
g_bytes_icon_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 192 & 0xffff
#if 192 > 0xffff
  movk ip0, 192 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_icon_new
  .p2align 4
  .type g_bytes_icon_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_icon_new
#endif
g_bytes_icon_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 193 & 0xffff
#if 193 > 0xffff
  movk ip0, 193 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_cancel
  .p2align 4
  .type g_cancellable_cancel, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_cancel
#endif
g_cancellable_cancel:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 194 & 0xffff
#if 194 > 0xffff
  movk ip0, 194 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_connect
  .p2align 4
  .type g_cancellable_connect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_connect
#endif
g_cancellable_connect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 195 & 0xffff
#if 195 > 0xffff
  movk ip0, 195 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_disconnect
  .p2align 4
  .type g_cancellable_disconnect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_disconnect
#endif
g_cancellable_disconnect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 196 & 0xffff
#if 196 > 0xffff
  movk ip0, 196 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_get_current
  .p2align 4
  .type g_cancellable_get_current, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_get_current
#endif
g_cancellable_get_current:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 197 & 0xffff
#if 197 > 0xffff
  movk ip0, 197 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_get_fd
  .p2align 4
  .type g_cancellable_get_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_get_fd
#endif
g_cancellable_get_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 198 & 0xffff
#if 198 > 0xffff
  movk ip0, 198 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_get_type
  .p2align 4
  .type g_cancellable_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_get_type
#endif
g_cancellable_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 199 & 0xffff
#if 199 > 0xffff
  movk ip0, 199 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_is_cancelled
  .p2align 4
  .type g_cancellable_is_cancelled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_is_cancelled
#endif
g_cancellable_is_cancelled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 200 & 0xffff
#if 200 > 0xffff
  movk ip0, 200 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_make_pollfd
  .p2align 4
  .type g_cancellable_make_pollfd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_make_pollfd
#endif
g_cancellable_make_pollfd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 201 & 0xffff
#if 201 > 0xffff
  movk ip0, 201 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_new
  .p2align 4
  .type g_cancellable_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_new
#endif
g_cancellable_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 202 & 0xffff
#if 202 > 0xffff
  movk ip0, 202 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_pop_current
  .p2align 4
  .type g_cancellable_pop_current, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_pop_current
#endif
g_cancellable_pop_current:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 203 & 0xffff
#if 203 > 0xffff
  movk ip0, 203 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_push_current
  .p2align 4
  .type g_cancellable_push_current, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_push_current
#endif
g_cancellable_push_current:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 204 & 0xffff
#if 204 > 0xffff
  movk ip0, 204 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_release_fd
  .p2align 4
  .type g_cancellable_release_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_release_fd
#endif
g_cancellable_release_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 205 & 0xffff
#if 205 > 0xffff
  movk ip0, 205 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_reset
  .p2align 4
  .type g_cancellable_reset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_reset
#endif
g_cancellable_reset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 206 & 0xffff
#if 206 > 0xffff
  movk ip0, 206 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_set_error_if_cancelled
  .p2align 4
  .type g_cancellable_set_error_if_cancelled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_set_error_if_cancelled
#endif
g_cancellable_set_error_if_cancelled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 207 & 0xffff
#if 207 > 0xffff
  movk ip0, 207 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cancellable_source_new
  .p2align 4
  .type g_cancellable_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cancellable_source_new
#endif
g_cancellable_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 208 & 0xffff
#if 208 > 0xffff
  movk ip0, 208 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_charset_converter_get_num_fallbacks
  .p2align 4
  .type g_charset_converter_get_num_fallbacks, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_charset_converter_get_num_fallbacks
#endif
g_charset_converter_get_num_fallbacks:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 209 & 0xffff
#if 209 > 0xffff
  movk ip0, 209 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_charset_converter_get_type
  .p2align 4
  .type g_charset_converter_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_charset_converter_get_type
#endif
g_charset_converter_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 210 & 0xffff
#if 210 > 0xffff
  movk ip0, 210 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_charset_converter_get_use_fallback
  .p2align 4
  .type g_charset_converter_get_use_fallback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_charset_converter_get_use_fallback
#endif
g_charset_converter_get_use_fallback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 211 & 0xffff
#if 211 > 0xffff
  movk ip0, 211 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_charset_converter_new
  .p2align 4
  .type g_charset_converter_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_charset_converter_new
#endif
g_charset_converter_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 212 & 0xffff
#if 212 > 0xffff
  movk ip0, 212 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_charset_converter_set_use_fallback
  .p2align 4
  .type g_charset_converter_set_use_fallback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_charset_converter_set_use_fallback
#endif
g_charset_converter_set_use_fallback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 213 & 0xffff
#if 213 > 0xffff
  movk ip0, 213 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_can_be_executable
  .p2align 4
  .type g_content_type_can_be_executable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_can_be_executable
#endif
g_content_type_can_be_executable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 214 & 0xffff
#if 214 > 0xffff
  movk ip0, 214 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_equals
  .p2align 4
  .type g_content_type_equals, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_equals
#endif
g_content_type_equals:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 215 & 0xffff
#if 215 > 0xffff
  movk ip0, 215 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_from_mime_type
  .p2align 4
  .type g_content_type_from_mime_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_from_mime_type
#endif
g_content_type_from_mime_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 216 & 0xffff
#if 216 > 0xffff
  movk ip0, 216 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_get_description
  .p2align 4
  .type g_content_type_get_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_get_description
#endif
g_content_type_get_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 217 & 0xffff
#if 217 > 0xffff
  movk ip0, 217 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_get_generic_icon_name
  .p2align 4
  .type g_content_type_get_generic_icon_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_get_generic_icon_name
#endif
g_content_type_get_generic_icon_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 218 & 0xffff
#if 218 > 0xffff
  movk ip0, 218 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_get_icon
  .p2align 4
  .type g_content_type_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_get_icon
#endif
g_content_type_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 219 & 0xffff
#if 219 > 0xffff
  movk ip0, 219 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_get_mime_dirs
  .p2align 4
  .type g_content_type_get_mime_dirs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_get_mime_dirs
#endif
g_content_type_get_mime_dirs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 220 & 0xffff
#if 220 > 0xffff
  movk ip0, 220 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_get_mime_type
  .p2align 4
  .type g_content_type_get_mime_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_get_mime_type
#endif
g_content_type_get_mime_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 221 & 0xffff
#if 221 > 0xffff
  movk ip0, 221 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_get_symbolic_icon
  .p2align 4
  .type g_content_type_get_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_get_symbolic_icon
#endif
g_content_type_get_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 222 & 0xffff
#if 222 > 0xffff
  movk ip0, 222 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_guess
  .p2align 4
  .type g_content_type_guess, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_guess
#endif
g_content_type_guess:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 223 & 0xffff
#if 223 > 0xffff
  movk ip0, 223 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_guess_for_tree
  .p2align 4
  .type g_content_type_guess_for_tree, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_guess_for_tree
#endif
g_content_type_guess_for_tree:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 224 & 0xffff
#if 224 > 0xffff
  movk ip0, 224 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_is_a
  .p2align 4
  .type g_content_type_is_a, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_is_a
#endif
g_content_type_is_a:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 225 & 0xffff
#if 225 > 0xffff
  movk ip0, 225 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_is_mime_type
  .p2align 4
  .type g_content_type_is_mime_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_is_mime_type
#endif
g_content_type_is_mime_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 226 & 0xffff
#if 226 > 0xffff
  movk ip0, 226 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_is_unknown
  .p2align 4
  .type g_content_type_is_unknown, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_is_unknown
#endif
g_content_type_is_unknown:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 227 & 0xffff
#if 227 > 0xffff
  movk ip0, 227 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_type_set_mime_dirs
  .p2align 4
  .type g_content_type_set_mime_dirs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_type_set_mime_dirs
#endif
g_content_type_set_mime_dirs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 228 & 0xffff
#if 228 > 0xffff
  movk ip0, 228 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_content_types_get_registered
  .p2align 4
  .type g_content_types_get_registered, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_content_types_get_registered
#endif
g_content_types_get_registered:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 229 & 0xffff
#if 229 > 0xffff
  movk ip0, 229 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_convert
  .p2align 4
  .type g_converter_convert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_convert
#endif
g_converter_convert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 230 & 0xffff
#if 230 > 0xffff
  movk ip0, 230 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_convert_bytes
  .p2align 4
  .type g_converter_convert_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_convert_bytes
#endif
g_converter_convert_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 231 & 0xffff
#if 231 > 0xffff
  movk ip0, 231 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_flags_get_type
  .p2align 4
  .type g_converter_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_flags_get_type
#endif
g_converter_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 232 & 0xffff
#if 232 > 0xffff
  movk ip0, 232 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_get_type
  .p2align 4
  .type g_converter_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_get_type
#endif
g_converter_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 233 & 0xffff
#if 233 > 0xffff
  movk ip0, 233 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_input_stream_get_converter
  .p2align 4
  .type g_converter_input_stream_get_converter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_input_stream_get_converter
#endif
g_converter_input_stream_get_converter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 234 & 0xffff
#if 234 > 0xffff
  movk ip0, 234 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_input_stream_get_type
  .p2align 4
  .type g_converter_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_input_stream_get_type
#endif
g_converter_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 235 & 0xffff
#if 235 > 0xffff
  movk ip0, 235 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_input_stream_new
  .p2align 4
  .type g_converter_input_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_input_stream_new
#endif
g_converter_input_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 236 & 0xffff
#if 236 > 0xffff
  movk ip0, 236 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_output_stream_get_converter
  .p2align 4
  .type g_converter_output_stream_get_converter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_output_stream_get_converter
#endif
g_converter_output_stream_get_converter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 237 & 0xffff
#if 237 > 0xffff
  movk ip0, 237 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_output_stream_get_type
  .p2align 4
  .type g_converter_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_output_stream_get_type
#endif
g_converter_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 238 & 0xffff
#if 238 > 0xffff
  movk ip0, 238 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_output_stream_new
  .p2align 4
  .type g_converter_output_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_output_stream_new
#endif
g_converter_output_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 239 & 0xffff
#if 239 > 0xffff
  movk ip0, 239 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_reset
  .p2align 4
  .type g_converter_reset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_reset
#endif
g_converter_reset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 240 & 0xffff
#if 240 > 0xffff
  movk ip0, 240 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_converter_result_get_type
  .p2align 4
  .type g_converter_result_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_converter_result_get_type
#endif
g_converter_result_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 241 & 0xffff
#if 241 > 0xffff
  movk ip0, 241 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_get_native
  .p2align 4
  .type g_credentials_get_native, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_get_native
#endif
g_credentials_get_native:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 242 & 0xffff
#if 242 > 0xffff
  movk ip0, 242 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_get_type
  .p2align 4
  .type g_credentials_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_get_type
#endif
g_credentials_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 243 & 0xffff
#if 243 > 0xffff
  movk ip0, 243 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_get_unix_pid
  .p2align 4
  .type g_credentials_get_unix_pid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_get_unix_pid
#endif
g_credentials_get_unix_pid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 244 & 0xffff
#if 244 > 0xffff
  movk ip0, 244 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_get_unix_user
  .p2align 4
  .type g_credentials_get_unix_user, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_get_unix_user
#endif
g_credentials_get_unix_user:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 245 & 0xffff
#if 245 > 0xffff
  movk ip0, 245 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_is_same_user
  .p2align 4
  .type g_credentials_is_same_user, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_is_same_user
#endif
g_credentials_is_same_user:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 246 & 0xffff
#if 246 > 0xffff
  movk ip0, 246 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_new
  .p2align 4
  .type g_credentials_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_new
#endif
g_credentials_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 247 & 0xffff
#if 247 > 0xffff
  movk ip0, 247 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_set_native
  .p2align 4
  .type g_credentials_set_native, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_set_native
#endif
g_credentials_set_native:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 248 & 0xffff
#if 248 > 0xffff
  movk ip0, 248 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_set_unix_user
  .p2align 4
  .type g_credentials_set_unix_user, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_set_unix_user
#endif
g_credentials_set_unix_user:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+1992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+1992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 249 & 0xffff
#if 249 > 0xffff
  movk ip0, 249 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_to_string
  .p2align 4
  .type g_credentials_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_to_string
#endif
g_credentials_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 250 & 0xffff
#if 250 > 0xffff
  movk ip0, 250 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_credentials_type_get_type
  .p2align 4
  .type g_credentials_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_credentials_type_get_type
#endif
g_credentials_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 251 & 0xffff
#if 251 > 0xffff
  movk ip0, 251 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_get_byte_order
  .p2align 4
  .type g_data_input_stream_get_byte_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_get_byte_order
#endif
g_data_input_stream_get_byte_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 252 & 0xffff
#if 252 > 0xffff
  movk ip0, 252 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_get_newline_type
  .p2align 4
  .type g_data_input_stream_get_newline_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_get_newline_type
#endif
g_data_input_stream_get_newline_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 253 & 0xffff
#if 253 > 0xffff
  movk ip0, 253 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_get_type
  .p2align 4
  .type g_data_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_get_type
#endif
g_data_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 254 & 0xffff
#if 254 > 0xffff
  movk ip0, 254 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_new
  .p2align 4
  .type g_data_input_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_new
#endif
g_data_input_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 255 & 0xffff
#if 255 > 0xffff
  movk ip0, 255 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_byte
  .p2align 4
  .type g_data_input_stream_read_byte, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_byte
#endif
g_data_input_stream_read_byte:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 256 & 0xffff
#if 256 > 0xffff
  movk ip0, 256 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_int16
  .p2align 4
  .type g_data_input_stream_read_int16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_int16
#endif
g_data_input_stream_read_int16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 257 & 0xffff
#if 257 > 0xffff
  movk ip0, 257 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_int32
  .p2align 4
  .type g_data_input_stream_read_int32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_int32
#endif
g_data_input_stream_read_int32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 258 & 0xffff
#if 258 > 0xffff
  movk ip0, 258 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_int64
  .p2align 4
  .type g_data_input_stream_read_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_int64
#endif
g_data_input_stream_read_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 259 & 0xffff
#if 259 > 0xffff
  movk ip0, 259 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_line
  .p2align 4
  .type g_data_input_stream_read_line, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_line
#endif
g_data_input_stream_read_line:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 260 & 0xffff
#if 260 > 0xffff
  movk ip0, 260 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_line_async
  .p2align 4
  .type g_data_input_stream_read_line_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_line_async
#endif
g_data_input_stream_read_line_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 261 & 0xffff
#if 261 > 0xffff
  movk ip0, 261 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_line_finish
  .p2align 4
  .type g_data_input_stream_read_line_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_line_finish
#endif
g_data_input_stream_read_line_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 262 & 0xffff
#if 262 > 0xffff
  movk ip0, 262 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_line_finish_utf8
  .p2align 4
  .type g_data_input_stream_read_line_finish_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_line_finish_utf8
#endif
g_data_input_stream_read_line_finish_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 263 & 0xffff
#if 263 > 0xffff
  movk ip0, 263 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_line_utf8
  .p2align 4
  .type g_data_input_stream_read_line_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_line_utf8
#endif
g_data_input_stream_read_line_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 264 & 0xffff
#if 264 > 0xffff
  movk ip0, 264 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_uint16
  .p2align 4
  .type g_data_input_stream_read_uint16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_uint16
#endif
g_data_input_stream_read_uint16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 265 & 0xffff
#if 265 > 0xffff
  movk ip0, 265 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_uint32
  .p2align 4
  .type g_data_input_stream_read_uint32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_uint32
#endif
g_data_input_stream_read_uint32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 266 & 0xffff
#if 266 > 0xffff
  movk ip0, 266 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_uint64
  .p2align 4
  .type g_data_input_stream_read_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_uint64
#endif
g_data_input_stream_read_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 267 & 0xffff
#if 267 > 0xffff
  movk ip0, 267 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_until
  .p2align 4
  .type g_data_input_stream_read_until, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_until
#endif
g_data_input_stream_read_until:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 268 & 0xffff
#if 268 > 0xffff
  movk ip0, 268 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_until_async
  .p2align 4
  .type g_data_input_stream_read_until_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_until_async
#endif
g_data_input_stream_read_until_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 269 & 0xffff
#if 269 > 0xffff
  movk ip0, 269 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_until_finish
  .p2align 4
  .type g_data_input_stream_read_until_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_until_finish
#endif
g_data_input_stream_read_until_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 270 & 0xffff
#if 270 > 0xffff
  movk ip0, 270 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_upto
  .p2align 4
  .type g_data_input_stream_read_upto, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_upto
#endif
g_data_input_stream_read_upto:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 271 & 0xffff
#if 271 > 0xffff
  movk ip0, 271 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_upto_async
  .p2align 4
  .type g_data_input_stream_read_upto_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_upto_async
#endif
g_data_input_stream_read_upto_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 272 & 0xffff
#if 272 > 0xffff
  movk ip0, 272 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_read_upto_finish
  .p2align 4
  .type g_data_input_stream_read_upto_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_read_upto_finish
#endif
g_data_input_stream_read_upto_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 273 & 0xffff
#if 273 > 0xffff
  movk ip0, 273 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_set_byte_order
  .p2align 4
  .type g_data_input_stream_set_byte_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_set_byte_order
#endif
g_data_input_stream_set_byte_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 274 & 0xffff
#if 274 > 0xffff
  movk ip0, 274 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_input_stream_set_newline_type
  .p2align 4
  .type g_data_input_stream_set_newline_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_input_stream_set_newline_type
#endif
g_data_input_stream_set_newline_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 275 & 0xffff
#if 275 > 0xffff
  movk ip0, 275 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_get_byte_order
  .p2align 4
  .type g_data_output_stream_get_byte_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_get_byte_order
#endif
g_data_output_stream_get_byte_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 276 & 0xffff
#if 276 > 0xffff
  movk ip0, 276 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_get_type
  .p2align 4
  .type g_data_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_get_type
#endif
g_data_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 277 & 0xffff
#if 277 > 0xffff
  movk ip0, 277 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_new
  .p2align 4
  .type g_data_output_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_new
#endif
g_data_output_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 278 & 0xffff
#if 278 > 0xffff
  movk ip0, 278 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_byte
  .p2align 4
  .type g_data_output_stream_put_byte, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_byte
#endif
g_data_output_stream_put_byte:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 279 & 0xffff
#if 279 > 0xffff
  movk ip0, 279 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_int16
  .p2align 4
  .type g_data_output_stream_put_int16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_int16
#endif
g_data_output_stream_put_int16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 280 & 0xffff
#if 280 > 0xffff
  movk ip0, 280 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_int32
  .p2align 4
  .type g_data_output_stream_put_int32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_int32
#endif
g_data_output_stream_put_int32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 281 & 0xffff
#if 281 > 0xffff
  movk ip0, 281 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_int64
  .p2align 4
  .type g_data_output_stream_put_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_int64
#endif
g_data_output_stream_put_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 282 & 0xffff
#if 282 > 0xffff
  movk ip0, 282 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_string
  .p2align 4
  .type g_data_output_stream_put_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_string
#endif
g_data_output_stream_put_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 283 & 0xffff
#if 283 > 0xffff
  movk ip0, 283 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_uint16
  .p2align 4
  .type g_data_output_stream_put_uint16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_uint16
#endif
g_data_output_stream_put_uint16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 284 & 0xffff
#if 284 > 0xffff
  movk ip0, 284 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_uint32
  .p2align 4
  .type g_data_output_stream_put_uint32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_uint32
#endif
g_data_output_stream_put_uint32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 285 & 0xffff
#if 285 > 0xffff
  movk ip0, 285 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_put_uint64
  .p2align 4
  .type g_data_output_stream_put_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_put_uint64
#endif
g_data_output_stream_put_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 286 & 0xffff
#if 286 > 0xffff
  movk ip0, 286 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_output_stream_set_byte_order
  .p2align 4
  .type g_data_output_stream_set_byte_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_output_stream_set_byte_order
#endif
g_data_output_stream_set_byte_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 287 & 0xffff
#if 287 > 0xffff
  movk ip0, 287 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_stream_byte_order_get_type
  .p2align 4
  .type g_data_stream_byte_order_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_stream_byte_order_get_type
#endif
g_data_stream_byte_order_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 288 & 0xffff
#if 288 > 0xffff
  movk ip0, 288 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_data_stream_newline_type_get_type
  .p2align 4
  .type g_data_stream_newline_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_data_stream_newline_type_get_type
#endif
g_data_stream_newline_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 289 & 0xffff
#if 289 > 0xffff
  movk ip0, 289 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datagram_based_condition_check
  .p2align 4
  .type g_datagram_based_condition_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datagram_based_condition_check
#endif
g_datagram_based_condition_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 290 & 0xffff
#if 290 > 0xffff
  movk ip0, 290 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datagram_based_condition_wait
  .p2align 4
  .type g_datagram_based_condition_wait, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datagram_based_condition_wait
#endif
g_datagram_based_condition_wait:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 291 & 0xffff
#if 291 > 0xffff
  movk ip0, 291 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datagram_based_create_source
  .p2align 4
  .type g_datagram_based_create_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datagram_based_create_source
#endif
g_datagram_based_create_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 292 & 0xffff
#if 292 > 0xffff
  movk ip0, 292 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datagram_based_get_type
  .p2align 4
  .type g_datagram_based_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datagram_based_get_type
#endif
g_datagram_based_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 293 & 0xffff
#if 293 > 0xffff
  movk ip0, 293 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datagram_based_receive_messages
  .p2align 4
  .type g_datagram_based_receive_messages, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datagram_based_receive_messages
#endif
g_datagram_based_receive_messages:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 294 & 0xffff
#if 294 > 0xffff
  movk ip0, 294 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datagram_based_send_messages
  .p2align 4
  .type g_datagram_based_send_messages, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datagram_based_send_messages
#endif
g_datagram_based_send_messages:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 295 & 0xffff
#if 295 > 0xffff
  movk ip0, 295 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_action_group_get
  .p2align 4
  .type g_dbus_action_group_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_action_group_get
#endif
g_dbus_action_group_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 296 & 0xffff
#if 296 > 0xffff
  movk ip0, 296 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_action_group_get_type
  .p2align 4
  .type g_dbus_action_group_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_action_group_get_type
#endif
g_dbus_action_group_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 297 & 0xffff
#if 297 > 0xffff
  movk ip0, 297 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_address_escape_value
  .p2align 4
  .type g_dbus_address_escape_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_address_escape_value
#endif
g_dbus_address_escape_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 298 & 0xffff
#if 298 > 0xffff
  movk ip0, 298 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_address_get_for_bus_sync
  .p2align 4
  .type g_dbus_address_get_for_bus_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_address_get_for_bus_sync
#endif
g_dbus_address_get_for_bus_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 299 & 0xffff
#if 299 > 0xffff
  movk ip0, 299 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_address_get_stream
  .p2align 4
  .type g_dbus_address_get_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_address_get_stream
#endif
g_dbus_address_get_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 300 & 0xffff
#if 300 > 0xffff
  movk ip0, 300 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_address_get_stream_finish
  .p2align 4
  .type g_dbus_address_get_stream_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_address_get_stream_finish
#endif
g_dbus_address_get_stream_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 301 & 0xffff
#if 301 > 0xffff
  movk ip0, 301 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_address_get_stream_sync
  .p2align 4
  .type g_dbus_address_get_stream_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_address_get_stream_sync
#endif
g_dbus_address_get_stream_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 302 & 0xffff
#if 302 > 0xffff
  movk ip0, 302 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_annotation_info_get_type
  .p2align 4
  .type g_dbus_annotation_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_annotation_info_get_type
#endif
g_dbus_annotation_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 303 & 0xffff
#if 303 > 0xffff
  movk ip0, 303 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_annotation_info_lookup
  .p2align 4
  .type g_dbus_annotation_info_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_annotation_info_lookup
#endif
g_dbus_annotation_info_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 304 & 0xffff
#if 304 > 0xffff
  movk ip0, 304 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_annotation_info_ref
  .p2align 4
  .type g_dbus_annotation_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_annotation_info_ref
#endif
g_dbus_annotation_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 305 & 0xffff
#if 305 > 0xffff
  movk ip0, 305 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_annotation_info_unref
  .p2align 4
  .type g_dbus_annotation_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_annotation_info_unref
#endif
g_dbus_annotation_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 306 & 0xffff
#if 306 > 0xffff
  movk ip0, 306 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_arg_info_get_type
  .p2align 4
  .type g_dbus_arg_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_arg_info_get_type
#endif
g_dbus_arg_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 307 & 0xffff
#if 307 > 0xffff
  movk ip0, 307 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_arg_info_ref
  .p2align 4
  .type g_dbus_arg_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_arg_info_ref
#endif
g_dbus_arg_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 308 & 0xffff
#if 308 > 0xffff
  movk ip0, 308 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_arg_info_unref
  .p2align 4
  .type g_dbus_arg_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_arg_info_unref
#endif
g_dbus_arg_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 309 & 0xffff
#if 309 > 0xffff
  movk ip0, 309 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_auth_observer_allow_mechanism
  .p2align 4
  .type g_dbus_auth_observer_allow_mechanism, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_auth_observer_allow_mechanism
#endif
g_dbus_auth_observer_allow_mechanism:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 310 & 0xffff
#if 310 > 0xffff
  movk ip0, 310 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_auth_observer_authorize_authenticated_peer
  .p2align 4
  .type g_dbus_auth_observer_authorize_authenticated_peer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_auth_observer_authorize_authenticated_peer
#endif
g_dbus_auth_observer_authorize_authenticated_peer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 311 & 0xffff
#if 311 > 0xffff
  movk ip0, 311 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_auth_observer_get_type
  .p2align 4
  .type g_dbus_auth_observer_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_auth_observer_get_type
#endif
g_dbus_auth_observer_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 312 & 0xffff
#if 312 > 0xffff
  movk ip0, 312 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_auth_observer_new
  .p2align 4
  .type g_dbus_auth_observer_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_auth_observer_new
#endif
g_dbus_auth_observer_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 313 & 0xffff
#if 313 > 0xffff
  movk ip0, 313 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_call_flags_get_type
  .p2align 4
  .type g_dbus_call_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_call_flags_get_type
#endif
g_dbus_call_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 314 & 0xffff
#if 314 > 0xffff
  movk ip0, 314 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_capability_flags_get_type
  .p2align 4
  .type g_dbus_capability_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_capability_flags_get_type
#endif
g_dbus_capability_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 315 & 0xffff
#if 315 > 0xffff
  movk ip0, 315 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_add_filter
  .p2align 4
  .type g_dbus_connection_add_filter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_add_filter
#endif
g_dbus_connection_add_filter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 316 & 0xffff
#if 316 > 0xffff
  movk ip0, 316 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_call
  .p2align 4
  .type g_dbus_connection_call, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_call
#endif
g_dbus_connection_call:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 317 & 0xffff
#if 317 > 0xffff
  movk ip0, 317 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_call_finish
  .p2align 4
  .type g_dbus_connection_call_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_call_finish
#endif
g_dbus_connection_call_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 318 & 0xffff
#if 318 > 0xffff
  movk ip0, 318 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_call_sync
  .p2align 4
  .type g_dbus_connection_call_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_call_sync
#endif
g_dbus_connection_call_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 319 & 0xffff
#if 319 > 0xffff
  movk ip0, 319 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_call_with_unix_fd_list
  .p2align 4
  .type g_dbus_connection_call_with_unix_fd_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_call_with_unix_fd_list
#endif
g_dbus_connection_call_with_unix_fd_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 320 & 0xffff
#if 320 > 0xffff
  movk ip0, 320 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_call_with_unix_fd_list_finish
  .p2align 4
  .type g_dbus_connection_call_with_unix_fd_list_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_call_with_unix_fd_list_finish
#endif
g_dbus_connection_call_with_unix_fd_list_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 321 & 0xffff
#if 321 > 0xffff
  movk ip0, 321 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_call_with_unix_fd_list_sync
  .p2align 4
  .type g_dbus_connection_call_with_unix_fd_list_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_call_with_unix_fd_list_sync
#endif
g_dbus_connection_call_with_unix_fd_list_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 322 & 0xffff
#if 322 > 0xffff
  movk ip0, 322 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_close
  .p2align 4
  .type g_dbus_connection_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_close
#endif
g_dbus_connection_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 323 & 0xffff
#if 323 > 0xffff
  movk ip0, 323 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_close_finish
  .p2align 4
  .type g_dbus_connection_close_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_close_finish
#endif
g_dbus_connection_close_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 324 & 0xffff
#if 324 > 0xffff
  movk ip0, 324 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_close_sync
  .p2align 4
  .type g_dbus_connection_close_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_close_sync
#endif
g_dbus_connection_close_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 325 & 0xffff
#if 325 > 0xffff
  movk ip0, 325 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_emit_signal
  .p2align 4
  .type g_dbus_connection_emit_signal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_emit_signal
#endif
g_dbus_connection_emit_signal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 326 & 0xffff
#if 326 > 0xffff
  movk ip0, 326 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_export_action_group
  .p2align 4
  .type g_dbus_connection_export_action_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_export_action_group
#endif
g_dbus_connection_export_action_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 327 & 0xffff
#if 327 > 0xffff
  movk ip0, 327 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_export_menu_model
  .p2align 4
  .type g_dbus_connection_export_menu_model, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_export_menu_model
#endif
g_dbus_connection_export_menu_model:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 328 & 0xffff
#if 328 > 0xffff
  movk ip0, 328 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_flags_get_type
  .p2align 4
  .type g_dbus_connection_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_flags_get_type
#endif
g_dbus_connection_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 329 & 0xffff
#if 329 > 0xffff
  movk ip0, 329 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_flush
  .p2align 4
  .type g_dbus_connection_flush, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_flush
#endif
g_dbus_connection_flush:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 330 & 0xffff
#if 330 > 0xffff
  movk ip0, 330 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_flush_finish
  .p2align 4
  .type g_dbus_connection_flush_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_flush_finish
#endif
g_dbus_connection_flush_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 331 & 0xffff
#if 331 > 0xffff
  movk ip0, 331 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_flush_sync
  .p2align 4
  .type g_dbus_connection_flush_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_flush_sync
#endif
g_dbus_connection_flush_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 332 & 0xffff
#if 332 > 0xffff
  movk ip0, 332 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_capabilities
  .p2align 4
  .type g_dbus_connection_get_capabilities, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_capabilities
#endif
g_dbus_connection_get_capabilities:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 333 & 0xffff
#if 333 > 0xffff
  movk ip0, 333 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_exit_on_close
  .p2align 4
  .type g_dbus_connection_get_exit_on_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_exit_on_close
#endif
g_dbus_connection_get_exit_on_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 334 & 0xffff
#if 334 > 0xffff
  movk ip0, 334 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_flags
  .p2align 4
  .type g_dbus_connection_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_flags
#endif
g_dbus_connection_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 335 & 0xffff
#if 335 > 0xffff
  movk ip0, 335 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_guid
  .p2align 4
  .type g_dbus_connection_get_guid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_guid
#endif
g_dbus_connection_get_guid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 336 & 0xffff
#if 336 > 0xffff
  movk ip0, 336 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_last_serial
  .p2align 4
  .type g_dbus_connection_get_last_serial, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_last_serial
#endif
g_dbus_connection_get_last_serial:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 337 & 0xffff
#if 337 > 0xffff
  movk ip0, 337 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_peer_credentials
  .p2align 4
  .type g_dbus_connection_get_peer_credentials, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_peer_credentials
#endif
g_dbus_connection_get_peer_credentials:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 338 & 0xffff
#if 338 > 0xffff
  movk ip0, 338 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_stream
  .p2align 4
  .type g_dbus_connection_get_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_stream
#endif
g_dbus_connection_get_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 339 & 0xffff
#if 339 > 0xffff
  movk ip0, 339 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_type
  .p2align 4
  .type g_dbus_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_type
#endif
g_dbus_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 340 & 0xffff
#if 340 > 0xffff
  movk ip0, 340 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_get_unique_name
  .p2align 4
  .type g_dbus_connection_get_unique_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_get_unique_name
#endif
g_dbus_connection_get_unique_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 341 & 0xffff
#if 341 > 0xffff
  movk ip0, 341 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_is_closed
  .p2align 4
  .type g_dbus_connection_is_closed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_is_closed
#endif
g_dbus_connection_is_closed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 342 & 0xffff
#if 342 > 0xffff
  movk ip0, 342 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_new
  .p2align 4
  .type g_dbus_connection_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_new
#endif
g_dbus_connection_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 343 & 0xffff
#if 343 > 0xffff
  movk ip0, 343 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_new_finish
  .p2align 4
  .type g_dbus_connection_new_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_new_finish
#endif
g_dbus_connection_new_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 344 & 0xffff
#if 344 > 0xffff
  movk ip0, 344 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_new_for_address
  .p2align 4
  .type g_dbus_connection_new_for_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_new_for_address
#endif
g_dbus_connection_new_for_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 345 & 0xffff
#if 345 > 0xffff
  movk ip0, 345 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_new_for_address_finish
  .p2align 4
  .type g_dbus_connection_new_for_address_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_new_for_address_finish
#endif
g_dbus_connection_new_for_address_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 346 & 0xffff
#if 346 > 0xffff
  movk ip0, 346 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_new_for_address_sync
  .p2align 4
  .type g_dbus_connection_new_for_address_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_new_for_address_sync
#endif
g_dbus_connection_new_for_address_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 347 & 0xffff
#if 347 > 0xffff
  movk ip0, 347 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_new_sync
  .p2align 4
  .type g_dbus_connection_new_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_new_sync
#endif
g_dbus_connection_new_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 348 & 0xffff
#if 348 > 0xffff
  movk ip0, 348 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_register_object
  .p2align 4
  .type g_dbus_connection_register_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_register_object
#endif
g_dbus_connection_register_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 349 & 0xffff
#if 349 > 0xffff
  movk ip0, 349 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_register_object_with_closures
  .p2align 4
  .type g_dbus_connection_register_object_with_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_register_object_with_closures
#endif
g_dbus_connection_register_object_with_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 350 & 0xffff
#if 350 > 0xffff
  movk ip0, 350 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_register_object_with_closures2
  .p2align 4
  .type g_dbus_connection_register_object_with_closures2, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_register_object_with_closures2
#endif
g_dbus_connection_register_object_with_closures2:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 351 & 0xffff
#if 351 > 0xffff
  movk ip0, 351 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_register_subtree
  .p2align 4
  .type g_dbus_connection_register_subtree, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_register_subtree
#endif
g_dbus_connection_register_subtree:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 352 & 0xffff
#if 352 > 0xffff
  movk ip0, 352 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_remove_filter
  .p2align 4
  .type g_dbus_connection_remove_filter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_remove_filter
#endif
g_dbus_connection_remove_filter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 353 & 0xffff
#if 353 > 0xffff
  movk ip0, 353 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_send_message
  .p2align 4
  .type g_dbus_connection_send_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_send_message
#endif
g_dbus_connection_send_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 354 & 0xffff
#if 354 > 0xffff
  movk ip0, 354 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_send_message_with_reply
  .p2align 4
  .type g_dbus_connection_send_message_with_reply, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_send_message_with_reply
#endif
g_dbus_connection_send_message_with_reply:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 355 & 0xffff
#if 355 > 0xffff
  movk ip0, 355 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_send_message_with_reply_finish
  .p2align 4
  .type g_dbus_connection_send_message_with_reply_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_send_message_with_reply_finish
#endif
g_dbus_connection_send_message_with_reply_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 356 & 0xffff
#if 356 > 0xffff
  movk ip0, 356 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_send_message_with_reply_sync
  .p2align 4
  .type g_dbus_connection_send_message_with_reply_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_send_message_with_reply_sync
#endif
g_dbus_connection_send_message_with_reply_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 357 & 0xffff
#if 357 > 0xffff
  movk ip0, 357 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_set_exit_on_close
  .p2align 4
  .type g_dbus_connection_set_exit_on_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_set_exit_on_close
#endif
g_dbus_connection_set_exit_on_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 358 & 0xffff
#if 358 > 0xffff
  movk ip0, 358 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_signal_subscribe
  .p2align 4
  .type g_dbus_connection_signal_subscribe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_signal_subscribe
#endif
g_dbus_connection_signal_subscribe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 359 & 0xffff
#if 359 > 0xffff
  movk ip0, 359 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_signal_unsubscribe
  .p2align 4
  .type g_dbus_connection_signal_unsubscribe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_signal_unsubscribe
#endif
g_dbus_connection_signal_unsubscribe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 360 & 0xffff
#if 360 > 0xffff
  movk ip0, 360 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_start_message_processing
  .p2align 4
  .type g_dbus_connection_start_message_processing, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_start_message_processing
#endif
g_dbus_connection_start_message_processing:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 361 & 0xffff
#if 361 > 0xffff
  movk ip0, 361 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_unexport_action_group
  .p2align 4
  .type g_dbus_connection_unexport_action_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_unexport_action_group
#endif
g_dbus_connection_unexport_action_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 362 & 0xffff
#if 362 > 0xffff
  movk ip0, 362 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_unexport_menu_model
  .p2align 4
  .type g_dbus_connection_unexport_menu_model, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_unexport_menu_model
#endif
g_dbus_connection_unexport_menu_model:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 363 & 0xffff
#if 363 > 0xffff
  movk ip0, 363 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_unregister_object
  .p2align 4
  .type g_dbus_connection_unregister_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_unregister_object
#endif
g_dbus_connection_unregister_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 364 & 0xffff
#if 364 > 0xffff
  movk ip0, 364 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_connection_unregister_subtree
  .p2align 4
  .type g_dbus_connection_unregister_subtree, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_connection_unregister_subtree
#endif
g_dbus_connection_unregister_subtree:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 365 & 0xffff
#if 365 > 0xffff
  movk ip0, 365 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_encode_gerror
  .p2align 4
  .type g_dbus_error_encode_gerror, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_encode_gerror
#endif
g_dbus_error_encode_gerror:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 366 & 0xffff
#if 366 > 0xffff
  movk ip0, 366 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_get_remote_error
  .p2align 4
  .type g_dbus_error_get_remote_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_get_remote_error
#endif
g_dbus_error_get_remote_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 367 & 0xffff
#if 367 > 0xffff
  movk ip0, 367 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_get_type
  .p2align 4
  .type g_dbus_error_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_get_type
#endif
g_dbus_error_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 368 & 0xffff
#if 368 > 0xffff
  movk ip0, 368 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_is_remote_error
  .p2align 4
  .type g_dbus_error_is_remote_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_is_remote_error
#endif
g_dbus_error_is_remote_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 369 & 0xffff
#if 369 > 0xffff
  movk ip0, 369 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_new_for_dbus_error
  .p2align 4
  .type g_dbus_error_new_for_dbus_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_new_for_dbus_error
#endif
g_dbus_error_new_for_dbus_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 370 & 0xffff
#if 370 > 0xffff
  movk ip0, 370 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_quark
  .p2align 4
  .type g_dbus_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_quark
#endif
g_dbus_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 371 & 0xffff
#if 371 > 0xffff
  movk ip0, 371 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_register_error
  .p2align 4
  .type g_dbus_error_register_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_register_error
#endif
g_dbus_error_register_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 372 & 0xffff
#if 372 > 0xffff
  movk ip0, 372 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_register_error_domain
  .p2align 4
  .type g_dbus_error_register_error_domain, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_register_error_domain
#endif
g_dbus_error_register_error_domain:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 373 & 0xffff
#if 373 > 0xffff
  movk ip0, 373 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_set_dbus_error
  .p2align 4
  .type g_dbus_error_set_dbus_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_set_dbus_error
#endif
g_dbus_error_set_dbus_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+2992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+2992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 374 & 0xffff
#if 374 > 0xffff
  movk ip0, 374 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_set_dbus_error_valist
  .p2align 4
  .type g_dbus_error_set_dbus_error_valist, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_set_dbus_error_valist
#endif
g_dbus_error_set_dbus_error_valist:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 375 & 0xffff
#if 375 > 0xffff
  movk ip0, 375 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_strip_remote_error
  .p2align 4
  .type g_dbus_error_strip_remote_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_strip_remote_error
#endif
g_dbus_error_strip_remote_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 376 & 0xffff
#if 376 > 0xffff
  movk ip0, 376 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_error_unregister_error
  .p2align 4
  .type g_dbus_error_unregister_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_error_unregister_error
#endif
g_dbus_error_unregister_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 377 & 0xffff
#if 377 > 0xffff
  movk ip0, 377 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_escape_object_path
  .p2align 4
  .type g_dbus_escape_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_escape_object_path
#endif
g_dbus_escape_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 378 & 0xffff
#if 378 > 0xffff
  movk ip0, 378 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_escape_object_path_bytestring
  .p2align 4
  .type g_dbus_escape_object_path_bytestring, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_escape_object_path_bytestring
#endif
g_dbus_escape_object_path_bytestring:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 379 & 0xffff
#if 379 > 0xffff
  movk ip0, 379 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_generate_guid
  .p2align 4
  .type g_dbus_generate_guid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_generate_guid
#endif
g_dbus_generate_guid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 380 & 0xffff
#if 380 > 0xffff
  movk ip0, 380 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_gvalue_to_gvariant
  .p2align 4
  .type g_dbus_gvalue_to_gvariant, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_gvalue_to_gvariant
#endif
g_dbus_gvalue_to_gvariant:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 381 & 0xffff
#if 381 > 0xffff
  movk ip0, 381 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_gvariant_to_gvalue
  .p2align 4
  .type g_dbus_gvariant_to_gvalue, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_gvariant_to_gvalue
#endif
g_dbus_gvariant_to_gvalue:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 382 & 0xffff
#if 382 > 0xffff
  movk ip0, 382 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_dup_object
  .p2align 4
  .type g_dbus_interface_dup_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_dup_object
#endif
g_dbus_interface_dup_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 383 & 0xffff
#if 383 > 0xffff
  movk ip0, 383 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_get_info
  .p2align 4
  .type g_dbus_interface_get_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_get_info
#endif
g_dbus_interface_get_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 384 & 0xffff
#if 384 > 0xffff
  movk ip0, 384 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_get_object
  .p2align 4
  .type g_dbus_interface_get_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_get_object
#endif
g_dbus_interface_get_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 385 & 0xffff
#if 385 > 0xffff
  movk ip0, 385 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_get_type
  .p2align 4
  .type g_dbus_interface_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_get_type
#endif
g_dbus_interface_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 386 & 0xffff
#if 386 > 0xffff
  movk ip0, 386 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_cache_build
  .p2align 4
  .type g_dbus_interface_info_cache_build, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_cache_build
#endif
g_dbus_interface_info_cache_build:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 387 & 0xffff
#if 387 > 0xffff
  movk ip0, 387 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_cache_release
  .p2align 4
  .type g_dbus_interface_info_cache_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_cache_release
#endif
g_dbus_interface_info_cache_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 388 & 0xffff
#if 388 > 0xffff
  movk ip0, 388 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_generate_xml
  .p2align 4
  .type g_dbus_interface_info_generate_xml, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_generate_xml
#endif
g_dbus_interface_info_generate_xml:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 389 & 0xffff
#if 389 > 0xffff
  movk ip0, 389 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_get_type
  .p2align 4
  .type g_dbus_interface_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_get_type
#endif
g_dbus_interface_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 390 & 0xffff
#if 390 > 0xffff
  movk ip0, 390 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_lookup_method
  .p2align 4
  .type g_dbus_interface_info_lookup_method, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_lookup_method
#endif
g_dbus_interface_info_lookup_method:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 391 & 0xffff
#if 391 > 0xffff
  movk ip0, 391 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_lookup_property
  .p2align 4
  .type g_dbus_interface_info_lookup_property, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_lookup_property
#endif
g_dbus_interface_info_lookup_property:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 392 & 0xffff
#if 392 > 0xffff
  movk ip0, 392 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_lookup_signal
  .p2align 4
  .type g_dbus_interface_info_lookup_signal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_lookup_signal
#endif
g_dbus_interface_info_lookup_signal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 393 & 0xffff
#if 393 > 0xffff
  movk ip0, 393 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_ref
  .p2align 4
  .type g_dbus_interface_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_ref
#endif
g_dbus_interface_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 394 & 0xffff
#if 394 > 0xffff
  movk ip0, 394 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_info_unref
  .p2align 4
  .type g_dbus_interface_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_info_unref
#endif
g_dbus_interface_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 395 & 0xffff
#if 395 > 0xffff
  movk ip0, 395 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_set_object
  .p2align 4
  .type g_dbus_interface_set_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_set_object
#endif
g_dbus_interface_set_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 396 & 0xffff
#if 396 > 0xffff
  movk ip0, 396 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_export
  .p2align 4
  .type g_dbus_interface_skeleton_export, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_export
#endif
g_dbus_interface_skeleton_export:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 397 & 0xffff
#if 397 > 0xffff
  movk ip0, 397 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_flags_get_type
  .p2align 4
  .type g_dbus_interface_skeleton_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_flags_get_type
#endif
g_dbus_interface_skeleton_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 398 & 0xffff
#if 398 > 0xffff
  movk ip0, 398 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_flush
  .p2align 4
  .type g_dbus_interface_skeleton_flush, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_flush
#endif
g_dbus_interface_skeleton_flush:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 399 & 0xffff
#if 399 > 0xffff
  movk ip0, 399 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_connection
  .p2align 4
  .type g_dbus_interface_skeleton_get_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_connection
#endif
g_dbus_interface_skeleton_get_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 400 & 0xffff
#if 400 > 0xffff
  movk ip0, 400 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_connections
  .p2align 4
  .type g_dbus_interface_skeleton_get_connections, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_connections
#endif
g_dbus_interface_skeleton_get_connections:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 401 & 0xffff
#if 401 > 0xffff
  movk ip0, 401 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_flags
  .p2align 4
  .type g_dbus_interface_skeleton_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_flags
#endif
g_dbus_interface_skeleton_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 402 & 0xffff
#if 402 > 0xffff
  movk ip0, 402 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_info
  .p2align 4
  .type g_dbus_interface_skeleton_get_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_info
#endif
g_dbus_interface_skeleton_get_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 403 & 0xffff
#if 403 > 0xffff
  movk ip0, 403 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_object_path
  .p2align 4
  .type g_dbus_interface_skeleton_get_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_object_path
#endif
g_dbus_interface_skeleton_get_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 404 & 0xffff
#if 404 > 0xffff
  movk ip0, 404 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_properties
  .p2align 4
  .type g_dbus_interface_skeleton_get_properties, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_properties
#endif
g_dbus_interface_skeleton_get_properties:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 405 & 0xffff
#if 405 > 0xffff
  movk ip0, 405 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_type
  .p2align 4
  .type g_dbus_interface_skeleton_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_type
#endif
g_dbus_interface_skeleton_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 406 & 0xffff
#if 406 > 0xffff
  movk ip0, 406 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_get_vtable
  .p2align 4
  .type g_dbus_interface_skeleton_get_vtable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_get_vtable
#endif
g_dbus_interface_skeleton_get_vtable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 407 & 0xffff
#if 407 > 0xffff
  movk ip0, 407 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_has_connection
  .p2align 4
  .type g_dbus_interface_skeleton_has_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_has_connection
#endif
g_dbus_interface_skeleton_has_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 408 & 0xffff
#if 408 > 0xffff
  movk ip0, 408 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_set_flags
  .p2align 4
  .type g_dbus_interface_skeleton_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_set_flags
#endif
g_dbus_interface_skeleton_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 409 & 0xffff
#if 409 > 0xffff
  movk ip0, 409 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_unexport
  .p2align 4
  .type g_dbus_interface_skeleton_unexport, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_unexport
#endif
g_dbus_interface_skeleton_unexport:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 410 & 0xffff
#if 410 > 0xffff
  movk ip0, 410 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_interface_skeleton_unexport_from_connection
  .p2align 4
  .type g_dbus_interface_skeleton_unexport_from_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_interface_skeleton_unexport_from_connection
#endif
g_dbus_interface_skeleton_unexport_from_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 411 & 0xffff
#if 411 > 0xffff
  movk ip0, 411 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_address
  .p2align 4
  .type g_dbus_is_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_address
#endif
g_dbus_is_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 412 & 0xffff
#if 412 > 0xffff
  movk ip0, 412 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_error_name
  .p2align 4
  .type g_dbus_is_error_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_error_name
#endif
g_dbus_is_error_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 413 & 0xffff
#if 413 > 0xffff
  movk ip0, 413 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_guid
  .p2align 4
  .type g_dbus_is_guid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_guid
#endif
g_dbus_is_guid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 414 & 0xffff
#if 414 > 0xffff
  movk ip0, 414 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_interface_name
  .p2align 4
  .type g_dbus_is_interface_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_interface_name
#endif
g_dbus_is_interface_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 415 & 0xffff
#if 415 > 0xffff
  movk ip0, 415 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_member_name
  .p2align 4
  .type g_dbus_is_member_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_member_name
#endif
g_dbus_is_member_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 416 & 0xffff
#if 416 > 0xffff
  movk ip0, 416 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_name
  .p2align 4
  .type g_dbus_is_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_name
#endif
g_dbus_is_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 417 & 0xffff
#if 417 > 0xffff
  movk ip0, 417 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_supported_address
  .p2align 4
  .type g_dbus_is_supported_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_supported_address
#endif
g_dbus_is_supported_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 418 & 0xffff
#if 418 > 0xffff
  movk ip0, 418 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_is_unique_name
  .p2align 4
  .type g_dbus_is_unique_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_is_unique_name
#endif
g_dbus_is_unique_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 419 & 0xffff
#if 419 > 0xffff
  movk ip0, 419 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_menu_model_get
  .p2align 4
  .type g_dbus_menu_model_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_menu_model_get
#endif
g_dbus_menu_model_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 420 & 0xffff
#if 420 > 0xffff
  movk ip0, 420 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_menu_model_get_type
  .p2align 4
  .type g_dbus_menu_model_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_menu_model_get_type
#endif
g_dbus_menu_model_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 421 & 0xffff
#if 421 > 0xffff
  movk ip0, 421 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_byte_order_get_type
  .p2align 4
  .type g_dbus_message_byte_order_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_byte_order_get_type
#endif
g_dbus_message_byte_order_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 422 & 0xffff
#if 422 > 0xffff
  movk ip0, 422 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_bytes_needed
  .p2align 4
  .type g_dbus_message_bytes_needed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_bytes_needed
#endif
g_dbus_message_bytes_needed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 423 & 0xffff
#if 423 > 0xffff
  movk ip0, 423 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_copy
  .p2align 4
  .type g_dbus_message_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_copy
#endif
g_dbus_message_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 424 & 0xffff
#if 424 > 0xffff
  movk ip0, 424 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_flags_get_type
  .p2align 4
  .type g_dbus_message_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_flags_get_type
#endif
g_dbus_message_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 425 & 0xffff
#if 425 > 0xffff
  movk ip0, 425 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_arg0
  .p2align 4
  .type g_dbus_message_get_arg0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_arg0
#endif
g_dbus_message_get_arg0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 426 & 0xffff
#if 426 > 0xffff
  movk ip0, 426 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_arg0_path
  .p2align 4
  .type g_dbus_message_get_arg0_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_arg0_path
#endif
g_dbus_message_get_arg0_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 427 & 0xffff
#if 427 > 0xffff
  movk ip0, 427 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_body
  .p2align 4
  .type g_dbus_message_get_body, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_body
#endif
g_dbus_message_get_body:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 428 & 0xffff
#if 428 > 0xffff
  movk ip0, 428 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_byte_order
  .p2align 4
  .type g_dbus_message_get_byte_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_byte_order
#endif
g_dbus_message_get_byte_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 429 & 0xffff
#if 429 > 0xffff
  movk ip0, 429 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_destination
  .p2align 4
  .type g_dbus_message_get_destination, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_destination
#endif
g_dbus_message_get_destination:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 430 & 0xffff
#if 430 > 0xffff
  movk ip0, 430 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_error_name
  .p2align 4
  .type g_dbus_message_get_error_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_error_name
#endif
g_dbus_message_get_error_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 431 & 0xffff
#if 431 > 0xffff
  movk ip0, 431 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_flags
  .p2align 4
  .type g_dbus_message_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_flags
#endif
g_dbus_message_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 432 & 0xffff
#if 432 > 0xffff
  movk ip0, 432 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_header
  .p2align 4
  .type g_dbus_message_get_header, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_header
#endif
g_dbus_message_get_header:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 433 & 0xffff
#if 433 > 0xffff
  movk ip0, 433 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_header_fields
  .p2align 4
  .type g_dbus_message_get_header_fields, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_header_fields
#endif
g_dbus_message_get_header_fields:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 434 & 0xffff
#if 434 > 0xffff
  movk ip0, 434 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_interface
  .p2align 4
  .type g_dbus_message_get_interface, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_interface
#endif
g_dbus_message_get_interface:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 435 & 0xffff
#if 435 > 0xffff
  movk ip0, 435 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_locked
  .p2align 4
  .type g_dbus_message_get_locked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_locked
#endif
g_dbus_message_get_locked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 436 & 0xffff
#if 436 > 0xffff
  movk ip0, 436 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_member
  .p2align 4
  .type g_dbus_message_get_member, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_member
#endif
g_dbus_message_get_member:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 437 & 0xffff
#if 437 > 0xffff
  movk ip0, 437 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_message_type
  .p2align 4
  .type g_dbus_message_get_message_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_message_type
#endif
g_dbus_message_get_message_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 438 & 0xffff
#if 438 > 0xffff
  movk ip0, 438 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_num_unix_fds
  .p2align 4
  .type g_dbus_message_get_num_unix_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_num_unix_fds
#endif
g_dbus_message_get_num_unix_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 439 & 0xffff
#if 439 > 0xffff
  movk ip0, 439 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_path
  .p2align 4
  .type g_dbus_message_get_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_path
#endif
g_dbus_message_get_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 440 & 0xffff
#if 440 > 0xffff
  movk ip0, 440 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_reply_serial
  .p2align 4
  .type g_dbus_message_get_reply_serial, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_reply_serial
#endif
g_dbus_message_get_reply_serial:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 441 & 0xffff
#if 441 > 0xffff
  movk ip0, 441 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_sender
  .p2align 4
  .type g_dbus_message_get_sender, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_sender
#endif
g_dbus_message_get_sender:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 442 & 0xffff
#if 442 > 0xffff
  movk ip0, 442 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_serial
  .p2align 4
  .type g_dbus_message_get_serial, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_serial
#endif
g_dbus_message_get_serial:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 443 & 0xffff
#if 443 > 0xffff
  movk ip0, 443 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_signature
  .p2align 4
  .type g_dbus_message_get_signature, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_signature
#endif
g_dbus_message_get_signature:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 444 & 0xffff
#if 444 > 0xffff
  movk ip0, 444 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_type
  .p2align 4
  .type g_dbus_message_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_type
#endif
g_dbus_message_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 445 & 0xffff
#if 445 > 0xffff
  movk ip0, 445 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_get_unix_fd_list
  .p2align 4
  .type g_dbus_message_get_unix_fd_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_get_unix_fd_list
#endif
g_dbus_message_get_unix_fd_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 446 & 0xffff
#if 446 > 0xffff
  movk ip0, 446 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_header_field_get_type
  .p2align 4
  .type g_dbus_message_header_field_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_header_field_get_type
#endif
g_dbus_message_header_field_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 447 & 0xffff
#if 447 > 0xffff
  movk ip0, 447 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_lock
  .p2align 4
  .type g_dbus_message_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_lock
#endif
g_dbus_message_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 448 & 0xffff
#if 448 > 0xffff
  movk ip0, 448 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new
  .p2align 4
  .type g_dbus_message_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new
#endif
g_dbus_message_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 449 & 0xffff
#if 449 > 0xffff
  movk ip0, 449 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new_from_blob
  .p2align 4
  .type g_dbus_message_new_from_blob, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new_from_blob
#endif
g_dbus_message_new_from_blob:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 450 & 0xffff
#if 450 > 0xffff
  movk ip0, 450 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new_method_call
  .p2align 4
  .type g_dbus_message_new_method_call, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new_method_call
#endif
g_dbus_message_new_method_call:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 451 & 0xffff
#if 451 > 0xffff
  movk ip0, 451 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new_method_error
  .p2align 4
  .type g_dbus_message_new_method_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new_method_error
#endif
g_dbus_message_new_method_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 452 & 0xffff
#if 452 > 0xffff
  movk ip0, 452 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new_method_error_literal
  .p2align 4
  .type g_dbus_message_new_method_error_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new_method_error_literal
#endif
g_dbus_message_new_method_error_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 453 & 0xffff
#if 453 > 0xffff
  movk ip0, 453 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new_method_error_valist
  .p2align 4
  .type g_dbus_message_new_method_error_valist, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new_method_error_valist
#endif
g_dbus_message_new_method_error_valist:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 454 & 0xffff
#if 454 > 0xffff
  movk ip0, 454 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new_method_reply
  .p2align 4
  .type g_dbus_message_new_method_reply, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new_method_reply
#endif
g_dbus_message_new_method_reply:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 455 & 0xffff
#if 455 > 0xffff
  movk ip0, 455 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_new_signal
  .p2align 4
  .type g_dbus_message_new_signal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_new_signal
#endif
g_dbus_message_new_signal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 456 & 0xffff
#if 456 > 0xffff
  movk ip0, 456 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_print
  .p2align 4
  .type g_dbus_message_print, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_print
#endif
g_dbus_message_print:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 457 & 0xffff
#if 457 > 0xffff
  movk ip0, 457 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_body
  .p2align 4
  .type g_dbus_message_set_body, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_body
#endif
g_dbus_message_set_body:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 458 & 0xffff
#if 458 > 0xffff
  movk ip0, 458 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_byte_order
  .p2align 4
  .type g_dbus_message_set_byte_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_byte_order
#endif
g_dbus_message_set_byte_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 459 & 0xffff
#if 459 > 0xffff
  movk ip0, 459 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_destination
  .p2align 4
  .type g_dbus_message_set_destination, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_destination
#endif
g_dbus_message_set_destination:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 460 & 0xffff
#if 460 > 0xffff
  movk ip0, 460 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_error_name
  .p2align 4
  .type g_dbus_message_set_error_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_error_name
#endif
g_dbus_message_set_error_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 461 & 0xffff
#if 461 > 0xffff
  movk ip0, 461 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_flags
  .p2align 4
  .type g_dbus_message_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_flags
#endif
g_dbus_message_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 462 & 0xffff
#if 462 > 0xffff
  movk ip0, 462 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_header
  .p2align 4
  .type g_dbus_message_set_header, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_header
#endif
g_dbus_message_set_header:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 463 & 0xffff
#if 463 > 0xffff
  movk ip0, 463 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_interface
  .p2align 4
  .type g_dbus_message_set_interface, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_interface
#endif
g_dbus_message_set_interface:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 464 & 0xffff
#if 464 > 0xffff
  movk ip0, 464 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_member
  .p2align 4
  .type g_dbus_message_set_member, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_member
#endif
g_dbus_message_set_member:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 465 & 0xffff
#if 465 > 0xffff
  movk ip0, 465 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_message_type
  .p2align 4
  .type g_dbus_message_set_message_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_message_type
#endif
g_dbus_message_set_message_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 466 & 0xffff
#if 466 > 0xffff
  movk ip0, 466 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_num_unix_fds
  .p2align 4
  .type g_dbus_message_set_num_unix_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_num_unix_fds
#endif
g_dbus_message_set_num_unix_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 467 & 0xffff
#if 467 > 0xffff
  movk ip0, 467 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_path
  .p2align 4
  .type g_dbus_message_set_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_path
#endif
g_dbus_message_set_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 468 & 0xffff
#if 468 > 0xffff
  movk ip0, 468 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_reply_serial
  .p2align 4
  .type g_dbus_message_set_reply_serial, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_reply_serial
#endif
g_dbus_message_set_reply_serial:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 469 & 0xffff
#if 469 > 0xffff
  movk ip0, 469 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_sender
  .p2align 4
  .type g_dbus_message_set_sender, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_sender
#endif
g_dbus_message_set_sender:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 470 & 0xffff
#if 470 > 0xffff
  movk ip0, 470 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_serial
  .p2align 4
  .type g_dbus_message_set_serial, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_serial
#endif
g_dbus_message_set_serial:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 471 & 0xffff
#if 471 > 0xffff
  movk ip0, 471 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_signature
  .p2align 4
  .type g_dbus_message_set_signature, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_signature
#endif
g_dbus_message_set_signature:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 472 & 0xffff
#if 472 > 0xffff
  movk ip0, 472 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_set_unix_fd_list
  .p2align 4
  .type g_dbus_message_set_unix_fd_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_set_unix_fd_list
#endif
g_dbus_message_set_unix_fd_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 473 & 0xffff
#if 473 > 0xffff
  movk ip0, 473 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_to_blob
  .p2align 4
  .type g_dbus_message_to_blob, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_to_blob
#endif
g_dbus_message_to_blob:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 474 & 0xffff
#if 474 > 0xffff
  movk ip0, 474 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_to_gerror
  .p2align 4
  .type g_dbus_message_to_gerror, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_to_gerror
#endif
g_dbus_message_to_gerror:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 475 & 0xffff
#if 475 > 0xffff
  movk ip0, 475 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_message_type_get_type
  .p2align 4
  .type g_dbus_message_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_message_type_get_type
#endif
g_dbus_message_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 476 & 0xffff
#if 476 > 0xffff
  movk ip0, 476 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_info_get_type
  .p2align 4
  .type g_dbus_method_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_info_get_type
#endif
g_dbus_method_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 477 & 0xffff
#if 477 > 0xffff
  movk ip0, 477 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_info_ref
  .p2align 4
  .type g_dbus_method_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_info_ref
#endif
g_dbus_method_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 478 & 0xffff
#if 478 > 0xffff
  movk ip0, 478 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_info_unref
  .p2align 4
  .type g_dbus_method_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_info_unref
#endif
g_dbus_method_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 479 & 0xffff
#if 479 > 0xffff
  movk ip0, 479 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_connection
  .p2align 4
  .type g_dbus_method_invocation_get_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_connection
#endif
g_dbus_method_invocation_get_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 480 & 0xffff
#if 480 > 0xffff
  movk ip0, 480 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_interface_name
  .p2align 4
  .type g_dbus_method_invocation_get_interface_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_interface_name
#endif
g_dbus_method_invocation_get_interface_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 481 & 0xffff
#if 481 > 0xffff
  movk ip0, 481 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_message
  .p2align 4
  .type g_dbus_method_invocation_get_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_message
#endif
g_dbus_method_invocation_get_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 482 & 0xffff
#if 482 > 0xffff
  movk ip0, 482 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_method_info
  .p2align 4
  .type g_dbus_method_invocation_get_method_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_method_info
#endif
g_dbus_method_invocation_get_method_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 483 & 0xffff
#if 483 > 0xffff
  movk ip0, 483 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_method_name
  .p2align 4
  .type g_dbus_method_invocation_get_method_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_method_name
#endif
g_dbus_method_invocation_get_method_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 484 & 0xffff
#if 484 > 0xffff
  movk ip0, 484 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_object_path
  .p2align 4
  .type g_dbus_method_invocation_get_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_object_path
#endif
g_dbus_method_invocation_get_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 485 & 0xffff
#if 485 > 0xffff
  movk ip0, 485 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_parameters
  .p2align 4
  .type g_dbus_method_invocation_get_parameters, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_parameters
#endif
g_dbus_method_invocation_get_parameters:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 486 & 0xffff
#if 486 > 0xffff
  movk ip0, 486 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_property_info
  .p2align 4
  .type g_dbus_method_invocation_get_property_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_property_info
#endif
g_dbus_method_invocation_get_property_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 487 & 0xffff
#if 487 > 0xffff
  movk ip0, 487 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_sender
  .p2align 4
  .type g_dbus_method_invocation_get_sender, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_sender
#endif
g_dbus_method_invocation_get_sender:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 488 & 0xffff
#if 488 > 0xffff
  movk ip0, 488 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_type
  .p2align 4
  .type g_dbus_method_invocation_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_type
#endif
g_dbus_method_invocation_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 489 & 0xffff
#if 489 > 0xffff
  movk ip0, 489 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_get_user_data
  .p2align 4
  .type g_dbus_method_invocation_get_user_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_get_user_data
#endif
g_dbus_method_invocation_get_user_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 490 & 0xffff
#if 490 > 0xffff
  movk ip0, 490 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_return_dbus_error
  .p2align 4
  .type g_dbus_method_invocation_return_dbus_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_return_dbus_error
#endif
g_dbus_method_invocation_return_dbus_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 491 & 0xffff
#if 491 > 0xffff
  movk ip0, 491 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_return_error
  .p2align 4
  .type g_dbus_method_invocation_return_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_return_error
#endif
g_dbus_method_invocation_return_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 492 & 0xffff
#if 492 > 0xffff
  movk ip0, 492 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_return_error_literal
  .p2align 4
  .type g_dbus_method_invocation_return_error_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_return_error_literal
#endif
g_dbus_method_invocation_return_error_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 493 & 0xffff
#if 493 > 0xffff
  movk ip0, 493 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_return_error_valist
  .p2align 4
  .type g_dbus_method_invocation_return_error_valist, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_return_error_valist
#endif
g_dbus_method_invocation_return_error_valist:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 494 & 0xffff
#if 494 > 0xffff
  movk ip0, 494 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_return_gerror
  .p2align 4
  .type g_dbus_method_invocation_return_gerror, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_return_gerror
#endif
g_dbus_method_invocation_return_gerror:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 495 & 0xffff
#if 495 > 0xffff
  movk ip0, 495 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_return_value
  .p2align 4
  .type g_dbus_method_invocation_return_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_return_value
#endif
g_dbus_method_invocation_return_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 496 & 0xffff
#if 496 > 0xffff
  movk ip0, 496 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_return_value_with_unix_fd_list
  .p2align 4
  .type g_dbus_method_invocation_return_value_with_unix_fd_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_return_value_with_unix_fd_list
#endif
g_dbus_method_invocation_return_value_with_unix_fd_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 497 & 0xffff
#if 497 > 0xffff
  movk ip0, 497 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_method_invocation_take_error
  .p2align 4
  .type g_dbus_method_invocation_take_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_method_invocation_take_error
#endif
g_dbus_method_invocation_take_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 498 & 0xffff
#if 498 > 0xffff
  movk ip0, 498 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_node_info_generate_xml
  .p2align 4
  .type g_dbus_node_info_generate_xml, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_node_info_generate_xml
#endif
g_dbus_node_info_generate_xml:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+3992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+3992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 499 & 0xffff
#if 499 > 0xffff
  movk ip0, 499 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_node_info_get_type
  .p2align 4
  .type g_dbus_node_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_node_info_get_type
#endif
g_dbus_node_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 500 & 0xffff
#if 500 > 0xffff
  movk ip0, 500 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_node_info_lookup_interface
  .p2align 4
  .type g_dbus_node_info_lookup_interface, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_node_info_lookup_interface
#endif
g_dbus_node_info_lookup_interface:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 501 & 0xffff
#if 501 > 0xffff
  movk ip0, 501 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_node_info_new_for_xml
  .p2align 4
  .type g_dbus_node_info_new_for_xml, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_node_info_new_for_xml
#endif
g_dbus_node_info_new_for_xml:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 502 & 0xffff
#if 502 > 0xffff
  movk ip0, 502 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_node_info_ref
  .p2align 4
  .type g_dbus_node_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_node_info_ref
#endif
g_dbus_node_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 503 & 0xffff
#if 503 > 0xffff
  movk ip0, 503 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_node_info_unref
  .p2align 4
  .type g_dbus_node_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_node_info_unref
#endif
g_dbus_node_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 504 & 0xffff
#if 504 > 0xffff
  movk ip0, 504 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_get_interface
  .p2align 4
  .type g_dbus_object_get_interface, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_get_interface
#endif
g_dbus_object_get_interface:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 505 & 0xffff
#if 505 > 0xffff
  movk ip0, 505 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_get_interfaces
  .p2align 4
  .type g_dbus_object_get_interfaces, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_get_interfaces
#endif
g_dbus_object_get_interfaces:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 506 & 0xffff
#if 506 > 0xffff
  movk ip0, 506 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_get_object_path
  .p2align 4
  .type g_dbus_object_get_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_get_object_path
#endif
g_dbus_object_get_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 507 & 0xffff
#if 507 > 0xffff
  movk ip0, 507 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_get_type
  .p2align 4
  .type g_dbus_object_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_get_type
#endif
g_dbus_object_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 508 & 0xffff
#if 508 > 0xffff
  movk ip0, 508 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_flags_get_type
  .p2align 4
  .type g_dbus_object_manager_client_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_flags_get_type
#endif
g_dbus_object_manager_client_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 509 & 0xffff
#if 509 > 0xffff
  movk ip0, 509 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_get_connection
  .p2align 4
  .type g_dbus_object_manager_client_get_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_get_connection
#endif
g_dbus_object_manager_client_get_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 510 & 0xffff
#if 510 > 0xffff
  movk ip0, 510 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_get_flags
  .p2align 4
  .type g_dbus_object_manager_client_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_get_flags
#endif
g_dbus_object_manager_client_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 511 & 0xffff
#if 511 > 0xffff
  movk ip0, 511 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_get_name
  .p2align 4
  .type g_dbus_object_manager_client_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_get_name
#endif
g_dbus_object_manager_client_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 512 & 0xffff
#if 512 > 0xffff
  movk ip0, 512 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_get_name_owner
  .p2align 4
  .type g_dbus_object_manager_client_get_name_owner, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_get_name_owner
#endif
g_dbus_object_manager_client_get_name_owner:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 513 & 0xffff
#if 513 > 0xffff
  movk ip0, 513 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_get_type
  .p2align 4
  .type g_dbus_object_manager_client_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_get_type
#endif
g_dbus_object_manager_client_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 514 & 0xffff
#if 514 > 0xffff
  movk ip0, 514 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_new
  .p2align 4
  .type g_dbus_object_manager_client_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_new
#endif
g_dbus_object_manager_client_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 515 & 0xffff
#if 515 > 0xffff
  movk ip0, 515 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_new_finish
  .p2align 4
  .type g_dbus_object_manager_client_new_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_new_finish
#endif
g_dbus_object_manager_client_new_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 516 & 0xffff
#if 516 > 0xffff
  movk ip0, 516 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_new_for_bus
  .p2align 4
  .type g_dbus_object_manager_client_new_for_bus, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_new_for_bus
#endif
g_dbus_object_manager_client_new_for_bus:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 517 & 0xffff
#if 517 > 0xffff
  movk ip0, 517 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_new_for_bus_finish
  .p2align 4
  .type g_dbus_object_manager_client_new_for_bus_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_new_for_bus_finish
#endif
g_dbus_object_manager_client_new_for_bus_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 518 & 0xffff
#if 518 > 0xffff
  movk ip0, 518 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_new_for_bus_sync
  .p2align 4
  .type g_dbus_object_manager_client_new_for_bus_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_new_for_bus_sync
#endif
g_dbus_object_manager_client_new_for_bus_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 519 & 0xffff
#if 519 > 0xffff
  movk ip0, 519 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_client_new_sync
  .p2align 4
  .type g_dbus_object_manager_client_new_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_client_new_sync
#endif
g_dbus_object_manager_client_new_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 520 & 0xffff
#if 520 > 0xffff
  movk ip0, 520 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_get_interface
  .p2align 4
  .type g_dbus_object_manager_get_interface, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_get_interface
#endif
g_dbus_object_manager_get_interface:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 521 & 0xffff
#if 521 > 0xffff
  movk ip0, 521 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_get_object
  .p2align 4
  .type g_dbus_object_manager_get_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_get_object
#endif
g_dbus_object_manager_get_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 522 & 0xffff
#if 522 > 0xffff
  movk ip0, 522 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_get_object_path
  .p2align 4
  .type g_dbus_object_manager_get_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_get_object_path
#endif
g_dbus_object_manager_get_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 523 & 0xffff
#if 523 > 0xffff
  movk ip0, 523 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_get_objects
  .p2align 4
  .type g_dbus_object_manager_get_objects, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_get_objects
#endif
g_dbus_object_manager_get_objects:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 524 & 0xffff
#if 524 > 0xffff
  movk ip0, 524 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_get_type
  .p2align 4
  .type g_dbus_object_manager_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_get_type
#endif
g_dbus_object_manager_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 525 & 0xffff
#if 525 > 0xffff
  movk ip0, 525 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_export
  .p2align 4
  .type g_dbus_object_manager_server_export, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_export
#endif
g_dbus_object_manager_server_export:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 526 & 0xffff
#if 526 > 0xffff
  movk ip0, 526 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_export_uniquely
  .p2align 4
  .type g_dbus_object_manager_server_export_uniquely, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_export_uniquely
#endif
g_dbus_object_manager_server_export_uniquely:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 527 & 0xffff
#if 527 > 0xffff
  movk ip0, 527 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_get_connection
  .p2align 4
  .type g_dbus_object_manager_server_get_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_get_connection
#endif
g_dbus_object_manager_server_get_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 528 & 0xffff
#if 528 > 0xffff
  movk ip0, 528 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_get_type
  .p2align 4
  .type g_dbus_object_manager_server_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_get_type
#endif
g_dbus_object_manager_server_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 529 & 0xffff
#if 529 > 0xffff
  movk ip0, 529 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_is_exported
  .p2align 4
  .type g_dbus_object_manager_server_is_exported, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_is_exported
#endif
g_dbus_object_manager_server_is_exported:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 530 & 0xffff
#if 530 > 0xffff
  movk ip0, 530 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_new
  .p2align 4
  .type g_dbus_object_manager_server_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_new
#endif
g_dbus_object_manager_server_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 531 & 0xffff
#if 531 > 0xffff
  movk ip0, 531 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_set_connection
  .p2align 4
  .type g_dbus_object_manager_server_set_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_set_connection
#endif
g_dbus_object_manager_server_set_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 532 & 0xffff
#if 532 > 0xffff
  movk ip0, 532 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_manager_server_unexport
  .p2align 4
  .type g_dbus_object_manager_server_unexport, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_manager_server_unexport
#endif
g_dbus_object_manager_server_unexport:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 533 & 0xffff
#if 533 > 0xffff
  movk ip0, 533 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_proxy_get_connection
  .p2align 4
  .type g_dbus_object_proxy_get_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_proxy_get_connection
#endif
g_dbus_object_proxy_get_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 534 & 0xffff
#if 534 > 0xffff
  movk ip0, 534 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_proxy_get_type
  .p2align 4
  .type g_dbus_object_proxy_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_proxy_get_type
#endif
g_dbus_object_proxy_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 535 & 0xffff
#if 535 > 0xffff
  movk ip0, 535 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_proxy_new
  .p2align 4
  .type g_dbus_object_proxy_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_proxy_new
#endif
g_dbus_object_proxy_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 536 & 0xffff
#if 536 > 0xffff
  movk ip0, 536 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_skeleton_add_interface
  .p2align 4
  .type g_dbus_object_skeleton_add_interface, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_skeleton_add_interface
#endif
g_dbus_object_skeleton_add_interface:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 537 & 0xffff
#if 537 > 0xffff
  movk ip0, 537 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_skeleton_flush
  .p2align 4
  .type g_dbus_object_skeleton_flush, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_skeleton_flush
#endif
g_dbus_object_skeleton_flush:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 538 & 0xffff
#if 538 > 0xffff
  movk ip0, 538 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_skeleton_get_type
  .p2align 4
  .type g_dbus_object_skeleton_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_skeleton_get_type
#endif
g_dbus_object_skeleton_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 539 & 0xffff
#if 539 > 0xffff
  movk ip0, 539 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_skeleton_new
  .p2align 4
  .type g_dbus_object_skeleton_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_skeleton_new
#endif
g_dbus_object_skeleton_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 540 & 0xffff
#if 540 > 0xffff
  movk ip0, 540 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_skeleton_remove_interface
  .p2align 4
  .type g_dbus_object_skeleton_remove_interface, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_skeleton_remove_interface
#endif
g_dbus_object_skeleton_remove_interface:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 541 & 0xffff
#if 541 > 0xffff
  movk ip0, 541 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_skeleton_remove_interface_by_name
  .p2align 4
  .type g_dbus_object_skeleton_remove_interface_by_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_skeleton_remove_interface_by_name
#endif
g_dbus_object_skeleton_remove_interface_by_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 542 & 0xffff
#if 542 > 0xffff
  movk ip0, 542 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_object_skeleton_set_object_path
  .p2align 4
  .type g_dbus_object_skeleton_set_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_object_skeleton_set_object_path
#endif
g_dbus_object_skeleton_set_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 543 & 0xffff
#if 543 > 0xffff
  movk ip0, 543 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_property_info_flags_get_type
  .p2align 4
  .type g_dbus_property_info_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_property_info_flags_get_type
#endif
g_dbus_property_info_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 544 & 0xffff
#if 544 > 0xffff
  movk ip0, 544 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_property_info_get_type
  .p2align 4
  .type g_dbus_property_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_property_info_get_type
#endif
g_dbus_property_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 545 & 0xffff
#if 545 > 0xffff
  movk ip0, 545 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_property_info_ref
  .p2align 4
  .type g_dbus_property_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_property_info_ref
#endif
g_dbus_property_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 546 & 0xffff
#if 546 > 0xffff
  movk ip0, 546 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_property_info_unref
  .p2align 4
  .type g_dbus_property_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_property_info_unref
#endif
g_dbus_property_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 547 & 0xffff
#if 547 > 0xffff
  movk ip0, 547 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_call
  .p2align 4
  .type g_dbus_proxy_call, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_call
#endif
g_dbus_proxy_call:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 548 & 0xffff
#if 548 > 0xffff
  movk ip0, 548 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_call_finish
  .p2align 4
  .type g_dbus_proxy_call_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_call_finish
#endif
g_dbus_proxy_call_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 549 & 0xffff
#if 549 > 0xffff
  movk ip0, 549 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_call_sync
  .p2align 4
  .type g_dbus_proxy_call_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_call_sync
#endif
g_dbus_proxy_call_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 550 & 0xffff
#if 550 > 0xffff
  movk ip0, 550 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_call_with_unix_fd_list
  .p2align 4
  .type g_dbus_proxy_call_with_unix_fd_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_call_with_unix_fd_list
#endif
g_dbus_proxy_call_with_unix_fd_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 551 & 0xffff
#if 551 > 0xffff
  movk ip0, 551 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_call_with_unix_fd_list_finish
  .p2align 4
  .type g_dbus_proxy_call_with_unix_fd_list_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_call_with_unix_fd_list_finish
#endif
g_dbus_proxy_call_with_unix_fd_list_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 552 & 0xffff
#if 552 > 0xffff
  movk ip0, 552 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_call_with_unix_fd_list_sync
  .p2align 4
  .type g_dbus_proxy_call_with_unix_fd_list_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_call_with_unix_fd_list_sync
#endif
g_dbus_proxy_call_with_unix_fd_list_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 553 & 0xffff
#if 553 > 0xffff
  movk ip0, 553 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_flags_get_type
  .p2align 4
  .type g_dbus_proxy_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_flags_get_type
#endif
g_dbus_proxy_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 554 & 0xffff
#if 554 > 0xffff
  movk ip0, 554 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_cached_property
  .p2align 4
  .type g_dbus_proxy_get_cached_property, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_cached_property
#endif
g_dbus_proxy_get_cached_property:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 555 & 0xffff
#if 555 > 0xffff
  movk ip0, 555 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_cached_property_names
  .p2align 4
  .type g_dbus_proxy_get_cached_property_names, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_cached_property_names
#endif
g_dbus_proxy_get_cached_property_names:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 556 & 0xffff
#if 556 > 0xffff
  movk ip0, 556 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_connection
  .p2align 4
  .type g_dbus_proxy_get_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_connection
#endif
g_dbus_proxy_get_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 557 & 0xffff
#if 557 > 0xffff
  movk ip0, 557 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_default_timeout
  .p2align 4
  .type g_dbus_proxy_get_default_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_default_timeout
#endif
g_dbus_proxy_get_default_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 558 & 0xffff
#if 558 > 0xffff
  movk ip0, 558 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_flags
  .p2align 4
  .type g_dbus_proxy_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_flags
#endif
g_dbus_proxy_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 559 & 0xffff
#if 559 > 0xffff
  movk ip0, 559 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_interface_info
  .p2align 4
  .type g_dbus_proxy_get_interface_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_interface_info
#endif
g_dbus_proxy_get_interface_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 560 & 0xffff
#if 560 > 0xffff
  movk ip0, 560 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_interface_name
  .p2align 4
  .type g_dbus_proxy_get_interface_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_interface_name
#endif
g_dbus_proxy_get_interface_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 561 & 0xffff
#if 561 > 0xffff
  movk ip0, 561 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_name
  .p2align 4
  .type g_dbus_proxy_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_name
#endif
g_dbus_proxy_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 562 & 0xffff
#if 562 > 0xffff
  movk ip0, 562 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_name_owner
  .p2align 4
  .type g_dbus_proxy_get_name_owner, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_name_owner
#endif
g_dbus_proxy_get_name_owner:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 563 & 0xffff
#if 563 > 0xffff
  movk ip0, 563 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_object_path
  .p2align 4
  .type g_dbus_proxy_get_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_object_path
#endif
g_dbus_proxy_get_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 564 & 0xffff
#if 564 > 0xffff
  movk ip0, 564 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_get_type
  .p2align 4
  .type g_dbus_proxy_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_get_type
#endif
g_dbus_proxy_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 565 & 0xffff
#if 565 > 0xffff
  movk ip0, 565 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_new
  .p2align 4
  .type g_dbus_proxy_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_new
#endif
g_dbus_proxy_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 566 & 0xffff
#if 566 > 0xffff
  movk ip0, 566 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_new_finish
  .p2align 4
  .type g_dbus_proxy_new_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_new_finish
#endif
g_dbus_proxy_new_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 567 & 0xffff
#if 567 > 0xffff
  movk ip0, 567 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_new_for_bus
  .p2align 4
  .type g_dbus_proxy_new_for_bus, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_new_for_bus
#endif
g_dbus_proxy_new_for_bus:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 568 & 0xffff
#if 568 > 0xffff
  movk ip0, 568 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_new_for_bus_finish
  .p2align 4
  .type g_dbus_proxy_new_for_bus_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_new_for_bus_finish
#endif
g_dbus_proxy_new_for_bus_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 569 & 0xffff
#if 569 > 0xffff
  movk ip0, 569 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_new_for_bus_sync
  .p2align 4
  .type g_dbus_proxy_new_for_bus_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_new_for_bus_sync
#endif
g_dbus_proxy_new_for_bus_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 570 & 0xffff
#if 570 > 0xffff
  movk ip0, 570 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_new_sync
  .p2align 4
  .type g_dbus_proxy_new_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_new_sync
#endif
g_dbus_proxy_new_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 571 & 0xffff
#if 571 > 0xffff
  movk ip0, 571 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_set_cached_property
  .p2align 4
  .type g_dbus_proxy_set_cached_property, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_set_cached_property
#endif
g_dbus_proxy_set_cached_property:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 572 & 0xffff
#if 572 > 0xffff
  movk ip0, 572 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_set_default_timeout
  .p2align 4
  .type g_dbus_proxy_set_default_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_set_default_timeout
#endif
g_dbus_proxy_set_default_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 573 & 0xffff
#if 573 > 0xffff
  movk ip0, 573 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_proxy_set_interface_info
  .p2align 4
  .type g_dbus_proxy_set_interface_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_proxy_set_interface_info
#endif
g_dbus_proxy_set_interface_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 574 & 0xffff
#if 574 > 0xffff
  movk ip0, 574 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_send_message_flags_get_type
  .p2align 4
  .type g_dbus_send_message_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_send_message_flags_get_type
#endif
g_dbus_send_message_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 575 & 0xffff
#if 575 > 0xffff
  movk ip0, 575 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_flags_get_type
  .p2align 4
  .type g_dbus_server_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_flags_get_type
#endif
g_dbus_server_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 576 & 0xffff
#if 576 > 0xffff
  movk ip0, 576 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_get_client_address
  .p2align 4
  .type g_dbus_server_get_client_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_get_client_address
#endif
g_dbus_server_get_client_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 577 & 0xffff
#if 577 > 0xffff
  movk ip0, 577 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_get_flags
  .p2align 4
  .type g_dbus_server_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_get_flags
#endif
g_dbus_server_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 578 & 0xffff
#if 578 > 0xffff
  movk ip0, 578 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_get_guid
  .p2align 4
  .type g_dbus_server_get_guid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_get_guid
#endif
g_dbus_server_get_guid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 579 & 0xffff
#if 579 > 0xffff
  movk ip0, 579 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_get_type
  .p2align 4
  .type g_dbus_server_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_get_type
#endif
g_dbus_server_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 580 & 0xffff
#if 580 > 0xffff
  movk ip0, 580 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_is_active
  .p2align 4
  .type g_dbus_server_is_active, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_is_active
#endif
g_dbus_server_is_active:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 581 & 0xffff
#if 581 > 0xffff
  movk ip0, 581 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_new_sync
  .p2align 4
  .type g_dbus_server_new_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_new_sync
#endif
g_dbus_server_new_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 582 & 0xffff
#if 582 > 0xffff
  movk ip0, 582 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_start
  .p2align 4
  .type g_dbus_server_start, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_start
#endif
g_dbus_server_start:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 583 & 0xffff
#if 583 > 0xffff
  movk ip0, 583 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_server_stop
  .p2align 4
  .type g_dbus_server_stop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_server_stop
#endif
g_dbus_server_stop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 584 & 0xffff
#if 584 > 0xffff
  movk ip0, 584 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_signal_flags_get_type
  .p2align 4
  .type g_dbus_signal_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_signal_flags_get_type
#endif
g_dbus_signal_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 585 & 0xffff
#if 585 > 0xffff
  movk ip0, 585 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_signal_info_get_type
  .p2align 4
  .type g_dbus_signal_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_signal_info_get_type
#endif
g_dbus_signal_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 586 & 0xffff
#if 586 > 0xffff
  movk ip0, 586 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_signal_info_ref
  .p2align 4
  .type g_dbus_signal_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_signal_info_ref
#endif
g_dbus_signal_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 587 & 0xffff
#if 587 > 0xffff
  movk ip0, 587 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_signal_info_unref
  .p2align 4
  .type g_dbus_signal_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_signal_info_unref
#endif
g_dbus_signal_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 588 & 0xffff
#if 588 > 0xffff
  movk ip0, 588 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_subtree_flags_get_type
  .p2align 4
  .type g_dbus_subtree_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_subtree_flags_get_type
#endif
g_dbus_subtree_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 589 & 0xffff
#if 589 > 0xffff
  movk ip0, 589 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dbus_unescape_object_path
  .p2align 4
  .type g_dbus_unescape_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dbus_unescape_object_path
#endif
g_dbus_unescape_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 590 & 0xffff
#if 590 > 0xffff
  movk ip0, 590 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_debug_controller_dbus_get_type
  .p2align 4
  .type g_debug_controller_dbus_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_debug_controller_dbus_get_type
#endif
g_debug_controller_dbus_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 591 & 0xffff
#if 591 > 0xffff
  movk ip0, 591 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_debug_controller_dbus_new
  .p2align 4
  .type g_debug_controller_dbus_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_debug_controller_dbus_new
#endif
g_debug_controller_dbus_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 592 & 0xffff
#if 592 > 0xffff
  movk ip0, 592 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_debug_controller_dbus_stop
  .p2align 4
  .type g_debug_controller_dbus_stop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_debug_controller_dbus_stop
#endif
g_debug_controller_dbus_stop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 593 & 0xffff
#if 593 > 0xffff
  movk ip0, 593 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_debug_controller_get_debug_enabled
  .p2align 4
  .type g_debug_controller_get_debug_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_debug_controller_get_debug_enabled
#endif
g_debug_controller_get_debug_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 594 & 0xffff
#if 594 > 0xffff
  movk ip0, 594 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_debug_controller_get_type
  .p2align 4
  .type g_debug_controller_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_debug_controller_get_type
#endif
g_debug_controller_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 595 & 0xffff
#if 595 > 0xffff
  movk ip0, 595 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_debug_controller_set_debug_enabled
  .p2align 4
  .type g_debug_controller_set_debug_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_debug_controller_set_debug_enabled
#endif
g_debug_controller_set_debug_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 596 & 0xffff
#if 596 > 0xffff
  movk ip0, 596 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_action_name
  .p2align 4
  .type g_desktop_app_info_get_action_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_action_name
#endif
g_desktop_app_info_get_action_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 597 & 0xffff
#if 597 > 0xffff
  movk ip0, 597 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_boolean
  .p2align 4
  .type g_desktop_app_info_get_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_boolean
#endif
g_desktop_app_info_get_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 598 & 0xffff
#if 598 > 0xffff
  movk ip0, 598 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_categories
  .p2align 4
  .type g_desktop_app_info_get_categories, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_categories
#endif
g_desktop_app_info_get_categories:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 599 & 0xffff
#if 599 > 0xffff
  movk ip0, 599 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_filename
  .p2align 4
  .type g_desktop_app_info_get_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_filename
#endif
g_desktop_app_info_get_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 600 & 0xffff
#if 600 > 0xffff
  movk ip0, 600 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_generic_name
  .p2align 4
  .type g_desktop_app_info_get_generic_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_generic_name
#endif
g_desktop_app_info_get_generic_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 601 & 0xffff
#if 601 > 0xffff
  movk ip0, 601 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_implementations
  .p2align 4
  .type g_desktop_app_info_get_implementations, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_implementations
#endif
g_desktop_app_info_get_implementations:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 602 & 0xffff
#if 602 > 0xffff
  movk ip0, 602 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_is_hidden
  .p2align 4
  .type g_desktop_app_info_get_is_hidden, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_is_hidden
#endif
g_desktop_app_info_get_is_hidden:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 603 & 0xffff
#if 603 > 0xffff
  movk ip0, 603 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_keywords
  .p2align 4
  .type g_desktop_app_info_get_keywords, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_keywords
#endif
g_desktop_app_info_get_keywords:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 604 & 0xffff
#if 604 > 0xffff
  movk ip0, 604 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_locale_string
  .p2align 4
  .type g_desktop_app_info_get_locale_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_locale_string
#endif
g_desktop_app_info_get_locale_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 605 & 0xffff
#if 605 > 0xffff
  movk ip0, 605 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_nodisplay
  .p2align 4
  .type g_desktop_app_info_get_nodisplay, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_nodisplay
#endif
g_desktop_app_info_get_nodisplay:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 606 & 0xffff
#if 606 > 0xffff
  movk ip0, 606 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_show_in
  .p2align 4
  .type g_desktop_app_info_get_show_in, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_show_in
#endif
g_desktop_app_info_get_show_in:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 607 & 0xffff
#if 607 > 0xffff
  movk ip0, 607 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_startup_wm_class
  .p2align 4
  .type g_desktop_app_info_get_startup_wm_class, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_startup_wm_class
#endif
g_desktop_app_info_get_startup_wm_class:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 608 & 0xffff
#if 608 > 0xffff
  movk ip0, 608 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_string
  .p2align 4
  .type g_desktop_app_info_get_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_string
#endif
g_desktop_app_info_get_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 609 & 0xffff
#if 609 > 0xffff
  movk ip0, 609 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_string_list
  .p2align 4
  .type g_desktop_app_info_get_string_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_string_list
#endif
g_desktop_app_info_get_string_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 610 & 0xffff
#if 610 > 0xffff
  movk ip0, 610 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_get_type
  .p2align 4
  .type g_desktop_app_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_get_type
#endif
g_desktop_app_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 611 & 0xffff
#if 611 > 0xffff
  movk ip0, 611 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_has_key
  .p2align 4
  .type g_desktop_app_info_has_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_has_key
#endif
g_desktop_app_info_has_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 612 & 0xffff
#if 612 > 0xffff
  movk ip0, 612 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_launch_action
  .p2align 4
  .type g_desktop_app_info_launch_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_launch_action
#endif
g_desktop_app_info_launch_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 613 & 0xffff
#if 613 > 0xffff
  movk ip0, 613 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_launch_uris_as_manager
  .p2align 4
  .type g_desktop_app_info_launch_uris_as_manager, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_launch_uris_as_manager
#endif
g_desktop_app_info_launch_uris_as_manager:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 614 & 0xffff
#if 614 > 0xffff
  movk ip0, 614 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_launch_uris_as_manager_with_fds
  .p2align 4
  .type g_desktop_app_info_launch_uris_as_manager_with_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_launch_uris_as_manager_with_fds
#endif
g_desktop_app_info_launch_uris_as_manager_with_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 615 & 0xffff
#if 615 > 0xffff
  movk ip0, 615 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_list_actions
  .p2align 4
  .type g_desktop_app_info_list_actions, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_list_actions
#endif
g_desktop_app_info_list_actions:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 616 & 0xffff
#if 616 > 0xffff
  movk ip0, 616 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_lookup_get_default_for_uri_scheme
  .p2align 4
  .type g_desktop_app_info_lookup_get_default_for_uri_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_lookup_get_default_for_uri_scheme
#endif
g_desktop_app_info_lookup_get_default_for_uri_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 617 & 0xffff
#if 617 > 0xffff
  movk ip0, 617 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_lookup_get_type
  .p2align 4
  .type g_desktop_app_info_lookup_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_lookup_get_type
#endif
g_desktop_app_info_lookup_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 618 & 0xffff
#if 618 > 0xffff
  movk ip0, 618 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_new
  .p2align 4
  .type g_desktop_app_info_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_new
#endif
g_desktop_app_info_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 619 & 0xffff
#if 619 > 0xffff
  movk ip0, 619 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_new_from_filename
  .p2align 4
  .type g_desktop_app_info_new_from_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_new_from_filename
#endif
g_desktop_app_info_new_from_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 620 & 0xffff
#if 620 > 0xffff
  movk ip0, 620 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_new_from_keyfile
  .p2align 4
  .type g_desktop_app_info_new_from_keyfile, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_new_from_keyfile
#endif
g_desktop_app_info_new_from_keyfile:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 621 & 0xffff
#if 621 > 0xffff
  movk ip0, 621 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_search
  .p2align 4
  .type g_desktop_app_info_search, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_search
#endif
g_desktop_app_info_search:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 622 & 0xffff
#if 622 > 0xffff
  movk ip0, 622 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_desktop_app_info_set_desktop_env
  .p2align 4
  .type g_desktop_app_info_set_desktop_env, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_desktop_app_info_set_desktop_env
#endif
g_desktop_app_info_set_desktop_env:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 623 & 0xffff
#if 623 > 0xffff
  movk ip0, 623 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_can_eject
  .p2align 4
  .type g_drive_can_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_can_eject
#endif
g_drive_can_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+4992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+4992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 624 & 0xffff
#if 624 > 0xffff
  movk ip0, 624 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_can_poll_for_media
  .p2align 4
  .type g_drive_can_poll_for_media, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_can_poll_for_media
#endif
g_drive_can_poll_for_media:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 625 & 0xffff
#if 625 > 0xffff
  movk ip0, 625 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_can_start
  .p2align 4
  .type g_drive_can_start, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_can_start
#endif
g_drive_can_start:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 626 & 0xffff
#if 626 > 0xffff
  movk ip0, 626 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_can_start_degraded
  .p2align 4
  .type g_drive_can_start_degraded, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_can_start_degraded
#endif
g_drive_can_start_degraded:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 627 & 0xffff
#if 627 > 0xffff
  movk ip0, 627 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_can_stop
  .p2align 4
  .type g_drive_can_stop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_can_stop
#endif
g_drive_can_stop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 628 & 0xffff
#if 628 > 0xffff
  movk ip0, 628 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_eject
  .p2align 4
  .type g_drive_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_eject
#endif
g_drive_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 629 & 0xffff
#if 629 > 0xffff
  movk ip0, 629 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_eject_finish
  .p2align 4
  .type g_drive_eject_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_eject_finish
#endif
g_drive_eject_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 630 & 0xffff
#if 630 > 0xffff
  movk ip0, 630 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_eject_with_operation
  .p2align 4
  .type g_drive_eject_with_operation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_eject_with_operation
#endif
g_drive_eject_with_operation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 631 & 0xffff
#if 631 > 0xffff
  movk ip0, 631 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_eject_with_operation_finish
  .p2align 4
  .type g_drive_eject_with_operation_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_eject_with_operation_finish
#endif
g_drive_eject_with_operation_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 632 & 0xffff
#if 632 > 0xffff
  movk ip0, 632 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_enumerate_identifiers
  .p2align 4
  .type g_drive_enumerate_identifiers, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_enumerate_identifiers
#endif
g_drive_enumerate_identifiers:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 633 & 0xffff
#if 633 > 0xffff
  movk ip0, 633 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_icon
  .p2align 4
  .type g_drive_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_icon
#endif
g_drive_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 634 & 0xffff
#if 634 > 0xffff
  movk ip0, 634 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_identifier
  .p2align 4
  .type g_drive_get_identifier, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_identifier
#endif
g_drive_get_identifier:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 635 & 0xffff
#if 635 > 0xffff
  movk ip0, 635 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_name
  .p2align 4
  .type g_drive_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_name
#endif
g_drive_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 636 & 0xffff
#if 636 > 0xffff
  movk ip0, 636 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_sort_key
  .p2align 4
  .type g_drive_get_sort_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_sort_key
#endif
g_drive_get_sort_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 637 & 0xffff
#if 637 > 0xffff
  movk ip0, 637 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_start_stop_type
  .p2align 4
  .type g_drive_get_start_stop_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_start_stop_type
#endif
g_drive_get_start_stop_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 638 & 0xffff
#if 638 > 0xffff
  movk ip0, 638 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_symbolic_icon
  .p2align 4
  .type g_drive_get_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_symbolic_icon
#endif
g_drive_get_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 639 & 0xffff
#if 639 > 0xffff
  movk ip0, 639 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_type
  .p2align 4
  .type g_drive_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_type
#endif
g_drive_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 640 & 0xffff
#if 640 > 0xffff
  movk ip0, 640 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_get_volumes
  .p2align 4
  .type g_drive_get_volumes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_get_volumes
#endif
g_drive_get_volumes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 641 & 0xffff
#if 641 > 0xffff
  movk ip0, 641 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_has_media
  .p2align 4
  .type g_drive_has_media, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_has_media
#endif
g_drive_has_media:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 642 & 0xffff
#if 642 > 0xffff
  movk ip0, 642 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_has_volumes
  .p2align 4
  .type g_drive_has_volumes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_has_volumes
#endif
g_drive_has_volumes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 643 & 0xffff
#if 643 > 0xffff
  movk ip0, 643 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_is_media_check_automatic
  .p2align 4
  .type g_drive_is_media_check_automatic, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_is_media_check_automatic
#endif
g_drive_is_media_check_automatic:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 644 & 0xffff
#if 644 > 0xffff
  movk ip0, 644 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_is_media_removable
  .p2align 4
  .type g_drive_is_media_removable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_is_media_removable
#endif
g_drive_is_media_removable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 645 & 0xffff
#if 645 > 0xffff
  movk ip0, 645 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_is_removable
  .p2align 4
  .type g_drive_is_removable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_is_removable
#endif
g_drive_is_removable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 646 & 0xffff
#if 646 > 0xffff
  movk ip0, 646 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_poll_for_media
  .p2align 4
  .type g_drive_poll_for_media, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_poll_for_media
#endif
g_drive_poll_for_media:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 647 & 0xffff
#if 647 > 0xffff
  movk ip0, 647 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_poll_for_media_finish
  .p2align 4
  .type g_drive_poll_for_media_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_poll_for_media_finish
#endif
g_drive_poll_for_media_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 648 & 0xffff
#if 648 > 0xffff
  movk ip0, 648 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_start
  .p2align 4
  .type g_drive_start, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_start
#endif
g_drive_start:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 649 & 0xffff
#if 649 > 0xffff
  movk ip0, 649 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_start_finish
  .p2align 4
  .type g_drive_start_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_start_finish
#endif
g_drive_start_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 650 & 0xffff
#if 650 > 0xffff
  movk ip0, 650 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_start_flags_get_type
  .p2align 4
  .type g_drive_start_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_start_flags_get_type
#endif
g_drive_start_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 651 & 0xffff
#if 651 > 0xffff
  movk ip0, 651 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_start_stop_type_get_type
  .p2align 4
  .type g_drive_start_stop_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_start_stop_type_get_type
#endif
g_drive_start_stop_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 652 & 0xffff
#if 652 > 0xffff
  movk ip0, 652 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_stop
  .p2align 4
  .type g_drive_stop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_stop
#endif
g_drive_stop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 653 & 0xffff
#if 653 > 0xffff
  movk ip0, 653 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_drive_stop_finish
  .p2align 4
  .type g_drive_stop_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_drive_stop_finish
#endif
g_drive_stop_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 654 & 0xffff
#if 654 > 0xffff
  movk ip0, 654 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_client_connection_get_accepted_cas
  .p2align 4
  .type g_dtls_client_connection_get_accepted_cas, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_client_connection_get_accepted_cas
#endif
g_dtls_client_connection_get_accepted_cas:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 655 & 0xffff
#if 655 > 0xffff
  movk ip0, 655 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_client_connection_get_server_identity
  .p2align 4
  .type g_dtls_client_connection_get_server_identity, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_client_connection_get_server_identity
#endif
g_dtls_client_connection_get_server_identity:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 656 & 0xffff
#if 656 > 0xffff
  movk ip0, 656 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_client_connection_get_type
  .p2align 4
  .type g_dtls_client_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_client_connection_get_type
#endif
g_dtls_client_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 657 & 0xffff
#if 657 > 0xffff
  movk ip0, 657 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_client_connection_get_validation_flags
  .p2align 4
  .type g_dtls_client_connection_get_validation_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_client_connection_get_validation_flags
#endif
g_dtls_client_connection_get_validation_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 658 & 0xffff
#if 658 > 0xffff
  movk ip0, 658 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_client_connection_new
  .p2align 4
  .type g_dtls_client_connection_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_client_connection_new
#endif
g_dtls_client_connection_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 659 & 0xffff
#if 659 > 0xffff
  movk ip0, 659 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_client_connection_set_server_identity
  .p2align 4
  .type g_dtls_client_connection_set_server_identity, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_client_connection_set_server_identity
#endif
g_dtls_client_connection_set_server_identity:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 660 & 0xffff
#if 660 > 0xffff
  movk ip0, 660 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_client_connection_set_validation_flags
  .p2align 4
  .type g_dtls_client_connection_set_validation_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_client_connection_set_validation_flags
#endif
g_dtls_client_connection_set_validation_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 661 & 0xffff
#if 661 > 0xffff
  movk ip0, 661 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_close
  .p2align 4
  .type g_dtls_connection_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_close
#endif
g_dtls_connection_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 662 & 0xffff
#if 662 > 0xffff
  movk ip0, 662 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_close_async
  .p2align 4
  .type g_dtls_connection_close_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_close_async
#endif
g_dtls_connection_close_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 663 & 0xffff
#if 663 > 0xffff
  movk ip0, 663 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_close_finish
  .p2align 4
  .type g_dtls_connection_close_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_close_finish
#endif
g_dtls_connection_close_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 664 & 0xffff
#if 664 > 0xffff
  movk ip0, 664 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_emit_accept_certificate
  .p2align 4
  .type g_dtls_connection_emit_accept_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_emit_accept_certificate
#endif
g_dtls_connection_emit_accept_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 665 & 0xffff
#if 665 > 0xffff
  movk ip0, 665 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_certificate
  .p2align 4
  .type g_dtls_connection_get_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_certificate
#endif
g_dtls_connection_get_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 666 & 0xffff
#if 666 > 0xffff
  movk ip0, 666 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_channel_binding_data
  .p2align 4
  .type g_dtls_connection_get_channel_binding_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_channel_binding_data
#endif
g_dtls_connection_get_channel_binding_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 667 & 0xffff
#if 667 > 0xffff
  movk ip0, 667 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_ciphersuite_name
  .p2align 4
  .type g_dtls_connection_get_ciphersuite_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_ciphersuite_name
#endif
g_dtls_connection_get_ciphersuite_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 668 & 0xffff
#if 668 > 0xffff
  movk ip0, 668 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_database
  .p2align 4
  .type g_dtls_connection_get_database, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_database
#endif
g_dtls_connection_get_database:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 669 & 0xffff
#if 669 > 0xffff
  movk ip0, 669 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_interaction
  .p2align 4
  .type g_dtls_connection_get_interaction, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_interaction
#endif
g_dtls_connection_get_interaction:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 670 & 0xffff
#if 670 > 0xffff
  movk ip0, 670 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_negotiated_protocol
  .p2align 4
  .type g_dtls_connection_get_negotiated_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_negotiated_protocol
#endif
g_dtls_connection_get_negotiated_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 671 & 0xffff
#if 671 > 0xffff
  movk ip0, 671 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_peer_certificate
  .p2align 4
  .type g_dtls_connection_get_peer_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_peer_certificate
#endif
g_dtls_connection_get_peer_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 672 & 0xffff
#if 672 > 0xffff
  movk ip0, 672 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_peer_certificate_errors
  .p2align 4
  .type g_dtls_connection_get_peer_certificate_errors, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_peer_certificate_errors
#endif
g_dtls_connection_get_peer_certificate_errors:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 673 & 0xffff
#if 673 > 0xffff
  movk ip0, 673 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_protocol_version
  .p2align 4
  .type g_dtls_connection_get_protocol_version, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_protocol_version
#endif
g_dtls_connection_get_protocol_version:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 674 & 0xffff
#if 674 > 0xffff
  movk ip0, 674 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_rehandshake_mode
  .p2align 4
  .type g_dtls_connection_get_rehandshake_mode, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_rehandshake_mode
#endif
g_dtls_connection_get_rehandshake_mode:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 675 & 0xffff
#if 675 > 0xffff
  movk ip0, 675 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_require_close_notify
  .p2align 4
  .type g_dtls_connection_get_require_close_notify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_require_close_notify
#endif
g_dtls_connection_get_require_close_notify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 676 & 0xffff
#if 676 > 0xffff
  movk ip0, 676 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_get_type
  .p2align 4
  .type g_dtls_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_get_type
#endif
g_dtls_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 677 & 0xffff
#if 677 > 0xffff
  movk ip0, 677 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_handshake
  .p2align 4
  .type g_dtls_connection_handshake, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_handshake
#endif
g_dtls_connection_handshake:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 678 & 0xffff
#if 678 > 0xffff
  movk ip0, 678 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_handshake_async
  .p2align 4
  .type g_dtls_connection_handshake_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_handshake_async
#endif
g_dtls_connection_handshake_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 679 & 0xffff
#if 679 > 0xffff
  movk ip0, 679 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_handshake_finish
  .p2align 4
  .type g_dtls_connection_handshake_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_handshake_finish
#endif
g_dtls_connection_handshake_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 680 & 0xffff
#if 680 > 0xffff
  movk ip0, 680 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_set_advertised_protocols
  .p2align 4
  .type g_dtls_connection_set_advertised_protocols, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_set_advertised_protocols
#endif
g_dtls_connection_set_advertised_protocols:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 681 & 0xffff
#if 681 > 0xffff
  movk ip0, 681 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_set_certificate
  .p2align 4
  .type g_dtls_connection_set_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_set_certificate
#endif
g_dtls_connection_set_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 682 & 0xffff
#if 682 > 0xffff
  movk ip0, 682 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_set_database
  .p2align 4
  .type g_dtls_connection_set_database, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_set_database
#endif
g_dtls_connection_set_database:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 683 & 0xffff
#if 683 > 0xffff
  movk ip0, 683 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_set_interaction
  .p2align 4
  .type g_dtls_connection_set_interaction, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_set_interaction
#endif
g_dtls_connection_set_interaction:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 684 & 0xffff
#if 684 > 0xffff
  movk ip0, 684 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_set_rehandshake_mode
  .p2align 4
  .type g_dtls_connection_set_rehandshake_mode, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_set_rehandshake_mode
#endif
g_dtls_connection_set_rehandshake_mode:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 685 & 0xffff
#if 685 > 0xffff
  movk ip0, 685 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_set_require_close_notify
  .p2align 4
  .type g_dtls_connection_set_require_close_notify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_set_require_close_notify
#endif
g_dtls_connection_set_require_close_notify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 686 & 0xffff
#if 686 > 0xffff
  movk ip0, 686 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_shutdown
  .p2align 4
  .type g_dtls_connection_shutdown, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_shutdown
#endif
g_dtls_connection_shutdown:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 687 & 0xffff
#if 687 > 0xffff
  movk ip0, 687 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_shutdown_async
  .p2align 4
  .type g_dtls_connection_shutdown_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_shutdown_async
#endif
g_dtls_connection_shutdown_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 688 & 0xffff
#if 688 > 0xffff
  movk ip0, 688 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_connection_shutdown_finish
  .p2align 4
  .type g_dtls_connection_shutdown_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_connection_shutdown_finish
#endif
g_dtls_connection_shutdown_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 689 & 0xffff
#if 689 > 0xffff
  movk ip0, 689 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_server_connection_get_type
  .p2align 4
  .type g_dtls_server_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_server_connection_get_type
#endif
g_dtls_server_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 690 & 0xffff
#if 690 > 0xffff
  movk ip0, 690 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dtls_server_connection_new
  .p2align 4
  .type g_dtls_server_connection_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dtls_server_connection_new
#endif
g_dtls_server_connection_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 691 & 0xffff
#if 691 > 0xffff
  movk ip0, 691 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblem_get_icon
  .p2align 4
  .type g_emblem_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblem_get_icon
#endif
g_emblem_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 692 & 0xffff
#if 692 > 0xffff
  movk ip0, 692 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblem_get_origin
  .p2align 4
  .type g_emblem_get_origin, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblem_get_origin
#endif
g_emblem_get_origin:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 693 & 0xffff
#if 693 > 0xffff
  movk ip0, 693 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblem_get_type
  .p2align 4
  .type g_emblem_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblem_get_type
#endif
g_emblem_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 694 & 0xffff
#if 694 > 0xffff
  movk ip0, 694 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblem_new
  .p2align 4
  .type g_emblem_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblem_new
#endif
g_emblem_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 695 & 0xffff
#if 695 > 0xffff
  movk ip0, 695 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblem_new_with_origin
  .p2align 4
  .type g_emblem_new_with_origin, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblem_new_with_origin
#endif
g_emblem_new_with_origin:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 696 & 0xffff
#if 696 > 0xffff
  movk ip0, 696 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblem_origin_get_type
  .p2align 4
  .type g_emblem_origin_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblem_origin_get_type
#endif
g_emblem_origin_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 697 & 0xffff
#if 697 > 0xffff
  movk ip0, 697 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblemed_icon_add_emblem
  .p2align 4
  .type g_emblemed_icon_add_emblem, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblemed_icon_add_emblem
#endif
g_emblemed_icon_add_emblem:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 698 & 0xffff
#if 698 > 0xffff
  movk ip0, 698 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblemed_icon_clear_emblems
  .p2align 4
  .type g_emblemed_icon_clear_emblems, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblemed_icon_clear_emblems
#endif
g_emblemed_icon_clear_emblems:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 699 & 0xffff
#if 699 > 0xffff
  movk ip0, 699 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblemed_icon_get_emblems
  .p2align 4
  .type g_emblemed_icon_get_emblems, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblemed_icon_get_emblems
#endif
g_emblemed_icon_get_emblems:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 700 & 0xffff
#if 700 > 0xffff
  movk ip0, 700 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblemed_icon_get_icon
  .p2align 4
  .type g_emblemed_icon_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblemed_icon_get_icon
#endif
g_emblemed_icon_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 701 & 0xffff
#if 701 > 0xffff
  movk ip0, 701 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblemed_icon_get_type
  .p2align 4
  .type g_emblemed_icon_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblemed_icon_get_type
#endif
g_emblemed_icon_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 702 & 0xffff
#if 702 > 0xffff
  movk ip0, 702 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_emblemed_icon_new
  .p2align 4
  .type g_emblemed_icon_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_emblemed_icon_new
#endif
g_emblemed_icon_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 703 & 0xffff
#if 703 > 0xffff
  movk ip0, 703 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_append_to
  .p2align 4
  .type g_file_append_to, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_append_to
#endif
g_file_append_to:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 704 & 0xffff
#if 704 > 0xffff
  movk ip0, 704 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_append_to_async
  .p2align 4
  .type g_file_append_to_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_append_to_async
#endif
g_file_append_to_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 705 & 0xffff
#if 705 > 0xffff
  movk ip0, 705 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_append_to_finish
  .p2align 4
  .type g_file_append_to_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_append_to_finish
#endif
g_file_append_to_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 706 & 0xffff
#if 706 > 0xffff
  movk ip0, 706 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_flags_get_type
  .p2align 4
  .type g_file_attribute_info_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_flags_get_type
#endif
g_file_attribute_info_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 707 & 0xffff
#if 707 > 0xffff
  movk ip0, 707 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_list_add
  .p2align 4
  .type g_file_attribute_info_list_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_list_add
#endif
g_file_attribute_info_list_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 708 & 0xffff
#if 708 > 0xffff
  movk ip0, 708 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_list_dup
  .p2align 4
  .type g_file_attribute_info_list_dup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_list_dup
#endif
g_file_attribute_info_list_dup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 709 & 0xffff
#if 709 > 0xffff
  movk ip0, 709 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_list_get_type
  .p2align 4
  .type g_file_attribute_info_list_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_list_get_type
#endif
g_file_attribute_info_list_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 710 & 0xffff
#if 710 > 0xffff
  movk ip0, 710 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_list_lookup
  .p2align 4
  .type g_file_attribute_info_list_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_list_lookup
#endif
g_file_attribute_info_list_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 711 & 0xffff
#if 711 > 0xffff
  movk ip0, 711 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_list_new
  .p2align 4
  .type g_file_attribute_info_list_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_list_new
#endif
g_file_attribute_info_list_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 712 & 0xffff
#if 712 > 0xffff
  movk ip0, 712 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_list_ref
  .p2align 4
  .type g_file_attribute_info_list_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_list_ref
#endif
g_file_attribute_info_list_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 713 & 0xffff
#if 713 > 0xffff
  movk ip0, 713 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_info_list_unref
  .p2align 4
  .type g_file_attribute_info_list_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_info_list_unref
#endif
g_file_attribute_info_list_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 714 & 0xffff
#if 714 > 0xffff
  movk ip0, 714 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_enumerate_namespace
  .p2align 4
  .type g_file_attribute_matcher_enumerate_namespace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_enumerate_namespace
#endif
g_file_attribute_matcher_enumerate_namespace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 715 & 0xffff
#if 715 > 0xffff
  movk ip0, 715 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_enumerate_next
  .p2align 4
  .type g_file_attribute_matcher_enumerate_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_enumerate_next
#endif
g_file_attribute_matcher_enumerate_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 716 & 0xffff
#if 716 > 0xffff
  movk ip0, 716 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_get_type
  .p2align 4
  .type g_file_attribute_matcher_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_get_type
#endif
g_file_attribute_matcher_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 717 & 0xffff
#if 717 > 0xffff
  movk ip0, 717 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_matches
  .p2align 4
  .type g_file_attribute_matcher_matches, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_matches
#endif
g_file_attribute_matcher_matches:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 718 & 0xffff
#if 718 > 0xffff
  movk ip0, 718 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_matches_only
  .p2align 4
  .type g_file_attribute_matcher_matches_only, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_matches_only
#endif
g_file_attribute_matcher_matches_only:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 719 & 0xffff
#if 719 > 0xffff
  movk ip0, 719 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_new
  .p2align 4
  .type g_file_attribute_matcher_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_new
#endif
g_file_attribute_matcher_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 720 & 0xffff
#if 720 > 0xffff
  movk ip0, 720 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_ref
  .p2align 4
  .type g_file_attribute_matcher_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_ref
#endif
g_file_attribute_matcher_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 721 & 0xffff
#if 721 > 0xffff
  movk ip0, 721 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_subtract
  .p2align 4
  .type g_file_attribute_matcher_subtract, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_subtract
#endif
g_file_attribute_matcher_subtract:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 722 & 0xffff
#if 722 > 0xffff
  movk ip0, 722 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_to_string
  .p2align 4
  .type g_file_attribute_matcher_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_to_string
#endif
g_file_attribute_matcher_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 723 & 0xffff
#if 723 > 0xffff
  movk ip0, 723 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_matcher_unref
  .p2align 4
  .type g_file_attribute_matcher_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_matcher_unref
#endif
g_file_attribute_matcher_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 724 & 0xffff
#if 724 > 0xffff
  movk ip0, 724 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_status_get_type
  .p2align 4
  .type g_file_attribute_status_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_status_get_type
#endif
g_file_attribute_status_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 725 & 0xffff
#if 725 > 0xffff
  movk ip0, 725 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_attribute_type_get_type
  .p2align 4
  .type g_file_attribute_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_attribute_type_get_type
#endif
g_file_attribute_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 726 & 0xffff
#if 726 > 0xffff
  movk ip0, 726 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_build_attribute_list_for_copy
  .p2align 4
  .type g_file_build_attribute_list_for_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_build_attribute_list_for_copy
#endif
g_file_build_attribute_list_for_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 727 & 0xffff
#if 727 > 0xffff
  movk ip0, 727 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_copy
  .p2align 4
  .type g_file_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_copy
#endif
g_file_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 728 & 0xffff
#if 728 > 0xffff
  movk ip0, 728 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_copy_async
  .p2align 4
  .type g_file_copy_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_copy_async
#endif
g_file_copy_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 729 & 0xffff
#if 729 > 0xffff
  movk ip0, 729 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_copy_async_with_closures
  .p2align 4
  .type g_file_copy_async_with_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_copy_async_with_closures
#endif
g_file_copy_async_with_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 730 & 0xffff
#if 730 > 0xffff
  movk ip0, 730 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_copy_attributes
  .p2align 4
  .type g_file_copy_attributes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_copy_attributes
#endif
g_file_copy_attributes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 731 & 0xffff
#if 731 > 0xffff
  movk ip0, 731 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_copy_finish
  .p2align 4
  .type g_file_copy_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_copy_finish
#endif
g_file_copy_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 732 & 0xffff
#if 732 > 0xffff
  movk ip0, 732 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_copy_flags_get_type
  .p2align 4
  .type g_file_copy_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_copy_flags_get_type
#endif
g_file_copy_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 733 & 0xffff
#if 733 > 0xffff
  movk ip0, 733 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_create
  .p2align 4
  .type g_file_create, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_create
#endif
g_file_create:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 734 & 0xffff
#if 734 > 0xffff
  movk ip0, 734 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_create_async
  .p2align 4
  .type g_file_create_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_create_async
#endif
g_file_create_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 735 & 0xffff
#if 735 > 0xffff
  movk ip0, 735 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_create_finish
  .p2align 4
  .type g_file_create_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_create_finish
#endif
g_file_create_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 736 & 0xffff
#if 736 > 0xffff
  movk ip0, 736 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_create_flags_get_type
  .p2align 4
  .type g_file_create_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_create_flags_get_type
#endif
g_file_create_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 737 & 0xffff
#if 737 > 0xffff
  movk ip0, 737 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_create_readwrite
  .p2align 4
  .type g_file_create_readwrite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_create_readwrite
#endif
g_file_create_readwrite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 738 & 0xffff
#if 738 > 0xffff
  movk ip0, 738 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_create_readwrite_async
  .p2align 4
  .type g_file_create_readwrite_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_create_readwrite_async
#endif
g_file_create_readwrite_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 739 & 0xffff
#if 739 > 0xffff
  movk ip0, 739 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_create_readwrite_finish
  .p2align 4
  .type g_file_create_readwrite_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_create_readwrite_finish
#endif
g_file_create_readwrite_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 740 & 0xffff
#if 740 > 0xffff
  movk ip0, 740 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_delete
  .p2align 4
  .type g_file_delete, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_delete
#endif
g_file_delete:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 741 & 0xffff
#if 741 > 0xffff
  movk ip0, 741 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_delete_async
  .p2align 4
  .type g_file_delete_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_delete_async
#endif
g_file_delete_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 742 & 0xffff
#if 742 > 0xffff
  movk ip0, 742 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_delete_finish
  .p2align 4
  .type g_file_delete_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_delete_finish
#endif
g_file_delete_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 743 & 0xffff
#if 743 > 0xffff
  movk ip0, 743 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_descriptor_based_get_fd
  .p2align 4
  .type g_file_descriptor_based_get_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_descriptor_based_get_fd
#endif
g_file_descriptor_based_get_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 744 & 0xffff
#if 744 > 0xffff
  movk ip0, 744 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_descriptor_based_get_type
  .p2align 4
  .type g_file_descriptor_based_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_descriptor_based_get_type
#endif
g_file_descriptor_based_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 745 & 0xffff
#if 745 > 0xffff
  movk ip0, 745 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_dup
  .p2align 4
  .type g_file_dup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_dup
#endif
g_file_dup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 746 & 0xffff
#if 746 > 0xffff
  movk ip0, 746 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_eject_mountable
  .p2align 4
  .type g_file_eject_mountable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_eject_mountable
#endif
g_file_eject_mountable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 747 & 0xffff
#if 747 > 0xffff
  movk ip0, 747 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_eject_mountable_finish
  .p2align 4
  .type g_file_eject_mountable_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_eject_mountable_finish
#endif
g_file_eject_mountable_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 748 & 0xffff
#if 748 > 0xffff
  movk ip0, 748 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_eject_mountable_with_operation
  .p2align 4
  .type g_file_eject_mountable_with_operation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_eject_mountable_with_operation
#endif
g_file_eject_mountable_with_operation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+5992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+5992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 749 & 0xffff
#if 749 > 0xffff
  movk ip0, 749 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_eject_mountable_with_operation_finish
  .p2align 4
  .type g_file_eject_mountable_with_operation_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_eject_mountable_with_operation_finish
#endif
g_file_eject_mountable_with_operation_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 750 & 0xffff
#if 750 > 0xffff
  movk ip0, 750 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerate_children
  .p2align 4
  .type g_file_enumerate_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerate_children
#endif
g_file_enumerate_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 751 & 0xffff
#if 751 > 0xffff
  movk ip0, 751 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerate_children_async
  .p2align 4
  .type g_file_enumerate_children_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerate_children_async
#endif
g_file_enumerate_children_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 752 & 0xffff
#if 752 > 0xffff
  movk ip0, 752 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerate_children_finish
  .p2align 4
  .type g_file_enumerate_children_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerate_children_finish
#endif
g_file_enumerate_children_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 753 & 0xffff
#if 753 > 0xffff
  movk ip0, 753 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_close
  .p2align 4
  .type g_file_enumerator_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_close
#endif
g_file_enumerator_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 754 & 0xffff
#if 754 > 0xffff
  movk ip0, 754 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_close_async
  .p2align 4
  .type g_file_enumerator_close_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_close_async
#endif
g_file_enumerator_close_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 755 & 0xffff
#if 755 > 0xffff
  movk ip0, 755 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_close_finish
  .p2align 4
  .type g_file_enumerator_close_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_close_finish
#endif
g_file_enumerator_close_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 756 & 0xffff
#if 756 > 0xffff
  movk ip0, 756 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_get_child
  .p2align 4
  .type g_file_enumerator_get_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_get_child
#endif
g_file_enumerator_get_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 757 & 0xffff
#if 757 > 0xffff
  movk ip0, 757 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_get_container
  .p2align 4
  .type g_file_enumerator_get_container, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_get_container
#endif
g_file_enumerator_get_container:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 758 & 0xffff
#if 758 > 0xffff
  movk ip0, 758 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_get_type
  .p2align 4
  .type g_file_enumerator_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_get_type
#endif
g_file_enumerator_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 759 & 0xffff
#if 759 > 0xffff
  movk ip0, 759 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_has_pending
  .p2align 4
  .type g_file_enumerator_has_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_has_pending
#endif
g_file_enumerator_has_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 760 & 0xffff
#if 760 > 0xffff
  movk ip0, 760 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_is_closed
  .p2align 4
  .type g_file_enumerator_is_closed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_is_closed
#endif
g_file_enumerator_is_closed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 761 & 0xffff
#if 761 > 0xffff
  movk ip0, 761 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_iterate
  .p2align 4
  .type g_file_enumerator_iterate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_iterate
#endif
g_file_enumerator_iterate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 762 & 0xffff
#if 762 > 0xffff
  movk ip0, 762 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_next_file
  .p2align 4
  .type g_file_enumerator_next_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_next_file
#endif
g_file_enumerator_next_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 763 & 0xffff
#if 763 > 0xffff
  movk ip0, 763 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_next_files_async
  .p2align 4
  .type g_file_enumerator_next_files_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_next_files_async
#endif
g_file_enumerator_next_files_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 764 & 0xffff
#if 764 > 0xffff
  movk ip0, 764 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_next_files_finish
  .p2align 4
  .type g_file_enumerator_next_files_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_next_files_finish
#endif
g_file_enumerator_next_files_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 765 & 0xffff
#if 765 > 0xffff
  movk ip0, 765 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_enumerator_set_pending
  .p2align 4
  .type g_file_enumerator_set_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_enumerator_set_pending
#endif
g_file_enumerator_set_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 766 & 0xffff
#if 766 > 0xffff
  movk ip0, 766 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_equal
  .p2align 4
  .type g_file_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_equal
#endif
g_file_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 767 & 0xffff
#if 767 > 0xffff
  movk ip0, 767 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_find_enclosing_mount
  .p2align 4
  .type g_file_find_enclosing_mount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_find_enclosing_mount
#endif
g_file_find_enclosing_mount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 768 & 0xffff
#if 768 > 0xffff
  movk ip0, 768 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_find_enclosing_mount_async
  .p2align 4
  .type g_file_find_enclosing_mount_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_find_enclosing_mount_async
#endif
g_file_find_enclosing_mount_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 769 & 0xffff
#if 769 > 0xffff
  movk ip0, 769 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_find_enclosing_mount_finish
  .p2align 4
  .type g_file_find_enclosing_mount_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_find_enclosing_mount_finish
#endif
g_file_find_enclosing_mount_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 770 & 0xffff
#if 770 > 0xffff
  movk ip0, 770 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_basename
  .p2align 4
  .type g_file_get_basename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_basename
#endif
g_file_get_basename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 771 & 0xffff
#if 771 > 0xffff
  movk ip0, 771 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_child
  .p2align 4
  .type g_file_get_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_child
#endif
g_file_get_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 772 & 0xffff
#if 772 > 0xffff
  movk ip0, 772 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_child_for_display_name
  .p2align 4
  .type g_file_get_child_for_display_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_child_for_display_name
#endif
g_file_get_child_for_display_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 773 & 0xffff
#if 773 > 0xffff
  movk ip0, 773 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_parent
  .p2align 4
  .type g_file_get_parent, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_parent
#endif
g_file_get_parent:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 774 & 0xffff
#if 774 > 0xffff
  movk ip0, 774 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_parse_name
  .p2align 4
  .type g_file_get_parse_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_parse_name
#endif
g_file_get_parse_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 775 & 0xffff
#if 775 > 0xffff
  movk ip0, 775 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_path
  .p2align 4
  .type g_file_get_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_path
#endif
g_file_get_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 776 & 0xffff
#if 776 > 0xffff
  movk ip0, 776 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_relative_path
  .p2align 4
  .type g_file_get_relative_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_relative_path
#endif
g_file_get_relative_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 777 & 0xffff
#if 777 > 0xffff
  movk ip0, 777 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_type
  .p2align 4
  .type g_file_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_type
#endif
g_file_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 778 & 0xffff
#if 778 > 0xffff
  movk ip0, 778 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_uri
  .p2align 4
  .type g_file_get_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_uri
#endif
g_file_get_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 779 & 0xffff
#if 779 > 0xffff
  movk ip0, 779 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_uri_scheme
  .p2align 4
  .type g_file_get_uri_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_uri_scheme
#endif
g_file_get_uri_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 780 & 0xffff
#if 780 > 0xffff
  movk ip0, 780 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_has_parent
  .p2align 4
  .type g_file_has_parent, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_has_parent
#endif
g_file_has_parent:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 781 & 0xffff
#if 781 > 0xffff
  movk ip0, 781 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_has_prefix
  .p2align 4
  .type g_file_has_prefix, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_has_prefix
#endif
g_file_has_prefix:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 782 & 0xffff
#if 782 > 0xffff
  movk ip0, 782 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_has_uri_scheme
  .p2align 4
  .type g_file_has_uri_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_has_uri_scheme
#endif
g_file_has_uri_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 783 & 0xffff
#if 783 > 0xffff
  movk ip0, 783 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_hash
  .p2align 4
  .type g_file_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_hash
#endif
g_file_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 784 & 0xffff
#if 784 > 0xffff
  movk ip0, 784 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_icon_get_file
  .p2align 4
  .type g_file_icon_get_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_icon_get_file
#endif
g_file_icon_get_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 785 & 0xffff
#if 785 > 0xffff
  movk ip0, 785 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_icon_get_type
  .p2align 4
  .type g_file_icon_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_icon_get_type
#endif
g_file_icon_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 786 & 0xffff
#if 786 > 0xffff
  movk ip0, 786 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_icon_new
  .p2align 4
  .type g_file_icon_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_icon_new
#endif
g_file_icon_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 787 & 0xffff
#if 787 > 0xffff
  movk ip0, 787 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_clear_status
  .p2align 4
  .type g_file_info_clear_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_clear_status
#endif
g_file_info_clear_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 788 & 0xffff
#if 788 > 0xffff
  movk ip0, 788 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_copy_into
  .p2align 4
  .type g_file_info_copy_into, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_copy_into
#endif
g_file_info_copy_into:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 789 & 0xffff
#if 789 > 0xffff
  movk ip0, 789 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_dup
  .p2align 4
  .type g_file_info_dup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_dup
#endif
g_file_info_dup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 790 & 0xffff
#if 790 > 0xffff
  movk ip0, 790 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_access_date_time
  .p2align 4
  .type g_file_info_get_access_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_access_date_time
#endif
g_file_info_get_access_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 791 & 0xffff
#if 791 > 0xffff
  movk ip0, 791 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_as_string
  .p2align 4
  .type g_file_info_get_attribute_as_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_as_string
#endif
g_file_info_get_attribute_as_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 792 & 0xffff
#if 792 > 0xffff
  movk ip0, 792 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_boolean
  .p2align 4
  .type g_file_info_get_attribute_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_boolean
#endif
g_file_info_get_attribute_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 793 & 0xffff
#if 793 > 0xffff
  movk ip0, 793 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_byte_string
  .p2align 4
  .type g_file_info_get_attribute_byte_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_byte_string
#endif
g_file_info_get_attribute_byte_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 794 & 0xffff
#if 794 > 0xffff
  movk ip0, 794 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_data
  .p2align 4
  .type g_file_info_get_attribute_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_data
#endif
g_file_info_get_attribute_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 795 & 0xffff
#if 795 > 0xffff
  movk ip0, 795 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_file_path
  .p2align 4
  .type g_file_info_get_attribute_file_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_file_path
#endif
g_file_info_get_attribute_file_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 796 & 0xffff
#if 796 > 0xffff
  movk ip0, 796 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_int32
  .p2align 4
  .type g_file_info_get_attribute_int32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_int32
#endif
g_file_info_get_attribute_int32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 797 & 0xffff
#if 797 > 0xffff
  movk ip0, 797 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_int64
  .p2align 4
  .type g_file_info_get_attribute_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_int64
#endif
g_file_info_get_attribute_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 798 & 0xffff
#if 798 > 0xffff
  movk ip0, 798 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_object
  .p2align 4
  .type g_file_info_get_attribute_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_object
#endif
g_file_info_get_attribute_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 799 & 0xffff
#if 799 > 0xffff
  movk ip0, 799 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_status
  .p2align 4
  .type g_file_info_get_attribute_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_status
#endif
g_file_info_get_attribute_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 800 & 0xffff
#if 800 > 0xffff
  movk ip0, 800 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_string
  .p2align 4
  .type g_file_info_get_attribute_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_string
#endif
g_file_info_get_attribute_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 801 & 0xffff
#if 801 > 0xffff
  movk ip0, 801 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_stringv
  .p2align 4
  .type g_file_info_get_attribute_stringv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_stringv
#endif
g_file_info_get_attribute_stringv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 802 & 0xffff
#if 802 > 0xffff
  movk ip0, 802 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_type
  .p2align 4
  .type g_file_info_get_attribute_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_type
#endif
g_file_info_get_attribute_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 803 & 0xffff
#if 803 > 0xffff
  movk ip0, 803 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_uint32
  .p2align 4
  .type g_file_info_get_attribute_uint32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_uint32
#endif
g_file_info_get_attribute_uint32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 804 & 0xffff
#if 804 > 0xffff
  movk ip0, 804 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_attribute_uint64
  .p2align 4
  .type g_file_info_get_attribute_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_attribute_uint64
#endif
g_file_info_get_attribute_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 805 & 0xffff
#if 805 > 0xffff
  movk ip0, 805 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_content_type
  .p2align 4
  .type g_file_info_get_content_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_content_type
#endif
g_file_info_get_content_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 806 & 0xffff
#if 806 > 0xffff
  movk ip0, 806 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_creation_date_time
  .p2align 4
  .type g_file_info_get_creation_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_creation_date_time
#endif
g_file_info_get_creation_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 807 & 0xffff
#if 807 > 0xffff
  movk ip0, 807 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_deletion_date
  .p2align 4
  .type g_file_info_get_deletion_date, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_deletion_date
#endif
g_file_info_get_deletion_date:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 808 & 0xffff
#if 808 > 0xffff
  movk ip0, 808 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_display_name
  .p2align 4
  .type g_file_info_get_display_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_display_name
#endif
g_file_info_get_display_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 809 & 0xffff
#if 809 > 0xffff
  movk ip0, 809 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_edit_name
  .p2align 4
  .type g_file_info_get_edit_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_edit_name
#endif
g_file_info_get_edit_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 810 & 0xffff
#if 810 > 0xffff
  movk ip0, 810 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_etag
  .p2align 4
  .type g_file_info_get_etag, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_etag
#endif
g_file_info_get_etag:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 811 & 0xffff
#if 811 > 0xffff
  movk ip0, 811 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_file_type
  .p2align 4
  .type g_file_info_get_file_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_file_type
#endif
g_file_info_get_file_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 812 & 0xffff
#if 812 > 0xffff
  movk ip0, 812 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_icon
  .p2align 4
  .type g_file_info_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_icon
#endif
g_file_info_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 813 & 0xffff
#if 813 > 0xffff
  movk ip0, 813 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_is_backup
  .p2align 4
  .type g_file_info_get_is_backup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_is_backup
#endif
g_file_info_get_is_backup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 814 & 0xffff
#if 814 > 0xffff
  movk ip0, 814 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_is_hidden
  .p2align 4
  .type g_file_info_get_is_hidden, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_is_hidden
#endif
g_file_info_get_is_hidden:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 815 & 0xffff
#if 815 > 0xffff
  movk ip0, 815 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_is_symlink
  .p2align 4
  .type g_file_info_get_is_symlink, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_is_symlink
#endif
g_file_info_get_is_symlink:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 816 & 0xffff
#if 816 > 0xffff
  movk ip0, 816 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_modification_date_time
  .p2align 4
  .type g_file_info_get_modification_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_modification_date_time
#endif
g_file_info_get_modification_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 817 & 0xffff
#if 817 > 0xffff
  movk ip0, 817 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_modification_time
  .p2align 4
  .type g_file_info_get_modification_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_modification_time
#endif
g_file_info_get_modification_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 818 & 0xffff
#if 818 > 0xffff
  movk ip0, 818 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_name
  .p2align 4
  .type g_file_info_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_name
#endif
g_file_info_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 819 & 0xffff
#if 819 > 0xffff
  movk ip0, 819 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_size
  .p2align 4
  .type g_file_info_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_size
#endif
g_file_info_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 820 & 0xffff
#if 820 > 0xffff
  movk ip0, 820 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_sort_order
  .p2align 4
  .type g_file_info_get_sort_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_sort_order
#endif
g_file_info_get_sort_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 821 & 0xffff
#if 821 > 0xffff
  movk ip0, 821 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_symbolic_icon
  .p2align 4
  .type g_file_info_get_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_symbolic_icon
#endif
g_file_info_get_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 822 & 0xffff
#if 822 > 0xffff
  movk ip0, 822 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_symlink_target
  .p2align 4
  .type g_file_info_get_symlink_target, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_symlink_target
#endif
g_file_info_get_symlink_target:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 823 & 0xffff
#if 823 > 0xffff
  movk ip0, 823 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_get_type
  .p2align 4
  .type g_file_info_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_get_type
#endif
g_file_info_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 824 & 0xffff
#if 824 > 0xffff
  movk ip0, 824 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_has_attribute
  .p2align 4
  .type g_file_info_has_attribute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_has_attribute
#endif
g_file_info_has_attribute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 825 & 0xffff
#if 825 > 0xffff
  movk ip0, 825 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_has_namespace
  .p2align 4
  .type g_file_info_has_namespace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_has_namespace
#endif
g_file_info_has_namespace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 826 & 0xffff
#if 826 > 0xffff
  movk ip0, 826 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_list_attributes
  .p2align 4
  .type g_file_info_list_attributes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_list_attributes
#endif
g_file_info_list_attributes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 827 & 0xffff
#if 827 > 0xffff
  movk ip0, 827 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_new
  .p2align 4
  .type g_file_info_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_new
#endif
g_file_info_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 828 & 0xffff
#if 828 > 0xffff
  movk ip0, 828 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_remove_attribute
  .p2align 4
  .type g_file_info_remove_attribute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_remove_attribute
#endif
g_file_info_remove_attribute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 829 & 0xffff
#if 829 > 0xffff
  movk ip0, 829 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_access_date_time
  .p2align 4
  .type g_file_info_set_access_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_access_date_time
#endif
g_file_info_set_access_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 830 & 0xffff
#if 830 > 0xffff
  movk ip0, 830 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute
  .p2align 4
  .type g_file_info_set_attribute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute
#endif
g_file_info_set_attribute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 831 & 0xffff
#if 831 > 0xffff
  movk ip0, 831 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_boolean
  .p2align 4
  .type g_file_info_set_attribute_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_boolean
#endif
g_file_info_set_attribute_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 832 & 0xffff
#if 832 > 0xffff
  movk ip0, 832 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_byte_string
  .p2align 4
  .type g_file_info_set_attribute_byte_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_byte_string
#endif
g_file_info_set_attribute_byte_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 833 & 0xffff
#if 833 > 0xffff
  movk ip0, 833 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_file_path
  .p2align 4
  .type g_file_info_set_attribute_file_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_file_path
#endif
g_file_info_set_attribute_file_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 834 & 0xffff
#if 834 > 0xffff
  movk ip0, 834 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_int32
  .p2align 4
  .type g_file_info_set_attribute_int32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_int32
#endif
g_file_info_set_attribute_int32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 835 & 0xffff
#if 835 > 0xffff
  movk ip0, 835 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_int64
  .p2align 4
  .type g_file_info_set_attribute_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_int64
#endif
g_file_info_set_attribute_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 836 & 0xffff
#if 836 > 0xffff
  movk ip0, 836 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_mask
  .p2align 4
  .type g_file_info_set_attribute_mask, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_mask
#endif
g_file_info_set_attribute_mask:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 837 & 0xffff
#if 837 > 0xffff
  movk ip0, 837 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_object
  .p2align 4
  .type g_file_info_set_attribute_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_object
#endif
g_file_info_set_attribute_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 838 & 0xffff
#if 838 > 0xffff
  movk ip0, 838 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_status
  .p2align 4
  .type g_file_info_set_attribute_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_status
#endif
g_file_info_set_attribute_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 839 & 0xffff
#if 839 > 0xffff
  movk ip0, 839 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_string
  .p2align 4
  .type g_file_info_set_attribute_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_string
#endif
g_file_info_set_attribute_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 840 & 0xffff
#if 840 > 0xffff
  movk ip0, 840 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_stringv
  .p2align 4
  .type g_file_info_set_attribute_stringv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_stringv
#endif
g_file_info_set_attribute_stringv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 841 & 0xffff
#if 841 > 0xffff
  movk ip0, 841 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_uint32
  .p2align 4
  .type g_file_info_set_attribute_uint32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_uint32
#endif
g_file_info_set_attribute_uint32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 842 & 0xffff
#if 842 > 0xffff
  movk ip0, 842 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_attribute_uint64
  .p2align 4
  .type g_file_info_set_attribute_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_attribute_uint64
#endif
g_file_info_set_attribute_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 843 & 0xffff
#if 843 > 0xffff
  movk ip0, 843 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_content_type
  .p2align 4
  .type g_file_info_set_content_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_content_type
#endif
g_file_info_set_content_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 844 & 0xffff
#if 844 > 0xffff
  movk ip0, 844 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_creation_date_time
  .p2align 4
  .type g_file_info_set_creation_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_creation_date_time
#endif
g_file_info_set_creation_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 845 & 0xffff
#if 845 > 0xffff
  movk ip0, 845 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_display_name
  .p2align 4
  .type g_file_info_set_display_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_display_name
#endif
g_file_info_set_display_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 846 & 0xffff
#if 846 > 0xffff
  movk ip0, 846 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_edit_name
  .p2align 4
  .type g_file_info_set_edit_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_edit_name
#endif
g_file_info_set_edit_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 847 & 0xffff
#if 847 > 0xffff
  movk ip0, 847 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_file_type
  .p2align 4
  .type g_file_info_set_file_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_file_type
#endif
g_file_info_set_file_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 848 & 0xffff
#if 848 > 0xffff
  movk ip0, 848 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_icon
  .p2align 4
  .type g_file_info_set_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_icon
#endif
g_file_info_set_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 849 & 0xffff
#if 849 > 0xffff
  movk ip0, 849 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_is_hidden
  .p2align 4
  .type g_file_info_set_is_hidden, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_is_hidden
#endif
g_file_info_set_is_hidden:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 850 & 0xffff
#if 850 > 0xffff
  movk ip0, 850 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_is_symlink
  .p2align 4
  .type g_file_info_set_is_symlink, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_is_symlink
#endif
g_file_info_set_is_symlink:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 851 & 0xffff
#if 851 > 0xffff
  movk ip0, 851 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_modification_date_time
  .p2align 4
  .type g_file_info_set_modification_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_modification_date_time
#endif
g_file_info_set_modification_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 852 & 0xffff
#if 852 > 0xffff
  movk ip0, 852 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_modification_time
  .p2align 4
  .type g_file_info_set_modification_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_modification_time
#endif
g_file_info_set_modification_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 853 & 0xffff
#if 853 > 0xffff
  movk ip0, 853 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_name
  .p2align 4
  .type g_file_info_set_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_name
#endif
g_file_info_set_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 854 & 0xffff
#if 854 > 0xffff
  movk ip0, 854 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_size
  .p2align 4
  .type g_file_info_set_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_size
#endif
g_file_info_set_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 855 & 0xffff
#if 855 > 0xffff
  movk ip0, 855 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_sort_order
  .p2align 4
  .type g_file_info_set_sort_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_sort_order
#endif
g_file_info_set_sort_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 856 & 0xffff
#if 856 > 0xffff
  movk ip0, 856 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_symbolic_icon
  .p2align 4
  .type g_file_info_set_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_symbolic_icon
#endif
g_file_info_set_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 857 & 0xffff
#if 857 > 0xffff
  movk ip0, 857 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_set_symlink_target
  .p2align 4
  .type g_file_info_set_symlink_target, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_set_symlink_target
#endif
g_file_info_set_symlink_target:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 858 & 0xffff
#if 858 > 0xffff
  movk ip0, 858 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_info_unset_attribute_mask
  .p2align 4
  .type g_file_info_unset_attribute_mask, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_info_unset_attribute_mask
#endif
g_file_info_unset_attribute_mask:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 859 & 0xffff
#if 859 > 0xffff
  movk ip0, 859 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_input_stream_get_type
  .p2align 4
  .type g_file_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_input_stream_get_type
#endif
g_file_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 860 & 0xffff
#if 860 > 0xffff
  movk ip0, 860 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_input_stream_query_info
  .p2align 4
  .type g_file_input_stream_query_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_input_stream_query_info
#endif
g_file_input_stream_query_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 861 & 0xffff
#if 861 > 0xffff
  movk ip0, 861 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_input_stream_query_info_async
  .p2align 4
  .type g_file_input_stream_query_info_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_input_stream_query_info_async
#endif
g_file_input_stream_query_info_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 862 & 0xffff
#if 862 > 0xffff
  movk ip0, 862 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_input_stream_query_info_finish
  .p2align 4
  .type g_file_input_stream_query_info_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_input_stream_query_info_finish
#endif
g_file_input_stream_query_info_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 863 & 0xffff
#if 863 > 0xffff
  movk ip0, 863 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_io_stream_get_etag
  .p2align 4
  .type g_file_io_stream_get_etag, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_io_stream_get_etag
#endif
g_file_io_stream_get_etag:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 864 & 0xffff
#if 864 > 0xffff
  movk ip0, 864 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_io_stream_get_type
  .p2align 4
  .type g_file_io_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_io_stream_get_type
#endif
g_file_io_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 865 & 0xffff
#if 865 > 0xffff
  movk ip0, 865 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_io_stream_query_info
  .p2align 4
  .type g_file_io_stream_query_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_io_stream_query_info
#endif
g_file_io_stream_query_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 866 & 0xffff
#if 866 > 0xffff
  movk ip0, 866 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_io_stream_query_info_async
  .p2align 4
  .type g_file_io_stream_query_info_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_io_stream_query_info_async
#endif
g_file_io_stream_query_info_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 867 & 0xffff
#if 867 > 0xffff
  movk ip0, 867 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_io_stream_query_info_finish
  .p2align 4
  .type g_file_io_stream_query_info_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_io_stream_query_info_finish
#endif
g_file_io_stream_query_info_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 868 & 0xffff
#if 868 > 0xffff
  movk ip0, 868 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_is_native
  .p2align 4
  .type g_file_is_native, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_is_native
#endif
g_file_is_native:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 869 & 0xffff
#if 869 > 0xffff
  movk ip0, 869 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_bytes
  .p2align 4
  .type g_file_load_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_bytes
#endif
g_file_load_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 870 & 0xffff
#if 870 > 0xffff
  movk ip0, 870 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_bytes_async
  .p2align 4
  .type g_file_load_bytes_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_bytes_async
#endif
g_file_load_bytes_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 871 & 0xffff
#if 871 > 0xffff
  movk ip0, 871 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_bytes_finish
  .p2align 4
  .type g_file_load_bytes_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_bytes_finish
#endif
g_file_load_bytes_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 872 & 0xffff
#if 872 > 0xffff
  movk ip0, 872 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_contents
  .p2align 4
  .type g_file_load_contents, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_contents
#endif
g_file_load_contents:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 873 & 0xffff
#if 873 > 0xffff
  movk ip0, 873 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_contents_async
  .p2align 4
  .type g_file_load_contents_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_contents_async
#endif
g_file_load_contents_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+6992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+6992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 874 & 0xffff
#if 874 > 0xffff
  movk ip0, 874 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_contents_finish
  .p2align 4
  .type g_file_load_contents_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_contents_finish
#endif
g_file_load_contents_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 875 & 0xffff
#if 875 > 0xffff
  movk ip0, 875 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_partial_contents_async
  .p2align 4
  .type g_file_load_partial_contents_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_partial_contents_async
#endif
g_file_load_partial_contents_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 876 & 0xffff
#if 876 > 0xffff
  movk ip0, 876 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_load_partial_contents_finish
  .p2align 4
  .type g_file_load_partial_contents_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_load_partial_contents_finish
#endif
g_file_load_partial_contents_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 877 & 0xffff
#if 877 > 0xffff
  movk ip0, 877 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_make_directory
  .p2align 4
  .type g_file_make_directory, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_make_directory
#endif
g_file_make_directory:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 878 & 0xffff
#if 878 > 0xffff
  movk ip0, 878 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_make_directory_async
  .p2align 4
  .type g_file_make_directory_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_make_directory_async
#endif
g_file_make_directory_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 879 & 0xffff
#if 879 > 0xffff
  movk ip0, 879 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_make_directory_finish
  .p2align 4
  .type g_file_make_directory_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_make_directory_finish
#endif
g_file_make_directory_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 880 & 0xffff
#if 880 > 0xffff
  movk ip0, 880 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_make_directory_with_parents
  .p2align 4
  .type g_file_make_directory_with_parents, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_make_directory_with_parents
#endif
g_file_make_directory_with_parents:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 881 & 0xffff
#if 881 > 0xffff
  movk ip0, 881 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_make_symbolic_link
  .p2align 4
  .type g_file_make_symbolic_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_make_symbolic_link
#endif
g_file_make_symbolic_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 882 & 0xffff
#if 882 > 0xffff
  movk ip0, 882 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_make_symbolic_link_async
  .p2align 4
  .type g_file_make_symbolic_link_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_make_symbolic_link_async
#endif
g_file_make_symbolic_link_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 883 & 0xffff
#if 883 > 0xffff
  movk ip0, 883 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_make_symbolic_link_finish
  .p2align 4
  .type g_file_make_symbolic_link_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_make_symbolic_link_finish
#endif
g_file_make_symbolic_link_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 884 & 0xffff
#if 884 > 0xffff
  movk ip0, 884 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_measure_disk_usage
  .p2align 4
  .type g_file_measure_disk_usage, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_measure_disk_usage
#endif
g_file_measure_disk_usage:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 885 & 0xffff
#if 885 > 0xffff
  movk ip0, 885 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_measure_disk_usage_async
  .p2align 4
  .type g_file_measure_disk_usage_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_measure_disk_usage_async
#endif
g_file_measure_disk_usage_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 886 & 0xffff
#if 886 > 0xffff
  movk ip0, 886 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_measure_disk_usage_finish
  .p2align 4
  .type g_file_measure_disk_usage_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_measure_disk_usage_finish
#endif
g_file_measure_disk_usage_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 887 & 0xffff
#if 887 > 0xffff
  movk ip0, 887 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_measure_flags_get_type
  .p2align 4
  .type g_file_measure_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_measure_flags_get_type
#endif
g_file_measure_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 888 & 0xffff
#if 888 > 0xffff
  movk ip0, 888 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor
  .p2align 4
  .type g_file_monitor, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor
#endif
g_file_monitor:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 889 & 0xffff
#if 889 > 0xffff
  movk ip0, 889 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_cancel
  .p2align 4
  .type g_file_monitor_cancel, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_cancel
#endif
g_file_monitor_cancel:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 890 & 0xffff
#if 890 > 0xffff
  movk ip0, 890 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_directory
  .p2align 4
  .type g_file_monitor_directory, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_directory
#endif
g_file_monitor_directory:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 891 & 0xffff
#if 891 > 0xffff
  movk ip0, 891 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_emit_event
  .p2align 4
  .type g_file_monitor_emit_event, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_emit_event
#endif
g_file_monitor_emit_event:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 892 & 0xffff
#if 892 > 0xffff
  movk ip0, 892 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_event_get_type
  .p2align 4
  .type g_file_monitor_event_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_event_get_type
#endif
g_file_monitor_event_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 893 & 0xffff
#if 893 > 0xffff
  movk ip0, 893 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_file
  .p2align 4
  .type g_file_monitor_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_file
#endif
g_file_monitor_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 894 & 0xffff
#if 894 > 0xffff
  movk ip0, 894 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_flags_get_type
  .p2align 4
  .type g_file_monitor_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_flags_get_type
#endif
g_file_monitor_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 895 & 0xffff
#if 895 > 0xffff
  movk ip0, 895 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_get_type
  .p2align 4
  .type g_file_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_get_type
#endif
g_file_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 896 & 0xffff
#if 896 > 0xffff
  movk ip0, 896 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_is_cancelled
  .p2align 4
  .type g_file_monitor_is_cancelled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_is_cancelled
#endif
g_file_monitor_is_cancelled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 897 & 0xffff
#if 897 > 0xffff
  movk ip0, 897 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_set_rate_limit
  .p2align 4
  .type g_file_monitor_set_rate_limit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_set_rate_limit
#endif
g_file_monitor_set_rate_limit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 898 & 0xffff
#if 898 > 0xffff
  movk ip0, 898 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_monitor_source_handle_event
  .p2align 4
  .type g_file_monitor_source_handle_event, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_monitor_source_handle_event
#endif
g_file_monitor_source_handle_event:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 899 & 0xffff
#if 899 > 0xffff
  movk ip0, 899 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_mount_enclosing_volume
  .p2align 4
  .type g_file_mount_enclosing_volume, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_mount_enclosing_volume
#endif
g_file_mount_enclosing_volume:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 900 & 0xffff
#if 900 > 0xffff
  movk ip0, 900 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_mount_enclosing_volume_finish
  .p2align 4
  .type g_file_mount_enclosing_volume_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_mount_enclosing_volume_finish
#endif
g_file_mount_enclosing_volume_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 901 & 0xffff
#if 901 > 0xffff
  movk ip0, 901 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_mount_mountable
  .p2align 4
  .type g_file_mount_mountable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_mount_mountable
#endif
g_file_mount_mountable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 902 & 0xffff
#if 902 > 0xffff
  movk ip0, 902 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_mount_mountable_finish
  .p2align 4
  .type g_file_mount_mountable_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_mount_mountable_finish
#endif
g_file_mount_mountable_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 903 & 0xffff
#if 903 > 0xffff
  movk ip0, 903 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_move
  .p2align 4
  .type g_file_move, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_move
#endif
g_file_move:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 904 & 0xffff
#if 904 > 0xffff
  movk ip0, 904 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_move_async
  .p2align 4
  .type g_file_move_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_move_async
#endif
g_file_move_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 905 & 0xffff
#if 905 > 0xffff
  movk ip0, 905 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_move_async_with_closures
  .p2align 4
  .type g_file_move_async_with_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_move_async_with_closures
#endif
g_file_move_async_with_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 906 & 0xffff
#if 906 > 0xffff
  movk ip0, 906 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_move_finish
  .p2align 4
  .type g_file_move_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_move_finish
#endif
g_file_move_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 907 & 0xffff
#if 907 > 0xffff
  movk ip0, 907 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_build_filename
  .p2align 4
  .type g_file_new_build_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_build_filename
#endif
g_file_new_build_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 908 & 0xffff
#if 908 > 0xffff
  movk ip0, 908 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_build_filenamev
  .p2align 4
  .type g_file_new_build_filenamev, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_build_filenamev
#endif
g_file_new_build_filenamev:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 909 & 0xffff
#if 909 > 0xffff
  movk ip0, 909 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_for_commandline_arg
  .p2align 4
  .type g_file_new_for_commandline_arg, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_for_commandline_arg
#endif
g_file_new_for_commandline_arg:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 910 & 0xffff
#if 910 > 0xffff
  movk ip0, 910 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_for_commandline_arg_and_cwd
  .p2align 4
  .type g_file_new_for_commandline_arg_and_cwd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_for_commandline_arg_and_cwd
#endif
g_file_new_for_commandline_arg_and_cwd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 911 & 0xffff
#if 911 > 0xffff
  movk ip0, 911 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_for_path
  .p2align 4
  .type g_file_new_for_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_for_path
#endif
g_file_new_for_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 912 & 0xffff
#if 912 > 0xffff
  movk ip0, 912 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_for_uri
  .p2align 4
  .type g_file_new_for_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_for_uri
#endif
g_file_new_for_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 913 & 0xffff
#if 913 > 0xffff
  movk ip0, 913 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_tmp
  .p2align 4
  .type g_file_new_tmp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_tmp
#endif
g_file_new_tmp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 914 & 0xffff
#if 914 > 0xffff
  movk ip0, 914 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_tmp_async
  .p2align 4
  .type g_file_new_tmp_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_tmp_async
#endif
g_file_new_tmp_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 915 & 0xffff
#if 915 > 0xffff
  movk ip0, 915 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_tmp_dir_async
  .p2align 4
  .type g_file_new_tmp_dir_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_tmp_dir_async
#endif
g_file_new_tmp_dir_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 916 & 0xffff
#if 916 > 0xffff
  movk ip0, 916 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_tmp_dir_finish
  .p2align 4
  .type g_file_new_tmp_dir_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_tmp_dir_finish
#endif
g_file_new_tmp_dir_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 917 & 0xffff
#if 917 > 0xffff
  movk ip0, 917 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_new_tmp_finish
  .p2align 4
  .type g_file_new_tmp_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_new_tmp_finish
#endif
g_file_new_tmp_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 918 & 0xffff
#if 918 > 0xffff
  movk ip0, 918 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_open_readwrite
  .p2align 4
  .type g_file_open_readwrite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_open_readwrite
#endif
g_file_open_readwrite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 919 & 0xffff
#if 919 > 0xffff
  movk ip0, 919 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_open_readwrite_async
  .p2align 4
  .type g_file_open_readwrite_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_open_readwrite_async
#endif
g_file_open_readwrite_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 920 & 0xffff
#if 920 > 0xffff
  movk ip0, 920 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_open_readwrite_finish
  .p2align 4
  .type g_file_open_readwrite_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_open_readwrite_finish
#endif
g_file_open_readwrite_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 921 & 0xffff
#if 921 > 0xffff
  movk ip0, 921 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_output_stream_get_etag
  .p2align 4
  .type g_file_output_stream_get_etag, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_output_stream_get_etag
#endif
g_file_output_stream_get_etag:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 922 & 0xffff
#if 922 > 0xffff
  movk ip0, 922 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_output_stream_get_type
  .p2align 4
  .type g_file_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_output_stream_get_type
#endif
g_file_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 923 & 0xffff
#if 923 > 0xffff
  movk ip0, 923 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_output_stream_query_info
  .p2align 4
  .type g_file_output_stream_query_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_output_stream_query_info
#endif
g_file_output_stream_query_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 924 & 0xffff
#if 924 > 0xffff
  movk ip0, 924 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_output_stream_query_info_async
  .p2align 4
  .type g_file_output_stream_query_info_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_output_stream_query_info_async
#endif
g_file_output_stream_query_info_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 925 & 0xffff
#if 925 > 0xffff
  movk ip0, 925 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_output_stream_query_info_finish
  .p2align 4
  .type g_file_output_stream_query_info_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_output_stream_query_info_finish
#endif
g_file_output_stream_query_info_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 926 & 0xffff
#if 926 > 0xffff
  movk ip0, 926 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_parse_name
  .p2align 4
  .type g_file_parse_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_parse_name
#endif
g_file_parse_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 927 & 0xffff
#if 927 > 0xffff
  movk ip0, 927 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_peek_path
  .p2align 4
  .type g_file_peek_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_peek_path
#endif
g_file_peek_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 928 & 0xffff
#if 928 > 0xffff
  movk ip0, 928 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_poll_mountable
  .p2align 4
  .type g_file_poll_mountable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_poll_mountable
#endif
g_file_poll_mountable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 929 & 0xffff
#if 929 > 0xffff
  movk ip0, 929 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_poll_mountable_finish
  .p2align 4
  .type g_file_poll_mountable_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_poll_mountable_finish
#endif
g_file_poll_mountable_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 930 & 0xffff
#if 930 > 0xffff
  movk ip0, 930 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_default_handler
  .p2align 4
  .type g_file_query_default_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_default_handler
#endif
g_file_query_default_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 931 & 0xffff
#if 931 > 0xffff
  movk ip0, 931 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_default_handler_async
  .p2align 4
  .type g_file_query_default_handler_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_default_handler_async
#endif
g_file_query_default_handler_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 932 & 0xffff
#if 932 > 0xffff
  movk ip0, 932 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_default_handler_finish
  .p2align 4
  .type g_file_query_default_handler_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_default_handler_finish
#endif
g_file_query_default_handler_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 933 & 0xffff
#if 933 > 0xffff
  movk ip0, 933 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_exists
  .p2align 4
  .type g_file_query_exists, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_exists
#endif
g_file_query_exists:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 934 & 0xffff
#if 934 > 0xffff
  movk ip0, 934 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_file_type
  .p2align 4
  .type g_file_query_file_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_file_type
#endif
g_file_query_file_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 935 & 0xffff
#if 935 > 0xffff
  movk ip0, 935 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_filesystem_info
  .p2align 4
  .type g_file_query_filesystem_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_filesystem_info
#endif
g_file_query_filesystem_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 936 & 0xffff
#if 936 > 0xffff
  movk ip0, 936 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_filesystem_info_async
  .p2align 4
  .type g_file_query_filesystem_info_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_filesystem_info_async
#endif
g_file_query_filesystem_info_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 937 & 0xffff
#if 937 > 0xffff
  movk ip0, 937 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_filesystem_info_finish
  .p2align 4
  .type g_file_query_filesystem_info_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_filesystem_info_finish
#endif
g_file_query_filesystem_info_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 938 & 0xffff
#if 938 > 0xffff
  movk ip0, 938 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_info
  .p2align 4
  .type g_file_query_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_info
#endif
g_file_query_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 939 & 0xffff
#if 939 > 0xffff
  movk ip0, 939 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_info_async
  .p2align 4
  .type g_file_query_info_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_info_async
#endif
g_file_query_info_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 940 & 0xffff
#if 940 > 0xffff
  movk ip0, 940 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_info_finish
  .p2align 4
  .type g_file_query_info_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_info_finish
#endif
g_file_query_info_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 941 & 0xffff
#if 941 > 0xffff
  movk ip0, 941 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_info_flags_get_type
  .p2align 4
  .type g_file_query_info_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_info_flags_get_type
#endif
g_file_query_info_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 942 & 0xffff
#if 942 > 0xffff
  movk ip0, 942 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_settable_attributes
  .p2align 4
  .type g_file_query_settable_attributes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_settable_attributes
#endif
g_file_query_settable_attributes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 943 & 0xffff
#if 943 > 0xffff
  movk ip0, 943 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_query_writable_namespaces
  .p2align 4
  .type g_file_query_writable_namespaces, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_query_writable_namespaces
#endif
g_file_query_writable_namespaces:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 944 & 0xffff
#if 944 > 0xffff
  movk ip0, 944 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_read
  .p2align 4
  .type g_file_read, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_read
#endif
g_file_read:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 945 & 0xffff
#if 945 > 0xffff
  movk ip0, 945 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_read_async
  .p2align 4
  .type g_file_read_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_read_async
#endif
g_file_read_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 946 & 0xffff
#if 946 > 0xffff
  movk ip0, 946 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_read_finish
  .p2align 4
  .type g_file_read_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_read_finish
#endif
g_file_read_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 947 & 0xffff
#if 947 > 0xffff
  movk ip0, 947 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace
  .p2align 4
  .type g_file_replace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace
#endif
g_file_replace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 948 & 0xffff
#if 948 > 0xffff
  movk ip0, 948 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_async
  .p2align 4
  .type g_file_replace_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_async
#endif
g_file_replace_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 949 & 0xffff
#if 949 > 0xffff
  movk ip0, 949 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_contents
  .p2align 4
  .type g_file_replace_contents, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_contents
#endif
g_file_replace_contents:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 950 & 0xffff
#if 950 > 0xffff
  movk ip0, 950 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_contents_async
  .p2align 4
  .type g_file_replace_contents_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_contents_async
#endif
g_file_replace_contents_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 951 & 0xffff
#if 951 > 0xffff
  movk ip0, 951 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_contents_bytes_async
  .p2align 4
  .type g_file_replace_contents_bytes_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_contents_bytes_async
#endif
g_file_replace_contents_bytes_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 952 & 0xffff
#if 952 > 0xffff
  movk ip0, 952 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_contents_finish
  .p2align 4
  .type g_file_replace_contents_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_contents_finish
#endif
g_file_replace_contents_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 953 & 0xffff
#if 953 > 0xffff
  movk ip0, 953 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_finish
  .p2align 4
  .type g_file_replace_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_finish
#endif
g_file_replace_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 954 & 0xffff
#if 954 > 0xffff
  movk ip0, 954 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_readwrite
  .p2align 4
  .type g_file_replace_readwrite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_readwrite
#endif
g_file_replace_readwrite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 955 & 0xffff
#if 955 > 0xffff
  movk ip0, 955 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_readwrite_async
  .p2align 4
  .type g_file_replace_readwrite_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_readwrite_async
#endif
g_file_replace_readwrite_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 956 & 0xffff
#if 956 > 0xffff
  movk ip0, 956 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_replace_readwrite_finish
  .p2align 4
  .type g_file_replace_readwrite_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_replace_readwrite_finish
#endif
g_file_replace_readwrite_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 957 & 0xffff
#if 957 > 0xffff
  movk ip0, 957 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_resolve_relative_path
  .p2align 4
  .type g_file_resolve_relative_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_resolve_relative_path
#endif
g_file_resolve_relative_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 958 & 0xffff
#if 958 > 0xffff
  movk ip0, 958 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attribute
  .p2align 4
  .type g_file_set_attribute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attribute
#endif
g_file_set_attribute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 959 & 0xffff
#if 959 > 0xffff
  movk ip0, 959 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attribute_byte_string
  .p2align 4
  .type g_file_set_attribute_byte_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attribute_byte_string
#endif
g_file_set_attribute_byte_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 960 & 0xffff
#if 960 > 0xffff
  movk ip0, 960 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attribute_int32
  .p2align 4
  .type g_file_set_attribute_int32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attribute_int32
#endif
g_file_set_attribute_int32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 961 & 0xffff
#if 961 > 0xffff
  movk ip0, 961 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attribute_int64
  .p2align 4
  .type g_file_set_attribute_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attribute_int64
#endif
g_file_set_attribute_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 962 & 0xffff
#if 962 > 0xffff
  movk ip0, 962 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attribute_string
  .p2align 4
  .type g_file_set_attribute_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attribute_string
#endif
g_file_set_attribute_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 963 & 0xffff
#if 963 > 0xffff
  movk ip0, 963 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attribute_uint32
  .p2align 4
  .type g_file_set_attribute_uint32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attribute_uint32
#endif
g_file_set_attribute_uint32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 964 & 0xffff
#if 964 > 0xffff
  movk ip0, 964 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attribute_uint64
  .p2align 4
  .type g_file_set_attribute_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attribute_uint64
#endif
g_file_set_attribute_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 965 & 0xffff
#if 965 > 0xffff
  movk ip0, 965 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attributes_async
  .p2align 4
  .type g_file_set_attributes_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attributes_async
#endif
g_file_set_attributes_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 966 & 0xffff
#if 966 > 0xffff
  movk ip0, 966 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attributes_finish
  .p2align 4
  .type g_file_set_attributes_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attributes_finish
#endif
g_file_set_attributes_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 967 & 0xffff
#if 967 > 0xffff
  movk ip0, 967 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_attributes_from_info
  .p2align 4
  .type g_file_set_attributes_from_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_attributes_from_info
#endif
g_file_set_attributes_from_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 968 & 0xffff
#if 968 > 0xffff
  movk ip0, 968 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_display_name
  .p2align 4
  .type g_file_set_display_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_display_name
#endif
g_file_set_display_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 969 & 0xffff
#if 969 > 0xffff
  movk ip0, 969 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_display_name_async
  .p2align 4
  .type g_file_set_display_name_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_display_name_async
#endif
g_file_set_display_name_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 970 & 0xffff
#if 970 > 0xffff
  movk ip0, 970 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_display_name_finish
  .p2align 4
  .type g_file_set_display_name_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_display_name_finish
#endif
g_file_set_display_name_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 971 & 0xffff
#if 971 > 0xffff
  movk ip0, 971 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_start_mountable
  .p2align 4
  .type g_file_start_mountable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_start_mountable
#endif
g_file_start_mountable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 972 & 0xffff
#if 972 > 0xffff
  movk ip0, 972 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_start_mountable_finish
  .p2align 4
  .type g_file_start_mountable_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_start_mountable_finish
#endif
g_file_start_mountable_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 973 & 0xffff
#if 973 > 0xffff
  movk ip0, 973 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_stop_mountable
  .p2align 4
  .type g_file_stop_mountable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_stop_mountable
#endif
g_file_stop_mountable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 974 & 0xffff
#if 974 > 0xffff
  movk ip0, 974 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_stop_mountable_finish
  .p2align 4
  .type g_file_stop_mountable_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_stop_mountable_finish
#endif
g_file_stop_mountable_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 975 & 0xffff
#if 975 > 0xffff
  movk ip0, 975 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_supports_thread_contexts
  .p2align 4
  .type g_file_supports_thread_contexts, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_supports_thread_contexts
#endif
g_file_supports_thread_contexts:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 976 & 0xffff
#if 976 > 0xffff
  movk ip0, 976 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_trash
  .p2align 4
  .type g_file_trash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_trash
#endif
g_file_trash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 977 & 0xffff
#if 977 > 0xffff
  movk ip0, 977 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_trash_async
  .p2align 4
  .type g_file_trash_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_trash_async
#endif
g_file_trash_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 978 & 0xffff
#if 978 > 0xffff
  movk ip0, 978 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_trash_finish
  .p2align 4
  .type g_file_trash_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_trash_finish
#endif
g_file_trash_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 979 & 0xffff
#if 979 > 0xffff
  movk ip0, 979 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_type_get_type
  .p2align 4
  .type g_file_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_type_get_type
#endif
g_file_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 980 & 0xffff
#if 980 > 0xffff
  movk ip0, 980 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_unmount_mountable
  .p2align 4
  .type g_file_unmount_mountable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_unmount_mountable
#endif
g_file_unmount_mountable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 981 & 0xffff
#if 981 > 0xffff
  movk ip0, 981 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_unmount_mountable_finish
  .p2align 4
  .type g_file_unmount_mountable_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_unmount_mountable_finish
#endif
g_file_unmount_mountable_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 982 & 0xffff
#if 982 > 0xffff
  movk ip0, 982 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_unmount_mountable_with_operation
  .p2align 4
  .type g_file_unmount_mountable_with_operation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_unmount_mountable_with_operation
#endif
g_file_unmount_mountable_with_operation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 983 & 0xffff
#if 983 > 0xffff
  movk ip0, 983 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_unmount_mountable_with_operation_finish
  .p2align 4
  .type g_file_unmount_mountable_with_operation_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_unmount_mountable_with_operation_finish
#endif
g_file_unmount_mountable_with_operation_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 984 & 0xffff
#if 984 > 0xffff
  movk ip0, 984 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_completer_get_completion_suffix
  .p2align 4
  .type g_filename_completer_get_completion_suffix, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_completer_get_completion_suffix
#endif
g_filename_completer_get_completion_suffix:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 985 & 0xffff
#if 985 > 0xffff
  movk ip0, 985 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_completer_get_completions
  .p2align 4
  .type g_filename_completer_get_completions, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_completer_get_completions
#endif
g_filename_completer_get_completions:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 986 & 0xffff
#if 986 > 0xffff
  movk ip0, 986 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_completer_get_type
  .p2align 4
  .type g_filename_completer_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_completer_get_type
#endif
g_filename_completer_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 987 & 0xffff
#if 987 > 0xffff
  movk ip0, 987 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_completer_new
  .p2align 4
  .type g_filename_completer_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_completer_new
#endif
g_filename_completer_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 988 & 0xffff
#if 988 > 0xffff
  movk ip0, 988 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_completer_set_dirs_only
  .p2align 4
  .type g_filename_completer_set_dirs_only, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_completer_set_dirs_only
#endif
g_filename_completer_set_dirs_only:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 989 & 0xffff
#if 989 > 0xffff
  movk ip0, 989 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filesystem_preview_type_get_type
  .p2align 4
  .type g_filesystem_preview_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filesystem_preview_type_get_type
#endif
g_filesystem_preview_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 990 & 0xffff
#if 990 > 0xffff
  movk ip0, 990 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_input_stream_get_base_stream
  .p2align 4
  .type g_filter_input_stream_get_base_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_input_stream_get_base_stream
#endif
g_filter_input_stream_get_base_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 991 & 0xffff
#if 991 > 0xffff
  movk ip0, 991 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_input_stream_get_close_base_stream
  .p2align 4
  .type g_filter_input_stream_get_close_base_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_input_stream_get_close_base_stream
#endif
g_filter_input_stream_get_close_base_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 992 & 0xffff
#if 992 > 0xffff
  movk ip0, 992 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_input_stream_get_type
  .p2align 4
  .type g_filter_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_input_stream_get_type
#endif
g_filter_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 993 & 0xffff
#if 993 > 0xffff
  movk ip0, 993 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_input_stream_set_close_base_stream
  .p2align 4
  .type g_filter_input_stream_set_close_base_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_input_stream_set_close_base_stream
#endif
g_filter_input_stream_set_close_base_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 994 & 0xffff
#if 994 > 0xffff
  movk ip0, 994 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_output_stream_get_base_stream
  .p2align 4
  .type g_filter_output_stream_get_base_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_output_stream_get_base_stream
#endif
g_filter_output_stream_get_base_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 995 & 0xffff
#if 995 > 0xffff
  movk ip0, 995 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_output_stream_get_close_base_stream
  .p2align 4
  .type g_filter_output_stream_get_close_base_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_output_stream_get_close_base_stream
#endif
g_filter_output_stream_get_close_base_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 996 & 0xffff
#if 996 > 0xffff
  movk ip0, 996 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_output_stream_get_type
  .p2align 4
  .type g_filter_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_output_stream_get_type
#endif
g_filter_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 997 & 0xffff
#if 997 > 0xffff
  movk ip0, 997 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filter_output_stream_set_close_base_stream
  .p2align 4
  .type g_filter_output_stream_set_close_base_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filter_output_stream_set_close_base_stream
#endif
g_filter_output_stream_set_close_base_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 998 & 0xffff
#if 998 > 0xffff
  movk ip0, 998 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_icon_deserialize
  .p2align 4
  .type g_icon_deserialize, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_icon_deserialize
#endif
g_icon_deserialize:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+7992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+7992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 999 & 0xffff
#if 999 > 0xffff
  movk ip0, 999 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_icon_equal
  .p2align 4
  .type g_icon_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_icon_equal
#endif
g_icon_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1000 & 0xffff
#if 1000 > 0xffff
  movk ip0, 1000 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_icon_get_type
  .p2align 4
  .type g_icon_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_icon_get_type
#endif
g_icon_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1001 & 0xffff
#if 1001 > 0xffff
  movk ip0, 1001 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_icon_hash
  .p2align 4
  .type g_icon_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_icon_hash
#endif
g_icon_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1002 & 0xffff
#if 1002 > 0xffff
  movk ip0, 1002 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_icon_new_for_string
  .p2align 4
  .type g_icon_new_for_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_icon_new_for_string
#endif
g_icon_new_for_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1003 & 0xffff
#if 1003 > 0xffff
  movk ip0, 1003 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_icon_serialize
  .p2align 4
  .type g_icon_serialize, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_icon_serialize
#endif
g_icon_serialize:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1004 & 0xffff
#if 1004 > 0xffff
  movk ip0, 1004 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_icon_to_string
  .p2align 4
  .type g_icon_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_icon_to_string
#endif
g_icon_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1005 & 0xffff
#if 1005 > 0xffff
  movk ip0, 1005 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_equal
  .p2align 4
  .type g_inet_address_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_equal
#endif
g_inet_address_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1006 & 0xffff
#if 1006 > 0xffff
  movk ip0, 1006 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_family
  .p2align 4
  .type g_inet_address_get_family, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_family
#endif
g_inet_address_get_family:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1007 & 0xffff
#if 1007 > 0xffff
  movk ip0, 1007 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_any
  .p2align 4
  .type g_inet_address_get_is_any, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_any
#endif
g_inet_address_get_is_any:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1008 & 0xffff
#if 1008 > 0xffff
  movk ip0, 1008 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_link_local
  .p2align 4
  .type g_inet_address_get_is_link_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_link_local
#endif
g_inet_address_get_is_link_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1009 & 0xffff
#if 1009 > 0xffff
  movk ip0, 1009 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_loopback
  .p2align 4
  .type g_inet_address_get_is_loopback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_loopback
#endif
g_inet_address_get_is_loopback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1010 & 0xffff
#if 1010 > 0xffff
  movk ip0, 1010 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_mc_global
  .p2align 4
  .type g_inet_address_get_is_mc_global, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_mc_global
#endif
g_inet_address_get_is_mc_global:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1011 & 0xffff
#if 1011 > 0xffff
  movk ip0, 1011 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_mc_link_local
  .p2align 4
  .type g_inet_address_get_is_mc_link_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_mc_link_local
#endif
g_inet_address_get_is_mc_link_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1012 & 0xffff
#if 1012 > 0xffff
  movk ip0, 1012 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_mc_node_local
  .p2align 4
  .type g_inet_address_get_is_mc_node_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_mc_node_local
#endif
g_inet_address_get_is_mc_node_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1013 & 0xffff
#if 1013 > 0xffff
  movk ip0, 1013 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_mc_org_local
  .p2align 4
  .type g_inet_address_get_is_mc_org_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_mc_org_local
#endif
g_inet_address_get_is_mc_org_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1014 & 0xffff
#if 1014 > 0xffff
  movk ip0, 1014 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_mc_site_local
  .p2align 4
  .type g_inet_address_get_is_mc_site_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_mc_site_local
#endif
g_inet_address_get_is_mc_site_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1015 & 0xffff
#if 1015 > 0xffff
  movk ip0, 1015 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_multicast
  .p2align 4
  .type g_inet_address_get_is_multicast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_multicast
#endif
g_inet_address_get_is_multicast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1016 & 0xffff
#if 1016 > 0xffff
  movk ip0, 1016 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_is_site_local
  .p2align 4
  .type g_inet_address_get_is_site_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_is_site_local
#endif
g_inet_address_get_is_site_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1017 & 0xffff
#if 1017 > 0xffff
  movk ip0, 1017 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_native_size
  .p2align 4
  .type g_inet_address_get_native_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_native_size
#endif
g_inet_address_get_native_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1018 & 0xffff
#if 1018 > 0xffff
  movk ip0, 1018 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_get_type
  .p2align 4
  .type g_inet_address_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_get_type
#endif
g_inet_address_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1019 & 0xffff
#if 1019 > 0xffff
  movk ip0, 1019 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_equal
  .p2align 4
  .type g_inet_address_mask_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_equal
#endif
g_inet_address_mask_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1020 & 0xffff
#if 1020 > 0xffff
  movk ip0, 1020 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_get_address
  .p2align 4
  .type g_inet_address_mask_get_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_get_address
#endif
g_inet_address_mask_get_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1021 & 0xffff
#if 1021 > 0xffff
  movk ip0, 1021 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_get_family
  .p2align 4
  .type g_inet_address_mask_get_family, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_get_family
#endif
g_inet_address_mask_get_family:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1022 & 0xffff
#if 1022 > 0xffff
  movk ip0, 1022 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_get_length
  .p2align 4
  .type g_inet_address_mask_get_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_get_length
#endif
g_inet_address_mask_get_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1023 & 0xffff
#if 1023 > 0xffff
  movk ip0, 1023 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_get_type
  .p2align 4
  .type g_inet_address_mask_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_get_type
#endif
g_inet_address_mask_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1024 & 0xffff
#if 1024 > 0xffff
  movk ip0, 1024 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_matches
  .p2align 4
  .type g_inet_address_mask_matches, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_matches
#endif
g_inet_address_mask_matches:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1025 & 0xffff
#if 1025 > 0xffff
  movk ip0, 1025 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_new
  .p2align 4
  .type g_inet_address_mask_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_new
#endif
g_inet_address_mask_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1026 & 0xffff
#if 1026 > 0xffff
  movk ip0, 1026 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_new_from_string
  .p2align 4
  .type g_inet_address_mask_new_from_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_new_from_string
#endif
g_inet_address_mask_new_from_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1027 & 0xffff
#if 1027 > 0xffff
  movk ip0, 1027 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_mask_to_string
  .p2align 4
  .type g_inet_address_mask_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_mask_to_string
#endif
g_inet_address_mask_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1028 & 0xffff
#if 1028 > 0xffff
  movk ip0, 1028 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_new_any
  .p2align 4
  .type g_inet_address_new_any, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_new_any
#endif
g_inet_address_new_any:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1029 & 0xffff
#if 1029 > 0xffff
  movk ip0, 1029 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_new_from_bytes
  .p2align 4
  .type g_inet_address_new_from_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_new_from_bytes
#endif
g_inet_address_new_from_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1030 & 0xffff
#if 1030 > 0xffff
  movk ip0, 1030 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_new_from_string
  .p2align 4
  .type g_inet_address_new_from_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_new_from_string
#endif
g_inet_address_new_from_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1031 & 0xffff
#if 1031 > 0xffff
  movk ip0, 1031 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_new_loopback
  .p2align 4
  .type g_inet_address_new_loopback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_new_loopback
#endif
g_inet_address_new_loopback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1032 & 0xffff
#if 1032 > 0xffff
  movk ip0, 1032 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_to_bytes
  .p2align 4
  .type g_inet_address_to_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_to_bytes
#endif
g_inet_address_to_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1033 & 0xffff
#if 1033 > 0xffff
  movk ip0, 1033 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_address_to_string
  .p2align 4
  .type g_inet_address_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_address_to_string
#endif
g_inet_address_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1034 & 0xffff
#if 1034 > 0xffff
  movk ip0, 1034 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_socket_address_get_address
  .p2align 4
  .type g_inet_socket_address_get_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_socket_address_get_address
#endif
g_inet_socket_address_get_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1035 & 0xffff
#if 1035 > 0xffff
  movk ip0, 1035 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_socket_address_get_flowinfo
  .p2align 4
  .type g_inet_socket_address_get_flowinfo, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_socket_address_get_flowinfo
#endif
g_inet_socket_address_get_flowinfo:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1036 & 0xffff
#if 1036 > 0xffff
  movk ip0, 1036 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_socket_address_get_port
  .p2align 4
  .type g_inet_socket_address_get_port, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_socket_address_get_port
#endif
g_inet_socket_address_get_port:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1037 & 0xffff
#if 1037 > 0xffff
  movk ip0, 1037 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_socket_address_get_scope_id
  .p2align 4
  .type g_inet_socket_address_get_scope_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_socket_address_get_scope_id
#endif
g_inet_socket_address_get_scope_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1038 & 0xffff
#if 1038 > 0xffff
  movk ip0, 1038 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_socket_address_get_type
  .p2align 4
  .type g_inet_socket_address_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_socket_address_get_type
#endif
g_inet_socket_address_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1039 & 0xffff
#if 1039 > 0xffff
  movk ip0, 1039 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_socket_address_new
  .p2align 4
  .type g_inet_socket_address_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_socket_address_new
#endif
g_inet_socket_address_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1040 & 0xffff
#if 1040 > 0xffff
  movk ip0, 1040 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_inet_socket_address_new_from_string
  .p2align 4
  .type g_inet_socket_address_new_from_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_inet_socket_address_new_from_string
#endif
g_inet_socket_address_new_from_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1041 & 0xffff
#if 1041 > 0xffff
  movk ip0, 1041 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_initable_get_type
  .p2align 4
  .type g_initable_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_initable_get_type
#endif
g_initable_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1042 & 0xffff
#if 1042 > 0xffff
  movk ip0, 1042 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_initable_init
  .p2align 4
  .type g_initable_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_initable_init
#endif
g_initable_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1043 & 0xffff
#if 1043 > 0xffff
  movk ip0, 1043 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_initable_new
  .p2align 4
  .type g_initable_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_initable_new
#endif
g_initable_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1044 & 0xffff
#if 1044 > 0xffff
  movk ip0, 1044 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_initable_new_valist
  .p2align 4
  .type g_initable_new_valist, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_initable_new_valist
#endif
g_initable_new_valist:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1045 & 0xffff
#if 1045 > 0xffff
  movk ip0, 1045 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_initable_newv
  .p2align 4
  .type g_initable_newv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_initable_newv
#endif
g_initable_newv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1046 & 0xffff
#if 1046 > 0xffff
  movk ip0, 1046 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_clear_pending
  .p2align 4
  .type g_input_stream_clear_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_clear_pending
#endif
g_input_stream_clear_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1047 & 0xffff
#if 1047 > 0xffff
  movk ip0, 1047 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_close
  .p2align 4
  .type g_input_stream_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_close
#endif
g_input_stream_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1048 & 0xffff
#if 1048 > 0xffff
  movk ip0, 1048 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_close_async
  .p2align 4
  .type g_input_stream_close_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_close_async
#endif
g_input_stream_close_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1049 & 0xffff
#if 1049 > 0xffff
  movk ip0, 1049 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_close_finish
  .p2align 4
  .type g_input_stream_close_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_close_finish
#endif
g_input_stream_close_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1050 & 0xffff
#if 1050 > 0xffff
  movk ip0, 1050 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_get_type
  .p2align 4
  .type g_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_get_type
#endif
g_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1051 & 0xffff
#if 1051 > 0xffff
  movk ip0, 1051 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_has_pending
  .p2align 4
  .type g_input_stream_has_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_has_pending
#endif
g_input_stream_has_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1052 & 0xffff
#if 1052 > 0xffff
  movk ip0, 1052 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_is_closed
  .p2align 4
  .type g_input_stream_is_closed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_is_closed
#endif
g_input_stream_is_closed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1053 & 0xffff
#if 1053 > 0xffff
  movk ip0, 1053 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read
  .p2align 4
  .type g_input_stream_read, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read
#endif
g_input_stream_read:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1054 & 0xffff
#if 1054 > 0xffff
  movk ip0, 1054 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_all
  .p2align 4
  .type g_input_stream_read_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_all
#endif
g_input_stream_read_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1055 & 0xffff
#if 1055 > 0xffff
  movk ip0, 1055 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_all_async
  .p2align 4
  .type g_input_stream_read_all_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_all_async
#endif
g_input_stream_read_all_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1056 & 0xffff
#if 1056 > 0xffff
  movk ip0, 1056 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_all_finish
  .p2align 4
  .type g_input_stream_read_all_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_all_finish
#endif
g_input_stream_read_all_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1057 & 0xffff
#if 1057 > 0xffff
  movk ip0, 1057 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_async
  .p2align 4
  .type g_input_stream_read_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_async
#endif
g_input_stream_read_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1058 & 0xffff
#if 1058 > 0xffff
  movk ip0, 1058 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_bytes
  .p2align 4
  .type g_input_stream_read_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_bytes
#endif
g_input_stream_read_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1059 & 0xffff
#if 1059 > 0xffff
  movk ip0, 1059 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_bytes_async
  .p2align 4
  .type g_input_stream_read_bytes_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_bytes_async
#endif
g_input_stream_read_bytes_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1060 & 0xffff
#if 1060 > 0xffff
  movk ip0, 1060 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_bytes_finish
  .p2align 4
  .type g_input_stream_read_bytes_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_bytes_finish
#endif
g_input_stream_read_bytes_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1061 & 0xffff
#if 1061 > 0xffff
  movk ip0, 1061 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_read_finish
  .p2align 4
  .type g_input_stream_read_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_read_finish
#endif
g_input_stream_read_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1062 & 0xffff
#if 1062 > 0xffff
  movk ip0, 1062 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_set_pending
  .p2align 4
  .type g_input_stream_set_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_set_pending
#endif
g_input_stream_set_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1063 & 0xffff
#if 1063 > 0xffff
  movk ip0, 1063 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_skip
  .p2align 4
  .type g_input_stream_skip, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_skip
#endif
g_input_stream_skip:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1064 & 0xffff
#if 1064 > 0xffff
  movk ip0, 1064 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_skip_async
  .p2align 4
  .type g_input_stream_skip_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_skip_async
#endif
g_input_stream_skip_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1065 & 0xffff
#if 1065 > 0xffff
  movk ip0, 1065 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_input_stream_skip_finish
  .p2align 4
  .type g_input_stream_skip_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_input_stream_skip_finish
#endif
g_input_stream_skip_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1066 & 0xffff
#if 1066 > 0xffff
  movk ip0, 1066 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_error_enum_get_type
  .p2align 4
  .type g_io_error_enum_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_error_enum_get_type
#endif
g_io_error_enum_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1067 & 0xffff
#if 1067 > 0xffff
  movk ip0, 1067 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_error_from_errno
  .p2align 4
  .type g_io_error_from_errno, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_error_from_errno
#endif
g_io_error_from_errno:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1068 & 0xffff
#if 1068 > 0xffff
  movk ip0, 1068 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_error_from_file_error
  .p2align 4
  .type g_io_error_from_file_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_error_from_file_error
#endif
g_io_error_from_file_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1069 & 0xffff
#if 1069 > 0xffff
  movk ip0, 1069 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_error_quark
  .p2align 4
  .type g_io_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_error_quark
#endif
g_io_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1070 & 0xffff
#if 1070 > 0xffff
  movk ip0, 1070 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_get_name
  .p2align 4
  .type g_io_extension_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_get_name
#endif
g_io_extension_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1071 & 0xffff
#if 1071 > 0xffff
  movk ip0, 1071 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_get_priority
  .p2align 4
  .type g_io_extension_get_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_get_priority
#endif
g_io_extension_get_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1072 & 0xffff
#if 1072 > 0xffff
  movk ip0, 1072 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_get_type
  .p2align 4
  .type g_io_extension_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_get_type
#endif
g_io_extension_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1073 & 0xffff
#if 1073 > 0xffff
  movk ip0, 1073 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_point_get_extension_by_name
  .p2align 4
  .type g_io_extension_point_get_extension_by_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_point_get_extension_by_name
#endif
g_io_extension_point_get_extension_by_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1074 & 0xffff
#if 1074 > 0xffff
  movk ip0, 1074 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_point_get_extensions
  .p2align 4
  .type g_io_extension_point_get_extensions, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_point_get_extensions
#endif
g_io_extension_point_get_extensions:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1075 & 0xffff
#if 1075 > 0xffff
  movk ip0, 1075 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_point_get_required_type
  .p2align 4
  .type g_io_extension_point_get_required_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_point_get_required_type
#endif
g_io_extension_point_get_required_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1076 & 0xffff
#if 1076 > 0xffff
  movk ip0, 1076 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_point_implement
  .p2align 4
  .type g_io_extension_point_implement, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_point_implement
#endif
g_io_extension_point_implement:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1077 & 0xffff
#if 1077 > 0xffff
  movk ip0, 1077 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_point_lookup
  .p2align 4
  .type g_io_extension_point_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_point_lookup
#endif
g_io_extension_point_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1078 & 0xffff
#if 1078 > 0xffff
  movk ip0, 1078 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_point_register
  .p2align 4
  .type g_io_extension_point_register, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_point_register
#endif
g_io_extension_point_register:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1079 & 0xffff
#if 1079 > 0xffff
  movk ip0, 1079 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_point_set_required_type
  .p2align 4
  .type g_io_extension_point_set_required_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_point_set_required_type
#endif
g_io_extension_point_set_required_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1080 & 0xffff
#if 1080 > 0xffff
  movk ip0, 1080 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_extension_ref_class
  .p2align 4
  .type g_io_extension_ref_class, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_extension_ref_class
#endif
g_io_extension_ref_class:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1081 & 0xffff
#if 1081 > 0xffff
  movk ip0, 1081 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_module_get_type
  .p2align 4
  .type g_io_module_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_module_get_type
#endif
g_io_module_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1082 & 0xffff
#if 1082 > 0xffff
  movk ip0, 1082 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_module_new
  .p2align 4
  .type g_io_module_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_module_new
#endif
g_io_module_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1083 & 0xffff
#if 1083 > 0xffff
  movk ip0, 1083 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_module_scope_block
  .p2align 4
  .type g_io_module_scope_block, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_module_scope_block
#endif
g_io_module_scope_block:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1084 & 0xffff
#if 1084 > 0xffff
  movk ip0, 1084 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_module_scope_flags_get_type
  .p2align 4
  .type g_io_module_scope_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_module_scope_flags_get_type
#endif
g_io_module_scope_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1085 & 0xffff
#if 1085 > 0xffff
  movk ip0, 1085 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_module_scope_free
  .p2align 4
  .type g_io_module_scope_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_module_scope_free
#endif
g_io_module_scope_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1086 & 0xffff
#if 1086 > 0xffff
  movk ip0, 1086 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_module_scope_new
  .p2align 4
  .type g_io_module_scope_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_module_scope_new
#endif
g_io_module_scope_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1087 & 0xffff
#if 1087 > 0xffff
  movk ip0, 1087 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_modules_load_all_in_directory
  .p2align 4
  .type g_io_modules_load_all_in_directory, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_modules_load_all_in_directory
#endif
g_io_modules_load_all_in_directory:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1088 & 0xffff
#if 1088 > 0xffff
  movk ip0, 1088 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_modules_load_all_in_directory_with_scope
  .p2align 4
  .type g_io_modules_load_all_in_directory_with_scope, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_modules_load_all_in_directory_with_scope
#endif
g_io_modules_load_all_in_directory_with_scope:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1089 & 0xffff
#if 1089 > 0xffff
  movk ip0, 1089 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_modules_scan_all_in_directory
  .p2align 4
  .type g_io_modules_scan_all_in_directory, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_modules_scan_all_in_directory
#endif
g_io_modules_scan_all_in_directory:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1090 & 0xffff
#if 1090 > 0xffff
  movk ip0, 1090 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_modules_scan_all_in_directory_with_scope
  .p2align 4
  .type g_io_modules_scan_all_in_directory_with_scope, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_modules_scan_all_in_directory_with_scope
#endif
g_io_modules_scan_all_in_directory_with_scope:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1091 & 0xffff
#if 1091 > 0xffff
  movk ip0, 1091 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_scheduler_cancel_all_jobs
  .p2align 4
  .type g_io_scheduler_cancel_all_jobs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_scheduler_cancel_all_jobs
#endif
g_io_scheduler_cancel_all_jobs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1092 & 0xffff
#if 1092 > 0xffff
  movk ip0, 1092 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_scheduler_job_send_to_mainloop
  .p2align 4
  .type g_io_scheduler_job_send_to_mainloop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_scheduler_job_send_to_mainloop
#endif
g_io_scheduler_job_send_to_mainloop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1093 & 0xffff
#if 1093 > 0xffff
  movk ip0, 1093 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_scheduler_job_send_to_mainloop_async
  .p2align 4
  .type g_io_scheduler_job_send_to_mainloop_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_scheduler_job_send_to_mainloop_async
#endif
g_io_scheduler_job_send_to_mainloop_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1094 & 0xffff
#if 1094 > 0xffff
  movk ip0, 1094 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_scheduler_push_job
  .p2align 4
  .type g_io_scheduler_push_job, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_scheduler_push_job
#endif
g_io_scheduler_push_job:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1095 & 0xffff
#if 1095 > 0xffff
  movk ip0, 1095 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_clear_pending
  .p2align 4
  .type g_io_stream_clear_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_clear_pending
#endif
g_io_stream_clear_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1096 & 0xffff
#if 1096 > 0xffff
  movk ip0, 1096 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_close
  .p2align 4
  .type g_io_stream_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_close
#endif
g_io_stream_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1097 & 0xffff
#if 1097 > 0xffff
  movk ip0, 1097 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_close_async
  .p2align 4
  .type g_io_stream_close_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_close_async
#endif
g_io_stream_close_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1098 & 0xffff
#if 1098 > 0xffff
  movk ip0, 1098 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_close_finish
  .p2align 4
  .type g_io_stream_close_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_close_finish
#endif
g_io_stream_close_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1099 & 0xffff
#if 1099 > 0xffff
  movk ip0, 1099 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_get_input_stream
  .p2align 4
  .type g_io_stream_get_input_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_get_input_stream
#endif
g_io_stream_get_input_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1100 & 0xffff
#if 1100 > 0xffff
  movk ip0, 1100 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_get_output_stream
  .p2align 4
  .type g_io_stream_get_output_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_get_output_stream
#endif
g_io_stream_get_output_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1101 & 0xffff
#if 1101 > 0xffff
  movk ip0, 1101 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_get_type
  .p2align 4
  .type g_io_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_get_type
#endif
g_io_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1102 & 0xffff
#if 1102 > 0xffff
  movk ip0, 1102 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_has_pending
  .p2align 4
  .type g_io_stream_has_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_has_pending
#endif
g_io_stream_has_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1103 & 0xffff
#if 1103 > 0xffff
  movk ip0, 1103 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_is_closed
  .p2align 4
  .type g_io_stream_is_closed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_is_closed
#endif
g_io_stream_is_closed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1104 & 0xffff
#if 1104 > 0xffff
  movk ip0, 1104 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_set_pending
  .p2align 4
  .type g_io_stream_set_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_set_pending
#endif
g_io_stream_set_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1105 & 0xffff
#if 1105 > 0xffff
  movk ip0, 1105 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_splice_async
  .p2align 4
  .type g_io_stream_splice_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_splice_async
#endif
g_io_stream_splice_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1106 & 0xffff
#if 1106 > 0xffff
  movk ip0, 1106 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_splice_finish
  .p2align 4
  .type g_io_stream_splice_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_splice_finish
#endif
g_io_stream_splice_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1107 & 0xffff
#if 1107 > 0xffff
  movk ip0, 1107 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_stream_splice_flags_get_type
  .p2align 4
  .type g_io_stream_splice_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_stream_splice_flags_get_type
#endif
g_io_stream_splice_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1108 & 0xffff
#if 1108 > 0xffff
  movk ip0, 1108 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_keyfile_settings_backend_new
  .p2align 4
  .type g_keyfile_settings_backend_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_keyfile_settings_backend_new
#endif
g_keyfile_settings_backend_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1109 & 0xffff
#if 1109 > 0xffff
  movk ip0, 1109 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_model_get_item
  .p2align 4
  .type g_list_model_get_item, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_model_get_item
#endif
g_list_model_get_item:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1110 & 0xffff
#if 1110 > 0xffff
  movk ip0, 1110 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_model_get_item_type
  .p2align 4
  .type g_list_model_get_item_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_model_get_item_type
#endif
g_list_model_get_item_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1111 & 0xffff
#if 1111 > 0xffff
  movk ip0, 1111 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_model_get_n_items
  .p2align 4
  .type g_list_model_get_n_items, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_model_get_n_items
#endif
g_list_model_get_n_items:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1112 & 0xffff
#if 1112 > 0xffff
  movk ip0, 1112 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_model_get_object
  .p2align 4
  .type g_list_model_get_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_model_get_object
#endif
g_list_model_get_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1113 & 0xffff
#if 1113 > 0xffff
  movk ip0, 1113 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_model_get_type
  .p2align 4
  .type g_list_model_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_model_get_type
#endif
g_list_model_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1114 & 0xffff
#if 1114 > 0xffff
  movk ip0, 1114 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_model_items_changed
  .p2align 4
  .type g_list_model_items_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_model_items_changed
#endif
g_list_model_items_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1115 & 0xffff
#if 1115 > 0xffff
  movk ip0, 1115 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_append
  .p2align 4
  .type g_list_store_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_append
#endif
g_list_store_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1116 & 0xffff
#if 1116 > 0xffff
  movk ip0, 1116 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_find
  .p2align 4
  .type g_list_store_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_find
#endif
g_list_store_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1117 & 0xffff
#if 1117 > 0xffff
  movk ip0, 1117 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_find_with_equal_func
  .p2align 4
  .type g_list_store_find_with_equal_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_find_with_equal_func
#endif
g_list_store_find_with_equal_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1118 & 0xffff
#if 1118 > 0xffff
  movk ip0, 1118 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_find_with_equal_func_full
  .p2align 4
  .type g_list_store_find_with_equal_func_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_find_with_equal_func_full
#endif
g_list_store_find_with_equal_func_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1119 & 0xffff
#if 1119 > 0xffff
  movk ip0, 1119 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_get_type
  .p2align 4
  .type g_list_store_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_get_type
#endif
g_list_store_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1120 & 0xffff
#if 1120 > 0xffff
  movk ip0, 1120 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_insert
  .p2align 4
  .type g_list_store_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_insert
#endif
g_list_store_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1121 & 0xffff
#if 1121 > 0xffff
  movk ip0, 1121 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_insert_sorted
  .p2align 4
  .type g_list_store_insert_sorted, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_insert_sorted
#endif
g_list_store_insert_sorted:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1122 & 0xffff
#if 1122 > 0xffff
  movk ip0, 1122 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_new
  .p2align 4
  .type g_list_store_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_new
#endif
g_list_store_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1123 & 0xffff
#if 1123 > 0xffff
  movk ip0, 1123 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_remove
  .p2align 4
  .type g_list_store_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_remove
#endif
g_list_store_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+8992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+8992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1124 & 0xffff
#if 1124 > 0xffff
  movk ip0, 1124 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_remove_all
  .p2align 4
  .type g_list_store_remove_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_remove_all
#endif
g_list_store_remove_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1125 & 0xffff
#if 1125 > 0xffff
  movk ip0, 1125 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_sort
  .p2align 4
  .type g_list_store_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_sort
#endif
g_list_store_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1126 & 0xffff
#if 1126 > 0xffff
  movk ip0, 1126 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_store_splice
  .p2align 4
  .type g_list_store_splice, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_store_splice
#endif
g_list_store_splice:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1127 & 0xffff
#if 1127 > 0xffff
  movk ip0, 1127 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_loadable_icon_get_type
  .p2align 4
  .type g_loadable_icon_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_loadable_icon_get_type
#endif
g_loadable_icon_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1128 & 0xffff
#if 1128 > 0xffff
  movk ip0, 1128 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_loadable_icon_load
  .p2align 4
  .type g_loadable_icon_load, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_loadable_icon_load
#endif
g_loadable_icon_load:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1129 & 0xffff
#if 1129 > 0xffff
  movk ip0, 1129 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_loadable_icon_load_async
  .p2align 4
  .type g_loadable_icon_load_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_loadable_icon_load_async
#endif
g_loadable_icon_load_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1130 & 0xffff
#if 1130 > 0xffff
  movk ip0, 1130 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_loadable_icon_load_finish
  .p2align 4
  .type g_loadable_icon_load_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_loadable_icon_load_finish
#endif
g_loadable_icon_load_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1131 & 0xffff
#if 1131 > 0xffff
  movk ip0, 1131 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_local_file_monitor_get_type
  .p2align 4
  .type g_local_file_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_local_file_monitor_get_type
#endif
g_local_file_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1132 & 0xffff
#if 1132 > 0xffff
  movk ip0, 1132 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_input_stream_add_bytes
  .p2align 4
  .type g_memory_input_stream_add_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_input_stream_add_bytes
#endif
g_memory_input_stream_add_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1133 & 0xffff
#if 1133 > 0xffff
  movk ip0, 1133 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_input_stream_add_data
  .p2align 4
  .type g_memory_input_stream_add_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_input_stream_add_data
#endif
g_memory_input_stream_add_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1134 & 0xffff
#if 1134 > 0xffff
  movk ip0, 1134 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_input_stream_get_type
  .p2align 4
  .type g_memory_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_input_stream_get_type
#endif
g_memory_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1135 & 0xffff
#if 1135 > 0xffff
  movk ip0, 1135 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_input_stream_new
  .p2align 4
  .type g_memory_input_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_input_stream_new
#endif
g_memory_input_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1136 & 0xffff
#if 1136 > 0xffff
  movk ip0, 1136 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_input_stream_new_from_bytes
  .p2align 4
  .type g_memory_input_stream_new_from_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_input_stream_new_from_bytes
#endif
g_memory_input_stream_new_from_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1137 & 0xffff
#if 1137 > 0xffff
  movk ip0, 1137 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_input_stream_new_from_data
  .p2align 4
  .type g_memory_input_stream_new_from_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_input_stream_new_from_data
#endif
g_memory_input_stream_new_from_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1138 & 0xffff
#if 1138 > 0xffff
  movk ip0, 1138 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_monitor_dup_default
  .p2align 4
  .type g_memory_monitor_dup_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_monitor_dup_default
#endif
g_memory_monitor_dup_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1139 & 0xffff
#if 1139 > 0xffff
  movk ip0, 1139 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_monitor_get_type
  .p2align 4
  .type g_memory_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_monitor_get_type
#endif
g_memory_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1140 & 0xffff
#if 1140 > 0xffff
  movk ip0, 1140 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_monitor_warning_level_get_type
  .p2align 4
  .type g_memory_monitor_warning_level_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_monitor_warning_level_get_type
#endif
g_memory_monitor_warning_level_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1141 & 0xffff
#if 1141 > 0xffff
  movk ip0, 1141 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_get_data
  .p2align 4
  .type g_memory_output_stream_get_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_get_data
#endif
g_memory_output_stream_get_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1142 & 0xffff
#if 1142 > 0xffff
  movk ip0, 1142 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_get_data_size
  .p2align 4
  .type g_memory_output_stream_get_data_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_get_data_size
#endif
g_memory_output_stream_get_data_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1143 & 0xffff
#if 1143 > 0xffff
  movk ip0, 1143 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_get_size
  .p2align 4
  .type g_memory_output_stream_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_get_size
#endif
g_memory_output_stream_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1144 & 0xffff
#if 1144 > 0xffff
  movk ip0, 1144 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_get_type
  .p2align 4
  .type g_memory_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_get_type
#endif
g_memory_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1145 & 0xffff
#if 1145 > 0xffff
  movk ip0, 1145 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_new
  .p2align 4
  .type g_memory_output_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_new
#endif
g_memory_output_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1146 & 0xffff
#if 1146 > 0xffff
  movk ip0, 1146 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_new_resizable
  .p2align 4
  .type g_memory_output_stream_new_resizable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_new_resizable
#endif
g_memory_output_stream_new_resizable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1147 & 0xffff
#if 1147 > 0xffff
  movk ip0, 1147 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_steal_as_bytes
  .p2align 4
  .type g_memory_output_stream_steal_as_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_steal_as_bytes
#endif
g_memory_output_stream_steal_as_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1148 & 0xffff
#if 1148 > 0xffff
  movk ip0, 1148 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_output_stream_steal_data
  .p2align 4
  .type g_memory_output_stream_steal_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_output_stream_steal_data
#endif
g_memory_output_stream_steal_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1149 & 0xffff
#if 1149 > 0xffff
  movk ip0, 1149 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memory_settings_backend_new
  .p2align 4
  .type g_memory_settings_backend_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memory_settings_backend_new
#endif
g_memory_settings_backend_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1150 & 0xffff
#if 1150 > 0xffff
  movk ip0, 1150 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_append
  .p2align 4
  .type g_menu_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_append
#endif
g_menu_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1151 & 0xffff
#if 1151 > 0xffff
  movk ip0, 1151 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_append_item
  .p2align 4
  .type g_menu_append_item, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_append_item
#endif
g_menu_append_item:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1152 & 0xffff
#if 1152 > 0xffff
  movk ip0, 1152 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_append_section
  .p2align 4
  .type g_menu_append_section, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_append_section
#endif
g_menu_append_section:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1153 & 0xffff
#if 1153 > 0xffff
  movk ip0, 1153 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_append_submenu
  .p2align 4
  .type g_menu_append_submenu, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_append_submenu
#endif
g_menu_append_submenu:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1154 & 0xffff
#if 1154 > 0xffff
  movk ip0, 1154 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_attribute_iter_get_name
  .p2align 4
  .type g_menu_attribute_iter_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_attribute_iter_get_name
#endif
g_menu_attribute_iter_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1155 & 0xffff
#if 1155 > 0xffff
  movk ip0, 1155 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_attribute_iter_get_next
  .p2align 4
  .type g_menu_attribute_iter_get_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_attribute_iter_get_next
#endif
g_menu_attribute_iter_get_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1156 & 0xffff
#if 1156 > 0xffff
  movk ip0, 1156 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_attribute_iter_get_type
  .p2align 4
  .type g_menu_attribute_iter_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_attribute_iter_get_type
#endif
g_menu_attribute_iter_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1157 & 0xffff
#if 1157 > 0xffff
  movk ip0, 1157 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_attribute_iter_get_value
  .p2align 4
  .type g_menu_attribute_iter_get_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_attribute_iter_get_value
#endif
g_menu_attribute_iter_get_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1158 & 0xffff
#if 1158 > 0xffff
  movk ip0, 1158 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_attribute_iter_next
  .p2align 4
  .type g_menu_attribute_iter_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_attribute_iter_next
#endif
g_menu_attribute_iter_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1159 & 0xffff
#if 1159 > 0xffff
  movk ip0, 1159 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_freeze
  .p2align 4
  .type g_menu_freeze, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_freeze
#endif
g_menu_freeze:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1160 & 0xffff
#if 1160 > 0xffff
  movk ip0, 1160 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_get_type
  .p2align 4
  .type g_menu_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_get_type
#endif
g_menu_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1161 & 0xffff
#if 1161 > 0xffff
  movk ip0, 1161 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_insert
  .p2align 4
  .type g_menu_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_insert
#endif
g_menu_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1162 & 0xffff
#if 1162 > 0xffff
  movk ip0, 1162 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_insert_item
  .p2align 4
  .type g_menu_insert_item, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_insert_item
#endif
g_menu_insert_item:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1163 & 0xffff
#if 1163 > 0xffff
  movk ip0, 1163 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_insert_section
  .p2align 4
  .type g_menu_insert_section, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_insert_section
#endif
g_menu_insert_section:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1164 & 0xffff
#if 1164 > 0xffff
  movk ip0, 1164 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_insert_submenu
  .p2align 4
  .type g_menu_insert_submenu, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_insert_submenu
#endif
g_menu_insert_submenu:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1165 & 0xffff
#if 1165 > 0xffff
  movk ip0, 1165 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_get_attribute
  .p2align 4
  .type g_menu_item_get_attribute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_get_attribute
#endif
g_menu_item_get_attribute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1166 & 0xffff
#if 1166 > 0xffff
  movk ip0, 1166 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_get_attribute_value
  .p2align 4
  .type g_menu_item_get_attribute_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_get_attribute_value
#endif
g_menu_item_get_attribute_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1167 & 0xffff
#if 1167 > 0xffff
  movk ip0, 1167 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_get_link
  .p2align 4
  .type g_menu_item_get_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_get_link
#endif
g_menu_item_get_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1168 & 0xffff
#if 1168 > 0xffff
  movk ip0, 1168 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_get_type
  .p2align 4
  .type g_menu_item_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_get_type
#endif
g_menu_item_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1169 & 0xffff
#if 1169 > 0xffff
  movk ip0, 1169 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_new
  .p2align 4
  .type g_menu_item_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_new
#endif
g_menu_item_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1170 & 0xffff
#if 1170 > 0xffff
  movk ip0, 1170 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_new_from_model
  .p2align 4
  .type g_menu_item_new_from_model, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_new_from_model
#endif
g_menu_item_new_from_model:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1171 & 0xffff
#if 1171 > 0xffff
  movk ip0, 1171 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_new_section
  .p2align 4
  .type g_menu_item_new_section, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_new_section
#endif
g_menu_item_new_section:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1172 & 0xffff
#if 1172 > 0xffff
  movk ip0, 1172 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_new_submenu
  .p2align 4
  .type g_menu_item_new_submenu, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_new_submenu
#endif
g_menu_item_new_submenu:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1173 & 0xffff
#if 1173 > 0xffff
  movk ip0, 1173 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_action_and_target
  .p2align 4
  .type g_menu_item_set_action_and_target, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_action_and_target
#endif
g_menu_item_set_action_and_target:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1174 & 0xffff
#if 1174 > 0xffff
  movk ip0, 1174 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_action_and_target_value
  .p2align 4
  .type g_menu_item_set_action_and_target_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_action_and_target_value
#endif
g_menu_item_set_action_and_target_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1175 & 0xffff
#if 1175 > 0xffff
  movk ip0, 1175 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_attribute
  .p2align 4
  .type g_menu_item_set_attribute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_attribute
#endif
g_menu_item_set_attribute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1176 & 0xffff
#if 1176 > 0xffff
  movk ip0, 1176 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_attribute_value
  .p2align 4
  .type g_menu_item_set_attribute_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_attribute_value
#endif
g_menu_item_set_attribute_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1177 & 0xffff
#if 1177 > 0xffff
  movk ip0, 1177 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_detailed_action
  .p2align 4
  .type g_menu_item_set_detailed_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_detailed_action
#endif
g_menu_item_set_detailed_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1178 & 0xffff
#if 1178 > 0xffff
  movk ip0, 1178 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_icon
  .p2align 4
  .type g_menu_item_set_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_icon
#endif
g_menu_item_set_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1179 & 0xffff
#if 1179 > 0xffff
  movk ip0, 1179 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_label
  .p2align 4
  .type g_menu_item_set_label, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_label
#endif
g_menu_item_set_label:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1180 & 0xffff
#if 1180 > 0xffff
  movk ip0, 1180 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_link
  .p2align 4
  .type g_menu_item_set_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_link
#endif
g_menu_item_set_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1181 & 0xffff
#if 1181 > 0xffff
  movk ip0, 1181 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_section
  .p2align 4
  .type g_menu_item_set_section, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_section
#endif
g_menu_item_set_section:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1182 & 0xffff
#if 1182 > 0xffff
  movk ip0, 1182 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_item_set_submenu
  .p2align 4
  .type g_menu_item_set_submenu, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_item_set_submenu
#endif
g_menu_item_set_submenu:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1183 & 0xffff
#if 1183 > 0xffff
  movk ip0, 1183 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_link_iter_get_name
  .p2align 4
  .type g_menu_link_iter_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_link_iter_get_name
#endif
g_menu_link_iter_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1184 & 0xffff
#if 1184 > 0xffff
  movk ip0, 1184 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_link_iter_get_next
  .p2align 4
  .type g_menu_link_iter_get_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_link_iter_get_next
#endif
g_menu_link_iter_get_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1185 & 0xffff
#if 1185 > 0xffff
  movk ip0, 1185 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_link_iter_get_type
  .p2align 4
  .type g_menu_link_iter_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_link_iter_get_type
#endif
g_menu_link_iter_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1186 & 0xffff
#if 1186 > 0xffff
  movk ip0, 1186 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_link_iter_get_value
  .p2align 4
  .type g_menu_link_iter_get_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_link_iter_get_value
#endif
g_menu_link_iter_get_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1187 & 0xffff
#if 1187 > 0xffff
  movk ip0, 1187 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_link_iter_next
  .p2align 4
  .type g_menu_link_iter_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_link_iter_next
#endif
g_menu_link_iter_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1188 & 0xffff
#if 1188 > 0xffff
  movk ip0, 1188 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_get_item_attribute
  .p2align 4
  .type g_menu_model_get_item_attribute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_get_item_attribute
#endif
g_menu_model_get_item_attribute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1189 & 0xffff
#if 1189 > 0xffff
  movk ip0, 1189 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_get_item_attribute_value
  .p2align 4
  .type g_menu_model_get_item_attribute_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_get_item_attribute_value
#endif
g_menu_model_get_item_attribute_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1190 & 0xffff
#if 1190 > 0xffff
  movk ip0, 1190 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_get_item_link
  .p2align 4
  .type g_menu_model_get_item_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_get_item_link
#endif
g_menu_model_get_item_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1191 & 0xffff
#if 1191 > 0xffff
  movk ip0, 1191 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_get_n_items
  .p2align 4
  .type g_menu_model_get_n_items, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_get_n_items
#endif
g_menu_model_get_n_items:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1192 & 0xffff
#if 1192 > 0xffff
  movk ip0, 1192 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_get_type
  .p2align 4
  .type g_menu_model_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_get_type
#endif
g_menu_model_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1193 & 0xffff
#if 1193 > 0xffff
  movk ip0, 1193 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_is_mutable
  .p2align 4
  .type g_menu_model_is_mutable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_is_mutable
#endif
g_menu_model_is_mutable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1194 & 0xffff
#if 1194 > 0xffff
  movk ip0, 1194 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_items_changed
  .p2align 4
  .type g_menu_model_items_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_items_changed
#endif
g_menu_model_items_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1195 & 0xffff
#if 1195 > 0xffff
  movk ip0, 1195 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_iterate_item_attributes
  .p2align 4
  .type g_menu_model_iterate_item_attributes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_iterate_item_attributes
#endif
g_menu_model_iterate_item_attributes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1196 & 0xffff
#if 1196 > 0xffff
  movk ip0, 1196 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_model_iterate_item_links
  .p2align 4
  .type g_menu_model_iterate_item_links, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_model_iterate_item_links
#endif
g_menu_model_iterate_item_links:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1197 & 0xffff
#if 1197 > 0xffff
  movk ip0, 1197 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_new
  .p2align 4
  .type g_menu_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_new
#endif
g_menu_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1198 & 0xffff
#if 1198 > 0xffff
  movk ip0, 1198 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_prepend
  .p2align 4
  .type g_menu_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_prepend
#endif
g_menu_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1199 & 0xffff
#if 1199 > 0xffff
  movk ip0, 1199 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_prepend_item
  .p2align 4
  .type g_menu_prepend_item, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_prepend_item
#endif
g_menu_prepend_item:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1200 & 0xffff
#if 1200 > 0xffff
  movk ip0, 1200 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_prepend_section
  .p2align 4
  .type g_menu_prepend_section, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_prepend_section
#endif
g_menu_prepend_section:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1201 & 0xffff
#if 1201 > 0xffff
  movk ip0, 1201 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_prepend_submenu
  .p2align 4
  .type g_menu_prepend_submenu, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_prepend_submenu
#endif
g_menu_prepend_submenu:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1202 & 0xffff
#if 1202 > 0xffff
  movk ip0, 1202 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_remove
  .p2align 4
  .type g_menu_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_remove
#endif
g_menu_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1203 & 0xffff
#if 1203 > 0xffff
  movk ip0, 1203 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_menu_remove_all
  .p2align 4
  .type g_menu_remove_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_menu_remove_all
#endif
g_menu_remove_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1204 & 0xffff
#if 1204 > 0xffff
  movk ip0, 1204 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_can_eject
  .p2align 4
  .type g_mount_can_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_can_eject
#endif
g_mount_can_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1205 & 0xffff
#if 1205 > 0xffff
  movk ip0, 1205 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_can_unmount
  .p2align 4
  .type g_mount_can_unmount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_can_unmount
#endif
g_mount_can_unmount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1206 & 0xffff
#if 1206 > 0xffff
  movk ip0, 1206 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_eject
  .p2align 4
  .type g_mount_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_eject
#endif
g_mount_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1207 & 0xffff
#if 1207 > 0xffff
  movk ip0, 1207 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_eject_finish
  .p2align 4
  .type g_mount_eject_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_eject_finish
#endif
g_mount_eject_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1208 & 0xffff
#if 1208 > 0xffff
  movk ip0, 1208 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_eject_with_operation
  .p2align 4
  .type g_mount_eject_with_operation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_eject_with_operation
#endif
g_mount_eject_with_operation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1209 & 0xffff
#if 1209 > 0xffff
  movk ip0, 1209 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_eject_with_operation_finish
  .p2align 4
  .type g_mount_eject_with_operation_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_eject_with_operation_finish
#endif
g_mount_eject_with_operation_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1210 & 0xffff
#if 1210 > 0xffff
  movk ip0, 1210 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_default_location
  .p2align 4
  .type g_mount_get_default_location, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_default_location
#endif
g_mount_get_default_location:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1211 & 0xffff
#if 1211 > 0xffff
  movk ip0, 1211 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_drive
  .p2align 4
  .type g_mount_get_drive, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_drive
#endif
g_mount_get_drive:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1212 & 0xffff
#if 1212 > 0xffff
  movk ip0, 1212 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_icon
  .p2align 4
  .type g_mount_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_icon
#endif
g_mount_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1213 & 0xffff
#if 1213 > 0xffff
  movk ip0, 1213 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_name
  .p2align 4
  .type g_mount_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_name
#endif
g_mount_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1214 & 0xffff
#if 1214 > 0xffff
  movk ip0, 1214 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_root
  .p2align 4
  .type g_mount_get_root, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_root
#endif
g_mount_get_root:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1215 & 0xffff
#if 1215 > 0xffff
  movk ip0, 1215 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_sort_key
  .p2align 4
  .type g_mount_get_sort_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_sort_key
#endif
g_mount_get_sort_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1216 & 0xffff
#if 1216 > 0xffff
  movk ip0, 1216 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_symbolic_icon
  .p2align 4
  .type g_mount_get_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_symbolic_icon
#endif
g_mount_get_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1217 & 0xffff
#if 1217 > 0xffff
  movk ip0, 1217 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_type
  .p2align 4
  .type g_mount_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_type
#endif
g_mount_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1218 & 0xffff
#if 1218 > 0xffff
  movk ip0, 1218 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_uuid
  .p2align 4
  .type g_mount_get_uuid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_uuid
#endif
g_mount_get_uuid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1219 & 0xffff
#if 1219 > 0xffff
  movk ip0, 1219 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_get_volume
  .p2align 4
  .type g_mount_get_volume, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_get_volume
#endif
g_mount_get_volume:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1220 & 0xffff
#if 1220 > 0xffff
  movk ip0, 1220 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_guess_content_type
  .p2align 4
  .type g_mount_guess_content_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_guess_content_type
#endif
g_mount_guess_content_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1221 & 0xffff
#if 1221 > 0xffff
  movk ip0, 1221 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_guess_content_type_finish
  .p2align 4
  .type g_mount_guess_content_type_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_guess_content_type_finish
#endif
g_mount_guess_content_type_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1222 & 0xffff
#if 1222 > 0xffff
  movk ip0, 1222 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_guess_content_type_sync
  .p2align 4
  .type g_mount_guess_content_type_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_guess_content_type_sync
#endif
g_mount_guess_content_type_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1223 & 0xffff
#if 1223 > 0xffff
  movk ip0, 1223 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_is_shadowed
  .p2align 4
  .type g_mount_is_shadowed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_is_shadowed
#endif
g_mount_is_shadowed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1224 & 0xffff
#if 1224 > 0xffff
  movk ip0, 1224 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_mount_flags_get_type
  .p2align 4
  .type g_mount_mount_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_mount_flags_get_type
#endif
g_mount_mount_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1225 & 0xffff
#if 1225 > 0xffff
  movk ip0, 1225 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_anonymous
  .p2align 4
  .type g_mount_operation_get_anonymous, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_anonymous
#endif
g_mount_operation_get_anonymous:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1226 & 0xffff
#if 1226 > 0xffff
  movk ip0, 1226 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_choice
  .p2align 4
  .type g_mount_operation_get_choice, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_choice
#endif
g_mount_operation_get_choice:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1227 & 0xffff
#if 1227 > 0xffff
  movk ip0, 1227 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_domain
  .p2align 4
  .type g_mount_operation_get_domain, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_domain
#endif
g_mount_operation_get_domain:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1228 & 0xffff
#if 1228 > 0xffff
  movk ip0, 1228 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_is_tcrypt_hidden_volume
  .p2align 4
  .type g_mount_operation_get_is_tcrypt_hidden_volume, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_is_tcrypt_hidden_volume
#endif
g_mount_operation_get_is_tcrypt_hidden_volume:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1229 & 0xffff
#if 1229 > 0xffff
  movk ip0, 1229 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_is_tcrypt_system_volume
  .p2align 4
  .type g_mount_operation_get_is_tcrypt_system_volume, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_is_tcrypt_system_volume
#endif
g_mount_operation_get_is_tcrypt_system_volume:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1230 & 0xffff
#if 1230 > 0xffff
  movk ip0, 1230 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_password
  .p2align 4
  .type g_mount_operation_get_password, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_password
#endif
g_mount_operation_get_password:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1231 & 0xffff
#if 1231 > 0xffff
  movk ip0, 1231 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_password_save
  .p2align 4
  .type g_mount_operation_get_password_save, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_password_save
#endif
g_mount_operation_get_password_save:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1232 & 0xffff
#if 1232 > 0xffff
  movk ip0, 1232 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_pim
  .p2align 4
  .type g_mount_operation_get_pim, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_pim
#endif
g_mount_operation_get_pim:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1233 & 0xffff
#if 1233 > 0xffff
  movk ip0, 1233 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_type
  .p2align 4
  .type g_mount_operation_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_type
#endif
g_mount_operation_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1234 & 0xffff
#if 1234 > 0xffff
  movk ip0, 1234 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_get_username
  .p2align 4
  .type g_mount_operation_get_username, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_get_username
#endif
g_mount_operation_get_username:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1235 & 0xffff
#if 1235 > 0xffff
  movk ip0, 1235 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_new
  .p2align 4
  .type g_mount_operation_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_new
#endif
g_mount_operation_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1236 & 0xffff
#if 1236 > 0xffff
  movk ip0, 1236 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_reply
  .p2align 4
  .type g_mount_operation_reply, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_reply
#endif
g_mount_operation_reply:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1237 & 0xffff
#if 1237 > 0xffff
  movk ip0, 1237 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_result_get_type
  .p2align 4
  .type g_mount_operation_result_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_result_get_type
#endif
g_mount_operation_result_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1238 & 0xffff
#if 1238 > 0xffff
  movk ip0, 1238 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_anonymous
  .p2align 4
  .type g_mount_operation_set_anonymous, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_anonymous
#endif
g_mount_operation_set_anonymous:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1239 & 0xffff
#if 1239 > 0xffff
  movk ip0, 1239 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_choice
  .p2align 4
  .type g_mount_operation_set_choice, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_choice
#endif
g_mount_operation_set_choice:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1240 & 0xffff
#if 1240 > 0xffff
  movk ip0, 1240 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_domain
  .p2align 4
  .type g_mount_operation_set_domain, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_domain
#endif
g_mount_operation_set_domain:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1241 & 0xffff
#if 1241 > 0xffff
  movk ip0, 1241 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_is_tcrypt_hidden_volume
  .p2align 4
  .type g_mount_operation_set_is_tcrypt_hidden_volume, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_is_tcrypt_hidden_volume
#endif
g_mount_operation_set_is_tcrypt_hidden_volume:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1242 & 0xffff
#if 1242 > 0xffff
  movk ip0, 1242 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_is_tcrypt_system_volume
  .p2align 4
  .type g_mount_operation_set_is_tcrypt_system_volume, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_is_tcrypt_system_volume
#endif
g_mount_operation_set_is_tcrypt_system_volume:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1243 & 0xffff
#if 1243 > 0xffff
  movk ip0, 1243 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_password
  .p2align 4
  .type g_mount_operation_set_password, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_password
#endif
g_mount_operation_set_password:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1244 & 0xffff
#if 1244 > 0xffff
  movk ip0, 1244 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_password_save
  .p2align 4
  .type g_mount_operation_set_password_save, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_password_save
#endif
g_mount_operation_set_password_save:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1245 & 0xffff
#if 1245 > 0xffff
  movk ip0, 1245 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_pim
  .p2align 4
  .type g_mount_operation_set_pim, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_pim
#endif
g_mount_operation_set_pim:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1246 & 0xffff
#if 1246 > 0xffff
  movk ip0, 1246 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_operation_set_username
  .p2align 4
  .type g_mount_operation_set_username, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_operation_set_username
#endif
g_mount_operation_set_username:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1247 & 0xffff
#if 1247 > 0xffff
  movk ip0, 1247 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_remount
  .p2align 4
  .type g_mount_remount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_remount
#endif
g_mount_remount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1248 & 0xffff
#if 1248 > 0xffff
  movk ip0, 1248 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_remount_finish
  .p2align 4
  .type g_mount_remount_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_remount_finish
#endif
g_mount_remount_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+9992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+9992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1249 & 0xffff
#if 1249 > 0xffff
  movk ip0, 1249 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_shadow
  .p2align 4
  .type g_mount_shadow, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_shadow
#endif
g_mount_shadow:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1250 & 0xffff
#if 1250 > 0xffff
  movk ip0, 1250 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_unmount
  .p2align 4
  .type g_mount_unmount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_unmount
#endif
g_mount_unmount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1251 & 0xffff
#if 1251 > 0xffff
  movk ip0, 1251 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_unmount_finish
  .p2align 4
  .type g_mount_unmount_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_unmount_finish
#endif
g_mount_unmount_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1252 & 0xffff
#if 1252 > 0xffff
  movk ip0, 1252 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_unmount_flags_get_type
  .p2align 4
  .type g_mount_unmount_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_unmount_flags_get_type
#endif
g_mount_unmount_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1253 & 0xffff
#if 1253 > 0xffff
  movk ip0, 1253 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_unmount_with_operation
  .p2align 4
  .type g_mount_unmount_with_operation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_unmount_with_operation
#endif
g_mount_unmount_with_operation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1254 & 0xffff
#if 1254 > 0xffff
  movk ip0, 1254 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_unmount_with_operation_finish
  .p2align 4
  .type g_mount_unmount_with_operation_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_unmount_with_operation_finish
#endif
g_mount_unmount_with_operation_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1255 & 0xffff
#if 1255 > 0xffff
  movk ip0, 1255 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mount_unshadow
  .p2align 4
  .type g_mount_unshadow, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mount_unshadow
#endif
g_mount_unshadow:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1256 & 0xffff
#if 1256 > 0xffff
  movk ip0, 1256 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_native_socket_address_get_type
  .p2align 4
  .type g_native_socket_address_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_native_socket_address_get_type
#endif
g_native_socket_address_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1257 & 0xffff
#if 1257 > 0xffff
  movk ip0, 1257 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_native_socket_address_new
  .p2align 4
  .type g_native_socket_address_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_native_socket_address_new
#endif
g_native_socket_address_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1258 & 0xffff
#if 1258 > 0xffff
  movk ip0, 1258 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_native_volume_monitor_get_type
  .p2align 4
  .type g_native_volume_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_native_volume_monitor_get_type
#endif
g_native_volume_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1259 & 0xffff
#if 1259 > 0xffff
  movk ip0, 1259 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_get_hostname
  .p2align 4
  .type g_network_address_get_hostname, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_get_hostname
#endif
g_network_address_get_hostname:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1260 & 0xffff
#if 1260 > 0xffff
  movk ip0, 1260 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_get_port
  .p2align 4
  .type g_network_address_get_port, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_get_port
#endif
g_network_address_get_port:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1261 & 0xffff
#if 1261 > 0xffff
  movk ip0, 1261 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_get_scheme
  .p2align 4
  .type g_network_address_get_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_get_scheme
#endif
g_network_address_get_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1262 & 0xffff
#if 1262 > 0xffff
  movk ip0, 1262 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_get_type
  .p2align 4
  .type g_network_address_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_get_type
#endif
g_network_address_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1263 & 0xffff
#if 1263 > 0xffff
  movk ip0, 1263 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_new
  .p2align 4
  .type g_network_address_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_new
#endif
g_network_address_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1264 & 0xffff
#if 1264 > 0xffff
  movk ip0, 1264 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_new_loopback
  .p2align 4
  .type g_network_address_new_loopback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_new_loopback
#endif
g_network_address_new_loopback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1265 & 0xffff
#if 1265 > 0xffff
  movk ip0, 1265 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_parse
  .p2align 4
  .type g_network_address_parse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_parse
#endif
g_network_address_parse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1266 & 0xffff
#if 1266 > 0xffff
  movk ip0, 1266 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_address_parse_uri
  .p2align 4
  .type g_network_address_parse_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_address_parse_uri
#endif
g_network_address_parse_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1267 & 0xffff
#if 1267 > 0xffff
  movk ip0, 1267 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_connectivity_get_type
  .p2align 4
  .type g_network_connectivity_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_connectivity_get_type
#endif
g_network_connectivity_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1268 & 0xffff
#if 1268 > 0xffff
  movk ip0, 1268 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_base_add_network
  .p2align 4
  .type g_network_monitor_base_add_network, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_base_add_network
#endif
g_network_monitor_base_add_network:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1269 & 0xffff
#if 1269 > 0xffff
  movk ip0, 1269 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_base_get_type
  .p2align 4
  .type g_network_monitor_base_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_base_get_type
#endif
g_network_monitor_base_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1270 & 0xffff
#if 1270 > 0xffff
  movk ip0, 1270 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_base_remove_network
  .p2align 4
  .type g_network_monitor_base_remove_network, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_base_remove_network
#endif
g_network_monitor_base_remove_network:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1271 & 0xffff
#if 1271 > 0xffff
  movk ip0, 1271 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_base_set_networks
  .p2align 4
  .type g_network_monitor_base_set_networks, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_base_set_networks
#endif
g_network_monitor_base_set_networks:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1272 & 0xffff
#if 1272 > 0xffff
  movk ip0, 1272 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_can_reach
  .p2align 4
  .type g_network_monitor_can_reach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_can_reach
#endif
g_network_monitor_can_reach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1273 & 0xffff
#if 1273 > 0xffff
  movk ip0, 1273 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_can_reach_async
  .p2align 4
  .type g_network_monitor_can_reach_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_can_reach_async
#endif
g_network_monitor_can_reach_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1274 & 0xffff
#if 1274 > 0xffff
  movk ip0, 1274 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_can_reach_finish
  .p2align 4
  .type g_network_monitor_can_reach_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_can_reach_finish
#endif
g_network_monitor_can_reach_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1275 & 0xffff
#if 1275 > 0xffff
  movk ip0, 1275 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_get_connectivity
  .p2align 4
  .type g_network_monitor_get_connectivity, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_get_connectivity
#endif
g_network_monitor_get_connectivity:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1276 & 0xffff
#if 1276 > 0xffff
  movk ip0, 1276 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_get_default
  .p2align 4
  .type g_network_monitor_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_get_default
#endif
g_network_monitor_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1277 & 0xffff
#if 1277 > 0xffff
  movk ip0, 1277 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_get_network_available
  .p2align 4
  .type g_network_monitor_get_network_available, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_get_network_available
#endif
g_network_monitor_get_network_available:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1278 & 0xffff
#if 1278 > 0xffff
  movk ip0, 1278 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_get_network_metered
  .p2align 4
  .type g_network_monitor_get_network_metered, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_get_network_metered
#endif
g_network_monitor_get_network_metered:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1279 & 0xffff
#if 1279 > 0xffff
  movk ip0, 1279 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_monitor_get_type
  .p2align 4
  .type g_network_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_monitor_get_type
#endif
g_network_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1280 & 0xffff
#if 1280 > 0xffff
  movk ip0, 1280 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_service_get_domain
  .p2align 4
  .type g_network_service_get_domain, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_service_get_domain
#endif
g_network_service_get_domain:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1281 & 0xffff
#if 1281 > 0xffff
  movk ip0, 1281 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_service_get_protocol
  .p2align 4
  .type g_network_service_get_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_service_get_protocol
#endif
g_network_service_get_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1282 & 0xffff
#if 1282 > 0xffff
  movk ip0, 1282 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_service_get_scheme
  .p2align 4
  .type g_network_service_get_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_service_get_scheme
#endif
g_network_service_get_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1283 & 0xffff
#if 1283 > 0xffff
  movk ip0, 1283 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_service_get_service
  .p2align 4
  .type g_network_service_get_service, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_service_get_service
#endif
g_network_service_get_service:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1284 & 0xffff
#if 1284 > 0xffff
  movk ip0, 1284 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_service_get_type
  .p2align 4
  .type g_network_service_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_service_get_type
#endif
g_network_service_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1285 & 0xffff
#if 1285 > 0xffff
  movk ip0, 1285 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_service_new
  .p2align 4
  .type g_network_service_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_service_new
#endif
g_network_service_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1286 & 0xffff
#if 1286 > 0xffff
  movk ip0, 1286 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_network_service_set_scheme
  .p2align 4
  .type g_network_service_set_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_network_service_set_scheme
#endif
g_network_service_set_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1287 & 0xffff
#if 1287 > 0xffff
  movk ip0, 1287 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_networking_init
  .p2align 4
  .type g_networking_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_networking_init
#endif
g_networking_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1288 & 0xffff
#if 1288 > 0xffff
  movk ip0, 1288 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_add_button
  .p2align 4
  .type g_notification_add_button, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_add_button
#endif
g_notification_add_button:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1289 & 0xffff
#if 1289 > 0xffff
  movk ip0, 1289 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_add_button_with_target
  .p2align 4
  .type g_notification_add_button_with_target, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_add_button_with_target
#endif
g_notification_add_button_with_target:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1290 & 0xffff
#if 1290 > 0xffff
  movk ip0, 1290 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_add_button_with_target_value
  .p2align 4
  .type g_notification_add_button_with_target_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_add_button_with_target_value
#endif
g_notification_add_button_with_target_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1291 & 0xffff
#if 1291 > 0xffff
  movk ip0, 1291 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_get_type
  .p2align 4
  .type g_notification_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_get_type
#endif
g_notification_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1292 & 0xffff
#if 1292 > 0xffff
  movk ip0, 1292 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_new
  .p2align 4
  .type g_notification_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_new
#endif
g_notification_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1293 & 0xffff
#if 1293 > 0xffff
  movk ip0, 1293 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_priority_get_type
  .p2align 4
  .type g_notification_priority_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_priority_get_type
#endif
g_notification_priority_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1294 & 0xffff
#if 1294 > 0xffff
  movk ip0, 1294 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_body
  .p2align 4
  .type g_notification_set_body, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_body
#endif
g_notification_set_body:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1295 & 0xffff
#if 1295 > 0xffff
  movk ip0, 1295 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_category
  .p2align 4
  .type g_notification_set_category, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_category
#endif
g_notification_set_category:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1296 & 0xffff
#if 1296 > 0xffff
  movk ip0, 1296 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_default_action
  .p2align 4
  .type g_notification_set_default_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_default_action
#endif
g_notification_set_default_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1297 & 0xffff
#if 1297 > 0xffff
  movk ip0, 1297 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_default_action_and_target
  .p2align 4
  .type g_notification_set_default_action_and_target, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_default_action_and_target
#endif
g_notification_set_default_action_and_target:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1298 & 0xffff
#if 1298 > 0xffff
  movk ip0, 1298 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_default_action_and_target_value
  .p2align 4
  .type g_notification_set_default_action_and_target_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_default_action_and_target_value
#endif
g_notification_set_default_action_and_target_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1299 & 0xffff
#if 1299 > 0xffff
  movk ip0, 1299 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_icon
  .p2align 4
  .type g_notification_set_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_icon
#endif
g_notification_set_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1300 & 0xffff
#if 1300 > 0xffff
  movk ip0, 1300 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_priority
  .p2align 4
  .type g_notification_set_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_priority
#endif
g_notification_set_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1301 & 0xffff
#if 1301 > 0xffff
  movk ip0, 1301 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_title
  .p2align 4
  .type g_notification_set_title, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_title
#endif
g_notification_set_title:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1302 & 0xffff
#if 1302 > 0xffff
  movk ip0, 1302 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_notification_set_urgent
  .p2align 4
  .type g_notification_set_urgent, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_notification_set_urgent
#endif
g_notification_set_urgent:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1303 & 0xffff
#if 1303 > 0xffff
  movk ip0, 1303 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_null_settings_backend_new
  .p2align 4
  .type g_null_settings_backend_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_null_settings_backend_new
#endif
g_null_settings_backend_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1304 & 0xffff
#if 1304 > 0xffff
  movk ip0, 1304 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_clear_pending
  .p2align 4
  .type g_output_stream_clear_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_clear_pending
#endif
g_output_stream_clear_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1305 & 0xffff
#if 1305 > 0xffff
  movk ip0, 1305 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_close
  .p2align 4
  .type g_output_stream_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_close
#endif
g_output_stream_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1306 & 0xffff
#if 1306 > 0xffff
  movk ip0, 1306 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_close_async
  .p2align 4
  .type g_output_stream_close_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_close_async
#endif
g_output_stream_close_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1307 & 0xffff
#if 1307 > 0xffff
  movk ip0, 1307 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_close_finish
  .p2align 4
  .type g_output_stream_close_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_close_finish
#endif
g_output_stream_close_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1308 & 0xffff
#if 1308 > 0xffff
  movk ip0, 1308 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_flush
  .p2align 4
  .type g_output_stream_flush, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_flush
#endif
g_output_stream_flush:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1309 & 0xffff
#if 1309 > 0xffff
  movk ip0, 1309 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_flush_async
  .p2align 4
  .type g_output_stream_flush_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_flush_async
#endif
g_output_stream_flush_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1310 & 0xffff
#if 1310 > 0xffff
  movk ip0, 1310 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_flush_finish
  .p2align 4
  .type g_output_stream_flush_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_flush_finish
#endif
g_output_stream_flush_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1311 & 0xffff
#if 1311 > 0xffff
  movk ip0, 1311 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_get_type
  .p2align 4
  .type g_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_get_type
#endif
g_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1312 & 0xffff
#if 1312 > 0xffff
  movk ip0, 1312 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_has_pending
  .p2align 4
  .type g_output_stream_has_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_has_pending
#endif
g_output_stream_has_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1313 & 0xffff
#if 1313 > 0xffff
  movk ip0, 1313 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_is_closed
  .p2align 4
  .type g_output_stream_is_closed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_is_closed
#endif
g_output_stream_is_closed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1314 & 0xffff
#if 1314 > 0xffff
  movk ip0, 1314 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_is_closing
  .p2align 4
  .type g_output_stream_is_closing, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_is_closing
#endif
g_output_stream_is_closing:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1315 & 0xffff
#if 1315 > 0xffff
  movk ip0, 1315 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_printf
  .p2align 4
  .type g_output_stream_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_printf
#endif
g_output_stream_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1316 & 0xffff
#if 1316 > 0xffff
  movk ip0, 1316 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_set_pending
  .p2align 4
  .type g_output_stream_set_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_set_pending
#endif
g_output_stream_set_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1317 & 0xffff
#if 1317 > 0xffff
  movk ip0, 1317 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_splice
  .p2align 4
  .type g_output_stream_splice, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_splice
#endif
g_output_stream_splice:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1318 & 0xffff
#if 1318 > 0xffff
  movk ip0, 1318 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_splice_async
  .p2align 4
  .type g_output_stream_splice_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_splice_async
#endif
g_output_stream_splice_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1319 & 0xffff
#if 1319 > 0xffff
  movk ip0, 1319 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_splice_finish
  .p2align 4
  .type g_output_stream_splice_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_splice_finish
#endif
g_output_stream_splice_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1320 & 0xffff
#if 1320 > 0xffff
  movk ip0, 1320 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_splice_flags_get_type
  .p2align 4
  .type g_output_stream_splice_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_splice_flags_get_type
#endif
g_output_stream_splice_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1321 & 0xffff
#if 1321 > 0xffff
  movk ip0, 1321 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_vprintf
  .p2align 4
  .type g_output_stream_vprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_vprintf
#endif
g_output_stream_vprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1322 & 0xffff
#if 1322 > 0xffff
  movk ip0, 1322 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write
  .p2align 4
  .type g_output_stream_write, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write
#endif
g_output_stream_write:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1323 & 0xffff
#if 1323 > 0xffff
  movk ip0, 1323 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_all
  .p2align 4
  .type g_output_stream_write_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_all
#endif
g_output_stream_write_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1324 & 0xffff
#if 1324 > 0xffff
  movk ip0, 1324 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_all_async
  .p2align 4
  .type g_output_stream_write_all_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_all_async
#endif
g_output_stream_write_all_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1325 & 0xffff
#if 1325 > 0xffff
  movk ip0, 1325 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_all_finish
  .p2align 4
  .type g_output_stream_write_all_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_all_finish
#endif
g_output_stream_write_all_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1326 & 0xffff
#if 1326 > 0xffff
  movk ip0, 1326 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_async
  .p2align 4
  .type g_output_stream_write_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_async
#endif
g_output_stream_write_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1327 & 0xffff
#if 1327 > 0xffff
  movk ip0, 1327 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_bytes
  .p2align 4
  .type g_output_stream_write_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_bytes
#endif
g_output_stream_write_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1328 & 0xffff
#if 1328 > 0xffff
  movk ip0, 1328 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_bytes_async
  .p2align 4
  .type g_output_stream_write_bytes_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_bytes_async
#endif
g_output_stream_write_bytes_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1329 & 0xffff
#if 1329 > 0xffff
  movk ip0, 1329 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_bytes_finish
  .p2align 4
  .type g_output_stream_write_bytes_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_bytes_finish
#endif
g_output_stream_write_bytes_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1330 & 0xffff
#if 1330 > 0xffff
  movk ip0, 1330 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_write_finish
  .p2align 4
  .type g_output_stream_write_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_write_finish
#endif
g_output_stream_write_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1331 & 0xffff
#if 1331 > 0xffff
  movk ip0, 1331 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_writev
  .p2align 4
  .type g_output_stream_writev, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_writev
#endif
g_output_stream_writev:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1332 & 0xffff
#if 1332 > 0xffff
  movk ip0, 1332 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_writev_all
  .p2align 4
  .type g_output_stream_writev_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_writev_all
#endif
g_output_stream_writev_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1333 & 0xffff
#if 1333 > 0xffff
  movk ip0, 1333 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_writev_all_async
  .p2align 4
  .type g_output_stream_writev_all_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_writev_all_async
#endif
g_output_stream_writev_all_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1334 & 0xffff
#if 1334 > 0xffff
  movk ip0, 1334 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_writev_all_finish
  .p2align 4
  .type g_output_stream_writev_all_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_writev_all_finish
#endif
g_output_stream_writev_all_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1335 & 0xffff
#if 1335 > 0xffff
  movk ip0, 1335 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_writev_async
  .p2align 4
  .type g_output_stream_writev_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_writev_async
#endif
g_output_stream_writev_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1336 & 0xffff
#if 1336 > 0xffff
  movk ip0, 1336 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_output_stream_writev_finish
  .p2align 4
  .type g_output_stream_writev_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_output_stream_writev_finish
#endif
g_output_stream_writev_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1337 & 0xffff
#if 1337 > 0xffff
  movk ip0, 1337 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_password_save_get_type
  .p2align 4
  .type g_password_save_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_password_save_get_type
#endif
g_password_save_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1338 & 0xffff
#if 1338 > 0xffff
  movk ip0, 1338 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_acquire
  .p2align 4
  .type g_permission_acquire, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_acquire
#endif
g_permission_acquire:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1339 & 0xffff
#if 1339 > 0xffff
  movk ip0, 1339 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_acquire_async
  .p2align 4
  .type g_permission_acquire_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_acquire_async
#endif
g_permission_acquire_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1340 & 0xffff
#if 1340 > 0xffff
  movk ip0, 1340 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_acquire_finish
  .p2align 4
  .type g_permission_acquire_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_acquire_finish
#endif
g_permission_acquire_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1341 & 0xffff
#if 1341 > 0xffff
  movk ip0, 1341 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_get_allowed
  .p2align 4
  .type g_permission_get_allowed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_get_allowed
#endif
g_permission_get_allowed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1342 & 0xffff
#if 1342 > 0xffff
  movk ip0, 1342 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_get_can_acquire
  .p2align 4
  .type g_permission_get_can_acquire, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_get_can_acquire
#endif
g_permission_get_can_acquire:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1343 & 0xffff
#if 1343 > 0xffff
  movk ip0, 1343 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_get_can_release
  .p2align 4
  .type g_permission_get_can_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_get_can_release
#endif
g_permission_get_can_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1344 & 0xffff
#if 1344 > 0xffff
  movk ip0, 1344 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_get_type
  .p2align 4
  .type g_permission_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_get_type
#endif
g_permission_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1345 & 0xffff
#if 1345 > 0xffff
  movk ip0, 1345 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_impl_update
  .p2align 4
  .type g_permission_impl_update, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_impl_update
#endif
g_permission_impl_update:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1346 & 0xffff
#if 1346 > 0xffff
  movk ip0, 1346 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_release
  .p2align 4
  .type g_permission_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_release
#endif
g_permission_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1347 & 0xffff
#if 1347 > 0xffff
  movk ip0, 1347 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_release_async
  .p2align 4
  .type g_permission_release_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_release_async
#endif
g_permission_release_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1348 & 0xffff
#if 1348 > 0xffff
  movk ip0, 1348 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_permission_release_finish
  .p2align 4
  .type g_permission_release_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_permission_release_finish
#endif
g_permission_release_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1349 & 0xffff
#if 1349 > 0xffff
  movk ip0, 1349 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_input_stream_can_poll
  .p2align 4
  .type g_pollable_input_stream_can_poll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_input_stream_can_poll
#endif
g_pollable_input_stream_can_poll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1350 & 0xffff
#if 1350 > 0xffff
  movk ip0, 1350 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_input_stream_create_source
  .p2align 4
  .type g_pollable_input_stream_create_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_input_stream_create_source
#endif
g_pollable_input_stream_create_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1351 & 0xffff
#if 1351 > 0xffff
  movk ip0, 1351 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_input_stream_get_type
  .p2align 4
  .type g_pollable_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_input_stream_get_type
#endif
g_pollable_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1352 & 0xffff
#if 1352 > 0xffff
  movk ip0, 1352 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_input_stream_is_readable
  .p2align 4
  .type g_pollable_input_stream_is_readable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_input_stream_is_readable
#endif
g_pollable_input_stream_is_readable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1353 & 0xffff
#if 1353 > 0xffff
  movk ip0, 1353 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_input_stream_read_nonblocking
  .p2align 4
  .type g_pollable_input_stream_read_nonblocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_input_stream_read_nonblocking
#endif
g_pollable_input_stream_read_nonblocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1354 & 0xffff
#if 1354 > 0xffff
  movk ip0, 1354 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_output_stream_can_poll
  .p2align 4
  .type g_pollable_output_stream_can_poll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_output_stream_can_poll
#endif
g_pollable_output_stream_can_poll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1355 & 0xffff
#if 1355 > 0xffff
  movk ip0, 1355 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_output_stream_create_source
  .p2align 4
  .type g_pollable_output_stream_create_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_output_stream_create_source
#endif
g_pollable_output_stream_create_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1356 & 0xffff
#if 1356 > 0xffff
  movk ip0, 1356 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_output_stream_get_type
  .p2align 4
  .type g_pollable_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_output_stream_get_type
#endif
g_pollable_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1357 & 0xffff
#if 1357 > 0xffff
  movk ip0, 1357 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_output_stream_is_writable
  .p2align 4
  .type g_pollable_output_stream_is_writable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_output_stream_is_writable
#endif
g_pollable_output_stream_is_writable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1358 & 0xffff
#if 1358 > 0xffff
  movk ip0, 1358 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_output_stream_write_nonblocking
  .p2align 4
  .type g_pollable_output_stream_write_nonblocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_output_stream_write_nonblocking
#endif
g_pollable_output_stream_write_nonblocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1359 & 0xffff
#if 1359 > 0xffff
  movk ip0, 1359 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_output_stream_writev_nonblocking
  .p2align 4
  .type g_pollable_output_stream_writev_nonblocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_output_stream_writev_nonblocking
#endif
g_pollable_output_stream_writev_nonblocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1360 & 0xffff
#if 1360 > 0xffff
  movk ip0, 1360 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_return_get_type
  .p2align 4
  .type g_pollable_return_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_return_get_type
#endif
g_pollable_return_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1361 & 0xffff
#if 1361 > 0xffff
  movk ip0, 1361 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_source_new
  .p2align 4
  .type g_pollable_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_source_new
#endif
g_pollable_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1362 & 0xffff
#if 1362 > 0xffff
  movk ip0, 1362 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_source_new_full
  .p2align 4
  .type g_pollable_source_new_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_source_new_full
#endif
g_pollable_source_new_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1363 & 0xffff
#if 1363 > 0xffff
  movk ip0, 1363 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_stream_read
  .p2align 4
  .type g_pollable_stream_read, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_stream_read
#endif
g_pollable_stream_read:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1364 & 0xffff
#if 1364 > 0xffff
  movk ip0, 1364 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_stream_write
  .p2align 4
  .type g_pollable_stream_write, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_stream_write
#endif
g_pollable_stream_write:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1365 & 0xffff
#if 1365 > 0xffff
  movk ip0, 1365 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pollable_stream_write_all
  .p2align 4
  .type g_pollable_stream_write_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pollable_stream_write_all
#endif
g_pollable_stream_write_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1366 & 0xffff
#if 1366 > 0xffff
  movk ip0, 1366 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_power_profile_monitor_dup_default
  .p2align 4
  .type g_power_profile_monitor_dup_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_power_profile_monitor_dup_default
#endif
g_power_profile_monitor_dup_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1367 & 0xffff
#if 1367 > 0xffff
  movk ip0, 1367 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_power_profile_monitor_get_power_saver_enabled
  .p2align 4
  .type g_power_profile_monitor_get_power_saver_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_power_profile_monitor_get_power_saver_enabled
#endif
g_power_profile_monitor_get_power_saver_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1368 & 0xffff
#if 1368 > 0xffff
  movk ip0, 1368 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_power_profile_monitor_get_type
  .p2align 4
  .type g_power_profile_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_power_profile_monitor_get_type
#endif
g_power_profile_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1369 & 0xffff
#if 1369 > 0xffff
  movk ip0, 1369 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_property_action_get_type
  .p2align 4
  .type g_property_action_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_property_action_get_type
#endif
g_property_action_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1370 & 0xffff
#if 1370 > 0xffff
  movk ip0, 1370 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_property_action_new
  .p2align 4
  .type g_property_action_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_property_action_new
#endif
g_property_action_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1371 & 0xffff
#if 1371 > 0xffff
  movk ip0, 1371 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_enumerator_get_type
  .p2align 4
  .type g_proxy_address_enumerator_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_enumerator_get_type
#endif
g_proxy_address_enumerator_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1372 & 0xffff
#if 1372 > 0xffff
  movk ip0, 1372 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_destination_hostname
  .p2align 4
  .type g_proxy_address_get_destination_hostname, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_destination_hostname
#endif
g_proxy_address_get_destination_hostname:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1373 & 0xffff
#if 1373 > 0xffff
  movk ip0, 1373 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_destination_port
  .p2align 4
  .type g_proxy_address_get_destination_port, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_destination_port
#endif
g_proxy_address_get_destination_port:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+10992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+10992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1374 & 0xffff
#if 1374 > 0xffff
  movk ip0, 1374 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_destination_protocol
  .p2align 4
  .type g_proxy_address_get_destination_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_destination_protocol
#endif
g_proxy_address_get_destination_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1375 & 0xffff
#if 1375 > 0xffff
  movk ip0, 1375 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_password
  .p2align 4
  .type g_proxy_address_get_password, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_password
#endif
g_proxy_address_get_password:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1376 & 0xffff
#if 1376 > 0xffff
  movk ip0, 1376 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_protocol
  .p2align 4
  .type g_proxy_address_get_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_protocol
#endif
g_proxy_address_get_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1377 & 0xffff
#if 1377 > 0xffff
  movk ip0, 1377 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_type
  .p2align 4
  .type g_proxy_address_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_type
#endif
g_proxy_address_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1378 & 0xffff
#if 1378 > 0xffff
  movk ip0, 1378 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_uri
  .p2align 4
  .type g_proxy_address_get_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_uri
#endif
g_proxy_address_get_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1379 & 0xffff
#if 1379 > 0xffff
  movk ip0, 1379 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_get_username
  .p2align 4
  .type g_proxy_address_get_username, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_get_username
#endif
g_proxy_address_get_username:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1380 & 0xffff
#if 1380 > 0xffff
  movk ip0, 1380 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_address_new
  .p2align 4
  .type g_proxy_address_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_address_new
#endif
g_proxy_address_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1381 & 0xffff
#if 1381 > 0xffff
  movk ip0, 1381 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_connect
  .p2align 4
  .type g_proxy_connect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_connect
#endif
g_proxy_connect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1382 & 0xffff
#if 1382 > 0xffff
  movk ip0, 1382 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_connect_async
  .p2align 4
  .type g_proxy_connect_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_connect_async
#endif
g_proxy_connect_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1383 & 0xffff
#if 1383 > 0xffff
  movk ip0, 1383 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_connect_finish
  .p2align 4
  .type g_proxy_connect_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_connect_finish
#endif
g_proxy_connect_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1384 & 0xffff
#if 1384 > 0xffff
  movk ip0, 1384 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_get_default_for_protocol
  .p2align 4
  .type g_proxy_get_default_for_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_get_default_for_protocol
#endif
g_proxy_get_default_for_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1385 & 0xffff
#if 1385 > 0xffff
  movk ip0, 1385 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_get_type
  .p2align 4
  .type g_proxy_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_get_type
#endif
g_proxy_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1386 & 0xffff
#if 1386 > 0xffff
  movk ip0, 1386 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_resolver_get_default
  .p2align 4
  .type g_proxy_resolver_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_resolver_get_default
#endif
g_proxy_resolver_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1387 & 0xffff
#if 1387 > 0xffff
  movk ip0, 1387 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_resolver_get_type
  .p2align 4
  .type g_proxy_resolver_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_resolver_get_type
#endif
g_proxy_resolver_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1388 & 0xffff
#if 1388 > 0xffff
  movk ip0, 1388 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_resolver_is_supported
  .p2align 4
  .type g_proxy_resolver_is_supported, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_resolver_is_supported
#endif
g_proxy_resolver_is_supported:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1389 & 0xffff
#if 1389 > 0xffff
  movk ip0, 1389 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_resolver_lookup
  .p2align 4
  .type g_proxy_resolver_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_resolver_lookup
#endif
g_proxy_resolver_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1390 & 0xffff
#if 1390 > 0xffff
  movk ip0, 1390 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_resolver_lookup_async
  .p2align 4
  .type g_proxy_resolver_lookup_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_resolver_lookup_async
#endif
g_proxy_resolver_lookup_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1391 & 0xffff
#if 1391 > 0xffff
  movk ip0, 1391 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_resolver_lookup_finish
  .p2align 4
  .type g_proxy_resolver_lookup_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_resolver_lookup_finish
#endif
g_proxy_resolver_lookup_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1392 & 0xffff
#if 1392 > 0xffff
  movk ip0, 1392 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_proxy_supports_hostname
  .p2align 4
  .type g_proxy_supports_hostname, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_proxy_supports_hostname
#endif
g_proxy_supports_hostname:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1393 & 0xffff
#if 1393 > 0xffff
  movk ip0, 1393 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_remote_action_group_activate_action_full
  .p2align 4
  .type g_remote_action_group_activate_action_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_remote_action_group_activate_action_full
#endif
g_remote_action_group_activate_action_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1394 & 0xffff
#if 1394 > 0xffff
  movk ip0, 1394 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_remote_action_group_change_action_state_full
  .p2align 4
  .type g_remote_action_group_change_action_state_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_remote_action_group_change_action_state_full
#endif
g_remote_action_group_change_action_state_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1395 & 0xffff
#if 1395 > 0xffff
  movk ip0, 1395 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_remote_action_group_get_type
  .p2align 4
  .type g_remote_action_group_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_remote_action_group_get_type
#endif
g_remote_action_group_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1396 & 0xffff
#if 1396 > 0xffff
  movk ip0, 1396 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_error_get_type
  .p2align 4
  .type g_resolver_error_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_error_get_type
#endif
g_resolver_error_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1397 & 0xffff
#if 1397 > 0xffff
  movk ip0, 1397 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_error_quark
  .p2align 4
  .type g_resolver_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_error_quark
#endif
g_resolver_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1398 & 0xffff
#if 1398 > 0xffff
  movk ip0, 1398 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_free_addresses
  .p2align 4
  .type g_resolver_free_addresses, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_free_addresses
#endif
g_resolver_free_addresses:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1399 & 0xffff
#if 1399 > 0xffff
  movk ip0, 1399 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_free_targets
  .p2align 4
  .type g_resolver_free_targets, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_free_targets
#endif
g_resolver_free_targets:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1400 & 0xffff
#if 1400 > 0xffff
  movk ip0, 1400 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_get_default
  .p2align 4
  .type g_resolver_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_get_default
#endif
g_resolver_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1401 & 0xffff
#if 1401 > 0xffff
  movk ip0, 1401 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_get_timeout
  .p2align 4
  .type g_resolver_get_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_get_timeout
#endif
g_resolver_get_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1402 & 0xffff
#if 1402 > 0xffff
  movk ip0, 1402 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_get_type
  .p2align 4
  .type g_resolver_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_get_type
#endif
g_resolver_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1403 & 0xffff
#if 1403 > 0xffff
  movk ip0, 1403 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_address
  .p2align 4
  .type g_resolver_lookup_by_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_address
#endif
g_resolver_lookup_by_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1404 & 0xffff
#if 1404 > 0xffff
  movk ip0, 1404 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_address_async
  .p2align 4
  .type g_resolver_lookup_by_address_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_address_async
#endif
g_resolver_lookup_by_address_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1405 & 0xffff
#if 1405 > 0xffff
  movk ip0, 1405 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_address_finish
  .p2align 4
  .type g_resolver_lookup_by_address_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_address_finish
#endif
g_resolver_lookup_by_address_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1406 & 0xffff
#if 1406 > 0xffff
  movk ip0, 1406 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_name
  .p2align 4
  .type g_resolver_lookup_by_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_name
#endif
g_resolver_lookup_by_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1407 & 0xffff
#if 1407 > 0xffff
  movk ip0, 1407 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_name_async
  .p2align 4
  .type g_resolver_lookup_by_name_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_name_async
#endif
g_resolver_lookup_by_name_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1408 & 0xffff
#if 1408 > 0xffff
  movk ip0, 1408 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_name_finish
  .p2align 4
  .type g_resolver_lookup_by_name_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_name_finish
#endif
g_resolver_lookup_by_name_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1409 & 0xffff
#if 1409 > 0xffff
  movk ip0, 1409 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_name_with_flags
  .p2align 4
  .type g_resolver_lookup_by_name_with_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_name_with_flags
#endif
g_resolver_lookup_by_name_with_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1410 & 0xffff
#if 1410 > 0xffff
  movk ip0, 1410 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_name_with_flags_async
  .p2align 4
  .type g_resolver_lookup_by_name_with_flags_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_name_with_flags_async
#endif
g_resolver_lookup_by_name_with_flags_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1411 & 0xffff
#if 1411 > 0xffff
  movk ip0, 1411 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_by_name_with_flags_finish
  .p2align 4
  .type g_resolver_lookup_by_name_with_flags_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_by_name_with_flags_finish
#endif
g_resolver_lookup_by_name_with_flags_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1412 & 0xffff
#if 1412 > 0xffff
  movk ip0, 1412 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_records
  .p2align 4
  .type g_resolver_lookup_records, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_records
#endif
g_resolver_lookup_records:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1413 & 0xffff
#if 1413 > 0xffff
  movk ip0, 1413 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_records_async
  .p2align 4
  .type g_resolver_lookup_records_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_records_async
#endif
g_resolver_lookup_records_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1414 & 0xffff
#if 1414 > 0xffff
  movk ip0, 1414 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_records_finish
  .p2align 4
  .type g_resolver_lookup_records_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_records_finish
#endif
g_resolver_lookup_records_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1415 & 0xffff
#if 1415 > 0xffff
  movk ip0, 1415 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_service
  .p2align 4
  .type g_resolver_lookup_service, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_service
#endif
g_resolver_lookup_service:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1416 & 0xffff
#if 1416 > 0xffff
  movk ip0, 1416 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_service_async
  .p2align 4
  .type g_resolver_lookup_service_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_service_async
#endif
g_resolver_lookup_service_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1417 & 0xffff
#if 1417 > 0xffff
  movk ip0, 1417 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_lookup_service_finish
  .p2align 4
  .type g_resolver_lookup_service_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_lookup_service_finish
#endif
g_resolver_lookup_service_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1418 & 0xffff
#if 1418 > 0xffff
  movk ip0, 1418 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_name_lookup_flags_get_type
  .p2align 4
  .type g_resolver_name_lookup_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_name_lookup_flags_get_type
#endif
g_resolver_name_lookup_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1419 & 0xffff
#if 1419 > 0xffff
  movk ip0, 1419 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_record_type_get_type
  .p2align 4
  .type g_resolver_record_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_record_type_get_type
#endif
g_resolver_record_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1420 & 0xffff
#if 1420 > 0xffff
  movk ip0, 1420 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_record_type_to_rrtype
  .p2align 4
  .type g_resolver_record_type_to_rrtype, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_record_type_to_rrtype
#endif
g_resolver_record_type_to_rrtype:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1421 & 0xffff
#if 1421 > 0xffff
  movk ip0, 1421 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_records_from_res_query
  .p2align 4
  .type g_resolver_records_from_res_query, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_records_from_res_query
#endif
g_resolver_records_from_res_query:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1422 & 0xffff
#if 1422 > 0xffff
  movk ip0, 1422 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_set_default
  .p2align 4
  .type g_resolver_set_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_set_default
#endif
g_resolver_set_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1423 & 0xffff
#if 1423 > 0xffff
  movk ip0, 1423 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resolver_set_timeout
  .p2align 4
  .type g_resolver_set_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resolver_set_timeout
#endif
g_resolver_set_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1424 & 0xffff
#if 1424 > 0xffff
  movk ip0, 1424 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_enumerate_children
  .p2align 4
  .type g_resource_enumerate_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_enumerate_children
#endif
g_resource_enumerate_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1425 & 0xffff
#if 1425 > 0xffff
  movk ip0, 1425 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_error_get_type
  .p2align 4
  .type g_resource_error_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_error_get_type
#endif
g_resource_error_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1426 & 0xffff
#if 1426 > 0xffff
  movk ip0, 1426 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_error_quark
  .p2align 4
  .type g_resource_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_error_quark
#endif
g_resource_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1427 & 0xffff
#if 1427 > 0xffff
  movk ip0, 1427 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_flags_get_type
  .p2align 4
  .type g_resource_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_flags_get_type
#endif
g_resource_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1428 & 0xffff
#if 1428 > 0xffff
  movk ip0, 1428 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_get_info
  .p2align 4
  .type g_resource_get_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_get_info
#endif
g_resource_get_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1429 & 0xffff
#if 1429 > 0xffff
  movk ip0, 1429 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_get_type
  .p2align 4
  .type g_resource_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_get_type
#endif
g_resource_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1430 & 0xffff
#if 1430 > 0xffff
  movk ip0, 1430 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_has_children
  .p2align 4
  .type g_resource_has_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_has_children
#endif
g_resource_has_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1431 & 0xffff
#if 1431 > 0xffff
  movk ip0, 1431 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_load
  .p2align 4
  .type g_resource_load, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_load
#endif
g_resource_load:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1432 & 0xffff
#if 1432 > 0xffff
  movk ip0, 1432 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_lookup_data
  .p2align 4
  .type g_resource_lookup_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_lookup_data
#endif
g_resource_lookup_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1433 & 0xffff
#if 1433 > 0xffff
  movk ip0, 1433 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_lookup_flags_get_type
  .p2align 4
  .type g_resource_lookup_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_lookup_flags_get_type
#endif
g_resource_lookup_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1434 & 0xffff
#if 1434 > 0xffff
  movk ip0, 1434 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_new_from_data
  .p2align 4
  .type g_resource_new_from_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_new_from_data
#endif
g_resource_new_from_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1435 & 0xffff
#if 1435 > 0xffff
  movk ip0, 1435 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_open_stream
  .p2align 4
  .type g_resource_open_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_open_stream
#endif
g_resource_open_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1436 & 0xffff
#if 1436 > 0xffff
  movk ip0, 1436 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_ref
  .p2align 4
  .type g_resource_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_ref
#endif
g_resource_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1437 & 0xffff
#if 1437 > 0xffff
  movk ip0, 1437 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resource_unref
  .p2align 4
  .type g_resource_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resource_unref
#endif
g_resource_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1438 & 0xffff
#if 1438 > 0xffff
  movk ip0, 1438 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resources_enumerate_children
  .p2align 4
  .type g_resources_enumerate_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resources_enumerate_children
#endif
g_resources_enumerate_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1439 & 0xffff
#if 1439 > 0xffff
  movk ip0, 1439 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resources_get_info
  .p2align 4
  .type g_resources_get_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resources_get_info
#endif
g_resources_get_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1440 & 0xffff
#if 1440 > 0xffff
  movk ip0, 1440 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resources_has_children
  .p2align 4
  .type g_resources_has_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resources_has_children
#endif
g_resources_has_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1441 & 0xffff
#if 1441 > 0xffff
  movk ip0, 1441 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resources_lookup_data
  .p2align 4
  .type g_resources_lookup_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resources_lookup_data
#endif
g_resources_lookup_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1442 & 0xffff
#if 1442 > 0xffff
  movk ip0, 1442 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resources_open_stream
  .p2align 4
  .type g_resources_open_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resources_open_stream
#endif
g_resources_open_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1443 & 0xffff
#if 1443 > 0xffff
  movk ip0, 1443 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resources_register
  .p2align 4
  .type g_resources_register, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resources_register
#endif
g_resources_register:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1444 & 0xffff
#if 1444 > 0xffff
  movk ip0, 1444 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_resources_unregister
  .p2align 4
  .type g_resources_unregister, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_resources_unregister
#endif
g_resources_unregister:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1445 & 0xffff
#if 1445 > 0xffff
  movk ip0, 1445 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_seekable_can_seek
  .p2align 4
  .type g_seekable_can_seek, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_seekable_can_seek
#endif
g_seekable_can_seek:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1446 & 0xffff
#if 1446 > 0xffff
  movk ip0, 1446 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_seekable_can_truncate
  .p2align 4
  .type g_seekable_can_truncate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_seekable_can_truncate
#endif
g_seekable_can_truncate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1447 & 0xffff
#if 1447 > 0xffff
  movk ip0, 1447 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_seekable_get_type
  .p2align 4
  .type g_seekable_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_seekable_get_type
#endif
g_seekable_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1448 & 0xffff
#if 1448 > 0xffff
  movk ip0, 1448 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_seekable_seek
  .p2align 4
  .type g_seekable_seek, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_seekable_seek
#endif
g_seekable_seek:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1449 & 0xffff
#if 1449 > 0xffff
  movk ip0, 1449 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_seekable_tell
  .p2align 4
  .type g_seekable_tell, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_seekable_tell
#endif
g_seekable_tell:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1450 & 0xffff
#if 1450 > 0xffff
  movk ip0, 1450 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_seekable_truncate
  .p2align 4
  .type g_seekable_truncate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_seekable_truncate
#endif
g_seekable_truncate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1451 & 0xffff
#if 1451 > 0xffff
  movk ip0, 1451 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_apply
  .p2align 4
  .type g_settings_apply, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_apply
#endif
g_settings_apply:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1452 & 0xffff
#if 1452 > 0xffff
  movk ip0, 1452 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_changed
  .p2align 4
  .type g_settings_backend_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_changed
#endif
g_settings_backend_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1453 & 0xffff
#if 1453 > 0xffff
  movk ip0, 1453 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_changed_tree
  .p2align 4
  .type g_settings_backend_changed_tree, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_changed_tree
#endif
g_settings_backend_changed_tree:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1454 & 0xffff
#if 1454 > 0xffff
  movk ip0, 1454 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_flatten_tree
  .p2align 4
  .type g_settings_backend_flatten_tree, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_flatten_tree
#endif
g_settings_backend_flatten_tree:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1455 & 0xffff
#if 1455 > 0xffff
  movk ip0, 1455 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_get_default
  .p2align 4
  .type g_settings_backend_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_get_default
#endif
g_settings_backend_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1456 & 0xffff
#if 1456 > 0xffff
  movk ip0, 1456 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_get_type
  .p2align 4
  .type g_settings_backend_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_get_type
#endif
g_settings_backend_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1457 & 0xffff
#if 1457 > 0xffff
  movk ip0, 1457 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_keys_changed
  .p2align 4
  .type g_settings_backend_keys_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_keys_changed
#endif
g_settings_backend_keys_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1458 & 0xffff
#if 1458 > 0xffff
  movk ip0, 1458 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_path_changed
  .p2align 4
  .type g_settings_backend_path_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_path_changed
#endif
g_settings_backend_path_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1459 & 0xffff
#if 1459 > 0xffff
  movk ip0, 1459 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_path_writable_changed
  .p2align 4
  .type g_settings_backend_path_writable_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_path_writable_changed
#endif
g_settings_backend_path_writable_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1460 & 0xffff
#if 1460 > 0xffff
  movk ip0, 1460 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_backend_writable_changed
  .p2align 4
  .type g_settings_backend_writable_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_backend_writable_changed
#endif
g_settings_backend_writable_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1461 & 0xffff
#if 1461 > 0xffff
  movk ip0, 1461 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_bind
  .p2align 4
  .type g_settings_bind, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_bind
#endif
g_settings_bind:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1462 & 0xffff
#if 1462 > 0xffff
  movk ip0, 1462 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_bind_flags_get_type
  .p2align 4
  .type g_settings_bind_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_bind_flags_get_type
#endif
g_settings_bind_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1463 & 0xffff
#if 1463 > 0xffff
  movk ip0, 1463 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_bind_with_mapping
  .p2align 4
  .type g_settings_bind_with_mapping, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_bind_with_mapping
#endif
g_settings_bind_with_mapping:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1464 & 0xffff
#if 1464 > 0xffff
  movk ip0, 1464 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_bind_with_mapping_closures
  .p2align 4
  .type g_settings_bind_with_mapping_closures, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_bind_with_mapping_closures
#endif
g_settings_bind_with_mapping_closures:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1465 & 0xffff
#if 1465 > 0xffff
  movk ip0, 1465 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_bind_writable
  .p2align 4
  .type g_settings_bind_writable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_bind_writable
#endif
g_settings_bind_writable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1466 & 0xffff
#if 1466 > 0xffff
  movk ip0, 1466 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_create_action
  .p2align 4
  .type g_settings_create_action, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_create_action
#endif
g_settings_create_action:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1467 & 0xffff
#if 1467 > 0xffff
  movk ip0, 1467 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_delay
  .p2align 4
  .type g_settings_delay, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_delay
#endif
g_settings_delay:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1468 & 0xffff
#if 1468 > 0xffff
  movk ip0, 1468 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get
  .p2align 4
  .type g_settings_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get
#endif
g_settings_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1469 & 0xffff
#if 1469 > 0xffff
  movk ip0, 1469 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_boolean
  .p2align 4
  .type g_settings_get_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_boolean
#endif
g_settings_get_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1470 & 0xffff
#if 1470 > 0xffff
  movk ip0, 1470 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_child
  .p2align 4
  .type g_settings_get_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_child
#endif
g_settings_get_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1471 & 0xffff
#if 1471 > 0xffff
  movk ip0, 1471 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_default_value
  .p2align 4
  .type g_settings_get_default_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_default_value
#endif
g_settings_get_default_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1472 & 0xffff
#if 1472 > 0xffff
  movk ip0, 1472 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_double
  .p2align 4
  .type g_settings_get_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_double
#endif
g_settings_get_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1473 & 0xffff
#if 1473 > 0xffff
  movk ip0, 1473 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_enum
  .p2align 4
  .type g_settings_get_enum, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_enum
#endif
g_settings_get_enum:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1474 & 0xffff
#if 1474 > 0xffff
  movk ip0, 1474 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_flags
  .p2align 4
  .type g_settings_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_flags
#endif
g_settings_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1475 & 0xffff
#if 1475 > 0xffff
  movk ip0, 1475 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_has_unapplied
  .p2align 4
  .type g_settings_get_has_unapplied, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_has_unapplied
#endif
g_settings_get_has_unapplied:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1476 & 0xffff
#if 1476 > 0xffff
  movk ip0, 1476 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_int
  .p2align 4
  .type g_settings_get_int, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_int
#endif
g_settings_get_int:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1477 & 0xffff
#if 1477 > 0xffff
  movk ip0, 1477 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_int64
  .p2align 4
  .type g_settings_get_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_int64
#endif
g_settings_get_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1478 & 0xffff
#if 1478 > 0xffff
  movk ip0, 1478 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_mapped
  .p2align 4
  .type g_settings_get_mapped, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_mapped
#endif
g_settings_get_mapped:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1479 & 0xffff
#if 1479 > 0xffff
  movk ip0, 1479 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_range
  .p2align 4
  .type g_settings_get_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_range
#endif
g_settings_get_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1480 & 0xffff
#if 1480 > 0xffff
  movk ip0, 1480 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_string
  .p2align 4
  .type g_settings_get_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_string
#endif
g_settings_get_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1481 & 0xffff
#if 1481 > 0xffff
  movk ip0, 1481 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_strv
  .p2align 4
  .type g_settings_get_strv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_strv
#endif
g_settings_get_strv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1482 & 0xffff
#if 1482 > 0xffff
  movk ip0, 1482 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_type
  .p2align 4
  .type g_settings_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_type
#endif
g_settings_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1483 & 0xffff
#if 1483 > 0xffff
  movk ip0, 1483 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_uint
  .p2align 4
  .type g_settings_get_uint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_uint
#endif
g_settings_get_uint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1484 & 0xffff
#if 1484 > 0xffff
  movk ip0, 1484 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_uint64
  .p2align 4
  .type g_settings_get_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_uint64
#endif
g_settings_get_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1485 & 0xffff
#if 1485 > 0xffff
  movk ip0, 1485 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_user_value
  .p2align 4
  .type g_settings_get_user_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_user_value
#endif
g_settings_get_user_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1486 & 0xffff
#if 1486 > 0xffff
  movk ip0, 1486 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_get_value
  .p2align 4
  .type g_settings_get_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_get_value
#endif
g_settings_get_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1487 & 0xffff
#if 1487 > 0xffff
  movk ip0, 1487 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_is_writable
  .p2align 4
  .type g_settings_is_writable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_is_writable
#endif
g_settings_is_writable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1488 & 0xffff
#if 1488 > 0xffff
  movk ip0, 1488 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_list_children
  .p2align 4
  .type g_settings_list_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_list_children
#endif
g_settings_list_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1489 & 0xffff
#if 1489 > 0xffff
  movk ip0, 1489 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_list_keys
  .p2align 4
  .type g_settings_list_keys, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_list_keys
#endif
g_settings_list_keys:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1490 & 0xffff
#if 1490 > 0xffff
  movk ip0, 1490 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_list_relocatable_schemas
  .p2align 4
  .type g_settings_list_relocatable_schemas, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_list_relocatable_schemas
#endif
g_settings_list_relocatable_schemas:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1491 & 0xffff
#if 1491 > 0xffff
  movk ip0, 1491 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_list_schemas
  .p2align 4
  .type g_settings_list_schemas, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_list_schemas
#endif
g_settings_list_schemas:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1492 & 0xffff
#if 1492 > 0xffff
  movk ip0, 1492 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_new
  .p2align 4
  .type g_settings_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_new
#endif
g_settings_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1493 & 0xffff
#if 1493 > 0xffff
  movk ip0, 1493 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_new_full
  .p2align 4
  .type g_settings_new_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_new_full
#endif
g_settings_new_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1494 & 0xffff
#if 1494 > 0xffff
  movk ip0, 1494 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_new_with_backend
  .p2align 4
  .type g_settings_new_with_backend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_new_with_backend
#endif
g_settings_new_with_backend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1495 & 0xffff
#if 1495 > 0xffff
  movk ip0, 1495 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_new_with_backend_and_path
  .p2align 4
  .type g_settings_new_with_backend_and_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_new_with_backend_and_path
#endif
g_settings_new_with_backend_and_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1496 & 0xffff
#if 1496 > 0xffff
  movk ip0, 1496 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_new_with_path
  .p2align 4
  .type g_settings_new_with_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_new_with_path
#endif
g_settings_new_with_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1497 & 0xffff
#if 1497 > 0xffff
  movk ip0, 1497 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_range_check
  .p2align 4
  .type g_settings_range_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_range_check
#endif
g_settings_range_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1498 & 0xffff
#if 1498 > 0xffff
  movk ip0, 1498 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_reset
  .p2align 4
  .type g_settings_reset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_reset
#endif
g_settings_reset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+11992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+11992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1499 & 0xffff
#if 1499 > 0xffff
  movk ip0, 1499 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_revert
  .p2align 4
  .type g_settings_revert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_revert
#endif
g_settings_revert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1500 & 0xffff
#if 1500 > 0xffff
  movk ip0, 1500 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_get_id
  .p2align 4
  .type g_settings_schema_get_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_get_id
#endif
g_settings_schema_get_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1501 & 0xffff
#if 1501 > 0xffff
  movk ip0, 1501 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_get_key
  .p2align 4
  .type g_settings_schema_get_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_get_key
#endif
g_settings_schema_get_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1502 & 0xffff
#if 1502 > 0xffff
  movk ip0, 1502 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_get_path
  .p2align 4
  .type g_settings_schema_get_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_get_path
#endif
g_settings_schema_get_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1503 & 0xffff
#if 1503 > 0xffff
  movk ip0, 1503 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_get_type
  .p2align 4
  .type g_settings_schema_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_get_type
#endif
g_settings_schema_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1504 & 0xffff
#if 1504 > 0xffff
  movk ip0, 1504 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_has_key
  .p2align 4
  .type g_settings_schema_has_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_has_key
#endif
g_settings_schema_has_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1505 & 0xffff
#if 1505 > 0xffff
  movk ip0, 1505 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_get_default_value
  .p2align 4
  .type g_settings_schema_key_get_default_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_get_default_value
#endif
g_settings_schema_key_get_default_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1506 & 0xffff
#if 1506 > 0xffff
  movk ip0, 1506 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_get_description
  .p2align 4
  .type g_settings_schema_key_get_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_get_description
#endif
g_settings_schema_key_get_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1507 & 0xffff
#if 1507 > 0xffff
  movk ip0, 1507 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_get_name
  .p2align 4
  .type g_settings_schema_key_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_get_name
#endif
g_settings_schema_key_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1508 & 0xffff
#if 1508 > 0xffff
  movk ip0, 1508 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_get_range
  .p2align 4
  .type g_settings_schema_key_get_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_get_range
#endif
g_settings_schema_key_get_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1509 & 0xffff
#if 1509 > 0xffff
  movk ip0, 1509 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_get_summary
  .p2align 4
  .type g_settings_schema_key_get_summary, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_get_summary
#endif
g_settings_schema_key_get_summary:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1510 & 0xffff
#if 1510 > 0xffff
  movk ip0, 1510 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_get_type
  .p2align 4
  .type g_settings_schema_key_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_get_type
#endif
g_settings_schema_key_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1511 & 0xffff
#if 1511 > 0xffff
  movk ip0, 1511 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_get_value_type
  .p2align 4
  .type g_settings_schema_key_get_value_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_get_value_type
#endif
g_settings_schema_key_get_value_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1512 & 0xffff
#if 1512 > 0xffff
  movk ip0, 1512 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_range_check
  .p2align 4
  .type g_settings_schema_key_range_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_range_check
#endif
g_settings_schema_key_range_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1513 & 0xffff
#if 1513 > 0xffff
  movk ip0, 1513 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_ref
  .p2align 4
  .type g_settings_schema_key_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_ref
#endif
g_settings_schema_key_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1514 & 0xffff
#if 1514 > 0xffff
  movk ip0, 1514 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_key_unref
  .p2align 4
  .type g_settings_schema_key_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_key_unref
#endif
g_settings_schema_key_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1515 & 0xffff
#if 1515 > 0xffff
  movk ip0, 1515 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_list_children
  .p2align 4
  .type g_settings_schema_list_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_list_children
#endif
g_settings_schema_list_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1516 & 0xffff
#if 1516 > 0xffff
  movk ip0, 1516 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_list_keys
  .p2align 4
  .type g_settings_schema_list_keys, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_list_keys
#endif
g_settings_schema_list_keys:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1517 & 0xffff
#if 1517 > 0xffff
  movk ip0, 1517 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_ref
  .p2align 4
  .type g_settings_schema_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_ref
#endif
g_settings_schema_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1518 & 0xffff
#if 1518 > 0xffff
  movk ip0, 1518 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_source_get_default
  .p2align 4
  .type g_settings_schema_source_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_source_get_default
#endif
g_settings_schema_source_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1519 & 0xffff
#if 1519 > 0xffff
  movk ip0, 1519 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_source_get_type
  .p2align 4
  .type g_settings_schema_source_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_source_get_type
#endif
g_settings_schema_source_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1520 & 0xffff
#if 1520 > 0xffff
  movk ip0, 1520 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_source_list_schemas
  .p2align 4
  .type g_settings_schema_source_list_schemas, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_source_list_schemas
#endif
g_settings_schema_source_list_schemas:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1521 & 0xffff
#if 1521 > 0xffff
  movk ip0, 1521 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_source_lookup
  .p2align 4
  .type g_settings_schema_source_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_source_lookup
#endif
g_settings_schema_source_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1522 & 0xffff
#if 1522 > 0xffff
  movk ip0, 1522 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_source_new_from_directory
  .p2align 4
  .type g_settings_schema_source_new_from_directory, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_source_new_from_directory
#endif
g_settings_schema_source_new_from_directory:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1523 & 0xffff
#if 1523 > 0xffff
  movk ip0, 1523 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_source_ref
  .p2align 4
  .type g_settings_schema_source_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_source_ref
#endif
g_settings_schema_source_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1524 & 0xffff
#if 1524 > 0xffff
  movk ip0, 1524 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_source_unref
  .p2align 4
  .type g_settings_schema_source_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_source_unref
#endif
g_settings_schema_source_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1525 & 0xffff
#if 1525 > 0xffff
  movk ip0, 1525 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_schema_unref
  .p2align 4
  .type g_settings_schema_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_schema_unref
#endif
g_settings_schema_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1526 & 0xffff
#if 1526 > 0xffff
  movk ip0, 1526 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set
  .p2align 4
  .type g_settings_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set
#endif
g_settings_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1527 & 0xffff
#if 1527 > 0xffff
  movk ip0, 1527 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_boolean
  .p2align 4
  .type g_settings_set_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_boolean
#endif
g_settings_set_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1528 & 0xffff
#if 1528 > 0xffff
  movk ip0, 1528 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_double
  .p2align 4
  .type g_settings_set_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_double
#endif
g_settings_set_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1529 & 0xffff
#if 1529 > 0xffff
  movk ip0, 1529 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_enum
  .p2align 4
  .type g_settings_set_enum, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_enum
#endif
g_settings_set_enum:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1530 & 0xffff
#if 1530 > 0xffff
  movk ip0, 1530 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_flags
  .p2align 4
  .type g_settings_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_flags
#endif
g_settings_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1531 & 0xffff
#if 1531 > 0xffff
  movk ip0, 1531 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_int
  .p2align 4
  .type g_settings_set_int, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_int
#endif
g_settings_set_int:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1532 & 0xffff
#if 1532 > 0xffff
  movk ip0, 1532 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_int64
  .p2align 4
  .type g_settings_set_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_int64
#endif
g_settings_set_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1533 & 0xffff
#if 1533 > 0xffff
  movk ip0, 1533 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_string
  .p2align 4
  .type g_settings_set_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_string
#endif
g_settings_set_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1534 & 0xffff
#if 1534 > 0xffff
  movk ip0, 1534 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_strv
  .p2align 4
  .type g_settings_set_strv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_strv
#endif
g_settings_set_strv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1535 & 0xffff
#if 1535 > 0xffff
  movk ip0, 1535 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_uint
  .p2align 4
  .type g_settings_set_uint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_uint
#endif
g_settings_set_uint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1536 & 0xffff
#if 1536 > 0xffff
  movk ip0, 1536 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_uint64
  .p2align 4
  .type g_settings_set_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_uint64
#endif
g_settings_set_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1537 & 0xffff
#if 1537 > 0xffff
  movk ip0, 1537 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_set_value
  .p2align 4
  .type g_settings_set_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_set_value
#endif
g_settings_set_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1538 & 0xffff
#if 1538 > 0xffff
  movk ip0, 1538 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_sync
  .p2align 4
  .type g_settings_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_sync
#endif
g_settings_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1539 & 0xffff
#if 1539 > 0xffff
  movk ip0, 1539 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_settings_unbind
  .p2align 4
  .type g_settings_unbind, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_settings_unbind
#endif
g_settings_unbind:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1540 & 0xffff
#if 1540 > 0xffff
  movk ip0, 1540 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_get_type
  .p2align 4
  .type g_simple_action_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_get_type
#endif
g_simple_action_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1541 & 0xffff
#if 1541 > 0xffff
  movk ip0, 1541 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_group_add_entries
  .p2align 4
  .type g_simple_action_group_add_entries, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_group_add_entries
#endif
g_simple_action_group_add_entries:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1542 & 0xffff
#if 1542 > 0xffff
  movk ip0, 1542 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_group_get_type
  .p2align 4
  .type g_simple_action_group_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_group_get_type
#endif
g_simple_action_group_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1543 & 0xffff
#if 1543 > 0xffff
  movk ip0, 1543 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_group_insert
  .p2align 4
  .type g_simple_action_group_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_group_insert
#endif
g_simple_action_group_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1544 & 0xffff
#if 1544 > 0xffff
  movk ip0, 1544 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_group_lookup
  .p2align 4
  .type g_simple_action_group_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_group_lookup
#endif
g_simple_action_group_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1545 & 0xffff
#if 1545 > 0xffff
  movk ip0, 1545 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_group_new
  .p2align 4
  .type g_simple_action_group_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_group_new
#endif
g_simple_action_group_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1546 & 0xffff
#if 1546 > 0xffff
  movk ip0, 1546 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_group_remove
  .p2align 4
  .type g_simple_action_group_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_group_remove
#endif
g_simple_action_group_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1547 & 0xffff
#if 1547 > 0xffff
  movk ip0, 1547 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_new
  .p2align 4
  .type g_simple_action_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_new
#endif
g_simple_action_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1548 & 0xffff
#if 1548 > 0xffff
  movk ip0, 1548 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_new_stateful
  .p2align 4
  .type g_simple_action_new_stateful, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_new_stateful
#endif
g_simple_action_new_stateful:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1549 & 0xffff
#if 1549 > 0xffff
  movk ip0, 1549 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_set_enabled
  .p2align 4
  .type g_simple_action_set_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_set_enabled
#endif
g_simple_action_set_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1550 & 0xffff
#if 1550 > 0xffff
  movk ip0, 1550 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_set_state
  .p2align 4
  .type g_simple_action_set_state, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_set_state
#endif
g_simple_action_set_state:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1551 & 0xffff
#if 1551 > 0xffff
  movk ip0, 1551 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_action_set_state_hint
  .p2align 4
  .type g_simple_action_set_state_hint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_action_set_state_hint
#endif
g_simple_action_set_state_hint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1552 & 0xffff
#if 1552 > 0xffff
  movk ip0, 1552 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_report_error_in_idle
  .p2align 4
  .type g_simple_async_report_error_in_idle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_report_error_in_idle
#endif
g_simple_async_report_error_in_idle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1553 & 0xffff
#if 1553 > 0xffff
  movk ip0, 1553 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_report_gerror_in_idle
  .p2align 4
  .type g_simple_async_report_gerror_in_idle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_report_gerror_in_idle
#endif
g_simple_async_report_gerror_in_idle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1554 & 0xffff
#if 1554 > 0xffff
  movk ip0, 1554 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_report_take_gerror_in_idle
  .p2align 4
  .type g_simple_async_report_take_gerror_in_idle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_report_take_gerror_in_idle
#endif
g_simple_async_report_take_gerror_in_idle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1555 & 0xffff
#if 1555 > 0xffff
  movk ip0, 1555 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_complete
  .p2align 4
  .type g_simple_async_result_complete, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_complete
#endif
g_simple_async_result_complete:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1556 & 0xffff
#if 1556 > 0xffff
  movk ip0, 1556 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_complete_in_idle
  .p2align 4
  .type g_simple_async_result_complete_in_idle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_complete_in_idle
#endif
g_simple_async_result_complete_in_idle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1557 & 0xffff
#if 1557 > 0xffff
  movk ip0, 1557 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_get_op_res_gboolean
  .p2align 4
  .type g_simple_async_result_get_op_res_gboolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_get_op_res_gboolean
#endif
g_simple_async_result_get_op_res_gboolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1558 & 0xffff
#if 1558 > 0xffff
  movk ip0, 1558 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_get_op_res_gpointer
  .p2align 4
  .type g_simple_async_result_get_op_res_gpointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_get_op_res_gpointer
#endif
g_simple_async_result_get_op_res_gpointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1559 & 0xffff
#if 1559 > 0xffff
  movk ip0, 1559 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_get_op_res_gssize
  .p2align 4
  .type g_simple_async_result_get_op_res_gssize, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_get_op_res_gssize
#endif
g_simple_async_result_get_op_res_gssize:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1560 & 0xffff
#if 1560 > 0xffff
  movk ip0, 1560 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_get_source_tag
  .p2align 4
  .type g_simple_async_result_get_source_tag, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_get_source_tag
#endif
g_simple_async_result_get_source_tag:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1561 & 0xffff
#if 1561 > 0xffff
  movk ip0, 1561 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_get_type
  .p2align 4
  .type g_simple_async_result_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_get_type
#endif
g_simple_async_result_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1562 & 0xffff
#if 1562 > 0xffff
  movk ip0, 1562 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_is_valid
  .p2align 4
  .type g_simple_async_result_is_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_is_valid
#endif
g_simple_async_result_is_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1563 & 0xffff
#if 1563 > 0xffff
  movk ip0, 1563 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_new
  .p2align 4
  .type g_simple_async_result_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_new
#endif
g_simple_async_result_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1564 & 0xffff
#if 1564 > 0xffff
  movk ip0, 1564 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_new_error
  .p2align 4
  .type g_simple_async_result_new_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_new_error
#endif
g_simple_async_result_new_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1565 & 0xffff
#if 1565 > 0xffff
  movk ip0, 1565 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_new_from_error
  .p2align 4
  .type g_simple_async_result_new_from_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_new_from_error
#endif
g_simple_async_result_new_from_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1566 & 0xffff
#if 1566 > 0xffff
  movk ip0, 1566 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_new_take_error
  .p2align 4
  .type g_simple_async_result_new_take_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_new_take_error
#endif
g_simple_async_result_new_take_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1567 & 0xffff
#if 1567 > 0xffff
  movk ip0, 1567 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_propagate_error
  .p2align 4
  .type g_simple_async_result_propagate_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_propagate_error
#endif
g_simple_async_result_propagate_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1568 & 0xffff
#if 1568 > 0xffff
  movk ip0, 1568 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_run_in_thread
  .p2align 4
  .type g_simple_async_result_run_in_thread, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_run_in_thread
#endif
g_simple_async_result_run_in_thread:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1569 & 0xffff
#if 1569 > 0xffff
  movk ip0, 1569 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_check_cancellable
  .p2align 4
  .type g_simple_async_result_set_check_cancellable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_check_cancellable
#endif
g_simple_async_result_set_check_cancellable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1570 & 0xffff
#if 1570 > 0xffff
  movk ip0, 1570 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_error
  .p2align 4
  .type g_simple_async_result_set_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_error
#endif
g_simple_async_result_set_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1571 & 0xffff
#if 1571 > 0xffff
  movk ip0, 1571 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_error_va
  .p2align 4
  .type g_simple_async_result_set_error_va, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_error_va
#endif
g_simple_async_result_set_error_va:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1572 & 0xffff
#if 1572 > 0xffff
  movk ip0, 1572 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_from_error
  .p2align 4
  .type g_simple_async_result_set_from_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_from_error
#endif
g_simple_async_result_set_from_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1573 & 0xffff
#if 1573 > 0xffff
  movk ip0, 1573 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_handle_cancellation
  .p2align 4
  .type g_simple_async_result_set_handle_cancellation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_handle_cancellation
#endif
g_simple_async_result_set_handle_cancellation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1574 & 0xffff
#if 1574 > 0xffff
  movk ip0, 1574 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_op_res_gboolean
  .p2align 4
  .type g_simple_async_result_set_op_res_gboolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_op_res_gboolean
#endif
g_simple_async_result_set_op_res_gboolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1575 & 0xffff
#if 1575 > 0xffff
  movk ip0, 1575 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_op_res_gpointer
  .p2align 4
  .type g_simple_async_result_set_op_res_gpointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_op_res_gpointer
#endif
g_simple_async_result_set_op_res_gpointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1576 & 0xffff
#if 1576 > 0xffff
  movk ip0, 1576 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_set_op_res_gssize
  .p2align 4
  .type g_simple_async_result_set_op_res_gssize, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_set_op_res_gssize
#endif
g_simple_async_result_set_op_res_gssize:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1577 & 0xffff
#if 1577 > 0xffff
  movk ip0, 1577 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_async_result_take_error
  .p2align 4
  .type g_simple_async_result_take_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_async_result_take_error
#endif
g_simple_async_result_take_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1578 & 0xffff
#if 1578 > 0xffff
  movk ip0, 1578 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_io_stream_get_type
  .p2align 4
  .type g_simple_io_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_io_stream_get_type
#endif
g_simple_io_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1579 & 0xffff
#if 1579 > 0xffff
  movk ip0, 1579 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_io_stream_new
  .p2align 4
  .type g_simple_io_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_io_stream_new
#endif
g_simple_io_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1580 & 0xffff
#if 1580 > 0xffff
  movk ip0, 1580 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_permission_get_type
  .p2align 4
  .type g_simple_permission_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_permission_get_type
#endif
g_simple_permission_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1581 & 0xffff
#if 1581 > 0xffff
  movk ip0, 1581 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_permission_new
  .p2align 4
  .type g_simple_permission_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_permission_new
#endif
g_simple_permission_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1582 & 0xffff
#if 1582 > 0xffff
  movk ip0, 1582 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_proxy_resolver_get_type
  .p2align 4
  .type g_simple_proxy_resolver_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_proxy_resolver_get_type
#endif
g_simple_proxy_resolver_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1583 & 0xffff
#if 1583 > 0xffff
  movk ip0, 1583 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_proxy_resolver_new
  .p2align 4
  .type g_simple_proxy_resolver_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_proxy_resolver_new
#endif
g_simple_proxy_resolver_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1584 & 0xffff
#if 1584 > 0xffff
  movk ip0, 1584 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_proxy_resolver_set_default_proxy
  .p2align 4
  .type g_simple_proxy_resolver_set_default_proxy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_proxy_resolver_set_default_proxy
#endif
g_simple_proxy_resolver_set_default_proxy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1585 & 0xffff
#if 1585 > 0xffff
  movk ip0, 1585 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_proxy_resolver_set_ignore_hosts
  .p2align 4
  .type g_simple_proxy_resolver_set_ignore_hosts, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_proxy_resolver_set_ignore_hosts
#endif
g_simple_proxy_resolver_set_ignore_hosts:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1586 & 0xffff
#if 1586 > 0xffff
  movk ip0, 1586 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_simple_proxy_resolver_set_uri_proxy
  .p2align 4
  .type g_simple_proxy_resolver_set_uri_proxy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_simple_proxy_resolver_set_uri_proxy
#endif
g_simple_proxy_resolver_set_uri_proxy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1587 & 0xffff
#if 1587 > 0xffff
  movk ip0, 1587 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_accept
  .p2align 4
  .type g_socket_accept, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_accept
#endif
g_socket_accept:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1588 & 0xffff
#if 1588 > 0xffff
  movk ip0, 1588 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_enumerator_get_type
  .p2align 4
  .type g_socket_address_enumerator_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_enumerator_get_type
#endif
g_socket_address_enumerator_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1589 & 0xffff
#if 1589 > 0xffff
  movk ip0, 1589 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_enumerator_next
  .p2align 4
  .type g_socket_address_enumerator_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_enumerator_next
#endif
g_socket_address_enumerator_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1590 & 0xffff
#if 1590 > 0xffff
  movk ip0, 1590 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_enumerator_next_async
  .p2align 4
  .type g_socket_address_enumerator_next_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_enumerator_next_async
#endif
g_socket_address_enumerator_next_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1591 & 0xffff
#if 1591 > 0xffff
  movk ip0, 1591 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_enumerator_next_finish
  .p2align 4
  .type g_socket_address_enumerator_next_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_enumerator_next_finish
#endif
g_socket_address_enumerator_next_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1592 & 0xffff
#if 1592 > 0xffff
  movk ip0, 1592 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_get_family
  .p2align 4
  .type g_socket_address_get_family, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_get_family
#endif
g_socket_address_get_family:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1593 & 0xffff
#if 1593 > 0xffff
  movk ip0, 1593 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_get_native_size
  .p2align 4
  .type g_socket_address_get_native_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_get_native_size
#endif
g_socket_address_get_native_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1594 & 0xffff
#if 1594 > 0xffff
  movk ip0, 1594 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_get_type
  .p2align 4
  .type g_socket_address_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_get_type
#endif
g_socket_address_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1595 & 0xffff
#if 1595 > 0xffff
  movk ip0, 1595 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_new_from_native
  .p2align 4
  .type g_socket_address_new_from_native, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_new_from_native
#endif
g_socket_address_new_from_native:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1596 & 0xffff
#if 1596 > 0xffff
  movk ip0, 1596 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_address_to_native
  .p2align 4
  .type g_socket_address_to_native, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_address_to_native
#endif
g_socket_address_to_native:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1597 & 0xffff
#if 1597 > 0xffff
  movk ip0, 1597 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_bind
  .p2align 4
  .type g_socket_bind, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_bind
#endif
g_socket_bind:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1598 & 0xffff
#if 1598 > 0xffff
  movk ip0, 1598 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_check_connect_result
  .p2align 4
  .type g_socket_check_connect_result, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_check_connect_result
#endif
g_socket_check_connect_result:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1599 & 0xffff
#if 1599 > 0xffff
  movk ip0, 1599 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_add_application_proxy
  .p2align 4
  .type g_socket_client_add_application_proxy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_add_application_proxy
#endif
g_socket_client_add_application_proxy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1600 & 0xffff
#if 1600 > 0xffff
  movk ip0, 1600 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect
  .p2align 4
  .type g_socket_client_connect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect
#endif
g_socket_client_connect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1601 & 0xffff
#if 1601 > 0xffff
  movk ip0, 1601 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_async
  .p2align 4
  .type g_socket_client_connect_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_async
#endif
g_socket_client_connect_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1602 & 0xffff
#if 1602 > 0xffff
  movk ip0, 1602 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_finish
  .p2align 4
  .type g_socket_client_connect_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_finish
#endif
g_socket_client_connect_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1603 & 0xffff
#if 1603 > 0xffff
  movk ip0, 1603 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_host
  .p2align 4
  .type g_socket_client_connect_to_host, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_host
#endif
g_socket_client_connect_to_host:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1604 & 0xffff
#if 1604 > 0xffff
  movk ip0, 1604 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_host_async
  .p2align 4
  .type g_socket_client_connect_to_host_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_host_async
#endif
g_socket_client_connect_to_host_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1605 & 0xffff
#if 1605 > 0xffff
  movk ip0, 1605 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_host_finish
  .p2align 4
  .type g_socket_client_connect_to_host_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_host_finish
#endif
g_socket_client_connect_to_host_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1606 & 0xffff
#if 1606 > 0xffff
  movk ip0, 1606 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_service
  .p2align 4
  .type g_socket_client_connect_to_service, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_service
#endif
g_socket_client_connect_to_service:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1607 & 0xffff
#if 1607 > 0xffff
  movk ip0, 1607 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_service_async
  .p2align 4
  .type g_socket_client_connect_to_service_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_service_async
#endif
g_socket_client_connect_to_service_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1608 & 0xffff
#if 1608 > 0xffff
  movk ip0, 1608 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_service_finish
  .p2align 4
  .type g_socket_client_connect_to_service_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_service_finish
#endif
g_socket_client_connect_to_service_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1609 & 0xffff
#if 1609 > 0xffff
  movk ip0, 1609 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_uri
  .p2align 4
  .type g_socket_client_connect_to_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_uri
#endif
g_socket_client_connect_to_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1610 & 0xffff
#if 1610 > 0xffff
  movk ip0, 1610 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_uri_async
  .p2align 4
  .type g_socket_client_connect_to_uri_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_uri_async
#endif
g_socket_client_connect_to_uri_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1611 & 0xffff
#if 1611 > 0xffff
  movk ip0, 1611 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_connect_to_uri_finish
  .p2align 4
  .type g_socket_client_connect_to_uri_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_connect_to_uri_finish
#endif
g_socket_client_connect_to_uri_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1612 & 0xffff
#if 1612 > 0xffff
  movk ip0, 1612 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_event_get_type
  .p2align 4
  .type g_socket_client_event_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_event_get_type
#endif
g_socket_client_event_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1613 & 0xffff
#if 1613 > 0xffff
  movk ip0, 1613 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_enable_proxy
  .p2align 4
  .type g_socket_client_get_enable_proxy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_enable_proxy
#endif
g_socket_client_get_enable_proxy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1614 & 0xffff
#if 1614 > 0xffff
  movk ip0, 1614 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_family
  .p2align 4
  .type g_socket_client_get_family, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_family
#endif
g_socket_client_get_family:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1615 & 0xffff
#if 1615 > 0xffff
  movk ip0, 1615 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_local_address
  .p2align 4
  .type g_socket_client_get_local_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_local_address
#endif
g_socket_client_get_local_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1616 & 0xffff
#if 1616 > 0xffff
  movk ip0, 1616 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_protocol
  .p2align 4
  .type g_socket_client_get_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_protocol
#endif
g_socket_client_get_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1617 & 0xffff
#if 1617 > 0xffff
  movk ip0, 1617 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_proxy_resolver
  .p2align 4
  .type g_socket_client_get_proxy_resolver, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_proxy_resolver
#endif
g_socket_client_get_proxy_resolver:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1618 & 0xffff
#if 1618 > 0xffff
  movk ip0, 1618 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_socket_type
  .p2align 4
  .type g_socket_client_get_socket_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_socket_type
#endif
g_socket_client_get_socket_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1619 & 0xffff
#if 1619 > 0xffff
  movk ip0, 1619 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_timeout
  .p2align 4
  .type g_socket_client_get_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_timeout
#endif
g_socket_client_get_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1620 & 0xffff
#if 1620 > 0xffff
  movk ip0, 1620 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_tls
  .p2align 4
  .type g_socket_client_get_tls, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_tls
#endif
g_socket_client_get_tls:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1621 & 0xffff
#if 1621 > 0xffff
  movk ip0, 1621 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_tls_validation_flags
  .p2align 4
  .type g_socket_client_get_tls_validation_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_tls_validation_flags
#endif
g_socket_client_get_tls_validation_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1622 & 0xffff
#if 1622 > 0xffff
  movk ip0, 1622 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_get_type
  .p2align 4
  .type g_socket_client_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_get_type
#endif
g_socket_client_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1623 & 0xffff
#if 1623 > 0xffff
  movk ip0, 1623 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_new
  .p2align 4
  .type g_socket_client_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_new
#endif
g_socket_client_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+12992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+12992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1624 & 0xffff
#if 1624 > 0xffff
  movk ip0, 1624 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_enable_proxy
  .p2align 4
  .type g_socket_client_set_enable_proxy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_enable_proxy
#endif
g_socket_client_set_enable_proxy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1625 & 0xffff
#if 1625 > 0xffff
  movk ip0, 1625 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_family
  .p2align 4
  .type g_socket_client_set_family, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_family
#endif
g_socket_client_set_family:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1626 & 0xffff
#if 1626 > 0xffff
  movk ip0, 1626 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_local_address
  .p2align 4
  .type g_socket_client_set_local_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_local_address
#endif
g_socket_client_set_local_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1627 & 0xffff
#if 1627 > 0xffff
  movk ip0, 1627 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_protocol
  .p2align 4
  .type g_socket_client_set_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_protocol
#endif
g_socket_client_set_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1628 & 0xffff
#if 1628 > 0xffff
  movk ip0, 1628 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_proxy_resolver
  .p2align 4
  .type g_socket_client_set_proxy_resolver, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_proxy_resolver
#endif
g_socket_client_set_proxy_resolver:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1629 & 0xffff
#if 1629 > 0xffff
  movk ip0, 1629 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_socket_type
  .p2align 4
  .type g_socket_client_set_socket_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_socket_type
#endif
g_socket_client_set_socket_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1630 & 0xffff
#if 1630 > 0xffff
  movk ip0, 1630 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_timeout
  .p2align 4
  .type g_socket_client_set_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_timeout
#endif
g_socket_client_set_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1631 & 0xffff
#if 1631 > 0xffff
  movk ip0, 1631 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_tls
  .p2align 4
  .type g_socket_client_set_tls, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_tls
#endif
g_socket_client_set_tls:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1632 & 0xffff
#if 1632 > 0xffff
  movk ip0, 1632 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_client_set_tls_validation_flags
  .p2align 4
  .type g_socket_client_set_tls_validation_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_client_set_tls_validation_flags
#endif
g_socket_client_set_tls_validation_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1633 & 0xffff
#if 1633 > 0xffff
  movk ip0, 1633 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_close
  .p2align 4
  .type g_socket_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_close
#endif
g_socket_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1634 & 0xffff
#if 1634 > 0xffff
  movk ip0, 1634 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_condition_check
  .p2align 4
  .type g_socket_condition_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_condition_check
#endif
g_socket_condition_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1635 & 0xffff
#if 1635 > 0xffff
  movk ip0, 1635 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_condition_timed_wait
  .p2align 4
  .type g_socket_condition_timed_wait, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_condition_timed_wait
#endif
g_socket_condition_timed_wait:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1636 & 0xffff
#if 1636 > 0xffff
  movk ip0, 1636 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_condition_wait
  .p2align 4
  .type g_socket_condition_wait, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_condition_wait
#endif
g_socket_condition_wait:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1637 & 0xffff
#if 1637 > 0xffff
  movk ip0, 1637 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connect
  .p2align 4
  .type g_socket_connect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connect
#endif
g_socket_connect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1638 & 0xffff
#if 1638 > 0xffff
  movk ip0, 1638 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connectable_enumerate
  .p2align 4
  .type g_socket_connectable_enumerate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connectable_enumerate
#endif
g_socket_connectable_enumerate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1639 & 0xffff
#if 1639 > 0xffff
  movk ip0, 1639 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connectable_get_type
  .p2align 4
  .type g_socket_connectable_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connectable_get_type
#endif
g_socket_connectable_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1640 & 0xffff
#if 1640 > 0xffff
  movk ip0, 1640 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connectable_proxy_enumerate
  .p2align 4
  .type g_socket_connectable_proxy_enumerate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connectable_proxy_enumerate
#endif
g_socket_connectable_proxy_enumerate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1641 & 0xffff
#if 1641 > 0xffff
  movk ip0, 1641 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connectable_to_string
  .p2align 4
  .type g_socket_connectable_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connectable_to_string
#endif
g_socket_connectable_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1642 & 0xffff
#if 1642 > 0xffff
  movk ip0, 1642 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_connect
  .p2align 4
  .type g_socket_connection_connect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_connect
#endif
g_socket_connection_connect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1643 & 0xffff
#if 1643 > 0xffff
  movk ip0, 1643 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_connect_async
  .p2align 4
  .type g_socket_connection_connect_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_connect_async
#endif
g_socket_connection_connect_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1644 & 0xffff
#if 1644 > 0xffff
  movk ip0, 1644 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_connect_finish
  .p2align 4
  .type g_socket_connection_connect_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_connect_finish
#endif
g_socket_connection_connect_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1645 & 0xffff
#if 1645 > 0xffff
  movk ip0, 1645 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_factory_create_connection
  .p2align 4
  .type g_socket_connection_factory_create_connection, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_factory_create_connection
#endif
g_socket_connection_factory_create_connection:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1646 & 0xffff
#if 1646 > 0xffff
  movk ip0, 1646 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_factory_lookup_type
  .p2align 4
  .type g_socket_connection_factory_lookup_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_factory_lookup_type
#endif
g_socket_connection_factory_lookup_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1647 & 0xffff
#if 1647 > 0xffff
  movk ip0, 1647 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_factory_register_type
  .p2align 4
  .type g_socket_connection_factory_register_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_factory_register_type
#endif
g_socket_connection_factory_register_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1648 & 0xffff
#if 1648 > 0xffff
  movk ip0, 1648 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_get_local_address
  .p2align 4
  .type g_socket_connection_get_local_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_get_local_address
#endif
g_socket_connection_get_local_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1649 & 0xffff
#if 1649 > 0xffff
  movk ip0, 1649 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_get_remote_address
  .p2align 4
  .type g_socket_connection_get_remote_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_get_remote_address
#endif
g_socket_connection_get_remote_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1650 & 0xffff
#if 1650 > 0xffff
  movk ip0, 1650 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_get_socket
  .p2align 4
  .type g_socket_connection_get_socket, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_get_socket
#endif
g_socket_connection_get_socket:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1651 & 0xffff
#if 1651 > 0xffff
  movk ip0, 1651 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_get_type
  .p2align 4
  .type g_socket_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_get_type
#endif
g_socket_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1652 & 0xffff
#if 1652 > 0xffff
  movk ip0, 1652 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_connection_is_connected
  .p2align 4
  .type g_socket_connection_is_connected, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_connection_is_connected
#endif
g_socket_connection_is_connected:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1653 & 0xffff
#if 1653 > 0xffff
  movk ip0, 1653 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_control_message_deserialize
  .p2align 4
  .type g_socket_control_message_deserialize, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_control_message_deserialize
#endif
g_socket_control_message_deserialize:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1654 & 0xffff
#if 1654 > 0xffff
  movk ip0, 1654 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_control_message_get_level
  .p2align 4
  .type g_socket_control_message_get_level, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_control_message_get_level
#endif
g_socket_control_message_get_level:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1655 & 0xffff
#if 1655 > 0xffff
  movk ip0, 1655 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_control_message_get_msg_type
  .p2align 4
  .type g_socket_control_message_get_msg_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_control_message_get_msg_type
#endif
g_socket_control_message_get_msg_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1656 & 0xffff
#if 1656 > 0xffff
  movk ip0, 1656 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_control_message_get_size
  .p2align 4
  .type g_socket_control_message_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_control_message_get_size
#endif
g_socket_control_message_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1657 & 0xffff
#if 1657 > 0xffff
  movk ip0, 1657 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_control_message_get_type
  .p2align 4
  .type g_socket_control_message_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_control_message_get_type
#endif
g_socket_control_message_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1658 & 0xffff
#if 1658 > 0xffff
  movk ip0, 1658 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_control_message_serialize
  .p2align 4
  .type g_socket_control_message_serialize, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_control_message_serialize
#endif
g_socket_control_message_serialize:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1659 & 0xffff
#if 1659 > 0xffff
  movk ip0, 1659 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_create_source
  .p2align 4
  .type g_socket_create_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_create_source
#endif
g_socket_create_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1660 & 0xffff
#if 1660 > 0xffff
  movk ip0, 1660 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_family_get_type
  .p2align 4
  .type g_socket_family_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_family_get_type
#endif
g_socket_family_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1661 & 0xffff
#if 1661 > 0xffff
  movk ip0, 1661 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_available_bytes
  .p2align 4
  .type g_socket_get_available_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_available_bytes
#endif
g_socket_get_available_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1662 & 0xffff
#if 1662 > 0xffff
  movk ip0, 1662 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_blocking
  .p2align 4
  .type g_socket_get_blocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_blocking
#endif
g_socket_get_blocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1663 & 0xffff
#if 1663 > 0xffff
  movk ip0, 1663 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_broadcast
  .p2align 4
  .type g_socket_get_broadcast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_broadcast
#endif
g_socket_get_broadcast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1664 & 0xffff
#if 1664 > 0xffff
  movk ip0, 1664 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_credentials
  .p2align 4
  .type g_socket_get_credentials, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_credentials
#endif
g_socket_get_credentials:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1665 & 0xffff
#if 1665 > 0xffff
  movk ip0, 1665 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_family
  .p2align 4
  .type g_socket_get_family, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_family
#endif
g_socket_get_family:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1666 & 0xffff
#if 1666 > 0xffff
  movk ip0, 1666 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_fd
  .p2align 4
  .type g_socket_get_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_fd
#endif
g_socket_get_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1667 & 0xffff
#if 1667 > 0xffff
  movk ip0, 1667 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_keepalive
  .p2align 4
  .type g_socket_get_keepalive, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_keepalive
#endif
g_socket_get_keepalive:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1668 & 0xffff
#if 1668 > 0xffff
  movk ip0, 1668 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_listen_backlog
  .p2align 4
  .type g_socket_get_listen_backlog, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_listen_backlog
#endif
g_socket_get_listen_backlog:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1669 & 0xffff
#if 1669 > 0xffff
  movk ip0, 1669 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_local_address
  .p2align 4
  .type g_socket_get_local_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_local_address
#endif
g_socket_get_local_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1670 & 0xffff
#if 1670 > 0xffff
  movk ip0, 1670 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_multicast_loopback
  .p2align 4
  .type g_socket_get_multicast_loopback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_multicast_loopback
#endif
g_socket_get_multicast_loopback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1671 & 0xffff
#if 1671 > 0xffff
  movk ip0, 1671 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_multicast_ttl
  .p2align 4
  .type g_socket_get_multicast_ttl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_multicast_ttl
#endif
g_socket_get_multicast_ttl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1672 & 0xffff
#if 1672 > 0xffff
  movk ip0, 1672 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_option
  .p2align 4
  .type g_socket_get_option, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_option
#endif
g_socket_get_option:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1673 & 0xffff
#if 1673 > 0xffff
  movk ip0, 1673 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_protocol
  .p2align 4
  .type g_socket_get_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_protocol
#endif
g_socket_get_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1674 & 0xffff
#if 1674 > 0xffff
  movk ip0, 1674 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_remote_address
  .p2align 4
  .type g_socket_get_remote_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_remote_address
#endif
g_socket_get_remote_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1675 & 0xffff
#if 1675 > 0xffff
  movk ip0, 1675 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_socket_type
  .p2align 4
  .type g_socket_get_socket_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_socket_type
#endif
g_socket_get_socket_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1676 & 0xffff
#if 1676 > 0xffff
  movk ip0, 1676 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_timeout
  .p2align 4
  .type g_socket_get_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_timeout
#endif
g_socket_get_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1677 & 0xffff
#if 1677 > 0xffff
  movk ip0, 1677 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_ttl
  .p2align 4
  .type g_socket_get_ttl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_ttl
#endif
g_socket_get_ttl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1678 & 0xffff
#if 1678 > 0xffff
  movk ip0, 1678 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_get_type
  .p2align 4
  .type g_socket_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_get_type
#endif
g_socket_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1679 & 0xffff
#if 1679 > 0xffff
  movk ip0, 1679 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_is_closed
  .p2align 4
  .type g_socket_is_closed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_is_closed
#endif
g_socket_is_closed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1680 & 0xffff
#if 1680 > 0xffff
  movk ip0, 1680 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_is_connected
  .p2align 4
  .type g_socket_is_connected, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_is_connected
#endif
g_socket_is_connected:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1681 & 0xffff
#if 1681 > 0xffff
  movk ip0, 1681 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_join_multicast_group
  .p2align 4
  .type g_socket_join_multicast_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_join_multicast_group
#endif
g_socket_join_multicast_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1682 & 0xffff
#if 1682 > 0xffff
  movk ip0, 1682 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_join_multicast_group_ssm
  .p2align 4
  .type g_socket_join_multicast_group_ssm, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_join_multicast_group_ssm
#endif
g_socket_join_multicast_group_ssm:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1683 & 0xffff
#if 1683 > 0xffff
  movk ip0, 1683 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_leave_multicast_group
  .p2align 4
  .type g_socket_leave_multicast_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_leave_multicast_group
#endif
g_socket_leave_multicast_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1684 & 0xffff
#if 1684 > 0xffff
  movk ip0, 1684 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_leave_multicast_group_ssm
  .p2align 4
  .type g_socket_leave_multicast_group_ssm, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_leave_multicast_group_ssm
#endif
g_socket_leave_multicast_group_ssm:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1685 & 0xffff
#if 1685 > 0xffff
  movk ip0, 1685 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listen
  .p2align 4
  .type g_socket_listen, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listen
#endif
g_socket_listen:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1686 & 0xffff
#if 1686 > 0xffff
  movk ip0, 1686 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_accept
  .p2align 4
  .type g_socket_listener_accept, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_accept
#endif
g_socket_listener_accept:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1687 & 0xffff
#if 1687 > 0xffff
  movk ip0, 1687 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_accept_async
  .p2align 4
  .type g_socket_listener_accept_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_accept_async
#endif
g_socket_listener_accept_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1688 & 0xffff
#if 1688 > 0xffff
  movk ip0, 1688 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_accept_finish
  .p2align 4
  .type g_socket_listener_accept_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_accept_finish
#endif
g_socket_listener_accept_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1689 & 0xffff
#if 1689 > 0xffff
  movk ip0, 1689 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_accept_socket
  .p2align 4
  .type g_socket_listener_accept_socket, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_accept_socket
#endif
g_socket_listener_accept_socket:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1690 & 0xffff
#if 1690 > 0xffff
  movk ip0, 1690 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_accept_socket_async
  .p2align 4
  .type g_socket_listener_accept_socket_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_accept_socket_async
#endif
g_socket_listener_accept_socket_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1691 & 0xffff
#if 1691 > 0xffff
  movk ip0, 1691 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_accept_socket_finish
  .p2align 4
  .type g_socket_listener_accept_socket_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_accept_socket_finish
#endif
g_socket_listener_accept_socket_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1692 & 0xffff
#if 1692 > 0xffff
  movk ip0, 1692 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_add_address
  .p2align 4
  .type g_socket_listener_add_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_add_address
#endif
g_socket_listener_add_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1693 & 0xffff
#if 1693 > 0xffff
  movk ip0, 1693 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_add_any_inet_port
  .p2align 4
  .type g_socket_listener_add_any_inet_port, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_add_any_inet_port
#endif
g_socket_listener_add_any_inet_port:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1694 & 0xffff
#if 1694 > 0xffff
  movk ip0, 1694 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_add_inet_port
  .p2align 4
  .type g_socket_listener_add_inet_port, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_add_inet_port
#endif
g_socket_listener_add_inet_port:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1695 & 0xffff
#if 1695 > 0xffff
  movk ip0, 1695 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_add_socket
  .p2align 4
  .type g_socket_listener_add_socket, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_add_socket
#endif
g_socket_listener_add_socket:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1696 & 0xffff
#if 1696 > 0xffff
  movk ip0, 1696 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_close
  .p2align 4
  .type g_socket_listener_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_close
#endif
g_socket_listener_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1697 & 0xffff
#if 1697 > 0xffff
  movk ip0, 1697 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_event_get_type
  .p2align 4
  .type g_socket_listener_event_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_event_get_type
#endif
g_socket_listener_event_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1698 & 0xffff
#if 1698 > 0xffff
  movk ip0, 1698 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_get_type
  .p2align 4
  .type g_socket_listener_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_get_type
#endif
g_socket_listener_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1699 & 0xffff
#if 1699 > 0xffff
  movk ip0, 1699 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_new
  .p2align 4
  .type g_socket_listener_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_new
#endif
g_socket_listener_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1700 & 0xffff
#if 1700 > 0xffff
  movk ip0, 1700 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_listener_set_backlog
  .p2align 4
  .type g_socket_listener_set_backlog, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_listener_set_backlog
#endif
g_socket_listener_set_backlog:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1701 & 0xffff
#if 1701 > 0xffff
  movk ip0, 1701 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_msg_flags_get_type
  .p2align 4
  .type g_socket_msg_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_msg_flags_get_type
#endif
g_socket_msg_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1702 & 0xffff
#if 1702 > 0xffff
  movk ip0, 1702 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_new
  .p2align 4
  .type g_socket_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_new
#endif
g_socket_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1703 & 0xffff
#if 1703 > 0xffff
  movk ip0, 1703 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_new_from_fd
  .p2align 4
  .type g_socket_new_from_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_new_from_fd
#endif
g_socket_new_from_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1704 & 0xffff
#if 1704 > 0xffff
  movk ip0, 1704 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_protocol_get_type
  .p2align 4
  .type g_socket_protocol_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_protocol_get_type
#endif
g_socket_protocol_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1705 & 0xffff
#if 1705 > 0xffff
  movk ip0, 1705 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_receive
  .p2align 4
  .type g_socket_receive, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_receive
#endif
g_socket_receive:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1706 & 0xffff
#if 1706 > 0xffff
  movk ip0, 1706 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_receive_bytes
  .p2align 4
  .type g_socket_receive_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_receive_bytes
#endif
g_socket_receive_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1707 & 0xffff
#if 1707 > 0xffff
  movk ip0, 1707 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_receive_bytes_from
  .p2align 4
  .type g_socket_receive_bytes_from, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_receive_bytes_from
#endif
g_socket_receive_bytes_from:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1708 & 0xffff
#if 1708 > 0xffff
  movk ip0, 1708 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_receive_from
  .p2align 4
  .type g_socket_receive_from, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_receive_from
#endif
g_socket_receive_from:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1709 & 0xffff
#if 1709 > 0xffff
  movk ip0, 1709 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_receive_message
  .p2align 4
  .type g_socket_receive_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_receive_message
#endif
g_socket_receive_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1710 & 0xffff
#if 1710 > 0xffff
  movk ip0, 1710 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_receive_messages
  .p2align 4
  .type g_socket_receive_messages, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_receive_messages
#endif
g_socket_receive_messages:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1711 & 0xffff
#if 1711 > 0xffff
  movk ip0, 1711 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_receive_with_blocking
  .p2align 4
  .type g_socket_receive_with_blocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_receive_with_blocking
#endif
g_socket_receive_with_blocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1712 & 0xffff
#if 1712 > 0xffff
  movk ip0, 1712 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_send
  .p2align 4
  .type g_socket_send, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_send
#endif
g_socket_send:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1713 & 0xffff
#if 1713 > 0xffff
  movk ip0, 1713 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_send_message
  .p2align 4
  .type g_socket_send_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_send_message
#endif
g_socket_send_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1714 & 0xffff
#if 1714 > 0xffff
  movk ip0, 1714 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_send_message_with_timeout
  .p2align 4
  .type g_socket_send_message_with_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_send_message_with_timeout
#endif
g_socket_send_message_with_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1715 & 0xffff
#if 1715 > 0xffff
  movk ip0, 1715 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_send_messages
  .p2align 4
  .type g_socket_send_messages, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_send_messages
#endif
g_socket_send_messages:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1716 & 0xffff
#if 1716 > 0xffff
  movk ip0, 1716 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_send_to
  .p2align 4
  .type g_socket_send_to, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_send_to
#endif
g_socket_send_to:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1717 & 0xffff
#if 1717 > 0xffff
  movk ip0, 1717 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_send_with_blocking
  .p2align 4
  .type g_socket_send_with_blocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_send_with_blocking
#endif
g_socket_send_with_blocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1718 & 0xffff
#if 1718 > 0xffff
  movk ip0, 1718 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_service_get_type
  .p2align 4
  .type g_socket_service_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_service_get_type
#endif
g_socket_service_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1719 & 0xffff
#if 1719 > 0xffff
  movk ip0, 1719 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_service_is_active
  .p2align 4
  .type g_socket_service_is_active, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_service_is_active
#endif
g_socket_service_is_active:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1720 & 0xffff
#if 1720 > 0xffff
  movk ip0, 1720 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_service_new
  .p2align 4
  .type g_socket_service_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_service_new
#endif
g_socket_service_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1721 & 0xffff
#if 1721 > 0xffff
  movk ip0, 1721 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_service_start
  .p2align 4
  .type g_socket_service_start, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_service_start
#endif
g_socket_service_start:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1722 & 0xffff
#if 1722 > 0xffff
  movk ip0, 1722 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_service_stop
  .p2align 4
  .type g_socket_service_stop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_service_stop
#endif
g_socket_service_stop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1723 & 0xffff
#if 1723 > 0xffff
  movk ip0, 1723 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_blocking
  .p2align 4
  .type g_socket_set_blocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_blocking
#endif
g_socket_set_blocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1724 & 0xffff
#if 1724 > 0xffff
  movk ip0, 1724 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_broadcast
  .p2align 4
  .type g_socket_set_broadcast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_broadcast
#endif
g_socket_set_broadcast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1725 & 0xffff
#if 1725 > 0xffff
  movk ip0, 1725 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_keepalive
  .p2align 4
  .type g_socket_set_keepalive, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_keepalive
#endif
g_socket_set_keepalive:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1726 & 0xffff
#if 1726 > 0xffff
  movk ip0, 1726 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_listen_backlog
  .p2align 4
  .type g_socket_set_listen_backlog, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_listen_backlog
#endif
g_socket_set_listen_backlog:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1727 & 0xffff
#if 1727 > 0xffff
  movk ip0, 1727 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_multicast_loopback
  .p2align 4
  .type g_socket_set_multicast_loopback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_multicast_loopback
#endif
g_socket_set_multicast_loopback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1728 & 0xffff
#if 1728 > 0xffff
  movk ip0, 1728 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_multicast_ttl
  .p2align 4
  .type g_socket_set_multicast_ttl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_multicast_ttl
#endif
g_socket_set_multicast_ttl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1729 & 0xffff
#if 1729 > 0xffff
  movk ip0, 1729 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_option
  .p2align 4
  .type g_socket_set_option, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_option
#endif
g_socket_set_option:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1730 & 0xffff
#if 1730 > 0xffff
  movk ip0, 1730 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_timeout
  .p2align 4
  .type g_socket_set_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_timeout
#endif
g_socket_set_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1731 & 0xffff
#if 1731 > 0xffff
  movk ip0, 1731 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_set_ttl
  .p2align 4
  .type g_socket_set_ttl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_set_ttl
#endif
g_socket_set_ttl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1732 & 0xffff
#if 1732 > 0xffff
  movk ip0, 1732 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_shutdown
  .p2align 4
  .type g_socket_shutdown, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_shutdown
#endif
g_socket_shutdown:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1733 & 0xffff
#if 1733 > 0xffff
  movk ip0, 1733 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_speaks_ipv4
  .p2align 4
  .type g_socket_speaks_ipv4, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_speaks_ipv4
#endif
g_socket_speaks_ipv4:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1734 & 0xffff
#if 1734 > 0xffff
  movk ip0, 1734 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_socket_type_get_type
  .p2align 4
  .type g_socket_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_socket_type_get_type
#endif
g_socket_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1735 & 0xffff
#if 1735 > 0xffff
  movk ip0, 1735 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_copy
  .p2align 4
  .type g_srv_target_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_copy
#endif
g_srv_target_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1736 & 0xffff
#if 1736 > 0xffff
  movk ip0, 1736 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_free
  .p2align 4
  .type g_srv_target_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_free
#endif
g_srv_target_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1737 & 0xffff
#if 1737 > 0xffff
  movk ip0, 1737 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_get_hostname
  .p2align 4
  .type g_srv_target_get_hostname, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_get_hostname
#endif
g_srv_target_get_hostname:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1738 & 0xffff
#if 1738 > 0xffff
  movk ip0, 1738 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_get_port
  .p2align 4
  .type g_srv_target_get_port, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_get_port
#endif
g_srv_target_get_port:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1739 & 0xffff
#if 1739 > 0xffff
  movk ip0, 1739 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_get_priority
  .p2align 4
  .type g_srv_target_get_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_get_priority
#endif
g_srv_target_get_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1740 & 0xffff
#if 1740 > 0xffff
  movk ip0, 1740 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_get_type
  .p2align 4
  .type g_srv_target_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_get_type
#endif
g_srv_target_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1741 & 0xffff
#if 1741 > 0xffff
  movk ip0, 1741 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_get_weight
  .p2align 4
  .type g_srv_target_get_weight, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_get_weight
#endif
g_srv_target_get_weight:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1742 & 0xffff
#if 1742 > 0xffff
  movk ip0, 1742 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_list_sort
  .p2align 4
  .type g_srv_target_list_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_list_sort
#endif
g_srv_target_list_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1743 & 0xffff
#if 1743 > 0xffff
  movk ip0, 1743 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_srv_target_new
  .p2align 4
  .type g_srv_target_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_srv_target_new
#endif
g_srv_target_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1744 & 0xffff
#if 1744 > 0xffff
  movk ip0, 1744 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_resource_fini
  .p2align 4
  .type g_static_resource_fini, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_resource_fini
#endif
g_static_resource_fini:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1745 & 0xffff
#if 1745 > 0xffff
  movk ip0, 1745 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_resource_get_resource
  .p2align 4
  .type g_static_resource_get_resource, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_resource_get_resource
#endif
g_static_resource_get_resource:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1746 & 0xffff
#if 1746 > 0xffff
  movk ip0, 1746 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_resource_init
  .p2align 4
  .type g_static_resource_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_resource_init
#endif
g_static_resource_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1747 & 0xffff
#if 1747 > 0xffff
  movk ip0, 1747 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_communicate
  .p2align 4
  .type g_subprocess_communicate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_communicate
#endif
g_subprocess_communicate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1748 & 0xffff
#if 1748 > 0xffff
  movk ip0, 1748 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_communicate_async
  .p2align 4
  .type g_subprocess_communicate_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_communicate_async
#endif
g_subprocess_communicate_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+13992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+13992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1749 & 0xffff
#if 1749 > 0xffff
  movk ip0, 1749 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_communicate_finish
  .p2align 4
  .type g_subprocess_communicate_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_communicate_finish
#endif
g_subprocess_communicate_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1750 & 0xffff
#if 1750 > 0xffff
  movk ip0, 1750 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_communicate_utf8
  .p2align 4
  .type g_subprocess_communicate_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_communicate_utf8
#endif
g_subprocess_communicate_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1751 & 0xffff
#if 1751 > 0xffff
  movk ip0, 1751 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_communicate_utf8_async
  .p2align 4
  .type g_subprocess_communicate_utf8_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_communicate_utf8_async
#endif
g_subprocess_communicate_utf8_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1752 & 0xffff
#if 1752 > 0xffff
  movk ip0, 1752 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_communicate_utf8_finish
  .p2align 4
  .type g_subprocess_communicate_utf8_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_communicate_utf8_finish
#endif
g_subprocess_communicate_utf8_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1753 & 0xffff
#if 1753 > 0xffff
  movk ip0, 1753 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_flags_get_type
  .p2align 4
  .type g_subprocess_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_flags_get_type
#endif
g_subprocess_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1754 & 0xffff
#if 1754 > 0xffff
  movk ip0, 1754 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_force_exit
  .p2align 4
  .type g_subprocess_force_exit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_force_exit
#endif
g_subprocess_force_exit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1755 & 0xffff
#if 1755 > 0xffff
  movk ip0, 1755 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_exit_status
  .p2align 4
  .type g_subprocess_get_exit_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_exit_status
#endif
g_subprocess_get_exit_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1756 & 0xffff
#if 1756 > 0xffff
  movk ip0, 1756 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_identifier
  .p2align 4
  .type g_subprocess_get_identifier, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_identifier
#endif
g_subprocess_get_identifier:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1757 & 0xffff
#if 1757 > 0xffff
  movk ip0, 1757 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_if_exited
  .p2align 4
  .type g_subprocess_get_if_exited, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_if_exited
#endif
g_subprocess_get_if_exited:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1758 & 0xffff
#if 1758 > 0xffff
  movk ip0, 1758 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_if_signaled
  .p2align 4
  .type g_subprocess_get_if_signaled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_if_signaled
#endif
g_subprocess_get_if_signaled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1759 & 0xffff
#if 1759 > 0xffff
  movk ip0, 1759 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_status
  .p2align 4
  .type g_subprocess_get_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_status
#endif
g_subprocess_get_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1760 & 0xffff
#if 1760 > 0xffff
  movk ip0, 1760 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_stderr_pipe
  .p2align 4
  .type g_subprocess_get_stderr_pipe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_stderr_pipe
#endif
g_subprocess_get_stderr_pipe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1761 & 0xffff
#if 1761 > 0xffff
  movk ip0, 1761 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_stdin_pipe
  .p2align 4
  .type g_subprocess_get_stdin_pipe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_stdin_pipe
#endif
g_subprocess_get_stdin_pipe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1762 & 0xffff
#if 1762 > 0xffff
  movk ip0, 1762 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_stdout_pipe
  .p2align 4
  .type g_subprocess_get_stdout_pipe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_stdout_pipe
#endif
g_subprocess_get_stdout_pipe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1763 & 0xffff
#if 1763 > 0xffff
  movk ip0, 1763 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_successful
  .p2align 4
  .type g_subprocess_get_successful, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_successful
#endif
g_subprocess_get_successful:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1764 & 0xffff
#if 1764 > 0xffff
  movk ip0, 1764 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_term_sig
  .p2align 4
  .type g_subprocess_get_term_sig, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_term_sig
#endif
g_subprocess_get_term_sig:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1765 & 0xffff
#if 1765 > 0xffff
  movk ip0, 1765 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_get_type
  .p2align 4
  .type g_subprocess_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_get_type
#endif
g_subprocess_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1766 & 0xffff
#if 1766 > 0xffff
  movk ip0, 1766 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_close
  .p2align 4
  .type g_subprocess_launcher_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_close
#endif
g_subprocess_launcher_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1767 & 0xffff
#if 1767 > 0xffff
  movk ip0, 1767 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_get_type
  .p2align 4
  .type g_subprocess_launcher_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_get_type
#endif
g_subprocess_launcher_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1768 & 0xffff
#if 1768 > 0xffff
  movk ip0, 1768 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_getenv
  .p2align 4
  .type g_subprocess_launcher_getenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_getenv
#endif
g_subprocess_launcher_getenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1769 & 0xffff
#if 1769 > 0xffff
  movk ip0, 1769 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_new
  .p2align 4
  .type g_subprocess_launcher_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_new
#endif
g_subprocess_launcher_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1770 & 0xffff
#if 1770 > 0xffff
  movk ip0, 1770 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_set_child_setup
  .p2align 4
  .type g_subprocess_launcher_set_child_setup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_set_child_setup
#endif
g_subprocess_launcher_set_child_setup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1771 & 0xffff
#if 1771 > 0xffff
  movk ip0, 1771 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_set_cwd
  .p2align 4
  .type g_subprocess_launcher_set_cwd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_set_cwd
#endif
g_subprocess_launcher_set_cwd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1772 & 0xffff
#if 1772 > 0xffff
  movk ip0, 1772 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_set_environ
  .p2align 4
  .type g_subprocess_launcher_set_environ, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_set_environ
#endif
g_subprocess_launcher_set_environ:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1773 & 0xffff
#if 1773 > 0xffff
  movk ip0, 1773 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_set_flags
  .p2align 4
  .type g_subprocess_launcher_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_set_flags
#endif
g_subprocess_launcher_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1774 & 0xffff
#if 1774 > 0xffff
  movk ip0, 1774 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_set_stderr_file_path
  .p2align 4
  .type g_subprocess_launcher_set_stderr_file_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_set_stderr_file_path
#endif
g_subprocess_launcher_set_stderr_file_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1775 & 0xffff
#if 1775 > 0xffff
  movk ip0, 1775 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_set_stdin_file_path
  .p2align 4
  .type g_subprocess_launcher_set_stdin_file_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_set_stdin_file_path
#endif
g_subprocess_launcher_set_stdin_file_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1776 & 0xffff
#if 1776 > 0xffff
  movk ip0, 1776 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_set_stdout_file_path
  .p2align 4
  .type g_subprocess_launcher_set_stdout_file_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_set_stdout_file_path
#endif
g_subprocess_launcher_set_stdout_file_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1777 & 0xffff
#if 1777 > 0xffff
  movk ip0, 1777 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_setenv
  .p2align 4
  .type g_subprocess_launcher_setenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_setenv
#endif
g_subprocess_launcher_setenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1778 & 0xffff
#if 1778 > 0xffff
  movk ip0, 1778 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_spawn
  .p2align 4
  .type g_subprocess_launcher_spawn, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_spawn
#endif
g_subprocess_launcher_spawn:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1779 & 0xffff
#if 1779 > 0xffff
  movk ip0, 1779 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_spawnv
  .p2align 4
  .type g_subprocess_launcher_spawnv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_spawnv
#endif
g_subprocess_launcher_spawnv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1780 & 0xffff
#if 1780 > 0xffff
  movk ip0, 1780 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_take_fd
  .p2align 4
  .type g_subprocess_launcher_take_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_take_fd
#endif
g_subprocess_launcher_take_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1781 & 0xffff
#if 1781 > 0xffff
  movk ip0, 1781 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_take_stderr_fd
  .p2align 4
  .type g_subprocess_launcher_take_stderr_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_take_stderr_fd
#endif
g_subprocess_launcher_take_stderr_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1782 & 0xffff
#if 1782 > 0xffff
  movk ip0, 1782 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_take_stdin_fd
  .p2align 4
  .type g_subprocess_launcher_take_stdin_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_take_stdin_fd
#endif
g_subprocess_launcher_take_stdin_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1783 & 0xffff
#if 1783 > 0xffff
  movk ip0, 1783 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_take_stdout_fd
  .p2align 4
  .type g_subprocess_launcher_take_stdout_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_take_stdout_fd
#endif
g_subprocess_launcher_take_stdout_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1784 & 0xffff
#if 1784 > 0xffff
  movk ip0, 1784 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_launcher_unsetenv
  .p2align 4
  .type g_subprocess_launcher_unsetenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_launcher_unsetenv
#endif
g_subprocess_launcher_unsetenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1785 & 0xffff
#if 1785 > 0xffff
  movk ip0, 1785 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_new
  .p2align 4
  .type g_subprocess_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_new
#endif
g_subprocess_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1786 & 0xffff
#if 1786 > 0xffff
  movk ip0, 1786 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_newv
  .p2align 4
  .type g_subprocess_newv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_newv
#endif
g_subprocess_newv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1787 & 0xffff
#if 1787 > 0xffff
  movk ip0, 1787 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_send_signal
  .p2align 4
  .type g_subprocess_send_signal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_send_signal
#endif
g_subprocess_send_signal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1788 & 0xffff
#if 1788 > 0xffff
  movk ip0, 1788 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_wait
  .p2align 4
  .type g_subprocess_wait, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_wait
#endif
g_subprocess_wait:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1789 & 0xffff
#if 1789 > 0xffff
  movk ip0, 1789 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_wait_async
  .p2align 4
  .type g_subprocess_wait_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_wait_async
#endif
g_subprocess_wait_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1790 & 0xffff
#if 1790 > 0xffff
  movk ip0, 1790 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_wait_check
  .p2align 4
  .type g_subprocess_wait_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_wait_check
#endif
g_subprocess_wait_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1791 & 0xffff
#if 1791 > 0xffff
  movk ip0, 1791 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_wait_check_async
  .p2align 4
  .type g_subprocess_wait_check_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_wait_check_async
#endif
g_subprocess_wait_check_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1792 & 0xffff
#if 1792 > 0xffff
  movk ip0, 1792 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_wait_check_finish
  .p2align 4
  .type g_subprocess_wait_check_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_wait_check_finish
#endif
g_subprocess_wait_check_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1793 & 0xffff
#if 1793 > 0xffff
  movk ip0, 1793 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_subprocess_wait_finish
  .p2align 4
  .type g_subprocess_wait_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_subprocess_wait_finish
#endif
g_subprocess_wait_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1794 & 0xffff
#if 1794 > 0xffff
  movk ip0, 1794 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_attach_source
  .p2align 4
  .type g_task_attach_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_attach_source
#endif
g_task_attach_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1795 & 0xffff
#if 1795 > 0xffff
  movk ip0, 1795 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_cancellable
  .p2align 4
  .type g_task_get_cancellable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_cancellable
#endif
g_task_get_cancellable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1796 & 0xffff
#if 1796 > 0xffff
  movk ip0, 1796 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_check_cancellable
  .p2align 4
  .type g_task_get_check_cancellable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_check_cancellable
#endif
g_task_get_check_cancellable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1797 & 0xffff
#if 1797 > 0xffff
  movk ip0, 1797 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_completed
  .p2align 4
  .type g_task_get_completed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_completed
#endif
g_task_get_completed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1798 & 0xffff
#if 1798 > 0xffff
  movk ip0, 1798 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_context
  .p2align 4
  .type g_task_get_context, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_context
#endif
g_task_get_context:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1799 & 0xffff
#if 1799 > 0xffff
  movk ip0, 1799 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_name
  .p2align 4
  .type g_task_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_name
#endif
g_task_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1800 & 0xffff
#if 1800 > 0xffff
  movk ip0, 1800 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_priority
  .p2align 4
  .type g_task_get_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_priority
#endif
g_task_get_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1801 & 0xffff
#if 1801 > 0xffff
  movk ip0, 1801 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_return_on_cancel
  .p2align 4
  .type g_task_get_return_on_cancel, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_return_on_cancel
#endif
g_task_get_return_on_cancel:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1802 & 0xffff
#if 1802 > 0xffff
  movk ip0, 1802 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_source_object
  .p2align 4
  .type g_task_get_source_object, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_source_object
#endif
g_task_get_source_object:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1803 & 0xffff
#if 1803 > 0xffff
  movk ip0, 1803 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_source_tag
  .p2align 4
  .type g_task_get_source_tag, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_source_tag
#endif
g_task_get_source_tag:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1804 & 0xffff
#if 1804 > 0xffff
  movk ip0, 1804 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_task_data
  .p2align 4
  .type g_task_get_task_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_task_data
#endif
g_task_get_task_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1805 & 0xffff
#if 1805 > 0xffff
  movk ip0, 1805 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_get_type
  .p2align 4
  .type g_task_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_get_type
#endif
g_task_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1806 & 0xffff
#if 1806 > 0xffff
  movk ip0, 1806 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_had_error
  .p2align 4
  .type g_task_had_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_had_error
#endif
g_task_had_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1807 & 0xffff
#if 1807 > 0xffff
  movk ip0, 1807 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_is_valid
  .p2align 4
  .type g_task_is_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_is_valid
#endif
g_task_is_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1808 & 0xffff
#if 1808 > 0xffff
  movk ip0, 1808 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_new
  .p2align 4
  .type g_task_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_new
#endif
g_task_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1809 & 0xffff
#if 1809 > 0xffff
  movk ip0, 1809 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_propagate_boolean
  .p2align 4
  .type g_task_propagate_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_propagate_boolean
#endif
g_task_propagate_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1810 & 0xffff
#if 1810 > 0xffff
  movk ip0, 1810 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_propagate_int
  .p2align 4
  .type g_task_propagate_int, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_propagate_int
#endif
g_task_propagate_int:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1811 & 0xffff
#if 1811 > 0xffff
  movk ip0, 1811 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_propagate_pointer
  .p2align 4
  .type g_task_propagate_pointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_propagate_pointer
#endif
g_task_propagate_pointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1812 & 0xffff
#if 1812 > 0xffff
  movk ip0, 1812 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_propagate_value
  .p2align 4
  .type g_task_propagate_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_propagate_value
#endif
g_task_propagate_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1813 & 0xffff
#if 1813 > 0xffff
  movk ip0, 1813 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_report_error
  .p2align 4
  .type g_task_report_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_report_error
#endif
g_task_report_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1814 & 0xffff
#if 1814 > 0xffff
  movk ip0, 1814 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_report_new_error
  .p2align 4
  .type g_task_report_new_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_report_new_error
#endif
g_task_report_new_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1815 & 0xffff
#if 1815 > 0xffff
  movk ip0, 1815 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_boolean
  .p2align 4
  .type g_task_return_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_boolean
#endif
g_task_return_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1816 & 0xffff
#if 1816 > 0xffff
  movk ip0, 1816 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_error
  .p2align 4
  .type g_task_return_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_error
#endif
g_task_return_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1817 & 0xffff
#if 1817 > 0xffff
  movk ip0, 1817 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_error_if_cancelled
  .p2align 4
  .type g_task_return_error_if_cancelled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_error_if_cancelled
#endif
g_task_return_error_if_cancelled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1818 & 0xffff
#if 1818 > 0xffff
  movk ip0, 1818 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_int
  .p2align 4
  .type g_task_return_int, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_int
#endif
g_task_return_int:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1819 & 0xffff
#if 1819 > 0xffff
  movk ip0, 1819 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_new_error
  .p2align 4
  .type g_task_return_new_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_new_error
#endif
g_task_return_new_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1820 & 0xffff
#if 1820 > 0xffff
  movk ip0, 1820 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_new_error_literal
  .p2align 4
  .type g_task_return_new_error_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_new_error_literal
#endif
g_task_return_new_error_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1821 & 0xffff
#if 1821 > 0xffff
  movk ip0, 1821 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_pointer
  .p2align 4
  .type g_task_return_pointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_pointer
#endif
g_task_return_pointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1822 & 0xffff
#if 1822 > 0xffff
  movk ip0, 1822 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_prefixed_error
  .p2align 4
  .type g_task_return_prefixed_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_prefixed_error
#endif
g_task_return_prefixed_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1823 & 0xffff
#if 1823 > 0xffff
  movk ip0, 1823 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_return_value
  .p2align 4
  .type g_task_return_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_return_value
#endif
g_task_return_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1824 & 0xffff
#if 1824 > 0xffff
  movk ip0, 1824 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_run_in_thread
  .p2align 4
  .type g_task_run_in_thread, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_run_in_thread
#endif
g_task_run_in_thread:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1825 & 0xffff
#if 1825 > 0xffff
  movk ip0, 1825 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_run_in_thread_sync
  .p2align 4
  .type g_task_run_in_thread_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_run_in_thread_sync
#endif
g_task_run_in_thread_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1826 & 0xffff
#if 1826 > 0xffff
  movk ip0, 1826 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_set_check_cancellable
  .p2align 4
  .type g_task_set_check_cancellable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_set_check_cancellable
#endif
g_task_set_check_cancellable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1827 & 0xffff
#if 1827 > 0xffff
  movk ip0, 1827 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_set_name
  .p2align 4
  .type g_task_set_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_set_name
#endif
g_task_set_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1828 & 0xffff
#if 1828 > 0xffff
  movk ip0, 1828 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_set_priority
  .p2align 4
  .type g_task_set_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_set_priority
#endif
g_task_set_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1829 & 0xffff
#if 1829 > 0xffff
  movk ip0, 1829 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_set_return_on_cancel
  .p2align 4
  .type g_task_set_return_on_cancel, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_set_return_on_cancel
#endif
g_task_set_return_on_cancel:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1830 & 0xffff
#if 1830 > 0xffff
  movk ip0, 1830 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_set_source_tag
  .p2align 4
  .type g_task_set_source_tag, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_set_source_tag
#endif
g_task_set_source_tag:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1831 & 0xffff
#if 1831 > 0xffff
  movk ip0, 1831 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_set_static_name
  .p2align 4
  .type g_task_set_static_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_set_static_name
#endif
g_task_set_static_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1832 & 0xffff
#if 1832 > 0xffff
  movk ip0, 1832 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_task_set_task_data
  .p2align 4
  .type g_task_set_task_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_task_set_task_data
#endif
g_task_set_task_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1833 & 0xffff
#if 1833 > 0xffff
  movk ip0, 1833 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tcp_connection_get_graceful_disconnect
  .p2align 4
  .type g_tcp_connection_get_graceful_disconnect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tcp_connection_get_graceful_disconnect
#endif
g_tcp_connection_get_graceful_disconnect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1834 & 0xffff
#if 1834 > 0xffff
  movk ip0, 1834 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tcp_connection_get_type
  .p2align 4
  .type g_tcp_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tcp_connection_get_type
#endif
g_tcp_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1835 & 0xffff
#if 1835 > 0xffff
  movk ip0, 1835 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tcp_connection_set_graceful_disconnect
  .p2align 4
  .type g_tcp_connection_set_graceful_disconnect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tcp_connection_set_graceful_disconnect
#endif
g_tcp_connection_set_graceful_disconnect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1836 & 0xffff
#if 1836 > 0xffff
  movk ip0, 1836 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tcp_wrapper_connection_get_base_io_stream
  .p2align 4
  .type g_tcp_wrapper_connection_get_base_io_stream, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tcp_wrapper_connection_get_base_io_stream
#endif
g_tcp_wrapper_connection_get_base_io_stream:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1837 & 0xffff
#if 1837 > 0xffff
  movk ip0, 1837 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tcp_wrapper_connection_get_type
  .p2align 4
  .type g_tcp_wrapper_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tcp_wrapper_connection_get_type
#endif
g_tcp_wrapper_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1838 & 0xffff
#if 1838 > 0xffff
  movk ip0, 1838 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tcp_wrapper_connection_new
  .p2align 4
  .type g_tcp_wrapper_connection_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tcp_wrapper_connection_new
#endif
g_tcp_wrapper_connection_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1839 & 0xffff
#if 1839 > 0xffff
  movk ip0, 1839 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_add_service_dir
  .p2align 4
  .type g_test_dbus_add_service_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_add_service_dir
#endif
g_test_dbus_add_service_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1840 & 0xffff
#if 1840 > 0xffff
  movk ip0, 1840 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_down
  .p2align 4
  .type g_test_dbus_down, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_down
#endif
g_test_dbus_down:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1841 & 0xffff
#if 1841 > 0xffff
  movk ip0, 1841 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_flags_get_type
  .p2align 4
  .type g_test_dbus_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_flags_get_type
#endif
g_test_dbus_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1842 & 0xffff
#if 1842 > 0xffff
  movk ip0, 1842 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_get_bus_address
  .p2align 4
  .type g_test_dbus_get_bus_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_get_bus_address
#endif
g_test_dbus_get_bus_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1843 & 0xffff
#if 1843 > 0xffff
  movk ip0, 1843 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_get_flags
  .p2align 4
  .type g_test_dbus_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_get_flags
#endif
g_test_dbus_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1844 & 0xffff
#if 1844 > 0xffff
  movk ip0, 1844 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_get_type
  .p2align 4
  .type g_test_dbus_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_get_type
#endif
g_test_dbus_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1845 & 0xffff
#if 1845 > 0xffff
  movk ip0, 1845 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_new
  .p2align 4
  .type g_test_dbus_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_new
#endif
g_test_dbus_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1846 & 0xffff
#if 1846 > 0xffff
  movk ip0, 1846 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_stop
  .p2align 4
  .type g_test_dbus_stop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_stop
#endif
g_test_dbus_stop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1847 & 0xffff
#if 1847 > 0xffff
  movk ip0, 1847 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_unset
  .p2align 4
  .type g_test_dbus_unset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_unset
#endif
g_test_dbus_unset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1848 & 0xffff
#if 1848 > 0xffff
  movk ip0, 1848 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_dbus_up
  .p2align 4
  .type g_test_dbus_up, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_dbus_up
#endif
g_test_dbus_up:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1849 & 0xffff
#if 1849 > 0xffff
  movk ip0, 1849 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_themed_icon_append_name
  .p2align 4
  .type g_themed_icon_append_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_themed_icon_append_name
#endif
g_themed_icon_append_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1850 & 0xffff
#if 1850 > 0xffff
  movk ip0, 1850 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_themed_icon_get_names
  .p2align 4
  .type g_themed_icon_get_names, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_themed_icon_get_names
#endif
g_themed_icon_get_names:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1851 & 0xffff
#if 1851 > 0xffff
  movk ip0, 1851 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_themed_icon_get_type
  .p2align 4
  .type g_themed_icon_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_themed_icon_get_type
#endif
g_themed_icon_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1852 & 0xffff
#if 1852 > 0xffff
  movk ip0, 1852 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_themed_icon_new
  .p2align 4
  .type g_themed_icon_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_themed_icon_new
#endif
g_themed_icon_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1853 & 0xffff
#if 1853 > 0xffff
  movk ip0, 1853 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_themed_icon_new_from_names
  .p2align 4
  .type g_themed_icon_new_from_names, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_themed_icon_new_from_names
#endif
g_themed_icon_new_from_names:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1854 & 0xffff
#if 1854 > 0xffff
  movk ip0, 1854 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_themed_icon_new_with_default_fallbacks
  .p2align 4
  .type g_themed_icon_new_with_default_fallbacks, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_themed_icon_new_with_default_fallbacks
#endif
g_themed_icon_new_with_default_fallbacks:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1855 & 0xffff
#if 1855 > 0xffff
  movk ip0, 1855 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_themed_icon_prepend_name
  .p2align 4
  .type g_themed_icon_prepend_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_themed_icon_prepend_name
#endif
g_themed_icon_prepend_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1856 & 0xffff
#if 1856 > 0xffff
  movk ip0, 1856 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_threaded_resolver_get_type
  .p2align 4
  .type g_threaded_resolver_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_threaded_resolver_get_type
#endif
g_threaded_resolver_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1857 & 0xffff
#if 1857 > 0xffff
  movk ip0, 1857 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_threaded_socket_service_get_type
  .p2align 4
  .type g_threaded_socket_service_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_threaded_socket_service_get_type
#endif
g_threaded_socket_service_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1858 & 0xffff
#if 1858 > 0xffff
  movk ip0, 1858 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_threaded_socket_service_new
  .p2align 4
  .type g_threaded_socket_service_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_threaded_socket_service_new
#endif
g_threaded_socket_service_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1859 & 0xffff
#if 1859 > 0xffff
  movk ip0, 1859 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_authentication_mode_get_type
  .p2align 4
  .type g_tls_authentication_mode_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_authentication_mode_get_type
#endif
g_tls_authentication_mode_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1860 & 0xffff
#if 1860 > 0xffff
  movk ip0, 1860 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_certificate_type
  .p2align 4
  .type g_tls_backend_get_certificate_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_certificate_type
#endif
g_tls_backend_get_certificate_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1861 & 0xffff
#if 1861 > 0xffff
  movk ip0, 1861 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_client_connection_type
  .p2align 4
  .type g_tls_backend_get_client_connection_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_client_connection_type
#endif
g_tls_backend_get_client_connection_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1862 & 0xffff
#if 1862 > 0xffff
  movk ip0, 1862 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_default
  .p2align 4
  .type g_tls_backend_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_default
#endif
g_tls_backend_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1863 & 0xffff
#if 1863 > 0xffff
  movk ip0, 1863 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_default_database
  .p2align 4
  .type g_tls_backend_get_default_database, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_default_database
#endif
g_tls_backend_get_default_database:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1864 & 0xffff
#if 1864 > 0xffff
  movk ip0, 1864 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_dtls_client_connection_type
  .p2align 4
  .type g_tls_backend_get_dtls_client_connection_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_dtls_client_connection_type
#endif
g_tls_backend_get_dtls_client_connection_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1865 & 0xffff
#if 1865 > 0xffff
  movk ip0, 1865 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_dtls_server_connection_type
  .p2align 4
  .type g_tls_backend_get_dtls_server_connection_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_dtls_server_connection_type
#endif
g_tls_backend_get_dtls_server_connection_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1866 & 0xffff
#if 1866 > 0xffff
  movk ip0, 1866 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_file_database_type
  .p2align 4
  .type g_tls_backend_get_file_database_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_file_database_type
#endif
g_tls_backend_get_file_database_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1867 & 0xffff
#if 1867 > 0xffff
  movk ip0, 1867 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_server_connection_type
  .p2align 4
  .type g_tls_backend_get_server_connection_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_server_connection_type
#endif
g_tls_backend_get_server_connection_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1868 & 0xffff
#if 1868 > 0xffff
  movk ip0, 1868 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_get_type
  .p2align 4
  .type g_tls_backend_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_get_type
#endif
g_tls_backend_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1869 & 0xffff
#if 1869 > 0xffff
  movk ip0, 1869 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_set_default_database
  .p2align 4
  .type g_tls_backend_set_default_database, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_set_default_database
#endif
g_tls_backend_set_default_database:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1870 & 0xffff
#if 1870 > 0xffff
  movk ip0, 1870 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_supports_dtls
  .p2align 4
  .type g_tls_backend_supports_dtls, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_supports_dtls
#endif
g_tls_backend_supports_dtls:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1871 & 0xffff
#if 1871 > 0xffff
  movk ip0, 1871 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_backend_supports_tls
  .p2align 4
  .type g_tls_backend_supports_tls, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_backend_supports_tls
#endif
g_tls_backend_supports_tls:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1872 & 0xffff
#if 1872 > 0xffff
  movk ip0, 1872 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_flags_get_type
  .p2align 4
  .type g_tls_certificate_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_flags_get_type
#endif
g_tls_certificate_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1873 & 0xffff
#if 1873 > 0xffff
  movk ip0, 1873 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_dns_names
  .p2align 4
  .type g_tls_certificate_get_dns_names, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_dns_names
#endif
g_tls_certificate_get_dns_names:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+14992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+14992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1874 & 0xffff
#if 1874 > 0xffff
  movk ip0, 1874 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_ip_addresses
  .p2align 4
  .type g_tls_certificate_get_ip_addresses, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_ip_addresses
#endif
g_tls_certificate_get_ip_addresses:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1875 & 0xffff
#if 1875 > 0xffff
  movk ip0, 1875 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_issuer
  .p2align 4
  .type g_tls_certificate_get_issuer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_issuer
#endif
g_tls_certificate_get_issuer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1876 & 0xffff
#if 1876 > 0xffff
  movk ip0, 1876 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_issuer_name
  .p2align 4
  .type g_tls_certificate_get_issuer_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_issuer_name
#endif
g_tls_certificate_get_issuer_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1877 & 0xffff
#if 1877 > 0xffff
  movk ip0, 1877 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_not_valid_after
  .p2align 4
  .type g_tls_certificate_get_not_valid_after, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_not_valid_after
#endif
g_tls_certificate_get_not_valid_after:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1878 & 0xffff
#if 1878 > 0xffff
  movk ip0, 1878 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_not_valid_before
  .p2align 4
  .type g_tls_certificate_get_not_valid_before, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_not_valid_before
#endif
g_tls_certificate_get_not_valid_before:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1879 & 0xffff
#if 1879 > 0xffff
  movk ip0, 1879 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_subject_name
  .p2align 4
  .type g_tls_certificate_get_subject_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_subject_name
#endif
g_tls_certificate_get_subject_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1880 & 0xffff
#if 1880 > 0xffff
  movk ip0, 1880 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_get_type
  .p2align 4
  .type g_tls_certificate_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_get_type
#endif
g_tls_certificate_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1881 & 0xffff
#if 1881 > 0xffff
  movk ip0, 1881 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_is_same
  .p2align 4
  .type g_tls_certificate_is_same, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_is_same
#endif
g_tls_certificate_is_same:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1882 & 0xffff
#if 1882 > 0xffff
  movk ip0, 1882 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_list_new_from_file
  .p2align 4
  .type g_tls_certificate_list_new_from_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_list_new_from_file
#endif
g_tls_certificate_list_new_from_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1883 & 0xffff
#if 1883 > 0xffff
  movk ip0, 1883 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_new_from_file
  .p2align 4
  .type g_tls_certificate_new_from_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_new_from_file
#endif
g_tls_certificate_new_from_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1884 & 0xffff
#if 1884 > 0xffff
  movk ip0, 1884 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_new_from_file_with_password
  .p2align 4
  .type g_tls_certificate_new_from_file_with_password, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_new_from_file_with_password
#endif
g_tls_certificate_new_from_file_with_password:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1885 & 0xffff
#if 1885 > 0xffff
  movk ip0, 1885 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_new_from_files
  .p2align 4
  .type g_tls_certificate_new_from_files, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_new_from_files
#endif
g_tls_certificate_new_from_files:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1886 & 0xffff
#if 1886 > 0xffff
  movk ip0, 1886 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_new_from_pem
  .p2align 4
  .type g_tls_certificate_new_from_pem, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_new_from_pem
#endif
g_tls_certificate_new_from_pem:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1887 & 0xffff
#if 1887 > 0xffff
  movk ip0, 1887 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_new_from_pkcs11_uris
  .p2align 4
  .type g_tls_certificate_new_from_pkcs11_uris, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_new_from_pkcs11_uris
#endif
g_tls_certificate_new_from_pkcs11_uris:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1888 & 0xffff
#if 1888 > 0xffff
  movk ip0, 1888 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_new_from_pkcs12
  .p2align 4
  .type g_tls_certificate_new_from_pkcs12, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_new_from_pkcs12
#endif
g_tls_certificate_new_from_pkcs12:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1889 & 0xffff
#if 1889 > 0xffff
  movk ip0, 1889 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_request_flags_get_type
  .p2align 4
  .type g_tls_certificate_request_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_request_flags_get_type
#endif
g_tls_certificate_request_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1890 & 0xffff
#if 1890 > 0xffff
  movk ip0, 1890 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_certificate_verify
  .p2align 4
  .type g_tls_certificate_verify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_certificate_verify
#endif
g_tls_certificate_verify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1891 & 0xffff
#if 1891 > 0xffff
  movk ip0, 1891 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_channel_binding_error_get_type
  .p2align 4
  .type g_tls_channel_binding_error_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_channel_binding_error_get_type
#endif
g_tls_channel_binding_error_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1892 & 0xffff
#if 1892 > 0xffff
  movk ip0, 1892 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_channel_binding_error_quark
  .p2align 4
  .type g_tls_channel_binding_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_channel_binding_error_quark
#endif
g_tls_channel_binding_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1893 & 0xffff
#if 1893 > 0xffff
  movk ip0, 1893 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_channel_binding_type_get_type
  .p2align 4
  .type g_tls_channel_binding_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_channel_binding_type_get_type
#endif
g_tls_channel_binding_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1894 & 0xffff
#if 1894 > 0xffff
  movk ip0, 1894 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_copy_session_state
  .p2align 4
  .type g_tls_client_connection_copy_session_state, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_copy_session_state
#endif
g_tls_client_connection_copy_session_state:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1895 & 0xffff
#if 1895 > 0xffff
  movk ip0, 1895 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_get_accepted_cas
  .p2align 4
  .type g_tls_client_connection_get_accepted_cas, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_get_accepted_cas
#endif
g_tls_client_connection_get_accepted_cas:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1896 & 0xffff
#if 1896 > 0xffff
  movk ip0, 1896 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_get_server_identity
  .p2align 4
  .type g_tls_client_connection_get_server_identity, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_get_server_identity
#endif
g_tls_client_connection_get_server_identity:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1897 & 0xffff
#if 1897 > 0xffff
  movk ip0, 1897 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_get_type
  .p2align 4
  .type g_tls_client_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_get_type
#endif
g_tls_client_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1898 & 0xffff
#if 1898 > 0xffff
  movk ip0, 1898 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_get_use_ssl3
  .p2align 4
  .type g_tls_client_connection_get_use_ssl3, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_get_use_ssl3
#endif
g_tls_client_connection_get_use_ssl3:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1899 & 0xffff
#if 1899 > 0xffff
  movk ip0, 1899 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_get_validation_flags
  .p2align 4
  .type g_tls_client_connection_get_validation_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_get_validation_flags
#endif
g_tls_client_connection_get_validation_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1900 & 0xffff
#if 1900 > 0xffff
  movk ip0, 1900 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_new
  .p2align 4
  .type g_tls_client_connection_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_new
#endif
g_tls_client_connection_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1901 & 0xffff
#if 1901 > 0xffff
  movk ip0, 1901 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_set_server_identity
  .p2align 4
  .type g_tls_client_connection_set_server_identity, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_set_server_identity
#endif
g_tls_client_connection_set_server_identity:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1902 & 0xffff
#if 1902 > 0xffff
  movk ip0, 1902 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_set_use_ssl3
  .p2align 4
  .type g_tls_client_connection_set_use_ssl3, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_set_use_ssl3
#endif
g_tls_client_connection_set_use_ssl3:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1903 & 0xffff
#if 1903 > 0xffff
  movk ip0, 1903 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_client_connection_set_validation_flags
  .p2align 4
  .type g_tls_client_connection_set_validation_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_client_connection_set_validation_flags
#endif
g_tls_client_connection_set_validation_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1904 & 0xffff
#if 1904 > 0xffff
  movk ip0, 1904 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_emit_accept_certificate
  .p2align 4
  .type g_tls_connection_emit_accept_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_emit_accept_certificate
#endif
g_tls_connection_emit_accept_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1905 & 0xffff
#if 1905 > 0xffff
  movk ip0, 1905 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_certificate
  .p2align 4
  .type g_tls_connection_get_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_certificate
#endif
g_tls_connection_get_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1906 & 0xffff
#if 1906 > 0xffff
  movk ip0, 1906 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_channel_binding_data
  .p2align 4
  .type g_tls_connection_get_channel_binding_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_channel_binding_data
#endif
g_tls_connection_get_channel_binding_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1907 & 0xffff
#if 1907 > 0xffff
  movk ip0, 1907 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_ciphersuite_name
  .p2align 4
  .type g_tls_connection_get_ciphersuite_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_ciphersuite_name
#endif
g_tls_connection_get_ciphersuite_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1908 & 0xffff
#if 1908 > 0xffff
  movk ip0, 1908 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_database
  .p2align 4
  .type g_tls_connection_get_database, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_database
#endif
g_tls_connection_get_database:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1909 & 0xffff
#if 1909 > 0xffff
  movk ip0, 1909 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_interaction
  .p2align 4
  .type g_tls_connection_get_interaction, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_interaction
#endif
g_tls_connection_get_interaction:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1910 & 0xffff
#if 1910 > 0xffff
  movk ip0, 1910 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_negotiated_protocol
  .p2align 4
  .type g_tls_connection_get_negotiated_protocol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_negotiated_protocol
#endif
g_tls_connection_get_negotiated_protocol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1911 & 0xffff
#if 1911 > 0xffff
  movk ip0, 1911 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_peer_certificate
  .p2align 4
  .type g_tls_connection_get_peer_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_peer_certificate
#endif
g_tls_connection_get_peer_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1912 & 0xffff
#if 1912 > 0xffff
  movk ip0, 1912 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_peer_certificate_errors
  .p2align 4
  .type g_tls_connection_get_peer_certificate_errors, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_peer_certificate_errors
#endif
g_tls_connection_get_peer_certificate_errors:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1913 & 0xffff
#if 1913 > 0xffff
  movk ip0, 1913 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_protocol_version
  .p2align 4
  .type g_tls_connection_get_protocol_version, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_protocol_version
#endif
g_tls_connection_get_protocol_version:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1914 & 0xffff
#if 1914 > 0xffff
  movk ip0, 1914 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_rehandshake_mode
  .p2align 4
  .type g_tls_connection_get_rehandshake_mode, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_rehandshake_mode
#endif
g_tls_connection_get_rehandshake_mode:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1915 & 0xffff
#if 1915 > 0xffff
  movk ip0, 1915 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_require_close_notify
  .p2align 4
  .type g_tls_connection_get_require_close_notify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_require_close_notify
#endif
g_tls_connection_get_require_close_notify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1916 & 0xffff
#if 1916 > 0xffff
  movk ip0, 1916 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_type
  .p2align 4
  .type g_tls_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_type
#endif
g_tls_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1917 & 0xffff
#if 1917 > 0xffff
  movk ip0, 1917 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_get_use_system_certdb
  .p2align 4
  .type g_tls_connection_get_use_system_certdb, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_get_use_system_certdb
#endif
g_tls_connection_get_use_system_certdb:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1918 & 0xffff
#if 1918 > 0xffff
  movk ip0, 1918 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_handshake
  .p2align 4
  .type g_tls_connection_handshake, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_handshake
#endif
g_tls_connection_handshake:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1919 & 0xffff
#if 1919 > 0xffff
  movk ip0, 1919 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_handshake_async
  .p2align 4
  .type g_tls_connection_handshake_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_handshake_async
#endif
g_tls_connection_handshake_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1920 & 0xffff
#if 1920 > 0xffff
  movk ip0, 1920 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_handshake_finish
  .p2align 4
  .type g_tls_connection_handshake_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_handshake_finish
#endif
g_tls_connection_handshake_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1921 & 0xffff
#if 1921 > 0xffff
  movk ip0, 1921 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_set_advertised_protocols
  .p2align 4
  .type g_tls_connection_set_advertised_protocols, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_set_advertised_protocols
#endif
g_tls_connection_set_advertised_protocols:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1922 & 0xffff
#if 1922 > 0xffff
  movk ip0, 1922 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_set_certificate
  .p2align 4
  .type g_tls_connection_set_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_set_certificate
#endif
g_tls_connection_set_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1923 & 0xffff
#if 1923 > 0xffff
  movk ip0, 1923 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_set_database
  .p2align 4
  .type g_tls_connection_set_database, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_set_database
#endif
g_tls_connection_set_database:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1924 & 0xffff
#if 1924 > 0xffff
  movk ip0, 1924 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_set_interaction
  .p2align 4
  .type g_tls_connection_set_interaction, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_set_interaction
#endif
g_tls_connection_set_interaction:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1925 & 0xffff
#if 1925 > 0xffff
  movk ip0, 1925 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_set_rehandshake_mode
  .p2align 4
  .type g_tls_connection_set_rehandshake_mode, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_set_rehandshake_mode
#endif
g_tls_connection_set_rehandshake_mode:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1926 & 0xffff
#if 1926 > 0xffff
  movk ip0, 1926 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_set_require_close_notify
  .p2align 4
  .type g_tls_connection_set_require_close_notify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_set_require_close_notify
#endif
g_tls_connection_set_require_close_notify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1927 & 0xffff
#if 1927 > 0xffff
  movk ip0, 1927 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_connection_set_use_system_certdb
  .p2align 4
  .type g_tls_connection_set_use_system_certdb, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_connection_set_use_system_certdb
#endif
g_tls_connection_set_use_system_certdb:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1928 & 0xffff
#if 1928 > 0xffff
  movk ip0, 1928 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_create_certificate_handle
  .p2align 4
  .type g_tls_database_create_certificate_handle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_create_certificate_handle
#endif
g_tls_database_create_certificate_handle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1929 & 0xffff
#if 1929 > 0xffff
  movk ip0, 1929 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_get_type
  .p2align 4
  .type g_tls_database_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_get_type
#endif
g_tls_database_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1930 & 0xffff
#if 1930 > 0xffff
  movk ip0, 1930 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificate_for_handle
  .p2align 4
  .type g_tls_database_lookup_certificate_for_handle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificate_for_handle
#endif
g_tls_database_lookup_certificate_for_handle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1931 & 0xffff
#if 1931 > 0xffff
  movk ip0, 1931 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificate_for_handle_async
  .p2align 4
  .type g_tls_database_lookup_certificate_for_handle_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificate_for_handle_async
#endif
g_tls_database_lookup_certificate_for_handle_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1932 & 0xffff
#if 1932 > 0xffff
  movk ip0, 1932 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificate_for_handle_finish
  .p2align 4
  .type g_tls_database_lookup_certificate_for_handle_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificate_for_handle_finish
#endif
g_tls_database_lookup_certificate_for_handle_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1933 & 0xffff
#if 1933 > 0xffff
  movk ip0, 1933 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificate_issuer
  .p2align 4
  .type g_tls_database_lookup_certificate_issuer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificate_issuer
#endif
g_tls_database_lookup_certificate_issuer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1934 & 0xffff
#if 1934 > 0xffff
  movk ip0, 1934 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificate_issuer_async
  .p2align 4
  .type g_tls_database_lookup_certificate_issuer_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificate_issuer_async
#endif
g_tls_database_lookup_certificate_issuer_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1935 & 0xffff
#if 1935 > 0xffff
  movk ip0, 1935 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificate_issuer_finish
  .p2align 4
  .type g_tls_database_lookup_certificate_issuer_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificate_issuer_finish
#endif
g_tls_database_lookup_certificate_issuer_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1936 & 0xffff
#if 1936 > 0xffff
  movk ip0, 1936 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificates_issued_by
  .p2align 4
  .type g_tls_database_lookup_certificates_issued_by, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificates_issued_by
#endif
g_tls_database_lookup_certificates_issued_by:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1937 & 0xffff
#if 1937 > 0xffff
  movk ip0, 1937 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificates_issued_by_async
  .p2align 4
  .type g_tls_database_lookup_certificates_issued_by_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificates_issued_by_async
#endif
g_tls_database_lookup_certificates_issued_by_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1938 & 0xffff
#if 1938 > 0xffff
  movk ip0, 1938 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_certificates_issued_by_finish
  .p2align 4
  .type g_tls_database_lookup_certificates_issued_by_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_certificates_issued_by_finish
#endif
g_tls_database_lookup_certificates_issued_by_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1939 & 0xffff
#if 1939 > 0xffff
  movk ip0, 1939 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_lookup_flags_get_type
  .p2align 4
  .type g_tls_database_lookup_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_lookup_flags_get_type
#endif
g_tls_database_lookup_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1940 & 0xffff
#if 1940 > 0xffff
  movk ip0, 1940 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_verify_chain
  .p2align 4
  .type g_tls_database_verify_chain, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_verify_chain
#endif
g_tls_database_verify_chain:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1941 & 0xffff
#if 1941 > 0xffff
  movk ip0, 1941 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_verify_chain_async
  .p2align 4
  .type g_tls_database_verify_chain_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_verify_chain_async
#endif
g_tls_database_verify_chain_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1942 & 0xffff
#if 1942 > 0xffff
  movk ip0, 1942 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_verify_chain_finish
  .p2align 4
  .type g_tls_database_verify_chain_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_verify_chain_finish
#endif
g_tls_database_verify_chain_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1943 & 0xffff
#if 1943 > 0xffff
  movk ip0, 1943 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_database_verify_flags_get_type
  .p2align 4
  .type g_tls_database_verify_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_database_verify_flags_get_type
#endif
g_tls_database_verify_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1944 & 0xffff
#if 1944 > 0xffff
  movk ip0, 1944 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_error_get_type
  .p2align 4
  .type g_tls_error_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_error_get_type
#endif
g_tls_error_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1945 & 0xffff
#if 1945 > 0xffff
  movk ip0, 1945 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_error_quark
  .p2align 4
  .type g_tls_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_error_quark
#endif
g_tls_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1946 & 0xffff
#if 1946 > 0xffff
  movk ip0, 1946 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_file_database_get_type
  .p2align 4
  .type g_tls_file_database_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_file_database_get_type
#endif
g_tls_file_database_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1947 & 0xffff
#if 1947 > 0xffff
  movk ip0, 1947 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_file_database_new
  .p2align 4
  .type g_tls_file_database_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_file_database_new
#endif
g_tls_file_database_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1948 & 0xffff
#if 1948 > 0xffff
  movk ip0, 1948 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_ask_password
  .p2align 4
  .type g_tls_interaction_ask_password, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_ask_password
#endif
g_tls_interaction_ask_password:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1949 & 0xffff
#if 1949 > 0xffff
  movk ip0, 1949 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_ask_password_async
  .p2align 4
  .type g_tls_interaction_ask_password_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_ask_password_async
#endif
g_tls_interaction_ask_password_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1950 & 0xffff
#if 1950 > 0xffff
  movk ip0, 1950 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_ask_password_finish
  .p2align 4
  .type g_tls_interaction_ask_password_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_ask_password_finish
#endif
g_tls_interaction_ask_password_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1951 & 0xffff
#if 1951 > 0xffff
  movk ip0, 1951 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_get_type
  .p2align 4
  .type g_tls_interaction_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_get_type
#endif
g_tls_interaction_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1952 & 0xffff
#if 1952 > 0xffff
  movk ip0, 1952 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_invoke_ask_password
  .p2align 4
  .type g_tls_interaction_invoke_ask_password, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_invoke_ask_password
#endif
g_tls_interaction_invoke_ask_password:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1953 & 0xffff
#if 1953 > 0xffff
  movk ip0, 1953 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_invoke_request_certificate
  .p2align 4
  .type g_tls_interaction_invoke_request_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_invoke_request_certificate
#endif
g_tls_interaction_invoke_request_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1954 & 0xffff
#if 1954 > 0xffff
  movk ip0, 1954 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_request_certificate
  .p2align 4
  .type g_tls_interaction_request_certificate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_request_certificate
#endif
g_tls_interaction_request_certificate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1955 & 0xffff
#if 1955 > 0xffff
  movk ip0, 1955 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_request_certificate_async
  .p2align 4
  .type g_tls_interaction_request_certificate_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_request_certificate_async
#endif
g_tls_interaction_request_certificate_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1956 & 0xffff
#if 1956 > 0xffff
  movk ip0, 1956 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_request_certificate_finish
  .p2align 4
  .type g_tls_interaction_request_certificate_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_request_certificate_finish
#endif
g_tls_interaction_request_certificate_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1957 & 0xffff
#if 1957 > 0xffff
  movk ip0, 1957 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_interaction_result_get_type
  .p2align 4
  .type g_tls_interaction_result_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_interaction_result_get_type
#endif
g_tls_interaction_result_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1958 & 0xffff
#if 1958 > 0xffff
  movk ip0, 1958 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_flags_get_type
  .p2align 4
  .type g_tls_password_flags_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_flags_get_type
#endif
g_tls_password_flags_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1959 & 0xffff
#if 1959 > 0xffff
  movk ip0, 1959 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_get_description
  .p2align 4
  .type g_tls_password_get_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_get_description
#endif
g_tls_password_get_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1960 & 0xffff
#if 1960 > 0xffff
  movk ip0, 1960 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_get_flags
  .p2align 4
  .type g_tls_password_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_get_flags
#endif
g_tls_password_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1961 & 0xffff
#if 1961 > 0xffff
  movk ip0, 1961 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_get_type
  .p2align 4
  .type g_tls_password_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_get_type
#endif
g_tls_password_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1962 & 0xffff
#if 1962 > 0xffff
  movk ip0, 1962 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_get_value
  .p2align 4
  .type g_tls_password_get_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_get_value
#endif
g_tls_password_get_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1963 & 0xffff
#if 1963 > 0xffff
  movk ip0, 1963 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_get_warning
  .p2align 4
  .type g_tls_password_get_warning, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_get_warning
#endif
g_tls_password_get_warning:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1964 & 0xffff
#if 1964 > 0xffff
  movk ip0, 1964 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_new
  .p2align 4
  .type g_tls_password_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_new
#endif
g_tls_password_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1965 & 0xffff
#if 1965 > 0xffff
  movk ip0, 1965 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_set_description
  .p2align 4
  .type g_tls_password_set_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_set_description
#endif
g_tls_password_set_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1966 & 0xffff
#if 1966 > 0xffff
  movk ip0, 1966 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_set_flags
  .p2align 4
  .type g_tls_password_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_set_flags
#endif
g_tls_password_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1967 & 0xffff
#if 1967 > 0xffff
  movk ip0, 1967 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_set_value
  .p2align 4
  .type g_tls_password_set_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_set_value
#endif
g_tls_password_set_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1968 & 0xffff
#if 1968 > 0xffff
  movk ip0, 1968 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_set_value_full
  .p2align 4
  .type g_tls_password_set_value_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_set_value_full
#endif
g_tls_password_set_value_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1969 & 0xffff
#if 1969 > 0xffff
  movk ip0, 1969 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_password_set_warning
  .p2align 4
  .type g_tls_password_set_warning, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_password_set_warning
#endif
g_tls_password_set_warning:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1970 & 0xffff
#if 1970 > 0xffff
  movk ip0, 1970 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_protocol_version_get_type
  .p2align 4
  .type g_tls_protocol_version_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_protocol_version_get_type
#endif
g_tls_protocol_version_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1971 & 0xffff
#if 1971 > 0xffff
  movk ip0, 1971 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_rehandshake_mode_get_type
  .p2align 4
  .type g_tls_rehandshake_mode_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_rehandshake_mode_get_type
#endif
g_tls_rehandshake_mode_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1972 & 0xffff
#if 1972 > 0xffff
  movk ip0, 1972 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_server_connection_get_type
  .p2align 4
  .type g_tls_server_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_server_connection_get_type
#endif
g_tls_server_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1973 & 0xffff
#if 1973 > 0xffff
  movk ip0, 1973 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tls_server_connection_new
  .p2align 4
  .type g_tls_server_connection_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tls_server_connection_new
#endif
g_tls_server_connection_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1974 & 0xffff
#if 1974 > 0xffff
  movk ip0, 1974 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_get_type
  .p2align 4
  .type g_unix_connection_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_get_type
#endif
g_unix_connection_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1975 & 0xffff
#if 1975 > 0xffff
  movk ip0, 1975 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_receive_credentials
  .p2align 4
  .type g_unix_connection_receive_credentials, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_receive_credentials
#endif
g_unix_connection_receive_credentials:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1976 & 0xffff
#if 1976 > 0xffff
  movk ip0, 1976 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_receive_credentials_async
  .p2align 4
  .type g_unix_connection_receive_credentials_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_receive_credentials_async
#endif
g_unix_connection_receive_credentials_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1977 & 0xffff
#if 1977 > 0xffff
  movk ip0, 1977 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_receive_credentials_finish
  .p2align 4
  .type g_unix_connection_receive_credentials_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_receive_credentials_finish
#endif
g_unix_connection_receive_credentials_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1978 & 0xffff
#if 1978 > 0xffff
  movk ip0, 1978 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_receive_fd
  .p2align 4
  .type g_unix_connection_receive_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_receive_fd
#endif
g_unix_connection_receive_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1979 & 0xffff
#if 1979 > 0xffff
  movk ip0, 1979 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_send_credentials
  .p2align 4
  .type g_unix_connection_send_credentials, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_send_credentials
#endif
g_unix_connection_send_credentials:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1980 & 0xffff
#if 1980 > 0xffff
  movk ip0, 1980 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_send_credentials_async
  .p2align 4
  .type g_unix_connection_send_credentials_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_send_credentials_async
#endif
g_unix_connection_send_credentials_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1981 & 0xffff
#if 1981 > 0xffff
  movk ip0, 1981 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_send_credentials_finish
  .p2align 4
  .type g_unix_connection_send_credentials_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_send_credentials_finish
#endif
g_unix_connection_send_credentials_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1982 & 0xffff
#if 1982 > 0xffff
  movk ip0, 1982 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_connection_send_fd
  .p2align 4
  .type g_unix_connection_send_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_connection_send_fd
#endif
g_unix_connection_send_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1983 & 0xffff
#if 1983 > 0xffff
  movk ip0, 1983 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_credentials_message_get_credentials
  .p2align 4
  .type g_unix_credentials_message_get_credentials, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_credentials_message_get_credentials
#endif
g_unix_credentials_message_get_credentials:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1984 & 0xffff
#if 1984 > 0xffff
  movk ip0, 1984 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_credentials_message_get_type
  .p2align 4
  .type g_unix_credentials_message_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_credentials_message_get_type
#endif
g_unix_credentials_message_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1985 & 0xffff
#if 1985 > 0xffff
  movk ip0, 1985 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_credentials_message_is_supported
  .p2align 4
  .type g_unix_credentials_message_is_supported, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_credentials_message_is_supported
#endif
g_unix_credentials_message_is_supported:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1986 & 0xffff
#if 1986 > 0xffff
  movk ip0, 1986 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_credentials_message_new
  .p2align 4
  .type g_unix_credentials_message_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_credentials_message_new
#endif
g_unix_credentials_message_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1987 & 0xffff
#if 1987 > 0xffff
  movk ip0, 1987 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_credentials_message_new_with_credentials
  .p2align 4
  .type g_unix_credentials_message_new_with_credentials, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_credentials_message_new_with_credentials
#endif
g_unix_credentials_message_new_with_credentials:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1988 & 0xffff
#if 1988 > 0xffff
  movk ip0, 1988 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_append
  .p2align 4
  .type g_unix_fd_list_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_append
#endif
g_unix_fd_list_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1989 & 0xffff
#if 1989 > 0xffff
  movk ip0, 1989 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_get
  .p2align 4
  .type g_unix_fd_list_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_get
#endif
g_unix_fd_list_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1990 & 0xffff
#if 1990 > 0xffff
  movk ip0, 1990 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_get_length
  .p2align 4
  .type g_unix_fd_list_get_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_get_length
#endif
g_unix_fd_list_get_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1991 & 0xffff
#if 1991 > 0xffff
  movk ip0, 1991 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_get_type
  .p2align 4
  .type g_unix_fd_list_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_get_type
#endif
g_unix_fd_list_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1992 & 0xffff
#if 1992 > 0xffff
  movk ip0, 1992 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_new
  .p2align 4
  .type g_unix_fd_list_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_new
#endif
g_unix_fd_list_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1993 & 0xffff
#if 1993 > 0xffff
  movk ip0, 1993 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_new_from_array
  .p2align 4
  .type g_unix_fd_list_new_from_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_new_from_array
#endif
g_unix_fd_list_new_from_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1994 & 0xffff
#if 1994 > 0xffff
  movk ip0, 1994 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_peek_fds
  .p2align 4
  .type g_unix_fd_list_peek_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_peek_fds
#endif
g_unix_fd_list_peek_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1995 & 0xffff
#if 1995 > 0xffff
  movk ip0, 1995 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_list_steal_fds
  .p2align 4
  .type g_unix_fd_list_steal_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_list_steal_fds
#endif
g_unix_fd_list_steal_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1996 & 0xffff
#if 1996 > 0xffff
  movk ip0, 1996 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_message_append_fd
  .p2align 4
  .type g_unix_fd_message_append_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_message_append_fd
#endif
g_unix_fd_message_append_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1997 & 0xffff
#if 1997 > 0xffff
  movk ip0, 1997 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_message_get_fd_list
  .p2align 4
  .type g_unix_fd_message_get_fd_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_message_get_fd_list
#endif
g_unix_fd_message_get_fd_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1998 & 0xffff
#if 1998 > 0xffff
  movk ip0, 1998 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_message_get_type
  .p2align 4
  .type g_unix_fd_message_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_message_get_type
#endif
g_unix_fd_message_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+15992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+15992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1999 & 0xffff
#if 1999 > 0xffff
  movk ip0, 1999 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_message_new
  .p2align 4
  .type g_unix_fd_message_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_message_new
#endif
g_unix_fd_message_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2000 & 0xffff
#if 2000 > 0xffff
  movk ip0, 2000 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_message_new_with_fd_list
  .p2align 4
  .type g_unix_fd_message_new_with_fd_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_message_new_with_fd_list
#endif
g_unix_fd_message_new_with_fd_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2001 & 0xffff
#if 2001 > 0xffff
  movk ip0, 2001 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_message_steal_fds
  .p2align 4
  .type g_unix_fd_message_steal_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_message_steal_fds
#endif
g_unix_fd_message_steal_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2002 & 0xffff
#if 2002 > 0xffff
  movk ip0, 2002 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_input_stream_get_close_fd
  .p2align 4
  .type g_unix_input_stream_get_close_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_input_stream_get_close_fd
#endif
g_unix_input_stream_get_close_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2003 & 0xffff
#if 2003 > 0xffff
  movk ip0, 2003 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_input_stream_get_fd
  .p2align 4
  .type g_unix_input_stream_get_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_input_stream_get_fd
#endif
g_unix_input_stream_get_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2004 & 0xffff
#if 2004 > 0xffff
  movk ip0, 2004 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_input_stream_get_type
  .p2align 4
  .type g_unix_input_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_input_stream_get_type
#endif
g_unix_input_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2005 & 0xffff
#if 2005 > 0xffff
  movk ip0, 2005 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_input_stream_new
  .p2align 4
  .type g_unix_input_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_input_stream_new
#endif
g_unix_input_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2006 & 0xffff
#if 2006 > 0xffff
  movk ip0, 2006 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_input_stream_set_close_fd
  .p2align 4
  .type g_unix_input_stream_set_close_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_input_stream_set_close_fd
#endif
g_unix_input_stream_set_close_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2007 & 0xffff
#if 2007 > 0xffff
  movk ip0, 2007 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_is_mount_path_system_internal
  .p2align 4
  .type g_unix_is_mount_path_system_internal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_is_mount_path_system_internal
#endif
g_unix_is_mount_path_system_internal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2008 & 0xffff
#if 2008 > 0xffff
  movk ip0, 2008 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_is_system_device_path
  .p2align 4
  .type g_unix_is_system_device_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_is_system_device_path
#endif
g_unix_is_system_device_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2009 & 0xffff
#if 2009 > 0xffff
  movk ip0, 2009 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_is_system_fs_type
  .p2align 4
  .type g_unix_is_system_fs_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_is_system_fs_type
#endif
g_unix_is_system_fs_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2010 & 0xffff
#if 2010 > 0xffff
  movk ip0, 2010 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_at
  .p2align 4
  .type g_unix_mount_at, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_at
#endif
g_unix_mount_at:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16088
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2011 & 0xffff
#if 2011 > 0xffff
  movk ip0, 2011 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_compare
  .p2align 4
  .type g_unix_mount_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_compare
#endif
g_unix_mount_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16096
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2012 & 0xffff
#if 2012 > 0xffff
  movk ip0, 2012 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_copy
  .p2align 4
  .type g_unix_mount_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_copy
#endif
g_unix_mount_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16104
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2013 & 0xffff
#if 2013 > 0xffff
  movk ip0, 2013 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entries_changed_since
  .p2align 4
  .type g_unix_mount_entries_changed_since, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entries_changed_since
#endif
g_unix_mount_entries_changed_since:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16112
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2014 & 0xffff
#if 2014 > 0xffff
  movk ip0, 2014 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entries_get
  .p2align 4
  .type g_unix_mount_entries_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entries_get
#endif
g_unix_mount_entries_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16120
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2015 & 0xffff
#if 2015 > 0xffff
  movk ip0, 2015 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entries_get_from_file
  .p2align 4
  .type g_unix_mount_entries_get_from_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entries_get_from_file
#endif
g_unix_mount_entries_get_from_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16128
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2016 & 0xffff
#if 2016 > 0xffff
  movk ip0, 2016 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_at
  .p2align 4
  .type g_unix_mount_entry_at, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_at
#endif
g_unix_mount_entry_at:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16136
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2017 & 0xffff
#if 2017 > 0xffff
  movk ip0, 2017 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_compare
  .p2align 4
  .type g_unix_mount_entry_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_compare
#endif
g_unix_mount_entry_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16144
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2018 & 0xffff
#if 2018 > 0xffff
  movk ip0, 2018 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_copy
  .p2align 4
  .type g_unix_mount_entry_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_copy
#endif
g_unix_mount_entry_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16152
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2019 & 0xffff
#if 2019 > 0xffff
  movk ip0, 2019 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_for
  .p2align 4
  .type g_unix_mount_entry_for, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_for
#endif
g_unix_mount_entry_for:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16160
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2020 & 0xffff
#if 2020 > 0xffff
  movk ip0, 2020 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_free
  .p2align 4
  .type g_unix_mount_entry_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_free
#endif
g_unix_mount_entry_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16168
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2021 & 0xffff
#if 2021 > 0xffff
  movk ip0, 2021 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_get_device_path
  .p2align 4
  .type g_unix_mount_entry_get_device_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_get_device_path
#endif
g_unix_mount_entry_get_device_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16176
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2022 & 0xffff
#if 2022 > 0xffff
  movk ip0, 2022 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_get_fs_type
  .p2align 4
  .type g_unix_mount_entry_get_fs_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_get_fs_type
#endif
g_unix_mount_entry_get_fs_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16184
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2023 & 0xffff
#if 2023 > 0xffff
  movk ip0, 2023 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_get_mount_path
  .p2align 4
  .type g_unix_mount_entry_get_mount_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_get_mount_path
#endif
g_unix_mount_entry_get_mount_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16192
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2024 & 0xffff
#if 2024 > 0xffff
  movk ip0, 2024 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_get_options
  .p2align 4
  .type g_unix_mount_entry_get_options, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_get_options
#endif
g_unix_mount_entry_get_options:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16200
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2025 & 0xffff
#if 2025 > 0xffff
  movk ip0, 2025 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_get_root_path
  .p2align 4
  .type g_unix_mount_entry_get_root_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_get_root_path
#endif
g_unix_mount_entry_get_root_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16208
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2026 & 0xffff
#if 2026 > 0xffff
  movk ip0, 2026 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_get_type
  .p2align 4
  .type g_unix_mount_entry_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_get_type
#endif
g_unix_mount_entry_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16216
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2027 & 0xffff
#if 2027 > 0xffff
  movk ip0, 2027 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_guess_can_eject
  .p2align 4
  .type g_unix_mount_entry_guess_can_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_guess_can_eject
#endif
g_unix_mount_entry_guess_can_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16224
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2028 & 0xffff
#if 2028 > 0xffff
  movk ip0, 2028 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_guess_icon
  .p2align 4
  .type g_unix_mount_entry_guess_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_guess_icon
#endif
g_unix_mount_entry_guess_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16232
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2029 & 0xffff
#if 2029 > 0xffff
  movk ip0, 2029 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_guess_name
  .p2align 4
  .type g_unix_mount_entry_guess_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_guess_name
#endif
g_unix_mount_entry_guess_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16240
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2030 & 0xffff
#if 2030 > 0xffff
  movk ip0, 2030 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_guess_should_display
  .p2align 4
  .type g_unix_mount_entry_guess_should_display, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_guess_should_display
#endif
g_unix_mount_entry_guess_should_display:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16248
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2031 & 0xffff
#if 2031 > 0xffff
  movk ip0, 2031 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_guess_symbolic_icon
  .p2align 4
  .type g_unix_mount_entry_guess_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_guess_symbolic_icon
#endif
g_unix_mount_entry_guess_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16256
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2032 & 0xffff
#if 2032 > 0xffff
  movk ip0, 2032 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_is_readonly
  .p2align 4
  .type g_unix_mount_entry_is_readonly, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_is_readonly
#endif
g_unix_mount_entry_is_readonly:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16264
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2033 & 0xffff
#if 2033 > 0xffff
  movk ip0, 2033 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_entry_is_system_internal
  .p2align 4
  .type g_unix_mount_entry_is_system_internal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_entry_is_system_internal
#endif
g_unix_mount_entry_is_system_internal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16272
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2034 & 0xffff
#if 2034 > 0xffff
  movk ip0, 2034 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_for
  .p2align 4
  .type g_unix_mount_for, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_for
#endif
g_unix_mount_for:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16280
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2035 & 0xffff
#if 2035 > 0xffff
  movk ip0, 2035 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_free
  .p2align 4
  .type g_unix_mount_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_free
#endif
g_unix_mount_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16288
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2036 & 0xffff
#if 2036 > 0xffff
  movk ip0, 2036 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_get_device_path
  .p2align 4
  .type g_unix_mount_get_device_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_get_device_path
#endif
g_unix_mount_get_device_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16296
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2037 & 0xffff
#if 2037 > 0xffff
  movk ip0, 2037 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_get_fs_type
  .p2align 4
  .type g_unix_mount_get_fs_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_get_fs_type
#endif
g_unix_mount_get_fs_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16304
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2038 & 0xffff
#if 2038 > 0xffff
  movk ip0, 2038 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_get_mount_path
  .p2align 4
  .type g_unix_mount_get_mount_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_get_mount_path
#endif
g_unix_mount_get_mount_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16312
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2039 & 0xffff
#if 2039 > 0xffff
  movk ip0, 2039 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_get_options
  .p2align 4
  .type g_unix_mount_get_options, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_get_options
#endif
g_unix_mount_get_options:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16320
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2040 & 0xffff
#if 2040 > 0xffff
  movk ip0, 2040 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_get_root_path
  .p2align 4
  .type g_unix_mount_get_root_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_get_root_path
#endif
g_unix_mount_get_root_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16328
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2041 & 0xffff
#if 2041 > 0xffff
  movk ip0, 2041 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_guess_can_eject
  .p2align 4
  .type g_unix_mount_guess_can_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_guess_can_eject
#endif
g_unix_mount_guess_can_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16336
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2042 & 0xffff
#if 2042 > 0xffff
  movk ip0, 2042 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_guess_icon
  .p2align 4
  .type g_unix_mount_guess_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_guess_icon
#endif
g_unix_mount_guess_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16344
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2043 & 0xffff
#if 2043 > 0xffff
  movk ip0, 2043 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_guess_name
  .p2align 4
  .type g_unix_mount_guess_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_guess_name
#endif
g_unix_mount_guess_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16352
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2044 & 0xffff
#if 2044 > 0xffff
  movk ip0, 2044 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_guess_should_display
  .p2align 4
  .type g_unix_mount_guess_should_display, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_guess_should_display
#endif
g_unix_mount_guess_should_display:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16360
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2045 & 0xffff
#if 2045 > 0xffff
  movk ip0, 2045 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_guess_symbolic_icon
  .p2align 4
  .type g_unix_mount_guess_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_guess_symbolic_icon
#endif
g_unix_mount_guess_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16368
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2046 & 0xffff
#if 2046 > 0xffff
  movk ip0, 2046 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_is_readonly
  .p2align 4
  .type g_unix_mount_is_readonly, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_is_readonly
#endif
g_unix_mount_is_readonly:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16376
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2047 & 0xffff
#if 2047 > 0xffff
  movk ip0, 2047 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_is_system_internal
  .p2align 4
  .type g_unix_mount_is_system_internal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_is_system_internal
#endif
g_unix_mount_is_system_internal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16384
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2048 & 0xffff
#if 2048 > 0xffff
  movk ip0, 2048 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_monitor_get
  .p2align 4
  .type g_unix_mount_monitor_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_monitor_get
#endif
g_unix_mount_monitor_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16392
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2049 & 0xffff
#if 2049 > 0xffff
  movk ip0, 2049 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_monitor_get_type
  .p2align 4
  .type g_unix_mount_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_monitor_get_type
#endif
g_unix_mount_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16400
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2050 & 0xffff
#if 2050 > 0xffff
  movk ip0, 2050 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_monitor_new
  .p2align 4
  .type g_unix_mount_monitor_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_monitor_new
#endif
g_unix_mount_monitor_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16408
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2051 & 0xffff
#if 2051 > 0xffff
  movk ip0, 2051 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_monitor_set_rate_limit
  .p2align 4
  .type g_unix_mount_monitor_set_rate_limit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_monitor_set_rate_limit
#endif
g_unix_mount_monitor_set_rate_limit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16416
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2052 & 0xffff
#if 2052 > 0xffff
  movk ip0, 2052 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_at
  .p2align 4
  .type g_unix_mount_point_at, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_at
#endif
g_unix_mount_point_at:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16424
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2053 & 0xffff
#if 2053 > 0xffff
  movk ip0, 2053 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_compare
  .p2align 4
  .type g_unix_mount_point_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_compare
#endif
g_unix_mount_point_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16432
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2054 & 0xffff
#if 2054 > 0xffff
  movk ip0, 2054 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_copy
  .p2align 4
  .type g_unix_mount_point_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_copy
#endif
g_unix_mount_point_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16440
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2055 & 0xffff
#if 2055 > 0xffff
  movk ip0, 2055 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_free
  .p2align 4
  .type g_unix_mount_point_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_free
#endif
g_unix_mount_point_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16448
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2056 & 0xffff
#if 2056 > 0xffff
  movk ip0, 2056 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_get_device_path
  .p2align 4
  .type g_unix_mount_point_get_device_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_get_device_path
#endif
g_unix_mount_point_get_device_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16456
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2057 & 0xffff
#if 2057 > 0xffff
  movk ip0, 2057 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_get_fs_type
  .p2align 4
  .type g_unix_mount_point_get_fs_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_get_fs_type
#endif
g_unix_mount_point_get_fs_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16464
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2058 & 0xffff
#if 2058 > 0xffff
  movk ip0, 2058 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_get_mount_path
  .p2align 4
  .type g_unix_mount_point_get_mount_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_get_mount_path
#endif
g_unix_mount_point_get_mount_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16472
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2059 & 0xffff
#if 2059 > 0xffff
  movk ip0, 2059 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_get_options
  .p2align 4
  .type g_unix_mount_point_get_options, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_get_options
#endif
g_unix_mount_point_get_options:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16480
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2060 & 0xffff
#if 2060 > 0xffff
  movk ip0, 2060 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_get_type
  .p2align 4
  .type g_unix_mount_point_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_get_type
#endif
g_unix_mount_point_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16488
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2061 & 0xffff
#if 2061 > 0xffff
  movk ip0, 2061 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_guess_can_eject
  .p2align 4
  .type g_unix_mount_point_guess_can_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_guess_can_eject
#endif
g_unix_mount_point_guess_can_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16496
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2062 & 0xffff
#if 2062 > 0xffff
  movk ip0, 2062 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_guess_icon
  .p2align 4
  .type g_unix_mount_point_guess_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_guess_icon
#endif
g_unix_mount_point_guess_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16504
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2063 & 0xffff
#if 2063 > 0xffff
  movk ip0, 2063 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_guess_name
  .p2align 4
  .type g_unix_mount_point_guess_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_guess_name
#endif
g_unix_mount_point_guess_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16512
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2064 & 0xffff
#if 2064 > 0xffff
  movk ip0, 2064 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_guess_symbolic_icon
  .p2align 4
  .type g_unix_mount_point_guess_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_guess_symbolic_icon
#endif
g_unix_mount_point_guess_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16520
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2065 & 0xffff
#if 2065 > 0xffff
  movk ip0, 2065 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_is_loopback
  .p2align 4
  .type g_unix_mount_point_is_loopback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_is_loopback
#endif
g_unix_mount_point_is_loopback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16528
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2066 & 0xffff
#if 2066 > 0xffff
  movk ip0, 2066 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_is_readonly
  .p2align 4
  .type g_unix_mount_point_is_readonly, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_is_readonly
#endif
g_unix_mount_point_is_readonly:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16536
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2067 & 0xffff
#if 2067 > 0xffff
  movk ip0, 2067 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_point_is_user_mountable
  .p2align 4
  .type g_unix_mount_point_is_user_mountable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_point_is_user_mountable
#endif
g_unix_mount_point_is_user_mountable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16544
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2068 & 0xffff
#if 2068 > 0xffff
  movk ip0, 2068 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_points_changed_since
  .p2align 4
  .type g_unix_mount_points_changed_since, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_points_changed_since
#endif
g_unix_mount_points_changed_since:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16552
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2069 & 0xffff
#if 2069 > 0xffff
  movk ip0, 2069 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_points_get
  .p2align 4
  .type g_unix_mount_points_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_points_get
#endif
g_unix_mount_points_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16560
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2070 & 0xffff
#if 2070 > 0xffff
  movk ip0, 2070 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mount_points_get_from_file
  .p2align 4
  .type g_unix_mount_points_get_from_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mount_points_get_from_file
#endif
g_unix_mount_points_get_from_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16568
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2071 & 0xffff
#if 2071 > 0xffff
  movk ip0, 2071 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mounts_changed_since
  .p2align 4
  .type g_unix_mounts_changed_since, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mounts_changed_since
#endif
g_unix_mounts_changed_since:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16576
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2072 & 0xffff
#if 2072 > 0xffff
  movk ip0, 2072 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mounts_get
  .p2align 4
  .type g_unix_mounts_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mounts_get
#endif
g_unix_mounts_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16584
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2073 & 0xffff
#if 2073 > 0xffff
  movk ip0, 2073 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_mounts_get_from_file
  .p2align 4
  .type g_unix_mounts_get_from_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_mounts_get_from_file
#endif
g_unix_mounts_get_from_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16592
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2074 & 0xffff
#if 2074 > 0xffff
  movk ip0, 2074 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_output_stream_get_close_fd
  .p2align 4
  .type g_unix_output_stream_get_close_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_output_stream_get_close_fd
#endif
g_unix_output_stream_get_close_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16600
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2075 & 0xffff
#if 2075 > 0xffff
  movk ip0, 2075 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_output_stream_get_fd
  .p2align 4
  .type g_unix_output_stream_get_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_output_stream_get_fd
#endif
g_unix_output_stream_get_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16608
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2076 & 0xffff
#if 2076 > 0xffff
  movk ip0, 2076 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_output_stream_get_type
  .p2align 4
  .type g_unix_output_stream_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_output_stream_get_type
#endif
g_unix_output_stream_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16616
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2077 & 0xffff
#if 2077 > 0xffff
  movk ip0, 2077 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_output_stream_new
  .p2align 4
  .type g_unix_output_stream_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_output_stream_new
#endif
g_unix_output_stream_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16624
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2078 & 0xffff
#if 2078 > 0xffff
  movk ip0, 2078 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_output_stream_set_close_fd
  .p2align 4
  .type g_unix_output_stream_set_close_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_output_stream_set_close_fd
#endif
g_unix_output_stream_set_close_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16632
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2079 & 0xffff
#if 2079 > 0xffff
  movk ip0, 2079 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_abstract_names_supported
  .p2align 4
  .type g_unix_socket_address_abstract_names_supported, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_abstract_names_supported
#endif
g_unix_socket_address_abstract_names_supported:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16640
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2080 & 0xffff
#if 2080 > 0xffff
  movk ip0, 2080 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_get_address_type
  .p2align 4
  .type g_unix_socket_address_get_address_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_get_address_type
#endif
g_unix_socket_address_get_address_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16648
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2081 & 0xffff
#if 2081 > 0xffff
  movk ip0, 2081 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_get_is_abstract
  .p2align 4
  .type g_unix_socket_address_get_is_abstract, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_get_is_abstract
#endif
g_unix_socket_address_get_is_abstract:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16656
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2082 & 0xffff
#if 2082 > 0xffff
  movk ip0, 2082 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_get_path
  .p2align 4
  .type g_unix_socket_address_get_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_get_path
#endif
g_unix_socket_address_get_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16664
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2083 & 0xffff
#if 2083 > 0xffff
  movk ip0, 2083 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_get_path_len
  .p2align 4
  .type g_unix_socket_address_get_path_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_get_path_len
#endif
g_unix_socket_address_get_path_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16672
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2084 & 0xffff
#if 2084 > 0xffff
  movk ip0, 2084 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_get_type
  .p2align 4
  .type g_unix_socket_address_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_get_type
#endif
g_unix_socket_address_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16680
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2085 & 0xffff
#if 2085 > 0xffff
  movk ip0, 2085 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_new
  .p2align 4
  .type g_unix_socket_address_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_new
#endif
g_unix_socket_address_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16688
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2086 & 0xffff
#if 2086 > 0xffff
  movk ip0, 2086 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_new_abstract
  .p2align 4
  .type g_unix_socket_address_new_abstract, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_new_abstract
#endif
g_unix_socket_address_new_abstract:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16696
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2087 & 0xffff
#if 2087 > 0xffff
  movk ip0, 2087 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_new_with_type
  .p2align 4
  .type g_unix_socket_address_new_with_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_new_with_type
#endif
g_unix_socket_address_new_with_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16704
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2088 & 0xffff
#if 2088 > 0xffff
  movk ip0, 2088 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_socket_address_type_get_type
  .p2align 4
  .type g_unix_socket_address_type_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_socket_address_type_get_type
#endif
g_unix_socket_address_type_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16712
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2089 & 0xffff
#if 2089 > 0xffff
  movk ip0, 2089 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_get_default
  .p2align 4
  .type g_vfs_get_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_get_default
#endif
g_vfs_get_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16720
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2090 & 0xffff
#if 2090 > 0xffff
  movk ip0, 2090 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_get_file_for_path
  .p2align 4
  .type g_vfs_get_file_for_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_get_file_for_path
#endif
g_vfs_get_file_for_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16728
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2091 & 0xffff
#if 2091 > 0xffff
  movk ip0, 2091 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_get_file_for_uri
  .p2align 4
  .type g_vfs_get_file_for_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_get_file_for_uri
#endif
g_vfs_get_file_for_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16736
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2092 & 0xffff
#if 2092 > 0xffff
  movk ip0, 2092 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_get_local
  .p2align 4
  .type g_vfs_get_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_get_local
#endif
g_vfs_get_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16744
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2093 & 0xffff
#if 2093 > 0xffff
  movk ip0, 2093 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_get_supported_uri_schemes
  .p2align 4
  .type g_vfs_get_supported_uri_schemes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_get_supported_uri_schemes
#endif
g_vfs_get_supported_uri_schemes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16752
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2094 & 0xffff
#if 2094 > 0xffff
  movk ip0, 2094 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_get_type
  .p2align 4
  .type g_vfs_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_get_type
#endif
g_vfs_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16760
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2095 & 0xffff
#if 2095 > 0xffff
  movk ip0, 2095 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_is_active
  .p2align 4
  .type g_vfs_is_active, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_is_active
#endif
g_vfs_is_active:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16768
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2096 & 0xffff
#if 2096 > 0xffff
  movk ip0, 2096 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_parse_name
  .p2align 4
  .type g_vfs_parse_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_parse_name
#endif
g_vfs_parse_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16776
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2097 & 0xffff
#if 2097 > 0xffff
  movk ip0, 2097 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_register_uri_scheme
  .p2align 4
  .type g_vfs_register_uri_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_register_uri_scheme
#endif
g_vfs_register_uri_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16784
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2098 & 0xffff
#if 2098 > 0xffff
  movk ip0, 2098 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfs_unregister_uri_scheme
  .p2align 4
  .type g_vfs_unregister_uri_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfs_unregister_uri_scheme
#endif
g_vfs_unregister_uri_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16792
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2099 & 0xffff
#if 2099 > 0xffff
  movk ip0, 2099 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_can_eject
  .p2align 4
  .type g_volume_can_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_can_eject
#endif
g_volume_can_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16800
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2100 & 0xffff
#if 2100 > 0xffff
  movk ip0, 2100 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_can_mount
  .p2align 4
  .type g_volume_can_mount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_can_mount
#endif
g_volume_can_mount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16808
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2101 & 0xffff
#if 2101 > 0xffff
  movk ip0, 2101 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_eject
  .p2align 4
  .type g_volume_eject, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_eject
#endif
g_volume_eject:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16816
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2102 & 0xffff
#if 2102 > 0xffff
  movk ip0, 2102 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_eject_finish
  .p2align 4
  .type g_volume_eject_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_eject_finish
#endif
g_volume_eject_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16824
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2103 & 0xffff
#if 2103 > 0xffff
  movk ip0, 2103 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_eject_with_operation
  .p2align 4
  .type g_volume_eject_with_operation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_eject_with_operation
#endif
g_volume_eject_with_operation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16832
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2104 & 0xffff
#if 2104 > 0xffff
  movk ip0, 2104 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_eject_with_operation_finish
  .p2align 4
  .type g_volume_eject_with_operation_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_eject_with_operation_finish
#endif
g_volume_eject_with_operation_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16840
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2105 & 0xffff
#if 2105 > 0xffff
  movk ip0, 2105 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_enumerate_identifiers
  .p2align 4
  .type g_volume_enumerate_identifiers, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_enumerate_identifiers
#endif
g_volume_enumerate_identifiers:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16848
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2106 & 0xffff
#if 2106 > 0xffff
  movk ip0, 2106 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_activation_root
  .p2align 4
  .type g_volume_get_activation_root, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_activation_root
#endif
g_volume_get_activation_root:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16856
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2107 & 0xffff
#if 2107 > 0xffff
  movk ip0, 2107 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_drive
  .p2align 4
  .type g_volume_get_drive, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_drive
#endif
g_volume_get_drive:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16864
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2108 & 0xffff
#if 2108 > 0xffff
  movk ip0, 2108 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_icon
  .p2align 4
  .type g_volume_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_icon
#endif
g_volume_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16872
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2109 & 0xffff
#if 2109 > 0xffff
  movk ip0, 2109 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_identifier
  .p2align 4
  .type g_volume_get_identifier, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_identifier
#endif
g_volume_get_identifier:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16880
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2110 & 0xffff
#if 2110 > 0xffff
  movk ip0, 2110 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_mount
  .p2align 4
  .type g_volume_get_mount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_mount
#endif
g_volume_get_mount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16888
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2111 & 0xffff
#if 2111 > 0xffff
  movk ip0, 2111 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_name
  .p2align 4
  .type g_volume_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_name
#endif
g_volume_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16896
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2112 & 0xffff
#if 2112 > 0xffff
  movk ip0, 2112 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_sort_key
  .p2align 4
  .type g_volume_get_sort_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_sort_key
#endif
g_volume_get_sort_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16904
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2113 & 0xffff
#if 2113 > 0xffff
  movk ip0, 2113 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_symbolic_icon
  .p2align 4
  .type g_volume_get_symbolic_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_symbolic_icon
#endif
g_volume_get_symbolic_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16912
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2114 & 0xffff
#if 2114 > 0xffff
  movk ip0, 2114 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_type
  .p2align 4
  .type g_volume_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_type
#endif
g_volume_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16920
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2115 & 0xffff
#if 2115 > 0xffff
  movk ip0, 2115 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_get_uuid
  .p2align 4
  .type g_volume_get_uuid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_get_uuid
#endif
g_volume_get_uuid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16928
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2116 & 0xffff
#if 2116 > 0xffff
  movk ip0, 2116 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_adopt_orphan_mount
  .p2align 4
  .type g_volume_monitor_adopt_orphan_mount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_adopt_orphan_mount
#endif
g_volume_monitor_adopt_orphan_mount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16936
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2117 & 0xffff
#if 2117 > 0xffff
  movk ip0, 2117 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_get
  .p2align 4
  .type g_volume_monitor_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_get
#endif
g_volume_monitor_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16944
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2118 & 0xffff
#if 2118 > 0xffff
  movk ip0, 2118 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_get_connected_drives
  .p2align 4
  .type g_volume_monitor_get_connected_drives, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_get_connected_drives
#endif
g_volume_monitor_get_connected_drives:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16952
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2119 & 0xffff
#if 2119 > 0xffff
  movk ip0, 2119 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_get_mount_for_uuid
  .p2align 4
  .type g_volume_monitor_get_mount_for_uuid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_get_mount_for_uuid
#endif
g_volume_monitor_get_mount_for_uuid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16960
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2120 & 0xffff
#if 2120 > 0xffff
  movk ip0, 2120 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_get_mounts
  .p2align 4
  .type g_volume_monitor_get_mounts, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_get_mounts
#endif
g_volume_monitor_get_mounts:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16968
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2121 & 0xffff
#if 2121 > 0xffff
  movk ip0, 2121 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_get_type
  .p2align 4
  .type g_volume_monitor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_get_type
#endif
g_volume_monitor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16976
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2122 & 0xffff
#if 2122 > 0xffff
  movk ip0, 2122 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_get_volume_for_uuid
  .p2align 4
  .type g_volume_monitor_get_volume_for_uuid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_get_volume_for_uuid
#endif
g_volume_monitor_get_volume_for_uuid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16984
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2123 & 0xffff
#if 2123 > 0xffff
  movk ip0, 2123 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_monitor_get_volumes
  .p2align 4
  .type g_volume_monitor_get_volumes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_monitor_get_volumes
#endif
g_volume_monitor_get_volumes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+16992
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+16992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2124 & 0xffff
#if 2124 > 0xffff
  movk ip0, 2124 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_mount
  .p2align 4
  .type g_volume_mount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_mount
#endif
g_volume_mount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17000
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2125 & 0xffff
#if 2125 > 0xffff
  movk ip0, 2125 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_mount_finish
  .p2align 4
  .type g_volume_mount_finish, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_mount_finish
#endif
g_volume_mount_finish:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17008
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2126 & 0xffff
#if 2126 > 0xffff
  movk ip0, 2126 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_volume_should_automount
  .p2align 4
  .type g_volume_should_automount, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_volume_should_automount
#endif
g_volume_should_automount:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17016
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2127 & 0xffff
#if 2127 > 0xffff
  movk ip0, 2127 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_compressor_format_get_type
  .p2align 4
  .type g_zlib_compressor_format_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_compressor_format_get_type
#endif
g_zlib_compressor_format_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17024
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2128 & 0xffff
#if 2128 > 0xffff
  movk ip0, 2128 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_compressor_get_file_info
  .p2align 4
  .type g_zlib_compressor_get_file_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_compressor_get_file_info
#endif
g_zlib_compressor_get_file_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17032
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2129 & 0xffff
#if 2129 > 0xffff
  movk ip0, 2129 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_compressor_get_type
  .p2align 4
  .type g_zlib_compressor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_compressor_get_type
#endif
g_zlib_compressor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17040
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2130 & 0xffff
#if 2130 > 0xffff
  movk ip0, 2130 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_compressor_new
  .p2align 4
  .type g_zlib_compressor_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_compressor_new
#endif
g_zlib_compressor_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17048
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2131 & 0xffff
#if 2131 > 0xffff
  movk ip0, 2131 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_compressor_set_file_info
  .p2align 4
  .type g_zlib_compressor_set_file_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_compressor_set_file_info
#endif
g_zlib_compressor_set_file_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17056
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2132 & 0xffff
#if 2132 > 0xffff
  movk ip0, 2132 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_decompressor_get_file_info
  .p2align 4
  .type g_zlib_decompressor_get_file_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_decompressor_get_file_info
#endif
g_zlib_decompressor_get_file_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17064
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2133 & 0xffff
#if 2133 > 0xffff
  movk ip0, 2133 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_decompressor_get_type
  .p2align 4
  .type g_zlib_decompressor_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_decompressor_get_type
#endif
g_zlib_decompressor_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17072
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2134 & 0xffff
#if 2134 > 0xffff
  movk ip0, 2134 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_zlib_decompressor_new
  .p2align 4
  .type g_zlib_decompressor_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_zlib_decompressor_new
#endif
g_zlib_decompressor_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libgio_2_0_so_tramp_table+17080
  ldr ip0, [ip0, #:lo12:_libgio_2_0_so_tramp_table+17080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2135 & 0xffff
#if 2135 > 0xffff
  movk ip0, 2135 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libgio_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

