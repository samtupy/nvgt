/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

#define lr x30
#define ip0 x16

  .section .note.GNU-stack,"",@progbits

  .data

  .globl _libglib_2_0_so_tramp_table
  .hidden _libglib_2_0_so_tramp_table
  .align 8
_libglib_2_0_so_tramp_table:
  .zero 14848

  .text

  .globl _libglib_2_0_so_tramp_resolve
  .hidden _libglib_2_0_so_tramp_resolve

  .globl _libglib_2_0_so_save_regs_and_resolve
  .hidden _libglib_2_0_so_save_regs_and_resolve
  .type _libglib_2_0_so_save_regs_and_resolve, %function
_libglib_2_0_so_save_regs_and_resolve:
  .cfi_startproc

  // Slow path which calls dlsym, taken only on first call.
  // Registers are saved according to "Procedure Call Standard for the Arm® 64-bit Architecture".
  // For DWARF directives, read https://www.imperialviolet.org/2017/01/18/cfi.html.

  // Stack is aligned at 16 bytes

#define PUSH_PAIR(reg1, reg2) stp reg1, reg2, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset reg1, 0; .cfi_rel_offset reg2, 8
#define POP_PAIR(reg1, reg2) ldp reg1, reg2, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore reg2; .cfi_restore reg1

#define PUSH_WIDE_PAIR(reg1, reg2) stp reg1, reg2, [sp, #-32]!; .cfi_adjust_cfa_offset 32; .cfi_rel_offset reg1, 0; .cfi_rel_offset reg2, 16
#define POP_WIDE_PAIR(reg1, reg2) ldp reg1, reg2, [sp], #32; .cfi_adjust_cfa_offset -32; .cfi_restore reg2; .cfi_restore reg1

  // Save only arguments (and lr)
  PUSH_PAIR(x0, x1)
  PUSH_PAIR(x2, x3)
  PUSH_PAIR(x4, x5)
  PUSH_PAIR(x6, x7)
  PUSH_PAIR(x8, lr)

  ldr x0, [sp, #80]  // 16*5

  PUSH_WIDE_PAIR(q0, q1)
  PUSH_WIDE_PAIR(q2, q3)
  PUSH_WIDE_PAIR(q4, q5)
  PUSH_WIDE_PAIR(q6, q7)

  // Stack is aligned at 16 bytes

  bl _libglib_2_0_so_tramp_resolve
  mov ip0, x0

  // TODO: pop pc?

  POP_WIDE_PAIR(q6, q7)
  POP_WIDE_PAIR(q4, q5)
  POP_WIDE_PAIR(q2, q3)
  POP_WIDE_PAIR(q0, q1)

  POP_PAIR(x8, lr)
  POP_PAIR(x6, x7)
  POP_PAIR(x4, x5)
  POP_PAIR(x2, x3)
  POP_PAIR(x0, x1)

  br lr

  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_access
  .p2align 4
  .type g_access, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_access
#endif
g_access:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+0
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+0]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 0 & 0xffff
#if 0 > 0xffff
  movk ip0, 0 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_aligned_alloc
  .p2align 4
  .type g_aligned_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_aligned_alloc
#endif
g_aligned_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1 & 0xffff
#if 1 > 0xffff
  movk ip0, 1 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_aligned_alloc0
  .p2align 4
  .type g_aligned_alloc0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_aligned_alloc0
#endif
g_aligned_alloc0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+16
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+16]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 2 & 0xffff
#if 2 > 0xffff
  movk ip0, 2 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_aligned_free
  .p2align 4
  .type g_aligned_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_aligned_free
#endif
g_aligned_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+24
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+24]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 3 & 0xffff
#if 3 > 0xffff
  movk ip0, 3 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_aligned_free_sized
  .p2align 4
  .type g_aligned_free_sized, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_aligned_free_sized
#endif
g_aligned_free_sized:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+32
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+32]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 4 & 0xffff
#if 4 > 0xffff
  movk ip0, 4 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_allocator_free
  .p2align 4
  .type g_allocator_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_allocator_free
#endif
g_allocator_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+40
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+40]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 5 & 0xffff
#if 5 > 0xffff
  movk ip0, 5 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_allocator_new
  .p2align 4
  .type g_allocator_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_allocator_new
#endif
g_allocator_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+48
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+48]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 6 & 0xffff
#if 6 > 0xffff
  movk ip0, 6 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_append_vals
  .p2align 4
  .type g_array_append_vals, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_append_vals
#endif
g_array_append_vals:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+56
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+56]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 7 & 0xffff
#if 7 > 0xffff
  movk ip0, 7 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_binary_search
  .p2align 4
  .type g_array_binary_search, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_binary_search
#endif
g_array_binary_search:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+64
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+64]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 8 & 0xffff
#if 8 > 0xffff
  movk ip0, 8 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_copy
  .p2align 4
  .type g_array_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_copy
#endif
g_array_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+72
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+72]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 9 & 0xffff
#if 9 > 0xffff
  movk ip0, 9 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_free
  .p2align 4
  .type g_array_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_free
#endif
g_array_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+80
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+80]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 10 & 0xffff
#if 10 > 0xffff
  movk ip0, 10 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_get_element_size
  .p2align 4
  .type g_array_get_element_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_get_element_size
#endif
g_array_get_element_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+88
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+88]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 11 & 0xffff
#if 11 > 0xffff
  movk ip0, 11 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_insert_vals
  .p2align 4
  .type g_array_insert_vals, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_insert_vals
#endif
g_array_insert_vals:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+96
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+96]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 12 & 0xffff
#if 12 > 0xffff
  movk ip0, 12 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_new
  .p2align 4
  .type g_array_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_new
#endif
g_array_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 13 & 0xffff
#if 13 > 0xffff
  movk ip0, 13 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_new_take
  .p2align 4
  .type g_array_new_take, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_new_take
#endif
g_array_new_take:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 14 & 0xffff
#if 14 > 0xffff
  movk ip0, 14 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_new_take_zero_terminated
  .p2align 4
  .type g_array_new_take_zero_terminated, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_new_take_zero_terminated
#endif
g_array_new_take_zero_terminated:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 15 & 0xffff
#if 15 > 0xffff
  movk ip0, 15 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_prepend_vals
  .p2align 4
  .type g_array_prepend_vals, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_prepend_vals
#endif
g_array_prepend_vals:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 16 & 0xffff
#if 16 > 0xffff
  movk ip0, 16 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_ref
  .p2align 4
  .type g_array_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_ref
#endif
g_array_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 17 & 0xffff
#if 17 > 0xffff
  movk ip0, 17 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_remove_index
  .p2align 4
  .type g_array_remove_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_remove_index
#endif
g_array_remove_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 18 & 0xffff
#if 18 > 0xffff
  movk ip0, 18 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_remove_index_fast
  .p2align 4
  .type g_array_remove_index_fast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_remove_index_fast
#endif
g_array_remove_index_fast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 19 & 0xffff
#if 19 > 0xffff
  movk ip0, 19 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_remove_range
  .p2align 4
  .type g_array_remove_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_remove_range
#endif
g_array_remove_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 20 & 0xffff
#if 20 > 0xffff
  movk ip0, 20 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_set_clear_func
  .p2align 4
  .type g_array_set_clear_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_set_clear_func
#endif
g_array_set_clear_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 21 & 0xffff
#if 21 > 0xffff
  movk ip0, 21 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_set_size
  .p2align 4
  .type g_array_set_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_set_size
#endif
g_array_set_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 22 & 0xffff
#if 22 > 0xffff
  movk ip0, 22 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_sized_new
  .p2align 4
  .type g_array_sized_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_sized_new
#endif
g_array_sized_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 23 & 0xffff
#if 23 > 0xffff
  movk ip0, 23 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_sort
  .p2align 4
  .type g_array_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_sort
#endif
g_array_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 24 & 0xffff
#if 24 > 0xffff
  movk ip0, 24 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_sort_with_data
  .p2align 4
  .type g_array_sort_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_sort_with_data
#endif
g_array_sort_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 25 & 0xffff
#if 25 > 0xffff
  movk ip0, 25 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_steal
  .p2align 4
  .type g_array_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_steal
#endif
g_array_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 26 & 0xffff
#if 26 > 0xffff
  movk ip0, 26 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_array_unref
  .p2align 4
  .type g_array_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_array_unref
#endif
g_array_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 27 & 0xffff
#if 27 > 0xffff
  movk ip0, 27 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_digit_value
  .p2align 4
  .type g_ascii_digit_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_digit_value
#endif
g_ascii_digit_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 28 & 0xffff
#if 28 > 0xffff
  movk ip0, 28 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_dtostr
  .p2align 4
  .type g_ascii_dtostr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_dtostr
#endif
g_ascii_dtostr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 29 & 0xffff
#if 29 > 0xffff
  movk ip0, 29 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_formatd
  .p2align 4
  .type g_ascii_formatd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_formatd
#endif
g_ascii_formatd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 30 & 0xffff
#if 30 > 0xffff
  movk ip0, 30 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_strcasecmp
  .p2align 4
  .type g_ascii_strcasecmp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_strcasecmp
#endif
g_ascii_strcasecmp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 31 & 0xffff
#if 31 > 0xffff
  movk ip0, 31 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_strdown
  .p2align 4
  .type g_ascii_strdown, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_strdown
#endif
g_ascii_strdown:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 32 & 0xffff
#if 32 > 0xffff
  movk ip0, 32 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_string_to_signed
  .p2align 4
  .type g_ascii_string_to_signed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_string_to_signed
#endif
g_ascii_string_to_signed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 33 & 0xffff
#if 33 > 0xffff
  movk ip0, 33 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_string_to_unsigned
  .p2align 4
  .type g_ascii_string_to_unsigned, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_string_to_unsigned
#endif
g_ascii_string_to_unsigned:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 34 & 0xffff
#if 34 > 0xffff
  movk ip0, 34 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_strncasecmp
  .p2align 4
  .type g_ascii_strncasecmp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_strncasecmp
#endif
g_ascii_strncasecmp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 35 & 0xffff
#if 35 > 0xffff
  movk ip0, 35 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_strtod
  .p2align 4
  .type g_ascii_strtod, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_strtod
#endif
g_ascii_strtod:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 36 & 0xffff
#if 36 > 0xffff
  movk ip0, 36 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_strtoll
  .p2align 4
  .type g_ascii_strtoll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_strtoll
#endif
g_ascii_strtoll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 37 & 0xffff
#if 37 > 0xffff
  movk ip0, 37 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_strtoull
  .p2align 4
  .type g_ascii_strtoull, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_strtoull
#endif
g_ascii_strtoull:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 38 & 0xffff
#if 38 > 0xffff
  movk ip0, 38 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_strup
  .p2align 4
  .type g_ascii_strup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_strup
#endif
g_ascii_strup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 39 & 0xffff
#if 39 > 0xffff
  movk ip0, 39 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_tolower
  .p2align 4
  .type g_ascii_tolower, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_tolower
#endif
g_ascii_tolower:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 40 & 0xffff
#if 40 > 0xffff
  movk ip0, 40 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_toupper
  .p2align 4
  .type g_ascii_toupper, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_toupper
#endif
g_ascii_toupper:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 41 & 0xffff
#if 41 > 0xffff
  movk ip0, 41 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ascii_xdigit_value
  .p2align 4
  .type g_ascii_xdigit_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ascii_xdigit_value
#endif
g_ascii_xdigit_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 42 & 0xffff
#if 42 > 0xffff
  movk ip0, 42 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assert_warning
  .p2align 4
  .type g_assert_warning, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assert_warning
#endif
g_assert_warning:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 43 & 0xffff
#if 43 > 0xffff
  movk ip0, 43 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assertion_message
  .p2align 4
  .type g_assertion_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assertion_message
#endif
g_assertion_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 44 & 0xffff
#if 44 > 0xffff
  movk ip0, 44 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assertion_message_cmpint
  .p2align 4
  .type g_assertion_message_cmpint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assertion_message_cmpint
#endif
g_assertion_message_cmpint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 45 & 0xffff
#if 45 > 0xffff
  movk ip0, 45 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assertion_message_cmpnum
  .p2align 4
  .type g_assertion_message_cmpnum, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assertion_message_cmpnum
#endif
g_assertion_message_cmpnum:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 46 & 0xffff
#if 46 > 0xffff
  movk ip0, 46 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assertion_message_cmpstr
  .p2align 4
  .type g_assertion_message_cmpstr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assertion_message_cmpstr
#endif
g_assertion_message_cmpstr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 47 & 0xffff
#if 47 > 0xffff
  movk ip0, 47 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assertion_message_cmpstrv
  .p2align 4
  .type g_assertion_message_cmpstrv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assertion_message_cmpstrv
#endif
g_assertion_message_cmpstrv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 48 & 0xffff
#if 48 > 0xffff
  movk ip0, 48 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assertion_message_error
  .p2align 4
  .type g_assertion_message_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assertion_message_error
#endif
g_assertion_message_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 49 & 0xffff
#if 49 > 0xffff
  movk ip0, 49 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_assertion_message_expr
  .p2align 4
  .type g_assertion_message_expr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_assertion_message_expr
#endif
g_assertion_message_expr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 50 & 0xffff
#if 50 > 0xffff
  movk ip0, 50 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_length
  .p2align 4
  .type g_async_queue_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_length
#endif
g_async_queue_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 51 & 0xffff
#if 51 > 0xffff
  movk ip0, 51 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_length_unlocked
  .p2align 4
  .type g_async_queue_length_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_length_unlocked
#endif
g_async_queue_length_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 52 & 0xffff
#if 52 > 0xffff
  movk ip0, 52 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_lock
  .p2align 4
  .type g_async_queue_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_lock
#endif
g_async_queue_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 53 & 0xffff
#if 53 > 0xffff
  movk ip0, 53 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_new
  .p2align 4
  .type g_async_queue_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_new
#endif
g_async_queue_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 54 & 0xffff
#if 54 > 0xffff
  movk ip0, 54 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_new_full
  .p2align 4
  .type g_async_queue_new_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_new_full
#endif
g_async_queue_new_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 55 & 0xffff
#if 55 > 0xffff
  movk ip0, 55 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_pop
  .p2align 4
  .type g_async_queue_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_pop
#endif
g_async_queue_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 56 & 0xffff
#if 56 > 0xffff
  movk ip0, 56 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_pop_unlocked
  .p2align 4
  .type g_async_queue_pop_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_pop_unlocked
#endif
g_async_queue_pop_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 57 & 0xffff
#if 57 > 0xffff
  movk ip0, 57 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_push
  .p2align 4
  .type g_async_queue_push, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_push
#endif
g_async_queue_push:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 58 & 0xffff
#if 58 > 0xffff
  movk ip0, 58 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_push_front
  .p2align 4
  .type g_async_queue_push_front, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_push_front
#endif
g_async_queue_push_front:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 59 & 0xffff
#if 59 > 0xffff
  movk ip0, 59 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_push_front_unlocked
  .p2align 4
  .type g_async_queue_push_front_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_push_front_unlocked
#endif
g_async_queue_push_front_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 60 & 0xffff
#if 60 > 0xffff
  movk ip0, 60 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_push_sorted
  .p2align 4
  .type g_async_queue_push_sorted, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_push_sorted
#endif
g_async_queue_push_sorted:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 61 & 0xffff
#if 61 > 0xffff
  movk ip0, 61 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_push_sorted_unlocked
  .p2align 4
  .type g_async_queue_push_sorted_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_push_sorted_unlocked
#endif
g_async_queue_push_sorted_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 62 & 0xffff
#if 62 > 0xffff
  movk ip0, 62 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_push_unlocked
  .p2align 4
  .type g_async_queue_push_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_push_unlocked
#endif
g_async_queue_push_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 63 & 0xffff
#if 63 > 0xffff
  movk ip0, 63 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_ref
  .p2align 4
  .type g_async_queue_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_ref
#endif
g_async_queue_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 64 & 0xffff
#if 64 > 0xffff
  movk ip0, 64 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_ref_unlocked
  .p2align 4
  .type g_async_queue_ref_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_ref_unlocked
#endif
g_async_queue_ref_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 65 & 0xffff
#if 65 > 0xffff
  movk ip0, 65 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_remove
  .p2align 4
  .type g_async_queue_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_remove
#endif
g_async_queue_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 66 & 0xffff
#if 66 > 0xffff
  movk ip0, 66 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_remove_unlocked
  .p2align 4
  .type g_async_queue_remove_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_remove_unlocked
#endif
g_async_queue_remove_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 67 & 0xffff
#if 67 > 0xffff
  movk ip0, 67 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_sort
  .p2align 4
  .type g_async_queue_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_sort
#endif
g_async_queue_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 68 & 0xffff
#if 68 > 0xffff
  movk ip0, 68 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_sort_unlocked
  .p2align 4
  .type g_async_queue_sort_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_sort_unlocked
#endif
g_async_queue_sort_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 69 & 0xffff
#if 69 > 0xffff
  movk ip0, 69 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_timed_pop
  .p2align 4
  .type g_async_queue_timed_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_timed_pop
#endif
g_async_queue_timed_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 70 & 0xffff
#if 70 > 0xffff
  movk ip0, 70 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_timed_pop_unlocked
  .p2align 4
  .type g_async_queue_timed_pop_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_timed_pop_unlocked
#endif
g_async_queue_timed_pop_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 71 & 0xffff
#if 71 > 0xffff
  movk ip0, 71 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_timeout_pop
  .p2align 4
  .type g_async_queue_timeout_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_timeout_pop
#endif
g_async_queue_timeout_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 72 & 0xffff
#if 72 > 0xffff
  movk ip0, 72 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_timeout_pop_unlocked
  .p2align 4
  .type g_async_queue_timeout_pop_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_timeout_pop_unlocked
#endif
g_async_queue_timeout_pop_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 73 & 0xffff
#if 73 > 0xffff
  movk ip0, 73 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_try_pop
  .p2align 4
  .type g_async_queue_try_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_try_pop
#endif
g_async_queue_try_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 74 & 0xffff
#if 74 > 0xffff
  movk ip0, 74 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_try_pop_unlocked
  .p2align 4
  .type g_async_queue_try_pop_unlocked, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_try_pop_unlocked
#endif
g_async_queue_try_pop_unlocked:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 75 & 0xffff
#if 75 > 0xffff
  movk ip0, 75 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_unlock
  .p2align 4
  .type g_async_queue_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_unlock
#endif
g_async_queue_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 76 & 0xffff
#if 76 > 0xffff
  movk ip0, 76 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_unref
  .p2align 4
  .type g_async_queue_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_unref
#endif
g_async_queue_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 77 & 0xffff
#if 77 > 0xffff
  movk ip0, 77 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_async_queue_unref_and_unlock
  .p2align 4
  .type g_async_queue_unref_and_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_async_queue_unref_and_unlock
#endif
g_async_queue_unref_and_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 78 & 0xffff
#if 78 > 0xffff
  movk ip0, 78 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atexit
  .p2align 4
  .type g_atexit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atexit
#endif
g_atexit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 79 & 0xffff
#if 79 > 0xffff
  movk ip0, 79 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_add
  .p2align 4
  .type g_atomic_int_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_add
#endif
g_atomic_int_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 80 & 0xffff
#if 80 > 0xffff
  movk ip0, 80 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_and
  .p2align 4
  .type g_atomic_int_and, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_and
#endif
g_atomic_int_and:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 81 & 0xffff
#if 81 > 0xffff
  movk ip0, 81 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_compare_and_exchange
  .p2align 4
  .type g_atomic_int_compare_and_exchange, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_compare_and_exchange
#endif
g_atomic_int_compare_and_exchange:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 82 & 0xffff
#if 82 > 0xffff
  movk ip0, 82 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_compare_and_exchange_full
  .p2align 4
  .type g_atomic_int_compare_and_exchange_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_compare_and_exchange_full
#endif
g_atomic_int_compare_and_exchange_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 83 & 0xffff
#if 83 > 0xffff
  movk ip0, 83 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_dec_and_test
  .p2align 4
  .type g_atomic_int_dec_and_test, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_dec_and_test
#endif
g_atomic_int_dec_and_test:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 84 & 0xffff
#if 84 > 0xffff
  movk ip0, 84 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_exchange
  .p2align 4
  .type g_atomic_int_exchange, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_exchange
#endif
g_atomic_int_exchange:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 85 & 0xffff
#if 85 > 0xffff
  movk ip0, 85 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_exchange_and_add
  .p2align 4
  .type g_atomic_int_exchange_and_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_exchange_and_add
#endif
g_atomic_int_exchange_and_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 86 & 0xffff
#if 86 > 0xffff
  movk ip0, 86 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_get
  .p2align 4
  .type g_atomic_int_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_get
#endif
g_atomic_int_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 87 & 0xffff
#if 87 > 0xffff
  movk ip0, 87 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_inc
  .p2align 4
  .type g_atomic_int_inc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_inc
#endif
g_atomic_int_inc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 88 & 0xffff
#if 88 > 0xffff
  movk ip0, 88 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_or
  .p2align 4
  .type g_atomic_int_or, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_or
#endif
g_atomic_int_or:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 89 & 0xffff
#if 89 > 0xffff
  movk ip0, 89 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_set
  .p2align 4
  .type g_atomic_int_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_set
#endif
g_atomic_int_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 90 & 0xffff
#if 90 > 0xffff
  movk ip0, 90 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_int_xor
  .p2align 4
  .type g_atomic_int_xor, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_int_xor
#endif
g_atomic_int_xor:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 91 & 0xffff
#if 91 > 0xffff
  movk ip0, 91 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_add
  .p2align 4
  .type g_atomic_pointer_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_add
#endif
g_atomic_pointer_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 92 & 0xffff
#if 92 > 0xffff
  movk ip0, 92 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_and
  .p2align 4
  .type g_atomic_pointer_and, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_and
#endif
g_atomic_pointer_and:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 93 & 0xffff
#if 93 > 0xffff
  movk ip0, 93 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_compare_and_exchange
  .p2align 4
  .type g_atomic_pointer_compare_and_exchange, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_compare_and_exchange
#endif
g_atomic_pointer_compare_and_exchange:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 94 & 0xffff
#if 94 > 0xffff
  movk ip0, 94 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_compare_and_exchange_full
  .p2align 4
  .type g_atomic_pointer_compare_and_exchange_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_compare_and_exchange_full
#endif
g_atomic_pointer_compare_and_exchange_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 95 & 0xffff
#if 95 > 0xffff
  movk ip0, 95 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_exchange
  .p2align 4
  .type g_atomic_pointer_exchange, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_exchange
#endif
g_atomic_pointer_exchange:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 96 & 0xffff
#if 96 > 0xffff
  movk ip0, 96 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_get
  .p2align 4
  .type g_atomic_pointer_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_get
#endif
g_atomic_pointer_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 97 & 0xffff
#if 97 > 0xffff
  movk ip0, 97 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_or
  .p2align 4
  .type g_atomic_pointer_or, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_or
#endif
g_atomic_pointer_or:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 98 & 0xffff
#if 98 > 0xffff
  movk ip0, 98 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_set
  .p2align 4
  .type g_atomic_pointer_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_set
#endif
g_atomic_pointer_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 99 & 0xffff
#if 99 > 0xffff
  movk ip0, 99 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_pointer_xor
  .p2align 4
  .type g_atomic_pointer_xor, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_pointer_xor
#endif
g_atomic_pointer_xor:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 100 & 0xffff
#if 100 > 0xffff
  movk ip0, 100 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_rc_box_acquire
  .p2align 4
  .type g_atomic_rc_box_acquire, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_rc_box_acquire
#endif
g_atomic_rc_box_acquire:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 101 & 0xffff
#if 101 > 0xffff
  movk ip0, 101 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_rc_box_alloc
  .p2align 4
  .type g_atomic_rc_box_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_rc_box_alloc
#endif
g_atomic_rc_box_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 102 & 0xffff
#if 102 > 0xffff
  movk ip0, 102 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_rc_box_alloc0
  .p2align 4
  .type g_atomic_rc_box_alloc0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_rc_box_alloc0
#endif
g_atomic_rc_box_alloc0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 103 & 0xffff
#if 103 > 0xffff
  movk ip0, 103 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_rc_box_dup
  .p2align 4
  .type g_atomic_rc_box_dup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_rc_box_dup
#endif
g_atomic_rc_box_dup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 104 & 0xffff
#if 104 > 0xffff
  movk ip0, 104 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_rc_box_get_size
  .p2align 4
  .type g_atomic_rc_box_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_rc_box_get_size
#endif
g_atomic_rc_box_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 105 & 0xffff
#if 105 > 0xffff
  movk ip0, 105 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_rc_box_release
  .p2align 4
  .type g_atomic_rc_box_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_rc_box_release
#endif
g_atomic_rc_box_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 106 & 0xffff
#if 106 > 0xffff
  movk ip0, 106 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_rc_box_release_full
  .p2align 4
  .type g_atomic_rc_box_release_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_rc_box_release_full
#endif
g_atomic_rc_box_release_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 107 & 0xffff
#if 107 > 0xffff
  movk ip0, 107 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_ref_count_compare
  .p2align 4
  .type g_atomic_ref_count_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_ref_count_compare
#endif
g_atomic_ref_count_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 108 & 0xffff
#if 108 > 0xffff
  movk ip0, 108 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_ref_count_dec
  .p2align 4
  .type g_atomic_ref_count_dec, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_ref_count_dec
#endif
g_atomic_ref_count_dec:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 109 & 0xffff
#if 109 > 0xffff
  movk ip0, 109 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_ref_count_inc
  .p2align 4
  .type g_atomic_ref_count_inc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_ref_count_inc
#endif
g_atomic_ref_count_inc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 110 & 0xffff
#if 110 > 0xffff
  movk ip0, 110 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_atomic_ref_count_init
  .p2align 4
  .type g_atomic_ref_count_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_atomic_ref_count_init
#endif
g_atomic_ref_count_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 111 & 0xffff
#if 111 > 0xffff
  movk ip0, 111 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_base64_decode
  .p2align 4
  .type g_base64_decode, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_base64_decode
#endif
g_base64_decode:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 112 & 0xffff
#if 112 > 0xffff
  movk ip0, 112 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_base64_decode_inplace
  .p2align 4
  .type g_base64_decode_inplace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_base64_decode_inplace
#endif
g_base64_decode_inplace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 113 & 0xffff
#if 113 > 0xffff
  movk ip0, 113 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_base64_decode_step
  .p2align 4
  .type g_base64_decode_step, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_base64_decode_step
#endif
g_base64_decode_step:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 114 & 0xffff
#if 114 > 0xffff
  movk ip0, 114 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_base64_encode
  .p2align 4
  .type g_base64_encode, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_base64_encode
#endif
g_base64_encode:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 115 & 0xffff
#if 115 > 0xffff
  movk ip0, 115 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_base64_encode_close
  .p2align 4
  .type g_base64_encode_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_base64_encode_close
#endif
g_base64_encode_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 116 & 0xffff
#if 116 > 0xffff
  movk ip0, 116 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_base64_encode_step
  .p2align 4
  .type g_base64_encode_step, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_base64_encode_step
#endif
g_base64_encode_step:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 117 & 0xffff
#if 117 > 0xffff
  movk ip0, 117 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_basename
  .p2align 4
  .type g_basename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_basename
#endif
g_basename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 118 & 0xffff
#if 118 > 0xffff
  movk ip0, 118 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bit_lock
  .p2align 4
  .type g_bit_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bit_lock
#endif
g_bit_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 119 & 0xffff
#if 119 > 0xffff
  movk ip0, 119 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bit_nth_lsf
  .p2align 4
  .type g_bit_nth_lsf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bit_nth_lsf
#endif
g_bit_nth_lsf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 120 & 0xffff
#if 120 > 0xffff
  movk ip0, 120 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bit_nth_msf
  .p2align 4
  .type g_bit_nth_msf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bit_nth_msf
#endif
g_bit_nth_msf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 121 & 0xffff
#if 121 > 0xffff
  movk ip0, 121 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bit_storage
  .p2align 4
  .type g_bit_storage, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bit_storage
#endif
g_bit_storage:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 122 & 0xffff
#if 122 > 0xffff
  movk ip0, 122 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bit_trylock
  .p2align 4
  .type g_bit_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bit_trylock
#endif
g_bit_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 123 & 0xffff
#if 123 > 0xffff
  movk ip0, 123 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bit_unlock
  .p2align 4
  .type g_bit_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bit_unlock
#endif
g_bit_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 124 & 0xffff
#if 124 > 0xffff
  movk ip0, 124 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_blow_chunks
  .p2align 4
  .type g_blow_chunks, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_blow_chunks
#endif
g_blow_chunks:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 125 & 0xffff
#if 125 > 0xffff
  movk ip0, 125 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_add_application
  .p2align 4
  .type g_bookmark_file_add_application, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_add_application
#endif
g_bookmark_file_add_application:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 126 & 0xffff
#if 126 > 0xffff
  movk ip0, 126 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_add_group
  .p2align 4
  .type g_bookmark_file_add_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_add_group
#endif
g_bookmark_file_add_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 127 & 0xffff
#if 127 > 0xffff
  movk ip0, 127 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_copy
  .p2align 4
  .type g_bookmark_file_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_copy
#endif
g_bookmark_file_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 128 & 0xffff
#if 128 > 0xffff
  movk ip0, 128 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_error_quark
  .p2align 4
  .type g_bookmark_file_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_error_quark
#endif
g_bookmark_file_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 129 & 0xffff
#if 129 > 0xffff
  movk ip0, 129 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_free
  .p2align 4
  .type g_bookmark_file_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_free
#endif
g_bookmark_file_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 130 & 0xffff
#if 130 > 0xffff
  movk ip0, 130 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_added
  .p2align 4
  .type g_bookmark_file_get_added, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_added
#endif
g_bookmark_file_get_added:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 131 & 0xffff
#if 131 > 0xffff
  movk ip0, 131 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_added_date_time
  .p2align 4
  .type g_bookmark_file_get_added_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_added_date_time
#endif
g_bookmark_file_get_added_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 132 & 0xffff
#if 132 > 0xffff
  movk ip0, 132 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_app_info
  .p2align 4
  .type g_bookmark_file_get_app_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_app_info
#endif
g_bookmark_file_get_app_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 133 & 0xffff
#if 133 > 0xffff
  movk ip0, 133 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_application_info
  .p2align 4
  .type g_bookmark_file_get_application_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_application_info
#endif
g_bookmark_file_get_application_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 134 & 0xffff
#if 134 > 0xffff
  movk ip0, 134 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_applications
  .p2align 4
  .type g_bookmark_file_get_applications, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_applications
#endif
g_bookmark_file_get_applications:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 135 & 0xffff
#if 135 > 0xffff
  movk ip0, 135 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_description
  .p2align 4
  .type g_bookmark_file_get_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_description
#endif
g_bookmark_file_get_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 136 & 0xffff
#if 136 > 0xffff
  movk ip0, 136 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_groups
  .p2align 4
  .type g_bookmark_file_get_groups, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_groups
#endif
g_bookmark_file_get_groups:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 137 & 0xffff
#if 137 > 0xffff
  movk ip0, 137 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_icon
  .p2align 4
  .type g_bookmark_file_get_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_icon
#endif
g_bookmark_file_get_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 138 & 0xffff
#if 138 > 0xffff
  movk ip0, 138 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_is_private
  .p2align 4
  .type g_bookmark_file_get_is_private, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_is_private
#endif
g_bookmark_file_get_is_private:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 139 & 0xffff
#if 139 > 0xffff
  movk ip0, 139 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_mime_type
  .p2align 4
  .type g_bookmark_file_get_mime_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_mime_type
#endif
g_bookmark_file_get_mime_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 140 & 0xffff
#if 140 > 0xffff
  movk ip0, 140 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_modified
  .p2align 4
  .type g_bookmark_file_get_modified, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_modified
#endif
g_bookmark_file_get_modified:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 141 & 0xffff
#if 141 > 0xffff
  movk ip0, 141 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_modified_date_time
  .p2align 4
  .type g_bookmark_file_get_modified_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_modified_date_time
#endif
g_bookmark_file_get_modified_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 142 & 0xffff
#if 142 > 0xffff
  movk ip0, 142 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_size
  .p2align 4
  .type g_bookmark_file_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_size
#endif
g_bookmark_file_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 143 & 0xffff
#if 143 > 0xffff
  movk ip0, 143 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_title
  .p2align 4
  .type g_bookmark_file_get_title, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_title
#endif
g_bookmark_file_get_title:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 144 & 0xffff
#if 144 > 0xffff
  movk ip0, 144 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_uris
  .p2align 4
  .type g_bookmark_file_get_uris, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_uris
#endif
g_bookmark_file_get_uris:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 145 & 0xffff
#if 145 > 0xffff
  movk ip0, 145 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_visited
  .p2align 4
  .type g_bookmark_file_get_visited, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_visited
#endif
g_bookmark_file_get_visited:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 146 & 0xffff
#if 146 > 0xffff
  movk ip0, 146 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_get_visited_date_time
  .p2align 4
  .type g_bookmark_file_get_visited_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_get_visited_date_time
#endif
g_bookmark_file_get_visited_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 147 & 0xffff
#if 147 > 0xffff
  movk ip0, 147 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_has_application
  .p2align 4
  .type g_bookmark_file_has_application, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_has_application
#endif
g_bookmark_file_has_application:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 148 & 0xffff
#if 148 > 0xffff
  movk ip0, 148 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_has_group
  .p2align 4
  .type g_bookmark_file_has_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_has_group
#endif
g_bookmark_file_has_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 149 & 0xffff
#if 149 > 0xffff
  movk ip0, 149 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_has_item
  .p2align 4
  .type g_bookmark_file_has_item, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_has_item
#endif
g_bookmark_file_has_item:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 150 & 0xffff
#if 150 > 0xffff
  movk ip0, 150 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_load_from_data
  .p2align 4
  .type g_bookmark_file_load_from_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_load_from_data
#endif
g_bookmark_file_load_from_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 151 & 0xffff
#if 151 > 0xffff
  movk ip0, 151 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_load_from_data_dirs
  .p2align 4
  .type g_bookmark_file_load_from_data_dirs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_load_from_data_dirs
#endif
g_bookmark_file_load_from_data_dirs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 152 & 0xffff
#if 152 > 0xffff
  movk ip0, 152 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_load_from_file
  .p2align 4
  .type g_bookmark_file_load_from_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_load_from_file
#endif
g_bookmark_file_load_from_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 153 & 0xffff
#if 153 > 0xffff
  movk ip0, 153 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_move_item
  .p2align 4
  .type g_bookmark_file_move_item, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_move_item
#endif
g_bookmark_file_move_item:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 154 & 0xffff
#if 154 > 0xffff
  movk ip0, 154 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_new
  .p2align 4
  .type g_bookmark_file_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_new
#endif
g_bookmark_file_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 155 & 0xffff
#if 155 > 0xffff
  movk ip0, 155 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_remove_application
  .p2align 4
  .type g_bookmark_file_remove_application, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_remove_application
#endif
g_bookmark_file_remove_application:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 156 & 0xffff
#if 156 > 0xffff
  movk ip0, 156 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_remove_group
  .p2align 4
  .type g_bookmark_file_remove_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_remove_group
#endif
g_bookmark_file_remove_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 157 & 0xffff
#if 157 > 0xffff
  movk ip0, 157 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_remove_item
  .p2align 4
  .type g_bookmark_file_remove_item, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_remove_item
#endif
g_bookmark_file_remove_item:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 158 & 0xffff
#if 158 > 0xffff
  movk ip0, 158 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_added
  .p2align 4
  .type g_bookmark_file_set_added, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_added
#endif
g_bookmark_file_set_added:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 159 & 0xffff
#if 159 > 0xffff
  movk ip0, 159 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_added_date_time
  .p2align 4
  .type g_bookmark_file_set_added_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_added_date_time
#endif
g_bookmark_file_set_added_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 160 & 0xffff
#if 160 > 0xffff
  movk ip0, 160 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_app_info
  .p2align 4
  .type g_bookmark_file_set_app_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_app_info
#endif
g_bookmark_file_set_app_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 161 & 0xffff
#if 161 > 0xffff
  movk ip0, 161 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_application_info
  .p2align 4
  .type g_bookmark_file_set_application_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_application_info
#endif
g_bookmark_file_set_application_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 162 & 0xffff
#if 162 > 0xffff
  movk ip0, 162 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_description
  .p2align 4
  .type g_bookmark_file_set_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_description
#endif
g_bookmark_file_set_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 163 & 0xffff
#if 163 > 0xffff
  movk ip0, 163 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_groups
  .p2align 4
  .type g_bookmark_file_set_groups, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_groups
#endif
g_bookmark_file_set_groups:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 164 & 0xffff
#if 164 > 0xffff
  movk ip0, 164 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_icon
  .p2align 4
  .type g_bookmark_file_set_icon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_icon
#endif
g_bookmark_file_set_icon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 165 & 0xffff
#if 165 > 0xffff
  movk ip0, 165 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_is_private
  .p2align 4
  .type g_bookmark_file_set_is_private, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_is_private
#endif
g_bookmark_file_set_is_private:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 166 & 0xffff
#if 166 > 0xffff
  movk ip0, 166 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_mime_type
  .p2align 4
  .type g_bookmark_file_set_mime_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_mime_type
#endif
g_bookmark_file_set_mime_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 167 & 0xffff
#if 167 > 0xffff
  movk ip0, 167 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_modified
  .p2align 4
  .type g_bookmark_file_set_modified, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_modified
#endif
g_bookmark_file_set_modified:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 168 & 0xffff
#if 168 > 0xffff
  movk ip0, 168 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_modified_date_time
  .p2align 4
  .type g_bookmark_file_set_modified_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_modified_date_time
#endif
g_bookmark_file_set_modified_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 169 & 0xffff
#if 169 > 0xffff
  movk ip0, 169 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_title
  .p2align 4
  .type g_bookmark_file_set_title, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_title
#endif
g_bookmark_file_set_title:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 170 & 0xffff
#if 170 > 0xffff
  movk ip0, 170 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_visited
  .p2align 4
  .type g_bookmark_file_set_visited, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_visited
#endif
g_bookmark_file_set_visited:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 171 & 0xffff
#if 171 > 0xffff
  movk ip0, 171 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_set_visited_date_time
  .p2align 4
  .type g_bookmark_file_set_visited_date_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_set_visited_date_time
#endif
g_bookmark_file_set_visited_date_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 172 & 0xffff
#if 172 > 0xffff
  movk ip0, 172 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_to_data
  .p2align 4
  .type g_bookmark_file_to_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_to_data
#endif
g_bookmark_file_to_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 173 & 0xffff
#if 173 > 0xffff
  movk ip0, 173 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bookmark_file_to_file
  .p2align 4
  .type g_bookmark_file_to_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bookmark_file_to_file
#endif
g_bookmark_file_to_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 174 & 0xffff
#if 174 > 0xffff
  movk ip0, 174 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_build_filename
  .p2align 4
  .type g_build_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_build_filename
#endif
g_build_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 175 & 0xffff
#if 175 > 0xffff
  movk ip0, 175 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_build_filename_valist
  .p2align 4
  .type g_build_filename_valist, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_build_filename_valist
#endif
g_build_filename_valist:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 176 & 0xffff
#if 176 > 0xffff
  movk ip0, 176 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_build_filenamev
  .p2align 4
  .type g_build_filenamev, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_build_filenamev
#endif
g_build_filenamev:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 177 & 0xffff
#if 177 > 0xffff
  movk ip0, 177 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_build_path
  .p2align 4
  .type g_build_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_build_path
#endif
g_build_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 178 & 0xffff
#if 178 > 0xffff
  movk ip0, 178 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_build_pathv
  .p2align 4
  .type g_build_pathv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_build_pathv
#endif
g_build_pathv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 179 & 0xffff
#if 179 > 0xffff
  movk ip0, 179 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_append
  .p2align 4
  .type g_byte_array_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_append
#endif
g_byte_array_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 180 & 0xffff
#if 180 > 0xffff
  movk ip0, 180 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_free
  .p2align 4
  .type g_byte_array_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_free
#endif
g_byte_array_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 181 & 0xffff
#if 181 > 0xffff
  movk ip0, 181 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_free_to_bytes
  .p2align 4
  .type g_byte_array_free_to_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_free_to_bytes
#endif
g_byte_array_free_to_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 182 & 0xffff
#if 182 > 0xffff
  movk ip0, 182 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_new
  .p2align 4
  .type g_byte_array_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_new
#endif
g_byte_array_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 183 & 0xffff
#if 183 > 0xffff
  movk ip0, 183 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_new_take
  .p2align 4
  .type g_byte_array_new_take, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_new_take
#endif
g_byte_array_new_take:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 184 & 0xffff
#if 184 > 0xffff
  movk ip0, 184 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_prepend
  .p2align 4
  .type g_byte_array_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_prepend
#endif
g_byte_array_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 185 & 0xffff
#if 185 > 0xffff
  movk ip0, 185 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_ref
  .p2align 4
  .type g_byte_array_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_ref
#endif
g_byte_array_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 186 & 0xffff
#if 186 > 0xffff
  movk ip0, 186 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_remove_index
  .p2align 4
  .type g_byte_array_remove_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_remove_index
#endif
g_byte_array_remove_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 187 & 0xffff
#if 187 > 0xffff
  movk ip0, 187 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_remove_index_fast
  .p2align 4
  .type g_byte_array_remove_index_fast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_remove_index_fast
#endif
g_byte_array_remove_index_fast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 188 & 0xffff
#if 188 > 0xffff
  movk ip0, 188 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_remove_range
  .p2align 4
  .type g_byte_array_remove_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_remove_range
#endif
g_byte_array_remove_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 189 & 0xffff
#if 189 > 0xffff
  movk ip0, 189 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_set_size
  .p2align 4
  .type g_byte_array_set_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_set_size
#endif
g_byte_array_set_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 190 & 0xffff
#if 190 > 0xffff
  movk ip0, 190 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_sized_new
  .p2align 4
  .type g_byte_array_sized_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_sized_new
#endif
g_byte_array_sized_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 191 & 0xffff
#if 191 > 0xffff
  movk ip0, 191 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_sort
  .p2align 4
  .type g_byte_array_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_sort
#endif
g_byte_array_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 192 & 0xffff
#if 192 > 0xffff
  movk ip0, 192 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_sort_with_data
  .p2align 4
  .type g_byte_array_sort_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_sort_with_data
#endif
g_byte_array_sort_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 193 & 0xffff
#if 193 > 0xffff
  movk ip0, 193 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_steal
  .p2align 4
  .type g_byte_array_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_steal
#endif
g_byte_array_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 194 & 0xffff
#if 194 > 0xffff
  movk ip0, 194 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_byte_array_unref
  .p2align 4
  .type g_byte_array_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_byte_array_unref
#endif
g_byte_array_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 195 & 0xffff
#if 195 > 0xffff
  movk ip0, 195 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_compare
  .p2align 4
  .type g_bytes_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_compare
#endif
g_bytes_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 196 & 0xffff
#if 196 > 0xffff
  movk ip0, 196 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_equal
  .p2align 4
  .type g_bytes_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_equal
#endif
g_bytes_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 197 & 0xffff
#if 197 > 0xffff
  movk ip0, 197 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_get_data
  .p2align 4
  .type g_bytes_get_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_get_data
#endif
g_bytes_get_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 198 & 0xffff
#if 198 > 0xffff
  movk ip0, 198 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_get_region
  .p2align 4
  .type g_bytes_get_region, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_get_region
#endif
g_bytes_get_region:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 199 & 0xffff
#if 199 > 0xffff
  movk ip0, 199 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_get_size
  .p2align 4
  .type g_bytes_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_get_size
#endif
g_bytes_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 200 & 0xffff
#if 200 > 0xffff
  movk ip0, 200 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_hash
  .p2align 4
  .type g_bytes_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_hash
#endif
g_bytes_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 201 & 0xffff
#if 201 > 0xffff
  movk ip0, 201 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_new
  .p2align 4
  .type g_bytes_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_new
#endif
g_bytes_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 202 & 0xffff
#if 202 > 0xffff
  movk ip0, 202 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_new_from_bytes
  .p2align 4
  .type g_bytes_new_from_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_new_from_bytes
#endif
g_bytes_new_from_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 203 & 0xffff
#if 203 > 0xffff
  movk ip0, 203 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_new_static
  .p2align 4
  .type g_bytes_new_static, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_new_static
#endif
g_bytes_new_static:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 204 & 0xffff
#if 204 > 0xffff
  movk ip0, 204 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_new_take
  .p2align 4
  .type g_bytes_new_take, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_new_take
#endif
g_bytes_new_take:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 205 & 0xffff
#if 205 > 0xffff
  movk ip0, 205 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_new_with_free_func
  .p2align 4
  .type g_bytes_new_with_free_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_new_with_free_func
#endif
g_bytes_new_with_free_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 206 & 0xffff
#if 206 > 0xffff
  movk ip0, 206 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_ref
  .p2align 4
  .type g_bytes_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_ref
#endif
g_bytes_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 207 & 0xffff
#if 207 > 0xffff
  movk ip0, 207 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_unref
  .p2align 4
  .type g_bytes_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_unref
#endif
g_bytes_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 208 & 0xffff
#if 208 > 0xffff
  movk ip0, 208 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_unref_to_array
  .p2align 4
  .type g_bytes_unref_to_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_unref_to_array
#endif
g_bytes_unref_to_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 209 & 0xffff
#if 209 > 0xffff
  movk ip0, 209 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_bytes_unref_to_data
  .p2align 4
  .type g_bytes_unref_to_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_bytes_unref_to_data
#endif
g_bytes_unref_to_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 210 & 0xffff
#if 210 > 0xffff
  movk ip0, 210 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cache_destroy
  .p2align 4
  .type g_cache_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cache_destroy
#endif
g_cache_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 211 & 0xffff
#if 211 > 0xffff
  movk ip0, 211 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cache_insert
  .p2align 4
  .type g_cache_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cache_insert
#endif
g_cache_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 212 & 0xffff
#if 212 > 0xffff
  movk ip0, 212 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cache_key_foreach
  .p2align 4
  .type g_cache_key_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cache_key_foreach
#endif
g_cache_key_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 213 & 0xffff
#if 213 > 0xffff
  movk ip0, 213 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cache_new
  .p2align 4
  .type g_cache_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cache_new
#endif
g_cache_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 214 & 0xffff
#if 214 > 0xffff
  movk ip0, 214 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cache_remove
  .p2align 4
  .type g_cache_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cache_remove
#endif
g_cache_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 215 & 0xffff
#if 215 > 0xffff
  movk ip0, 215 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cache_value_foreach
  .p2align 4
  .type g_cache_value_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cache_value_foreach
#endif
g_cache_value_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 216 & 0xffff
#if 216 > 0xffff
  movk ip0, 216 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_canonicalize_filename
  .p2align 4
  .type g_canonicalize_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_canonicalize_filename
#endif
g_canonicalize_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 217 & 0xffff
#if 217 > 0xffff
  movk ip0, 217 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_chdir
  .p2align 4
  .type g_chdir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_chdir
#endif
g_chdir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 218 & 0xffff
#if 218 > 0xffff
  movk ip0, 218 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_copy
  .p2align 4
  .type g_checksum_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_copy
#endif
g_checksum_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 219 & 0xffff
#if 219 > 0xffff
  movk ip0, 219 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_free
  .p2align 4
  .type g_checksum_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_free
#endif
g_checksum_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 220 & 0xffff
#if 220 > 0xffff
  movk ip0, 220 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_get_digest
  .p2align 4
  .type g_checksum_get_digest, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_get_digest
#endif
g_checksum_get_digest:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 221 & 0xffff
#if 221 > 0xffff
  movk ip0, 221 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_get_string
  .p2align 4
  .type g_checksum_get_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_get_string
#endif
g_checksum_get_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 222 & 0xffff
#if 222 > 0xffff
  movk ip0, 222 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_new
  .p2align 4
  .type g_checksum_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_new
#endif
g_checksum_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 223 & 0xffff
#if 223 > 0xffff
  movk ip0, 223 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_reset
  .p2align 4
  .type g_checksum_reset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_reset
#endif
g_checksum_reset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 224 & 0xffff
#if 224 > 0xffff
  movk ip0, 224 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_type_get_length
  .p2align 4
  .type g_checksum_type_get_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_type_get_length
#endif
g_checksum_type_get_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 225 & 0xffff
#if 225 > 0xffff
  movk ip0, 225 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_checksum_update
  .p2align 4
  .type g_checksum_update, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_checksum_update
#endif
g_checksum_update:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 226 & 0xffff
#if 226 > 0xffff
  movk ip0, 226 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_child_watch_add
  .p2align 4
  .type g_child_watch_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_child_watch_add
#endif
g_child_watch_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 227 & 0xffff
#if 227 > 0xffff
  movk ip0, 227 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_child_watch_add_full
  .p2align 4
  .type g_child_watch_add_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_child_watch_add_full
#endif
g_child_watch_add_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 228 & 0xffff
#if 228 > 0xffff
  movk ip0, 228 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_child_watch_source_new
  .p2align 4
  .type g_child_watch_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_child_watch_source_new
#endif
g_child_watch_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 229 & 0xffff
#if 229 > 0xffff
  movk ip0, 229 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_chmod
  .p2align 4
  .type g_chmod, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_chmod
#endif
g_chmod:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 230 & 0xffff
#if 230 > 0xffff
  movk ip0, 230 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_clear_error
  .p2align 4
  .type g_clear_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_clear_error
#endif
g_clear_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 231 & 0xffff
#if 231 > 0xffff
  movk ip0, 231 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_clear_handle_id
  .p2align 4
  .type g_clear_handle_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_clear_handle_id
#endif
g_clear_handle_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 232 & 0xffff
#if 232 > 0xffff
  movk ip0, 232 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_clear_list
  .p2align 4
  .type g_clear_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_clear_list
#endif
g_clear_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 233 & 0xffff
#if 233 > 0xffff
  movk ip0, 233 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_clear_pointer
  .p2align 4
  .type g_clear_pointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_clear_pointer
#endif
g_clear_pointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 234 & 0xffff
#if 234 > 0xffff
  movk ip0, 234 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_clear_slist
  .p2align 4
  .type g_clear_slist, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_clear_slist
#endif
g_clear_slist:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 235 & 0xffff
#if 235 > 0xffff
  movk ip0, 235 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_close
  .p2align 4
  .type g_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_close
#endif
g_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 236 & 0xffff
#if 236 > 0xffff
  movk ip0, 236 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_closefrom
  .p2align 4
  .type g_closefrom, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_closefrom
#endif
g_closefrom:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 237 & 0xffff
#if 237 > 0xffff
  movk ip0, 237 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_add_items
  .p2align 4
  .type g_completion_add_items, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_add_items
#endif
g_completion_add_items:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 238 & 0xffff
#if 238 > 0xffff
  movk ip0, 238 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_clear_items
  .p2align 4
  .type g_completion_clear_items, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_clear_items
#endif
g_completion_clear_items:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 239 & 0xffff
#if 239 > 0xffff
  movk ip0, 239 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_complete
  .p2align 4
  .type g_completion_complete, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_complete
#endif
g_completion_complete:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 240 & 0xffff
#if 240 > 0xffff
  movk ip0, 240 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_complete_utf8
  .p2align 4
  .type g_completion_complete_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_complete_utf8
#endif
g_completion_complete_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 241 & 0xffff
#if 241 > 0xffff
  movk ip0, 241 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_free
  .p2align 4
  .type g_completion_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_free
#endif
g_completion_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 242 & 0xffff
#if 242 > 0xffff
  movk ip0, 242 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_new
  .p2align 4
  .type g_completion_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_new
#endif
g_completion_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 243 & 0xffff
#if 243 > 0xffff
  movk ip0, 243 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_remove_items
  .p2align 4
  .type g_completion_remove_items, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_remove_items
#endif
g_completion_remove_items:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 244 & 0xffff
#if 244 > 0xffff
  movk ip0, 244 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_completion_set_compare
  .p2align 4
  .type g_completion_set_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_completion_set_compare
#endif
g_completion_set_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 245 & 0xffff
#if 245 > 0xffff
  movk ip0, 245 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_compute_checksum_for_bytes
  .p2align 4
  .type g_compute_checksum_for_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_compute_checksum_for_bytes
#endif
g_compute_checksum_for_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 246 & 0xffff
#if 246 > 0xffff
  movk ip0, 246 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_compute_checksum_for_data
  .p2align 4
  .type g_compute_checksum_for_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_compute_checksum_for_data
#endif
g_compute_checksum_for_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 247 & 0xffff
#if 247 > 0xffff
  movk ip0, 247 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_compute_checksum_for_string
  .p2align 4
  .type g_compute_checksum_for_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_compute_checksum_for_string
#endif
g_compute_checksum_for_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 248 & 0xffff
#if 248 > 0xffff
  movk ip0, 248 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_compute_hmac_for_bytes
  .p2align 4
  .type g_compute_hmac_for_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_compute_hmac_for_bytes
#endif
g_compute_hmac_for_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+1992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+1992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 249 & 0xffff
#if 249 > 0xffff
  movk ip0, 249 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_compute_hmac_for_data
  .p2align 4
  .type g_compute_hmac_for_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_compute_hmac_for_data
#endif
g_compute_hmac_for_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 250 & 0xffff
#if 250 > 0xffff
  movk ip0, 250 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_compute_hmac_for_string
  .p2align 4
  .type g_compute_hmac_for_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_compute_hmac_for_string
#endif
g_compute_hmac_for_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 251 & 0xffff
#if 251 > 0xffff
  movk ip0, 251 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_broadcast
  .p2align 4
  .type g_cond_broadcast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_broadcast
#endif
g_cond_broadcast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 252 & 0xffff
#if 252 > 0xffff
  movk ip0, 252 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_clear
  .p2align 4
  .type g_cond_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_clear
#endif
g_cond_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 253 & 0xffff
#if 253 > 0xffff
  movk ip0, 253 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_free
  .p2align 4
  .type g_cond_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_free
#endif
g_cond_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 254 & 0xffff
#if 254 > 0xffff
  movk ip0, 254 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_init
  .p2align 4
  .type g_cond_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_init
#endif
g_cond_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 255 & 0xffff
#if 255 > 0xffff
  movk ip0, 255 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_new
  .p2align 4
  .type g_cond_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_new
#endif
g_cond_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 256 & 0xffff
#if 256 > 0xffff
  movk ip0, 256 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_signal
  .p2align 4
  .type g_cond_signal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_signal
#endif
g_cond_signal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 257 & 0xffff
#if 257 > 0xffff
  movk ip0, 257 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_timed_wait
  .p2align 4
  .type g_cond_timed_wait, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_timed_wait
#endif
g_cond_timed_wait:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 258 & 0xffff
#if 258 > 0xffff
  movk ip0, 258 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_wait
  .p2align 4
  .type g_cond_wait, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_wait
#endif
g_cond_wait:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 259 & 0xffff
#if 259 > 0xffff
  movk ip0, 259 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_cond_wait_until
  .p2align 4
  .type g_cond_wait_until, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_cond_wait_until
#endif
g_cond_wait_until:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 260 & 0xffff
#if 260 > 0xffff
  movk ip0, 260 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_convert
  .p2align 4
  .type g_convert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_convert
#endif
g_convert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 261 & 0xffff
#if 261 > 0xffff
  movk ip0, 261 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_convert_error_quark
  .p2align 4
  .type g_convert_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_convert_error_quark
#endif
g_convert_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 262 & 0xffff
#if 262 > 0xffff
  movk ip0, 262 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_convert_with_fallback
  .p2align 4
  .type g_convert_with_fallback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_convert_with_fallback
#endif
g_convert_with_fallback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 263 & 0xffff
#if 263 > 0xffff
  movk ip0, 263 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_convert_with_iconv
  .p2align 4
  .type g_convert_with_iconv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_convert_with_iconv
#endif
g_convert_with_iconv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 264 & 0xffff
#if 264 > 0xffff
  movk ip0, 264 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_creat
  .p2align 4
  .type g_creat, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_creat
#endif
g_creat:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 265 & 0xffff
#if 265 > 0xffff
  movk ip0, 265 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_clear
  .p2align 4
  .type g_datalist_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_clear
#endif
g_datalist_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 266 & 0xffff
#if 266 > 0xffff
  movk ip0, 266 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_foreach
  .p2align 4
  .type g_datalist_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_foreach
#endif
g_datalist_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 267 & 0xffff
#if 267 > 0xffff
  movk ip0, 267 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_get_data
  .p2align 4
  .type g_datalist_get_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_get_data
#endif
g_datalist_get_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 268 & 0xffff
#if 268 > 0xffff
  movk ip0, 268 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_get_flags
  .p2align 4
  .type g_datalist_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_get_flags
#endif
g_datalist_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 269 & 0xffff
#if 269 > 0xffff
  movk ip0, 269 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_id_dup_data
  .p2align 4
  .type g_datalist_id_dup_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_id_dup_data
#endif
g_datalist_id_dup_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 270 & 0xffff
#if 270 > 0xffff
  movk ip0, 270 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_id_get_data
  .p2align 4
  .type g_datalist_id_get_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_id_get_data
#endif
g_datalist_id_get_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 271 & 0xffff
#if 271 > 0xffff
  movk ip0, 271 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_id_remove_multiple
  .p2align 4
  .type g_datalist_id_remove_multiple, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_id_remove_multiple
#endif
g_datalist_id_remove_multiple:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 272 & 0xffff
#if 272 > 0xffff
  movk ip0, 272 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_id_remove_no_notify
  .p2align 4
  .type g_datalist_id_remove_no_notify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_id_remove_no_notify
#endif
g_datalist_id_remove_no_notify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 273 & 0xffff
#if 273 > 0xffff
  movk ip0, 273 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_id_replace_data
  .p2align 4
  .type g_datalist_id_replace_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_id_replace_data
#endif
g_datalist_id_replace_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 274 & 0xffff
#if 274 > 0xffff
  movk ip0, 274 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_id_set_data_full
  .p2align 4
  .type g_datalist_id_set_data_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_id_set_data_full
#endif
g_datalist_id_set_data_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 275 & 0xffff
#if 275 > 0xffff
  movk ip0, 275 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_init
  .p2align 4
  .type g_datalist_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_init
#endif
g_datalist_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 276 & 0xffff
#if 276 > 0xffff
  movk ip0, 276 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_set_flags
  .p2align 4
  .type g_datalist_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_set_flags
#endif
g_datalist_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 277 & 0xffff
#if 277 > 0xffff
  movk ip0, 277 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_datalist_unset_flags
  .p2align 4
  .type g_datalist_unset_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_datalist_unset_flags
#endif
g_datalist_unset_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 278 & 0xffff
#if 278 > 0xffff
  movk ip0, 278 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dataset_destroy
  .p2align 4
  .type g_dataset_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dataset_destroy
#endif
g_dataset_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 279 & 0xffff
#if 279 > 0xffff
  movk ip0, 279 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dataset_foreach
  .p2align 4
  .type g_dataset_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dataset_foreach
#endif
g_dataset_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 280 & 0xffff
#if 280 > 0xffff
  movk ip0, 280 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dataset_id_get_data
  .p2align 4
  .type g_dataset_id_get_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dataset_id_get_data
#endif
g_dataset_id_get_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 281 & 0xffff
#if 281 > 0xffff
  movk ip0, 281 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dataset_id_remove_no_notify
  .p2align 4
  .type g_dataset_id_remove_no_notify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dataset_id_remove_no_notify
#endif
g_dataset_id_remove_no_notify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 282 & 0xffff
#if 282 > 0xffff
  movk ip0, 282 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dataset_id_set_data_full
  .p2align 4
  .type g_dataset_id_set_data_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dataset_id_set_data_full
#endif
g_dataset_id_set_data_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 283 & 0xffff
#if 283 > 0xffff
  movk ip0, 283 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_add_days
  .p2align 4
  .type g_date_add_days, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_add_days
#endif
g_date_add_days:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 284 & 0xffff
#if 284 > 0xffff
  movk ip0, 284 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_add_months
  .p2align 4
  .type g_date_add_months, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_add_months
#endif
g_date_add_months:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 285 & 0xffff
#if 285 > 0xffff
  movk ip0, 285 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_add_years
  .p2align 4
  .type g_date_add_years, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_add_years
#endif
g_date_add_years:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 286 & 0xffff
#if 286 > 0xffff
  movk ip0, 286 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_clamp
  .p2align 4
  .type g_date_clamp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_clamp
#endif
g_date_clamp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 287 & 0xffff
#if 287 > 0xffff
  movk ip0, 287 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_clear
  .p2align 4
  .type g_date_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_clear
#endif
g_date_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 288 & 0xffff
#if 288 > 0xffff
  movk ip0, 288 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_compare
  .p2align 4
  .type g_date_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_compare
#endif
g_date_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 289 & 0xffff
#if 289 > 0xffff
  movk ip0, 289 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_copy
  .p2align 4
  .type g_date_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_copy
#endif
g_date_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 290 & 0xffff
#if 290 > 0xffff
  movk ip0, 290 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_days_between
  .p2align 4
  .type g_date_days_between, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_days_between
#endif
g_date_days_between:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 291 & 0xffff
#if 291 > 0xffff
  movk ip0, 291 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_free
  .p2align 4
  .type g_date_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_free
#endif
g_date_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 292 & 0xffff
#if 292 > 0xffff
  movk ip0, 292 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_day
  .p2align 4
  .type g_date_get_day, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_day
#endif
g_date_get_day:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 293 & 0xffff
#if 293 > 0xffff
  movk ip0, 293 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_day_of_year
  .p2align 4
  .type g_date_get_day_of_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_day_of_year
#endif
g_date_get_day_of_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 294 & 0xffff
#if 294 > 0xffff
  movk ip0, 294 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_days_in_month
  .p2align 4
  .type g_date_get_days_in_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_days_in_month
#endif
g_date_get_days_in_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 295 & 0xffff
#if 295 > 0xffff
  movk ip0, 295 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_iso8601_week_of_year
  .p2align 4
  .type g_date_get_iso8601_week_of_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_iso8601_week_of_year
#endif
g_date_get_iso8601_week_of_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 296 & 0xffff
#if 296 > 0xffff
  movk ip0, 296 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_julian
  .p2align 4
  .type g_date_get_julian, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_julian
#endif
g_date_get_julian:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 297 & 0xffff
#if 297 > 0xffff
  movk ip0, 297 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_monday_week_of_year
  .p2align 4
  .type g_date_get_monday_week_of_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_monday_week_of_year
#endif
g_date_get_monday_week_of_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 298 & 0xffff
#if 298 > 0xffff
  movk ip0, 298 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_monday_weeks_in_year
  .p2align 4
  .type g_date_get_monday_weeks_in_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_monday_weeks_in_year
#endif
g_date_get_monday_weeks_in_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 299 & 0xffff
#if 299 > 0xffff
  movk ip0, 299 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_month
  .p2align 4
  .type g_date_get_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_month
#endif
g_date_get_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 300 & 0xffff
#if 300 > 0xffff
  movk ip0, 300 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_sunday_week_of_year
  .p2align 4
  .type g_date_get_sunday_week_of_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_sunday_week_of_year
#endif
g_date_get_sunday_week_of_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 301 & 0xffff
#if 301 > 0xffff
  movk ip0, 301 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_sunday_weeks_in_year
  .p2align 4
  .type g_date_get_sunday_weeks_in_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_sunday_weeks_in_year
#endif
g_date_get_sunday_weeks_in_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 302 & 0xffff
#if 302 > 0xffff
  movk ip0, 302 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_weekday
  .p2align 4
  .type g_date_get_weekday, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_weekday
#endif
g_date_get_weekday:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 303 & 0xffff
#if 303 > 0xffff
  movk ip0, 303 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_get_year
  .p2align 4
  .type g_date_get_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_get_year
#endif
g_date_get_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 304 & 0xffff
#if 304 > 0xffff
  movk ip0, 304 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_is_first_of_month
  .p2align 4
  .type g_date_is_first_of_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_is_first_of_month
#endif
g_date_is_first_of_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 305 & 0xffff
#if 305 > 0xffff
  movk ip0, 305 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_is_last_of_month
  .p2align 4
  .type g_date_is_last_of_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_is_last_of_month
#endif
g_date_is_last_of_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 306 & 0xffff
#if 306 > 0xffff
  movk ip0, 306 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_is_leap_year
  .p2align 4
  .type g_date_is_leap_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_is_leap_year
#endif
g_date_is_leap_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 307 & 0xffff
#if 307 > 0xffff
  movk ip0, 307 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_new
  .p2align 4
  .type g_date_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_new
#endif
g_date_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 308 & 0xffff
#if 308 > 0xffff
  movk ip0, 308 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_new_dmy
  .p2align 4
  .type g_date_new_dmy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_new_dmy
#endif
g_date_new_dmy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 309 & 0xffff
#if 309 > 0xffff
  movk ip0, 309 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_new_julian
  .p2align 4
  .type g_date_new_julian, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_new_julian
#endif
g_date_new_julian:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 310 & 0xffff
#if 310 > 0xffff
  movk ip0, 310 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_order
  .p2align 4
  .type g_date_order, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_order
#endif
g_date_order:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 311 & 0xffff
#if 311 > 0xffff
  movk ip0, 311 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_day
  .p2align 4
  .type g_date_set_day, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_day
#endif
g_date_set_day:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 312 & 0xffff
#if 312 > 0xffff
  movk ip0, 312 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_dmy
  .p2align 4
  .type g_date_set_dmy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_dmy
#endif
g_date_set_dmy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 313 & 0xffff
#if 313 > 0xffff
  movk ip0, 313 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_julian
  .p2align 4
  .type g_date_set_julian, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_julian
#endif
g_date_set_julian:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 314 & 0xffff
#if 314 > 0xffff
  movk ip0, 314 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_month
  .p2align 4
  .type g_date_set_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_month
#endif
g_date_set_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 315 & 0xffff
#if 315 > 0xffff
  movk ip0, 315 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_parse
  .p2align 4
  .type g_date_set_parse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_parse
#endif
g_date_set_parse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 316 & 0xffff
#if 316 > 0xffff
  movk ip0, 316 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_time
  .p2align 4
  .type g_date_set_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_time
#endif
g_date_set_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 317 & 0xffff
#if 317 > 0xffff
  movk ip0, 317 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_time_t
  .p2align 4
  .type g_date_set_time_t, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_time_t
#endif
g_date_set_time_t:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 318 & 0xffff
#if 318 > 0xffff
  movk ip0, 318 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_time_val
  .p2align 4
  .type g_date_set_time_val, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_time_val
#endif
g_date_set_time_val:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 319 & 0xffff
#if 319 > 0xffff
  movk ip0, 319 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_set_year
  .p2align 4
  .type g_date_set_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_set_year
#endif
g_date_set_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 320 & 0xffff
#if 320 > 0xffff
  movk ip0, 320 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_strftime
  .p2align 4
  .type g_date_strftime, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_strftime
#endif
g_date_strftime:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 321 & 0xffff
#if 321 > 0xffff
  movk ip0, 321 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_subtract_days
  .p2align 4
  .type g_date_subtract_days, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_subtract_days
#endif
g_date_subtract_days:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 322 & 0xffff
#if 322 > 0xffff
  movk ip0, 322 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_subtract_months
  .p2align 4
  .type g_date_subtract_months, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_subtract_months
#endif
g_date_subtract_months:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 323 & 0xffff
#if 323 > 0xffff
  movk ip0, 323 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_subtract_years
  .p2align 4
  .type g_date_subtract_years, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_subtract_years
#endif
g_date_subtract_years:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 324 & 0xffff
#if 324 > 0xffff
  movk ip0, 324 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add
  .p2align 4
  .type g_date_time_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add
#endif
g_date_time_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 325 & 0xffff
#if 325 > 0xffff
  movk ip0, 325 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_days
  .p2align 4
  .type g_date_time_add_days, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_days
#endif
g_date_time_add_days:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 326 & 0xffff
#if 326 > 0xffff
  movk ip0, 326 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_full
  .p2align 4
  .type g_date_time_add_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_full
#endif
g_date_time_add_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 327 & 0xffff
#if 327 > 0xffff
  movk ip0, 327 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_hours
  .p2align 4
  .type g_date_time_add_hours, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_hours
#endif
g_date_time_add_hours:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 328 & 0xffff
#if 328 > 0xffff
  movk ip0, 328 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_minutes
  .p2align 4
  .type g_date_time_add_minutes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_minutes
#endif
g_date_time_add_minutes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 329 & 0xffff
#if 329 > 0xffff
  movk ip0, 329 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_months
  .p2align 4
  .type g_date_time_add_months, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_months
#endif
g_date_time_add_months:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 330 & 0xffff
#if 330 > 0xffff
  movk ip0, 330 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_seconds
  .p2align 4
  .type g_date_time_add_seconds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_seconds
#endif
g_date_time_add_seconds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 331 & 0xffff
#if 331 > 0xffff
  movk ip0, 331 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_weeks
  .p2align 4
  .type g_date_time_add_weeks, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_weeks
#endif
g_date_time_add_weeks:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 332 & 0xffff
#if 332 > 0xffff
  movk ip0, 332 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_add_years
  .p2align 4
  .type g_date_time_add_years, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_add_years
#endif
g_date_time_add_years:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 333 & 0xffff
#if 333 > 0xffff
  movk ip0, 333 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_compare
  .p2align 4
  .type g_date_time_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_compare
#endif
g_date_time_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 334 & 0xffff
#if 334 > 0xffff
  movk ip0, 334 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_difference
  .p2align 4
  .type g_date_time_difference, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_difference
#endif
g_date_time_difference:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 335 & 0xffff
#if 335 > 0xffff
  movk ip0, 335 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_equal
  .p2align 4
  .type g_date_time_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_equal
#endif
g_date_time_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 336 & 0xffff
#if 336 > 0xffff
  movk ip0, 336 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_format
  .p2align 4
  .type g_date_time_format, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_format
#endif
g_date_time_format:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 337 & 0xffff
#if 337 > 0xffff
  movk ip0, 337 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_format_iso8601
  .p2align 4
  .type g_date_time_format_iso8601, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_format_iso8601
#endif
g_date_time_format_iso8601:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 338 & 0xffff
#if 338 > 0xffff
  movk ip0, 338 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_day_of_month
  .p2align 4
  .type g_date_time_get_day_of_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_day_of_month
#endif
g_date_time_get_day_of_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 339 & 0xffff
#if 339 > 0xffff
  movk ip0, 339 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_day_of_week
  .p2align 4
  .type g_date_time_get_day_of_week, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_day_of_week
#endif
g_date_time_get_day_of_week:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 340 & 0xffff
#if 340 > 0xffff
  movk ip0, 340 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_day_of_year
  .p2align 4
  .type g_date_time_get_day_of_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_day_of_year
#endif
g_date_time_get_day_of_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 341 & 0xffff
#if 341 > 0xffff
  movk ip0, 341 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_hour
  .p2align 4
  .type g_date_time_get_hour, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_hour
#endif
g_date_time_get_hour:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 342 & 0xffff
#if 342 > 0xffff
  movk ip0, 342 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_microsecond
  .p2align 4
  .type g_date_time_get_microsecond, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_microsecond
#endif
g_date_time_get_microsecond:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 343 & 0xffff
#if 343 > 0xffff
  movk ip0, 343 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_minute
  .p2align 4
  .type g_date_time_get_minute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_minute
#endif
g_date_time_get_minute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 344 & 0xffff
#if 344 > 0xffff
  movk ip0, 344 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_month
  .p2align 4
  .type g_date_time_get_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_month
#endif
g_date_time_get_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 345 & 0xffff
#if 345 > 0xffff
  movk ip0, 345 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_second
  .p2align 4
  .type g_date_time_get_second, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_second
#endif
g_date_time_get_second:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 346 & 0xffff
#if 346 > 0xffff
  movk ip0, 346 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_seconds
  .p2align 4
  .type g_date_time_get_seconds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_seconds
#endif
g_date_time_get_seconds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 347 & 0xffff
#if 347 > 0xffff
  movk ip0, 347 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_timezone
  .p2align 4
  .type g_date_time_get_timezone, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_timezone
#endif
g_date_time_get_timezone:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 348 & 0xffff
#if 348 > 0xffff
  movk ip0, 348 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_timezone_abbreviation
  .p2align 4
  .type g_date_time_get_timezone_abbreviation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_timezone_abbreviation
#endif
g_date_time_get_timezone_abbreviation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 349 & 0xffff
#if 349 > 0xffff
  movk ip0, 349 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_utc_offset
  .p2align 4
  .type g_date_time_get_utc_offset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_utc_offset
#endif
g_date_time_get_utc_offset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 350 & 0xffff
#if 350 > 0xffff
  movk ip0, 350 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_week_numbering_year
  .p2align 4
  .type g_date_time_get_week_numbering_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_week_numbering_year
#endif
g_date_time_get_week_numbering_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 351 & 0xffff
#if 351 > 0xffff
  movk ip0, 351 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_week_of_year
  .p2align 4
  .type g_date_time_get_week_of_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_week_of_year
#endif
g_date_time_get_week_of_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 352 & 0xffff
#if 352 > 0xffff
  movk ip0, 352 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_year
  .p2align 4
  .type g_date_time_get_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_year
#endif
g_date_time_get_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 353 & 0xffff
#if 353 > 0xffff
  movk ip0, 353 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_get_ymd
  .p2align 4
  .type g_date_time_get_ymd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_get_ymd
#endif
g_date_time_get_ymd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 354 & 0xffff
#if 354 > 0xffff
  movk ip0, 354 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_hash
  .p2align 4
  .type g_date_time_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_hash
#endif
g_date_time_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 355 & 0xffff
#if 355 > 0xffff
  movk ip0, 355 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_is_daylight_savings
  .p2align 4
  .type g_date_time_is_daylight_savings, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_is_daylight_savings
#endif
g_date_time_is_daylight_savings:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 356 & 0xffff
#if 356 > 0xffff
  movk ip0, 356 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new
  .p2align 4
  .type g_date_time_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new
#endif
g_date_time_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 357 & 0xffff
#if 357 > 0xffff
  movk ip0, 357 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_from_iso8601
  .p2align 4
  .type g_date_time_new_from_iso8601, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_from_iso8601
#endif
g_date_time_new_from_iso8601:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 358 & 0xffff
#if 358 > 0xffff
  movk ip0, 358 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_from_timeval_local
  .p2align 4
  .type g_date_time_new_from_timeval_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_from_timeval_local
#endif
g_date_time_new_from_timeval_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 359 & 0xffff
#if 359 > 0xffff
  movk ip0, 359 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_from_timeval_utc
  .p2align 4
  .type g_date_time_new_from_timeval_utc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_from_timeval_utc
#endif
g_date_time_new_from_timeval_utc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 360 & 0xffff
#if 360 > 0xffff
  movk ip0, 360 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_from_unix_local
  .p2align 4
  .type g_date_time_new_from_unix_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_from_unix_local
#endif
g_date_time_new_from_unix_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 361 & 0xffff
#if 361 > 0xffff
  movk ip0, 361 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_from_unix_local_usec
  .p2align 4
  .type g_date_time_new_from_unix_local_usec, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_from_unix_local_usec
#endif
g_date_time_new_from_unix_local_usec:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 362 & 0xffff
#if 362 > 0xffff
  movk ip0, 362 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_from_unix_utc
  .p2align 4
  .type g_date_time_new_from_unix_utc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_from_unix_utc
#endif
g_date_time_new_from_unix_utc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 363 & 0xffff
#if 363 > 0xffff
  movk ip0, 363 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_from_unix_utc_usec
  .p2align 4
  .type g_date_time_new_from_unix_utc_usec, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_from_unix_utc_usec
#endif
g_date_time_new_from_unix_utc_usec:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 364 & 0xffff
#if 364 > 0xffff
  movk ip0, 364 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_local
  .p2align 4
  .type g_date_time_new_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_local
#endif
g_date_time_new_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 365 & 0xffff
#if 365 > 0xffff
  movk ip0, 365 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_now
  .p2align 4
  .type g_date_time_new_now, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_now
#endif
g_date_time_new_now:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 366 & 0xffff
#if 366 > 0xffff
  movk ip0, 366 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_now_local
  .p2align 4
  .type g_date_time_new_now_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_now_local
#endif
g_date_time_new_now_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 367 & 0xffff
#if 367 > 0xffff
  movk ip0, 367 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_now_utc
  .p2align 4
  .type g_date_time_new_now_utc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_now_utc
#endif
g_date_time_new_now_utc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 368 & 0xffff
#if 368 > 0xffff
  movk ip0, 368 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_new_utc
  .p2align 4
  .type g_date_time_new_utc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_new_utc
#endif
g_date_time_new_utc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 369 & 0xffff
#if 369 > 0xffff
  movk ip0, 369 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_ref
  .p2align 4
  .type g_date_time_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_ref
#endif
g_date_time_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 370 & 0xffff
#if 370 > 0xffff
  movk ip0, 370 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_to_local
  .p2align 4
  .type g_date_time_to_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_to_local
#endif
g_date_time_to_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 371 & 0xffff
#if 371 > 0xffff
  movk ip0, 371 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_to_timeval
  .p2align 4
  .type g_date_time_to_timeval, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_to_timeval
#endif
g_date_time_to_timeval:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 372 & 0xffff
#if 372 > 0xffff
  movk ip0, 372 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_to_timezone
  .p2align 4
  .type g_date_time_to_timezone, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_to_timezone
#endif
g_date_time_to_timezone:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 373 & 0xffff
#if 373 > 0xffff
  movk ip0, 373 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_to_unix
  .p2align 4
  .type g_date_time_to_unix, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_to_unix
#endif
g_date_time_to_unix:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+2992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+2992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 374 & 0xffff
#if 374 > 0xffff
  movk ip0, 374 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_to_unix_usec
  .p2align 4
  .type g_date_time_to_unix_usec, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_to_unix_usec
#endif
g_date_time_to_unix_usec:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 375 & 0xffff
#if 375 > 0xffff
  movk ip0, 375 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_to_utc
  .p2align 4
  .type g_date_time_to_utc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_to_utc
#endif
g_date_time_to_utc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 376 & 0xffff
#if 376 > 0xffff
  movk ip0, 376 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_time_unref
  .p2align 4
  .type g_date_time_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_time_unref
#endif
g_date_time_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 377 & 0xffff
#if 377 > 0xffff
  movk ip0, 377 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_to_struct_tm
  .p2align 4
  .type g_date_to_struct_tm, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_to_struct_tm
#endif
g_date_to_struct_tm:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 378 & 0xffff
#if 378 > 0xffff
  movk ip0, 378 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_valid
  .p2align 4
  .type g_date_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_valid
#endif
g_date_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 379 & 0xffff
#if 379 > 0xffff
  movk ip0, 379 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_valid_day
  .p2align 4
  .type g_date_valid_day, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_valid_day
#endif
g_date_valid_day:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 380 & 0xffff
#if 380 > 0xffff
  movk ip0, 380 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_valid_dmy
  .p2align 4
  .type g_date_valid_dmy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_valid_dmy
#endif
g_date_valid_dmy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 381 & 0xffff
#if 381 > 0xffff
  movk ip0, 381 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_valid_julian
  .p2align 4
  .type g_date_valid_julian, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_valid_julian
#endif
g_date_valid_julian:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 382 & 0xffff
#if 382 > 0xffff
  movk ip0, 382 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_valid_month
  .p2align 4
  .type g_date_valid_month, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_valid_month
#endif
g_date_valid_month:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 383 & 0xffff
#if 383 > 0xffff
  movk ip0, 383 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_valid_weekday
  .p2align 4
  .type g_date_valid_weekday, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_valid_weekday
#endif
g_date_valid_weekday:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 384 & 0xffff
#if 384 > 0xffff
  movk ip0, 384 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_date_valid_year
  .p2align 4
  .type g_date_valid_year, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_date_valid_year
#endif
g_date_valid_year:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 385 & 0xffff
#if 385 > 0xffff
  movk ip0, 385 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dcgettext
  .p2align 4
  .type g_dcgettext, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dcgettext
#endif
g_dcgettext:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 386 & 0xffff
#if 386 > 0xffff
  movk ip0, 386 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dgettext
  .p2align 4
  .type g_dgettext, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dgettext
#endif
g_dgettext:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 387 & 0xffff
#if 387 > 0xffff
  movk ip0, 387 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dir_close
  .p2align 4
  .type g_dir_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dir_close
#endif
g_dir_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 388 & 0xffff
#if 388 > 0xffff
  movk ip0, 388 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dir_make_tmp
  .p2align 4
  .type g_dir_make_tmp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dir_make_tmp
#endif
g_dir_make_tmp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 389 & 0xffff
#if 389 > 0xffff
  movk ip0, 389 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dir_open
  .p2align 4
  .type g_dir_open, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dir_open
#endif
g_dir_open:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 390 & 0xffff
#if 390 > 0xffff
  movk ip0, 390 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dir_read_name
  .p2align 4
  .type g_dir_read_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dir_read_name
#endif
g_dir_read_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 391 & 0xffff
#if 391 > 0xffff
  movk ip0, 391 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dir_ref
  .p2align 4
  .type g_dir_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dir_ref
#endif
g_dir_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 392 & 0xffff
#if 392 > 0xffff
  movk ip0, 392 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dir_rewind
  .p2align 4
  .type g_dir_rewind, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dir_rewind
#endif
g_dir_rewind:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 393 & 0xffff
#if 393 > 0xffff
  movk ip0, 393 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dir_unref
  .p2align 4
  .type g_dir_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dir_unref
#endif
g_dir_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 394 & 0xffff
#if 394 > 0xffff
  movk ip0, 394 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_direct_equal
  .p2align 4
  .type g_direct_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_direct_equal
#endif
g_direct_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 395 & 0xffff
#if 395 > 0xffff
  movk ip0, 395 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_direct_hash
  .p2align 4
  .type g_direct_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_direct_hash
#endif
g_direct_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 396 & 0xffff
#if 396 > 0xffff
  movk ip0, 396 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dngettext
  .p2align 4
  .type g_dngettext, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dngettext
#endif
g_dngettext:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 397 & 0xffff
#if 397 > 0xffff
  movk ip0, 397 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_double_equal
  .p2align 4
  .type g_double_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_double_equal
#endif
g_double_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 398 & 0xffff
#if 398 > 0xffff
  movk ip0, 398 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_double_hash
  .p2align 4
  .type g_double_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_double_hash
#endif
g_double_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 399 & 0xffff
#if 399 > 0xffff
  movk ip0, 399 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dpgettext
  .p2align 4
  .type g_dpgettext, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dpgettext
#endif
g_dpgettext:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 400 & 0xffff
#if 400 > 0xffff
  movk ip0, 400 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_dpgettext2
  .p2align 4
  .type g_dpgettext2, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_dpgettext2
#endif
g_dpgettext2:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 401 & 0xffff
#if 401 > 0xffff
  movk ip0, 401 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_environ_getenv
  .p2align 4
  .type g_environ_getenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_environ_getenv
#endif
g_environ_getenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 402 & 0xffff
#if 402 > 0xffff
  movk ip0, 402 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_environ_setenv
  .p2align 4
  .type g_environ_setenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_environ_setenv
#endif
g_environ_setenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 403 & 0xffff
#if 403 > 0xffff
  movk ip0, 403 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_environ_unsetenv
  .p2align 4
  .type g_environ_unsetenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_environ_unsetenv
#endif
g_environ_unsetenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 404 & 0xffff
#if 404 > 0xffff
  movk ip0, 404 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_copy
  .p2align 4
  .type g_error_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_copy
#endif
g_error_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 405 & 0xffff
#if 405 > 0xffff
  movk ip0, 405 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_domain_register
  .p2align 4
  .type g_error_domain_register, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_domain_register
#endif
g_error_domain_register:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 406 & 0xffff
#if 406 > 0xffff
  movk ip0, 406 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_domain_register_static
  .p2align 4
  .type g_error_domain_register_static, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_domain_register_static
#endif
g_error_domain_register_static:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 407 & 0xffff
#if 407 > 0xffff
  movk ip0, 407 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_free
  .p2align 4
  .type g_error_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_free
#endif
g_error_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 408 & 0xffff
#if 408 > 0xffff
  movk ip0, 408 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_matches
  .p2align 4
  .type g_error_matches, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_matches
#endif
g_error_matches:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 409 & 0xffff
#if 409 > 0xffff
  movk ip0, 409 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_new
  .p2align 4
  .type g_error_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_new
#endif
g_error_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 410 & 0xffff
#if 410 > 0xffff
  movk ip0, 410 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_new_literal
  .p2align 4
  .type g_error_new_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_new_literal
#endif
g_error_new_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 411 & 0xffff
#if 411 > 0xffff
  movk ip0, 411 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_error_new_valist
  .p2align 4
  .type g_error_new_valist, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_error_new_valist
#endif
g_error_new_valist:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 412 & 0xffff
#if 412 > 0xffff
  movk ip0, 412 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_fdwalk_set_cloexec
  .p2align 4
  .type g_fdwalk_set_cloexec, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_fdwalk_set_cloexec
#endif
g_fdwalk_set_cloexec:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 413 & 0xffff
#if 413 > 0xffff
  movk ip0, 413 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_error_from_errno
  .p2align 4
  .type g_file_error_from_errno, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_error_from_errno
#endif
g_file_error_from_errno:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 414 & 0xffff
#if 414 > 0xffff
  movk ip0, 414 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_error_quark
  .p2align 4
  .type g_file_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_error_quark
#endif
g_file_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 415 & 0xffff
#if 415 > 0xffff
  movk ip0, 415 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_get_contents
  .p2align 4
  .type g_file_get_contents, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_get_contents
#endif
g_file_get_contents:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 416 & 0xffff
#if 416 > 0xffff
  movk ip0, 416 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_open_tmp
  .p2align 4
  .type g_file_open_tmp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_open_tmp
#endif
g_file_open_tmp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 417 & 0xffff
#if 417 > 0xffff
  movk ip0, 417 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_read_link
  .p2align 4
  .type g_file_read_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_read_link
#endif
g_file_read_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 418 & 0xffff
#if 418 > 0xffff
  movk ip0, 418 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_contents
  .p2align 4
  .type g_file_set_contents, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_contents
#endif
g_file_set_contents:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 419 & 0xffff
#if 419 > 0xffff
  movk ip0, 419 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_set_contents_full
  .p2align 4
  .type g_file_set_contents_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_set_contents_full
#endif
g_file_set_contents_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 420 & 0xffff
#if 420 > 0xffff
  movk ip0, 420 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_file_test
  .p2align 4
  .type g_file_test, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_file_test
#endif
g_file_test:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 421 & 0xffff
#if 421 > 0xffff
  movk ip0, 421 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_display_basename
  .p2align 4
  .type g_filename_display_basename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_display_basename
#endif
g_filename_display_basename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 422 & 0xffff
#if 422 > 0xffff
  movk ip0, 422 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_display_name
  .p2align 4
  .type g_filename_display_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_display_name
#endif
g_filename_display_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 423 & 0xffff
#if 423 > 0xffff
  movk ip0, 423 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_from_uri
  .p2align 4
  .type g_filename_from_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_from_uri
#endif
g_filename_from_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 424 & 0xffff
#if 424 > 0xffff
  movk ip0, 424 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_from_utf8
  .p2align 4
  .type g_filename_from_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_from_utf8
#endif
g_filename_from_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 425 & 0xffff
#if 425 > 0xffff
  movk ip0, 425 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_to_uri
  .p2align 4
  .type g_filename_to_uri, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_to_uri
#endif
g_filename_to_uri:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 426 & 0xffff
#if 426 > 0xffff
  movk ip0, 426 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_filename_to_utf8
  .p2align 4
  .type g_filename_to_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_filename_to_utf8
#endif
g_filename_to_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 427 & 0xffff
#if 427 > 0xffff
  movk ip0, 427 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_find_program_in_path
  .p2align 4
  .type g_find_program_in_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_find_program_in_path
#endif
g_find_program_in_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 428 & 0xffff
#if 428 > 0xffff
  movk ip0, 428 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_fopen
  .p2align 4
  .type g_fopen, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_fopen
#endif
g_fopen:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 429 & 0xffff
#if 429 > 0xffff
  movk ip0, 429 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_format_size
  .p2align 4
  .type g_format_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_format_size
#endif
g_format_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 430 & 0xffff
#if 430 > 0xffff
  movk ip0, 430 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_format_size_for_display
  .p2align 4
  .type g_format_size_for_display, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_format_size_for_display
#endif
g_format_size_for_display:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 431 & 0xffff
#if 431 > 0xffff
  movk ip0, 431 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_format_size_full
  .p2align 4
  .type g_format_size_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_format_size_full
#endif
g_format_size_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 432 & 0xffff
#if 432 > 0xffff
  movk ip0, 432 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_fprintf
  .p2align 4
  .type g_fprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_fprintf
#endif
g_fprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 433 & 0xffff
#if 433 > 0xffff
  movk ip0, 433 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_free
  .p2align 4
  .type g_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_free
#endif
g_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 434 & 0xffff
#if 434 > 0xffff
  movk ip0, 434 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_free_sized
  .p2align 4
  .type g_free_sized, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_free_sized
#endif
g_free_sized:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 435 & 0xffff
#if 435 > 0xffff
  movk ip0, 435 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_freopen
  .p2align 4
  .type g_freopen, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_freopen
#endif
g_freopen:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 436 & 0xffff
#if 436 > 0xffff
  movk ip0, 436 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_fsync
  .p2align 4
  .type g_fsync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_fsync
#endif
g_fsync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 437 & 0xffff
#if 437 > 0xffff
  movk ip0, 437 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_application_name
  .p2align 4
  .type g_get_application_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_application_name
#endif
g_get_application_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 438 & 0xffff
#if 438 > 0xffff
  movk ip0, 438 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_charset
  .p2align 4
  .type g_get_charset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_charset
#endif
g_get_charset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 439 & 0xffff
#if 439 > 0xffff
  movk ip0, 439 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_codeset
  .p2align 4
  .type g_get_codeset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_codeset
#endif
g_get_codeset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 440 & 0xffff
#if 440 > 0xffff
  movk ip0, 440 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_console_charset
  .p2align 4
  .type g_get_console_charset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_console_charset
#endif
g_get_console_charset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 441 & 0xffff
#if 441 > 0xffff
  movk ip0, 441 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_current_dir
  .p2align 4
  .type g_get_current_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_current_dir
#endif
g_get_current_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 442 & 0xffff
#if 442 > 0xffff
  movk ip0, 442 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_current_time
  .p2align 4
  .type g_get_current_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_current_time
#endif
g_get_current_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 443 & 0xffff
#if 443 > 0xffff
  movk ip0, 443 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_environ
  .p2align 4
  .type g_get_environ, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_environ
#endif
g_get_environ:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 444 & 0xffff
#if 444 > 0xffff
  movk ip0, 444 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_filename_charsets
  .p2align 4
  .type g_get_filename_charsets, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_filename_charsets
#endif
g_get_filename_charsets:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 445 & 0xffff
#if 445 > 0xffff
  movk ip0, 445 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_home_dir
  .p2align 4
  .type g_get_home_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_home_dir
#endif
g_get_home_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 446 & 0xffff
#if 446 > 0xffff
  movk ip0, 446 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_host_name
  .p2align 4
  .type g_get_host_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_host_name
#endif
g_get_host_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 447 & 0xffff
#if 447 > 0xffff
  movk ip0, 447 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_language_names
  .p2align 4
  .type g_get_language_names, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_language_names
#endif
g_get_language_names:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 448 & 0xffff
#if 448 > 0xffff
  movk ip0, 448 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_language_names_with_category
  .p2align 4
  .type g_get_language_names_with_category, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_language_names_with_category
#endif
g_get_language_names_with_category:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 449 & 0xffff
#if 449 > 0xffff
  movk ip0, 449 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_locale_variants
  .p2align 4
  .type g_get_locale_variants, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_locale_variants
#endif
g_get_locale_variants:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 450 & 0xffff
#if 450 > 0xffff
  movk ip0, 450 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_monotonic_time
  .p2align 4
  .type g_get_monotonic_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_monotonic_time
#endif
g_get_monotonic_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 451 & 0xffff
#if 451 > 0xffff
  movk ip0, 451 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_num_processors
  .p2align 4
  .type g_get_num_processors, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_num_processors
#endif
g_get_num_processors:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 452 & 0xffff
#if 452 > 0xffff
  movk ip0, 452 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_os_info
  .p2align 4
  .type g_get_os_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_os_info
#endif
g_get_os_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 453 & 0xffff
#if 453 > 0xffff
  movk ip0, 453 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_prgname
  .p2align 4
  .type g_get_prgname, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_prgname
#endif
g_get_prgname:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 454 & 0xffff
#if 454 > 0xffff
  movk ip0, 454 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_real_name
  .p2align 4
  .type g_get_real_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_real_name
#endif
g_get_real_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 455 & 0xffff
#if 455 > 0xffff
  movk ip0, 455 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_real_time
  .p2align 4
  .type g_get_real_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_real_time
#endif
g_get_real_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 456 & 0xffff
#if 456 > 0xffff
  movk ip0, 456 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_system_config_dirs
  .p2align 4
  .type g_get_system_config_dirs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_system_config_dirs
#endif
g_get_system_config_dirs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 457 & 0xffff
#if 457 > 0xffff
  movk ip0, 457 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_system_data_dirs
  .p2align 4
  .type g_get_system_data_dirs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_system_data_dirs
#endif
g_get_system_data_dirs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 458 & 0xffff
#if 458 > 0xffff
  movk ip0, 458 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_tmp_dir
  .p2align 4
  .type g_get_tmp_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_tmp_dir
#endif
g_get_tmp_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 459 & 0xffff
#if 459 > 0xffff
  movk ip0, 459 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_user_cache_dir
  .p2align 4
  .type g_get_user_cache_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_user_cache_dir
#endif
g_get_user_cache_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 460 & 0xffff
#if 460 > 0xffff
  movk ip0, 460 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_user_config_dir
  .p2align 4
  .type g_get_user_config_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_user_config_dir
#endif
g_get_user_config_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 461 & 0xffff
#if 461 > 0xffff
  movk ip0, 461 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_user_data_dir
  .p2align 4
  .type g_get_user_data_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_user_data_dir
#endif
g_get_user_data_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 462 & 0xffff
#if 462 > 0xffff
  movk ip0, 462 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_user_name
  .p2align 4
  .type g_get_user_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_user_name
#endif
g_get_user_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 463 & 0xffff
#if 463 > 0xffff
  movk ip0, 463 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_user_runtime_dir
  .p2align 4
  .type g_get_user_runtime_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_user_runtime_dir
#endif
g_get_user_runtime_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 464 & 0xffff
#if 464 > 0xffff
  movk ip0, 464 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_user_special_dir
  .p2align 4
  .type g_get_user_special_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_user_special_dir
#endif
g_get_user_special_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 465 & 0xffff
#if 465 > 0xffff
  movk ip0, 465 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_get_user_state_dir
  .p2align 4
  .type g_get_user_state_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_get_user_state_dir
#endif
g_get_user_state_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 466 & 0xffff
#if 466 > 0xffff
  movk ip0, 466 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_getenv
  .p2align 4
  .type g_getenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_getenv
#endif
g_getenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 467 & 0xffff
#if 467 > 0xffff
  movk ip0, 467 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_add
  .p2align 4
  .type g_hash_table_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_add
#endif
g_hash_table_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 468 & 0xffff
#if 468 > 0xffff
  movk ip0, 468 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_contains
  .p2align 4
  .type g_hash_table_contains, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_contains
#endif
g_hash_table_contains:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 469 & 0xffff
#if 469 > 0xffff
  movk ip0, 469 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_destroy
  .p2align 4
  .type g_hash_table_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_destroy
#endif
g_hash_table_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 470 & 0xffff
#if 470 > 0xffff
  movk ip0, 470 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_find
  .p2align 4
  .type g_hash_table_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_find
#endif
g_hash_table_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 471 & 0xffff
#if 471 > 0xffff
  movk ip0, 471 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_foreach
  .p2align 4
  .type g_hash_table_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_foreach
#endif
g_hash_table_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 472 & 0xffff
#if 472 > 0xffff
  movk ip0, 472 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_foreach_remove
  .p2align 4
  .type g_hash_table_foreach_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_foreach_remove
#endif
g_hash_table_foreach_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 473 & 0xffff
#if 473 > 0xffff
  movk ip0, 473 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_foreach_steal
  .p2align 4
  .type g_hash_table_foreach_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_foreach_steal
#endif
g_hash_table_foreach_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 474 & 0xffff
#if 474 > 0xffff
  movk ip0, 474 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_get_keys
  .p2align 4
  .type g_hash_table_get_keys, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_get_keys
#endif
g_hash_table_get_keys:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 475 & 0xffff
#if 475 > 0xffff
  movk ip0, 475 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_get_keys_as_array
  .p2align 4
  .type g_hash_table_get_keys_as_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_get_keys_as_array
#endif
g_hash_table_get_keys_as_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 476 & 0xffff
#if 476 > 0xffff
  movk ip0, 476 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_get_keys_as_ptr_array
  .p2align 4
  .type g_hash_table_get_keys_as_ptr_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_get_keys_as_ptr_array
#endif
g_hash_table_get_keys_as_ptr_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 477 & 0xffff
#if 477 > 0xffff
  movk ip0, 477 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_get_values
  .p2align 4
  .type g_hash_table_get_values, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_get_values
#endif
g_hash_table_get_values:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 478 & 0xffff
#if 478 > 0xffff
  movk ip0, 478 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_get_values_as_ptr_array
  .p2align 4
  .type g_hash_table_get_values_as_ptr_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_get_values_as_ptr_array
#endif
g_hash_table_get_values_as_ptr_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 479 & 0xffff
#if 479 > 0xffff
  movk ip0, 479 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_insert
  .p2align 4
  .type g_hash_table_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_insert
#endif
g_hash_table_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 480 & 0xffff
#if 480 > 0xffff
  movk ip0, 480 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_iter_get_hash_table
  .p2align 4
  .type g_hash_table_iter_get_hash_table, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_iter_get_hash_table
#endif
g_hash_table_iter_get_hash_table:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 481 & 0xffff
#if 481 > 0xffff
  movk ip0, 481 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_iter_init
  .p2align 4
  .type g_hash_table_iter_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_iter_init
#endif
g_hash_table_iter_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 482 & 0xffff
#if 482 > 0xffff
  movk ip0, 482 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_iter_next
  .p2align 4
  .type g_hash_table_iter_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_iter_next
#endif
g_hash_table_iter_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 483 & 0xffff
#if 483 > 0xffff
  movk ip0, 483 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_iter_remove
  .p2align 4
  .type g_hash_table_iter_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_iter_remove
#endif
g_hash_table_iter_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 484 & 0xffff
#if 484 > 0xffff
  movk ip0, 484 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_iter_replace
  .p2align 4
  .type g_hash_table_iter_replace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_iter_replace
#endif
g_hash_table_iter_replace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 485 & 0xffff
#if 485 > 0xffff
  movk ip0, 485 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_iter_steal
  .p2align 4
  .type g_hash_table_iter_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_iter_steal
#endif
g_hash_table_iter_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 486 & 0xffff
#if 486 > 0xffff
  movk ip0, 486 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_lookup
  .p2align 4
  .type g_hash_table_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_lookup
#endif
g_hash_table_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 487 & 0xffff
#if 487 > 0xffff
  movk ip0, 487 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_lookup_extended
  .p2align 4
  .type g_hash_table_lookup_extended, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_lookup_extended
#endif
g_hash_table_lookup_extended:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 488 & 0xffff
#if 488 > 0xffff
  movk ip0, 488 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_new
  .p2align 4
  .type g_hash_table_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_new
#endif
g_hash_table_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 489 & 0xffff
#if 489 > 0xffff
  movk ip0, 489 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_new_full
  .p2align 4
  .type g_hash_table_new_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_new_full
#endif
g_hash_table_new_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 490 & 0xffff
#if 490 > 0xffff
  movk ip0, 490 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_new_similar
  .p2align 4
  .type g_hash_table_new_similar, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_new_similar
#endif
g_hash_table_new_similar:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 491 & 0xffff
#if 491 > 0xffff
  movk ip0, 491 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_ref
  .p2align 4
  .type g_hash_table_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_ref
#endif
g_hash_table_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 492 & 0xffff
#if 492 > 0xffff
  movk ip0, 492 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_remove
  .p2align 4
  .type g_hash_table_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_remove
#endif
g_hash_table_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 493 & 0xffff
#if 493 > 0xffff
  movk ip0, 493 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_remove_all
  .p2align 4
  .type g_hash_table_remove_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_remove_all
#endif
g_hash_table_remove_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 494 & 0xffff
#if 494 > 0xffff
  movk ip0, 494 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_replace
  .p2align 4
  .type g_hash_table_replace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_replace
#endif
g_hash_table_replace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 495 & 0xffff
#if 495 > 0xffff
  movk ip0, 495 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_size
  .p2align 4
  .type g_hash_table_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_size
#endif
g_hash_table_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 496 & 0xffff
#if 496 > 0xffff
  movk ip0, 496 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_steal
  .p2align 4
  .type g_hash_table_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_steal
#endif
g_hash_table_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 497 & 0xffff
#if 497 > 0xffff
  movk ip0, 497 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_steal_all
  .p2align 4
  .type g_hash_table_steal_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_steal_all
#endif
g_hash_table_steal_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 498 & 0xffff
#if 498 > 0xffff
  movk ip0, 498 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_steal_all_keys
  .p2align 4
  .type g_hash_table_steal_all_keys, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_steal_all_keys
#endif
g_hash_table_steal_all_keys:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+3992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+3992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 499 & 0xffff
#if 499 > 0xffff
  movk ip0, 499 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_steal_all_values
  .p2align 4
  .type g_hash_table_steal_all_values, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_steal_all_values
#endif
g_hash_table_steal_all_values:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 500 & 0xffff
#if 500 > 0xffff
  movk ip0, 500 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_steal_extended
  .p2align 4
  .type g_hash_table_steal_extended, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_steal_extended
#endif
g_hash_table_steal_extended:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 501 & 0xffff
#if 501 > 0xffff
  movk ip0, 501 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hash_table_unref
  .p2align 4
  .type g_hash_table_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hash_table_unref
#endif
g_hash_table_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 502 & 0xffff
#if 502 > 0xffff
  movk ip0, 502 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hmac_copy
  .p2align 4
  .type g_hmac_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hmac_copy
#endif
g_hmac_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 503 & 0xffff
#if 503 > 0xffff
  movk ip0, 503 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hmac_get_digest
  .p2align 4
  .type g_hmac_get_digest, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hmac_get_digest
#endif
g_hmac_get_digest:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 504 & 0xffff
#if 504 > 0xffff
  movk ip0, 504 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hmac_get_string
  .p2align 4
  .type g_hmac_get_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hmac_get_string
#endif
g_hmac_get_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 505 & 0xffff
#if 505 > 0xffff
  movk ip0, 505 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hmac_new
  .p2align 4
  .type g_hmac_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hmac_new
#endif
g_hmac_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 506 & 0xffff
#if 506 > 0xffff
  movk ip0, 506 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hmac_ref
  .p2align 4
  .type g_hmac_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hmac_ref
#endif
g_hmac_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 507 & 0xffff
#if 507 > 0xffff
  movk ip0, 507 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hmac_unref
  .p2align 4
  .type g_hmac_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hmac_unref
#endif
g_hmac_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 508 & 0xffff
#if 508 > 0xffff
  movk ip0, 508 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hmac_update
  .p2align 4
  .type g_hmac_update, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hmac_update
#endif
g_hmac_update:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 509 & 0xffff
#if 509 > 0xffff
  movk ip0, 509 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_alloc
  .p2align 4
  .type g_hook_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_alloc
#endif
g_hook_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 510 & 0xffff
#if 510 > 0xffff
  movk ip0, 510 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_compare_ids
  .p2align 4
  .type g_hook_compare_ids, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_compare_ids
#endif
g_hook_compare_ids:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 511 & 0xffff
#if 511 > 0xffff
  movk ip0, 511 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_destroy
  .p2align 4
  .type g_hook_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_destroy
#endif
g_hook_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 512 & 0xffff
#if 512 > 0xffff
  movk ip0, 512 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_destroy_link
  .p2align 4
  .type g_hook_destroy_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_destroy_link
#endif
g_hook_destroy_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 513 & 0xffff
#if 513 > 0xffff
  movk ip0, 513 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_find
  .p2align 4
  .type g_hook_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_find
#endif
g_hook_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 514 & 0xffff
#if 514 > 0xffff
  movk ip0, 514 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_find_data
  .p2align 4
  .type g_hook_find_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_find_data
#endif
g_hook_find_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 515 & 0xffff
#if 515 > 0xffff
  movk ip0, 515 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_find_func
  .p2align 4
  .type g_hook_find_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_find_func
#endif
g_hook_find_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 516 & 0xffff
#if 516 > 0xffff
  movk ip0, 516 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_find_func_data
  .p2align 4
  .type g_hook_find_func_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_find_func_data
#endif
g_hook_find_func_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 517 & 0xffff
#if 517 > 0xffff
  movk ip0, 517 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_first_valid
  .p2align 4
  .type g_hook_first_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_first_valid
#endif
g_hook_first_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 518 & 0xffff
#if 518 > 0xffff
  movk ip0, 518 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_free
  .p2align 4
  .type g_hook_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_free
#endif
g_hook_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 519 & 0xffff
#if 519 > 0xffff
  movk ip0, 519 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_get
  .p2align 4
  .type g_hook_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_get
#endif
g_hook_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 520 & 0xffff
#if 520 > 0xffff
  movk ip0, 520 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_insert_before
  .p2align 4
  .type g_hook_insert_before, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_insert_before
#endif
g_hook_insert_before:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 521 & 0xffff
#if 521 > 0xffff
  movk ip0, 521 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_insert_sorted
  .p2align 4
  .type g_hook_insert_sorted, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_insert_sorted
#endif
g_hook_insert_sorted:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 522 & 0xffff
#if 522 > 0xffff
  movk ip0, 522 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_list_clear
  .p2align 4
  .type g_hook_list_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_list_clear
#endif
g_hook_list_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 523 & 0xffff
#if 523 > 0xffff
  movk ip0, 523 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_list_init
  .p2align 4
  .type g_hook_list_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_list_init
#endif
g_hook_list_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 524 & 0xffff
#if 524 > 0xffff
  movk ip0, 524 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_list_invoke
  .p2align 4
  .type g_hook_list_invoke, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_list_invoke
#endif
g_hook_list_invoke:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 525 & 0xffff
#if 525 > 0xffff
  movk ip0, 525 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_list_invoke_check
  .p2align 4
  .type g_hook_list_invoke_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_list_invoke_check
#endif
g_hook_list_invoke_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 526 & 0xffff
#if 526 > 0xffff
  movk ip0, 526 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_list_marshal
  .p2align 4
  .type g_hook_list_marshal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_list_marshal
#endif
g_hook_list_marshal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 527 & 0xffff
#if 527 > 0xffff
  movk ip0, 527 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_list_marshal_check
  .p2align 4
  .type g_hook_list_marshal_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_list_marshal_check
#endif
g_hook_list_marshal_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 528 & 0xffff
#if 528 > 0xffff
  movk ip0, 528 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_next_valid
  .p2align 4
  .type g_hook_next_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_next_valid
#endif
g_hook_next_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 529 & 0xffff
#if 529 > 0xffff
  movk ip0, 529 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_prepend
  .p2align 4
  .type g_hook_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_prepend
#endif
g_hook_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 530 & 0xffff
#if 530 > 0xffff
  movk ip0, 530 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_ref
  .p2align 4
  .type g_hook_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_ref
#endif
g_hook_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 531 & 0xffff
#if 531 > 0xffff
  movk ip0, 531 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hook_unref
  .p2align 4
  .type g_hook_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hook_unref
#endif
g_hook_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 532 & 0xffff
#if 532 > 0xffff
  movk ip0, 532 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hostname_is_ascii_encoded
  .p2align 4
  .type g_hostname_is_ascii_encoded, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hostname_is_ascii_encoded
#endif
g_hostname_is_ascii_encoded:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 533 & 0xffff
#if 533 > 0xffff
  movk ip0, 533 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hostname_is_ip_address
  .p2align 4
  .type g_hostname_is_ip_address, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hostname_is_ip_address
#endif
g_hostname_is_ip_address:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 534 & 0xffff
#if 534 > 0xffff
  movk ip0, 534 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hostname_is_non_ascii
  .p2align 4
  .type g_hostname_is_non_ascii, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hostname_is_non_ascii
#endif
g_hostname_is_non_ascii:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 535 & 0xffff
#if 535 > 0xffff
  movk ip0, 535 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hostname_to_ascii
  .p2align 4
  .type g_hostname_to_ascii, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hostname_to_ascii
#endif
g_hostname_to_ascii:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 536 & 0xffff
#if 536 > 0xffff
  movk ip0, 536 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_hostname_to_unicode
  .p2align 4
  .type g_hostname_to_unicode, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_hostname_to_unicode
#endif
g_hostname_to_unicode:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 537 & 0xffff
#if 537 > 0xffff
  movk ip0, 537 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_iconv
  .p2align 4
  .type g_iconv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_iconv
#endif
g_iconv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 538 & 0xffff
#if 538 > 0xffff
  movk ip0, 538 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_iconv_close
  .p2align 4
  .type g_iconv_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_iconv_close
#endif
g_iconv_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 539 & 0xffff
#if 539 > 0xffff
  movk ip0, 539 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_iconv_open
  .p2align 4
  .type g_iconv_open, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_iconv_open
#endif
g_iconv_open:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 540 & 0xffff
#if 540 > 0xffff
  movk ip0, 540 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_idle_add
  .p2align 4
  .type g_idle_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_idle_add
#endif
g_idle_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 541 & 0xffff
#if 541 > 0xffff
  movk ip0, 541 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_idle_add_full
  .p2align 4
  .type g_idle_add_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_idle_add_full
#endif
g_idle_add_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 542 & 0xffff
#if 542 > 0xffff
  movk ip0, 542 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_idle_add_once
  .p2align 4
  .type g_idle_add_once, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_idle_add_once
#endif
g_idle_add_once:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 543 & 0xffff
#if 543 > 0xffff
  movk ip0, 543 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_idle_remove_by_data
  .p2align 4
  .type g_idle_remove_by_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_idle_remove_by_data
#endif
g_idle_remove_by_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 544 & 0xffff
#if 544 > 0xffff
  movk ip0, 544 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_idle_source_new
  .p2align 4
  .type g_idle_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_idle_source_new
#endif
g_idle_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 545 & 0xffff
#if 545 > 0xffff
  movk ip0, 545 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_int64_equal
  .p2align 4
  .type g_int64_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_int64_equal
#endif
g_int64_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 546 & 0xffff
#if 546 > 0xffff
  movk ip0, 546 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_int64_hash
  .p2align 4
  .type g_int64_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_int64_hash
#endif
g_int64_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 547 & 0xffff
#if 547 > 0xffff
  movk ip0, 547 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_int_equal
  .p2align 4
  .type g_int_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_int_equal
#endif
g_int_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 548 & 0xffff
#if 548 > 0xffff
  movk ip0, 548 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_int_hash
  .p2align 4
  .type g_int_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_int_hash
#endif
g_int_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 549 & 0xffff
#if 549 > 0xffff
  movk ip0, 549 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_intern_static_string
  .p2align 4
  .type g_intern_static_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_intern_static_string
#endif
g_intern_static_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 550 & 0xffff
#if 550 > 0xffff
  movk ip0, 550 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_intern_string
  .p2align 4
  .type g_intern_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_intern_string
#endif
g_intern_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 551 & 0xffff
#if 551 > 0xffff
  movk ip0, 551 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_add_watch
  .p2align 4
  .type g_io_add_watch, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_add_watch
#endif
g_io_add_watch:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 552 & 0xffff
#if 552 > 0xffff
  movk ip0, 552 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_add_watch_full
  .p2align 4
  .type g_io_add_watch_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_add_watch_full
#endif
g_io_add_watch_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 553 & 0xffff
#if 553 > 0xffff
  movk ip0, 553 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_close
  .p2align 4
  .type g_io_channel_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_close
#endif
g_io_channel_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 554 & 0xffff
#if 554 > 0xffff
  movk ip0, 554 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_error_from_errno
  .p2align 4
  .type g_io_channel_error_from_errno, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_error_from_errno
#endif
g_io_channel_error_from_errno:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 555 & 0xffff
#if 555 > 0xffff
  movk ip0, 555 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_error_quark
  .p2align 4
  .type g_io_channel_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_error_quark
#endif
g_io_channel_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 556 & 0xffff
#if 556 > 0xffff
  movk ip0, 556 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_flush
  .p2align 4
  .type g_io_channel_flush, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_flush
#endif
g_io_channel_flush:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 557 & 0xffff
#if 557 > 0xffff
  movk ip0, 557 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_get_buffer_condition
  .p2align 4
  .type g_io_channel_get_buffer_condition, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_get_buffer_condition
#endif
g_io_channel_get_buffer_condition:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 558 & 0xffff
#if 558 > 0xffff
  movk ip0, 558 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_get_buffer_size
  .p2align 4
  .type g_io_channel_get_buffer_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_get_buffer_size
#endif
g_io_channel_get_buffer_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 559 & 0xffff
#if 559 > 0xffff
  movk ip0, 559 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_get_buffered
  .p2align 4
  .type g_io_channel_get_buffered, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_get_buffered
#endif
g_io_channel_get_buffered:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 560 & 0xffff
#if 560 > 0xffff
  movk ip0, 560 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_get_close_on_unref
  .p2align 4
  .type g_io_channel_get_close_on_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_get_close_on_unref
#endif
g_io_channel_get_close_on_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 561 & 0xffff
#if 561 > 0xffff
  movk ip0, 561 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_get_encoding
  .p2align 4
  .type g_io_channel_get_encoding, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_get_encoding
#endif
g_io_channel_get_encoding:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 562 & 0xffff
#if 562 > 0xffff
  movk ip0, 562 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_get_flags
  .p2align 4
  .type g_io_channel_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_get_flags
#endif
g_io_channel_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 563 & 0xffff
#if 563 > 0xffff
  movk ip0, 563 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_get_line_term
  .p2align 4
  .type g_io_channel_get_line_term, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_get_line_term
#endif
g_io_channel_get_line_term:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 564 & 0xffff
#if 564 > 0xffff
  movk ip0, 564 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_init
  .p2align 4
  .type g_io_channel_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_init
#endif
g_io_channel_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 565 & 0xffff
#if 565 > 0xffff
  movk ip0, 565 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_new_file
  .p2align 4
  .type g_io_channel_new_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_new_file
#endif
g_io_channel_new_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 566 & 0xffff
#if 566 > 0xffff
  movk ip0, 566 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_read
  .p2align 4
  .type g_io_channel_read, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_read
#endif
g_io_channel_read:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 567 & 0xffff
#if 567 > 0xffff
  movk ip0, 567 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_read_chars
  .p2align 4
  .type g_io_channel_read_chars, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_read_chars
#endif
g_io_channel_read_chars:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 568 & 0xffff
#if 568 > 0xffff
  movk ip0, 568 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_read_line
  .p2align 4
  .type g_io_channel_read_line, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_read_line
#endif
g_io_channel_read_line:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 569 & 0xffff
#if 569 > 0xffff
  movk ip0, 569 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_read_line_string
  .p2align 4
  .type g_io_channel_read_line_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_read_line_string
#endif
g_io_channel_read_line_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 570 & 0xffff
#if 570 > 0xffff
  movk ip0, 570 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_read_to_end
  .p2align 4
  .type g_io_channel_read_to_end, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_read_to_end
#endif
g_io_channel_read_to_end:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 571 & 0xffff
#if 571 > 0xffff
  movk ip0, 571 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_read_unichar
  .p2align 4
  .type g_io_channel_read_unichar, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_read_unichar
#endif
g_io_channel_read_unichar:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 572 & 0xffff
#if 572 > 0xffff
  movk ip0, 572 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_ref
  .p2align 4
  .type g_io_channel_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_ref
#endif
g_io_channel_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 573 & 0xffff
#if 573 > 0xffff
  movk ip0, 573 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_seek
  .p2align 4
  .type g_io_channel_seek, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_seek
#endif
g_io_channel_seek:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 574 & 0xffff
#if 574 > 0xffff
  movk ip0, 574 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_seek_position
  .p2align 4
  .type g_io_channel_seek_position, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_seek_position
#endif
g_io_channel_seek_position:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 575 & 0xffff
#if 575 > 0xffff
  movk ip0, 575 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_set_buffer_size
  .p2align 4
  .type g_io_channel_set_buffer_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_set_buffer_size
#endif
g_io_channel_set_buffer_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 576 & 0xffff
#if 576 > 0xffff
  movk ip0, 576 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_set_buffered
  .p2align 4
  .type g_io_channel_set_buffered, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_set_buffered
#endif
g_io_channel_set_buffered:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 577 & 0xffff
#if 577 > 0xffff
  movk ip0, 577 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_set_close_on_unref
  .p2align 4
  .type g_io_channel_set_close_on_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_set_close_on_unref
#endif
g_io_channel_set_close_on_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 578 & 0xffff
#if 578 > 0xffff
  movk ip0, 578 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_set_encoding
  .p2align 4
  .type g_io_channel_set_encoding, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_set_encoding
#endif
g_io_channel_set_encoding:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 579 & 0xffff
#if 579 > 0xffff
  movk ip0, 579 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_set_flags
  .p2align 4
  .type g_io_channel_set_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_set_flags
#endif
g_io_channel_set_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 580 & 0xffff
#if 580 > 0xffff
  movk ip0, 580 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_set_line_term
  .p2align 4
  .type g_io_channel_set_line_term, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_set_line_term
#endif
g_io_channel_set_line_term:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 581 & 0xffff
#if 581 > 0xffff
  movk ip0, 581 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_shutdown
  .p2align 4
  .type g_io_channel_shutdown, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_shutdown
#endif
g_io_channel_shutdown:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 582 & 0xffff
#if 582 > 0xffff
  movk ip0, 582 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_unix_get_fd
  .p2align 4
  .type g_io_channel_unix_get_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_unix_get_fd
#endif
g_io_channel_unix_get_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 583 & 0xffff
#if 583 > 0xffff
  movk ip0, 583 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_unix_new
  .p2align 4
  .type g_io_channel_unix_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_unix_new
#endif
g_io_channel_unix_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 584 & 0xffff
#if 584 > 0xffff
  movk ip0, 584 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_unref
  .p2align 4
  .type g_io_channel_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_unref
#endif
g_io_channel_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 585 & 0xffff
#if 585 > 0xffff
  movk ip0, 585 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_write
  .p2align 4
  .type g_io_channel_write, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_write
#endif
g_io_channel_write:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 586 & 0xffff
#if 586 > 0xffff
  movk ip0, 586 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_write_chars
  .p2align 4
  .type g_io_channel_write_chars, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_write_chars
#endif
g_io_channel_write_chars:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 587 & 0xffff
#if 587 > 0xffff
  movk ip0, 587 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_channel_write_unichar
  .p2align 4
  .type g_io_channel_write_unichar, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_channel_write_unichar
#endif
g_io_channel_write_unichar:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 588 & 0xffff
#if 588 > 0xffff
  movk ip0, 588 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_io_create_watch
  .p2align 4
  .type g_io_create_watch, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_io_create_watch
#endif
g_io_create_watch:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 589 & 0xffff
#if 589 > 0xffff
  movk ip0, 589 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_error_quark
  .p2align 4
  .type g_key_file_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_error_quark
#endif
g_key_file_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 590 & 0xffff
#if 590 > 0xffff
  movk ip0, 590 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_free
  .p2align 4
  .type g_key_file_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_free
#endif
g_key_file_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 591 & 0xffff
#if 591 > 0xffff
  movk ip0, 591 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_boolean
  .p2align 4
  .type g_key_file_get_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_boolean
#endif
g_key_file_get_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 592 & 0xffff
#if 592 > 0xffff
  movk ip0, 592 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_boolean_list
  .p2align 4
  .type g_key_file_get_boolean_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_boolean_list
#endif
g_key_file_get_boolean_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 593 & 0xffff
#if 593 > 0xffff
  movk ip0, 593 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_comment
  .p2align 4
  .type g_key_file_get_comment, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_comment
#endif
g_key_file_get_comment:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 594 & 0xffff
#if 594 > 0xffff
  movk ip0, 594 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_double
  .p2align 4
  .type g_key_file_get_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_double
#endif
g_key_file_get_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 595 & 0xffff
#if 595 > 0xffff
  movk ip0, 595 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_double_list
  .p2align 4
  .type g_key_file_get_double_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_double_list
#endif
g_key_file_get_double_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 596 & 0xffff
#if 596 > 0xffff
  movk ip0, 596 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_groups
  .p2align 4
  .type g_key_file_get_groups, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_groups
#endif
g_key_file_get_groups:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 597 & 0xffff
#if 597 > 0xffff
  movk ip0, 597 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_int64
  .p2align 4
  .type g_key_file_get_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_int64
#endif
g_key_file_get_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 598 & 0xffff
#if 598 > 0xffff
  movk ip0, 598 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_integer
  .p2align 4
  .type g_key_file_get_integer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_integer
#endif
g_key_file_get_integer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 599 & 0xffff
#if 599 > 0xffff
  movk ip0, 599 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_integer_list
  .p2align 4
  .type g_key_file_get_integer_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_integer_list
#endif
g_key_file_get_integer_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 600 & 0xffff
#if 600 > 0xffff
  movk ip0, 600 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_keys
  .p2align 4
  .type g_key_file_get_keys, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_keys
#endif
g_key_file_get_keys:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 601 & 0xffff
#if 601 > 0xffff
  movk ip0, 601 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_locale_for_key
  .p2align 4
  .type g_key_file_get_locale_for_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_locale_for_key
#endif
g_key_file_get_locale_for_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 602 & 0xffff
#if 602 > 0xffff
  movk ip0, 602 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_locale_string
  .p2align 4
  .type g_key_file_get_locale_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_locale_string
#endif
g_key_file_get_locale_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 603 & 0xffff
#if 603 > 0xffff
  movk ip0, 603 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_locale_string_list
  .p2align 4
  .type g_key_file_get_locale_string_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_locale_string_list
#endif
g_key_file_get_locale_string_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 604 & 0xffff
#if 604 > 0xffff
  movk ip0, 604 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_start_group
  .p2align 4
  .type g_key_file_get_start_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_start_group
#endif
g_key_file_get_start_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 605 & 0xffff
#if 605 > 0xffff
  movk ip0, 605 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_string
  .p2align 4
  .type g_key_file_get_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_string
#endif
g_key_file_get_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 606 & 0xffff
#if 606 > 0xffff
  movk ip0, 606 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_string_list
  .p2align 4
  .type g_key_file_get_string_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_string_list
#endif
g_key_file_get_string_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 607 & 0xffff
#if 607 > 0xffff
  movk ip0, 607 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_uint64
  .p2align 4
  .type g_key_file_get_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_uint64
#endif
g_key_file_get_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 608 & 0xffff
#if 608 > 0xffff
  movk ip0, 608 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_get_value
  .p2align 4
  .type g_key_file_get_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_get_value
#endif
g_key_file_get_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 609 & 0xffff
#if 609 > 0xffff
  movk ip0, 609 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_has_group
  .p2align 4
  .type g_key_file_has_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_has_group
#endif
g_key_file_has_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 610 & 0xffff
#if 610 > 0xffff
  movk ip0, 610 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_has_key
  .p2align 4
  .type g_key_file_has_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_has_key
#endif
g_key_file_has_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 611 & 0xffff
#if 611 > 0xffff
  movk ip0, 611 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_load_from_bytes
  .p2align 4
  .type g_key_file_load_from_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_load_from_bytes
#endif
g_key_file_load_from_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 612 & 0xffff
#if 612 > 0xffff
  movk ip0, 612 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_load_from_data
  .p2align 4
  .type g_key_file_load_from_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_load_from_data
#endif
g_key_file_load_from_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 613 & 0xffff
#if 613 > 0xffff
  movk ip0, 613 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_load_from_data_dirs
  .p2align 4
  .type g_key_file_load_from_data_dirs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_load_from_data_dirs
#endif
g_key_file_load_from_data_dirs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 614 & 0xffff
#if 614 > 0xffff
  movk ip0, 614 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_load_from_dirs
  .p2align 4
  .type g_key_file_load_from_dirs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_load_from_dirs
#endif
g_key_file_load_from_dirs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 615 & 0xffff
#if 615 > 0xffff
  movk ip0, 615 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_load_from_file
  .p2align 4
  .type g_key_file_load_from_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_load_from_file
#endif
g_key_file_load_from_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 616 & 0xffff
#if 616 > 0xffff
  movk ip0, 616 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_new
  .p2align 4
  .type g_key_file_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_new
#endif
g_key_file_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 617 & 0xffff
#if 617 > 0xffff
  movk ip0, 617 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_ref
  .p2align 4
  .type g_key_file_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_ref
#endif
g_key_file_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 618 & 0xffff
#if 618 > 0xffff
  movk ip0, 618 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_remove_comment
  .p2align 4
  .type g_key_file_remove_comment, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_remove_comment
#endif
g_key_file_remove_comment:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 619 & 0xffff
#if 619 > 0xffff
  movk ip0, 619 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_remove_group
  .p2align 4
  .type g_key_file_remove_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_remove_group
#endif
g_key_file_remove_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 620 & 0xffff
#if 620 > 0xffff
  movk ip0, 620 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_remove_key
  .p2align 4
  .type g_key_file_remove_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_remove_key
#endif
g_key_file_remove_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 621 & 0xffff
#if 621 > 0xffff
  movk ip0, 621 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_save_to_file
  .p2align 4
  .type g_key_file_save_to_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_save_to_file
#endif
g_key_file_save_to_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 622 & 0xffff
#if 622 > 0xffff
  movk ip0, 622 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_boolean
  .p2align 4
  .type g_key_file_set_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_boolean
#endif
g_key_file_set_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 623 & 0xffff
#if 623 > 0xffff
  movk ip0, 623 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_boolean_list
  .p2align 4
  .type g_key_file_set_boolean_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_boolean_list
#endif
g_key_file_set_boolean_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+4992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+4992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 624 & 0xffff
#if 624 > 0xffff
  movk ip0, 624 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_comment
  .p2align 4
  .type g_key_file_set_comment, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_comment
#endif
g_key_file_set_comment:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 625 & 0xffff
#if 625 > 0xffff
  movk ip0, 625 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_double
  .p2align 4
  .type g_key_file_set_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_double
#endif
g_key_file_set_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 626 & 0xffff
#if 626 > 0xffff
  movk ip0, 626 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_double_list
  .p2align 4
  .type g_key_file_set_double_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_double_list
#endif
g_key_file_set_double_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 627 & 0xffff
#if 627 > 0xffff
  movk ip0, 627 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_int64
  .p2align 4
  .type g_key_file_set_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_int64
#endif
g_key_file_set_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 628 & 0xffff
#if 628 > 0xffff
  movk ip0, 628 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_integer
  .p2align 4
  .type g_key_file_set_integer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_integer
#endif
g_key_file_set_integer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 629 & 0xffff
#if 629 > 0xffff
  movk ip0, 629 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_integer_list
  .p2align 4
  .type g_key_file_set_integer_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_integer_list
#endif
g_key_file_set_integer_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 630 & 0xffff
#if 630 > 0xffff
  movk ip0, 630 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_list_separator
  .p2align 4
  .type g_key_file_set_list_separator, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_list_separator
#endif
g_key_file_set_list_separator:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 631 & 0xffff
#if 631 > 0xffff
  movk ip0, 631 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_locale_string
  .p2align 4
  .type g_key_file_set_locale_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_locale_string
#endif
g_key_file_set_locale_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 632 & 0xffff
#if 632 > 0xffff
  movk ip0, 632 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_locale_string_list
  .p2align 4
  .type g_key_file_set_locale_string_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_locale_string_list
#endif
g_key_file_set_locale_string_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 633 & 0xffff
#if 633 > 0xffff
  movk ip0, 633 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_string
  .p2align 4
  .type g_key_file_set_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_string
#endif
g_key_file_set_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 634 & 0xffff
#if 634 > 0xffff
  movk ip0, 634 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_string_list
  .p2align 4
  .type g_key_file_set_string_list, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_string_list
#endif
g_key_file_set_string_list:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 635 & 0xffff
#if 635 > 0xffff
  movk ip0, 635 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_uint64
  .p2align 4
  .type g_key_file_set_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_uint64
#endif
g_key_file_set_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 636 & 0xffff
#if 636 > 0xffff
  movk ip0, 636 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_set_value
  .p2align 4
  .type g_key_file_set_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_set_value
#endif
g_key_file_set_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 637 & 0xffff
#if 637 > 0xffff
  movk ip0, 637 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_to_data
  .p2align 4
  .type g_key_file_to_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_to_data
#endif
g_key_file_to_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 638 & 0xffff
#if 638 > 0xffff
  movk ip0, 638 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_key_file_unref
  .p2align 4
  .type g_key_file_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_key_file_unref
#endif
g_key_file_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 639 & 0xffff
#if 639 > 0xffff
  movk ip0, 639 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_alloc
  .p2align 4
  .type g_list_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_alloc
#endif
g_list_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 640 & 0xffff
#if 640 > 0xffff
  movk ip0, 640 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_append
  .p2align 4
  .type g_list_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_append
#endif
g_list_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 641 & 0xffff
#if 641 > 0xffff
  movk ip0, 641 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_concat
  .p2align 4
  .type g_list_concat, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_concat
#endif
g_list_concat:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 642 & 0xffff
#if 642 > 0xffff
  movk ip0, 642 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_copy
  .p2align 4
  .type g_list_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_copy
#endif
g_list_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 643 & 0xffff
#if 643 > 0xffff
  movk ip0, 643 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_copy_deep
  .p2align 4
  .type g_list_copy_deep, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_copy_deep
#endif
g_list_copy_deep:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 644 & 0xffff
#if 644 > 0xffff
  movk ip0, 644 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_delete_link
  .p2align 4
  .type g_list_delete_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_delete_link
#endif
g_list_delete_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 645 & 0xffff
#if 645 > 0xffff
  movk ip0, 645 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_find
  .p2align 4
  .type g_list_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_find
#endif
g_list_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 646 & 0xffff
#if 646 > 0xffff
  movk ip0, 646 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_find_custom
  .p2align 4
  .type g_list_find_custom, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_find_custom
#endif
g_list_find_custom:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 647 & 0xffff
#if 647 > 0xffff
  movk ip0, 647 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_first
  .p2align 4
  .type g_list_first, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_first
#endif
g_list_first:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 648 & 0xffff
#if 648 > 0xffff
  movk ip0, 648 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_foreach
  .p2align 4
  .type g_list_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_foreach
#endif
g_list_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 649 & 0xffff
#if 649 > 0xffff
  movk ip0, 649 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_free
  .p2align 4
  .type g_list_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_free
#endif
g_list_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 650 & 0xffff
#if 650 > 0xffff
  movk ip0, 650 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_free_1
  .p2align 4
  .type g_list_free_1, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_free_1
#endif
g_list_free_1:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 651 & 0xffff
#if 651 > 0xffff
  movk ip0, 651 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_free_full
  .p2align 4
  .type g_list_free_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_free_full
#endif
g_list_free_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 652 & 0xffff
#if 652 > 0xffff
  movk ip0, 652 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_index
  .p2align 4
  .type g_list_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_index
#endif
g_list_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 653 & 0xffff
#if 653 > 0xffff
  movk ip0, 653 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_insert
  .p2align 4
  .type g_list_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_insert
#endif
g_list_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 654 & 0xffff
#if 654 > 0xffff
  movk ip0, 654 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_insert_before
  .p2align 4
  .type g_list_insert_before, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_insert_before
#endif
g_list_insert_before:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 655 & 0xffff
#if 655 > 0xffff
  movk ip0, 655 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_insert_before_link
  .p2align 4
  .type g_list_insert_before_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_insert_before_link
#endif
g_list_insert_before_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 656 & 0xffff
#if 656 > 0xffff
  movk ip0, 656 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_insert_sorted
  .p2align 4
  .type g_list_insert_sorted, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_insert_sorted
#endif
g_list_insert_sorted:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 657 & 0xffff
#if 657 > 0xffff
  movk ip0, 657 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_insert_sorted_with_data
  .p2align 4
  .type g_list_insert_sorted_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_insert_sorted_with_data
#endif
g_list_insert_sorted_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 658 & 0xffff
#if 658 > 0xffff
  movk ip0, 658 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_last
  .p2align 4
  .type g_list_last, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_last
#endif
g_list_last:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 659 & 0xffff
#if 659 > 0xffff
  movk ip0, 659 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_length
  .p2align 4
  .type g_list_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_length
#endif
g_list_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 660 & 0xffff
#if 660 > 0xffff
  movk ip0, 660 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_nth
  .p2align 4
  .type g_list_nth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_nth
#endif
g_list_nth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 661 & 0xffff
#if 661 > 0xffff
  movk ip0, 661 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_nth_data
  .p2align 4
  .type g_list_nth_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_nth_data
#endif
g_list_nth_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 662 & 0xffff
#if 662 > 0xffff
  movk ip0, 662 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_nth_prev
  .p2align 4
  .type g_list_nth_prev, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_nth_prev
#endif
g_list_nth_prev:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 663 & 0xffff
#if 663 > 0xffff
  movk ip0, 663 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_pop_allocator
  .p2align 4
  .type g_list_pop_allocator, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_pop_allocator
#endif
g_list_pop_allocator:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 664 & 0xffff
#if 664 > 0xffff
  movk ip0, 664 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_position
  .p2align 4
  .type g_list_position, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_position
#endif
g_list_position:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 665 & 0xffff
#if 665 > 0xffff
  movk ip0, 665 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_prepend
  .p2align 4
  .type g_list_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_prepend
#endif
g_list_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 666 & 0xffff
#if 666 > 0xffff
  movk ip0, 666 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_push_allocator
  .p2align 4
  .type g_list_push_allocator, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_push_allocator
#endif
g_list_push_allocator:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 667 & 0xffff
#if 667 > 0xffff
  movk ip0, 667 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_remove
  .p2align 4
  .type g_list_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_remove
#endif
g_list_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 668 & 0xffff
#if 668 > 0xffff
  movk ip0, 668 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_remove_all
  .p2align 4
  .type g_list_remove_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_remove_all
#endif
g_list_remove_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 669 & 0xffff
#if 669 > 0xffff
  movk ip0, 669 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_remove_link
  .p2align 4
  .type g_list_remove_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_remove_link
#endif
g_list_remove_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 670 & 0xffff
#if 670 > 0xffff
  movk ip0, 670 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_reverse
  .p2align 4
  .type g_list_reverse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_reverse
#endif
g_list_reverse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 671 & 0xffff
#if 671 > 0xffff
  movk ip0, 671 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_sort
  .p2align 4
  .type g_list_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_sort
#endif
g_list_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 672 & 0xffff
#if 672 > 0xffff
  movk ip0, 672 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_list_sort_with_data
  .p2align 4
  .type g_list_sort_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_list_sort_with_data
#endif
g_list_sort_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 673 & 0xffff
#if 673 > 0xffff
  movk ip0, 673 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_listenv
  .p2align 4
  .type g_listenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_listenv
#endif
g_listenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 674 & 0xffff
#if 674 > 0xffff
  movk ip0, 674 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_locale_from_utf8
  .p2align 4
  .type g_locale_from_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_locale_from_utf8
#endif
g_locale_from_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 675 & 0xffff
#if 675 > 0xffff
  movk ip0, 675 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_locale_to_utf8
  .p2align 4
  .type g_locale_to_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_locale_to_utf8
#endif
g_locale_to_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 676 & 0xffff
#if 676 > 0xffff
  movk ip0, 676 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log
  .p2align 4
  .type g_log, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log
#endif
g_log:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 677 & 0xffff
#if 677 > 0xffff
  movk ip0, 677 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_default_handler
  .p2align 4
  .type g_log_default_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_default_handler
#endif
g_log_default_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 678 & 0xffff
#if 678 > 0xffff
  movk ip0, 678 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_get_debug_enabled
  .p2align 4
  .type g_log_get_debug_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_get_debug_enabled
#endif
g_log_get_debug_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 679 & 0xffff
#if 679 > 0xffff
  movk ip0, 679 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_remove_handler
  .p2align 4
  .type g_log_remove_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_remove_handler
#endif
g_log_remove_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 680 & 0xffff
#if 680 > 0xffff
  movk ip0, 680 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_set_always_fatal
  .p2align 4
  .type g_log_set_always_fatal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_set_always_fatal
#endif
g_log_set_always_fatal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 681 & 0xffff
#if 681 > 0xffff
  movk ip0, 681 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_set_debug_enabled
  .p2align 4
  .type g_log_set_debug_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_set_debug_enabled
#endif
g_log_set_debug_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 682 & 0xffff
#if 682 > 0xffff
  movk ip0, 682 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_set_default_handler
  .p2align 4
  .type g_log_set_default_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_set_default_handler
#endif
g_log_set_default_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 683 & 0xffff
#if 683 > 0xffff
  movk ip0, 683 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_set_fatal_mask
  .p2align 4
  .type g_log_set_fatal_mask, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_set_fatal_mask
#endif
g_log_set_fatal_mask:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 684 & 0xffff
#if 684 > 0xffff
  movk ip0, 684 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_set_handler
  .p2align 4
  .type g_log_set_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_set_handler
#endif
g_log_set_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 685 & 0xffff
#if 685 > 0xffff
  movk ip0, 685 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_set_handler_full
  .p2align 4
  .type g_log_set_handler_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_set_handler_full
#endif
g_log_set_handler_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 686 & 0xffff
#if 686 > 0xffff
  movk ip0, 686 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_set_writer_func
  .p2align 4
  .type g_log_set_writer_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_set_writer_func
#endif
g_log_set_writer_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 687 & 0xffff
#if 687 > 0xffff
  movk ip0, 687 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_structured
  .p2align 4
  .type g_log_structured, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_structured
#endif
g_log_structured:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 688 & 0xffff
#if 688 > 0xffff
  movk ip0, 688 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_structured_array
  .p2align 4
  .type g_log_structured_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_structured_array
#endif
g_log_structured_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 689 & 0xffff
#if 689 > 0xffff
  movk ip0, 689 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_structured_standard
  .p2align 4
  .type g_log_structured_standard, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_structured_standard
#endif
g_log_structured_standard:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 690 & 0xffff
#if 690 > 0xffff
  movk ip0, 690 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_variant
  .p2align 4
  .type g_log_variant, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_variant
#endif
g_log_variant:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 691 & 0xffff
#if 691 > 0xffff
  movk ip0, 691 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_default
  .p2align 4
  .type g_log_writer_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_default
#endif
g_log_writer_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 692 & 0xffff
#if 692 > 0xffff
  movk ip0, 692 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_default_set_debug_domains
  .p2align 4
  .type g_log_writer_default_set_debug_domains, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_default_set_debug_domains
#endif
g_log_writer_default_set_debug_domains:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 693 & 0xffff
#if 693 > 0xffff
  movk ip0, 693 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_default_set_use_stderr
  .p2align 4
  .type g_log_writer_default_set_use_stderr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_default_set_use_stderr
#endif
g_log_writer_default_set_use_stderr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 694 & 0xffff
#if 694 > 0xffff
  movk ip0, 694 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_default_would_drop
  .p2align 4
  .type g_log_writer_default_would_drop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_default_would_drop
#endif
g_log_writer_default_would_drop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 695 & 0xffff
#if 695 > 0xffff
  movk ip0, 695 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_format_fields
  .p2align 4
  .type g_log_writer_format_fields, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_format_fields
#endif
g_log_writer_format_fields:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 696 & 0xffff
#if 696 > 0xffff
  movk ip0, 696 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_is_journald
  .p2align 4
  .type g_log_writer_is_journald, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_is_journald
#endif
g_log_writer_is_journald:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 697 & 0xffff
#if 697 > 0xffff
  movk ip0, 697 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_journald
  .p2align 4
  .type g_log_writer_journald, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_journald
#endif
g_log_writer_journald:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 698 & 0xffff
#if 698 > 0xffff
  movk ip0, 698 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_standard_streams
  .p2align 4
  .type g_log_writer_standard_streams, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_standard_streams
#endif
g_log_writer_standard_streams:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 699 & 0xffff
#if 699 > 0xffff
  movk ip0, 699 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_supports_color
  .p2align 4
  .type g_log_writer_supports_color, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_supports_color
#endif
g_log_writer_supports_color:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 700 & 0xffff
#if 700 > 0xffff
  movk ip0, 700 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_log_writer_syslog
  .p2align 4
  .type g_log_writer_syslog, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_log_writer_syslog
#endif
g_log_writer_syslog:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 701 & 0xffff
#if 701 > 0xffff
  movk ip0, 701 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_logv
  .p2align 4
  .type g_logv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_logv
#endif
g_logv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 702 & 0xffff
#if 702 > 0xffff
  movk ip0, 702 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_lstat
  .p2align 4
  .type g_lstat, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_lstat
#endif
g_lstat:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 703 & 0xffff
#if 703 > 0xffff
  movk ip0, 703 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_acquire
  .p2align 4
  .type g_main_context_acquire, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_acquire
#endif
g_main_context_acquire:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 704 & 0xffff
#if 704 > 0xffff
  movk ip0, 704 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_add_poll
  .p2align 4
  .type g_main_context_add_poll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_add_poll
#endif
g_main_context_add_poll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 705 & 0xffff
#if 705 > 0xffff
  movk ip0, 705 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_check
  .p2align 4
  .type g_main_context_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_check
#endif
g_main_context_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 706 & 0xffff
#if 706 > 0xffff
  movk ip0, 706 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_default
  .p2align 4
  .type g_main_context_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_default
#endif
g_main_context_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 707 & 0xffff
#if 707 > 0xffff
  movk ip0, 707 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_dispatch
  .p2align 4
  .type g_main_context_dispatch, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_dispatch
#endif
g_main_context_dispatch:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 708 & 0xffff
#if 708 > 0xffff
  movk ip0, 708 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_find_source_by_funcs_user_data
  .p2align 4
  .type g_main_context_find_source_by_funcs_user_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_find_source_by_funcs_user_data
#endif
g_main_context_find_source_by_funcs_user_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 709 & 0xffff
#if 709 > 0xffff
  movk ip0, 709 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_find_source_by_id
  .p2align 4
  .type g_main_context_find_source_by_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_find_source_by_id
#endif
g_main_context_find_source_by_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 710 & 0xffff
#if 710 > 0xffff
  movk ip0, 710 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_find_source_by_user_data
  .p2align 4
  .type g_main_context_find_source_by_user_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_find_source_by_user_data
#endif
g_main_context_find_source_by_user_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 711 & 0xffff
#if 711 > 0xffff
  movk ip0, 711 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_get_poll_func
  .p2align 4
  .type g_main_context_get_poll_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_get_poll_func
#endif
g_main_context_get_poll_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 712 & 0xffff
#if 712 > 0xffff
  movk ip0, 712 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_get_thread_default
  .p2align 4
  .type g_main_context_get_thread_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_get_thread_default
#endif
g_main_context_get_thread_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 713 & 0xffff
#if 713 > 0xffff
  movk ip0, 713 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_invoke
  .p2align 4
  .type g_main_context_invoke, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_invoke
#endif
g_main_context_invoke:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 714 & 0xffff
#if 714 > 0xffff
  movk ip0, 714 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_invoke_full
  .p2align 4
  .type g_main_context_invoke_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_invoke_full
#endif
g_main_context_invoke_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 715 & 0xffff
#if 715 > 0xffff
  movk ip0, 715 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_is_owner
  .p2align 4
  .type g_main_context_is_owner, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_is_owner
#endif
g_main_context_is_owner:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 716 & 0xffff
#if 716 > 0xffff
  movk ip0, 716 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_iteration
  .p2align 4
  .type g_main_context_iteration, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_iteration
#endif
g_main_context_iteration:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 717 & 0xffff
#if 717 > 0xffff
  movk ip0, 717 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_new
  .p2align 4
  .type g_main_context_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_new
#endif
g_main_context_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 718 & 0xffff
#if 718 > 0xffff
  movk ip0, 718 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_new_with_flags
  .p2align 4
  .type g_main_context_new_with_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_new_with_flags
#endif
g_main_context_new_with_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 719 & 0xffff
#if 719 > 0xffff
  movk ip0, 719 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_pending
  .p2align 4
  .type g_main_context_pending, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_pending
#endif
g_main_context_pending:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 720 & 0xffff
#if 720 > 0xffff
  movk ip0, 720 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_pop_thread_default
  .p2align 4
  .type g_main_context_pop_thread_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_pop_thread_default
#endif
g_main_context_pop_thread_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 721 & 0xffff
#if 721 > 0xffff
  movk ip0, 721 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_prepare
  .p2align 4
  .type g_main_context_prepare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_prepare
#endif
g_main_context_prepare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 722 & 0xffff
#if 722 > 0xffff
  movk ip0, 722 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_push_thread_default
  .p2align 4
  .type g_main_context_push_thread_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_push_thread_default
#endif
g_main_context_push_thread_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 723 & 0xffff
#if 723 > 0xffff
  movk ip0, 723 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_query
  .p2align 4
  .type g_main_context_query, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_query
#endif
g_main_context_query:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 724 & 0xffff
#if 724 > 0xffff
  movk ip0, 724 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_ref
  .p2align 4
  .type g_main_context_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_ref
#endif
g_main_context_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 725 & 0xffff
#if 725 > 0xffff
  movk ip0, 725 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_ref_thread_default
  .p2align 4
  .type g_main_context_ref_thread_default, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_ref_thread_default
#endif
g_main_context_ref_thread_default:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 726 & 0xffff
#if 726 > 0xffff
  movk ip0, 726 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_release
  .p2align 4
  .type g_main_context_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_release
#endif
g_main_context_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 727 & 0xffff
#if 727 > 0xffff
  movk ip0, 727 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_remove_poll
  .p2align 4
  .type g_main_context_remove_poll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_remove_poll
#endif
g_main_context_remove_poll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 728 & 0xffff
#if 728 > 0xffff
  movk ip0, 728 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_set_poll_func
  .p2align 4
  .type g_main_context_set_poll_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_set_poll_func
#endif
g_main_context_set_poll_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 729 & 0xffff
#if 729 > 0xffff
  movk ip0, 729 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_unref
  .p2align 4
  .type g_main_context_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_unref
#endif
g_main_context_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 730 & 0xffff
#if 730 > 0xffff
  movk ip0, 730 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_wait
  .p2align 4
  .type g_main_context_wait, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_wait
#endif
g_main_context_wait:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 731 & 0xffff
#if 731 > 0xffff
  movk ip0, 731 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_context_wakeup
  .p2align 4
  .type g_main_context_wakeup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_context_wakeup
#endif
g_main_context_wakeup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 732 & 0xffff
#if 732 > 0xffff
  movk ip0, 732 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_current_source
  .p2align 4
  .type g_main_current_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_current_source
#endif
g_main_current_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 733 & 0xffff
#if 733 > 0xffff
  movk ip0, 733 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_depth
  .p2align 4
  .type g_main_depth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_depth
#endif
g_main_depth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 734 & 0xffff
#if 734 > 0xffff
  movk ip0, 734 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_loop_get_context
  .p2align 4
  .type g_main_loop_get_context, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_loop_get_context
#endif
g_main_loop_get_context:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 735 & 0xffff
#if 735 > 0xffff
  movk ip0, 735 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_loop_is_running
  .p2align 4
  .type g_main_loop_is_running, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_loop_is_running
#endif
g_main_loop_is_running:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 736 & 0xffff
#if 736 > 0xffff
  movk ip0, 736 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_loop_new
  .p2align 4
  .type g_main_loop_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_loop_new
#endif
g_main_loop_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 737 & 0xffff
#if 737 > 0xffff
  movk ip0, 737 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_loop_quit
  .p2align 4
  .type g_main_loop_quit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_loop_quit
#endif
g_main_loop_quit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 738 & 0xffff
#if 738 > 0xffff
  movk ip0, 738 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_loop_ref
  .p2align 4
  .type g_main_loop_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_loop_ref
#endif
g_main_loop_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 739 & 0xffff
#if 739 > 0xffff
  movk ip0, 739 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_loop_run
  .p2align 4
  .type g_main_loop_run, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_loop_run
#endif
g_main_loop_run:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 740 & 0xffff
#if 740 > 0xffff
  movk ip0, 740 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_main_loop_unref
  .p2align 4
  .type g_main_loop_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_main_loop_unref
#endif
g_main_loop_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 741 & 0xffff
#if 741 > 0xffff
  movk ip0, 741 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_malloc
  .p2align 4
  .type g_malloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_malloc
#endif
g_malloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 742 & 0xffff
#if 742 > 0xffff
  movk ip0, 742 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_malloc0
  .p2align 4
  .type g_malloc0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_malloc0
#endif
g_malloc0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 743 & 0xffff
#if 743 > 0xffff
  movk ip0, 743 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_malloc0_n
  .p2align 4
  .type g_malloc0_n, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_malloc0_n
#endif
g_malloc0_n:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 744 & 0xffff
#if 744 > 0xffff
  movk ip0, 744 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_malloc_n
  .p2align 4
  .type g_malloc_n, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_malloc_n
#endif
g_malloc_n:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 745 & 0xffff
#if 745 > 0xffff
  movk ip0, 745 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_free
  .p2align 4
  .type g_mapped_file_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_free
#endif
g_mapped_file_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 746 & 0xffff
#if 746 > 0xffff
  movk ip0, 746 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_get_bytes
  .p2align 4
  .type g_mapped_file_get_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_get_bytes
#endif
g_mapped_file_get_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 747 & 0xffff
#if 747 > 0xffff
  movk ip0, 747 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_get_contents
  .p2align 4
  .type g_mapped_file_get_contents, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_get_contents
#endif
g_mapped_file_get_contents:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 748 & 0xffff
#if 748 > 0xffff
  movk ip0, 748 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_get_length
  .p2align 4
  .type g_mapped_file_get_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_get_length
#endif
g_mapped_file_get_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+5992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+5992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 749 & 0xffff
#if 749 > 0xffff
  movk ip0, 749 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_new
  .p2align 4
  .type g_mapped_file_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_new
#endif
g_mapped_file_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 750 & 0xffff
#if 750 > 0xffff
  movk ip0, 750 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_new_from_fd
  .p2align 4
  .type g_mapped_file_new_from_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_new_from_fd
#endif
g_mapped_file_new_from_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 751 & 0xffff
#if 751 > 0xffff
  movk ip0, 751 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_ref
  .p2align 4
  .type g_mapped_file_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_ref
#endif
g_mapped_file_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 752 & 0xffff
#if 752 > 0xffff
  movk ip0, 752 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mapped_file_unref
  .p2align 4
  .type g_mapped_file_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mapped_file_unref
#endif
g_mapped_file_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 753 & 0xffff
#if 753 > 0xffff
  movk ip0, 753 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_collect_attributes
  .p2align 4
  .type g_markup_collect_attributes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_collect_attributes
#endif
g_markup_collect_attributes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 754 & 0xffff
#if 754 > 0xffff
  movk ip0, 754 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_error_quark
  .p2align 4
  .type g_markup_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_error_quark
#endif
g_markup_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 755 & 0xffff
#if 755 > 0xffff
  movk ip0, 755 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_escape_text
  .p2align 4
  .type g_markup_escape_text, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_escape_text
#endif
g_markup_escape_text:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 756 & 0xffff
#if 756 > 0xffff
  movk ip0, 756 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_end_parse
  .p2align 4
  .type g_markup_parse_context_end_parse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_end_parse
#endif
g_markup_parse_context_end_parse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 757 & 0xffff
#if 757 > 0xffff
  movk ip0, 757 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_free
  .p2align 4
  .type g_markup_parse_context_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_free
#endif
g_markup_parse_context_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 758 & 0xffff
#if 758 > 0xffff
  movk ip0, 758 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_get_element
  .p2align 4
  .type g_markup_parse_context_get_element, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_get_element
#endif
g_markup_parse_context_get_element:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 759 & 0xffff
#if 759 > 0xffff
  movk ip0, 759 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_get_element_stack
  .p2align 4
  .type g_markup_parse_context_get_element_stack, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_get_element_stack
#endif
g_markup_parse_context_get_element_stack:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 760 & 0xffff
#if 760 > 0xffff
  movk ip0, 760 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_get_position
  .p2align 4
  .type g_markup_parse_context_get_position, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_get_position
#endif
g_markup_parse_context_get_position:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 761 & 0xffff
#if 761 > 0xffff
  movk ip0, 761 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_get_user_data
  .p2align 4
  .type g_markup_parse_context_get_user_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_get_user_data
#endif
g_markup_parse_context_get_user_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 762 & 0xffff
#if 762 > 0xffff
  movk ip0, 762 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_new
  .p2align 4
  .type g_markup_parse_context_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_new
#endif
g_markup_parse_context_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 763 & 0xffff
#if 763 > 0xffff
  movk ip0, 763 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_parse
  .p2align 4
  .type g_markup_parse_context_parse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_parse
#endif
g_markup_parse_context_parse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 764 & 0xffff
#if 764 > 0xffff
  movk ip0, 764 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_pop
  .p2align 4
  .type g_markup_parse_context_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_pop
#endif
g_markup_parse_context_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 765 & 0xffff
#if 765 > 0xffff
  movk ip0, 765 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_push
  .p2align 4
  .type g_markup_parse_context_push, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_push
#endif
g_markup_parse_context_push:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 766 & 0xffff
#if 766 > 0xffff
  movk ip0, 766 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_ref
  .p2align 4
  .type g_markup_parse_context_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_ref
#endif
g_markup_parse_context_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 767 & 0xffff
#if 767 > 0xffff
  movk ip0, 767 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_parse_context_unref
  .p2align 4
  .type g_markup_parse_context_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_parse_context_unref
#endif
g_markup_parse_context_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 768 & 0xffff
#if 768 > 0xffff
  movk ip0, 768 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_printf_escaped
  .p2align 4
  .type g_markup_printf_escaped, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_printf_escaped
#endif
g_markup_printf_escaped:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 769 & 0xffff
#if 769 > 0xffff
  movk ip0, 769 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_markup_vprintf_escaped
  .p2align 4
  .type g_markup_vprintf_escaped, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_markup_vprintf_escaped
#endif
g_markup_vprintf_escaped:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 770 & 0xffff
#if 770 > 0xffff
  movk ip0, 770 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_expand_references
  .p2align 4
  .type g_match_info_expand_references, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_expand_references
#endif
g_match_info_expand_references:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 771 & 0xffff
#if 771 > 0xffff
  movk ip0, 771 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_fetch
  .p2align 4
  .type g_match_info_fetch, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_fetch
#endif
g_match_info_fetch:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 772 & 0xffff
#if 772 > 0xffff
  movk ip0, 772 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_fetch_all
  .p2align 4
  .type g_match_info_fetch_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_fetch_all
#endif
g_match_info_fetch_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 773 & 0xffff
#if 773 > 0xffff
  movk ip0, 773 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_fetch_named
  .p2align 4
  .type g_match_info_fetch_named, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_fetch_named
#endif
g_match_info_fetch_named:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 774 & 0xffff
#if 774 > 0xffff
  movk ip0, 774 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_fetch_named_pos
  .p2align 4
  .type g_match_info_fetch_named_pos, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_fetch_named_pos
#endif
g_match_info_fetch_named_pos:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 775 & 0xffff
#if 775 > 0xffff
  movk ip0, 775 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_fetch_pos
  .p2align 4
  .type g_match_info_fetch_pos, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_fetch_pos
#endif
g_match_info_fetch_pos:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 776 & 0xffff
#if 776 > 0xffff
  movk ip0, 776 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_free
  .p2align 4
  .type g_match_info_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_free
#endif
g_match_info_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 777 & 0xffff
#if 777 > 0xffff
  movk ip0, 777 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_get_match_count
  .p2align 4
  .type g_match_info_get_match_count, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_get_match_count
#endif
g_match_info_get_match_count:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 778 & 0xffff
#if 778 > 0xffff
  movk ip0, 778 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_get_regex
  .p2align 4
  .type g_match_info_get_regex, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_get_regex
#endif
g_match_info_get_regex:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 779 & 0xffff
#if 779 > 0xffff
  movk ip0, 779 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_get_string
  .p2align 4
  .type g_match_info_get_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_get_string
#endif
g_match_info_get_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 780 & 0xffff
#if 780 > 0xffff
  movk ip0, 780 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_is_partial_match
  .p2align 4
  .type g_match_info_is_partial_match, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_is_partial_match
#endif
g_match_info_is_partial_match:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 781 & 0xffff
#if 781 > 0xffff
  movk ip0, 781 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_matches
  .p2align 4
  .type g_match_info_matches, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_matches
#endif
g_match_info_matches:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 782 & 0xffff
#if 782 > 0xffff
  movk ip0, 782 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_next
  .p2align 4
  .type g_match_info_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_next
#endif
g_match_info_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 783 & 0xffff
#if 783 > 0xffff
  movk ip0, 783 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_ref
  .p2align 4
  .type g_match_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_ref
#endif
g_match_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 784 & 0xffff
#if 784 > 0xffff
  movk ip0, 784 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_match_info_unref
  .p2align 4
  .type g_match_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_match_info_unref
#endif
g_match_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 785 & 0xffff
#if 785 > 0xffff
  movk ip0, 785 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_alloc
  .p2align 4
  .type g_mem_chunk_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_alloc
#endif
g_mem_chunk_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 786 & 0xffff
#if 786 > 0xffff
  movk ip0, 786 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_alloc0
  .p2align 4
  .type g_mem_chunk_alloc0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_alloc0
#endif
g_mem_chunk_alloc0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 787 & 0xffff
#if 787 > 0xffff
  movk ip0, 787 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_clean
  .p2align 4
  .type g_mem_chunk_clean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_clean
#endif
g_mem_chunk_clean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 788 & 0xffff
#if 788 > 0xffff
  movk ip0, 788 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_destroy
  .p2align 4
  .type g_mem_chunk_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_destroy
#endif
g_mem_chunk_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 789 & 0xffff
#if 789 > 0xffff
  movk ip0, 789 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_free
  .p2align 4
  .type g_mem_chunk_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_free
#endif
g_mem_chunk_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 790 & 0xffff
#if 790 > 0xffff
  movk ip0, 790 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_info
  .p2align 4
  .type g_mem_chunk_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_info
#endif
g_mem_chunk_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 791 & 0xffff
#if 791 > 0xffff
  movk ip0, 791 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_new
  .p2align 4
  .type g_mem_chunk_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_new
#endif
g_mem_chunk_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 792 & 0xffff
#if 792 > 0xffff
  movk ip0, 792 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_print
  .p2align 4
  .type g_mem_chunk_print, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_print
#endif
g_mem_chunk_print:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 793 & 0xffff
#if 793 > 0xffff
  movk ip0, 793 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_chunk_reset
  .p2align 4
  .type g_mem_chunk_reset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_chunk_reset
#endif
g_mem_chunk_reset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 794 & 0xffff
#if 794 > 0xffff
  movk ip0, 794 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_is_system_malloc
  .p2align 4
  .type g_mem_is_system_malloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_is_system_malloc
#endif
g_mem_is_system_malloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 795 & 0xffff
#if 795 > 0xffff
  movk ip0, 795 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_profile
  .p2align 4
  .type g_mem_profile, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_profile
#endif
g_mem_profile:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 796 & 0xffff
#if 796 > 0xffff
  movk ip0, 796 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mem_set_vtable
  .p2align 4
  .type g_mem_set_vtable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mem_set_vtable
#endif
g_mem_set_vtable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 797 & 0xffff
#if 797 > 0xffff
  movk ip0, 797 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memdup
  .p2align 4
  .type g_memdup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memdup
#endif
g_memdup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 798 & 0xffff
#if 798 > 0xffff
  movk ip0, 798 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_memdup2
  .p2align 4
  .type g_memdup2, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_memdup2
#endif
g_memdup2:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 799 & 0xffff
#if 799 > 0xffff
  movk ip0, 799 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mkdir
  .p2align 4
  .type g_mkdir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mkdir
#endif
g_mkdir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 800 & 0xffff
#if 800 > 0xffff
  movk ip0, 800 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mkdir_with_parents
  .p2align 4
  .type g_mkdir_with_parents, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mkdir_with_parents
#endif
g_mkdir_with_parents:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 801 & 0xffff
#if 801 > 0xffff
  movk ip0, 801 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mkdtemp
  .p2align 4
  .type g_mkdtemp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mkdtemp
#endif
g_mkdtemp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 802 & 0xffff
#if 802 > 0xffff
  movk ip0, 802 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mkdtemp_full
  .p2align 4
  .type g_mkdtemp_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mkdtemp_full
#endif
g_mkdtemp_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 803 & 0xffff
#if 803 > 0xffff
  movk ip0, 803 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mkstemp
  .p2align 4
  .type g_mkstemp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mkstemp
#endif
g_mkstemp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 804 & 0xffff
#if 804 > 0xffff
  movk ip0, 804 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mkstemp_full
  .p2align 4
  .type g_mkstemp_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mkstemp_full
#endif
g_mkstemp_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 805 & 0xffff
#if 805 > 0xffff
  movk ip0, 805 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mutex_clear
  .p2align 4
  .type g_mutex_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mutex_clear
#endif
g_mutex_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 806 & 0xffff
#if 806 > 0xffff
  movk ip0, 806 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mutex_free
  .p2align 4
  .type g_mutex_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mutex_free
#endif
g_mutex_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 807 & 0xffff
#if 807 > 0xffff
  movk ip0, 807 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mutex_init
  .p2align 4
  .type g_mutex_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mutex_init
#endif
g_mutex_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 808 & 0xffff
#if 808 > 0xffff
  movk ip0, 808 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mutex_lock
  .p2align 4
  .type g_mutex_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mutex_lock
#endif
g_mutex_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 809 & 0xffff
#if 809 > 0xffff
  movk ip0, 809 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mutex_new
  .p2align 4
  .type g_mutex_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mutex_new
#endif
g_mutex_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 810 & 0xffff
#if 810 > 0xffff
  movk ip0, 810 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mutex_trylock
  .p2align 4
  .type g_mutex_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mutex_trylock
#endif
g_mutex_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 811 & 0xffff
#if 811 > 0xffff
  movk ip0, 811 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_mutex_unlock
  .p2align 4
  .type g_mutex_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_mutex_unlock
#endif
g_mutex_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 812 & 0xffff
#if 812 > 0xffff
  movk ip0, 812 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_child_index
  .p2align 4
  .type g_node_child_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_child_index
#endif
g_node_child_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 813 & 0xffff
#if 813 > 0xffff
  movk ip0, 813 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_child_position
  .p2align 4
  .type g_node_child_position, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_child_position
#endif
g_node_child_position:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 814 & 0xffff
#if 814 > 0xffff
  movk ip0, 814 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_children_foreach
  .p2align 4
  .type g_node_children_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_children_foreach
#endif
g_node_children_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 815 & 0xffff
#if 815 > 0xffff
  movk ip0, 815 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_copy
  .p2align 4
  .type g_node_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_copy
#endif
g_node_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 816 & 0xffff
#if 816 > 0xffff
  movk ip0, 816 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_copy_deep
  .p2align 4
  .type g_node_copy_deep, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_copy_deep
#endif
g_node_copy_deep:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 817 & 0xffff
#if 817 > 0xffff
  movk ip0, 817 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_depth
  .p2align 4
  .type g_node_depth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_depth
#endif
g_node_depth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 818 & 0xffff
#if 818 > 0xffff
  movk ip0, 818 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_destroy
  .p2align 4
  .type g_node_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_destroy
#endif
g_node_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 819 & 0xffff
#if 819 > 0xffff
  movk ip0, 819 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_find
  .p2align 4
  .type g_node_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_find
#endif
g_node_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 820 & 0xffff
#if 820 > 0xffff
  movk ip0, 820 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_find_child
  .p2align 4
  .type g_node_find_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_find_child
#endif
g_node_find_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 821 & 0xffff
#if 821 > 0xffff
  movk ip0, 821 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_first_sibling
  .p2align 4
  .type g_node_first_sibling, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_first_sibling
#endif
g_node_first_sibling:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 822 & 0xffff
#if 822 > 0xffff
  movk ip0, 822 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_get_root
  .p2align 4
  .type g_node_get_root, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_get_root
#endif
g_node_get_root:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 823 & 0xffff
#if 823 > 0xffff
  movk ip0, 823 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_insert
  .p2align 4
  .type g_node_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_insert
#endif
g_node_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 824 & 0xffff
#if 824 > 0xffff
  movk ip0, 824 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_insert_after
  .p2align 4
  .type g_node_insert_after, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_insert_after
#endif
g_node_insert_after:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 825 & 0xffff
#if 825 > 0xffff
  movk ip0, 825 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_insert_before
  .p2align 4
  .type g_node_insert_before, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_insert_before
#endif
g_node_insert_before:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 826 & 0xffff
#if 826 > 0xffff
  movk ip0, 826 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_is_ancestor
  .p2align 4
  .type g_node_is_ancestor, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_is_ancestor
#endif
g_node_is_ancestor:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 827 & 0xffff
#if 827 > 0xffff
  movk ip0, 827 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_last_child
  .p2align 4
  .type g_node_last_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_last_child
#endif
g_node_last_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 828 & 0xffff
#if 828 > 0xffff
  movk ip0, 828 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_last_sibling
  .p2align 4
  .type g_node_last_sibling, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_last_sibling
#endif
g_node_last_sibling:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 829 & 0xffff
#if 829 > 0xffff
  movk ip0, 829 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_max_height
  .p2align 4
  .type g_node_max_height, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_max_height
#endif
g_node_max_height:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 830 & 0xffff
#if 830 > 0xffff
  movk ip0, 830 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_n_children
  .p2align 4
  .type g_node_n_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_n_children
#endif
g_node_n_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 831 & 0xffff
#if 831 > 0xffff
  movk ip0, 831 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_n_nodes
  .p2align 4
  .type g_node_n_nodes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_n_nodes
#endif
g_node_n_nodes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 832 & 0xffff
#if 832 > 0xffff
  movk ip0, 832 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_new
  .p2align 4
  .type g_node_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_new
#endif
g_node_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 833 & 0xffff
#if 833 > 0xffff
  movk ip0, 833 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_nth_child
  .p2align 4
  .type g_node_nth_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_nth_child
#endif
g_node_nth_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 834 & 0xffff
#if 834 > 0xffff
  movk ip0, 834 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_pop_allocator
  .p2align 4
  .type g_node_pop_allocator, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_pop_allocator
#endif
g_node_pop_allocator:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 835 & 0xffff
#if 835 > 0xffff
  movk ip0, 835 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_prepend
  .p2align 4
  .type g_node_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_prepend
#endif
g_node_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 836 & 0xffff
#if 836 > 0xffff
  movk ip0, 836 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_push_allocator
  .p2align 4
  .type g_node_push_allocator, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_push_allocator
#endif
g_node_push_allocator:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 837 & 0xffff
#if 837 > 0xffff
  movk ip0, 837 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_reverse_children
  .p2align 4
  .type g_node_reverse_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_reverse_children
#endif
g_node_reverse_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 838 & 0xffff
#if 838 > 0xffff
  movk ip0, 838 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_traverse
  .p2align 4
  .type g_node_traverse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_traverse
#endif
g_node_traverse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 839 & 0xffff
#if 839 > 0xffff
  movk ip0, 839 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_node_unlink
  .p2align 4
  .type g_node_unlink, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_node_unlink
#endif
g_node_unlink:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 840 & 0xffff
#if 840 > 0xffff
  movk ip0, 840 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_nullify_pointer
  .p2align 4
  .type g_nullify_pointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_nullify_pointer
#endif
g_nullify_pointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 841 & 0xffff
#if 841 > 0xffff
  movk ip0, 841 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_number_parser_error_quark
  .p2align 4
  .type g_number_parser_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_number_parser_error_quark
#endif
g_number_parser_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 842 & 0xffff
#if 842 > 0xffff
  movk ip0, 842 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_on_error_query
  .p2align 4
  .type g_on_error_query, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_on_error_query
#endif
g_on_error_query:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 843 & 0xffff
#if 843 > 0xffff
  movk ip0, 843 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_on_error_stack_trace
  .p2align 4
  .type g_on_error_stack_trace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_on_error_stack_trace
#endif
g_on_error_stack_trace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 844 & 0xffff
#if 844 > 0xffff
  movk ip0, 844 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_once_impl
  .p2align 4
  .type g_once_impl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_once_impl
#endif
g_once_impl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 845 & 0xffff
#if 845 > 0xffff
  movk ip0, 845 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_once_init_enter
  .p2align 4
  .type g_once_init_enter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_once_init_enter
#endif
g_once_init_enter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 846 & 0xffff
#if 846 > 0xffff
  movk ip0, 846 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_once_init_enter_impl
  .p2align 4
  .type g_once_init_enter_impl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_once_init_enter_impl
#endif
g_once_init_enter_impl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 847 & 0xffff
#if 847 > 0xffff
  movk ip0, 847 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_once_init_enter_pointer
  .p2align 4
  .type g_once_init_enter_pointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_once_init_enter_pointer
#endif
g_once_init_enter_pointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 848 & 0xffff
#if 848 > 0xffff
  movk ip0, 848 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_once_init_leave
  .p2align 4
  .type g_once_init_leave, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_once_init_leave
#endif
g_once_init_leave:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 849 & 0xffff
#if 849 > 0xffff
  movk ip0, 849 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_once_init_leave_pointer
  .p2align 4
  .type g_once_init_leave_pointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_once_init_leave_pointer
#endif
g_once_init_leave_pointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 850 & 0xffff
#if 850 > 0xffff
  movk ip0, 850 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_open
  .p2align 4
  .type g_open, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_open
#endif
g_open:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 851 & 0xffff
#if 851 > 0xffff
  movk ip0, 851 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_add_group
  .p2align 4
  .type g_option_context_add_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_add_group
#endif
g_option_context_add_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 852 & 0xffff
#if 852 > 0xffff
  movk ip0, 852 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_add_main_entries
  .p2align 4
  .type g_option_context_add_main_entries, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_add_main_entries
#endif
g_option_context_add_main_entries:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 853 & 0xffff
#if 853 > 0xffff
  movk ip0, 853 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_free
  .p2align 4
  .type g_option_context_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_free
#endif
g_option_context_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 854 & 0xffff
#if 854 > 0xffff
  movk ip0, 854 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_get_description
  .p2align 4
  .type g_option_context_get_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_get_description
#endif
g_option_context_get_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 855 & 0xffff
#if 855 > 0xffff
  movk ip0, 855 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_get_help
  .p2align 4
  .type g_option_context_get_help, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_get_help
#endif
g_option_context_get_help:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 856 & 0xffff
#if 856 > 0xffff
  movk ip0, 856 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_get_help_enabled
  .p2align 4
  .type g_option_context_get_help_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_get_help_enabled
#endif
g_option_context_get_help_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 857 & 0xffff
#if 857 > 0xffff
  movk ip0, 857 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_get_ignore_unknown_options
  .p2align 4
  .type g_option_context_get_ignore_unknown_options, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_get_ignore_unknown_options
#endif
g_option_context_get_ignore_unknown_options:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 858 & 0xffff
#if 858 > 0xffff
  movk ip0, 858 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_get_main_group
  .p2align 4
  .type g_option_context_get_main_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_get_main_group
#endif
g_option_context_get_main_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 859 & 0xffff
#if 859 > 0xffff
  movk ip0, 859 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_get_strict_posix
  .p2align 4
  .type g_option_context_get_strict_posix, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_get_strict_posix
#endif
g_option_context_get_strict_posix:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 860 & 0xffff
#if 860 > 0xffff
  movk ip0, 860 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_get_summary
  .p2align 4
  .type g_option_context_get_summary, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_get_summary
#endif
g_option_context_get_summary:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 861 & 0xffff
#if 861 > 0xffff
  movk ip0, 861 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_new
  .p2align 4
  .type g_option_context_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_new
#endif
g_option_context_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 862 & 0xffff
#if 862 > 0xffff
  movk ip0, 862 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_parse
  .p2align 4
  .type g_option_context_parse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_parse
#endif
g_option_context_parse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 863 & 0xffff
#if 863 > 0xffff
  movk ip0, 863 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_parse_strv
  .p2align 4
  .type g_option_context_parse_strv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_parse_strv
#endif
g_option_context_parse_strv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 864 & 0xffff
#if 864 > 0xffff
  movk ip0, 864 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_description
  .p2align 4
  .type g_option_context_set_description, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_description
#endif
g_option_context_set_description:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 865 & 0xffff
#if 865 > 0xffff
  movk ip0, 865 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_help_enabled
  .p2align 4
  .type g_option_context_set_help_enabled, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_help_enabled
#endif
g_option_context_set_help_enabled:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 866 & 0xffff
#if 866 > 0xffff
  movk ip0, 866 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_ignore_unknown_options
  .p2align 4
  .type g_option_context_set_ignore_unknown_options, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_ignore_unknown_options
#endif
g_option_context_set_ignore_unknown_options:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 867 & 0xffff
#if 867 > 0xffff
  movk ip0, 867 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_main_group
  .p2align 4
  .type g_option_context_set_main_group, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_main_group
#endif
g_option_context_set_main_group:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 868 & 0xffff
#if 868 > 0xffff
  movk ip0, 868 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_strict_posix
  .p2align 4
  .type g_option_context_set_strict_posix, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_strict_posix
#endif
g_option_context_set_strict_posix:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 869 & 0xffff
#if 869 > 0xffff
  movk ip0, 869 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_summary
  .p2align 4
  .type g_option_context_set_summary, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_summary
#endif
g_option_context_set_summary:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 870 & 0xffff
#if 870 > 0xffff
  movk ip0, 870 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_translate_func
  .p2align 4
  .type g_option_context_set_translate_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_translate_func
#endif
g_option_context_set_translate_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 871 & 0xffff
#if 871 > 0xffff
  movk ip0, 871 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_context_set_translation_domain
  .p2align 4
  .type g_option_context_set_translation_domain, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_context_set_translation_domain
#endif
g_option_context_set_translation_domain:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 872 & 0xffff
#if 872 > 0xffff
  movk ip0, 872 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_error_quark
  .p2align 4
  .type g_option_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_error_quark
#endif
g_option_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 873 & 0xffff
#if 873 > 0xffff
  movk ip0, 873 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_add_entries
  .p2align 4
  .type g_option_group_add_entries, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_add_entries
#endif
g_option_group_add_entries:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+6992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+6992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 874 & 0xffff
#if 874 > 0xffff
  movk ip0, 874 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_free
  .p2align 4
  .type g_option_group_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_free
#endif
g_option_group_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 875 & 0xffff
#if 875 > 0xffff
  movk ip0, 875 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_new
  .p2align 4
  .type g_option_group_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_new
#endif
g_option_group_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 876 & 0xffff
#if 876 > 0xffff
  movk ip0, 876 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_ref
  .p2align 4
  .type g_option_group_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_ref
#endif
g_option_group_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 877 & 0xffff
#if 877 > 0xffff
  movk ip0, 877 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_set_error_hook
  .p2align 4
  .type g_option_group_set_error_hook, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_set_error_hook
#endif
g_option_group_set_error_hook:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 878 & 0xffff
#if 878 > 0xffff
  movk ip0, 878 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_set_parse_hooks
  .p2align 4
  .type g_option_group_set_parse_hooks, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_set_parse_hooks
#endif
g_option_group_set_parse_hooks:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 879 & 0xffff
#if 879 > 0xffff
  movk ip0, 879 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_set_translate_func
  .p2align 4
  .type g_option_group_set_translate_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_set_translate_func
#endif
g_option_group_set_translate_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 880 & 0xffff
#if 880 > 0xffff
  movk ip0, 880 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_set_translation_domain
  .p2align 4
  .type g_option_group_set_translation_domain, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_set_translation_domain
#endif
g_option_group_set_translation_domain:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 881 & 0xffff
#if 881 > 0xffff
  movk ip0, 881 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_option_group_unref
  .p2align 4
  .type g_option_group_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_option_group_unref
#endif
g_option_group_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 882 & 0xffff
#if 882 > 0xffff
  movk ip0, 882 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_parse_debug_string
  .p2align 4
  .type g_parse_debug_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_parse_debug_string
#endif
g_parse_debug_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 883 & 0xffff
#if 883 > 0xffff
  movk ip0, 883 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_clear
  .p2align 4
  .type g_path_buf_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_clear
#endif
g_path_buf_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 884 & 0xffff
#if 884 > 0xffff
  movk ip0, 884 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_clear_to_path
  .p2align 4
  .type g_path_buf_clear_to_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_clear_to_path
#endif
g_path_buf_clear_to_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 885 & 0xffff
#if 885 > 0xffff
  movk ip0, 885 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_copy
  .p2align 4
  .type g_path_buf_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_copy
#endif
g_path_buf_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 886 & 0xffff
#if 886 > 0xffff
  movk ip0, 886 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_equal
  .p2align 4
  .type g_path_buf_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_equal
#endif
g_path_buf_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 887 & 0xffff
#if 887 > 0xffff
  movk ip0, 887 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_free
  .p2align 4
  .type g_path_buf_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_free
#endif
g_path_buf_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 888 & 0xffff
#if 888 > 0xffff
  movk ip0, 888 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_free_to_path
  .p2align 4
  .type g_path_buf_free_to_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_free_to_path
#endif
g_path_buf_free_to_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 889 & 0xffff
#if 889 > 0xffff
  movk ip0, 889 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_init
  .p2align 4
  .type g_path_buf_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_init
#endif
g_path_buf_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 890 & 0xffff
#if 890 > 0xffff
  movk ip0, 890 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_init_from_path
  .p2align 4
  .type g_path_buf_init_from_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_init_from_path
#endif
g_path_buf_init_from_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 891 & 0xffff
#if 891 > 0xffff
  movk ip0, 891 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_new
  .p2align 4
  .type g_path_buf_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_new
#endif
g_path_buf_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 892 & 0xffff
#if 892 > 0xffff
  movk ip0, 892 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_new_from_path
  .p2align 4
  .type g_path_buf_new_from_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_new_from_path
#endif
g_path_buf_new_from_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 893 & 0xffff
#if 893 > 0xffff
  movk ip0, 893 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_pop
  .p2align 4
  .type g_path_buf_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_pop
#endif
g_path_buf_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 894 & 0xffff
#if 894 > 0xffff
  movk ip0, 894 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_push
  .p2align 4
  .type g_path_buf_push, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_push
#endif
g_path_buf_push:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 895 & 0xffff
#if 895 > 0xffff
  movk ip0, 895 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_set_extension
  .p2align 4
  .type g_path_buf_set_extension, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_set_extension
#endif
g_path_buf_set_extension:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 896 & 0xffff
#if 896 > 0xffff
  movk ip0, 896 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_set_filename
  .p2align 4
  .type g_path_buf_set_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_set_filename
#endif
g_path_buf_set_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 897 & 0xffff
#if 897 > 0xffff
  movk ip0, 897 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_buf_to_path
  .p2align 4
  .type g_path_buf_to_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_buf_to_path
#endif
g_path_buf_to_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 898 & 0xffff
#if 898 > 0xffff
  movk ip0, 898 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_get_basename
  .p2align 4
  .type g_path_get_basename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_get_basename
#endif
g_path_get_basename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 899 & 0xffff
#if 899 > 0xffff
  movk ip0, 899 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_get_dirname
  .p2align 4
  .type g_path_get_dirname, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_get_dirname
#endif
g_path_get_dirname:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 900 & 0xffff
#if 900 > 0xffff
  movk ip0, 900 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_is_absolute
  .p2align 4
  .type g_path_is_absolute, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_is_absolute
#endif
g_path_is_absolute:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 901 & 0xffff
#if 901 > 0xffff
  movk ip0, 901 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_path_skip_root
  .p2align 4
  .type g_path_skip_root, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_path_skip_root
#endif
g_path_skip_root:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 902 & 0xffff
#if 902 > 0xffff
  movk ip0, 902 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_match
  .p2align 4
  .type g_pattern_match, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_match
#endif
g_pattern_match:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 903 & 0xffff
#if 903 > 0xffff
  movk ip0, 903 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_match_simple
  .p2align 4
  .type g_pattern_match_simple, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_match_simple
#endif
g_pattern_match_simple:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 904 & 0xffff
#if 904 > 0xffff
  movk ip0, 904 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_match_string
  .p2align 4
  .type g_pattern_match_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_match_string
#endif
g_pattern_match_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 905 & 0xffff
#if 905 > 0xffff
  movk ip0, 905 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_spec_copy
  .p2align 4
  .type g_pattern_spec_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_spec_copy
#endif
g_pattern_spec_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 906 & 0xffff
#if 906 > 0xffff
  movk ip0, 906 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_spec_equal
  .p2align 4
  .type g_pattern_spec_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_spec_equal
#endif
g_pattern_spec_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 907 & 0xffff
#if 907 > 0xffff
  movk ip0, 907 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_spec_free
  .p2align 4
  .type g_pattern_spec_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_spec_free
#endif
g_pattern_spec_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 908 & 0xffff
#if 908 > 0xffff
  movk ip0, 908 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_spec_match
  .p2align 4
  .type g_pattern_spec_match, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_spec_match
#endif
g_pattern_spec_match:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 909 & 0xffff
#if 909 > 0xffff
  movk ip0, 909 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_spec_match_string
  .p2align 4
  .type g_pattern_spec_match_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_spec_match_string
#endif
g_pattern_spec_match_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 910 & 0xffff
#if 910 > 0xffff
  movk ip0, 910 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pattern_spec_new
  .p2align 4
  .type g_pattern_spec_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pattern_spec_new
#endif
g_pattern_spec_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 911 & 0xffff
#if 911 > 0xffff
  movk ip0, 911 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pointer_bit_lock
  .p2align 4
  .type g_pointer_bit_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pointer_bit_lock
#endif
g_pointer_bit_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 912 & 0xffff
#if 912 > 0xffff
  movk ip0, 912 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pointer_bit_lock_and_get
  .p2align 4
  .type g_pointer_bit_lock_and_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pointer_bit_lock_and_get
#endif
g_pointer_bit_lock_and_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 913 & 0xffff
#if 913 > 0xffff
  movk ip0, 913 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pointer_bit_lock_mask_ptr
  .p2align 4
  .type g_pointer_bit_lock_mask_ptr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pointer_bit_lock_mask_ptr
#endif
g_pointer_bit_lock_mask_ptr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 914 & 0xffff
#if 914 > 0xffff
  movk ip0, 914 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pointer_bit_trylock
  .p2align 4
  .type g_pointer_bit_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pointer_bit_trylock
#endif
g_pointer_bit_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 915 & 0xffff
#if 915 > 0xffff
  movk ip0, 915 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pointer_bit_unlock
  .p2align 4
  .type g_pointer_bit_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pointer_bit_unlock
#endif
g_pointer_bit_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 916 & 0xffff
#if 916 > 0xffff
  movk ip0, 916 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_pointer_bit_unlock_and_set
  .p2align 4
  .type g_pointer_bit_unlock_and_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_pointer_bit_unlock_and_set
#endif
g_pointer_bit_unlock_and_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 917 & 0xffff
#if 917 > 0xffff
  movk ip0, 917 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_poll
  .p2align 4
  .type g_poll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_poll
#endif
g_poll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 918 & 0xffff
#if 918 > 0xffff
  movk ip0, 918 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_prefix_error
  .p2align 4
  .type g_prefix_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_prefix_error
#endif
g_prefix_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 919 & 0xffff
#if 919 > 0xffff
  movk ip0, 919 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_prefix_error_literal
  .p2align 4
  .type g_prefix_error_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_prefix_error_literal
#endif
g_prefix_error_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 920 & 0xffff
#if 920 > 0xffff
  movk ip0, 920 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_print
  .p2align 4
  .type g_print, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_print
#endif
g_print:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 921 & 0xffff
#if 921 > 0xffff
  movk ip0, 921 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_printerr
  .p2align 4
  .type g_printerr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_printerr
#endif
g_printerr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 922 & 0xffff
#if 922 > 0xffff
  movk ip0, 922 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_printf
  .p2align 4
  .type g_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_printf
#endif
g_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 923 & 0xffff
#if 923 > 0xffff
  movk ip0, 923 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_printf_string_upper_bound
  .p2align 4
  .type g_printf_string_upper_bound, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_printf_string_upper_bound
#endif
g_printf_string_upper_bound:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 924 & 0xffff
#if 924 > 0xffff
  movk ip0, 924 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_private_get
  .p2align 4
  .type g_private_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_private_get
#endif
g_private_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 925 & 0xffff
#if 925 > 0xffff
  movk ip0, 925 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_private_new
  .p2align 4
  .type g_private_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_private_new
#endif
g_private_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 926 & 0xffff
#if 926 > 0xffff
  movk ip0, 926 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_private_replace
  .p2align 4
  .type g_private_replace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_private_replace
#endif
g_private_replace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 927 & 0xffff
#if 927 > 0xffff
  movk ip0, 927 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_private_set
  .p2align 4
  .type g_private_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_private_set
#endif
g_private_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 928 & 0xffff
#if 928 > 0xffff
  movk ip0, 928 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_propagate_error
  .p2align 4
  .type g_propagate_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_propagate_error
#endif
g_propagate_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 929 & 0xffff
#if 929 > 0xffff
  movk ip0, 929 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_propagate_prefixed_error
  .p2align 4
  .type g_propagate_prefixed_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_propagate_prefixed_error
#endif
g_propagate_prefixed_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 930 & 0xffff
#if 930 > 0xffff
  movk ip0, 930 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_add
  .p2align 4
  .type g_ptr_array_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_add
#endif
g_ptr_array_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 931 & 0xffff
#if 931 > 0xffff
  movk ip0, 931 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_copy
  .p2align 4
  .type g_ptr_array_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_copy
#endif
g_ptr_array_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 932 & 0xffff
#if 932 > 0xffff
  movk ip0, 932 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_extend
  .p2align 4
  .type g_ptr_array_extend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_extend
#endif
g_ptr_array_extend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 933 & 0xffff
#if 933 > 0xffff
  movk ip0, 933 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_extend_and_steal
  .p2align 4
  .type g_ptr_array_extend_and_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_extend_and_steal
#endif
g_ptr_array_extend_and_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 934 & 0xffff
#if 934 > 0xffff
  movk ip0, 934 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_find
  .p2align 4
  .type g_ptr_array_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_find
#endif
g_ptr_array_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 935 & 0xffff
#if 935 > 0xffff
  movk ip0, 935 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_find_with_equal_func
  .p2align 4
  .type g_ptr_array_find_with_equal_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_find_with_equal_func
#endif
g_ptr_array_find_with_equal_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 936 & 0xffff
#if 936 > 0xffff
  movk ip0, 936 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_foreach
  .p2align 4
  .type g_ptr_array_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_foreach
#endif
g_ptr_array_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 937 & 0xffff
#if 937 > 0xffff
  movk ip0, 937 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_free
  .p2align 4
  .type g_ptr_array_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_free
#endif
g_ptr_array_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 938 & 0xffff
#if 938 > 0xffff
  movk ip0, 938 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_insert
  .p2align 4
  .type g_ptr_array_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_insert
#endif
g_ptr_array_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 939 & 0xffff
#if 939 > 0xffff
  movk ip0, 939 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_is_null_terminated
  .p2align 4
  .type g_ptr_array_is_null_terminated, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_is_null_terminated
#endif
g_ptr_array_is_null_terminated:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 940 & 0xffff
#if 940 > 0xffff
  movk ip0, 940 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new
  .p2align 4
  .type g_ptr_array_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new
#endif
g_ptr_array_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 941 & 0xffff
#if 941 > 0xffff
  movk ip0, 941 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new_from_array
  .p2align 4
  .type g_ptr_array_new_from_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new_from_array
#endif
g_ptr_array_new_from_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 942 & 0xffff
#if 942 > 0xffff
  movk ip0, 942 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new_from_null_terminated_array
  .p2align 4
  .type g_ptr_array_new_from_null_terminated_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new_from_null_terminated_array
#endif
g_ptr_array_new_from_null_terminated_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 943 & 0xffff
#if 943 > 0xffff
  movk ip0, 943 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new_full
  .p2align 4
  .type g_ptr_array_new_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new_full
#endif
g_ptr_array_new_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 944 & 0xffff
#if 944 > 0xffff
  movk ip0, 944 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new_null_terminated
  .p2align 4
  .type g_ptr_array_new_null_terminated, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new_null_terminated
#endif
g_ptr_array_new_null_terminated:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 945 & 0xffff
#if 945 > 0xffff
  movk ip0, 945 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new_take
  .p2align 4
  .type g_ptr_array_new_take, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new_take
#endif
g_ptr_array_new_take:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 946 & 0xffff
#if 946 > 0xffff
  movk ip0, 946 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new_take_null_terminated
  .p2align 4
  .type g_ptr_array_new_take_null_terminated, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new_take_null_terminated
#endif
g_ptr_array_new_take_null_terminated:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 947 & 0xffff
#if 947 > 0xffff
  movk ip0, 947 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_new_with_free_func
  .p2align 4
  .type g_ptr_array_new_with_free_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_new_with_free_func
#endif
g_ptr_array_new_with_free_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 948 & 0xffff
#if 948 > 0xffff
  movk ip0, 948 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_ref
  .p2align 4
  .type g_ptr_array_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_ref
#endif
g_ptr_array_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 949 & 0xffff
#if 949 > 0xffff
  movk ip0, 949 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_remove
  .p2align 4
  .type g_ptr_array_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_remove
#endif
g_ptr_array_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 950 & 0xffff
#if 950 > 0xffff
  movk ip0, 950 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_remove_fast
  .p2align 4
  .type g_ptr_array_remove_fast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_remove_fast
#endif
g_ptr_array_remove_fast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 951 & 0xffff
#if 951 > 0xffff
  movk ip0, 951 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_remove_index
  .p2align 4
  .type g_ptr_array_remove_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_remove_index
#endif
g_ptr_array_remove_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 952 & 0xffff
#if 952 > 0xffff
  movk ip0, 952 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_remove_index_fast
  .p2align 4
  .type g_ptr_array_remove_index_fast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_remove_index_fast
#endif
g_ptr_array_remove_index_fast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 953 & 0xffff
#if 953 > 0xffff
  movk ip0, 953 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_remove_range
  .p2align 4
  .type g_ptr_array_remove_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_remove_range
#endif
g_ptr_array_remove_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 954 & 0xffff
#if 954 > 0xffff
  movk ip0, 954 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_set_free_func
  .p2align 4
  .type g_ptr_array_set_free_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_set_free_func
#endif
g_ptr_array_set_free_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 955 & 0xffff
#if 955 > 0xffff
  movk ip0, 955 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_set_size
  .p2align 4
  .type g_ptr_array_set_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_set_size
#endif
g_ptr_array_set_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 956 & 0xffff
#if 956 > 0xffff
  movk ip0, 956 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_sized_new
  .p2align 4
  .type g_ptr_array_sized_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_sized_new
#endif
g_ptr_array_sized_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 957 & 0xffff
#if 957 > 0xffff
  movk ip0, 957 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_sort
  .p2align 4
  .type g_ptr_array_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_sort
#endif
g_ptr_array_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 958 & 0xffff
#if 958 > 0xffff
  movk ip0, 958 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_sort_values
  .p2align 4
  .type g_ptr_array_sort_values, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_sort_values
#endif
g_ptr_array_sort_values:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 959 & 0xffff
#if 959 > 0xffff
  movk ip0, 959 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_sort_values_with_data
  .p2align 4
  .type g_ptr_array_sort_values_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_sort_values_with_data
#endif
g_ptr_array_sort_values_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 960 & 0xffff
#if 960 > 0xffff
  movk ip0, 960 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_sort_with_data
  .p2align 4
  .type g_ptr_array_sort_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_sort_with_data
#endif
g_ptr_array_sort_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 961 & 0xffff
#if 961 > 0xffff
  movk ip0, 961 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_steal
  .p2align 4
  .type g_ptr_array_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_steal
#endif
g_ptr_array_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 962 & 0xffff
#if 962 > 0xffff
  movk ip0, 962 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_steal_index
  .p2align 4
  .type g_ptr_array_steal_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_steal_index
#endif
g_ptr_array_steal_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 963 & 0xffff
#if 963 > 0xffff
  movk ip0, 963 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_steal_index_fast
  .p2align 4
  .type g_ptr_array_steal_index_fast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_steal_index_fast
#endif
g_ptr_array_steal_index_fast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 964 & 0xffff
#if 964 > 0xffff
  movk ip0, 964 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ptr_array_unref
  .p2align 4
  .type g_ptr_array_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ptr_array_unref
#endif
g_ptr_array_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 965 & 0xffff
#if 965 > 0xffff
  movk ip0, 965 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_qsort_with_data
  .p2align 4
  .type g_qsort_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_qsort_with_data
#endif
g_qsort_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 966 & 0xffff
#if 966 > 0xffff
  movk ip0, 966 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_quark_from_static_string
  .p2align 4
  .type g_quark_from_static_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_quark_from_static_string
#endif
g_quark_from_static_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 967 & 0xffff
#if 967 > 0xffff
  movk ip0, 967 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_quark_from_string
  .p2align 4
  .type g_quark_from_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_quark_from_string
#endif
g_quark_from_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 968 & 0xffff
#if 968 > 0xffff
  movk ip0, 968 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_quark_to_string
  .p2align 4
  .type g_quark_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_quark_to_string
#endif
g_quark_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 969 & 0xffff
#if 969 > 0xffff
  movk ip0, 969 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_quark_try_string
  .p2align 4
  .type g_quark_try_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_quark_try_string
#endif
g_quark_try_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 970 & 0xffff
#if 970 > 0xffff
  movk ip0, 970 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_clear
  .p2align 4
  .type g_queue_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_clear
#endif
g_queue_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 971 & 0xffff
#if 971 > 0xffff
  movk ip0, 971 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_clear_full
  .p2align 4
  .type g_queue_clear_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_clear_full
#endif
g_queue_clear_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 972 & 0xffff
#if 972 > 0xffff
  movk ip0, 972 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_copy
  .p2align 4
  .type g_queue_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_copy
#endif
g_queue_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 973 & 0xffff
#if 973 > 0xffff
  movk ip0, 973 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_delete_link
  .p2align 4
  .type g_queue_delete_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_delete_link
#endif
g_queue_delete_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 974 & 0xffff
#if 974 > 0xffff
  movk ip0, 974 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_find
  .p2align 4
  .type g_queue_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_find
#endif
g_queue_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 975 & 0xffff
#if 975 > 0xffff
  movk ip0, 975 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_find_custom
  .p2align 4
  .type g_queue_find_custom, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_find_custom
#endif
g_queue_find_custom:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 976 & 0xffff
#if 976 > 0xffff
  movk ip0, 976 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_foreach
  .p2align 4
  .type g_queue_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_foreach
#endif
g_queue_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 977 & 0xffff
#if 977 > 0xffff
  movk ip0, 977 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_free
  .p2align 4
  .type g_queue_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_free
#endif
g_queue_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 978 & 0xffff
#if 978 > 0xffff
  movk ip0, 978 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_free_full
  .p2align 4
  .type g_queue_free_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_free_full
#endif
g_queue_free_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 979 & 0xffff
#if 979 > 0xffff
  movk ip0, 979 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_get_length
  .p2align 4
  .type g_queue_get_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_get_length
#endif
g_queue_get_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 980 & 0xffff
#if 980 > 0xffff
  movk ip0, 980 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_index
  .p2align 4
  .type g_queue_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_index
#endif
g_queue_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 981 & 0xffff
#if 981 > 0xffff
  movk ip0, 981 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_init
  .p2align 4
  .type g_queue_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_init
#endif
g_queue_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 982 & 0xffff
#if 982 > 0xffff
  movk ip0, 982 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_insert_after
  .p2align 4
  .type g_queue_insert_after, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_insert_after
#endif
g_queue_insert_after:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 983 & 0xffff
#if 983 > 0xffff
  movk ip0, 983 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_insert_after_link
  .p2align 4
  .type g_queue_insert_after_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_insert_after_link
#endif
g_queue_insert_after_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 984 & 0xffff
#if 984 > 0xffff
  movk ip0, 984 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_insert_before
  .p2align 4
  .type g_queue_insert_before, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_insert_before
#endif
g_queue_insert_before:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 985 & 0xffff
#if 985 > 0xffff
  movk ip0, 985 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_insert_before_link
  .p2align 4
  .type g_queue_insert_before_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_insert_before_link
#endif
g_queue_insert_before_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 986 & 0xffff
#if 986 > 0xffff
  movk ip0, 986 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_insert_sorted
  .p2align 4
  .type g_queue_insert_sorted, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_insert_sorted
#endif
g_queue_insert_sorted:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 987 & 0xffff
#if 987 > 0xffff
  movk ip0, 987 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_is_empty
  .p2align 4
  .type g_queue_is_empty, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_is_empty
#endif
g_queue_is_empty:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 988 & 0xffff
#if 988 > 0xffff
  movk ip0, 988 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_link_index
  .p2align 4
  .type g_queue_link_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_link_index
#endif
g_queue_link_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 989 & 0xffff
#if 989 > 0xffff
  movk ip0, 989 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_new
  .p2align 4
  .type g_queue_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_new
#endif
g_queue_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 990 & 0xffff
#if 990 > 0xffff
  movk ip0, 990 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_peek_head
  .p2align 4
  .type g_queue_peek_head, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_peek_head
#endif
g_queue_peek_head:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 991 & 0xffff
#if 991 > 0xffff
  movk ip0, 991 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_peek_head_link
  .p2align 4
  .type g_queue_peek_head_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_peek_head_link
#endif
g_queue_peek_head_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 992 & 0xffff
#if 992 > 0xffff
  movk ip0, 992 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_peek_nth
  .p2align 4
  .type g_queue_peek_nth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_peek_nth
#endif
g_queue_peek_nth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 993 & 0xffff
#if 993 > 0xffff
  movk ip0, 993 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_peek_nth_link
  .p2align 4
  .type g_queue_peek_nth_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_peek_nth_link
#endif
g_queue_peek_nth_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 994 & 0xffff
#if 994 > 0xffff
  movk ip0, 994 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_peek_tail
  .p2align 4
  .type g_queue_peek_tail, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_peek_tail
#endif
g_queue_peek_tail:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 995 & 0xffff
#if 995 > 0xffff
  movk ip0, 995 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_peek_tail_link
  .p2align 4
  .type g_queue_peek_tail_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_peek_tail_link
#endif
g_queue_peek_tail_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 996 & 0xffff
#if 996 > 0xffff
  movk ip0, 996 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_pop_head
  .p2align 4
  .type g_queue_pop_head, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_pop_head
#endif
g_queue_pop_head:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 997 & 0xffff
#if 997 > 0xffff
  movk ip0, 997 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_pop_head_link
  .p2align 4
  .type g_queue_pop_head_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_pop_head_link
#endif
g_queue_pop_head_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 998 & 0xffff
#if 998 > 0xffff
  movk ip0, 998 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_pop_nth
  .p2align 4
  .type g_queue_pop_nth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_pop_nth
#endif
g_queue_pop_nth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+7992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+7992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 999 & 0xffff
#if 999 > 0xffff
  movk ip0, 999 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_pop_nth_link
  .p2align 4
  .type g_queue_pop_nth_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_pop_nth_link
#endif
g_queue_pop_nth_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1000 & 0xffff
#if 1000 > 0xffff
  movk ip0, 1000 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_pop_tail
  .p2align 4
  .type g_queue_pop_tail, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_pop_tail
#endif
g_queue_pop_tail:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1001 & 0xffff
#if 1001 > 0xffff
  movk ip0, 1001 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_pop_tail_link
  .p2align 4
  .type g_queue_pop_tail_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_pop_tail_link
#endif
g_queue_pop_tail_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1002 & 0xffff
#if 1002 > 0xffff
  movk ip0, 1002 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_push_head
  .p2align 4
  .type g_queue_push_head, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_push_head
#endif
g_queue_push_head:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1003 & 0xffff
#if 1003 > 0xffff
  movk ip0, 1003 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_push_head_link
  .p2align 4
  .type g_queue_push_head_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_push_head_link
#endif
g_queue_push_head_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1004 & 0xffff
#if 1004 > 0xffff
  movk ip0, 1004 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_push_nth
  .p2align 4
  .type g_queue_push_nth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_push_nth
#endif
g_queue_push_nth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1005 & 0xffff
#if 1005 > 0xffff
  movk ip0, 1005 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_push_nth_link
  .p2align 4
  .type g_queue_push_nth_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_push_nth_link
#endif
g_queue_push_nth_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1006 & 0xffff
#if 1006 > 0xffff
  movk ip0, 1006 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_push_tail
  .p2align 4
  .type g_queue_push_tail, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_push_tail
#endif
g_queue_push_tail:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1007 & 0xffff
#if 1007 > 0xffff
  movk ip0, 1007 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_push_tail_link
  .p2align 4
  .type g_queue_push_tail_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_push_tail_link
#endif
g_queue_push_tail_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1008 & 0xffff
#if 1008 > 0xffff
  movk ip0, 1008 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_remove
  .p2align 4
  .type g_queue_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_remove
#endif
g_queue_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1009 & 0xffff
#if 1009 > 0xffff
  movk ip0, 1009 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_remove_all
  .p2align 4
  .type g_queue_remove_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_remove_all
#endif
g_queue_remove_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1010 & 0xffff
#if 1010 > 0xffff
  movk ip0, 1010 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_reverse
  .p2align 4
  .type g_queue_reverse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_reverse
#endif
g_queue_reverse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1011 & 0xffff
#if 1011 > 0xffff
  movk ip0, 1011 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_sort
  .p2align 4
  .type g_queue_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_sort
#endif
g_queue_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1012 & 0xffff
#if 1012 > 0xffff
  movk ip0, 1012 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_queue_unlink
  .p2align 4
  .type g_queue_unlink, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_queue_unlink
#endif
g_queue_unlink:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1013 & 0xffff
#if 1013 > 0xffff
  movk ip0, 1013 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_copy
  .p2align 4
  .type g_rand_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_copy
#endif
g_rand_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1014 & 0xffff
#if 1014 > 0xffff
  movk ip0, 1014 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_double
  .p2align 4
  .type g_rand_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_double
#endif
g_rand_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1015 & 0xffff
#if 1015 > 0xffff
  movk ip0, 1015 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_double_range
  .p2align 4
  .type g_rand_double_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_double_range
#endif
g_rand_double_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1016 & 0xffff
#if 1016 > 0xffff
  movk ip0, 1016 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_free
  .p2align 4
  .type g_rand_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_free
#endif
g_rand_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1017 & 0xffff
#if 1017 > 0xffff
  movk ip0, 1017 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_int
  .p2align 4
  .type g_rand_int, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_int
#endif
g_rand_int:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1018 & 0xffff
#if 1018 > 0xffff
  movk ip0, 1018 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_int_range
  .p2align 4
  .type g_rand_int_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_int_range
#endif
g_rand_int_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1019 & 0xffff
#if 1019 > 0xffff
  movk ip0, 1019 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_new
  .p2align 4
  .type g_rand_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_new
#endif
g_rand_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1020 & 0xffff
#if 1020 > 0xffff
  movk ip0, 1020 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_new_with_seed
  .p2align 4
  .type g_rand_new_with_seed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_new_with_seed
#endif
g_rand_new_with_seed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1021 & 0xffff
#if 1021 > 0xffff
  movk ip0, 1021 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_new_with_seed_array
  .p2align 4
  .type g_rand_new_with_seed_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_new_with_seed_array
#endif
g_rand_new_with_seed_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1022 & 0xffff
#if 1022 > 0xffff
  movk ip0, 1022 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_set_seed
  .p2align 4
  .type g_rand_set_seed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_set_seed
#endif
g_rand_set_seed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1023 & 0xffff
#if 1023 > 0xffff
  movk ip0, 1023 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rand_set_seed_array
  .p2align 4
  .type g_rand_set_seed_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rand_set_seed_array
#endif
g_rand_set_seed_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1024 & 0xffff
#if 1024 > 0xffff
  movk ip0, 1024 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_random_double
  .p2align 4
  .type g_random_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_random_double
#endif
g_random_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1025 & 0xffff
#if 1025 > 0xffff
  movk ip0, 1025 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_random_double_range
  .p2align 4
  .type g_random_double_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_random_double_range
#endif
g_random_double_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1026 & 0xffff
#if 1026 > 0xffff
  movk ip0, 1026 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_random_int
  .p2align 4
  .type g_random_int, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_random_int
#endif
g_random_int:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1027 & 0xffff
#if 1027 > 0xffff
  movk ip0, 1027 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_random_int_range
  .p2align 4
  .type g_random_int_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_random_int_range
#endif
g_random_int_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1028 & 0xffff
#if 1028 > 0xffff
  movk ip0, 1028 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_random_set_seed
  .p2align 4
  .type g_random_set_seed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_random_set_seed
#endif
g_random_set_seed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1029 & 0xffff
#if 1029 > 0xffff
  movk ip0, 1029 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rc_box_acquire
  .p2align 4
  .type g_rc_box_acquire, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rc_box_acquire
#endif
g_rc_box_acquire:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1030 & 0xffff
#if 1030 > 0xffff
  movk ip0, 1030 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rc_box_alloc
  .p2align 4
  .type g_rc_box_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rc_box_alloc
#endif
g_rc_box_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1031 & 0xffff
#if 1031 > 0xffff
  movk ip0, 1031 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rc_box_alloc0
  .p2align 4
  .type g_rc_box_alloc0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rc_box_alloc0
#endif
g_rc_box_alloc0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1032 & 0xffff
#if 1032 > 0xffff
  movk ip0, 1032 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rc_box_dup
  .p2align 4
  .type g_rc_box_dup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rc_box_dup
#endif
g_rc_box_dup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1033 & 0xffff
#if 1033 > 0xffff
  movk ip0, 1033 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rc_box_get_size
  .p2align 4
  .type g_rc_box_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rc_box_get_size
#endif
g_rc_box_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1034 & 0xffff
#if 1034 > 0xffff
  movk ip0, 1034 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rc_box_release
  .p2align 4
  .type g_rc_box_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rc_box_release
#endif
g_rc_box_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1035 & 0xffff
#if 1035 > 0xffff
  movk ip0, 1035 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rc_box_release_full
  .p2align 4
  .type g_rc_box_release_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rc_box_release_full
#endif
g_rc_box_release_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1036 & 0xffff
#if 1036 > 0xffff
  movk ip0, 1036 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_realloc
  .p2align 4
  .type g_realloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_realloc
#endif
g_realloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1037 & 0xffff
#if 1037 > 0xffff
  movk ip0, 1037 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_realloc_n
  .p2align 4
  .type g_realloc_n, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_realloc_n
#endif
g_realloc_n:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1038 & 0xffff
#if 1038 > 0xffff
  movk ip0, 1038 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rec_mutex_clear
  .p2align 4
  .type g_rec_mutex_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rec_mutex_clear
#endif
g_rec_mutex_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1039 & 0xffff
#if 1039 > 0xffff
  movk ip0, 1039 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rec_mutex_init
  .p2align 4
  .type g_rec_mutex_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rec_mutex_init
#endif
g_rec_mutex_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1040 & 0xffff
#if 1040 > 0xffff
  movk ip0, 1040 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rec_mutex_lock
  .p2align 4
  .type g_rec_mutex_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rec_mutex_lock
#endif
g_rec_mutex_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1041 & 0xffff
#if 1041 > 0xffff
  movk ip0, 1041 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rec_mutex_trylock
  .p2align 4
  .type g_rec_mutex_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rec_mutex_trylock
#endif
g_rec_mutex_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1042 & 0xffff
#if 1042 > 0xffff
  movk ip0, 1042 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rec_mutex_unlock
  .p2align 4
  .type g_rec_mutex_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rec_mutex_unlock
#endif
g_rec_mutex_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1043 & 0xffff
#if 1043 > 0xffff
  movk ip0, 1043 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_count_compare
  .p2align 4
  .type g_ref_count_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_count_compare
#endif
g_ref_count_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1044 & 0xffff
#if 1044 > 0xffff
  movk ip0, 1044 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_count_dec
  .p2align 4
  .type g_ref_count_dec, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_count_dec
#endif
g_ref_count_dec:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1045 & 0xffff
#if 1045 > 0xffff
  movk ip0, 1045 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_count_inc
  .p2align 4
  .type g_ref_count_inc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_count_inc
#endif
g_ref_count_inc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1046 & 0xffff
#if 1046 > 0xffff
  movk ip0, 1046 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_count_init
  .p2align 4
  .type g_ref_count_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_count_init
#endif
g_ref_count_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1047 & 0xffff
#if 1047 > 0xffff
  movk ip0, 1047 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_string_acquire
  .p2align 4
  .type g_ref_string_acquire, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_string_acquire
#endif
g_ref_string_acquire:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1048 & 0xffff
#if 1048 > 0xffff
  movk ip0, 1048 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_string_equal
  .p2align 4
  .type g_ref_string_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_string_equal
#endif
g_ref_string_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1049 & 0xffff
#if 1049 > 0xffff
  movk ip0, 1049 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_string_length
  .p2align 4
  .type g_ref_string_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_string_length
#endif
g_ref_string_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1050 & 0xffff
#if 1050 > 0xffff
  movk ip0, 1050 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_string_new
  .p2align 4
  .type g_ref_string_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_string_new
#endif
g_ref_string_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1051 & 0xffff
#if 1051 > 0xffff
  movk ip0, 1051 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_string_new_intern
  .p2align 4
  .type g_ref_string_new_intern, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_string_new_intern
#endif
g_ref_string_new_intern:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1052 & 0xffff
#if 1052 > 0xffff
  movk ip0, 1052 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_string_new_len
  .p2align 4
  .type g_ref_string_new_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_string_new_len
#endif
g_ref_string_new_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1053 & 0xffff
#if 1053 > 0xffff
  movk ip0, 1053 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ref_string_release
  .p2align 4
  .type g_ref_string_release, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ref_string_release
#endif
g_ref_string_release:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1054 & 0xffff
#if 1054 > 0xffff
  movk ip0, 1054 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_check_replacement
  .p2align 4
  .type g_regex_check_replacement, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_check_replacement
#endif
g_regex_check_replacement:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1055 & 0xffff
#if 1055 > 0xffff
  movk ip0, 1055 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_error_quark
  .p2align 4
  .type g_regex_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_error_quark
#endif
g_regex_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1056 & 0xffff
#if 1056 > 0xffff
  movk ip0, 1056 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_escape_nul
  .p2align 4
  .type g_regex_escape_nul, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_escape_nul
#endif
g_regex_escape_nul:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1057 & 0xffff
#if 1057 > 0xffff
  movk ip0, 1057 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_escape_string
  .p2align 4
  .type g_regex_escape_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_escape_string
#endif
g_regex_escape_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1058 & 0xffff
#if 1058 > 0xffff
  movk ip0, 1058 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_capture_count
  .p2align 4
  .type g_regex_get_capture_count, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_capture_count
#endif
g_regex_get_capture_count:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1059 & 0xffff
#if 1059 > 0xffff
  movk ip0, 1059 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_compile_flags
  .p2align 4
  .type g_regex_get_compile_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_compile_flags
#endif
g_regex_get_compile_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1060 & 0xffff
#if 1060 > 0xffff
  movk ip0, 1060 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_has_cr_or_lf
  .p2align 4
  .type g_regex_get_has_cr_or_lf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_has_cr_or_lf
#endif
g_regex_get_has_cr_or_lf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1061 & 0xffff
#if 1061 > 0xffff
  movk ip0, 1061 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_match_flags
  .p2align 4
  .type g_regex_get_match_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_match_flags
#endif
g_regex_get_match_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1062 & 0xffff
#if 1062 > 0xffff
  movk ip0, 1062 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_max_backref
  .p2align 4
  .type g_regex_get_max_backref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_max_backref
#endif
g_regex_get_max_backref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1063 & 0xffff
#if 1063 > 0xffff
  movk ip0, 1063 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_max_lookbehind
  .p2align 4
  .type g_regex_get_max_lookbehind, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_max_lookbehind
#endif
g_regex_get_max_lookbehind:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1064 & 0xffff
#if 1064 > 0xffff
  movk ip0, 1064 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_pattern
  .p2align 4
  .type g_regex_get_pattern, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_pattern
#endif
g_regex_get_pattern:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1065 & 0xffff
#if 1065 > 0xffff
  movk ip0, 1065 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_get_string_number
  .p2align 4
  .type g_regex_get_string_number, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_get_string_number
#endif
g_regex_get_string_number:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1066 & 0xffff
#if 1066 > 0xffff
  movk ip0, 1066 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_match
  .p2align 4
  .type g_regex_match, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_match
#endif
g_regex_match:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1067 & 0xffff
#if 1067 > 0xffff
  movk ip0, 1067 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_match_all
  .p2align 4
  .type g_regex_match_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_match_all
#endif
g_regex_match_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1068 & 0xffff
#if 1068 > 0xffff
  movk ip0, 1068 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_match_all_full
  .p2align 4
  .type g_regex_match_all_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_match_all_full
#endif
g_regex_match_all_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1069 & 0xffff
#if 1069 > 0xffff
  movk ip0, 1069 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_match_full
  .p2align 4
  .type g_regex_match_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_match_full
#endif
g_regex_match_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1070 & 0xffff
#if 1070 > 0xffff
  movk ip0, 1070 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_match_simple
  .p2align 4
  .type g_regex_match_simple, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_match_simple
#endif
g_regex_match_simple:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1071 & 0xffff
#if 1071 > 0xffff
  movk ip0, 1071 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_new
  .p2align 4
  .type g_regex_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_new
#endif
g_regex_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1072 & 0xffff
#if 1072 > 0xffff
  movk ip0, 1072 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_ref
  .p2align 4
  .type g_regex_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_ref
#endif
g_regex_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1073 & 0xffff
#if 1073 > 0xffff
  movk ip0, 1073 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_replace
  .p2align 4
  .type g_regex_replace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_replace
#endif
g_regex_replace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1074 & 0xffff
#if 1074 > 0xffff
  movk ip0, 1074 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_replace_eval
  .p2align 4
  .type g_regex_replace_eval, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_replace_eval
#endif
g_regex_replace_eval:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1075 & 0xffff
#if 1075 > 0xffff
  movk ip0, 1075 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_replace_literal
  .p2align 4
  .type g_regex_replace_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_replace_literal
#endif
g_regex_replace_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1076 & 0xffff
#if 1076 > 0xffff
  movk ip0, 1076 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_split
  .p2align 4
  .type g_regex_split, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_split
#endif
g_regex_split:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1077 & 0xffff
#if 1077 > 0xffff
  movk ip0, 1077 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_split_full
  .p2align 4
  .type g_regex_split_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_split_full
#endif
g_regex_split_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1078 & 0xffff
#if 1078 > 0xffff
  movk ip0, 1078 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_split_simple
  .p2align 4
  .type g_regex_split_simple, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_split_simple
#endif
g_regex_split_simple:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1079 & 0xffff
#if 1079 > 0xffff
  movk ip0, 1079 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_regex_unref
  .p2align 4
  .type g_regex_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_regex_unref
#endif
g_regex_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1080 & 0xffff
#if 1080 > 0xffff
  movk ip0, 1080 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_count
  .p2align 4
  .type g_relation_count, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_count
#endif
g_relation_count:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1081 & 0xffff
#if 1081 > 0xffff
  movk ip0, 1081 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_delete
  .p2align 4
  .type g_relation_delete, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_delete
#endif
g_relation_delete:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1082 & 0xffff
#if 1082 > 0xffff
  movk ip0, 1082 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_destroy
  .p2align 4
  .type g_relation_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_destroy
#endif
g_relation_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1083 & 0xffff
#if 1083 > 0xffff
  movk ip0, 1083 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_exists
  .p2align 4
  .type g_relation_exists, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_exists
#endif
g_relation_exists:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1084 & 0xffff
#if 1084 > 0xffff
  movk ip0, 1084 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_index
  .p2align 4
  .type g_relation_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_index
#endif
g_relation_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1085 & 0xffff
#if 1085 > 0xffff
  movk ip0, 1085 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_insert
  .p2align 4
  .type g_relation_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_insert
#endif
g_relation_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1086 & 0xffff
#if 1086 > 0xffff
  movk ip0, 1086 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_new
  .p2align 4
  .type g_relation_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_new
#endif
g_relation_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1087 & 0xffff
#if 1087 > 0xffff
  movk ip0, 1087 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_print
  .p2align 4
  .type g_relation_print, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_print
#endif
g_relation_print:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1088 & 0xffff
#if 1088 > 0xffff
  movk ip0, 1088 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_relation_select
  .p2align 4
  .type g_relation_select, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_relation_select
#endif
g_relation_select:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1089 & 0xffff
#if 1089 > 0xffff
  movk ip0, 1089 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_reload_user_special_dirs_cache
  .p2align 4
  .type g_reload_user_special_dirs_cache, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_reload_user_special_dirs_cache
#endif
g_reload_user_special_dirs_cache:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1090 & 0xffff
#if 1090 > 0xffff
  movk ip0, 1090 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_remove
  .p2align 4
  .type g_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_remove
#endif
g_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1091 & 0xffff
#if 1091 > 0xffff
  movk ip0, 1091 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rename
  .p2align 4
  .type g_rename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rename
#endif
g_rename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1092 & 0xffff
#if 1092 > 0xffff
  movk ip0, 1092 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_return_if_fail_warning
  .p2align 4
  .type g_return_if_fail_warning, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_return_if_fail_warning
#endif
g_return_if_fail_warning:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1093 & 0xffff
#if 1093 > 0xffff
  movk ip0, 1093 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rmdir
  .p2align 4
  .type g_rmdir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rmdir
#endif
g_rmdir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1094 & 0xffff
#if 1094 > 0xffff
  movk ip0, 1094 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_clear
  .p2align 4
  .type g_rw_lock_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_clear
#endif
g_rw_lock_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1095 & 0xffff
#if 1095 > 0xffff
  movk ip0, 1095 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_init
  .p2align 4
  .type g_rw_lock_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_init
#endif
g_rw_lock_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1096 & 0xffff
#if 1096 > 0xffff
  movk ip0, 1096 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_reader_lock
  .p2align 4
  .type g_rw_lock_reader_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_reader_lock
#endif
g_rw_lock_reader_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1097 & 0xffff
#if 1097 > 0xffff
  movk ip0, 1097 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_reader_trylock
  .p2align 4
  .type g_rw_lock_reader_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_reader_trylock
#endif
g_rw_lock_reader_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1098 & 0xffff
#if 1098 > 0xffff
  movk ip0, 1098 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_reader_unlock
  .p2align 4
  .type g_rw_lock_reader_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_reader_unlock
#endif
g_rw_lock_reader_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1099 & 0xffff
#if 1099 > 0xffff
  movk ip0, 1099 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_writer_lock
  .p2align 4
  .type g_rw_lock_writer_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_writer_lock
#endif
g_rw_lock_writer_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1100 & 0xffff
#if 1100 > 0xffff
  movk ip0, 1100 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_writer_trylock
  .p2align 4
  .type g_rw_lock_writer_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_writer_trylock
#endif
g_rw_lock_writer_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1101 & 0xffff
#if 1101 > 0xffff
  movk ip0, 1101 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_rw_lock_writer_unlock
  .p2align 4
  .type g_rw_lock_writer_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_rw_lock_writer_unlock
#endif
g_rw_lock_writer_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1102 & 0xffff
#if 1102 > 0xffff
  movk ip0, 1102 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_cur_line
  .p2align 4
  .type g_scanner_cur_line, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_cur_line
#endif
g_scanner_cur_line:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1103 & 0xffff
#if 1103 > 0xffff
  movk ip0, 1103 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_cur_position
  .p2align 4
  .type g_scanner_cur_position, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_cur_position
#endif
g_scanner_cur_position:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1104 & 0xffff
#if 1104 > 0xffff
  movk ip0, 1104 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_cur_token
  .p2align 4
  .type g_scanner_cur_token, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_cur_token
#endif
g_scanner_cur_token:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1105 & 0xffff
#if 1105 > 0xffff
  movk ip0, 1105 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_cur_value
  .p2align 4
  .type g_scanner_cur_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_cur_value
#endif
g_scanner_cur_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1106 & 0xffff
#if 1106 > 0xffff
  movk ip0, 1106 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_destroy
  .p2align 4
  .type g_scanner_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_destroy
#endif
g_scanner_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1107 & 0xffff
#if 1107 > 0xffff
  movk ip0, 1107 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_eof
  .p2align 4
  .type g_scanner_eof, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_eof
#endif
g_scanner_eof:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1108 & 0xffff
#if 1108 > 0xffff
  movk ip0, 1108 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_error
  .p2align 4
  .type g_scanner_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_error
#endif
g_scanner_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1109 & 0xffff
#if 1109 > 0xffff
  movk ip0, 1109 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_get_next_token
  .p2align 4
  .type g_scanner_get_next_token, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_get_next_token
#endif
g_scanner_get_next_token:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1110 & 0xffff
#if 1110 > 0xffff
  movk ip0, 1110 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_input_file
  .p2align 4
  .type g_scanner_input_file, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_input_file
#endif
g_scanner_input_file:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1111 & 0xffff
#if 1111 > 0xffff
  movk ip0, 1111 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_input_text
  .p2align 4
  .type g_scanner_input_text, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_input_text
#endif
g_scanner_input_text:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1112 & 0xffff
#if 1112 > 0xffff
  movk ip0, 1112 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_lookup_symbol
  .p2align 4
  .type g_scanner_lookup_symbol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_lookup_symbol
#endif
g_scanner_lookup_symbol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1113 & 0xffff
#if 1113 > 0xffff
  movk ip0, 1113 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_new
  .p2align 4
  .type g_scanner_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_new
#endif
g_scanner_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1114 & 0xffff
#if 1114 > 0xffff
  movk ip0, 1114 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_peek_next_token
  .p2align 4
  .type g_scanner_peek_next_token, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_peek_next_token
#endif
g_scanner_peek_next_token:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1115 & 0xffff
#if 1115 > 0xffff
  movk ip0, 1115 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_scope_add_symbol
  .p2align 4
  .type g_scanner_scope_add_symbol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_scope_add_symbol
#endif
g_scanner_scope_add_symbol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1116 & 0xffff
#if 1116 > 0xffff
  movk ip0, 1116 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_scope_foreach_symbol
  .p2align 4
  .type g_scanner_scope_foreach_symbol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_scope_foreach_symbol
#endif
g_scanner_scope_foreach_symbol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1117 & 0xffff
#if 1117 > 0xffff
  movk ip0, 1117 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_scope_lookup_symbol
  .p2align 4
  .type g_scanner_scope_lookup_symbol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_scope_lookup_symbol
#endif
g_scanner_scope_lookup_symbol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1118 & 0xffff
#if 1118 > 0xffff
  movk ip0, 1118 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_scope_remove_symbol
  .p2align 4
  .type g_scanner_scope_remove_symbol, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_scope_remove_symbol
#endif
g_scanner_scope_remove_symbol:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1119 & 0xffff
#if 1119 > 0xffff
  movk ip0, 1119 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_set_scope
  .p2align 4
  .type g_scanner_set_scope, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_set_scope
#endif
g_scanner_set_scope:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1120 & 0xffff
#if 1120 > 0xffff
  movk ip0, 1120 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_sync_file_offset
  .p2align 4
  .type g_scanner_sync_file_offset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_sync_file_offset
#endif
g_scanner_sync_file_offset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1121 & 0xffff
#if 1121 > 0xffff
  movk ip0, 1121 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_unexp_token
  .p2align 4
  .type g_scanner_unexp_token, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_unexp_token
#endif
g_scanner_unexp_token:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1122 & 0xffff
#if 1122 > 0xffff
  movk ip0, 1122 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_scanner_warn
  .p2align 4
  .type g_scanner_warn, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_scanner_warn
#endif
g_scanner_warn:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1123 & 0xffff
#if 1123 > 0xffff
  movk ip0, 1123 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_append
  .p2align 4
  .type g_sequence_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_append
#endif
g_sequence_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+8992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+8992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1124 & 0xffff
#if 1124 > 0xffff
  movk ip0, 1124 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_foreach
  .p2align 4
  .type g_sequence_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_foreach
#endif
g_sequence_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1125 & 0xffff
#if 1125 > 0xffff
  movk ip0, 1125 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_foreach_range
  .p2align 4
  .type g_sequence_foreach_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_foreach_range
#endif
g_sequence_foreach_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1126 & 0xffff
#if 1126 > 0xffff
  movk ip0, 1126 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_free
  .p2align 4
  .type g_sequence_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_free
#endif
g_sequence_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1127 & 0xffff
#if 1127 > 0xffff
  movk ip0, 1127 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_get
  .p2align 4
  .type g_sequence_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_get
#endif
g_sequence_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1128 & 0xffff
#if 1128 > 0xffff
  movk ip0, 1128 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_get_begin_iter
  .p2align 4
  .type g_sequence_get_begin_iter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_get_begin_iter
#endif
g_sequence_get_begin_iter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1129 & 0xffff
#if 1129 > 0xffff
  movk ip0, 1129 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_get_end_iter
  .p2align 4
  .type g_sequence_get_end_iter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_get_end_iter
#endif
g_sequence_get_end_iter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1130 & 0xffff
#if 1130 > 0xffff
  movk ip0, 1130 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_get_iter_at_pos
  .p2align 4
  .type g_sequence_get_iter_at_pos, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_get_iter_at_pos
#endif
g_sequence_get_iter_at_pos:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1131 & 0xffff
#if 1131 > 0xffff
  movk ip0, 1131 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_get_length
  .p2align 4
  .type g_sequence_get_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_get_length
#endif
g_sequence_get_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1132 & 0xffff
#if 1132 > 0xffff
  movk ip0, 1132 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_insert_before
  .p2align 4
  .type g_sequence_insert_before, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_insert_before
#endif
g_sequence_insert_before:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1133 & 0xffff
#if 1133 > 0xffff
  movk ip0, 1133 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_insert_sorted
  .p2align 4
  .type g_sequence_insert_sorted, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_insert_sorted
#endif
g_sequence_insert_sorted:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1134 & 0xffff
#if 1134 > 0xffff
  movk ip0, 1134 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_insert_sorted_iter
  .p2align 4
  .type g_sequence_insert_sorted_iter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_insert_sorted_iter
#endif
g_sequence_insert_sorted_iter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1135 & 0xffff
#if 1135 > 0xffff
  movk ip0, 1135 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_is_empty
  .p2align 4
  .type g_sequence_is_empty, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_is_empty
#endif
g_sequence_is_empty:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1136 & 0xffff
#if 1136 > 0xffff
  movk ip0, 1136 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_compare
  .p2align 4
  .type g_sequence_iter_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_compare
#endif
g_sequence_iter_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1137 & 0xffff
#if 1137 > 0xffff
  movk ip0, 1137 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_get_position
  .p2align 4
  .type g_sequence_iter_get_position, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_get_position
#endif
g_sequence_iter_get_position:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1138 & 0xffff
#if 1138 > 0xffff
  movk ip0, 1138 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_get_sequence
  .p2align 4
  .type g_sequence_iter_get_sequence, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_get_sequence
#endif
g_sequence_iter_get_sequence:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1139 & 0xffff
#if 1139 > 0xffff
  movk ip0, 1139 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_is_begin
  .p2align 4
  .type g_sequence_iter_is_begin, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_is_begin
#endif
g_sequence_iter_is_begin:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1140 & 0xffff
#if 1140 > 0xffff
  movk ip0, 1140 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_is_end
  .p2align 4
  .type g_sequence_iter_is_end, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_is_end
#endif
g_sequence_iter_is_end:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1141 & 0xffff
#if 1141 > 0xffff
  movk ip0, 1141 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_move
  .p2align 4
  .type g_sequence_iter_move, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_move
#endif
g_sequence_iter_move:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1142 & 0xffff
#if 1142 > 0xffff
  movk ip0, 1142 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_next
  .p2align 4
  .type g_sequence_iter_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_next
#endif
g_sequence_iter_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1143 & 0xffff
#if 1143 > 0xffff
  movk ip0, 1143 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_iter_prev
  .p2align 4
  .type g_sequence_iter_prev, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_iter_prev
#endif
g_sequence_iter_prev:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1144 & 0xffff
#if 1144 > 0xffff
  movk ip0, 1144 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_lookup
  .p2align 4
  .type g_sequence_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_lookup
#endif
g_sequence_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1145 & 0xffff
#if 1145 > 0xffff
  movk ip0, 1145 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_lookup_iter
  .p2align 4
  .type g_sequence_lookup_iter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_lookup_iter
#endif
g_sequence_lookup_iter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1146 & 0xffff
#if 1146 > 0xffff
  movk ip0, 1146 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_move
  .p2align 4
  .type g_sequence_move, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_move
#endif
g_sequence_move:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1147 & 0xffff
#if 1147 > 0xffff
  movk ip0, 1147 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_move_range
  .p2align 4
  .type g_sequence_move_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_move_range
#endif
g_sequence_move_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1148 & 0xffff
#if 1148 > 0xffff
  movk ip0, 1148 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_new
  .p2align 4
  .type g_sequence_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_new
#endif
g_sequence_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1149 & 0xffff
#if 1149 > 0xffff
  movk ip0, 1149 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_prepend
  .p2align 4
  .type g_sequence_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_prepend
#endif
g_sequence_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1150 & 0xffff
#if 1150 > 0xffff
  movk ip0, 1150 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_range_get_midpoint
  .p2align 4
  .type g_sequence_range_get_midpoint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_range_get_midpoint
#endif
g_sequence_range_get_midpoint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1151 & 0xffff
#if 1151 > 0xffff
  movk ip0, 1151 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_remove
  .p2align 4
  .type g_sequence_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_remove
#endif
g_sequence_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1152 & 0xffff
#if 1152 > 0xffff
  movk ip0, 1152 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_remove_range
  .p2align 4
  .type g_sequence_remove_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_remove_range
#endif
g_sequence_remove_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1153 & 0xffff
#if 1153 > 0xffff
  movk ip0, 1153 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_search
  .p2align 4
  .type g_sequence_search, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_search
#endif
g_sequence_search:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1154 & 0xffff
#if 1154 > 0xffff
  movk ip0, 1154 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_search_iter
  .p2align 4
  .type g_sequence_search_iter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_search_iter
#endif
g_sequence_search_iter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1155 & 0xffff
#if 1155 > 0xffff
  movk ip0, 1155 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_set
  .p2align 4
  .type g_sequence_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_set
#endif
g_sequence_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1156 & 0xffff
#if 1156 > 0xffff
  movk ip0, 1156 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_sort
  .p2align 4
  .type g_sequence_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_sort
#endif
g_sequence_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1157 & 0xffff
#if 1157 > 0xffff
  movk ip0, 1157 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_sort_changed
  .p2align 4
  .type g_sequence_sort_changed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_sort_changed
#endif
g_sequence_sort_changed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1158 & 0xffff
#if 1158 > 0xffff
  movk ip0, 1158 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_sort_changed_iter
  .p2align 4
  .type g_sequence_sort_changed_iter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_sort_changed_iter
#endif
g_sequence_sort_changed_iter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1159 & 0xffff
#if 1159 > 0xffff
  movk ip0, 1159 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_sort_iter
  .p2align 4
  .type g_sequence_sort_iter, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_sort_iter
#endif
g_sequence_sort_iter:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1160 & 0xffff
#if 1160 > 0xffff
  movk ip0, 1160 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sequence_swap
  .p2align 4
  .type g_sequence_swap, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sequence_swap
#endif
g_sequence_swap:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1161 & 0xffff
#if 1161 > 0xffff
  movk ip0, 1161 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_set_application_name
  .p2align 4
  .type g_set_application_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_set_application_name
#endif
g_set_application_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1162 & 0xffff
#if 1162 > 0xffff
  movk ip0, 1162 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_set_error
  .p2align 4
  .type g_set_error, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_set_error
#endif
g_set_error:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1163 & 0xffff
#if 1163 > 0xffff
  movk ip0, 1163 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_set_error_literal
  .p2align 4
  .type g_set_error_literal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_set_error_literal
#endif
g_set_error_literal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1164 & 0xffff
#if 1164 > 0xffff
  movk ip0, 1164 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_set_prgname
  .p2align 4
  .type g_set_prgname, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_set_prgname
#endif
g_set_prgname:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1165 & 0xffff
#if 1165 > 0xffff
  movk ip0, 1165 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_set_print_handler
  .p2align 4
  .type g_set_print_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_set_print_handler
#endif
g_set_print_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1166 & 0xffff
#if 1166 > 0xffff
  movk ip0, 1166 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_set_printerr_handler
  .p2align 4
  .type g_set_printerr_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_set_printerr_handler
#endif
g_set_printerr_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1167 & 0xffff
#if 1167 > 0xffff
  movk ip0, 1167 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_setenv
  .p2align 4
  .type g_setenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_setenv
#endif
g_setenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1168 & 0xffff
#if 1168 > 0xffff
  movk ip0, 1168 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_shell_error_quark
  .p2align 4
  .type g_shell_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_shell_error_quark
#endif
g_shell_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1169 & 0xffff
#if 1169 > 0xffff
  movk ip0, 1169 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_shell_parse_argv
  .p2align 4
  .type g_shell_parse_argv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_shell_parse_argv
#endif
g_shell_parse_argv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1170 & 0xffff
#if 1170 > 0xffff
  movk ip0, 1170 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_shell_quote
  .p2align 4
  .type g_shell_quote, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_shell_quote
#endif
g_shell_quote:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1171 & 0xffff
#if 1171 > 0xffff
  movk ip0, 1171 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_shell_unquote
  .p2align 4
  .type g_shell_unquote, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_shell_unquote
#endif
g_shell_unquote:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1172 & 0xffff
#if 1172 > 0xffff
  movk ip0, 1172 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_alloc
  .p2align 4
  .type g_slice_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_alloc
#endif
g_slice_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1173 & 0xffff
#if 1173 > 0xffff
  movk ip0, 1173 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_alloc0
  .p2align 4
  .type g_slice_alloc0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_alloc0
#endif
g_slice_alloc0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1174 & 0xffff
#if 1174 > 0xffff
  movk ip0, 1174 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_copy
  .p2align 4
  .type g_slice_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_copy
#endif
g_slice_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1175 & 0xffff
#if 1175 > 0xffff
  movk ip0, 1175 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_free1
  .p2align 4
  .type g_slice_free1, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_free1
#endif
g_slice_free1:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1176 & 0xffff
#if 1176 > 0xffff
  movk ip0, 1176 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_free_chain_with_offset
  .p2align 4
  .type g_slice_free_chain_with_offset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_free_chain_with_offset
#endif
g_slice_free_chain_with_offset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1177 & 0xffff
#if 1177 > 0xffff
  movk ip0, 1177 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_get_config
  .p2align 4
  .type g_slice_get_config, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_get_config
#endif
g_slice_get_config:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1178 & 0xffff
#if 1178 > 0xffff
  movk ip0, 1178 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_get_config_state
  .p2align 4
  .type g_slice_get_config_state, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_get_config_state
#endif
g_slice_get_config_state:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1179 & 0xffff
#if 1179 > 0xffff
  movk ip0, 1179 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slice_set_config
  .p2align 4
  .type g_slice_set_config, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slice_set_config
#endif
g_slice_set_config:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1180 & 0xffff
#if 1180 > 0xffff
  movk ip0, 1180 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_alloc
  .p2align 4
  .type g_slist_alloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_alloc
#endif
g_slist_alloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1181 & 0xffff
#if 1181 > 0xffff
  movk ip0, 1181 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_append
  .p2align 4
  .type g_slist_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_append
#endif
g_slist_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1182 & 0xffff
#if 1182 > 0xffff
  movk ip0, 1182 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_concat
  .p2align 4
  .type g_slist_concat, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_concat
#endif
g_slist_concat:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1183 & 0xffff
#if 1183 > 0xffff
  movk ip0, 1183 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_copy
  .p2align 4
  .type g_slist_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_copy
#endif
g_slist_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1184 & 0xffff
#if 1184 > 0xffff
  movk ip0, 1184 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_copy_deep
  .p2align 4
  .type g_slist_copy_deep, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_copy_deep
#endif
g_slist_copy_deep:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1185 & 0xffff
#if 1185 > 0xffff
  movk ip0, 1185 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_delete_link
  .p2align 4
  .type g_slist_delete_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_delete_link
#endif
g_slist_delete_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1186 & 0xffff
#if 1186 > 0xffff
  movk ip0, 1186 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_find
  .p2align 4
  .type g_slist_find, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_find
#endif
g_slist_find:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1187 & 0xffff
#if 1187 > 0xffff
  movk ip0, 1187 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_find_custom
  .p2align 4
  .type g_slist_find_custom, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_find_custom
#endif
g_slist_find_custom:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1188 & 0xffff
#if 1188 > 0xffff
  movk ip0, 1188 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_foreach
  .p2align 4
  .type g_slist_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_foreach
#endif
g_slist_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1189 & 0xffff
#if 1189 > 0xffff
  movk ip0, 1189 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_free
  .p2align 4
  .type g_slist_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_free
#endif
g_slist_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1190 & 0xffff
#if 1190 > 0xffff
  movk ip0, 1190 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_free_1
  .p2align 4
  .type g_slist_free_1, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_free_1
#endif
g_slist_free_1:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1191 & 0xffff
#if 1191 > 0xffff
  movk ip0, 1191 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_free_full
  .p2align 4
  .type g_slist_free_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_free_full
#endif
g_slist_free_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1192 & 0xffff
#if 1192 > 0xffff
  movk ip0, 1192 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_index
  .p2align 4
  .type g_slist_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_index
#endif
g_slist_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1193 & 0xffff
#if 1193 > 0xffff
  movk ip0, 1193 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_insert
  .p2align 4
  .type g_slist_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_insert
#endif
g_slist_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1194 & 0xffff
#if 1194 > 0xffff
  movk ip0, 1194 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_insert_before
  .p2align 4
  .type g_slist_insert_before, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_insert_before
#endif
g_slist_insert_before:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1195 & 0xffff
#if 1195 > 0xffff
  movk ip0, 1195 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_insert_sorted
  .p2align 4
  .type g_slist_insert_sorted, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_insert_sorted
#endif
g_slist_insert_sorted:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1196 & 0xffff
#if 1196 > 0xffff
  movk ip0, 1196 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_insert_sorted_with_data
  .p2align 4
  .type g_slist_insert_sorted_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_insert_sorted_with_data
#endif
g_slist_insert_sorted_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1197 & 0xffff
#if 1197 > 0xffff
  movk ip0, 1197 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_last
  .p2align 4
  .type g_slist_last, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_last
#endif
g_slist_last:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1198 & 0xffff
#if 1198 > 0xffff
  movk ip0, 1198 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_length
  .p2align 4
  .type g_slist_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_length
#endif
g_slist_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1199 & 0xffff
#if 1199 > 0xffff
  movk ip0, 1199 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_nth
  .p2align 4
  .type g_slist_nth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_nth
#endif
g_slist_nth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1200 & 0xffff
#if 1200 > 0xffff
  movk ip0, 1200 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_nth_data
  .p2align 4
  .type g_slist_nth_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_nth_data
#endif
g_slist_nth_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1201 & 0xffff
#if 1201 > 0xffff
  movk ip0, 1201 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_pop_allocator
  .p2align 4
  .type g_slist_pop_allocator, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_pop_allocator
#endif
g_slist_pop_allocator:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1202 & 0xffff
#if 1202 > 0xffff
  movk ip0, 1202 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_position
  .p2align 4
  .type g_slist_position, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_position
#endif
g_slist_position:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1203 & 0xffff
#if 1203 > 0xffff
  movk ip0, 1203 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_prepend
  .p2align 4
  .type g_slist_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_prepend
#endif
g_slist_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1204 & 0xffff
#if 1204 > 0xffff
  movk ip0, 1204 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_push_allocator
  .p2align 4
  .type g_slist_push_allocator, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_push_allocator
#endif
g_slist_push_allocator:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1205 & 0xffff
#if 1205 > 0xffff
  movk ip0, 1205 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_remove
  .p2align 4
  .type g_slist_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_remove
#endif
g_slist_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1206 & 0xffff
#if 1206 > 0xffff
  movk ip0, 1206 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_remove_all
  .p2align 4
  .type g_slist_remove_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_remove_all
#endif
g_slist_remove_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1207 & 0xffff
#if 1207 > 0xffff
  movk ip0, 1207 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_remove_link
  .p2align 4
  .type g_slist_remove_link, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_remove_link
#endif
g_slist_remove_link:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1208 & 0xffff
#if 1208 > 0xffff
  movk ip0, 1208 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_reverse
  .p2align 4
  .type g_slist_reverse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_reverse
#endif
g_slist_reverse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1209 & 0xffff
#if 1209 > 0xffff
  movk ip0, 1209 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_sort
  .p2align 4
  .type g_slist_sort, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_sort
#endif
g_slist_sort:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1210 & 0xffff
#if 1210 > 0xffff
  movk ip0, 1210 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_slist_sort_with_data
  .p2align 4
  .type g_slist_sort_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_slist_sort_with_data
#endif
g_slist_sort_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1211 & 0xffff
#if 1211 > 0xffff
  movk ip0, 1211 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_snprintf
  .p2align 4
  .type g_snprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_snprintf
#endif
g_snprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1212 & 0xffff
#if 1212 > 0xffff
  movk ip0, 1212 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sort_array
  .p2align 4
  .type g_sort_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sort_array
#endif
g_sort_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1213 & 0xffff
#if 1213 > 0xffff
  movk ip0, 1213 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_add_child_source
  .p2align 4
  .type g_source_add_child_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_add_child_source
#endif
g_source_add_child_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1214 & 0xffff
#if 1214 > 0xffff
  movk ip0, 1214 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_add_poll
  .p2align 4
  .type g_source_add_poll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_add_poll
#endif
g_source_add_poll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1215 & 0xffff
#if 1215 > 0xffff
  movk ip0, 1215 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_add_unix_fd
  .p2align 4
  .type g_source_add_unix_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_add_unix_fd
#endif
g_source_add_unix_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1216 & 0xffff
#if 1216 > 0xffff
  movk ip0, 1216 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_attach
  .p2align 4
  .type g_source_attach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_attach
#endif
g_source_attach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1217 & 0xffff
#if 1217 > 0xffff
  movk ip0, 1217 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_destroy
  .p2align 4
  .type g_source_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_destroy
#endif
g_source_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1218 & 0xffff
#if 1218 > 0xffff
  movk ip0, 1218 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_can_recurse
  .p2align 4
  .type g_source_get_can_recurse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_can_recurse
#endif
g_source_get_can_recurse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1219 & 0xffff
#if 1219 > 0xffff
  movk ip0, 1219 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_context
  .p2align 4
  .type g_source_get_context, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_context
#endif
g_source_get_context:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1220 & 0xffff
#if 1220 > 0xffff
  movk ip0, 1220 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_current_time
  .p2align 4
  .type g_source_get_current_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_current_time
#endif
g_source_get_current_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1221 & 0xffff
#if 1221 > 0xffff
  movk ip0, 1221 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_id
  .p2align 4
  .type g_source_get_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_id
#endif
g_source_get_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1222 & 0xffff
#if 1222 > 0xffff
  movk ip0, 1222 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_name
  .p2align 4
  .type g_source_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_name
#endif
g_source_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1223 & 0xffff
#if 1223 > 0xffff
  movk ip0, 1223 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_priority
  .p2align 4
  .type g_source_get_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_priority
#endif
g_source_get_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1224 & 0xffff
#if 1224 > 0xffff
  movk ip0, 1224 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_ready_time
  .p2align 4
  .type g_source_get_ready_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_ready_time
#endif
g_source_get_ready_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1225 & 0xffff
#if 1225 > 0xffff
  movk ip0, 1225 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_get_time
  .p2align 4
  .type g_source_get_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_get_time
#endif
g_source_get_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1226 & 0xffff
#if 1226 > 0xffff
  movk ip0, 1226 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_is_destroyed
  .p2align 4
  .type g_source_is_destroyed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_is_destroyed
#endif
g_source_is_destroyed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1227 & 0xffff
#if 1227 > 0xffff
  movk ip0, 1227 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_modify_unix_fd
  .p2align 4
  .type g_source_modify_unix_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_modify_unix_fd
#endif
g_source_modify_unix_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1228 & 0xffff
#if 1228 > 0xffff
  movk ip0, 1228 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_new
  .p2align 4
  .type g_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_new
#endif
g_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1229 & 0xffff
#if 1229 > 0xffff
  movk ip0, 1229 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_query_unix_fd
  .p2align 4
  .type g_source_query_unix_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_query_unix_fd
#endif
g_source_query_unix_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1230 & 0xffff
#if 1230 > 0xffff
  movk ip0, 1230 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_ref
  .p2align 4
  .type g_source_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_ref
#endif
g_source_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1231 & 0xffff
#if 1231 > 0xffff
  movk ip0, 1231 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_remove
  .p2align 4
  .type g_source_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_remove
#endif
g_source_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1232 & 0xffff
#if 1232 > 0xffff
  movk ip0, 1232 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_remove_by_funcs_user_data
  .p2align 4
  .type g_source_remove_by_funcs_user_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_remove_by_funcs_user_data
#endif
g_source_remove_by_funcs_user_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1233 & 0xffff
#if 1233 > 0xffff
  movk ip0, 1233 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_remove_by_user_data
  .p2align 4
  .type g_source_remove_by_user_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_remove_by_user_data
#endif
g_source_remove_by_user_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1234 & 0xffff
#if 1234 > 0xffff
  movk ip0, 1234 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_remove_child_source
  .p2align 4
  .type g_source_remove_child_source, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_remove_child_source
#endif
g_source_remove_child_source:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1235 & 0xffff
#if 1235 > 0xffff
  movk ip0, 1235 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_remove_poll
  .p2align 4
  .type g_source_remove_poll, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_remove_poll
#endif
g_source_remove_poll:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1236 & 0xffff
#if 1236 > 0xffff
  movk ip0, 1236 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_remove_unix_fd
  .p2align 4
  .type g_source_remove_unix_fd, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_remove_unix_fd
#endif
g_source_remove_unix_fd:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1237 & 0xffff
#if 1237 > 0xffff
  movk ip0, 1237 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_callback
  .p2align 4
  .type g_source_set_callback, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_callback
#endif
g_source_set_callback:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1238 & 0xffff
#if 1238 > 0xffff
  movk ip0, 1238 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_callback_indirect
  .p2align 4
  .type g_source_set_callback_indirect, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_callback_indirect
#endif
g_source_set_callback_indirect:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1239 & 0xffff
#if 1239 > 0xffff
  movk ip0, 1239 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_can_recurse
  .p2align 4
  .type g_source_set_can_recurse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_can_recurse
#endif
g_source_set_can_recurse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1240 & 0xffff
#if 1240 > 0xffff
  movk ip0, 1240 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_dispose_function
  .p2align 4
  .type g_source_set_dispose_function, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_dispose_function
#endif
g_source_set_dispose_function:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1241 & 0xffff
#if 1241 > 0xffff
  movk ip0, 1241 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_funcs
  .p2align 4
  .type g_source_set_funcs, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_funcs
#endif
g_source_set_funcs:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1242 & 0xffff
#if 1242 > 0xffff
  movk ip0, 1242 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_name
  .p2align 4
  .type g_source_set_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_name
#endif
g_source_set_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1243 & 0xffff
#if 1243 > 0xffff
  movk ip0, 1243 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_name_by_id
  .p2align 4
  .type g_source_set_name_by_id, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_name_by_id
#endif
g_source_set_name_by_id:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1244 & 0xffff
#if 1244 > 0xffff
  movk ip0, 1244 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_priority
  .p2align 4
  .type g_source_set_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_priority
#endif
g_source_set_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1245 & 0xffff
#if 1245 > 0xffff
  movk ip0, 1245 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_ready_time
  .p2align 4
  .type g_source_set_ready_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_ready_time
#endif
g_source_set_ready_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1246 & 0xffff
#if 1246 > 0xffff
  movk ip0, 1246 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_set_static_name
  .p2align 4
  .type g_source_set_static_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_set_static_name
#endif
g_source_set_static_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1247 & 0xffff
#if 1247 > 0xffff
  movk ip0, 1247 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_source_unref
  .p2align 4
  .type g_source_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_source_unref
#endif
g_source_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1248 & 0xffff
#if 1248 > 0xffff
  movk ip0, 1248 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spaced_primes_closest
  .p2align 4
  .type g_spaced_primes_closest, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spaced_primes_closest
#endif
g_spaced_primes_closest:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+9992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+9992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1249 & 0xffff
#if 1249 > 0xffff
  movk ip0, 1249 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_async
  .p2align 4
  .type g_spawn_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_async
#endif
g_spawn_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1250 & 0xffff
#if 1250 > 0xffff
  movk ip0, 1250 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_async_with_fds
  .p2align 4
  .type g_spawn_async_with_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_async_with_fds
#endif
g_spawn_async_with_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1251 & 0xffff
#if 1251 > 0xffff
  movk ip0, 1251 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_async_with_pipes
  .p2align 4
  .type g_spawn_async_with_pipes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_async_with_pipes
#endif
g_spawn_async_with_pipes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1252 & 0xffff
#if 1252 > 0xffff
  movk ip0, 1252 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_async_with_pipes_and_fds
  .p2align 4
  .type g_spawn_async_with_pipes_and_fds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_async_with_pipes_and_fds
#endif
g_spawn_async_with_pipes_and_fds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1253 & 0xffff
#if 1253 > 0xffff
  movk ip0, 1253 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_check_exit_status
  .p2align 4
  .type g_spawn_check_exit_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_check_exit_status
#endif
g_spawn_check_exit_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1254 & 0xffff
#if 1254 > 0xffff
  movk ip0, 1254 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_check_wait_status
  .p2align 4
  .type g_spawn_check_wait_status, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_check_wait_status
#endif
g_spawn_check_wait_status:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1255 & 0xffff
#if 1255 > 0xffff
  movk ip0, 1255 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_close_pid
  .p2align 4
  .type g_spawn_close_pid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_close_pid
#endif
g_spawn_close_pid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1256 & 0xffff
#if 1256 > 0xffff
  movk ip0, 1256 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_command_line_async
  .p2align 4
  .type g_spawn_command_line_async, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_command_line_async
#endif
g_spawn_command_line_async:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1257 & 0xffff
#if 1257 > 0xffff
  movk ip0, 1257 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_command_line_sync
  .p2align 4
  .type g_spawn_command_line_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_command_line_sync
#endif
g_spawn_command_line_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1258 & 0xffff
#if 1258 > 0xffff
  movk ip0, 1258 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_error_quark
  .p2align 4
  .type g_spawn_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_error_quark
#endif
g_spawn_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1259 & 0xffff
#if 1259 > 0xffff
  movk ip0, 1259 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_exit_error_quark
  .p2align 4
  .type g_spawn_exit_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_exit_error_quark
#endif
g_spawn_exit_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1260 & 0xffff
#if 1260 > 0xffff
  movk ip0, 1260 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_spawn_sync
  .p2align 4
  .type g_spawn_sync, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_spawn_sync
#endif
g_spawn_sync:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1261 & 0xffff
#if 1261 > 0xffff
  movk ip0, 1261 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_sprintf
  .p2align 4
  .type g_sprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_sprintf
#endif
g_sprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1262 & 0xffff
#if 1262 > 0xffff
  movk ip0, 1262 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_stat
  .p2align 4
  .type g_stat, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_stat
#endif
g_stat:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1263 & 0xffff
#if 1263 > 0xffff
  movk ip0, 1263 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_mutex_free
  .p2align 4
  .type g_static_mutex_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_mutex_free
#endif
g_static_mutex_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1264 & 0xffff
#if 1264 > 0xffff
  movk ip0, 1264 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_mutex_get_mutex_impl
  .p2align 4
  .type g_static_mutex_get_mutex_impl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_mutex_get_mutex_impl
#endif
g_static_mutex_get_mutex_impl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1265 & 0xffff
#if 1265 > 0xffff
  movk ip0, 1265 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_mutex_init
  .p2align 4
  .type g_static_mutex_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_mutex_init
#endif
g_static_mutex_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1266 & 0xffff
#if 1266 > 0xffff
  movk ip0, 1266 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_private_free
  .p2align 4
  .type g_static_private_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_private_free
#endif
g_static_private_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1267 & 0xffff
#if 1267 > 0xffff
  movk ip0, 1267 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_private_get
  .p2align 4
  .type g_static_private_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_private_get
#endif
g_static_private_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1268 & 0xffff
#if 1268 > 0xffff
  movk ip0, 1268 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_private_init
  .p2align 4
  .type g_static_private_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_private_init
#endif
g_static_private_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1269 & 0xffff
#if 1269 > 0xffff
  movk ip0, 1269 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_private_set
  .p2align 4
  .type g_static_private_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_private_set
#endif
g_static_private_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1270 & 0xffff
#if 1270 > 0xffff
  movk ip0, 1270 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rec_mutex_free
  .p2align 4
  .type g_static_rec_mutex_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rec_mutex_free
#endif
g_static_rec_mutex_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1271 & 0xffff
#if 1271 > 0xffff
  movk ip0, 1271 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rec_mutex_init
  .p2align 4
  .type g_static_rec_mutex_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rec_mutex_init
#endif
g_static_rec_mutex_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1272 & 0xffff
#if 1272 > 0xffff
  movk ip0, 1272 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rec_mutex_lock
  .p2align 4
  .type g_static_rec_mutex_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rec_mutex_lock
#endif
g_static_rec_mutex_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1273 & 0xffff
#if 1273 > 0xffff
  movk ip0, 1273 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rec_mutex_lock_full
  .p2align 4
  .type g_static_rec_mutex_lock_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rec_mutex_lock_full
#endif
g_static_rec_mutex_lock_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1274 & 0xffff
#if 1274 > 0xffff
  movk ip0, 1274 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rec_mutex_trylock
  .p2align 4
  .type g_static_rec_mutex_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rec_mutex_trylock
#endif
g_static_rec_mutex_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1275 & 0xffff
#if 1275 > 0xffff
  movk ip0, 1275 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rec_mutex_unlock
  .p2align 4
  .type g_static_rec_mutex_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rec_mutex_unlock
#endif
g_static_rec_mutex_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1276 & 0xffff
#if 1276 > 0xffff
  movk ip0, 1276 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rec_mutex_unlock_full
  .p2align 4
  .type g_static_rec_mutex_unlock_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rec_mutex_unlock_full
#endif
g_static_rec_mutex_unlock_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1277 & 0xffff
#if 1277 > 0xffff
  movk ip0, 1277 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_free
  .p2align 4
  .type g_static_rw_lock_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_free
#endif
g_static_rw_lock_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1278 & 0xffff
#if 1278 > 0xffff
  movk ip0, 1278 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_init
  .p2align 4
  .type g_static_rw_lock_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_init
#endif
g_static_rw_lock_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1279 & 0xffff
#if 1279 > 0xffff
  movk ip0, 1279 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_reader_lock
  .p2align 4
  .type g_static_rw_lock_reader_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_reader_lock
#endif
g_static_rw_lock_reader_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1280 & 0xffff
#if 1280 > 0xffff
  movk ip0, 1280 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_reader_trylock
  .p2align 4
  .type g_static_rw_lock_reader_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_reader_trylock
#endif
g_static_rw_lock_reader_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1281 & 0xffff
#if 1281 > 0xffff
  movk ip0, 1281 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_reader_unlock
  .p2align 4
  .type g_static_rw_lock_reader_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_reader_unlock
#endif
g_static_rw_lock_reader_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1282 & 0xffff
#if 1282 > 0xffff
  movk ip0, 1282 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_writer_lock
  .p2align 4
  .type g_static_rw_lock_writer_lock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_writer_lock
#endif
g_static_rw_lock_writer_lock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1283 & 0xffff
#if 1283 > 0xffff
  movk ip0, 1283 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_writer_trylock
  .p2align 4
  .type g_static_rw_lock_writer_trylock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_writer_trylock
#endif
g_static_rw_lock_writer_trylock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1284 & 0xffff
#if 1284 > 0xffff
  movk ip0, 1284 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_static_rw_lock_writer_unlock
  .p2align 4
  .type g_static_rw_lock_writer_unlock, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_static_rw_lock_writer_unlock
#endif
g_static_rw_lock_writer_unlock:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1285 & 0xffff
#if 1285 > 0xffff
  movk ip0, 1285 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_stpcpy
  .p2align 4
  .type g_stpcpy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_stpcpy
#endif
g_stpcpy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1286 & 0xffff
#if 1286 > 0xffff
  movk ip0, 1286 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_equal
  .p2align 4
  .type g_str_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_equal
#endif
g_str_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1287 & 0xffff
#if 1287 > 0xffff
  movk ip0, 1287 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_has_prefix
  .p2align 4
  .type g_str_has_prefix, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_has_prefix
#endif
g_str_has_prefix:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1288 & 0xffff
#if 1288 > 0xffff
  movk ip0, 1288 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_has_suffix
  .p2align 4
  .type g_str_has_suffix, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_has_suffix
#endif
g_str_has_suffix:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1289 & 0xffff
#if 1289 > 0xffff
  movk ip0, 1289 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_hash
  .p2align 4
  .type g_str_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_hash
#endif
g_str_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1290 & 0xffff
#if 1290 > 0xffff
  movk ip0, 1290 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_is_ascii
  .p2align 4
  .type g_str_is_ascii, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_is_ascii
#endif
g_str_is_ascii:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1291 & 0xffff
#if 1291 > 0xffff
  movk ip0, 1291 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_match_string
  .p2align 4
  .type g_str_match_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_match_string
#endif
g_str_match_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1292 & 0xffff
#if 1292 > 0xffff
  movk ip0, 1292 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_to_ascii
  .p2align 4
  .type g_str_to_ascii, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_to_ascii
#endif
g_str_to_ascii:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1293 & 0xffff
#if 1293 > 0xffff
  movk ip0, 1293 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_str_tokenize_and_fold
  .p2align 4
  .type g_str_tokenize_and_fold, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_str_tokenize_and_fold
#endif
g_str_tokenize_and_fold:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1294 & 0xffff
#if 1294 > 0xffff
  movk ip0, 1294 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strcanon
  .p2align 4
  .type g_strcanon, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strcanon
#endif
g_strcanon:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1295 & 0xffff
#if 1295 > 0xffff
  movk ip0, 1295 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strcasecmp
  .p2align 4
  .type g_strcasecmp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strcasecmp
#endif
g_strcasecmp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1296 & 0xffff
#if 1296 > 0xffff
  movk ip0, 1296 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strchomp
  .p2align 4
  .type g_strchomp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strchomp
#endif
g_strchomp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1297 & 0xffff
#if 1297 > 0xffff
  movk ip0, 1297 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strchug
  .p2align 4
  .type g_strchug, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strchug
#endif
g_strchug:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1298 & 0xffff
#if 1298 > 0xffff
  movk ip0, 1298 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strcmp0
  .p2align 4
  .type g_strcmp0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strcmp0
#endif
g_strcmp0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1299 & 0xffff
#if 1299 > 0xffff
  movk ip0, 1299 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strcompress
  .p2align 4
  .type g_strcompress, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strcompress
#endif
g_strcompress:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1300 & 0xffff
#if 1300 > 0xffff
  movk ip0, 1300 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strconcat
  .p2align 4
  .type g_strconcat, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strconcat
#endif
g_strconcat:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1301 & 0xffff
#if 1301 > 0xffff
  movk ip0, 1301 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strdelimit
  .p2align 4
  .type g_strdelimit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strdelimit
#endif
g_strdelimit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1302 & 0xffff
#if 1302 > 0xffff
  movk ip0, 1302 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strdown
  .p2align 4
  .type g_strdown, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strdown
#endif
g_strdown:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1303 & 0xffff
#if 1303 > 0xffff
  movk ip0, 1303 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strdup
  .p2align 4
  .type g_strdup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strdup
#endif
g_strdup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1304 & 0xffff
#if 1304 > 0xffff
  movk ip0, 1304 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strdup_printf
  .p2align 4
  .type g_strdup_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strdup_printf
#endif
g_strdup_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1305 & 0xffff
#if 1305 > 0xffff
  movk ip0, 1305 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strdup_vprintf
  .p2align 4
  .type g_strdup_vprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strdup_vprintf
#endif
g_strdup_vprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1306 & 0xffff
#if 1306 > 0xffff
  movk ip0, 1306 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strdupv
  .p2align 4
  .type g_strdupv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strdupv
#endif
g_strdupv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1307 & 0xffff
#if 1307 > 0xffff
  movk ip0, 1307 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strerror
  .p2align 4
  .type g_strerror, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strerror
#endif
g_strerror:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1308 & 0xffff
#if 1308 > 0xffff
  movk ip0, 1308 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strescape
  .p2align 4
  .type g_strescape, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strescape
#endif
g_strescape:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1309 & 0xffff
#if 1309 > 0xffff
  movk ip0, 1309 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strfreev
  .p2align 4
  .type g_strfreev, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strfreev
#endif
g_strfreev:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1310 & 0xffff
#if 1310 > 0xffff
  movk ip0, 1310 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_append
  .p2align 4
  .type g_string_append, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_append
#endif
g_string_append:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1311 & 0xffff
#if 1311 > 0xffff
  movk ip0, 1311 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_append_c
  .p2align 4
  .type g_string_append_c, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_append_c
#endif
g_string_append_c:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1312 & 0xffff
#if 1312 > 0xffff
  movk ip0, 1312 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_append_len
  .p2align 4
  .type g_string_append_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_append_len
#endif
g_string_append_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1313 & 0xffff
#if 1313 > 0xffff
  movk ip0, 1313 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_append_printf
  .p2align 4
  .type g_string_append_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_append_printf
#endif
g_string_append_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1314 & 0xffff
#if 1314 > 0xffff
  movk ip0, 1314 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_append_unichar
  .p2align 4
  .type g_string_append_unichar, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_append_unichar
#endif
g_string_append_unichar:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1315 & 0xffff
#if 1315 > 0xffff
  movk ip0, 1315 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_append_uri_escaped
  .p2align 4
  .type g_string_append_uri_escaped, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_append_uri_escaped
#endif
g_string_append_uri_escaped:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1316 & 0xffff
#if 1316 > 0xffff
  movk ip0, 1316 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_append_vprintf
  .p2align 4
  .type g_string_append_vprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_append_vprintf
#endif
g_string_append_vprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1317 & 0xffff
#if 1317 > 0xffff
  movk ip0, 1317 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_ascii_down
  .p2align 4
  .type g_string_ascii_down, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_ascii_down
#endif
g_string_ascii_down:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1318 & 0xffff
#if 1318 > 0xffff
  movk ip0, 1318 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_ascii_up
  .p2align 4
  .type g_string_ascii_up, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_ascii_up
#endif
g_string_ascii_up:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1319 & 0xffff
#if 1319 > 0xffff
  movk ip0, 1319 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_assign
  .p2align 4
  .type g_string_assign, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_assign
#endif
g_string_assign:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1320 & 0xffff
#if 1320 > 0xffff
  movk ip0, 1320 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_chunk_clear
  .p2align 4
  .type g_string_chunk_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_chunk_clear
#endif
g_string_chunk_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1321 & 0xffff
#if 1321 > 0xffff
  movk ip0, 1321 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_chunk_free
  .p2align 4
  .type g_string_chunk_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_chunk_free
#endif
g_string_chunk_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1322 & 0xffff
#if 1322 > 0xffff
  movk ip0, 1322 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_chunk_insert
  .p2align 4
  .type g_string_chunk_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_chunk_insert
#endif
g_string_chunk_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1323 & 0xffff
#if 1323 > 0xffff
  movk ip0, 1323 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_chunk_insert_const
  .p2align 4
  .type g_string_chunk_insert_const, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_chunk_insert_const
#endif
g_string_chunk_insert_const:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1324 & 0xffff
#if 1324 > 0xffff
  movk ip0, 1324 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_chunk_insert_len
  .p2align 4
  .type g_string_chunk_insert_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_chunk_insert_len
#endif
g_string_chunk_insert_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1325 & 0xffff
#if 1325 > 0xffff
  movk ip0, 1325 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_chunk_new
  .p2align 4
  .type g_string_chunk_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_chunk_new
#endif
g_string_chunk_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1326 & 0xffff
#if 1326 > 0xffff
  movk ip0, 1326 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_down
  .p2align 4
  .type g_string_down, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_down
#endif
g_string_down:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1327 & 0xffff
#if 1327 > 0xffff
  movk ip0, 1327 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_equal
  .p2align 4
  .type g_string_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_equal
#endif
g_string_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1328 & 0xffff
#if 1328 > 0xffff
  movk ip0, 1328 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_erase
  .p2align 4
  .type g_string_erase, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_erase
#endif
g_string_erase:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1329 & 0xffff
#if 1329 > 0xffff
  movk ip0, 1329 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_free
  .p2align 4
  .type g_string_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_free
#endif
g_string_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1330 & 0xffff
#if 1330 > 0xffff
  movk ip0, 1330 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_free_and_steal
  .p2align 4
  .type g_string_free_and_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_free_and_steal
#endif
g_string_free_and_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1331 & 0xffff
#if 1331 > 0xffff
  movk ip0, 1331 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_free_to_bytes
  .p2align 4
  .type g_string_free_to_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_free_to_bytes
#endif
g_string_free_to_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1332 & 0xffff
#if 1332 > 0xffff
  movk ip0, 1332 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_hash
  .p2align 4
  .type g_string_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_hash
#endif
g_string_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1333 & 0xffff
#if 1333 > 0xffff
  movk ip0, 1333 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_insert
  .p2align 4
  .type g_string_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_insert
#endif
g_string_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1334 & 0xffff
#if 1334 > 0xffff
  movk ip0, 1334 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_insert_c
  .p2align 4
  .type g_string_insert_c, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_insert_c
#endif
g_string_insert_c:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1335 & 0xffff
#if 1335 > 0xffff
  movk ip0, 1335 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_insert_len
  .p2align 4
  .type g_string_insert_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_insert_len
#endif
g_string_insert_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1336 & 0xffff
#if 1336 > 0xffff
  movk ip0, 1336 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_insert_unichar
  .p2align 4
  .type g_string_insert_unichar, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_insert_unichar
#endif
g_string_insert_unichar:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1337 & 0xffff
#if 1337 > 0xffff
  movk ip0, 1337 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_new
  .p2align 4
  .type g_string_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_new
#endif
g_string_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1338 & 0xffff
#if 1338 > 0xffff
  movk ip0, 1338 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_new_len
  .p2align 4
  .type g_string_new_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_new_len
#endif
g_string_new_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1339 & 0xffff
#if 1339 > 0xffff
  movk ip0, 1339 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_new_take
  .p2align 4
  .type g_string_new_take, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_new_take
#endif
g_string_new_take:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1340 & 0xffff
#if 1340 > 0xffff
  movk ip0, 1340 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_overwrite
  .p2align 4
  .type g_string_overwrite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_overwrite
#endif
g_string_overwrite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1341 & 0xffff
#if 1341 > 0xffff
  movk ip0, 1341 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_overwrite_len
  .p2align 4
  .type g_string_overwrite_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_overwrite_len
#endif
g_string_overwrite_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1342 & 0xffff
#if 1342 > 0xffff
  movk ip0, 1342 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_prepend
  .p2align 4
  .type g_string_prepend, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_prepend
#endif
g_string_prepend:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1343 & 0xffff
#if 1343 > 0xffff
  movk ip0, 1343 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_prepend_c
  .p2align 4
  .type g_string_prepend_c, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_prepend_c
#endif
g_string_prepend_c:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1344 & 0xffff
#if 1344 > 0xffff
  movk ip0, 1344 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_prepend_len
  .p2align 4
  .type g_string_prepend_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_prepend_len
#endif
g_string_prepend_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1345 & 0xffff
#if 1345 > 0xffff
  movk ip0, 1345 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_prepend_unichar
  .p2align 4
  .type g_string_prepend_unichar, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_prepend_unichar
#endif
g_string_prepend_unichar:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1346 & 0xffff
#if 1346 > 0xffff
  movk ip0, 1346 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_printf
  .p2align 4
  .type g_string_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_printf
#endif
g_string_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1347 & 0xffff
#if 1347 > 0xffff
  movk ip0, 1347 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_replace
  .p2align 4
  .type g_string_replace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_replace
#endif
g_string_replace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1348 & 0xffff
#if 1348 > 0xffff
  movk ip0, 1348 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_set_size
  .p2align 4
  .type g_string_set_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_set_size
#endif
g_string_set_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1349 & 0xffff
#if 1349 > 0xffff
  movk ip0, 1349 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_sized_new
  .p2align 4
  .type g_string_sized_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_sized_new
#endif
g_string_sized_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1350 & 0xffff
#if 1350 > 0xffff
  movk ip0, 1350 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_truncate
  .p2align 4
  .type g_string_truncate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_truncate
#endif
g_string_truncate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1351 & 0xffff
#if 1351 > 0xffff
  movk ip0, 1351 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_up
  .p2align 4
  .type g_string_up, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_up
#endif
g_string_up:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1352 & 0xffff
#if 1352 > 0xffff
  movk ip0, 1352 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_string_vprintf
  .p2align 4
  .type g_string_vprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_string_vprintf
#endif
g_string_vprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1353 & 0xffff
#if 1353 > 0xffff
  movk ip0, 1353 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strip_context
  .p2align 4
  .type g_strip_context, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strip_context
#endif
g_strip_context:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1354 & 0xffff
#if 1354 > 0xffff
  movk ip0, 1354 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strjoin
  .p2align 4
  .type g_strjoin, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strjoin
#endif
g_strjoin:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1355 & 0xffff
#if 1355 > 0xffff
  movk ip0, 1355 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strjoinv
  .p2align 4
  .type g_strjoinv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strjoinv
#endif
g_strjoinv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1356 & 0xffff
#if 1356 > 0xffff
  movk ip0, 1356 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strlcat
  .p2align 4
  .type g_strlcat, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strlcat
#endif
g_strlcat:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1357 & 0xffff
#if 1357 > 0xffff
  movk ip0, 1357 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strlcpy
  .p2align 4
  .type g_strlcpy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strlcpy
#endif
g_strlcpy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1358 & 0xffff
#if 1358 > 0xffff
  movk ip0, 1358 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strncasecmp
  .p2align 4
  .type g_strncasecmp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strncasecmp
#endif
g_strncasecmp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1359 & 0xffff
#if 1359 > 0xffff
  movk ip0, 1359 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strndup
  .p2align 4
  .type g_strndup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strndup
#endif
g_strndup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1360 & 0xffff
#if 1360 > 0xffff
  movk ip0, 1360 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strnfill
  .p2align 4
  .type g_strnfill, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strnfill
#endif
g_strnfill:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1361 & 0xffff
#if 1361 > 0xffff
  movk ip0, 1361 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strreverse
  .p2align 4
  .type g_strreverse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strreverse
#endif
g_strreverse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1362 & 0xffff
#if 1362 > 0xffff
  movk ip0, 1362 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strrstr
  .p2align 4
  .type g_strrstr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strrstr
#endif
g_strrstr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1363 & 0xffff
#if 1363 > 0xffff
  movk ip0, 1363 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strrstr_len
  .p2align 4
  .type g_strrstr_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strrstr_len
#endif
g_strrstr_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1364 & 0xffff
#if 1364 > 0xffff
  movk ip0, 1364 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strsignal
  .p2align 4
  .type g_strsignal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strsignal
#endif
g_strsignal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1365 & 0xffff
#if 1365 > 0xffff
  movk ip0, 1365 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strsplit
  .p2align 4
  .type g_strsplit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strsplit
#endif
g_strsplit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1366 & 0xffff
#if 1366 > 0xffff
  movk ip0, 1366 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strsplit_set
  .p2align 4
  .type g_strsplit_set, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strsplit_set
#endif
g_strsplit_set:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1367 & 0xffff
#if 1367 > 0xffff
  movk ip0, 1367 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strstr_len
  .p2align 4
  .type g_strstr_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strstr_len
#endif
g_strstr_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1368 & 0xffff
#if 1368 > 0xffff
  movk ip0, 1368 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strtod
  .p2align 4
  .type g_strtod, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strtod
#endif
g_strtod:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1369 & 0xffff
#if 1369 > 0xffff
  movk ip0, 1369 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strup
  .p2align 4
  .type g_strup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strup
#endif
g_strup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1370 & 0xffff
#if 1370 > 0xffff
  movk ip0, 1370 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_add
  .p2align 4
  .type g_strv_builder_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_add
#endif
g_strv_builder_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1371 & 0xffff
#if 1371 > 0xffff
  movk ip0, 1371 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_add_many
  .p2align 4
  .type g_strv_builder_add_many, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_add_many
#endif
g_strv_builder_add_many:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1372 & 0xffff
#if 1372 > 0xffff
  movk ip0, 1372 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_addv
  .p2align 4
  .type g_strv_builder_addv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_addv
#endif
g_strv_builder_addv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1373 & 0xffff
#if 1373 > 0xffff
  movk ip0, 1373 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_end
  .p2align 4
  .type g_strv_builder_end, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_end
#endif
g_strv_builder_end:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+10992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+10992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1374 & 0xffff
#if 1374 > 0xffff
  movk ip0, 1374 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_new
  .p2align 4
  .type g_strv_builder_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_new
#endif
g_strv_builder_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1375 & 0xffff
#if 1375 > 0xffff
  movk ip0, 1375 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_ref
  .p2align 4
  .type g_strv_builder_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_ref
#endif
g_strv_builder_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1376 & 0xffff
#if 1376 > 0xffff
  movk ip0, 1376 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_take
  .p2align 4
  .type g_strv_builder_take, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_take
#endif
g_strv_builder_take:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1377 & 0xffff
#if 1377 > 0xffff
  movk ip0, 1377 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_unref
  .p2align 4
  .type g_strv_builder_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_unref
#endif
g_strv_builder_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1378 & 0xffff
#if 1378 > 0xffff
  movk ip0, 1378 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_builder_unref_to_strv
  .p2align 4
  .type g_strv_builder_unref_to_strv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_builder_unref_to_strv
#endif
g_strv_builder_unref_to_strv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1379 & 0xffff
#if 1379 > 0xffff
  movk ip0, 1379 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_contains
  .p2align 4
  .type g_strv_contains, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_contains
#endif
g_strv_contains:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1380 & 0xffff
#if 1380 > 0xffff
  movk ip0, 1380 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_equal
  .p2align 4
  .type g_strv_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_equal
#endif
g_strv_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1381 & 0xffff
#if 1381 > 0xffff
  movk ip0, 1381 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_strv_length
  .p2align 4
  .type g_strv_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_strv_length
#endif
g_strv_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1382 & 0xffff
#if 1382 > 0xffff
  movk ip0, 1382 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_add_data_func
  .p2align 4
  .type g_test_add_data_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_add_data_func
#endif
g_test_add_data_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1383 & 0xffff
#if 1383 > 0xffff
  movk ip0, 1383 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_add_data_func_full
  .p2align 4
  .type g_test_add_data_func_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_add_data_func_full
#endif
g_test_add_data_func_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1384 & 0xffff
#if 1384 > 0xffff
  movk ip0, 1384 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_add_func
  .p2align 4
  .type g_test_add_func, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_add_func
#endif
g_test_add_func:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1385 & 0xffff
#if 1385 > 0xffff
  movk ip0, 1385 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_add_vtable
  .p2align 4
  .type g_test_add_vtable, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_add_vtable
#endif
g_test_add_vtable:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1386 & 0xffff
#if 1386 > 0xffff
  movk ip0, 1386 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_assert_expected_messages_internal
  .p2align 4
  .type g_test_assert_expected_messages_internal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_assert_expected_messages_internal
#endif
g_test_assert_expected_messages_internal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1387 & 0xffff
#if 1387 > 0xffff
  movk ip0, 1387 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_bug
  .p2align 4
  .type g_test_bug, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_bug
#endif
g_test_bug:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1388 & 0xffff
#if 1388 > 0xffff
  movk ip0, 1388 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_bug_base
  .p2align 4
  .type g_test_bug_base, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_bug_base
#endif
g_test_bug_base:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1389 & 0xffff
#if 1389 > 0xffff
  movk ip0, 1389 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_build_filename
  .p2align 4
  .type g_test_build_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_build_filename
#endif
g_test_build_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1390 & 0xffff
#if 1390 > 0xffff
  movk ip0, 1390 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_case_free
  .p2align 4
  .type g_test_case_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_case_free
#endif
g_test_case_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1391 & 0xffff
#if 1391 > 0xffff
  movk ip0, 1391 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_create_case
  .p2align 4
  .type g_test_create_case, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_create_case
#endif
g_test_create_case:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1392 & 0xffff
#if 1392 > 0xffff
  movk ip0, 1392 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_create_suite
  .p2align 4
  .type g_test_create_suite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_create_suite
#endif
g_test_create_suite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1393 & 0xffff
#if 1393 > 0xffff
  movk ip0, 1393 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_disable_crash_reporting
  .p2align 4
  .type g_test_disable_crash_reporting, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_disable_crash_reporting
#endif
g_test_disable_crash_reporting:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1394 & 0xffff
#if 1394 > 0xffff
  movk ip0, 1394 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_expect_message
  .p2align 4
  .type g_test_expect_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_expect_message
#endif
g_test_expect_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1395 & 0xffff
#if 1395 > 0xffff
  movk ip0, 1395 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_fail
  .p2align 4
  .type g_test_fail, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_fail
#endif
g_test_fail:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1396 & 0xffff
#if 1396 > 0xffff
  movk ip0, 1396 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_fail_printf
  .p2align 4
  .type g_test_fail_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_fail_printf
#endif
g_test_fail_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1397 & 0xffff
#if 1397 > 0xffff
  movk ip0, 1397 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_failed
  .p2align 4
  .type g_test_failed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_failed
#endif
g_test_failed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1398 & 0xffff
#if 1398 > 0xffff
  movk ip0, 1398 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_get_dir
  .p2align 4
  .type g_test_get_dir, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_get_dir
#endif
g_test_get_dir:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1399 & 0xffff
#if 1399 > 0xffff
  movk ip0, 1399 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_get_filename
  .p2align 4
  .type g_test_get_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_get_filename
#endif
g_test_get_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1400 & 0xffff
#if 1400 > 0xffff
  movk ip0, 1400 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_get_path
  .p2align 4
  .type g_test_get_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_get_path
#endif
g_test_get_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1401 & 0xffff
#if 1401 > 0xffff
  movk ip0, 1401 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_get_root
  .p2align 4
  .type g_test_get_root, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_get_root
#endif
g_test_get_root:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1402 & 0xffff
#if 1402 > 0xffff
  movk ip0, 1402 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_incomplete
  .p2align 4
  .type g_test_incomplete, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_incomplete
#endif
g_test_incomplete:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1403 & 0xffff
#if 1403 > 0xffff
  movk ip0, 1403 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_incomplete_printf
  .p2align 4
  .type g_test_incomplete_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_incomplete_printf
#endif
g_test_incomplete_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1404 & 0xffff
#if 1404 > 0xffff
  movk ip0, 1404 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_init
  .p2align 4
  .type g_test_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_init
#endif
g_test_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1405 & 0xffff
#if 1405 > 0xffff
  movk ip0, 1405 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_log_buffer_free
  .p2align 4
  .type g_test_log_buffer_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_log_buffer_free
#endif
g_test_log_buffer_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1406 & 0xffff
#if 1406 > 0xffff
  movk ip0, 1406 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_log_buffer_new
  .p2align 4
  .type g_test_log_buffer_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_log_buffer_new
#endif
g_test_log_buffer_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1407 & 0xffff
#if 1407 > 0xffff
  movk ip0, 1407 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_log_buffer_pop
  .p2align 4
  .type g_test_log_buffer_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_log_buffer_pop
#endif
g_test_log_buffer_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1408 & 0xffff
#if 1408 > 0xffff
  movk ip0, 1408 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_log_buffer_push
  .p2align 4
  .type g_test_log_buffer_push, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_log_buffer_push
#endif
g_test_log_buffer_push:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1409 & 0xffff
#if 1409 > 0xffff
  movk ip0, 1409 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_log_msg_free
  .p2align 4
  .type g_test_log_msg_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_log_msg_free
#endif
g_test_log_msg_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1410 & 0xffff
#if 1410 > 0xffff
  movk ip0, 1410 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_log_set_fatal_handler
  .p2align 4
  .type g_test_log_set_fatal_handler, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_log_set_fatal_handler
#endif
g_test_log_set_fatal_handler:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1411 & 0xffff
#if 1411 > 0xffff
  movk ip0, 1411 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_log_type_name
  .p2align 4
  .type g_test_log_type_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_log_type_name
#endif
g_test_log_type_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1412 & 0xffff
#if 1412 > 0xffff
  movk ip0, 1412 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_maximized_result
  .p2align 4
  .type g_test_maximized_result, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_maximized_result
#endif
g_test_maximized_result:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1413 & 0xffff
#if 1413 > 0xffff
  movk ip0, 1413 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_message
  .p2align 4
  .type g_test_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_message
#endif
g_test_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1414 & 0xffff
#if 1414 > 0xffff
  movk ip0, 1414 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_minimized_result
  .p2align 4
  .type g_test_minimized_result, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_minimized_result
#endif
g_test_minimized_result:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1415 & 0xffff
#if 1415 > 0xffff
  movk ip0, 1415 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_queue_destroy
  .p2align 4
  .type g_test_queue_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_queue_destroy
#endif
g_test_queue_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1416 & 0xffff
#if 1416 > 0xffff
  movk ip0, 1416 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_queue_free
  .p2align 4
  .type g_test_queue_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_queue_free
#endif
g_test_queue_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1417 & 0xffff
#if 1417 > 0xffff
  movk ip0, 1417 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_rand_double
  .p2align 4
  .type g_test_rand_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_rand_double
#endif
g_test_rand_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1418 & 0xffff
#if 1418 > 0xffff
  movk ip0, 1418 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_rand_double_range
  .p2align 4
  .type g_test_rand_double_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_rand_double_range
#endif
g_test_rand_double_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1419 & 0xffff
#if 1419 > 0xffff
  movk ip0, 1419 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_rand_int
  .p2align 4
  .type g_test_rand_int, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_rand_int
#endif
g_test_rand_int:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1420 & 0xffff
#if 1420 > 0xffff
  movk ip0, 1420 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_rand_int_range
  .p2align 4
  .type g_test_rand_int_range, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_rand_int_range
#endif
g_test_rand_int_range:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1421 & 0xffff
#if 1421 > 0xffff
  movk ip0, 1421 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_run
  .p2align 4
  .type g_test_run, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_run
#endif
g_test_run:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1422 & 0xffff
#if 1422 > 0xffff
  movk ip0, 1422 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_run_suite
  .p2align 4
  .type g_test_run_suite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_run_suite
#endif
g_test_run_suite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1423 & 0xffff
#if 1423 > 0xffff
  movk ip0, 1423 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_set_nonfatal_assertions
  .p2align 4
  .type g_test_set_nonfatal_assertions, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_set_nonfatal_assertions
#endif
g_test_set_nonfatal_assertions:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1424 & 0xffff
#if 1424 > 0xffff
  movk ip0, 1424 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_skip
  .p2align 4
  .type g_test_skip, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_skip
#endif
g_test_skip:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1425 & 0xffff
#if 1425 > 0xffff
  movk ip0, 1425 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_skip_printf
  .p2align 4
  .type g_test_skip_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_skip_printf
#endif
g_test_skip_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1426 & 0xffff
#if 1426 > 0xffff
  movk ip0, 1426 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_subprocess
  .p2align 4
  .type g_test_subprocess, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_subprocess
#endif
g_test_subprocess:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1427 & 0xffff
#if 1427 > 0xffff
  movk ip0, 1427 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_suite_add
  .p2align 4
  .type g_test_suite_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_suite_add
#endif
g_test_suite_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1428 & 0xffff
#if 1428 > 0xffff
  movk ip0, 1428 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_suite_add_suite
  .p2align 4
  .type g_test_suite_add_suite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_suite_add_suite
#endif
g_test_suite_add_suite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1429 & 0xffff
#if 1429 > 0xffff
  movk ip0, 1429 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_suite_free
  .p2align 4
  .type g_test_suite_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_suite_free
#endif
g_test_suite_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1430 & 0xffff
#if 1430 > 0xffff
  movk ip0, 1430 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_summary
  .p2align 4
  .type g_test_summary, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_summary
#endif
g_test_summary:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1431 & 0xffff
#if 1431 > 0xffff
  movk ip0, 1431 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_timer_elapsed
  .p2align 4
  .type g_test_timer_elapsed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_timer_elapsed
#endif
g_test_timer_elapsed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1432 & 0xffff
#if 1432 > 0xffff
  movk ip0, 1432 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_timer_last
  .p2align 4
  .type g_test_timer_last, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_timer_last
#endif
g_test_timer_last:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1433 & 0xffff
#if 1433 > 0xffff
  movk ip0, 1433 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_timer_start
  .p2align 4
  .type g_test_timer_start, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_timer_start
#endif
g_test_timer_start:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1434 & 0xffff
#if 1434 > 0xffff
  movk ip0, 1434 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_trap_assertions
  .p2align 4
  .type g_test_trap_assertions, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_trap_assertions
#endif
g_test_trap_assertions:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1435 & 0xffff
#if 1435 > 0xffff
  movk ip0, 1435 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_trap_fork
  .p2align 4
  .type g_test_trap_fork, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_trap_fork
#endif
g_test_trap_fork:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1436 & 0xffff
#if 1436 > 0xffff
  movk ip0, 1436 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_trap_has_passed
  .p2align 4
  .type g_test_trap_has_passed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_trap_has_passed
#endif
g_test_trap_has_passed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1437 & 0xffff
#if 1437 > 0xffff
  movk ip0, 1437 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_trap_reached_timeout
  .p2align 4
  .type g_test_trap_reached_timeout, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_trap_reached_timeout
#endif
g_test_trap_reached_timeout:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1438 & 0xffff
#if 1438 > 0xffff
  movk ip0, 1438 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_trap_subprocess
  .p2align 4
  .type g_test_trap_subprocess, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_trap_subprocess
#endif
g_test_trap_subprocess:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1439 & 0xffff
#if 1439 > 0xffff
  movk ip0, 1439 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_test_trap_subprocess_with_envp
  .p2align 4
  .type g_test_trap_subprocess_with_envp, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_test_trap_subprocess_with_envp
#endif
g_test_trap_subprocess_with_envp:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1440 & 0xffff
#if 1440 > 0xffff
  movk ip0, 1440 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_create
  .p2align 4
  .type g_thread_create, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_create
#endif
g_thread_create:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1441 & 0xffff
#if 1441 > 0xffff
  movk ip0, 1441 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_create_full
  .p2align 4
  .type g_thread_create_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_create_full
#endif
g_thread_create_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1442 & 0xffff
#if 1442 > 0xffff
  movk ip0, 1442 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_error_quark
  .p2align 4
  .type g_thread_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_error_quark
#endif
g_thread_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1443 & 0xffff
#if 1443 > 0xffff
  movk ip0, 1443 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_exit
  .p2align 4
  .type g_thread_exit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_exit
#endif
g_thread_exit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1444 & 0xffff
#if 1444 > 0xffff
  movk ip0, 1444 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_foreach
  .p2align 4
  .type g_thread_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_foreach
#endif
g_thread_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1445 & 0xffff
#if 1445 > 0xffff
  movk ip0, 1445 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_get_initialized
  .p2align 4
  .type g_thread_get_initialized, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_get_initialized
#endif
g_thread_get_initialized:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1446 & 0xffff
#if 1446 > 0xffff
  movk ip0, 1446 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_get_name
  .p2align 4
  .type g_thread_get_name, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_get_name
#endif
g_thread_get_name:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1447 & 0xffff
#if 1447 > 0xffff
  movk ip0, 1447 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_init_glib
  .p2align 4
  .type g_thread_init_glib, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_init_glib
#endif
g_thread_init_glib:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1448 & 0xffff
#if 1448 > 0xffff
  movk ip0, 1448 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_join
  .p2align 4
  .type g_thread_join, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_join
#endif
g_thread_join:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1449 & 0xffff
#if 1449 > 0xffff
  movk ip0, 1449 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_new
  .p2align 4
  .type g_thread_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_new
#endif
g_thread_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1450 & 0xffff
#if 1450 > 0xffff
  movk ip0, 1450 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_free
  .p2align 4
  .type g_thread_pool_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_free
#endif
g_thread_pool_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1451 & 0xffff
#if 1451 > 0xffff
  movk ip0, 1451 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_get_max_idle_time
  .p2align 4
  .type g_thread_pool_get_max_idle_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_get_max_idle_time
#endif
g_thread_pool_get_max_idle_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1452 & 0xffff
#if 1452 > 0xffff
  movk ip0, 1452 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_get_max_threads
  .p2align 4
  .type g_thread_pool_get_max_threads, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_get_max_threads
#endif
g_thread_pool_get_max_threads:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1453 & 0xffff
#if 1453 > 0xffff
  movk ip0, 1453 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_get_max_unused_threads
  .p2align 4
  .type g_thread_pool_get_max_unused_threads, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_get_max_unused_threads
#endif
g_thread_pool_get_max_unused_threads:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1454 & 0xffff
#if 1454 > 0xffff
  movk ip0, 1454 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_get_num_threads
  .p2align 4
  .type g_thread_pool_get_num_threads, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_get_num_threads
#endif
g_thread_pool_get_num_threads:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1455 & 0xffff
#if 1455 > 0xffff
  movk ip0, 1455 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_get_num_unused_threads
  .p2align 4
  .type g_thread_pool_get_num_unused_threads, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_get_num_unused_threads
#endif
g_thread_pool_get_num_unused_threads:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1456 & 0xffff
#if 1456 > 0xffff
  movk ip0, 1456 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_move_to_front
  .p2align 4
  .type g_thread_pool_move_to_front, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_move_to_front
#endif
g_thread_pool_move_to_front:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1457 & 0xffff
#if 1457 > 0xffff
  movk ip0, 1457 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_new
  .p2align 4
  .type g_thread_pool_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_new
#endif
g_thread_pool_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1458 & 0xffff
#if 1458 > 0xffff
  movk ip0, 1458 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_new_full
  .p2align 4
  .type g_thread_pool_new_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_new_full
#endif
g_thread_pool_new_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1459 & 0xffff
#if 1459 > 0xffff
  movk ip0, 1459 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_push
  .p2align 4
  .type g_thread_pool_push, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_push
#endif
g_thread_pool_push:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1460 & 0xffff
#if 1460 > 0xffff
  movk ip0, 1460 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_set_max_idle_time
  .p2align 4
  .type g_thread_pool_set_max_idle_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_set_max_idle_time
#endif
g_thread_pool_set_max_idle_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1461 & 0xffff
#if 1461 > 0xffff
  movk ip0, 1461 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_set_max_threads
  .p2align 4
  .type g_thread_pool_set_max_threads, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_set_max_threads
#endif
g_thread_pool_set_max_threads:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1462 & 0xffff
#if 1462 > 0xffff
  movk ip0, 1462 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_set_max_unused_threads
  .p2align 4
  .type g_thread_pool_set_max_unused_threads, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_set_max_unused_threads
#endif
g_thread_pool_set_max_unused_threads:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1463 & 0xffff
#if 1463 > 0xffff
  movk ip0, 1463 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_set_sort_function
  .p2align 4
  .type g_thread_pool_set_sort_function, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_set_sort_function
#endif
g_thread_pool_set_sort_function:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1464 & 0xffff
#if 1464 > 0xffff
  movk ip0, 1464 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_stop_unused_threads
  .p2align 4
  .type g_thread_pool_stop_unused_threads, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_stop_unused_threads
#endif
g_thread_pool_stop_unused_threads:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1465 & 0xffff
#if 1465 > 0xffff
  movk ip0, 1465 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_pool_unprocessed
  .p2align 4
  .type g_thread_pool_unprocessed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_pool_unprocessed
#endif
g_thread_pool_unprocessed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1466 & 0xffff
#if 1466 > 0xffff
  movk ip0, 1466 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_ref
  .p2align 4
  .type g_thread_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_ref
#endif
g_thread_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1467 & 0xffff
#if 1467 > 0xffff
  movk ip0, 1467 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_self
  .p2align 4
  .type g_thread_self, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_self
#endif
g_thread_self:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1468 & 0xffff
#if 1468 > 0xffff
  movk ip0, 1468 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_set_priority
  .p2align 4
  .type g_thread_set_priority, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_set_priority
#endif
g_thread_set_priority:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1469 & 0xffff
#if 1469 > 0xffff
  movk ip0, 1469 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_try_new
  .p2align 4
  .type g_thread_try_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_try_new
#endif
g_thread_try_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1470 & 0xffff
#if 1470 > 0xffff
  movk ip0, 1470 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_unref
  .p2align 4
  .type g_thread_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_unref
#endif
g_thread_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1471 & 0xffff
#if 1471 > 0xffff
  movk ip0, 1471 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_thread_yield
  .p2align 4
  .type g_thread_yield, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_thread_yield
#endif
g_thread_yield:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1472 & 0xffff
#if 1472 > 0xffff
  movk ip0, 1472 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_val_add
  .p2align 4
  .type g_time_val_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_val_add
#endif
g_time_val_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1473 & 0xffff
#if 1473 > 0xffff
  movk ip0, 1473 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_val_from_iso8601
  .p2align 4
  .type g_time_val_from_iso8601, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_val_from_iso8601
#endif
g_time_val_from_iso8601:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1474 & 0xffff
#if 1474 > 0xffff
  movk ip0, 1474 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_val_to_iso8601
  .p2align 4
  .type g_time_val_to_iso8601, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_val_to_iso8601
#endif
g_time_val_to_iso8601:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1475 & 0xffff
#if 1475 > 0xffff
  movk ip0, 1475 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_adjust_time
  .p2align 4
  .type g_time_zone_adjust_time, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_adjust_time
#endif
g_time_zone_adjust_time:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1476 & 0xffff
#if 1476 > 0xffff
  movk ip0, 1476 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_find_interval
  .p2align 4
  .type g_time_zone_find_interval, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_find_interval
#endif
g_time_zone_find_interval:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1477 & 0xffff
#if 1477 > 0xffff
  movk ip0, 1477 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_get_abbreviation
  .p2align 4
  .type g_time_zone_get_abbreviation, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_get_abbreviation
#endif
g_time_zone_get_abbreviation:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1478 & 0xffff
#if 1478 > 0xffff
  movk ip0, 1478 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_get_identifier
  .p2align 4
  .type g_time_zone_get_identifier, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_get_identifier
#endif
g_time_zone_get_identifier:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1479 & 0xffff
#if 1479 > 0xffff
  movk ip0, 1479 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_get_offset
  .p2align 4
  .type g_time_zone_get_offset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_get_offset
#endif
g_time_zone_get_offset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1480 & 0xffff
#if 1480 > 0xffff
  movk ip0, 1480 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_is_dst
  .p2align 4
  .type g_time_zone_is_dst, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_is_dst
#endif
g_time_zone_is_dst:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1481 & 0xffff
#if 1481 > 0xffff
  movk ip0, 1481 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_new
  .p2align 4
  .type g_time_zone_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_new
#endif
g_time_zone_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1482 & 0xffff
#if 1482 > 0xffff
  movk ip0, 1482 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_new_identifier
  .p2align 4
  .type g_time_zone_new_identifier, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_new_identifier
#endif
g_time_zone_new_identifier:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1483 & 0xffff
#if 1483 > 0xffff
  movk ip0, 1483 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_new_local
  .p2align 4
  .type g_time_zone_new_local, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_new_local
#endif
g_time_zone_new_local:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1484 & 0xffff
#if 1484 > 0xffff
  movk ip0, 1484 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_new_offset
  .p2align 4
  .type g_time_zone_new_offset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_new_offset
#endif
g_time_zone_new_offset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1485 & 0xffff
#if 1485 > 0xffff
  movk ip0, 1485 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_new_utc
  .p2align 4
  .type g_time_zone_new_utc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_new_utc
#endif
g_time_zone_new_utc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1486 & 0xffff
#if 1486 > 0xffff
  movk ip0, 1486 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_ref
  .p2align 4
  .type g_time_zone_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_ref
#endif
g_time_zone_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1487 & 0xffff
#if 1487 > 0xffff
  movk ip0, 1487 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_time_zone_unref
  .p2align 4
  .type g_time_zone_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_time_zone_unref
#endif
g_time_zone_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1488 & 0xffff
#if 1488 > 0xffff
  movk ip0, 1488 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_add
  .p2align 4
  .type g_timeout_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_add
#endif
g_timeout_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1489 & 0xffff
#if 1489 > 0xffff
  movk ip0, 1489 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_add_full
  .p2align 4
  .type g_timeout_add_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_add_full
#endif
g_timeout_add_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1490 & 0xffff
#if 1490 > 0xffff
  movk ip0, 1490 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_add_once
  .p2align 4
  .type g_timeout_add_once, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_add_once
#endif
g_timeout_add_once:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1491 & 0xffff
#if 1491 > 0xffff
  movk ip0, 1491 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_add_seconds
  .p2align 4
  .type g_timeout_add_seconds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_add_seconds
#endif
g_timeout_add_seconds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1492 & 0xffff
#if 1492 > 0xffff
  movk ip0, 1492 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_add_seconds_full
  .p2align 4
  .type g_timeout_add_seconds_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_add_seconds_full
#endif
g_timeout_add_seconds_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1493 & 0xffff
#if 1493 > 0xffff
  movk ip0, 1493 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_add_seconds_once
  .p2align 4
  .type g_timeout_add_seconds_once, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_add_seconds_once
#endif
g_timeout_add_seconds_once:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1494 & 0xffff
#if 1494 > 0xffff
  movk ip0, 1494 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_source_new
  .p2align 4
  .type g_timeout_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_source_new
#endif
g_timeout_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1495 & 0xffff
#if 1495 > 0xffff
  movk ip0, 1495 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timeout_source_new_seconds
  .p2align 4
  .type g_timeout_source_new_seconds, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timeout_source_new_seconds
#endif
g_timeout_source_new_seconds:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1496 & 0xffff
#if 1496 > 0xffff
  movk ip0, 1496 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_continue
  .p2align 4
  .type g_timer_continue, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_continue
#endif
g_timer_continue:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1497 & 0xffff
#if 1497 > 0xffff
  movk ip0, 1497 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_destroy
  .p2align 4
  .type g_timer_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_destroy
#endif
g_timer_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1498 & 0xffff
#if 1498 > 0xffff
  movk ip0, 1498 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_elapsed
  .p2align 4
  .type g_timer_elapsed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_elapsed
#endif
g_timer_elapsed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+11992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+11992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1499 & 0xffff
#if 1499 > 0xffff
  movk ip0, 1499 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_is_active
  .p2align 4
  .type g_timer_is_active, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_is_active
#endif
g_timer_is_active:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1500 & 0xffff
#if 1500 > 0xffff
  movk ip0, 1500 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_new
  .p2align 4
  .type g_timer_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_new
#endif
g_timer_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1501 & 0xffff
#if 1501 > 0xffff
  movk ip0, 1501 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_reset
  .p2align 4
  .type g_timer_reset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_reset
#endif
g_timer_reset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1502 & 0xffff
#if 1502 > 0xffff
  movk ip0, 1502 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_start
  .p2align 4
  .type g_timer_start, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_start
#endif
g_timer_start:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1503 & 0xffff
#if 1503 > 0xffff
  movk ip0, 1503 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_timer_stop
  .p2align 4
  .type g_timer_stop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_timer_stop
#endif
g_timer_stop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1504 & 0xffff
#if 1504 > 0xffff
  movk ip0, 1504 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_trash_stack_height
  .p2align 4
  .type g_trash_stack_height, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_trash_stack_height
#endif
g_trash_stack_height:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1505 & 0xffff
#if 1505 > 0xffff
  movk ip0, 1505 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_trash_stack_peek
  .p2align 4
  .type g_trash_stack_peek, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_trash_stack_peek
#endif
g_trash_stack_peek:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1506 & 0xffff
#if 1506 > 0xffff
  movk ip0, 1506 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_trash_stack_pop
  .p2align 4
  .type g_trash_stack_pop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_trash_stack_pop
#endif
g_trash_stack_pop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1507 & 0xffff
#if 1507 > 0xffff
  movk ip0, 1507 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_trash_stack_push
  .p2align 4
  .type g_trash_stack_push, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_trash_stack_push
#endif
g_trash_stack_push:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1508 & 0xffff
#if 1508 > 0xffff
  movk ip0, 1508 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_destroy
  .p2align 4
  .type g_tree_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_destroy
#endif
g_tree_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1509 & 0xffff
#if 1509 > 0xffff
  movk ip0, 1509 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_foreach
  .p2align 4
  .type g_tree_foreach, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_foreach
#endif
g_tree_foreach:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1510 & 0xffff
#if 1510 > 0xffff
  movk ip0, 1510 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_foreach_node
  .p2align 4
  .type g_tree_foreach_node, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_foreach_node
#endif
g_tree_foreach_node:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1511 & 0xffff
#if 1511 > 0xffff
  movk ip0, 1511 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_height
  .p2align 4
  .type g_tree_height, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_height
#endif
g_tree_height:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1512 & 0xffff
#if 1512 > 0xffff
  movk ip0, 1512 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_insert
  .p2align 4
  .type g_tree_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_insert
#endif
g_tree_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1513 & 0xffff
#if 1513 > 0xffff
  movk ip0, 1513 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_insert_node
  .p2align 4
  .type g_tree_insert_node, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_insert_node
#endif
g_tree_insert_node:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1514 & 0xffff
#if 1514 > 0xffff
  movk ip0, 1514 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_lookup
  .p2align 4
  .type g_tree_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_lookup
#endif
g_tree_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1515 & 0xffff
#if 1515 > 0xffff
  movk ip0, 1515 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_lookup_extended
  .p2align 4
  .type g_tree_lookup_extended, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_lookup_extended
#endif
g_tree_lookup_extended:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1516 & 0xffff
#if 1516 > 0xffff
  movk ip0, 1516 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_lookup_node
  .p2align 4
  .type g_tree_lookup_node, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_lookup_node
#endif
g_tree_lookup_node:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1517 & 0xffff
#if 1517 > 0xffff
  movk ip0, 1517 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_lower_bound
  .p2align 4
  .type g_tree_lower_bound, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_lower_bound
#endif
g_tree_lower_bound:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1518 & 0xffff
#if 1518 > 0xffff
  movk ip0, 1518 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_new
  .p2align 4
  .type g_tree_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_new
#endif
g_tree_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1519 & 0xffff
#if 1519 > 0xffff
  movk ip0, 1519 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_new_full
  .p2align 4
  .type g_tree_new_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_new_full
#endif
g_tree_new_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1520 & 0xffff
#if 1520 > 0xffff
  movk ip0, 1520 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_new_with_data
  .p2align 4
  .type g_tree_new_with_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_new_with_data
#endif
g_tree_new_with_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1521 & 0xffff
#if 1521 > 0xffff
  movk ip0, 1521 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_nnodes
  .p2align 4
  .type g_tree_nnodes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_nnodes
#endif
g_tree_nnodes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1522 & 0xffff
#if 1522 > 0xffff
  movk ip0, 1522 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_node_first
  .p2align 4
  .type g_tree_node_first, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_node_first
#endif
g_tree_node_first:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1523 & 0xffff
#if 1523 > 0xffff
  movk ip0, 1523 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_node_key
  .p2align 4
  .type g_tree_node_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_node_key
#endif
g_tree_node_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1524 & 0xffff
#if 1524 > 0xffff
  movk ip0, 1524 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_node_last
  .p2align 4
  .type g_tree_node_last, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_node_last
#endif
g_tree_node_last:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1525 & 0xffff
#if 1525 > 0xffff
  movk ip0, 1525 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_node_next
  .p2align 4
  .type g_tree_node_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_node_next
#endif
g_tree_node_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1526 & 0xffff
#if 1526 > 0xffff
  movk ip0, 1526 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_node_previous
  .p2align 4
  .type g_tree_node_previous, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_node_previous
#endif
g_tree_node_previous:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1527 & 0xffff
#if 1527 > 0xffff
  movk ip0, 1527 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_node_value
  .p2align 4
  .type g_tree_node_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_node_value
#endif
g_tree_node_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1528 & 0xffff
#if 1528 > 0xffff
  movk ip0, 1528 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_ref
  .p2align 4
  .type g_tree_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_ref
#endif
g_tree_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1529 & 0xffff
#if 1529 > 0xffff
  movk ip0, 1529 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_remove
  .p2align 4
  .type g_tree_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_remove
#endif
g_tree_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1530 & 0xffff
#if 1530 > 0xffff
  movk ip0, 1530 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_remove_all
  .p2align 4
  .type g_tree_remove_all, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_remove_all
#endif
g_tree_remove_all:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1531 & 0xffff
#if 1531 > 0xffff
  movk ip0, 1531 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_replace
  .p2align 4
  .type g_tree_replace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_replace
#endif
g_tree_replace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1532 & 0xffff
#if 1532 > 0xffff
  movk ip0, 1532 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_replace_node
  .p2align 4
  .type g_tree_replace_node, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_replace_node
#endif
g_tree_replace_node:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1533 & 0xffff
#if 1533 > 0xffff
  movk ip0, 1533 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_search
  .p2align 4
  .type g_tree_search, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_search
#endif
g_tree_search:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1534 & 0xffff
#if 1534 > 0xffff
  movk ip0, 1534 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_search_node
  .p2align 4
  .type g_tree_search_node, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_search_node
#endif
g_tree_search_node:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1535 & 0xffff
#if 1535 > 0xffff
  movk ip0, 1535 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_steal
  .p2align 4
  .type g_tree_steal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_steal
#endif
g_tree_steal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1536 & 0xffff
#if 1536 > 0xffff
  movk ip0, 1536 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_traverse
  .p2align 4
  .type g_tree_traverse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_traverse
#endif
g_tree_traverse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1537 & 0xffff
#if 1537 > 0xffff
  movk ip0, 1537 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_unref
  .p2align 4
  .type g_tree_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_unref
#endif
g_tree_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1538 & 0xffff
#if 1538 > 0xffff
  movk ip0, 1538 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tree_upper_bound
  .p2align 4
  .type g_tree_upper_bound, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tree_upper_bound
#endif
g_tree_upper_bound:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1539 & 0xffff
#if 1539 > 0xffff
  movk ip0, 1539 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_try_malloc
  .p2align 4
  .type g_try_malloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_try_malloc
#endif
g_try_malloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1540 & 0xffff
#if 1540 > 0xffff
  movk ip0, 1540 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_try_malloc0
  .p2align 4
  .type g_try_malloc0, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_try_malloc0
#endif
g_try_malloc0:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1541 & 0xffff
#if 1541 > 0xffff
  movk ip0, 1541 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_try_malloc0_n
  .p2align 4
  .type g_try_malloc0_n, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_try_malloc0_n
#endif
g_try_malloc0_n:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1542 & 0xffff
#if 1542 > 0xffff
  movk ip0, 1542 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_try_malloc_n
  .p2align 4
  .type g_try_malloc_n, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_try_malloc_n
#endif
g_try_malloc_n:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1543 & 0xffff
#if 1543 > 0xffff
  movk ip0, 1543 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_try_realloc
  .p2align 4
  .type g_try_realloc, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_try_realloc
#endif
g_try_realloc:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1544 & 0xffff
#if 1544 > 0xffff
  movk ip0, 1544 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_try_realloc_n
  .p2align 4
  .type g_try_realloc_n, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_try_realloc_n
#endif
g_try_realloc_n:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1545 & 0xffff
#if 1545 > 0xffff
  movk ip0, 1545 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tuples_destroy
  .p2align 4
  .type g_tuples_destroy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tuples_destroy
#endif
g_tuples_destroy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1546 & 0xffff
#if 1546 > 0xffff
  movk ip0, 1546 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_tuples_index
  .p2align 4
  .type g_tuples_index, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_tuples_index
#endif
g_tuples_index:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1547 & 0xffff
#if 1547 > 0xffff
  movk ip0, 1547 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ucs4_to_utf16
  .p2align 4
  .type g_ucs4_to_utf16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ucs4_to_utf16
#endif
g_ucs4_to_utf16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1548 & 0xffff
#if 1548 > 0xffff
  movk ip0, 1548 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_ucs4_to_utf8
  .p2align 4
  .type g_ucs4_to_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_ucs4_to_utf8
#endif
g_ucs4_to_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1549 & 0xffff
#if 1549 > 0xffff
  movk ip0, 1549 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_break_type
  .p2align 4
  .type g_unichar_break_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_break_type
#endif
g_unichar_break_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1550 & 0xffff
#if 1550 > 0xffff
  movk ip0, 1550 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_combining_class
  .p2align 4
  .type g_unichar_combining_class, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_combining_class
#endif
g_unichar_combining_class:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1551 & 0xffff
#if 1551 > 0xffff
  movk ip0, 1551 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_compose
  .p2align 4
  .type g_unichar_compose, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_compose
#endif
g_unichar_compose:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1552 & 0xffff
#if 1552 > 0xffff
  movk ip0, 1552 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_decompose
  .p2align 4
  .type g_unichar_decompose, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_decompose
#endif
g_unichar_decompose:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1553 & 0xffff
#if 1553 > 0xffff
  movk ip0, 1553 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_digit_value
  .p2align 4
  .type g_unichar_digit_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_digit_value
#endif
g_unichar_digit_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1554 & 0xffff
#if 1554 > 0xffff
  movk ip0, 1554 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_fully_decompose
  .p2align 4
  .type g_unichar_fully_decompose, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_fully_decompose
#endif
g_unichar_fully_decompose:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1555 & 0xffff
#if 1555 > 0xffff
  movk ip0, 1555 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_get_mirror_char
  .p2align 4
  .type g_unichar_get_mirror_char, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_get_mirror_char
#endif
g_unichar_get_mirror_char:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1556 & 0xffff
#if 1556 > 0xffff
  movk ip0, 1556 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_get_script
  .p2align 4
  .type g_unichar_get_script, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_get_script
#endif
g_unichar_get_script:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1557 & 0xffff
#if 1557 > 0xffff
  movk ip0, 1557 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isalnum
  .p2align 4
  .type g_unichar_isalnum, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isalnum
#endif
g_unichar_isalnum:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1558 & 0xffff
#if 1558 > 0xffff
  movk ip0, 1558 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isalpha
  .p2align 4
  .type g_unichar_isalpha, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isalpha
#endif
g_unichar_isalpha:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1559 & 0xffff
#if 1559 > 0xffff
  movk ip0, 1559 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_iscntrl
  .p2align 4
  .type g_unichar_iscntrl, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_iscntrl
#endif
g_unichar_iscntrl:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1560 & 0xffff
#if 1560 > 0xffff
  movk ip0, 1560 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isdefined
  .p2align 4
  .type g_unichar_isdefined, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isdefined
#endif
g_unichar_isdefined:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1561 & 0xffff
#if 1561 > 0xffff
  movk ip0, 1561 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isdigit
  .p2align 4
  .type g_unichar_isdigit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isdigit
#endif
g_unichar_isdigit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1562 & 0xffff
#if 1562 > 0xffff
  movk ip0, 1562 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isgraph
  .p2align 4
  .type g_unichar_isgraph, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isgraph
#endif
g_unichar_isgraph:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1563 & 0xffff
#if 1563 > 0xffff
  movk ip0, 1563 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_islower
  .p2align 4
  .type g_unichar_islower, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_islower
#endif
g_unichar_islower:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1564 & 0xffff
#if 1564 > 0xffff
  movk ip0, 1564 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_ismark
  .p2align 4
  .type g_unichar_ismark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_ismark
#endif
g_unichar_ismark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1565 & 0xffff
#if 1565 > 0xffff
  movk ip0, 1565 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isprint
  .p2align 4
  .type g_unichar_isprint, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isprint
#endif
g_unichar_isprint:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1566 & 0xffff
#if 1566 > 0xffff
  movk ip0, 1566 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_ispunct
  .p2align 4
  .type g_unichar_ispunct, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_ispunct
#endif
g_unichar_ispunct:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1567 & 0xffff
#if 1567 > 0xffff
  movk ip0, 1567 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isspace
  .p2align 4
  .type g_unichar_isspace, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isspace
#endif
g_unichar_isspace:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1568 & 0xffff
#if 1568 > 0xffff
  movk ip0, 1568 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_istitle
  .p2align 4
  .type g_unichar_istitle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_istitle
#endif
g_unichar_istitle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1569 & 0xffff
#if 1569 > 0xffff
  movk ip0, 1569 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isupper
  .p2align 4
  .type g_unichar_isupper, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isupper
#endif
g_unichar_isupper:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1570 & 0xffff
#if 1570 > 0xffff
  movk ip0, 1570 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_iswide
  .p2align 4
  .type g_unichar_iswide, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_iswide
#endif
g_unichar_iswide:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1571 & 0xffff
#if 1571 > 0xffff
  movk ip0, 1571 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_iswide_cjk
  .p2align 4
  .type g_unichar_iswide_cjk, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_iswide_cjk
#endif
g_unichar_iswide_cjk:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1572 & 0xffff
#if 1572 > 0xffff
  movk ip0, 1572 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_isxdigit
  .p2align 4
  .type g_unichar_isxdigit, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_isxdigit
#endif
g_unichar_isxdigit:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1573 & 0xffff
#if 1573 > 0xffff
  movk ip0, 1573 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_iszerowidth
  .p2align 4
  .type g_unichar_iszerowidth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_iszerowidth
#endif
g_unichar_iszerowidth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1574 & 0xffff
#if 1574 > 0xffff
  movk ip0, 1574 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_to_utf8
  .p2align 4
  .type g_unichar_to_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_to_utf8
#endif
g_unichar_to_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1575 & 0xffff
#if 1575 > 0xffff
  movk ip0, 1575 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_tolower
  .p2align 4
  .type g_unichar_tolower, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_tolower
#endif
g_unichar_tolower:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1576 & 0xffff
#if 1576 > 0xffff
  movk ip0, 1576 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_totitle
  .p2align 4
  .type g_unichar_totitle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_totitle
#endif
g_unichar_totitle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1577 & 0xffff
#if 1577 > 0xffff
  movk ip0, 1577 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_toupper
  .p2align 4
  .type g_unichar_toupper, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_toupper
#endif
g_unichar_toupper:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1578 & 0xffff
#if 1578 > 0xffff
  movk ip0, 1578 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_type
  .p2align 4
  .type g_unichar_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_type
#endif
g_unichar_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1579 & 0xffff
#if 1579 > 0xffff
  movk ip0, 1579 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_validate
  .p2align 4
  .type g_unichar_validate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_validate
#endif
g_unichar_validate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1580 & 0xffff
#if 1580 > 0xffff
  movk ip0, 1580 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unichar_xdigit_value
  .p2align 4
  .type g_unichar_xdigit_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unichar_xdigit_value
#endif
g_unichar_xdigit_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1581 & 0xffff
#if 1581 > 0xffff
  movk ip0, 1581 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unicode_canonical_decomposition
  .p2align 4
  .type g_unicode_canonical_decomposition, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unicode_canonical_decomposition
#endif
g_unicode_canonical_decomposition:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1582 & 0xffff
#if 1582 > 0xffff
  movk ip0, 1582 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unicode_canonical_ordering
  .p2align 4
  .type g_unicode_canonical_ordering, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unicode_canonical_ordering
#endif
g_unicode_canonical_ordering:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1583 & 0xffff
#if 1583 > 0xffff
  movk ip0, 1583 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unicode_script_from_iso15924
  .p2align 4
  .type g_unicode_script_from_iso15924, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unicode_script_from_iso15924
#endif
g_unicode_script_from_iso15924:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1584 & 0xffff
#if 1584 > 0xffff
  movk ip0, 1584 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unicode_script_to_iso15924
  .p2align 4
  .type g_unicode_script_to_iso15924, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unicode_script_to_iso15924
#endif
g_unicode_script_to_iso15924:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1585 & 0xffff
#if 1585 > 0xffff
  movk ip0, 1585 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_error_quark
  .p2align 4
  .type g_unix_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_error_quark
#endif
g_unix_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1586 & 0xffff
#if 1586 > 0xffff
  movk ip0, 1586 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_add
  .p2align 4
  .type g_unix_fd_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_add
#endif
g_unix_fd_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1587 & 0xffff
#if 1587 > 0xffff
  movk ip0, 1587 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_add_full
  .p2align 4
  .type g_unix_fd_add_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_add_full
#endif
g_unix_fd_add_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1588 & 0xffff
#if 1588 > 0xffff
  movk ip0, 1588 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_fd_source_new
  .p2align 4
  .type g_unix_fd_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_fd_source_new
#endif
g_unix_fd_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1589 & 0xffff
#if 1589 > 0xffff
  movk ip0, 1589 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_get_passwd_entry
  .p2align 4
  .type g_unix_get_passwd_entry, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_get_passwd_entry
#endif
g_unix_get_passwd_entry:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1590 & 0xffff
#if 1590 > 0xffff
  movk ip0, 1590 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_open_pipe
  .p2align 4
  .type g_unix_open_pipe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_open_pipe
#endif
g_unix_open_pipe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1591 & 0xffff
#if 1591 > 0xffff
  movk ip0, 1591 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_set_fd_nonblocking
  .p2align 4
  .type g_unix_set_fd_nonblocking, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_set_fd_nonblocking
#endif
g_unix_set_fd_nonblocking:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1592 & 0xffff
#if 1592 > 0xffff
  movk ip0, 1592 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_signal_add
  .p2align 4
  .type g_unix_signal_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_signal_add
#endif
g_unix_signal_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1593 & 0xffff
#if 1593 > 0xffff
  movk ip0, 1593 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_signal_add_full
  .p2align 4
  .type g_unix_signal_add_full, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_signal_add_full
#endif
g_unix_signal_add_full:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1594 & 0xffff
#if 1594 > 0xffff
  movk ip0, 1594 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unix_signal_source_new
  .p2align 4
  .type g_unix_signal_source_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unix_signal_source_new
#endif
g_unix_signal_source_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1595 & 0xffff
#if 1595 > 0xffff
  movk ip0, 1595 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unlink
  .p2align 4
  .type g_unlink, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unlink
#endif
g_unlink:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1596 & 0xffff
#if 1596 > 0xffff
  movk ip0, 1596 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_unsetenv
  .p2align 4
  .type g_unsetenv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_unsetenv
#endif
g_unsetenv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1597 & 0xffff
#if 1597 > 0xffff
  movk ip0, 1597 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_build
  .p2align 4
  .type g_uri_build, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_build
#endif
g_uri_build:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1598 & 0xffff
#if 1598 > 0xffff
  movk ip0, 1598 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_build_with_user
  .p2align 4
  .type g_uri_build_with_user, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_build_with_user
#endif
g_uri_build_with_user:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1599 & 0xffff
#if 1599 > 0xffff
  movk ip0, 1599 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_error_quark
  .p2align 4
  .type g_uri_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_error_quark
#endif
g_uri_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1600 & 0xffff
#if 1600 > 0xffff
  movk ip0, 1600 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_escape_bytes
  .p2align 4
  .type g_uri_escape_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_escape_bytes
#endif
g_uri_escape_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1601 & 0xffff
#if 1601 > 0xffff
  movk ip0, 1601 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_escape_string
  .p2align 4
  .type g_uri_escape_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_escape_string
#endif
g_uri_escape_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1602 & 0xffff
#if 1602 > 0xffff
  movk ip0, 1602 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_auth_params
  .p2align 4
  .type g_uri_get_auth_params, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_auth_params
#endif
g_uri_get_auth_params:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1603 & 0xffff
#if 1603 > 0xffff
  movk ip0, 1603 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_flags
  .p2align 4
  .type g_uri_get_flags, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_flags
#endif
g_uri_get_flags:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1604 & 0xffff
#if 1604 > 0xffff
  movk ip0, 1604 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_fragment
  .p2align 4
  .type g_uri_get_fragment, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_fragment
#endif
g_uri_get_fragment:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1605 & 0xffff
#if 1605 > 0xffff
  movk ip0, 1605 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_host
  .p2align 4
  .type g_uri_get_host, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_host
#endif
g_uri_get_host:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1606 & 0xffff
#if 1606 > 0xffff
  movk ip0, 1606 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_password
  .p2align 4
  .type g_uri_get_password, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_password
#endif
g_uri_get_password:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1607 & 0xffff
#if 1607 > 0xffff
  movk ip0, 1607 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_path
  .p2align 4
  .type g_uri_get_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_path
#endif
g_uri_get_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1608 & 0xffff
#if 1608 > 0xffff
  movk ip0, 1608 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_port
  .p2align 4
  .type g_uri_get_port, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_port
#endif
g_uri_get_port:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1609 & 0xffff
#if 1609 > 0xffff
  movk ip0, 1609 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_query
  .p2align 4
  .type g_uri_get_query, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_query
#endif
g_uri_get_query:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1610 & 0xffff
#if 1610 > 0xffff
  movk ip0, 1610 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_scheme
  .p2align 4
  .type g_uri_get_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_scheme
#endif
g_uri_get_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1611 & 0xffff
#if 1611 > 0xffff
  movk ip0, 1611 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_user
  .p2align 4
  .type g_uri_get_user, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_user
#endif
g_uri_get_user:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1612 & 0xffff
#if 1612 > 0xffff
  movk ip0, 1612 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_get_userinfo
  .p2align 4
  .type g_uri_get_userinfo, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_get_userinfo
#endif
g_uri_get_userinfo:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1613 & 0xffff
#if 1613 > 0xffff
  movk ip0, 1613 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_is_valid
  .p2align 4
  .type g_uri_is_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_is_valid
#endif
g_uri_is_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1614 & 0xffff
#if 1614 > 0xffff
  movk ip0, 1614 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_join
  .p2align 4
  .type g_uri_join, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_join
#endif
g_uri_join:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1615 & 0xffff
#if 1615 > 0xffff
  movk ip0, 1615 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_join_with_user
  .p2align 4
  .type g_uri_join_with_user, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_join_with_user
#endif
g_uri_join_with_user:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1616 & 0xffff
#if 1616 > 0xffff
  movk ip0, 1616 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_list_extract_uris
  .p2align 4
  .type g_uri_list_extract_uris, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_list_extract_uris
#endif
g_uri_list_extract_uris:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1617 & 0xffff
#if 1617 > 0xffff
  movk ip0, 1617 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_params_iter_init
  .p2align 4
  .type g_uri_params_iter_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_params_iter_init
#endif
g_uri_params_iter_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1618 & 0xffff
#if 1618 > 0xffff
  movk ip0, 1618 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_params_iter_next
  .p2align 4
  .type g_uri_params_iter_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_params_iter_next
#endif
g_uri_params_iter_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1619 & 0xffff
#if 1619 > 0xffff
  movk ip0, 1619 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_parse
  .p2align 4
  .type g_uri_parse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_parse
#endif
g_uri_parse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1620 & 0xffff
#if 1620 > 0xffff
  movk ip0, 1620 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_parse_params
  .p2align 4
  .type g_uri_parse_params, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_parse_params
#endif
g_uri_parse_params:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1621 & 0xffff
#if 1621 > 0xffff
  movk ip0, 1621 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_parse_relative
  .p2align 4
  .type g_uri_parse_relative, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_parse_relative
#endif
g_uri_parse_relative:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1622 & 0xffff
#if 1622 > 0xffff
  movk ip0, 1622 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_parse_scheme
  .p2align 4
  .type g_uri_parse_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_parse_scheme
#endif
g_uri_parse_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1623 & 0xffff
#if 1623 > 0xffff
  movk ip0, 1623 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_peek_scheme
  .p2align 4
  .type g_uri_peek_scheme, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_peek_scheme
#endif
g_uri_peek_scheme:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+12992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+12992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1624 & 0xffff
#if 1624 > 0xffff
  movk ip0, 1624 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_ref
  .p2align 4
  .type g_uri_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_ref
#endif
g_uri_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1625 & 0xffff
#if 1625 > 0xffff
  movk ip0, 1625 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_resolve_relative
  .p2align 4
  .type g_uri_resolve_relative, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_resolve_relative
#endif
g_uri_resolve_relative:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1626 & 0xffff
#if 1626 > 0xffff
  movk ip0, 1626 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_split
  .p2align 4
  .type g_uri_split, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_split
#endif
g_uri_split:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1627 & 0xffff
#if 1627 > 0xffff
  movk ip0, 1627 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_split_network
  .p2align 4
  .type g_uri_split_network, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_split_network
#endif
g_uri_split_network:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1628 & 0xffff
#if 1628 > 0xffff
  movk ip0, 1628 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_split_with_user
  .p2align 4
  .type g_uri_split_with_user, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_split_with_user
#endif
g_uri_split_with_user:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1629 & 0xffff
#if 1629 > 0xffff
  movk ip0, 1629 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_to_string
  .p2align 4
  .type g_uri_to_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_to_string
#endif
g_uri_to_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1630 & 0xffff
#if 1630 > 0xffff
  movk ip0, 1630 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_to_string_partial
  .p2align 4
  .type g_uri_to_string_partial, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_to_string_partial
#endif
g_uri_to_string_partial:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1631 & 0xffff
#if 1631 > 0xffff
  movk ip0, 1631 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_unescape_bytes
  .p2align 4
  .type g_uri_unescape_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_unescape_bytes
#endif
g_uri_unescape_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1632 & 0xffff
#if 1632 > 0xffff
  movk ip0, 1632 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_unescape_segment
  .p2align 4
  .type g_uri_unescape_segment, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_unescape_segment
#endif
g_uri_unescape_segment:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1633 & 0xffff
#if 1633 > 0xffff
  movk ip0, 1633 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_unescape_string
  .p2align 4
  .type g_uri_unescape_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_unescape_string
#endif
g_uri_unescape_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1634 & 0xffff
#if 1634 > 0xffff
  movk ip0, 1634 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uri_unref
  .p2align 4
  .type g_uri_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uri_unref
#endif
g_uri_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1635 & 0xffff
#if 1635 > 0xffff
  movk ip0, 1635 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_usleep
  .p2align 4
  .type g_usleep, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_usleep
#endif
g_usleep:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1636 & 0xffff
#if 1636 > 0xffff
  movk ip0, 1636 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf16_to_ucs4
  .p2align 4
  .type g_utf16_to_ucs4, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf16_to_ucs4
#endif
g_utf16_to_ucs4:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1637 & 0xffff
#if 1637 > 0xffff
  movk ip0, 1637 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf16_to_utf8
  .p2align 4
  .type g_utf16_to_utf8, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf16_to_utf8
#endif
g_utf16_to_utf8:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1638 & 0xffff
#if 1638 > 0xffff
  movk ip0, 1638 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_casefold
  .p2align 4
  .type g_utf8_casefold, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_casefold
#endif
g_utf8_casefold:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1639 & 0xffff
#if 1639 > 0xffff
  movk ip0, 1639 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_collate
  .p2align 4
  .type g_utf8_collate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_collate
#endif
g_utf8_collate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1640 & 0xffff
#if 1640 > 0xffff
  movk ip0, 1640 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_collate_key
  .p2align 4
  .type g_utf8_collate_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_collate_key
#endif
g_utf8_collate_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1641 & 0xffff
#if 1641 > 0xffff
  movk ip0, 1641 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_collate_key_for_filename
  .p2align 4
  .type g_utf8_collate_key_for_filename, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_collate_key_for_filename
#endif
g_utf8_collate_key_for_filename:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1642 & 0xffff
#if 1642 > 0xffff
  movk ip0, 1642 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_find_next_char
  .p2align 4
  .type g_utf8_find_next_char, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_find_next_char
#endif
g_utf8_find_next_char:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1643 & 0xffff
#if 1643 > 0xffff
  movk ip0, 1643 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_find_prev_char
  .p2align 4
  .type g_utf8_find_prev_char, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_find_prev_char
#endif
g_utf8_find_prev_char:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1644 & 0xffff
#if 1644 > 0xffff
  movk ip0, 1644 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_get_char
  .p2align 4
  .type g_utf8_get_char, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_get_char
#endif
g_utf8_get_char:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1645 & 0xffff
#if 1645 > 0xffff
  movk ip0, 1645 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_get_char_validated
  .p2align 4
  .type g_utf8_get_char_validated, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_get_char_validated
#endif
g_utf8_get_char_validated:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1646 & 0xffff
#if 1646 > 0xffff
  movk ip0, 1646 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_make_valid
  .p2align 4
  .type g_utf8_make_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_make_valid
#endif
g_utf8_make_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1647 & 0xffff
#if 1647 > 0xffff
  movk ip0, 1647 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_normalize
  .p2align 4
  .type g_utf8_normalize, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_normalize
#endif
g_utf8_normalize:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1648 & 0xffff
#if 1648 > 0xffff
  movk ip0, 1648 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_offset_to_pointer
  .p2align 4
  .type g_utf8_offset_to_pointer, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_offset_to_pointer
#endif
g_utf8_offset_to_pointer:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1649 & 0xffff
#if 1649 > 0xffff
  movk ip0, 1649 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_pointer_to_offset
  .p2align 4
  .type g_utf8_pointer_to_offset, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_pointer_to_offset
#endif
g_utf8_pointer_to_offset:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1650 & 0xffff
#if 1650 > 0xffff
  movk ip0, 1650 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_prev_char
  .p2align 4
  .type g_utf8_prev_char, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_prev_char
#endif
g_utf8_prev_char:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1651 & 0xffff
#if 1651 > 0xffff
  movk ip0, 1651 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_strchr
  .p2align 4
  .type g_utf8_strchr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_strchr
#endif
g_utf8_strchr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1652 & 0xffff
#if 1652 > 0xffff
  movk ip0, 1652 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_strdown
  .p2align 4
  .type g_utf8_strdown, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_strdown
#endif
g_utf8_strdown:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1653 & 0xffff
#if 1653 > 0xffff
  movk ip0, 1653 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_strlen
  .p2align 4
  .type g_utf8_strlen, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_strlen
#endif
g_utf8_strlen:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1654 & 0xffff
#if 1654 > 0xffff
  movk ip0, 1654 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_strncpy
  .p2align 4
  .type g_utf8_strncpy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_strncpy
#endif
g_utf8_strncpy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1655 & 0xffff
#if 1655 > 0xffff
  movk ip0, 1655 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_strrchr
  .p2align 4
  .type g_utf8_strrchr, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_strrchr
#endif
g_utf8_strrchr:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1656 & 0xffff
#if 1656 > 0xffff
  movk ip0, 1656 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_strreverse
  .p2align 4
  .type g_utf8_strreverse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_strreverse
#endif
g_utf8_strreverse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1657 & 0xffff
#if 1657 > 0xffff
  movk ip0, 1657 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_strup
  .p2align 4
  .type g_utf8_strup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_strup
#endif
g_utf8_strup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1658 & 0xffff
#if 1658 > 0xffff
  movk ip0, 1658 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_substring
  .p2align 4
  .type g_utf8_substring, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_substring
#endif
g_utf8_substring:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1659 & 0xffff
#if 1659 > 0xffff
  movk ip0, 1659 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_to_ucs4
  .p2align 4
  .type g_utf8_to_ucs4, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_to_ucs4
#endif
g_utf8_to_ucs4:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1660 & 0xffff
#if 1660 > 0xffff
  movk ip0, 1660 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_to_ucs4_fast
  .p2align 4
  .type g_utf8_to_ucs4_fast, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_to_ucs4_fast
#endif
g_utf8_to_ucs4_fast:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1661 & 0xffff
#if 1661 > 0xffff
  movk ip0, 1661 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_to_utf16
  .p2align 4
  .type g_utf8_to_utf16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_to_utf16
#endif
g_utf8_to_utf16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1662 & 0xffff
#if 1662 > 0xffff
  movk ip0, 1662 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_truncate_middle
  .p2align 4
  .type g_utf8_truncate_middle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_truncate_middle
#endif
g_utf8_truncate_middle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1663 & 0xffff
#if 1663 > 0xffff
  movk ip0, 1663 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_validate
  .p2align 4
  .type g_utf8_validate, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_validate
#endif
g_utf8_validate:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1664 & 0xffff
#if 1664 > 0xffff
  movk ip0, 1664 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utf8_validate_len
  .p2align 4
  .type g_utf8_validate_len, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utf8_validate_len
#endif
g_utf8_validate_len:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1665 & 0xffff
#if 1665 > 0xffff
  movk ip0, 1665 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_utime
  .p2align 4
  .type g_utime, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_utime
#endif
g_utime:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1666 & 0xffff
#if 1666 > 0xffff
  movk ip0, 1666 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uuid_string_is_valid
  .p2align 4
  .type g_uuid_string_is_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uuid_string_is_valid
#endif
g_uuid_string_is_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1667 & 0xffff
#if 1667 > 0xffff
  movk ip0, 1667 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_uuid_string_random
  .p2align 4
  .type g_uuid_string_random, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_uuid_string_random
#endif
g_uuid_string_random:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1668 & 0xffff
#if 1668 > 0xffff
  movk ip0, 1668 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_add
  .p2align 4
  .type g_variant_builder_add, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_add
#endif
g_variant_builder_add:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1669 & 0xffff
#if 1669 > 0xffff
  movk ip0, 1669 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_add_parsed
  .p2align 4
  .type g_variant_builder_add_parsed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_add_parsed
#endif
g_variant_builder_add_parsed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1670 & 0xffff
#if 1670 > 0xffff
  movk ip0, 1670 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_add_value
  .p2align 4
  .type g_variant_builder_add_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_add_value
#endif
g_variant_builder_add_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1671 & 0xffff
#if 1671 > 0xffff
  movk ip0, 1671 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_clear
  .p2align 4
  .type g_variant_builder_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_clear
#endif
g_variant_builder_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1672 & 0xffff
#if 1672 > 0xffff
  movk ip0, 1672 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_close
  .p2align 4
  .type g_variant_builder_close, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_close
#endif
g_variant_builder_close:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1673 & 0xffff
#if 1673 > 0xffff
  movk ip0, 1673 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_end
  .p2align 4
  .type g_variant_builder_end, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_end
#endif
g_variant_builder_end:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1674 & 0xffff
#if 1674 > 0xffff
  movk ip0, 1674 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_init
  .p2align 4
  .type g_variant_builder_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_init
#endif
g_variant_builder_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1675 & 0xffff
#if 1675 > 0xffff
  movk ip0, 1675 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_init_static
  .p2align 4
  .type g_variant_builder_init_static, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_init_static
#endif
g_variant_builder_init_static:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1676 & 0xffff
#if 1676 > 0xffff
  movk ip0, 1676 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_new
  .p2align 4
  .type g_variant_builder_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_new
#endif
g_variant_builder_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1677 & 0xffff
#if 1677 > 0xffff
  movk ip0, 1677 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_open
  .p2align 4
  .type g_variant_builder_open, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_open
#endif
g_variant_builder_open:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1678 & 0xffff
#if 1678 > 0xffff
  movk ip0, 1678 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_ref
  .p2align 4
  .type g_variant_builder_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_ref
#endif
g_variant_builder_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1679 & 0xffff
#if 1679 > 0xffff
  movk ip0, 1679 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_builder_unref
  .p2align 4
  .type g_variant_builder_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_builder_unref
#endif
g_variant_builder_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1680 & 0xffff
#if 1680 > 0xffff
  movk ip0, 1680 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_byteswap
  .p2align 4
  .type g_variant_byteswap, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_byteswap
#endif
g_variant_byteswap:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1681 & 0xffff
#if 1681 > 0xffff
  movk ip0, 1681 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_check_format_string
  .p2align 4
  .type g_variant_check_format_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_check_format_string
#endif
g_variant_check_format_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1682 & 0xffff
#if 1682 > 0xffff
  movk ip0, 1682 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_classify
  .p2align 4
  .type g_variant_classify, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_classify
#endif
g_variant_classify:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1683 & 0xffff
#if 1683 > 0xffff
  movk ip0, 1683 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_compare
  .p2align 4
  .type g_variant_compare, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_compare
#endif
g_variant_compare:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1684 & 0xffff
#if 1684 > 0xffff
  movk ip0, 1684 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_clear
  .p2align 4
  .type g_variant_dict_clear, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_clear
#endif
g_variant_dict_clear:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1685 & 0xffff
#if 1685 > 0xffff
  movk ip0, 1685 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_contains
  .p2align 4
  .type g_variant_dict_contains, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_contains
#endif
g_variant_dict_contains:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1686 & 0xffff
#if 1686 > 0xffff
  movk ip0, 1686 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_end
  .p2align 4
  .type g_variant_dict_end, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_end
#endif
g_variant_dict_end:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1687 & 0xffff
#if 1687 > 0xffff
  movk ip0, 1687 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_init
  .p2align 4
  .type g_variant_dict_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_init
#endif
g_variant_dict_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1688 & 0xffff
#if 1688 > 0xffff
  movk ip0, 1688 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_insert
  .p2align 4
  .type g_variant_dict_insert, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_insert
#endif
g_variant_dict_insert:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1689 & 0xffff
#if 1689 > 0xffff
  movk ip0, 1689 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_insert_value
  .p2align 4
  .type g_variant_dict_insert_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_insert_value
#endif
g_variant_dict_insert_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1690 & 0xffff
#if 1690 > 0xffff
  movk ip0, 1690 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_lookup
  .p2align 4
  .type g_variant_dict_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_lookup
#endif
g_variant_dict_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1691 & 0xffff
#if 1691 > 0xffff
  movk ip0, 1691 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_lookup_value
  .p2align 4
  .type g_variant_dict_lookup_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_lookup_value
#endif
g_variant_dict_lookup_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1692 & 0xffff
#if 1692 > 0xffff
  movk ip0, 1692 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_new
  .p2align 4
  .type g_variant_dict_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_new
#endif
g_variant_dict_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1693 & 0xffff
#if 1693 > 0xffff
  movk ip0, 1693 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_ref
  .p2align 4
  .type g_variant_dict_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_ref
#endif
g_variant_dict_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1694 & 0xffff
#if 1694 > 0xffff
  movk ip0, 1694 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_remove
  .p2align 4
  .type g_variant_dict_remove, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_remove
#endif
g_variant_dict_remove:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1695 & 0xffff
#if 1695 > 0xffff
  movk ip0, 1695 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dict_unref
  .p2align 4
  .type g_variant_dict_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dict_unref
#endif
g_variant_dict_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1696 & 0xffff
#if 1696 > 0xffff
  movk ip0, 1696 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dup_bytestring
  .p2align 4
  .type g_variant_dup_bytestring, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dup_bytestring
#endif
g_variant_dup_bytestring:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1697 & 0xffff
#if 1697 > 0xffff
  movk ip0, 1697 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dup_bytestring_array
  .p2align 4
  .type g_variant_dup_bytestring_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dup_bytestring_array
#endif
g_variant_dup_bytestring_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1698 & 0xffff
#if 1698 > 0xffff
  movk ip0, 1698 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dup_objv
  .p2align 4
  .type g_variant_dup_objv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dup_objv
#endif
g_variant_dup_objv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1699 & 0xffff
#if 1699 > 0xffff
  movk ip0, 1699 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dup_string
  .p2align 4
  .type g_variant_dup_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dup_string
#endif
g_variant_dup_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1700 & 0xffff
#if 1700 > 0xffff
  movk ip0, 1700 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_dup_strv
  .p2align 4
  .type g_variant_dup_strv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_dup_strv
#endif
g_variant_dup_strv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1701 & 0xffff
#if 1701 > 0xffff
  movk ip0, 1701 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_equal
  .p2align 4
  .type g_variant_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_equal
#endif
g_variant_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1702 & 0xffff
#if 1702 > 0xffff
  movk ip0, 1702 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_format_string_scan
  .p2align 4
  .type g_variant_format_string_scan, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_format_string_scan
#endif
g_variant_format_string_scan:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1703 & 0xffff
#if 1703 > 0xffff
  movk ip0, 1703 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_format_string_scan_type
  .p2align 4
  .type g_variant_format_string_scan_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_format_string_scan_type
#endif
g_variant_format_string_scan_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1704 & 0xffff
#if 1704 > 0xffff
  movk ip0, 1704 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get
  .p2align 4
  .type g_variant_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get
#endif
g_variant_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1705 & 0xffff
#if 1705 > 0xffff
  movk ip0, 1705 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_boolean
  .p2align 4
  .type g_variant_get_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_boolean
#endif
g_variant_get_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1706 & 0xffff
#if 1706 > 0xffff
  movk ip0, 1706 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_byte
  .p2align 4
  .type g_variant_get_byte, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_byte
#endif
g_variant_get_byte:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1707 & 0xffff
#if 1707 > 0xffff
  movk ip0, 1707 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_bytestring
  .p2align 4
  .type g_variant_get_bytestring, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_bytestring
#endif
g_variant_get_bytestring:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1708 & 0xffff
#if 1708 > 0xffff
  movk ip0, 1708 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_bytestring_array
  .p2align 4
  .type g_variant_get_bytestring_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_bytestring_array
#endif
g_variant_get_bytestring_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1709 & 0xffff
#if 1709 > 0xffff
  movk ip0, 1709 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_child
  .p2align 4
  .type g_variant_get_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_child
#endif
g_variant_get_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1710 & 0xffff
#if 1710 > 0xffff
  movk ip0, 1710 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_child_value
  .p2align 4
  .type g_variant_get_child_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_child_value
#endif
g_variant_get_child_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1711 & 0xffff
#if 1711 > 0xffff
  movk ip0, 1711 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_data
  .p2align 4
  .type g_variant_get_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_data
#endif
g_variant_get_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1712 & 0xffff
#if 1712 > 0xffff
  movk ip0, 1712 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_data_as_bytes
  .p2align 4
  .type g_variant_get_data_as_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_data_as_bytes
#endif
g_variant_get_data_as_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1713 & 0xffff
#if 1713 > 0xffff
  movk ip0, 1713 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_double
  .p2align 4
  .type g_variant_get_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_double
#endif
g_variant_get_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1714 & 0xffff
#if 1714 > 0xffff
  movk ip0, 1714 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_fixed_array
  .p2align 4
  .type g_variant_get_fixed_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_fixed_array
#endif
g_variant_get_fixed_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1715 & 0xffff
#if 1715 > 0xffff
  movk ip0, 1715 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_handle
  .p2align 4
  .type g_variant_get_handle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_handle
#endif
g_variant_get_handle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1716 & 0xffff
#if 1716 > 0xffff
  movk ip0, 1716 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_int16
  .p2align 4
  .type g_variant_get_int16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_int16
#endif
g_variant_get_int16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1717 & 0xffff
#if 1717 > 0xffff
  movk ip0, 1717 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_int32
  .p2align 4
  .type g_variant_get_int32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_int32
#endif
g_variant_get_int32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1718 & 0xffff
#if 1718 > 0xffff
  movk ip0, 1718 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_int64
  .p2align 4
  .type g_variant_get_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_int64
#endif
g_variant_get_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1719 & 0xffff
#if 1719 > 0xffff
  movk ip0, 1719 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_maybe
  .p2align 4
  .type g_variant_get_maybe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_maybe
#endif
g_variant_get_maybe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1720 & 0xffff
#if 1720 > 0xffff
  movk ip0, 1720 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_normal_form
  .p2align 4
  .type g_variant_get_normal_form, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_normal_form
#endif
g_variant_get_normal_form:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1721 & 0xffff
#if 1721 > 0xffff
  movk ip0, 1721 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_objv
  .p2align 4
  .type g_variant_get_objv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_objv
#endif
g_variant_get_objv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1722 & 0xffff
#if 1722 > 0xffff
  movk ip0, 1722 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_size
  .p2align 4
  .type g_variant_get_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_size
#endif
g_variant_get_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1723 & 0xffff
#if 1723 > 0xffff
  movk ip0, 1723 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_string
  .p2align 4
  .type g_variant_get_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_string
#endif
g_variant_get_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1724 & 0xffff
#if 1724 > 0xffff
  movk ip0, 1724 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_strv
  .p2align 4
  .type g_variant_get_strv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_strv
#endif
g_variant_get_strv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1725 & 0xffff
#if 1725 > 0xffff
  movk ip0, 1725 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_type
  .p2align 4
  .type g_variant_get_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_type
#endif
g_variant_get_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1726 & 0xffff
#if 1726 > 0xffff
  movk ip0, 1726 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_type_string
  .p2align 4
  .type g_variant_get_type_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_type_string
#endif
g_variant_get_type_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1727 & 0xffff
#if 1727 > 0xffff
  movk ip0, 1727 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_uint16
  .p2align 4
  .type g_variant_get_uint16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_uint16
#endif
g_variant_get_uint16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1728 & 0xffff
#if 1728 > 0xffff
  movk ip0, 1728 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_uint32
  .p2align 4
  .type g_variant_get_uint32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_uint32
#endif
g_variant_get_uint32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1729 & 0xffff
#if 1729 > 0xffff
  movk ip0, 1729 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_uint64
  .p2align 4
  .type g_variant_get_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_uint64
#endif
g_variant_get_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13840
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13840]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1730 & 0xffff
#if 1730 > 0xffff
  movk ip0, 1730 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_va
  .p2align 4
  .type g_variant_get_va, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_va
#endif
g_variant_get_va:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13848
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13848]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1731 & 0xffff
#if 1731 > 0xffff
  movk ip0, 1731 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_get_variant
  .p2align 4
  .type g_variant_get_variant, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_get_variant
#endif
g_variant_get_variant:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13856
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13856]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1732 & 0xffff
#if 1732 > 0xffff
  movk ip0, 1732 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_hash
  .p2align 4
  .type g_variant_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_hash
#endif
g_variant_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13864
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13864]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1733 & 0xffff
#if 1733 > 0xffff
  movk ip0, 1733 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_is_container
  .p2align 4
  .type g_variant_is_container, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_is_container
#endif
g_variant_is_container:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13872
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13872]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1734 & 0xffff
#if 1734 > 0xffff
  movk ip0, 1734 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_is_floating
  .p2align 4
  .type g_variant_is_floating, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_is_floating
#endif
g_variant_is_floating:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13880
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13880]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1735 & 0xffff
#if 1735 > 0xffff
  movk ip0, 1735 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_is_normal_form
  .p2align 4
  .type g_variant_is_normal_form, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_is_normal_form
#endif
g_variant_is_normal_form:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13888
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13888]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1736 & 0xffff
#if 1736 > 0xffff
  movk ip0, 1736 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_is_object_path
  .p2align 4
  .type g_variant_is_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_is_object_path
#endif
g_variant_is_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13896
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13896]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1737 & 0xffff
#if 1737 > 0xffff
  movk ip0, 1737 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_is_of_type
  .p2align 4
  .type g_variant_is_of_type, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_is_of_type
#endif
g_variant_is_of_type:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13904
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13904]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1738 & 0xffff
#if 1738 > 0xffff
  movk ip0, 1738 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_is_signature
  .p2align 4
  .type g_variant_is_signature, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_is_signature
#endif
g_variant_is_signature:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13912
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13912]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1739 & 0xffff
#if 1739 > 0xffff
  movk ip0, 1739 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_copy
  .p2align 4
  .type g_variant_iter_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_copy
#endif
g_variant_iter_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13920
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13920]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1740 & 0xffff
#if 1740 > 0xffff
  movk ip0, 1740 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_free
  .p2align 4
  .type g_variant_iter_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_free
#endif
g_variant_iter_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13928
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13928]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1741 & 0xffff
#if 1741 > 0xffff
  movk ip0, 1741 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_init
  .p2align 4
  .type g_variant_iter_init, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_init
#endif
g_variant_iter_init:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13936
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13936]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1742 & 0xffff
#if 1742 > 0xffff
  movk ip0, 1742 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_loop
  .p2align 4
  .type g_variant_iter_loop, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_loop
#endif
g_variant_iter_loop:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13944
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13944]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1743 & 0xffff
#if 1743 > 0xffff
  movk ip0, 1743 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_n_children
  .p2align 4
  .type g_variant_iter_n_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_n_children
#endif
g_variant_iter_n_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13952
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13952]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1744 & 0xffff
#if 1744 > 0xffff
  movk ip0, 1744 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_new
  .p2align 4
  .type g_variant_iter_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_new
#endif
g_variant_iter_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13960
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13960]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1745 & 0xffff
#if 1745 > 0xffff
  movk ip0, 1745 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_next
  .p2align 4
  .type g_variant_iter_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_next
#endif
g_variant_iter_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13968
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13968]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1746 & 0xffff
#if 1746 > 0xffff
  movk ip0, 1746 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_iter_next_value
  .p2align 4
  .type g_variant_iter_next_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_iter_next_value
#endif
g_variant_iter_next_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13976
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13976]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1747 & 0xffff
#if 1747 > 0xffff
  movk ip0, 1747 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_lookup
  .p2align 4
  .type g_variant_lookup, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_lookup
#endif
g_variant_lookup:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13984
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13984]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1748 & 0xffff
#if 1748 > 0xffff
  movk ip0, 1748 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_lookup_value
  .p2align 4
  .type g_variant_lookup_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_lookup_value
#endif
g_variant_lookup_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+13992
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+13992]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1749 & 0xffff
#if 1749 > 0xffff
  movk ip0, 1749 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_n_children
  .p2align 4
  .type g_variant_n_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_n_children
#endif
g_variant_n_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14000
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14000]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1750 & 0xffff
#if 1750 > 0xffff
  movk ip0, 1750 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new
  .p2align 4
  .type g_variant_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new
#endif
g_variant_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14008
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14008]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1751 & 0xffff
#if 1751 > 0xffff
  movk ip0, 1751 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_array
  .p2align 4
  .type g_variant_new_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_array
#endif
g_variant_new_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14016
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14016]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1752 & 0xffff
#if 1752 > 0xffff
  movk ip0, 1752 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_boolean
  .p2align 4
  .type g_variant_new_boolean, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_boolean
#endif
g_variant_new_boolean:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14024
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14024]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1753 & 0xffff
#if 1753 > 0xffff
  movk ip0, 1753 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_byte
  .p2align 4
  .type g_variant_new_byte, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_byte
#endif
g_variant_new_byte:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14032
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14032]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1754 & 0xffff
#if 1754 > 0xffff
  movk ip0, 1754 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_bytestring
  .p2align 4
  .type g_variant_new_bytestring, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_bytestring
#endif
g_variant_new_bytestring:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14040
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14040]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1755 & 0xffff
#if 1755 > 0xffff
  movk ip0, 1755 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_bytestring_array
  .p2align 4
  .type g_variant_new_bytestring_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_bytestring_array
#endif
g_variant_new_bytestring_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14048
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14048]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1756 & 0xffff
#if 1756 > 0xffff
  movk ip0, 1756 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_dict_entry
  .p2align 4
  .type g_variant_new_dict_entry, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_dict_entry
#endif
g_variant_new_dict_entry:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14056
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14056]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1757 & 0xffff
#if 1757 > 0xffff
  movk ip0, 1757 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_double
  .p2align 4
  .type g_variant_new_double, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_double
#endif
g_variant_new_double:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14064
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14064]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1758 & 0xffff
#if 1758 > 0xffff
  movk ip0, 1758 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_fixed_array
  .p2align 4
  .type g_variant_new_fixed_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_fixed_array
#endif
g_variant_new_fixed_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14072
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14072]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1759 & 0xffff
#if 1759 > 0xffff
  movk ip0, 1759 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_from_bytes
  .p2align 4
  .type g_variant_new_from_bytes, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_from_bytes
#endif
g_variant_new_from_bytes:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14080
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14080]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1760 & 0xffff
#if 1760 > 0xffff
  movk ip0, 1760 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_from_data
  .p2align 4
  .type g_variant_new_from_data, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_from_data
#endif
g_variant_new_from_data:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14088
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14088]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1761 & 0xffff
#if 1761 > 0xffff
  movk ip0, 1761 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_handle
  .p2align 4
  .type g_variant_new_handle, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_handle
#endif
g_variant_new_handle:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14096
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14096]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1762 & 0xffff
#if 1762 > 0xffff
  movk ip0, 1762 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_int16
  .p2align 4
  .type g_variant_new_int16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_int16
#endif
g_variant_new_int16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14104
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14104]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1763 & 0xffff
#if 1763 > 0xffff
  movk ip0, 1763 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_int32
  .p2align 4
  .type g_variant_new_int32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_int32
#endif
g_variant_new_int32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14112
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14112]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1764 & 0xffff
#if 1764 > 0xffff
  movk ip0, 1764 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_int64
  .p2align 4
  .type g_variant_new_int64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_int64
#endif
g_variant_new_int64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14120
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14120]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1765 & 0xffff
#if 1765 > 0xffff
  movk ip0, 1765 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_maybe
  .p2align 4
  .type g_variant_new_maybe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_maybe
#endif
g_variant_new_maybe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14128
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14128]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1766 & 0xffff
#if 1766 > 0xffff
  movk ip0, 1766 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_object_path
  .p2align 4
  .type g_variant_new_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_object_path
#endif
g_variant_new_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14136
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14136]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1767 & 0xffff
#if 1767 > 0xffff
  movk ip0, 1767 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_objv
  .p2align 4
  .type g_variant_new_objv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_objv
#endif
g_variant_new_objv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14144
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14144]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1768 & 0xffff
#if 1768 > 0xffff
  movk ip0, 1768 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_parsed
  .p2align 4
  .type g_variant_new_parsed, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_parsed
#endif
g_variant_new_parsed:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14152
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14152]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1769 & 0xffff
#if 1769 > 0xffff
  movk ip0, 1769 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_parsed_va
  .p2align 4
  .type g_variant_new_parsed_va, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_parsed_va
#endif
g_variant_new_parsed_va:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14160
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14160]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1770 & 0xffff
#if 1770 > 0xffff
  movk ip0, 1770 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_printf
  .p2align 4
  .type g_variant_new_printf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_printf
#endif
g_variant_new_printf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14168
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14168]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1771 & 0xffff
#if 1771 > 0xffff
  movk ip0, 1771 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_signature
  .p2align 4
  .type g_variant_new_signature, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_signature
#endif
g_variant_new_signature:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14176
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14176]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1772 & 0xffff
#if 1772 > 0xffff
  movk ip0, 1772 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_string
  .p2align 4
  .type g_variant_new_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_string
#endif
g_variant_new_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14184
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14184]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1773 & 0xffff
#if 1773 > 0xffff
  movk ip0, 1773 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_strv
  .p2align 4
  .type g_variant_new_strv, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_strv
#endif
g_variant_new_strv:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14192
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14192]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1774 & 0xffff
#if 1774 > 0xffff
  movk ip0, 1774 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_take_string
  .p2align 4
  .type g_variant_new_take_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_take_string
#endif
g_variant_new_take_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14200
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14200]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1775 & 0xffff
#if 1775 > 0xffff
  movk ip0, 1775 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_tuple
  .p2align 4
  .type g_variant_new_tuple, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_tuple
#endif
g_variant_new_tuple:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14208
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14208]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1776 & 0xffff
#if 1776 > 0xffff
  movk ip0, 1776 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_uint16
  .p2align 4
  .type g_variant_new_uint16, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_uint16
#endif
g_variant_new_uint16:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14216
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14216]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1777 & 0xffff
#if 1777 > 0xffff
  movk ip0, 1777 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_uint32
  .p2align 4
  .type g_variant_new_uint32, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_uint32
#endif
g_variant_new_uint32:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14224
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14224]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1778 & 0xffff
#if 1778 > 0xffff
  movk ip0, 1778 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_uint64
  .p2align 4
  .type g_variant_new_uint64, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_uint64
#endif
g_variant_new_uint64:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14232
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14232]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1779 & 0xffff
#if 1779 > 0xffff
  movk ip0, 1779 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_va
  .p2align 4
  .type g_variant_new_va, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_va
#endif
g_variant_new_va:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14240
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14240]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1780 & 0xffff
#if 1780 > 0xffff
  movk ip0, 1780 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_new_variant
  .p2align 4
  .type g_variant_new_variant, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_new_variant
#endif
g_variant_new_variant:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14248
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14248]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1781 & 0xffff
#if 1781 > 0xffff
  movk ip0, 1781 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_parse
  .p2align 4
  .type g_variant_parse, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_parse
#endif
g_variant_parse:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14256
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14256]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1782 & 0xffff
#if 1782 > 0xffff
  movk ip0, 1782 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_parse_error_print_context
  .p2align 4
  .type g_variant_parse_error_print_context, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_parse_error_print_context
#endif
g_variant_parse_error_print_context:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14264
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14264]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1783 & 0xffff
#if 1783 > 0xffff
  movk ip0, 1783 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_parse_error_quark
  .p2align 4
  .type g_variant_parse_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_parse_error_quark
#endif
g_variant_parse_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14272
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14272]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1784 & 0xffff
#if 1784 > 0xffff
  movk ip0, 1784 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_parser_get_error_quark
  .p2align 4
  .type g_variant_parser_get_error_quark, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_parser_get_error_quark
#endif
g_variant_parser_get_error_quark:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14280
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14280]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1785 & 0xffff
#if 1785 > 0xffff
  movk ip0, 1785 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_print
  .p2align 4
  .type g_variant_print, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_print
#endif
g_variant_print:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14288
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14288]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1786 & 0xffff
#if 1786 > 0xffff
  movk ip0, 1786 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_print_string
  .p2align 4
  .type g_variant_print_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_print_string
#endif
g_variant_print_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14296
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14296]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1787 & 0xffff
#if 1787 > 0xffff
  movk ip0, 1787 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_ref
  .p2align 4
  .type g_variant_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_ref
#endif
g_variant_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14304
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14304]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1788 & 0xffff
#if 1788 > 0xffff
  movk ip0, 1788 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_ref_sink
  .p2align 4
  .type g_variant_ref_sink, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_ref_sink
#endif
g_variant_ref_sink:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14312
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14312]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1789 & 0xffff
#if 1789 > 0xffff
  movk ip0, 1789 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialised_byteswap
  .p2align 4
  .type g_variant_serialised_byteswap, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialised_byteswap
#endif
g_variant_serialised_byteswap:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14320
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14320]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1790 & 0xffff
#if 1790 > 0xffff
  movk ip0, 1790 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialised_check
  .p2align 4
  .type g_variant_serialised_check, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialised_check
#endif
g_variant_serialised_check:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14328
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14328]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1791 & 0xffff
#if 1791 > 0xffff
  movk ip0, 1791 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialised_get_child
  .p2align 4
  .type g_variant_serialised_get_child, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialised_get_child
#endif
g_variant_serialised_get_child:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14336
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14336]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1792 & 0xffff
#if 1792 > 0xffff
  movk ip0, 1792 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialised_is_normal
  .p2align 4
  .type g_variant_serialised_is_normal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialised_is_normal
#endif
g_variant_serialised_is_normal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14344
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14344]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1793 & 0xffff
#if 1793 > 0xffff
  movk ip0, 1793 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialised_n_children
  .p2align 4
  .type g_variant_serialised_n_children, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialised_n_children
#endif
g_variant_serialised_n_children:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14352
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14352]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1794 & 0xffff
#if 1794 > 0xffff
  movk ip0, 1794 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialiser_is_object_path
  .p2align 4
  .type g_variant_serialiser_is_object_path, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialiser_is_object_path
#endif
g_variant_serialiser_is_object_path:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14360
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14360]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1795 & 0xffff
#if 1795 > 0xffff
  movk ip0, 1795 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialiser_is_signature
  .p2align 4
  .type g_variant_serialiser_is_signature, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialiser_is_signature
#endif
g_variant_serialiser_is_signature:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14368
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14368]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1796 & 0xffff
#if 1796 > 0xffff
  movk ip0, 1796 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialiser_is_string
  .p2align 4
  .type g_variant_serialiser_is_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialiser_is_string
#endif
g_variant_serialiser_is_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14376
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14376]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1797 & 0xffff
#if 1797 > 0xffff
  movk ip0, 1797 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialiser_needed_size
  .p2align 4
  .type g_variant_serialiser_needed_size, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialiser_needed_size
#endif
g_variant_serialiser_needed_size:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14384
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14384]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1798 & 0xffff
#if 1798 > 0xffff
  movk ip0, 1798 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_serialiser_serialise
  .p2align 4
  .type g_variant_serialiser_serialise, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_serialiser_serialise
#endif
g_variant_serialiser_serialise:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14392
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14392]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1799 & 0xffff
#if 1799 > 0xffff
  movk ip0, 1799 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_store
  .p2align 4
  .type g_variant_store, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_store
#endif
g_variant_store:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14400
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14400]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1800 & 0xffff
#if 1800 > 0xffff
  movk ip0, 1800 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_take_ref
  .p2align 4
  .type g_variant_take_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_take_ref
#endif
g_variant_take_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14408
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14408]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1801 & 0xffff
#if 1801 > 0xffff
  movk ip0, 1801 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_checked_
  .p2align 4
  .type g_variant_type_checked_, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_checked_
#endif
g_variant_type_checked_:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14416
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14416]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1802 & 0xffff
#if 1802 > 0xffff
  movk ip0, 1802 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_copy
  .p2align 4
  .type g_variant_type_copy, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_copy
#endif
g_variant_type_copy:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14424
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14424]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1803 & 0xffff
#if 1803 > 0xffff
  movk ip0, 1803 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_dup_string
  .p2align 4
  .type g_variant_type_dup_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_dup_string
#endif
g_variant_type_dup_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14432
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14432]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1804 & 0xffff
#if 1804 > 0xffff
  movk ip0, 1804 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_element
  .p2align 4
  .type g_variant_type_element, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_element
#endif
g_variant_type_element:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14440
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14440]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1805 & 0xffff
#if 1805 > 0xffff
  movk ip0, 1805 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_equal
  .p2align 4
  .type g_variant_type_equal, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_equal
#endif
g_variant_type_equal:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14448
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14448]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1806 & 0xffff
#if 1806 > 0xffff
  movk ip0, 1806 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_first
  .p2align 4
  .type g_variant_type_first, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_first
#endif
g_variant_type_first:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14456
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14456]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1807 & 0xffff
#if 1807 > 0xffff
  movk ip0, 1807 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_free
  .p2align 4
  .type g_variant_type_free, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_free
#endif
g_variant_type_free:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14464
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14464]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1808 & 0xffff
#if 1808 > 0xffff
  movk ip0, 1808 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_get_string_length
  .p2align 4
  .type g_variant_type_get_string_length, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_get_string_length
#endif
g_variant_type_get_string_length:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14472
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14472]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1809 & 0xffff
#if 1809 > 0xffff
  movk ip0, 1809 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_hash
  .p2align 4
  .type g_variant_type_hash, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_hash
#endif
g_variant_type_hash:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14480
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14480]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1810 & 0xffff
#if 1810 > 0xffff
  movk ip0, 1810 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_assert_no_infos
  .p2align 4
  .type g_variant_type_info_assert_no_infos, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_assert_no_infos
#endif
g_variant_type_info_assert_no_infos:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14488
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14488]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1811 & 0xffff
#if 1811 > 0xffff
  movk ip0, 1811 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_element
  .p2align 4
  .type g_variant_type_info_element, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_element
#endif
g_variant_type_info_element:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14496
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14496]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1812 & 0xffff
#if 1812 > 0xffff
  movk ip0, 1812 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_get
  .p2align 4
  .type g_variant_type_info_get, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_get
#endif
g_variant_type_info_get:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14504
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14504]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1813 & 0xffff
#if 1813 > 0xffff
  movk ip0, 1813 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_get_type_string
  .p2align 4
  .type g_variant_type_info_get_type_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_get_type_string
#endif
g_variant_type_info_get_type_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14512
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14512]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1814 & 0xffff
#if 1814 > 0xffff
  movk ip0, 1814 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_member_info
  .p2align 4
  .type g_variant_type_info_member_info, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_member_info
#endif
g_variant_type_info_member_info:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14520
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14520]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1815 & 0xffff
#if 1815 > 0xffff
  movk ip0, 1815 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_n_members
  .p2align 4
  .type g_variant_type_info_n_members, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_n_members
#endif
g_variant_type_info_n_members:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14528
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14528]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1816 & 0xffff
#if 1816 > 0xffff
  movk ip0, 1816 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_query
  .p2align 4
  .type g_variant_type_info_query, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_query
#endif
g_variant_type_info_query:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14536
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14536]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1817 & 0xffff
#if 1817 > 0xffff
  movk ip0, 1817 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_query_depth
  .p2align 4
  .type g_variant_type_info_query_depth, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_query_depth
#endif
g_variant_type_info_query_depth:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14544
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14544]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1818 & 0xffff
#if 1818 > 0xffff
  movk ip0, 1818 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_query_element
  .p2align 4
  .type g_variant_type_info_query_element, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_query_element
#endif
g_variant_type_info_query_element:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14552
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14552]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1819 & 0xffff
#if 1819 > 0xffff
  movk ip0, 1819 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_ref
  .p2align 4
  .type g_variant_type_info_ref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_ref
#endif
g_variant_type_info_ref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14560
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14560]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1820 & 0xffff
#if 1820 > 0xffff
  movk ip0, 1820 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_info_unref
  .p2align 4
  .type g_variant_type_info_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_info_unref
#endif
g_variant_type_info_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14568
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14568]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1821 & 0xffff
#if 1821 > 0xffff
  movk ip0, 1821 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_array
  .p2align 4
  .type g_variant_type_is_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_array
#endif
g_variant_type_is_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14576
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14576]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1822 & 0xffff
#if 1822 > 0xffff
  movk ip0, 1822 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_basic
  .p2align 4
  .type g_variant_type_is_basic, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_basic
#endif
g_variant_type_is_basic:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14584
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14584]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1823 & 0xffff
#if 1823 > 0xffff
  movk ip0, 1823 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_container
  .p2align 4
  .type g_variant_type_is_container, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_container
#endif
g_variant_type_is_container:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14592
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14592]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1824 & 0xffff
#if 1824 > 0xffff
  movk ip0, 1824 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_definite
  .p2align 4
  .type g_variant_type_is_definite, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_definite
#endif
g_variant_type_is_definite:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14600
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14600]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1825 & 0xffff
#if 1825 > 0xffff
  movk ip0, 1825 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_dict_entry
  .p2align 4
  .type g_variant_type_is_dict_entry, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_dict_entry
#endif
g_variant_type_is_dict_entry:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14608
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14608]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1826 & 0xffff
#if 1826 > 0xffff
  movk ip0, 1826 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_maybe
  .p2align 4
  .type g_variant_type_is_maybe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_maybe
#endif
g_variant_type_is_maybe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14616
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14616]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1827 & 0xffff
#if 1827 > 0xffff
  movk ip0, 1827 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_subtype_of
  .p2align 4
  .type g_variant_type_is_subtype_of, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_subtype_of
#endif
g_variant_type_is_subtype_of:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14624
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14624]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1828 & 0xffff
#if 1828 > 0xffff
  movk ip0, 1828 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_tuple
  .p2align 4
  .type g_variant_type_is_tuple, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_tuple
#endif
g_variant_type_is_tuple:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14632
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14632]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1829 & 0xffff
#if 1829 > 0xffff
  movk ip0, 1829 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_is_variant
  .p2align 4
  .type g_variant_type_is_variant, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_is_variant
#endif
g_variant_type_is_variant:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14640
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14640]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1830 & 0xffff
#if 1830 > 0xffff
  movk ip0, 1830 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_key
  .p2align 4
  .type g_variant_type_key, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_key
#endif
g_variant_type_key:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14648
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14648]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1831 & 0xffff
#if 1831 > 0xffff
  movk ip0, 1831 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_n_items
  .p2align 4
  .type g_variant_type_n_items, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_n_items
#endif
g_variant_type_n_items:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14656
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14656]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1832 & 0xffff
#if 1832 > 0xffff
  movk ip0, 1832 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_new
  .p2align 4
  .type g_variant_type_new, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_new
#endif
g_variant_type_new:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14664
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14664]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1833 & 0xffff
#if 1833 > 0xffff
  movk ip0, 1833 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_new_array
  .p2align 4
  .type g_variant_type_new_array, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_new_array
#endif
g_variant_type_new_array:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14672
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14672]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1834 & 0xffff
#if 1834 > 0xffff
  movk ip0, 1834 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_new_dict_entry
  .p2align 4
  .type g_variant_type_new_dict_entry, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_new_dict_entry
#endif
g_variant_type_new_dict_entry:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14680
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14680]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1835 & 0xffff
#if 1835 > 0xffff
  movk ip0, 1835 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_new_maybe
  .p2align 4
  .type g_variant_type_new_maybe, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_new_maybe
#endif
g_variant_type_new_maybe:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14688
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14688]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1836 & 0xffff
#if 1836 > 0xffff
  movk ip0, 1836 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_new_tuple
  .p2align 4
  .type g_variant_type_new_tuple, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_new_tuple
#endif
g_variant_type_new_tuple:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14696
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14696]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1837 & 0xffff
#if 1837 > 0xffff
  movk ip0, 1837 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_next
  .p2align 4
  .type g_variant_type_next, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_next
#endif
g_variant_type_next:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14704
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14704]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1838 & 0xffff
#if 1838 > 0xffff
  movk ip0, 1838 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_peek_string
  .p2align 4
  .type g_variant_type_peek_string, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_peek_string
#endif
g_variant_type_peek_string:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14712
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14712]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1839 & 0xffff
#if 1839 > 0xffff
  movk ip0, 1839 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_string_get_depth_
  .p2align 4
  .type g_variant_type_string_get_depth_, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_string_get_depth_
#endif
g_variant_type_string_get_depth_:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14720
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14720]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1840 & 0xffff
#if 1840 > 0xffff
  movk ip0, 1840 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_string_is_valid
  .p2align 4
  .type g_variant_type_string_is_valid, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_string_is_valid
#endif
g_variant_type_string_is_valid:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14728
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14728]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1841 & 0xffff
#if 1841 > 0xffff
  movk ip0, 1841 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_string_scan
  .p2align 4
  .type g_variant_type_string_scan, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_string_scan
#endif
g_variant_type_string_scan:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14736
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14736]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1842 & 0xffff
#if 1842 > 0xffff
  movk ip0, 1842 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_type_value
  .p2align 4
  .type g_variant_type_value, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_type_value
#endif
g_variant_type_value:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14744
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14744]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1843 & 0xffff
#if 1843 > 0xffff
  movk ip0, 1843 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_variant_unref
  .p2align 4
  .type g_variant_unref, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_variant_unref
#endif
g_variant_unref:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14752
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14752]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1844 & 0xffff
#if 1844 > 0xffff
  movk ip0, 1844 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vasprintf
  .p2align 4
  .type g_vasprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vasprintf
#endif
g_vasprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14760
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14760]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1845 & 0xffff
#if 1845 > 0xffff
  movk ip0, 1845 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vfprintf
  .p2align 4
  .type g_vfprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vfprintf
#endif
g_vfprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14768
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14768]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1846 & 0xffff
#if 1846 > 0xffff
  movk ip0, 1846 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vprintf
  .p2align 4
  .type g_vprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vprintf
#endif
g_vprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14776
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14776]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1847 & 0xffff
#if 1847 > 0xffff
  movk ip0, 1847 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vsnprintf
  .p2align 4
  .type g_vsnprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vsnprintf
#endif
g_vsnprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14784
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14784]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1848 & 0xffff
#if 1848 > 0xffff
  movk ip0, 1848 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_vsprintf
  .p2align 4
  .type g_vsprintf, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_vsprintf
#endif
g_vsprintf:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14792
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14792]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1849 & 0xffff
#if 1849 > 0xffff
  movk ip0, 1849 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl g_warn_message
  .p2align 4
  .type g_warn_message, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden g_warn_message
#endif
g_warn_message:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14800
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14800]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1850 & 0xffff
#if 1850 > 0xffff
  movk ip0, 1850 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl glib__private__
  .p2align 4
  .type glib__private__, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden glib__private__
#endif
glib__private__:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14808
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14808]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1851 & 0xffff
#if 1851 > 0xffff
  movk ip0, 1851 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl glib_check_version
  .p2align 4
  .type glib_check_version, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden glib_check_version
#endif
glib_check_version:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14816
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14816]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1852 & 0xffff
#if 1852 > 0xffff
  movk ip0, 1852 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl glib_gettext
  .p2align 4
  .type glib_gettext, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden glib_gettext
#endif
glib_gettext:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14824
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14824]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1853 & 0xffff
#if 1853 > 0xffff
  movk ip0, 1853 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

/*
 * Copyright 2018-2025 Yury Gribov
 *
 * The MIT License (MIT)
 *
 * Use of this source code is governed by MIT license that can be
 * found in the LICENSE.txt file.
 */

  .globl glib_pgettext
  .p2align 4
  .type glib_pgettext, %function
#ifndef IMPLIB_EXPORT_SHIMS
  .hidden glib_pgettext
#endif
glib_pgettext:
  .cfi_startproc

1:
  // Load address
  // TODO: can we do this faster on newer ARMs?
  adrp ip0, _libglib_2_0_so_tramp_table+14832
  ldr ip0, [ip0, #:lo12:_libglib_2_0_so_tramp_table+14832]
 
  cbz ip0, 2f

  // Fast path
  br ip0

2:
  // Slow path
  mov ip0, 1854 & 0xffff
#if 1854 > 0xffff
  movk ip0, 1854 >> 16, lsl #16
#endif
  stp ip0, lr, [sp, #-16]!; .cfi_adjust_cfa_offset 16; .cfi_rel_offset lr, 8
  bl _libglib_2_0_so_save_regs_and_resolve
  ldp xzr, lr, [sp], #16; .cfi_adjust_cfa_offset -16; .cfi_restore lr
  br ip0
  .cfi_endproc

